# Config for authorization net announcer
# First server must by self with the key to authorize clients
# Other servers url and keys use to broadcasts messages

# byond://127.0.0.1:30000 = SomeSuperSecretKey	# self
# byond://127.0.0.2:30000 = OtherSecretKey
# byond://127.0.0.1:30000 = SomeSuperSecretKey	# if you need to broadcast self, just reapet it