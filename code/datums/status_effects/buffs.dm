/datum/status_effect/wound_clotting
	id = "wound_clotting"
	duration = 20 // this should be one life tick so it remains active between metatrombine ticks but does not linger
	tick_interval = 0
	status_type = STATUS_EFFECT_REPLACE
	alert_type = null
