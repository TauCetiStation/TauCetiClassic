//can_move = 0 - user can't move when do_after()
//can_move < 0 - do_after() no distance to target after beginning, user can move.
//can_move > 0 - do_after() with can move user when he is nearby
#define CAN_MOVE_NO_DISTANCE   -1
#define CANT_MOVE               0
#define CAN_MOVE_NEARBY         1
