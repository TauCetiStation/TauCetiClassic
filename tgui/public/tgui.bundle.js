/******/ (function() { // webpackBootstrap
/******/ 	var __webpack_modules__ = ({

/***/ 92179:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.popperGenerator = popperGenerator;
exports.createPopper = void 0;

var _getCompositeRect = _interopRequireDefault(__webpack_require__(30505));

var _getLayoutRect = _interopRequireDefault(__webpack_require__(43545));

var _listScrollParents = _interopRequireDefault(__webpack_require__(46213));

var _getOffsetParent = _interopRequireDefault(__webpack_require__(32440));

var _getComputedStyle2 = _interopRequireDefault(__webpack_require__(61131));

var _orderModifiers = _interopRequireDefault(__webpack_require__(45700));

var _debounce = _interopRequireDefault(__webpack_require__(68369));

var _validateModifiers = _interopRequireDefault(__webpack_require__(38574));

var _uniqueBy = _interopRequireDefault(__webpack_require__(25435));

var _getBasePlacement = _interopRequireDefault(__webpack_require__(22709));

var _mergeByName = _interopRequireDefault(__webpack_require__(36284));

var _detectOverflow = _interopRequireDefault(__webpack_require__(19574));

exports.detectOverflow = _detectOverflow["default"];

var _instanceOf = __webpack_require__(67458);

var _enums = __webpack_require__(56728);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

var INVALID_ELEMENT_ERROR = 'Popper: Invalid reference or popper argument provided. They must be either a DOM element or virtual element.';
var INFINITE_LOOP_ERROR = 'Popper: An infinite loop in the modifiers cycle has been detected! The cycle has been interrupted to prevent a browser crash.';
var DEFAULT_OPTIONS = {
  placement: 'bottom',
  modifiers: [],
  strategy: 'absolute'
};

function areValidElements() {
  for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
    args[_key] = arguments[_key];
  }

  return !args.some(function (element) {
    return !(element && typeof element.getBoundingClientRect === 'function');
  });
}

function popperGenerator(generatorOptions) {
  if (generatorOptions === void 0) {
    generatorOptions = {};
  }

  var _generatorOptions = generatorOptions,
      _generatorOptions$def = _generatorOptions.defaultModifiers,
      defaultModifiers = _generatorOptions$def === void 0 ? [] : _generatorOptions$def,
      _generatorOptions$def2 = _generatorOptions.defaultOptions,
      defaultOptions = _generatorOptions$def2 === void 0 ? DEFAULT_OPTIONS : _generatorOptions$def2;
  return function () {
    function createPopper(reference, popper, options) {
      if (options === void 0) {
        options = defaultOptions;
      }

      var state = {
        placement: 'bottom',
        orderedModifiers: [],
        options: Object.assign({}, DEFAULT_OPTIONS, defaultOptions),
        modifiersData: {},
        elements: {
          reference: reference,
          popper: popper
        },
        attributes: {},
        styles: {}
      };
      var effectCleanupFns = [];
      var isDestroyed = false;
      var instance = {
        state: state,
        setOptions: function () {
          function setOptions(options) {
            cleanupModifierEffects();
            state.options = Object.assign({}, defaultOptions, state.options, options);
            state.scrollParents = {
              reference: (0, _instanceOf.isElement)(reference) ? (0, _listScrollParents["default"])(reference) : reference.contextElement ? (0, _listScrollParents["default"])(reference.contextElement) : [],
              popper: (0, _listScrollParents["default"])(popper)
            }; // Orders the modifiers based on their dependencies and `phase`
            // properties

            var orderedModifiers = (0, _orderModifiers["default"])((0, _mergeByName["default"])([].concat(defaultModifiers, state.options.modifiers))); // Strip out disabled modifiers

            state.orderedModifiers = orderedModifiers.filter(function (m) {
              return m.enabled;
            }); // Validate the provided modifiers so that the consumer will get warned
            // if one of the modifiers is invalid for any reason

            if (false) { var _getComputedStyle, marginTop, marginRight, marginBottom, marginLeft, flipModifier, modifiers; }

            runModifierEffects();
            return instance.update();
          }

          return setOptions;
        }(),
        // Sync update – it will always be executed, even if not necessary. This
        // is useful for low frequency updates where sync behavior simplifies the
        // logic.
        // For high frequency updates (e.g. `resize` and `scroll` events), always
        // prefer the async Popper#update method
        forceUpdate: function () {
          function forceUpdate() {
            if (isDestroyed) {
              return;
            }

            var _state$elements = state.elements,
                reference = _state$elements.reference,
                popper = _state$elements.popper; // Don't proceed if `reference` or `popper` are not valid elements
            // anymore

            if (!areValidElements(reference, popper)) {
              if (false) {}

              return;
            } // Store the reference and popper rects to be read by modifiers


            state.rects = {
              reference: (0, _getCompositeRect["default"])(reference, (0, _getOffsetParent["default"])(popper), state.options.strategy === 'fixed'),
              popper: (0, _getLayoutRect["default"])(popper)
            }; // Modifiers have the ability to reset the current update cycle. The
            // most common use case for this is the `flip` modifier changing the
            // placement, which then needs to re-run all the modifiers, because the
            // logic was previously ran for the previous placement and is therefore
            // stale/incorrect

            state.reset = false;
            state.placement = state.options.placement; // On each update cycle, the `modifiersData` property for each modifier
            // is filled with the initial data specified by the modifier. This means
            // it doesn't persist and is fresh on each update.
            // To ensure persistent data, use `${name}#persistent`

            state.orderedModifiers.forEach(function (modifier) {
              return state.modifiersData[modifier.name] = Object.assign({}, modifier.data);
            });
            var __debug_loops__ = 0;

            for (var index = 0; index < state.orderedModifiers.length; index++) {
              if (false) {}

              if (state.reset === true) {
                state.reset = false;
                index = -1;
                continue;
              }

              var _state$orderedModifie = state.orderedModifiers[index],
                  fn = _state$orderedModifie.fn,
                  _state$orderedModifie2 = _state$orderedModifie.options,
                  _options = _state$orderedModifie2 === void 0 ? {} : _state$orderedModifie2,
                  name = _state$orderedModifie.name;

              if (typeof fn === 'function') {
                state = fn({
                  state: state,
                  options: _options,
                  name: name,
                  instance: instance
                }) || state;
              }
            }
          }

          return forceUpdate;
        }(),
        // Async and optimistically optimized update – it will not be executed if
        // not necessary (debounced to run at most once-per-tick)
        update: (0, _debounce["default"])(function () {
          return new Promise(function (resolve) {
            instance.forceUpdate();
            resolve(state);
          });
        }),
        destroy: function () {
          function destroy() {
            cleanupModifierEffects();
            isDestroyed = true;
          }

          return destroy;
        }()
      };

      if (!areValidElements(reference, popper)) {
        if (false) {}

        return instance;
      }

      instance.setOptions(options).then(function (state) {
        if (!isDestroyed && options.onFirstUpdate) {
          options.onFirstUpdate(state);
        }
      }); // Modifiers have the ability to execute arbitrary code before the first
      // update cycle runs. They will be executed in the same order as the update
      // cycle. This is useful when a modifier adds some persistent data that
      // other modifiers need to use, but the modifier is run after the dependent
      // one.

      function runModifierEffects() {
        state.orderedModifiers.forEach(function (_ref3) {
          var name = _ref3.name,
              _ref3$options = _ref3.options,
              options = _ref3$options === void 0 ? {} : _ref3$options,
              effect = _ref3.effect;

          if (typeof effect === 'function') {
            var cleanupFn = effect({
              state: state,
              name: name,
              instance: instance,
              options: options
            });

            var noopFn = function () {
              function noopFn() {}

              return noopFn;
            }();

            effectCleanupFns.push(cleanupFn || noopFn);
          }
        });
      }

      function cleanupModifierEffects() {
        effectCleanupFns.forEach(function (fn) {
          return fn();
        });
        effectCleanupFns = [];
      }

      return instance;
    }

    return createPopper;
  }();
}

var createPopper = /*#__PURE__*/popperGenerator(); // eslint-disable-next-line import/no-unused-modules

exports.createPopper = createPopper;

/***/ }),

/***/ 97607:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.default = contains;

var _instanceOf = __webpack_require__(67458);

function contains(parent, child) {
  var rootNode = child.getRootNode && child.getRootNode(); // First, attempt with faster native method

  if (parent.contains(child)) {
    return true;
  } // then fallback to custom implementation with Shadow DOM support
  else if (rootNode && (0, _instanceOf.isShadowRoot)(rootNode)) {
    var next = child;

    do {
      if (next && parent.isSameNode(next)) {
        return true;
      } // $FlowFixMe[prop-missing]: need a better way to handle this...


      next = next.parentNode || next.host;
    } while (next);
  } // Give up, the result is false


  return false;
}

/***/ }),

/***/ 94294:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.default = getBoundingClientRect;

var _instanceOf = __webpack_require__(67458);

var round = Math.round;

function getBoundingClientRect(element, includeScale) {
  if (includeScale === void 0) {
    includeScale = false;
  }

  var rect = element.getBoundingClientRect();
  var scaleX = 1;
  var scaleY = 1;

  if ((0, _instanceOf.isHTMLElement)(element) && includeScale) {
    // Fallback to 1 in case both values are `0`
    scaleX = rect.width / element.offsetWidth || 1;
    scaleY = rect.height / element.offsetHeight || 1;
  }

  return {
    width: round(rect.width / scaleX),
    height: round(rect.height / scaleY),
    top: round(rect.top / scaleY),
    right: round(rect.right / scaleX),
    bottom: round(rect.bottom / scaleY),
    left: round(rect.left / scaleX),
    x: round(rect.left / scaleX),
    y: round(rect.top / scaleY)
  };
}

/***/ }),

/***/ 48162:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.default = getClippingRect;

var _enums = __webpack_require__(56728);

var _getViewportRect = _interopRequireDefault(__webpack_require__(78331));

var _getDocumentRect = _interopRequireDefault(__webpack_require__(68692));

var _listScrollParents = _interopRequireDefault(__webpack_require__(46213));

var _getOffsetParent = _interopRequireDefault(__webpack_require__(32440));

var _getDocumentElement = _interopRequireDefault(__webpack_require__(58386));

var _getComputedStyle = _interopRequireDefault(__webpack_require__(61131));

var _instanceOf = __webpack_require__(67458);

var _getBoundingClientRect = _interopRequireDefault(__webpack_require__(94294));

var _getParentNode = _interopRequireDefault(__webpack_require__(31613));

var _contains = _interopRequireDefault(__webpack_require__(97607));

var _getNodeName = _interopRequireDefault(__webpack_require__(99624));

var _rectToClientRect = _interopRequireDefault(__webpack_require__(20935));

var _math = __webpack_require__(62836);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

function getInnerBoundingClientRect(element) {
  var rect = (0, _getBoundingClientRect["default"])(element);
  rect.top = rect.top + element.clientTop;
  rect.left = rect.left + element.clientLeft;
  rect.bottom = rect.top + element.clientHeight;
  rect.right = rect.left + element.clientWidth;
  rect.width = element.clientWidth;
  rect.height = element.clientHeight;
  rect.x = rect.left;
  rect.y = rect.top;
  return rect;
}

function getClientRectFromMixedType(element, clippingParent) {
  return clippingParent === _enums.viewport ? (0, _rectToClientRect["default"])((0, _getViewportRect["default"])(element)) : (0, _instanceOf.isHTMLElement)(clippingParent) ? getInnerBoundingClientRect(clippingParent) : (0, _rectToClientRect["default"])((0, _getDocumentRect["default"])((0, _getDocumentElement["default"])(element)));
} // A "clipping parent" is an overflowable container with the characteristic of
// clipping (or hiding) overflowing elements with a position different from
// `initial`


function getClippingParents(element) {
  var clippingParents = (0, _listScrollParents["default"])((0, _getParentNode["default"])(element));
  var canEscapeClipping = ['absolute', 'fixed'].indexOf((0, _getComputedStyle["default"])(element).position) >= 0;
  var clipperElement = canEscapeClipping && (0, _instanceOf.isHTMLElement)(element) ? (0, _getOffsetParent["default"])(element) : element;

  if (!(0, _instanceOf.isElement)(clipperElement)) {
    return [];
  } // $FlowFixMe[incompatible-return]: https://github.com/facebook/flow/issues/1414


  return clippingParents.filter(function (clippingParent) {
    return (0, _instanceOf.isElement)(clippingParent) && (0, _contains["default"])(clippingParent, clipperElement) && (0, _getNodeName["default"])(clippingParent) !== 'body';
  });
} // Gets the maximum area that the element is visible in due to any number of
// clipping parents


function getClippingRect(element, boundary, rootBoundary) {
  var mainClippingParents = boundary === 'clippingParents' ? getClippingParents(element) : [].concat(boundary);
  var clippingParents = [].concat(mainClippingParents, [rootBoundary]);
  var firstClippingParent = clippingParents[0];
  var clippingRect = clippingParents.reduce(function (accRect, clippingParent) {
    var rect = getClientRectFromMixedType(element, clippingParent);
    accRect.top = (0, _math.max)(rect.top, accRect.top);
    accRect.right = (0, _math.min)(rect.right, accRect.right);
    accRect.bottom = (0, _math.min)(rect.bottom, accRect.bottom);
    accRect.left = (0, _math.max)(rect.left, accRect.left);
    return accRect;
  }, getClientRectFromMixedType(element, firstClippingParent));
  clippingRect.width = clippingRect.right - clippingRect.left;
  clippingRect.height = clippingRect.bottom - clippingRect.top;
  clippingRect.x = clippingRect.left;
  clippingRect.y = clippingRect.top;
  return clippingRect;
}

/***/ }),

/***/ 30505:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.default = getCompositeRect;

var _getBoundingClientRect = _interopRequireDefault(__webpack_require__(94294));

var _getNodeScroll = _interopRequireDefault(__webpack_require__(71942));

var _getNodeName = _interopRequireDefault(__webpack_require__(99624));

var _instanceOf = __webpack_require__(67458);

var _getWindowScrollBarX = _interopRequireDefault(__webpack_require__(45471));

var _getDocumentElement = _interopRequireDefault(__webpack_require__(58386));

var _isScrollParent = _interopRequireDefault(__webpack_require__(37229));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

function isElementScaled(element) {
  var rect = element.getBoundingClientRect();
  var scaleX = rect.width / element.offsetWidth || 1;
  var scaleY = rect.height / element.offsetHeight || 1;
  return scaleX !== 1 || scaleY !== 1;
} // Returns the composite rect of an element relative to its offsetParent.
// Composite means it takes into account transforms as well as layout.


function getCompositeRect(elementOrVirtualElement, offsetParent, isFixed) {
  if (isFixed === void 0) {
    isFixed = false;
  }

  var isOffsetParentAnElement = (0, _instanceOf.isHTMLElement)(offsetParent);
  var offsetParentIsScaled = (0, _instanceOf.isHTMLElement)(offsetParent) && isElementScaled(offsetParent);
  var documentElement = (0, _getDocumentElement["default"])(offsetParent);
  var rect = (0, _getBoundingClientRect["default"])(elementOrVirtualElement, offsetParentIsScaled);
  var scroll = {
    scrollLeft: 0,
    scrollTop: 0
  };
  var offsets = {
    x: 0,
    y: 0
  };

  if (isOffsetParentAnElement || !isOffsetParentAnElement && !isFixed) {
    if ((0, _getNodeName["default"])(offsetParent) !== 'body' || // https://github.com/popperjs/popper-core/issues/1078
    (0, _isScrollParent["default"])(documentElement)) {
      scroll = (0, _getNodeScroll["default"])(offsetParent);
    }

    if ((0, _instanceOf.isHTMLElement)(offsetParent)) {
      offsets = (0, _getBoundingClientRect["default"])(offsetParent, true);
      offsets.x += offsetParent.clientLeft;
      offsets.y += offsetParent.clientTop;
    } else if (documentElement) {
      offsets.x = (0, _getWindowScrollBarX["default"])(documentElement);
    }
  }

  return {
    x: rect.left + scroll.scrollLeft - offsets.x,
    y: rect.top + scroll.scrollTop - offsets.y,
    width: rect.width,
    height: rect.height
  };
}

/***/ }),

/***/ 61131:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.default = getComputedStyle;

var _getWindow = _interopRequireDefault(__webpack_require__(31677));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

function getComputedStyle(element) {
  return (0, _getWindow["default"])(element).getComputedStyle(element);
}

/***/ }),

/***/ 58386:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.default = getDocumentElement;

var _instanceOf = __webpack_require__(67458);

function getDocumentElement(element) {
  // $FlowFixMe[incompatible-return]: assume body is always available
  return (((0, _instanceOf.isElement)(element) ? element.ownerDocument : // $FlowFixMe[prop-missing]
  element.document) || window.document).documentElement;
}

/***/ }),

/***/ 68692:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.default = getDocumentRect;

var _getDocumentElement = _interopRequireDefault(__webpack_require__(58386));

var _getComputedStyle = _interopRequireDefault(__webpack_require__(61131));

var _getWindowScrollBarX = _interopRequireDefault(__webpack_require__(45471));

var _getWindowScroll = _interopRequireDefault(__webpack_require__(1255));

var _math = __webpack_require__(62836);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

// Gets the entire size of the scrollable document area, even extending outside
// of the `<html>` and `<body>` rect bounds if horizontally scrollable
function getDocumentRect(element) {
  var _element$ownerDocumen;

  var html = (0, _getDocumentElement["default"])(element);
  var winScroll = (0, _getWindowScroll["default"])(element);
  var body = (_element$ownerDocumen = element.ownerDocument) == null ? void 0 : _element$ownerDocumen.body;
  var width = (0, _math.max)(html.scrollWidth, html.clientWidth, body ? body.scrollWidth : 0, body ? body.clientWidth : 0);
  var height = (0, _math.max)(html.scrollHeight, html.clientHeight, body ? body.scrollHeight : 0, body ? body.clientHeight : 0);
  var x = -winScroll.scrollLeft + (0, _getWindowScrollBarX["default"])(element);
  var y = -winScroll.scrollTop;

  if ((0, _getComputedStyle["default"])(body || html).direction === 'rtl') {
    x += (0, _math.max)(html.clientWidth, body ? body.clientWidth : 0) - width;
  }

  return {
    width: width,
    height: height,
    x: x,
    y: y
  };
}

/***/ }),

/***/ 50552:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.default = getHTMLElementScroll;

function getHTMLElementScroll(element) {
  return {
    scrollLeft: element.scrollLeft,
    scrollTop: element.scrollTop
  };
}

/***/ }),

/***/ 43545:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.default = getLayoutRect;

var _getBoundingClientRect = _interopRequireDefault(__webpack_require__(94294));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

// Returns the layout rect of an element relative to its offsetParent. Layout
// means it doesn't take into account transforms.
function getLayoutRect(element) {
  var clientRect = (0, _getBoundingClientRect["default"])(element); // Use the clientRect sizes if it's not been transformed.
  // Fixes https://github.com/popperjs/popper-core/issues/1223

  var width = element.offsetWidth;
  var height = element.offsetHeight;

  if (Math.abs(clientRect.width - width) <= 1) {
    width = clientRect.width;
  }

  if (Math.abs(clientRect.height - height) <= 1) {
    height = clientRect.height;
  }

  return {
    x: element.offsetLeft,
    y: element.offsetTop,
    width: width,
    height: height
  };
}

/***/ }),

/***/ 99624:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.default = getNodeName;

function getNodeName(element) {
  return element ? (element.nodeName || '').toLowerCase() : null;
}

/***/ }),

/***/ 71942:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.default = getNodeScroll;

var _getWindowScroll = _interopRequireDefault(__webpack_require__(1255));

var _getWindow = _interopRequireDefault(__webpack_require__(31677));

var _instanceOf = __webpack_require__(67458);

var _getHTMLElementScroll = _interopRequireDefault(__webpack_require__(50552));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

function getNodeScroll(node) {
  if (node === (0, _getWindow["default"])(node) || !(0, _instanceOf.isHTMLElement)(node)) {
    return (0, _getWindowScroll["default"])(node);
  } else {
    return (0, _getHTMLElementScroll["default"])(node);
  }
}

/***/ }),

/***/ 32440:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.default = getOffsetParent;

var _getWindow = _interopRequireDefault(__webpack_require__(31677));

var _getNodeName = _interopRequireDefault(__webpack_require__(99624));

var _getComputedStyle = _interopRequireDefault(__webpack_require__(61131));

var _instanceOf = __webpack_require__(67458);

var _isTableElement = _interopRequireDefault(__webpack_require__(79130));

var _getParentNode = _interopRequireDefault(__webpack_require__(31613));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

function getTrueOffsetParent(element) {
  if (!(0, _instanceOf.isHTMLElement)(element) || // https://github.com/popperjs/popper-core/issues/837
  (0, _getComputedStyle["default"])(element).position === 'fixed') {
    return null;
  }

  return element.offsetParent;
} // `.offsetParent` reports `null` for fixed elements, while absolute elements
// return the containing block


function getContainingBlock(element) {
  var isFirefox = navigator.userAgent.toLowerCase().indexOf('firefox') !== -1;
  var isIE = navigator.userAgent.indexOf('Trident') !== -1;

  if (isIE && (0, _instanceOf.isHTMLElement)(element)) {
    // In IE 9, 10 and 11 fixed elements containing block is always established by the viewport
    var elementCss = (0, _getComputedStyle["default"])(element);

    if (elementCss.position === 'fixed') {
      return null;
    }
  }

  var currentNode = (0, _getParentNode["default"])(element);

  while ((0, _instanceOf.isHTMLElement)(currentNode) && ['html', 'body'].indexOf((0, _getNodeName["default"])(currentNode)) < 0) {
    var css = (0, _getComputedStyle["default"])(currentNode); // This is non-exhaustive but covers the most common CSS properties that
    // create a containing block.
    // https://developer.mozilla.org/en-US/docs/Web/CSS/Containing_block#identifying_the_containing_block

    if (css.transform !== 'none' || css.perspective !== 'none' || css.contain === 'paint' || ['transform', 'perspective'].indexOf(css.willChange) !== -1 || isFirefox && css.willChange === 'filter' || isFirefox && css.filter && css.filter !== 'none') {
      return currentNode;
    } else {
      currentNode = currentNode.parentNode;
    }
  }

  return null;
} // Gets the closest ancestor positioned element. Handles some edge cases,
// such as table ancestors and cross browser bugs.


function getOffsetParent(element) {
  var window = (0, _getWindow["default"])(element);
  var offsetParent = getTrueOffsetParent(element);

  while (offsetParent && (0, _isTableElement["default"])(offsetParent) && (0, _getComputedStyle["default"])(offsetParent).position === 'static') {
    offsetParent = getTrueOffsetParent(offsetParent);
  }

  if (offsetParent && ((0, _getNodeName["default"])(offsetParent) === 'html' || (0, _getNodeName["default"])(offsetParent) === 'body' && (0, _getComputedStyle["default"])(offsetParent).position === 'static')) {
    return window;
  }

  return offsetParent || getContainingBlock(element) || window;
}

/***/ }),

/***/ 31613:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.default = getParentNode;

var _getNodeName = _interopRequireDefault(__webpack_require__(99624));

var _getDocumentElement = _interopRequireDefault(__webpack_require__(58386));

var _instanceOf = __webpack_require__(67458);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

function getParentNode(element) {
  if ((0, _getNodeName["default"])(element) === 'html') {
    return element;
  }

  return (// this is a quicker (but less type safe) way to save quite some bytes from the bundle
    // $FlowFixMe[incompatible-return]
    // $FlowFixMe[prop-missing]
    element.assignedSlot || // step into the shadow DOM of the parent of a slotted node
    element.parentNode || ( // DOM Element detected
    (0, _instanceOf.isShadowRoot)(element) ? element.host : null) || // ShadowRoot detected
    // $FlowFixMe[incompatible-call]: HTMLElement is a Node
    (0, _getDocumentElement["default"])(element) // fallback

  );
}

/***/ }),

/***/ 66055:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.default = getScrollParent;

var _getParentNode = _interopRequireDefault(__webpack_require__(31613));

var _isScrollParent = _interopRequireDefault(__webpack_require__(37229));

var _getNodeName = _interopRequireDefault(__webpack_require__(99624));

var _instanceOf = __webpack_require__(67458);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

function getScrollParent(node) {
  if (['html', 'body', '#document'].indexOf((0, _getNodeName["default"])(node)) >= 0) {
    // $FlowFixMe[incompatible-return]: assume body is always available
    return node.ownerDocument.body;
  }

  if ((0, _instanceOf.isHTMLElement)(node) && (0, _isScrollParent["default"])(node)) {
    return node;
  }

  return getScrollParent((0, _getParentNode["default"])(node));
}

/***/ }),

/***/ 78331:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.default = getViewportRect;

var _getWindow = _interopRequireDefault(__webpack_require__(31677));

var _getDocumentElement = _interopRequireDefault(__webpack_require__(58386));

var _getWindowScrollBarX = _interopRequireDefault(__webpack_require__(45471));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

function getViewportRect(element) {
  var win = (0, _getWindow["default"])(element);
  var html = (0, _getDocumentElement["default"])(element);
  var visualViewport = win.visualViewport;
  var width = html.clientWidth;
  var height = html.clientHeight;
  var x = 0;
  var y = 0; // NB: This isn't supported on iOS <= 12. If the keyboard is open, the popper
  // can be obscured underneath it.
  // Also, `html.clientHeight` adds the bottom bar height in Safari iOS, even
  // if it isn't open, so if this isn't available, the popper will be detected
  // to overflow the bottom of the screen too early.

  if (visualViewport) {
    width = visualViewport.width;
    height = visualViewport.height; // Uses Layout Viewport (like Chrome; Safari does not currently)
    // In Chrome, it returns a value very close to 0 (+/-) but contains rounding
    // errors due to floating point numbers, so we need to check precision.
    // Safari returns a number <= 0, usually < -1 when pinch-zoomed
    // Feature detection fails in mobile emulation mode in Chrome.
    // Math.abs(win.innerWidth / visualViewport.scale - visualViewport.width) <
    // 0.001
    // Fallback here: "Not Safari" userAgent

    if (!/^((?!chrome|android).)*safari/i.test(navigator.userAgent)) {
      x = visualViewport.offsetLeft;
      y = visualViewport.offsetTop;
    }
  }

  return {
    width: width,
    height: height,
    x: x + (0, _getWindowScrollBarX["default"])(element),
    y: y
  };
}

/***/ }),

/***/ 31677:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.default = getWindow;

function getWindow(node) {
  if (node == null) {
    return window;
  }

  if (node.toString() !== '[object Window]') {
    var ownerDocument = node.ownerDocument;
    return ownerDocument ? ownerDocument.defaultView || window : window;
  }

  return node;
}

/***/ }),

/***/ 1255:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.default = getWindowScroll;

var _getWindow = _interopRequireDefault(__webpack_require__(31677));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

function getWindowScroll(node) {
  var win = (0, _getWindow["default"])(node);
  var scrollLeft = win.pageXOffset;
  var scrollTop = win.pageYOffset;
  return {
    scrollLeft: scrollLeft,
    scrollTop: scrollTop
  };
}

/***/ }),

/***/ 45471:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.default = getWindowScrollBarX;

var _getBoundingClientRect = _interopRequireDefault(__webpack_require__(94294));

var _getDocumentElement = _interopRequireDefault(__webpack_require__(58386));

var _getWindowScroll = _interopRequireDefault(__webpack_require__(1255));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

function getWindowScrollBarX(element) {
  // If <html> has a CSS width greater than the viewport, then this will be
  // incorrect for RTL.
  // Popper 1 is broken in this case and never had a bug report so let's assume
  // it's not an issue. I don't think anyone ever specifies width on <html>
  // anyway.
  // Browsers where the left scrollbar doesn't cause an issue report `0` for
  // this (e.g. Edge 2019, IE11, Safari)
  return (0, _getBoundingClientRect["default"])((0, _getDocumentElement["default"])(element)).left + (0, _getWindowScroll["default"])(element).scrollLeft;
}

/***/ }),

/***/ 67458:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.isElement = isElement;
exports.isHTMLElement = isHTMLElement;
exports.isShadowRoot = isShadowRoot;

var _getWindow = _interopRequireDefault(__webpack_require__(31677));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

function isElement(node) {
  var OwnElement = (0, _getWindow["default"])(node).Element;
  return node instanceof OwnElement || node instanceof Element;
}

function isHTMLElement(node) {
  var OwnElement = (0, _getWindow["default"])(node).HTMLElement;
  return node instanceof OwnElement || node instanceof HTMLElement;
}

function isShadowRoot(node) {
  // IE 11 has no ShadowRoot
  if (typeof ShadowRoot === 'undefined') {
    return false;
  }

  var OwnElement = (0, _getWindow["default"])(node).ShadowRoot;
  return node instanceof OwnElement || node instanceof ShadowRoot;
}

/***/ }),

/***/ 37229:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.default = isScrollParent;

var _getComputedStyle2 = _interopRequireDefault(__webpack_require__(61131));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

function isScrollParent(element) {
  // Firefox wants us to check `-x` and `-y` variations as well
  var _getComputedStyle = (0, _getComputedStyle2["default"])(element),
      overflow = _getComputedStyle.overflow,
      overflowX = _getComputedStyle.overflowX,
      overflowY = _getComputedStyle.overflowY;

  return /auto|scroll|overlay|hidden/.test(overflow + overflowY + overflowX);
}

/***/ }),

/***/ 79130:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.default = isTableElement;

var _getNodeName = _interopRequireDefault(__webpack_require__(99624));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

function isTableElement(element) {
  return ['table', 'td', 'th'].indexOf((0, _getNodeName["default"])(element)) >= 0;
}

/***/ }),

/***/ 46213:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.default = listScrollParents;

var _getScrollParent = _interopRequireDefault(__webpack_require__(66055));

var _getParentNode = _interopRequireDefault(__webpack_require__(31613));

var _getWindow = _interopRequireDefault(__webpack_require__(31677));

var _isScrollParent = _interopRequireDefault(__webpack_require__(37229));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

/*
given a DOM element, return the list of all scroll parents, up the list of ancesors
until we get to the top window object. This list is what we attach scroll listeners
to, because if any of these parent elements scroll, we'll need to re-calculate the
reference element's position.
*/
function listScrollParents(element, list) {
  var _element$ownerDocumen;

  if (list === void 0) {
    list = [];
  }

  var scrollParent = (0, _getScrollParent["default"])(element);
  var isBody = scrollParent === ((_element$ownerDocumen = element.ownerDocument) == null ? void 0 : _element$ownerDocumen.body);
  var win = (0, _getWindow["default"])(scrollParent);
  var target = isBody ? [win].concat(win.visualViewport || [], (0, _isScrollParent["default"])(scrollParent) ? scrollParent : []) : scrollParent;
  var updatedList = list.concat(target);
  return isBody ? updatedList : // $FlowFixMe[incompatible-call]: isBody tells us target will be an HTMLElement here
  updatedList.concat(listScrollParents((0, _getParentNode["default"])(target)));
}

/***/ }),

/***/ 56728:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.modifierPhases = exports.afterWrite = exports.write = exports.beforeWrite = exports.afterMain = exports.main = exports.beforeMain = exports.afterRead = exports.read = exports.beforeRead = exports.placements = exports.variationPlacements = exports.reference = exports.popper = exports.viewport = exports.clippingParents = exports.end = exports.start = exports.basePlacements = exports.auto = exports.left = exports.right = exports.bottom = exports.top = void 0;
var top = 'top';
exports.top = top;
var bottom = 'bottom';
exports.bottom = bottom;
var right = 'right';
exports.right = right;
var left = 'left';
exports.left = left;
var auto = 'auto';
exports.auto = auto;
var basePlacements = [top, bottom, right, left];
exports.basePlacements = basePlacements;
var start = 'start';
exports.start = start;
var end = 'end';
exports.end = end;
var clippingParents = 'clippingParents';
exports.clippingParents = clippingParents;
var viewport = 'viewport';
exports.viewport = viewport;
var popper = 'popper';
exports.popper = popper;
var reference = 'reference';
exports.reference = reference;
var variationPlacements = /*#__PURE__*/basePlacements.reduce(function (acc, placement) {
  return acc.concat([placement + "-" + start, placement + "-" + end]);
}, []);
exports.variationPlacements = variationPlacements;
var placements = /*#__PURE__*/[].concat(basePlacements, [auto]).reduce(function (acc, placement) {
  return acc.concat([placement, placement + "-" + start, placement + "-" + end]);
}, []); // modifiers that need to read the DOM

exports.placements = placements;
var beforeRead = 'beforeRead';
exports.beforeRead = beforeRead;
var read = 'read';
exports.read = read;
var afterRead = 'afterRead'; // pure-logic modifiers

exports.afterRead = afterRead;
var beforeMain = 'beforeMain';
exports.beforeMain = beforeMain;
var main = 'main';
exports.main = main;
var afterMain = 'afterMain'; // modifier with the purpose to write to the DOM (or write into a framework state)

exports.afterMain = afterMain;
var beforeWrite = 'beforeWrite';
exports.beforeWrite = beforeWrite;
var write = 'write';
exports.write = write;
var afterWrite = 'afterWrite';
exports.afterWrite = afterWrite;
var modifierPhases = [beforeRead, read, afterRead, beforeMain, main, afterMain, beforeWrite, write, afterWrite];
exports.modifierPhases = modifierPhases;

/***/ }),

/***/ 34739:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
var _exportNames = {
  popperGenerator: true,
  detectOverflow: true,
  createPopperBase: true,
  createPopper: true,
  createPopperLite: true
};
exports.createPopperLite = exports.createPopper = exports.createPopperBase = exports.detectOverflow = exports.popperGenerator = void 0;

var _enums = __webpack_require__(56728);

Object.keys(_enums).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  if (Object.prototype.hasOwnProperty.call(_exportNames, key)) return;
  if (key in exports && exports[key] === _enums[key]) return;
  exports[key] = _enums[key];
});

var _index = __webpack_require__(18726);

Object.keys(_index).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  if (Object.prototype.hasOwnProperty.call(_exportNames, key)) return;
  if (key in exports && exports[key] === _index[key]) return;
  exports[key] = _index[key];
});

var _createPopper = __webpack_require__(92179);

exports.popperGenerator = _createPopper.popperGenerator;
exports.detectOverflow = _createPopper.detectOverflow;
exports.createPopperBase = _createPopper.createPopper;

var _popper = __webpack_require__(48069);

exports.createPopper = _popper.createPopper;

var _popperLite = __webpack_require__(39030);

exports.createPopperLite = _popperLite.createPopper;

/***/ }),

/***/ 34559:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.default = void 0;

var _getNodeName = _interopRequireDefault(__webpack_require__(99624));

var _instanceOf = __webpack_require__(67458);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

// This modifier takes the styles prepared by the `computeStyles` modifier
// and applies them to the HTMLElements such as popper and arrow
function applyStyles(_ref) {
  var state = _ref.state;
  Object.keys(state.elements).forEach(function (name) {
    var style = state.styles[name] || {};
    var attributes = state.attributes[name] || {};
    var element = state.elements[name]; // arrow is optional + virtual elements

    if (!(0, _instanceOf.isHTMLElement)(element) || !(0, _getNodeName["default"])(element)) {
      return;
    } // Flow doesn't support to extend this property, but it's the most
    // effective way to apply styles to an HTMLElement
    // $FlowFixMe[cannot-write]


    Object.assign(element.style, style);
    Object.keys(attributes).forEach(function (name) {
      var value = attributes[name];

      if (value === false) {
        element.removeAttribute(name);
      } else {
        element.setAttribute(name, value === true ? '' : value);
      }
    });
  });
}

function effect(_ref2) {
  var state = _ref2.state;
  var initialStyles = {
    popper: {
      position: state.options.strategy,
      left: '0',
      top: '0',
      margin: '0'
    },
    arrow: {
      position: 'absolute'
    },
    reference: {}
  };
  Object.assign(state.elements.popper.style, initialStyles.popper);
  state.styles = initialStyles;

  if (state.elements.arrow) {
    Object.assign(state.elements.arrow.style, initialStyles.arrow);
  }

  return function () {
    Object.keys(state.elements).forEach(function (name) {
      var element = state.elements[name];
      var attributes = state.attributes[name] || {};
      var styleProperties = Object.keys(state.styles.hasOwnProperty(name) ? state.styles[name] : initialStyles[name]); // Set all values to an empty string to unset them

      var style = styleProperties.reduce(function (style, property) {
        style[property] = '';
        return style;
      }, {}); // arrow is optional + virtual elements

      if (!(0, _instanceOf.isHTMLElement)(element) || !(0, _getNodeName["default"])(element)) {
        return;
      }

      Object.assign(element.style, style);
      Object.keys(attributes).forEach(function (attribute) {
        element.removeAttribute(attribute);
      });
    });
  };
} // eslint-disable-next-line import/no-unused-modules


var _default = {
  name: 'applyStyles',
  enabled: true,
  phase: 'write',
  fn: applyStyles,
  effect: effect,
  requires: ['computeStyles']
};
exports.default = _default;

/***/ }),

/***/ 76340:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.default = void 0;

var _getBasePlacement = _interopRequireDefault(__webpack_require__(22709));

var _getLayoutRect = _interopRequireDefault(__webpack_require__(43545));

var _contains = _interopRequireDefault(__webpack_require__(97607));

var _getOffsetParent = _interopRequireDefault(__webpack_require__(32440));

var _getMainAxisFromPlacement = _interopRequireDefault(__webpack_require__(16615));

var _within = _interopRequireDefault(__webpack_require__(52534));

var _mergePaddingObject = _interopRequireDefault(__webpack_require__(32055));

var _expandToHashMap = _interopRequireDefault(__webpack_require__(76294));

var _enums = __webpack_require__(56728);

var _instanceOf = __webpack_require__(67458);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

// eslint-disable-next-line import/no-unused-modules
var toPaddingObject = function () {
  function toPaddingObject(padding, state) {
    padding = typeof padding === 'function' ? padding(Object.assign({}, state.rects, {
      placement: state.placement
    })) : padding;
    return (0, _mergePaddingObject["default"])(typeof padding !== 'number' ? padding : (0, _expandToHashMap["default"])(padding, _enums.basePlacements));
  }

  return toPaddingObject;
}();

function arrow(_ref) {
  var _state$modifiersData$;

  var state = _ref.state,
      name = _ref.name,
      options = _ref.options;
  var arrowElement = state.elements.arrow;
  var popperOffsets = state.modifiersData.popperOffsets;
  var basePlacement = (0, _getBasePlacement["default"])(state.placement);
  var axis = (0, _getMainAxisFromPlacement["default"])(basePlacement);
  var isVertical = [_enums.left, _enums.right].indexOf(basePlacement) >= 0;
  var len = isVertical ? 'height' : 'width';

  if (!arrowElement || !popperOffsets) {
    return;
  }

  var paddingObject = toPaddingObject(options.padding, state);
  var arrowRect = (0, _getLayoutRect["default"])(arrowElement);
  var minProp = axis === 'y' ? _enums.top : _enums.left;
  var maxProp = axis === 'y' ? _enums.bottom : _enums.right;
  var endDiff = state.rects.reference[len] + state.rects.reference[axis] - popperOffsets[axis] - state.rects.popper[len];
  var startDiff = popperOffsets[axis] - state.rects.reference[axis];
  var arrowOffsetParent = (0, _getOffsetParent["default"])(arrowElement);
  var clientSize = arrowOffsetParent ? axis === 'y' ? arrowOffsetParent.clientHeight || 0 : arrowOffsetParent.clientWidth || 0 : 0;
  var centerToReference = endDiff / 2 - startDiff / 2; // Make sure the arrow doesn't overflow the popper if the center point is
  // outside of the popper bounds

  var min = paddingObject[minProp];
  var max = clientSize - arrowRect[len] - paddingObject[maxProp];
  var center = clientSize / 2 - arrowRect[len] / 2 + centerToReference;
  var offset = (0, _within["default"])(min, center, max); // Prevents breaking syntax highlighting...

  var axisProp = axis;
  state.modifiersData[name] = (_state$modifiersData$ = {}, _state$modifiersData$[axisProp] = offset, _state$modifiersData$.centerOffset = offset - center, _state$modifiersData$);
}

function effect(_ref2) {
  var state = _ref2.state,
      options = _ref2.options;
  var _options$element = options.element,
      arrowElement = _options$element === void 0 ? '[data-popper-arrow]' : _options$element;

  if (arrowElement == null) {
    return;
  } // CSS selector


  if (typeof arrowElement === 'string') {
    arrowElement = state.elements.popper.querySelector(arrowElement);

    if (!arrowElement) {
      return;
    }
  }

  if (false) {}

  if (!(0, _contains["default"])(state.elements.popper, arrowElement)) {
    if (false) {}

    return;
  }

  state.elements.arrow = arrowElement;
} // eslint-disable-next-line import/no-unused-modules


var _default = {
  name: 'arrow',
  enabled: true,
  phase: 'main',
  fn: arrow,
  effect: effect,
  requires: ['popperOffsets'],
  requiresIfExists: ['preventOverflow']
};
exports.default = _default;

/***/ }),

/***/ 11300:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.mapToStyles = mapToStyles;
exports.default = void 0;

var _enums = __webpack_require__(56728);

var _getOffsetParent = _interopRequireDefault(__webpack_require__(32440));

var _getWindow = _interopRequireDefault(__webpack_require__(31677));

var _getDocumentElement = _interopRequireDefault(__webpack_require__(58386));

var _getComputedStyle = _interopRequireDefault(__webpack_require__(61131));

var _getBasePlacement = _interopRequireDefault(__webpack_require__(22709));

var _math = __webpack_require__(62836);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

// eslint-disable-next-line import/no-unused-modules
var unsetSides = {
  top: 'auto',
  right: 'auto',
  bottom: 'auto',
  left: 'auto'
}; // Round the offsets to the nearest suitable subpixel based on the DPR.
// Zooming can change the DPR, but it seems to report a value that will
// cleanly divide the values into the appropriate subpixels.

function roundOffsetsByDPR(_ref) {
  var x = _ref.x,
      y = _ref.y;
  var win = window;
  var dpr = win.devicePixelRatio || 1;
  return {
    x: (0, _math.round)((0, _math.round)(x * dpr) / dpr) || 0,
    y: (0, _math.round)((0, _math.round)(y * dpr) / dpr) || 0
  };
}

function mapToStyles(_ref2) {
  var _Object$assign2;

  var popper = _ref2.popper,
      popperRect = _ref2.popperRect,
      placement = _ref2.placement,
      offsets = _ref2.offsets,
      position = _ref2.position,
      gpuAcceleration = _ref2.gpuAcceleration,
      adaptive = _ref2.adaptive,
      roundOffsets = _ref2.roundOffsets;

  var _ref3 = roundOffsets === true ? roundOffsetsByDPR(offsets) : typeof roundOffsets === 'function' ? roundOffsets(offsets) : offsets,
      _ref3$x = _ref3.x,
      x = _ref3$x === void 0 ? 0 : _ref3$x,
      _ref3$y = _ref3.y,
      y = _ref3$y === void 0 ? 0 : _ref3$y;

  var hasX = offsets.hasOwnProperty('x');
  var hasY = offsets.hasOwnProperty('y');
  var sideX = _enums.left;
  var sideY = _enums.top;
  var win = window;

  if (adaptive) {
    var offsetParent = (0, _getOffsetParent["default"])(popper);
    var heightProp = 'clientHeight';
    var widthProp = 'clientWidth';

    if (offsetParent === (0, _getWindow["default"])(popper)) {
      offsetParent = (0, _getDocumentElement["default"])(popper);

      if ((0, _getComputedStyle["default"])(offsetParent).position !== 'static') {
        heightProp = 'scrollHeight';
        widthProp = 'scrollWidth';
      }
    } // $FlowFixMe[incompatible-cast]: force type refinement, we compare offsetParent with window above, but Flow doesn't detect it


    offsetParent = offsetParent;

    if (placement === _enums.top) {
      sideY = _enums.bottom; // $FlowFixMe[prop-missing]

      y -= offsetParent[heightProp] - popperRect.height;
      y *= gpuAcceleration ? 1 : -1;
    }

    if (placement === _enums.left) {
      sideX = _enums.right; // $FlowFixMe[prop-missing]

      x -= offsetParent[widthProp] - popperRect.width;
      x *= gpuAcceleration ? 1 : -1;
    }
  }

  var commonStyles = Object.assign({
    position: position
  }, adaptive && unsetSides);

  if (gpuAcceleration) {
    var _Object$assign;

    return Object.assign({}, commonStyles, (_Object$assign = {}, _Object$assign[sideY] = hasY ? '0' : '', _Object$assign[sideX] = hasX ? '0' : '', _Object$assign.transform = (win.devicePixelRatio || 1) < 2 ? "translate(" + x + "px, " + y + "px)" : "translate3d(" + x + "px, " + y + "px, 0)", _Object$assign));
  }

  return Object.assign({}, commonStyles, (_Object$assign2 = {}, _Object$assign2[sideY] = hasY ? y + "px" : '', _Object$assign2[sideX] = hasX ? x + "px" : '', _Object$assign2.transform = '', _Object$assign2));
}

function computeStyles(_ref4) {
  var state = _ref4.state,
      options = _ref4.options;
  var _options$gpuAccelerat = options.gpuAcceleration,
      gpuAcceleration = _options$gpuAccelerat === void 0 ? true : _options$gpuAccelerat,
      _options$adaptive = options.adaptive,
      adaptive = _options$adaptive === void 0 ? true : _options$adaptive,
      _options$roundOffsets = options.roundOffsets,
      roundOffsets = _options$roundOffsets === void 0 ? true : _options$roundOffsets;

  if (false) { var transitionProperty; }

  var commonStyles = {
    placement: (0, _getBasePlacement["default"])(state.placement),
    popper: state.elements.popper,
    popperRect: state.rects.popper,
    gpuAcceleration: gpuAcceleration
  };

  if (state.modifiersData.popperOffsets != null) {
    state.styles.popper = Object.assign({}, state.styles.popper, mapToStyles(Object.assign({}, commonStyles, {
      offsets: state.modifiersData.popperOffsets,
      position: state.options.strategy,
      adaptive: adaptive,
      roundOffsets: roundOffsets
    })));
  }

  if (state.modifiersData.arrow != null) {
    state.styles.arrow = Object.assign({}, state.styles.arrow, mapToStyles(Object.assign({}, commonStyles, {
      offsets: state.modifiersData.arrow,
      position: 'absolute',
      adaptive: false,
      roundOffsets: roundOffsets
    })));
  }

  state.attributes.popper = Object.assign({}, state.attributes.popper, {
    'data-popper-placement': state.placement
  });
} // eslint-disable-next-line import/no-unused-modules


var _default = {
  name: 'computeStyles',
  enabled: true,
  phase: 'beforeWrite',
  fn: computeStyles,
  data: {}
};
exports.default = _default;

/***/ }),

/***/ 27020:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.default = void 0;

var _getWindow = _interopRequireDefault(__webpack_require__(31677));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

// eslint-disable-next-line import/no-unused-modules
var passive = {
  passive: true
};

function effect(_ref) {
  var state = _ref.state,
      instance = _ref.instance,
      options = _ref.options;
  var _options$scroll = options.scroll,
      scroll = _options$scroll === void 0 ? true : _options$scroll,
      _options$resize = options.resize,
      resize = _options$resize === void 0 ? true : _options$resize;
  var window = (0, _getWindow["default"])(state.elements.popper);
  var scrollParents = [].concat(state.scrollParents.reference, state.scrollParents.popper);

  if (scroll) {
    scrollParents.forEach(function (scrollParent) {
      scrollParent.addEventListener('scroll', instance.update, passive);
    });
  }

  if (resize) {
    window.addEventListener('resize', instance.update, passive);
  }

  return function () {
    if (scroll) {
      scrollParents.forEach(function (scrollParent) {
        scrollParent.removeEventListener('scroll', instance.update, passive);
      });
    }

    if (resize) {
      window.removeEventListener('resize', instance.update, passive);
    }
  };
} // eslint-disable-next-line import/no-unused-modules


var _default = {
  name: 'eventListeners',
  enabled: true,
  phase: 'write',
  fn: function () {
    function fn() {}

    return fn;
  }(),
  effect: effect,
  data: {}
};
exports.default = _default;

/***/ }),

/***/ 45922:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.default = void 0;

var _getOppositePlacement = _interopRequireDefault(__webpack_require__(70045));

var _getBasePlacement = _interopRequireDefault(__webpack_require__(22709));

var _getOppositeVariationPlacement = _interopRequireDefault(__webpack_require__(28916));

var _detectOverflow = _interopRequireDefault(__webpack_require__(19574));

var _computeAutoPlacement = _interopRequireDefault(__webpack_require__(31565));

var _enums = __webpack_require__(56728);

var _getVariation = _interopRequireDefault(__webpack_require__(45412));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

// eslint-disable-next-line import/no-unused-modules
function getExpandedFallbackPlacements(placement) {
  if ((0, _getBasePlacement["default"])(placement) === _enums.auto) {
    return [];
  }

  var oppositePlacement = (0, _getOppositePlacement["default"])(placement);
  return [(0, _getOppositeVariationPlacement["default"])(placement), oppositePlacement, (0, _getOppositeVariationPlacement["default"])(oppositePlacement)];
}

function flip(_ref) {
  var state = _ref.state,
      options = _ref.options,
      name = _ref.name;

  if (state.modifiersData[name]._skip) {
    return;
  }

  var _options$mainAxis = options.mainAxis,
      checkMainAxis = _options$mainAxis === void 0 ? true : _options$mainAxis,
      _options$altAxis = options.altAxis,
      checkAltAxis = _options$altAxis === void 0 ? true : _options$altAxis,
      specifiedFallbackPlacements = options.fallbackPlacements,
      padding = options.padding,
      boundary = options.boundary,
      rootBoundary = options.rootBoundary,
      altBoundary = options.altBoundary,
      _options$flipVariatio = options.flipVariations,
      flipVariations = _options$flipVariatio === void 0 ? true : _options$flipVariatio,
      allowedAutoPlacements = options.allowedAutoPlacements;
  var preferredPlacement = state.options.placement;
  var basePlacement = (0, _getBasePlacement["default"])(preferredPlacement);
  var isBasePlacement = basePlacement === preferredPlacement;
  var fallbackPlacements = specifiedFallbackPlacements || (isBasePlacement || !flipVariations ? [(0, _getOppositePlacement["default"])(preferredPlacement)] : getExpandedFallbackPlacements(preferredPlacement));
  var placements = [preferredPlacement].concat(fallbackPlacements).reduce(function (acc, placement) {
    return acc.concat((0, _getBasePlacement["default"])(placement) === _enums.auto ? (0, _computeAutoPlacement["default"])(state, {
      placement: placement,
      boundary: boundary,
      rootBoundary: rootBoundary,
      padding: padding,
      flipVariations: flipVariations,
      allowedAutoPlacements: allowedAutoPlacements
    }) : placement);
  }, []);
  var referenceRect = state.rects.reference;
  var popperRect = state.rects.popper;
  var checksMap = new Map();
  var makeFallbackChecks = true;
  var firstFittingPlacement = placements[0];

  for (var i = 0; i < placements.length; i++) {
    var placement = placements[i];

    var _basePlacement = (0, _getBasePlacement["default"])(placement);

    var isStartVariation = (0, _getVariation["default"])(placement) === _enums.start;

    var isVertical = [_enums.top, _enums.bottom].indexOf(_basePlacement) >= 0;
    var len = isVertical ? 'width' : 'height';
    var overflow = (0, _detectOverflow["default"])(state, {
      placement: placement,
      boundary: boundary,
      rootBoundary: rootBoundary,
      altBoundary: altBoundary,
      padding: padding
    });
    var mainVariationSide = isVertical ? isStartVariation ? _enums.right : _enums.left : isStartVariation ? _enums.bottom : _enums.top;

    if (referenceRect[len] > popperRect[len]) {
      mainVariationSide = (0, _getOppositePlacement["default"])(mainVariationSide);
    }

    var altVariationSide = (0, _getOppositePlacement["default"])(mainVariationSide);
    var checks = [];

    if (checkMainAxis) {
      checks.push(overflow[_basePlacement] <= 0);
    }

    if (checkAltAxis) {
      checks.push(overflow[mainVariationSide] <= 0, overflow[altVariationSide] <= 0);
    }

    if (checks.every(function (check) {
      return check;
    })) {
      firstFittingPlacement = placement;
      makeFallbackChecks = false;
      break;
    }

    checksMap.set(placement, checks);
  }

  if (makeFallbackChecks) {
    // `2` may be desired in some cases – research later
    var numberOfChecks = flipVariations ? 3 : 1;

    var _loop = function () {
      function _loop(_i) {
        var fittingPlacement = placements.find(function (placement) {
          var checks = checksMap.get(placement);

          if (checks) {
            return checks.slice(0, _i).every(function (check) {
              return check;
            });
          }
        });

        if (fittingPlacement) {
          firstFittingPlacement = fittingPlacement;
          return "break";
        }
      }

      return _loop;
    }();

    for (var _i = numberOfChecks; _i > 0; _i--) {
      var _ret = _loop(_i);

      if (_ret === "break") break;
    }
  }

  if (state.placement !== firstFittingPlacement) {
    state.modifiersData[name]._skip = true;
    state.placement = firstFittingPlacement;
    state.reset = true;
  }
} // eslint-disable-next-line import/no-unused-modules


var _default = {
  name: 'flip',
  enabled: true,
  phase: 'main',
  fn: flip,
  requiresIfExists: ['offset'],
  data: {
    _skip: false
  }
};
exports.default = _default;

/***/ }),

/***/ 82860:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.default = void 0;

var _enums = __webpack_require__(56728);

var _detectOverflow = _interopRequireDefault(__webpack_require__(19574));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

function getSideOffsets(overflow, rect, preventedOffsets) {
  if (preventedOffsets === void 0) {
    preventedOffsets = {
      x: 0,
      y: 0
    };
  }

  return {
    top: overflow.top - rect.height - preventedOffsets.y,
    right: overflow.right - rect.width + preventedOffsets.x,
    bottom: overflow.bottom - rect.height + preventedOffsets.y,
    left: overflow.left - rect.width - preventedOffsets.x
  };
}

function isAnySideFullyClipped(overflow) {
  return [_enums.top, _enums.right, _enums.bottom, _enums.left].some(function (side) {
    return overflow[side] >= 0;
  });
}

function hide(_ref) {
  var state = _ref.state,
      name = _ref.name;
  var referenceRect = state.rects.reference;
  var popperRect = state.rects.popper;
  var preventedOffsets = state.modifiersData.preventOverflow;
  var referenceOverflow = (0, _detectOverflow["default"])(state, {
    elementContext: 'reference'
  });
  var popperAltOverflow = (0, _detectOverflow["default"])(state, {
    altBoundary: true
  });
  var referenceClippingOffsets = getSideOffsets(referenceOverflow, referenceRect);
  var popperEscapeOffsets = getSideOffsets(popperAltOverflow, popperRect, preventedOffsets);
  var isReferenceHidden = isAnySideFullyClipped(referenceClippingOffsets);
  var hasPopperEscaped = isAnySideFullyClipped(popperEscapeOffsets);
  state.modifiersData[name] = {
    referenceClippingOffsets: referenceClippingOffsets,
    popperEscapeOffsets: popperEscapeOffsets,
    isReferenceHidden: isReferenceHidden,
    hasPopperEscaped: hasPopperEscaped
  };
  state.attributes.popper = Object.assign({}, state.attributes.popper, {
    'data-popper-reference-hidden': isReferenceHidden,
    'data-popper-escaped': hasPopperEscaped
  });
} // eslint-disable-next-line import/no-unused-modules


var _default = {
  name: 'hide',
  enabled: true,
  phase: 'main',
  requiresIfExists: ['preventOverflow'],
  fn: hide
};
exports.default = _default;

/***/ }),

/***/ 18726:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.preventOverflow = exports.popperOffsets = exports.offset = exports.hide = exports.flip = exports.eventListeners = exports.computeStyles = exports.arrow = exports.applyStyles = void 0;

var _applyStyles = _interopRequireDefault(__webpack_require__(34559));

exports.applyStyles = _applyStyles["default"];

var _arrow = _interopRequireDefault(__webpack_require__(76340));

exports.arrow = _arrow["default"];

var _computeStyles = _interopRequireDefault(__webpack_require__(11300));

exports.computeStyles = _computeStyles["default"];

var _eventListeners = _interopRequireDefault(__webpack_require__(27020));

exports.eventListeners = _eventListeners["default"];

var _flip = _interopRequireDefault(__webpack_require__(45922));

exports.flip = _flip["default"];

var _hide = _interopRequireDefault(__webpack_require__(82860));

exports.hide = _hide["default"];

var _offset = _interopRequireDefault(__webpack_require__(33310));

exports.offset = _offset["default"];

var _popperOffsets = _interopRequireDefault(__webpack_require__(74371));

exports.popperOffsets = _popperOffsets["default"];

var _preventOverflow = _interopRequireDefault(__webpack_require__(21532));

exports.preventOverflow = _preventOverflow["default"];

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

/***/ }),

/***/ 33310:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.distanceAndSkiddingToXY = distanceAndSkiddingToXY;
exports.default = void 0;

var _getBasePlacement = _interopRequireDefault(__webpack_require__(22709));

var _enums = __webpack_require__(56728);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

function distanceAndSkiddingToXY(placement, rects, offset) {
  var basePlacement = (0, _getBasePlacement["default"])(placement);
  var invertDistance = [_enums.left, _enums.top].indexOf(basePlacement) >= 0 ? -1 : 1;

  var _ref = typeof offset === 'function' ? offset(Object.assign({}, rects, {
    placement: placement
  })) : offset,
      skidding = _ref[0],
      distance = _ref[1];

  skidding = skidding || 0;
  distance = (distance || 0) * invertDistance;
  return [_enums.left, _enums.right].indexOf(basePlacement) >= 0 ? {
    x: distance,
    y: skidding
  } : {
    x: skidding,
    y: distance
  };
}

function offset(_ref2) {
  var state = _ref2.state,
      options = _ref2.options,
      name = _ref2.name;
  var _options$offset = options.offset,
      offset = _options$offset === void 0 ? [0, 0] : _options$offset;

  var data = _enums.placements.reduce(function (acc, placement) {
    acc[placement] = distanceAndSkiddingToXY(placement, state.rects, offset);
    return acc;
  }, {});

  var _data$state$placement = data[state.placement],
      x = _data$state$placement.x,
      y = _data$state$placement.y;

  if (state.modifiersData.popperOffsets != null) {
    state.modifiersData.popperOffsets.x += x;
    state.modifiersData.popperOffsets.y += y;
  }

  state.modifiersData[name] = data;
} // eslint-disable-next-line import/no-unused-modules


var _default = {
  name: 'offset',
  enabled: true,
  phase: 'main',
  requires: ['popperOffsets'],
  fn: offset
};
exports.default = _default;

/***/ }),

/***/ 74371:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.default = void 0;

var _computeOffsets = _interopRequireDefault(__webpack_require__(17175));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

function popperOffsets(_ref) {
  var state = _ref.state,
      name = _ref.name; // Offsets are the actual position the popper needs to have to be
  // properly positioned near its reference element
  // This is the most basic placement, and will be adjusted by
  // the modifiers in the next step

  state.modifiersData[name] = (0, _computeOffsets["default"])({
    reference: state.rects.reference,
    element: state.rects.popper,
    strategy: 'absolute',
    placement: state.placement
  });
} // eslint-disable-next-line import/no-unused-modules


var _default = {
  name: 'popperOffsets',
  enabled: true,
  phase: 'read',
  fn: popperOffsets,
  data: {}
};
exports.default = _default;

/***/ }),

/***/ 21532:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.default = void 0;

var _enums = __webpack_require__(56728);

var _getBasePlacement = _interopRequireDefault(__webpack_require__(22709));

var _getMainAxisFromPlacement = _interopRequireDefault(__webpack_require__(16615));

var _getAltAxis = _interopRequireDefault(__webpack_require__(82898));

var _within = _interopRequireDefault(__webpack_require__(52534));

var _getLayoutRect = _interopRequireDefault(__webpack_require__(43545));

var _getOffsetParent = _interopRequireDefault(__webpack_require__(32440));

var _detectOverflow = _interopRequireDefault(__webpack_require__(19574));

var _getVariation = _interopRequireDefault(__webpack_require__(45412));

var _getFreshSideObject = _interopRequireDefault(__webpack_require__(41801));

var _math = __webpack_require__(62836);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

function preventOverflow(_ref) {
  var state = _ref.state,
      options = _ref.options,
      name = _ref.name;
  var _options$mainAxis = options.mainAxis,
      checkMainAxis = _options$mainAxis === void 0 ? true : _options$mainAxis,
      _options$altAxis = options.altAxis,
      checkAltAxis = _options$altAxis === void 0 ? false : _options$altAxis,
      boundary = options.boundary,
      rootBoundary = options.rootBoundary,
      altBoundary = options.altBoundary,
      padding = options.padding,
      _options$tether = options.tether,
      tether = _options$tether === void 0 ? true : _options$tether,
      _options$tetherOffset = options.tetherOffset,
      tetherOffset = _options$tetherOffset === void 0 ? 0 : _options$tetherOffset;
  var overflow = (0, _detectOverflow["default"])(state, {
    boundary: boundary,
    rootBoundary: rootBoundary,
    padding: padding,
    altBoundary: altBoundary
  });
  var basePlacement = (0, _getBasePlacement["default"])(state.placement);
  var variation = (0, _getVariation["default"])(state.placement);
  var isBasePlacement = !variation;
  var mainAxis = (0, _getMainAxisFromPlacement["default"])(basePlacement);
  var altAxis = (0, _getAltAxis["default"])(mainAxis);
  var popperOffsets = state.modifiersData.popperOffsets;
  var referenceRect = state.rects.reference;
  var popperRect = state.rects.popper;
  var tetherOffsetValue = typeof tetherOffset === 'function' ? tetherOffset(Object.assign({}, state.rects, {
    placement: state.placement
  })) : tetherOffset;
  var data = {
    x: 0,
    y: 0
  };

  if (!popperOffsets) {
    return;
  }

  if (checkMainAxis || checkAltAxis) {
    var mainSide = mainAxis === 'y' ? _enums.top : _enums.left;
    var altSide = mainAxis === 'y' ? _enums.bottom : _enums.right;
    var len = mainAxis === 'y' ? 'height' : 'width';
    var offset = popperOffsets[mainAxis];
    var min = popperOffsets[mainAxis] + overflow[mainSide];
    var max = popperOffsets[mainAxis] - overflow[altSide];
    var additive = tether ? -popperRect[len] / 2 : 0;
    var minLen = variation === _enums.start ? referenceRect[len] : popperRect[len];
    var maxLen = variation === _enums.start ? -popperRect[len] : -referenceRect[len]; // We need to include the arrow in the calculation so the arrow doesn't go
    // outside the reference bounds

    var arrowElement = state.elements.arrow;
    var arrowRect = tether && arrowElement ? (0, _getLayoutRect["default"])(arrowElement) : {
      width: 0,
      height: 0
    };
    var arrowPaddingObject = state.modifiersData['arrow#persistent'] ? state.modifiersData['arrow#persistent'].padding : (0, _getFreshSideObject["default"])();
    var arrowPaddingMin = arrowPaddingObject[mainSide];
    var arrowPaddingMax = arrowPaddingObject[altSide]; // If the reference length is smaller than the arrow length, we don't want
    // to include its full size in the calculation. If the reference is small
    // and near the edge of a boundary, the popper can overflow even if the
    // reference is not overflowing as well (e.g. virtual elements with no
    // width or height)

    var arrowLen = (0, _within["default"])(0, referenceRect[len], arrowRect[len]);
    var minOffset = isBasePlacement ? referenceRect[len] / 2 - additive - arrowLen - arrowPaddingMin - tetherOffsetValue : minLen - arrowLen - arrowPaddingMin - tetherOffsetValue;
    var maxOffset = isBasePlacement ? -referenceRect[len] / 2 + additive + arrowLen + arrowPaddingMax + tetherOffsetValue : maxLen + arrowLen + arrowPaddingMax + tetherOffsetValue;
    var arrowOffsetParent = state.elements.arrow && (0, _getOffsetParent["default"])(state.elements.arrow);
    var clientOffset = arrowOffsetParent ? mainAxis === 'y' ? arrowOffsetParent.clientTop || 0 : arrowOffsetParent.clientLeft || 0 : 0;
    var offsetModifierValue = state.modifiersData.offset ? state.modifiersData.offset[state.placement][mainAxis] : 0;
    var tetherMin = popperOffsets[mainAxis] + minOffset - offsetModifierValue - clientOffset;
    var tetherMax = popperOffsets[mainAxis] + maxOffset - offsetModifierValue;

    if (checkMainAxis) {
      var preventedOffset = (0, _within["default"])(tether ? (0, _math.min)(min, tetherMin) : min, offset, tether ? (0, _math.max)(max, tetherMax) : max);
      popperOffsets[mainAxis] = preventedOffset;
      data[mainAxis] = preventedOffset - offset;
    }

    if (checkAltAxis) {
      var _mainSide = mainAxis === 'x' ? _enums.top : _enums.left;

      var _altSide = mainAxis === 'x' ? _enums.bottom : _enums.right;

      var _offset = popperOffsets[altAxis];

      var _min = _offset + overflow[_mainSide];

      var _max = _offset - overflow[_altSide];

      var _preventedOffset = (0, _within["default"])(tether ? (0, _math.min)(_min, tetherMin) : _min, _offset, tether ? (0, _math.max)(_max, tetherMax) : _max);

      popperOffsets[altAxis] = _preventedOffset;
      data[altAxis] = _preventedOffset - _offset;
    }
  }

  state.modifiersData[name] = data;
} // eslint-disable-next-line import/no-unused-modules


var _default = {
  name: 'preventOverflow',
  enabled: true,
  phase: 'main',
  fn: preventOverflow,
  requiresIfExists: ['offset']
};
exports.default = _default;

/***/ }),

/***/ 39030:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.defaultModifiers = exports.createPopper = void 0;

var _createPopper = __webpack_require__(92179);

exports.popperGenerator = _createPopper.popperGenerator;
exports.detectOverflow = _createPopper.detectOverflow;

var _eventListeners = _interopRequireDefault(__webpack_require__(27020));

var _popperOffsets = _interopRequireDefault(__webpack_require__(74371));

var _computeStyles = _interopRequireDefault(__webpack_require__(11300));

var _applyStyles = _interopRequireDefault(__webpack_require__(34559));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

var defaultModifiers = [_eventListeners["default"], _popperOffsets["default"], _computeStyles["default"], _applyStyles["default"]];
exports.defaultModifiers = defaultModifiers;
var createPopper = /*#__PURE__*/(0, _createPopper.popperGenerator)({
  defaultModifiers: defaultModifiers
}); // eslint-disable-next-line import/no-unused-modules

exports.createPopper = createPopper;

/***/ }),

/***/ 48069:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
var _exportNames = {
  createPopper: true,
  createPopperLite: true,
  defaultModifiers: true,
  popperGenerator: true,
  detectOverflow: true
};
exports.defaultModifiers = exports.createPopperLite = exports.createPopper = void 0;

var _createPopper = __webpack_require__(92179);

exports.popperGenerator = _createPopper.popperGenerator;
exports.detectOverflow = _createPopper.detectOverflow;

var _eventListeners = _interopRequireDefault(__webpack_require__(27020));

var _popperOffsets = _interopRequireDefault(__webpack_require__(74371));

var _computeStyles = _interopRequireDefault(__webpack_require__(11300));

var _applyStyles = _interopRequireDefault(__webpack_require__(34559));

var _offset = _interopRequireDefault(__webpack_require__(33310));

var _flip = _interopRequireDefault(__webpack_require__(45922));

var _preventOverflow = _interopRequireDefault(__webpack_require__(21532));

var _arrow = _interopRequireDefault(__webpack_require__(76340));

var _hide = _interopRequireDefault(__webpack_require__(82860));

var _popperLite = __webpack_require__(39030);

exports.createPopperLite = _popperLite.createPopper;

var _index = __webpack_require__(18726);

Object.keys(_index).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  if (Object.prototype.hasOwnProperty.call(_exportNames, key)) return;
  if (key in exports && exports[key] === _index[key]) return;
  exports[key] = _index[key];
});

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

var defaultModifiers = [_eventListeners["default"], _popperOffsets["default"], _computeStyles["default"], _applyStyles["default"], _offset["default"], _flip["default"], _preventOverflow["default"], _arrow["default"], _hide["default"]];
exports.defaultModifiers = defaultModifiers;
var createPopper = /*#__PURE__*/(0, _createPopper.popperGenerator)({
  defaultModifiers: defaultModifiers
}); // eslint-disable-next-line import/no-unused-modules

exports.createPopperLite = exports.createPopper = createPopper;

/***/ }),

/***/ 31565:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.default = computeAutoPlacement;

var _getVariation = _interopRequireDefault(__webpack_require__(45412));

var _enums = __webpack_require__(56728);

var _detectOverflow = _interopRequireDefault(__webpack_require__(19574));

var _getBasePlacement = _interopRequireDefault(__webpack_require__(22709));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

function computeAutoPlacement(state, options) {
  if (options === void 0) {
    options = {};
  }

  var _options = options,
      placement = _options.placement,
      boundary = _options.boundary,
      rootBoundary = _options.rootBoundary,
      padding = _options.padding,
      flipVariations = _options.flipVariations,
      _options$allowedAutoP = _options.allowedAutoPlacements,
      allowedAutoPlacements = _options$allowedAutoP === void 0 ? _enums.placements : _options$allowedAutoP;
  var variation = (0, _getVariation["default"])(placement);
  var placements = variation ? flipVariations ? _enums.variationPlacements : _enums.variationPlacements.filter(function (placement) {
    return (0, _getVariation["default"])(placement) === variation;
  }) : _enums.basePlacements;
  var allowedPlacements = placements.filter(function (placement) {
    return allowedAutoPlacements.indexOf(placement) >= 0;
  });

  if (allowedPlacements.length === 0) {
    allowedPlacements = placements;

    if (false) {}
  } // $FlowFixMe[incompatible-type]: Flow seems to have problems with two array unions...


  var overflows = allowedPlacements.reduce(function (acc, placement) {
    acc[placement] = (0, _detectOverflow["default"])(state, {
      placement: placement,
      boundary: boundary,
      rootBoundary: rootBoundary,
      padding: padding
    })[(0, _getBasePlacement["default"])(placement)];
    return acc;
  }, {});
  return Object.keys(overflows).sort(function (a, b) {
    return overflows[a] - overflows[b];
  });
}

/***/ }),

/***/ 17175:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.default = computeOffsets;

var _getBasePlacement = _interopRequireDefault(__webpack_require__(22709));

var _getVariation = _interopRequireDefault(__webpack_require__(45412));

var _getMainAxisFromPlacement = _interopRequireDefault(__webpack_require__(16615));

var _enums = __webpack_require__(56728);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

function computeOffsets(_ref) {
  var reference = _ref.reference,
      element = _ref.element,
      placement = _ref.placement;
  var basePlacement = placement ? (0, _getBasePlacement["default"])(placement) : null;
  var variation = placement ? (0, _getVariation["default"])(placement) : null;
  var commonX = reference.x + reference.width / 2 - element.width / 2;
  var commonY = reference.y + reference.height / 2 - element.height / 2;
  var offsets;

  switch (basePlacement) {
    case _enums.top:
      offsets = {
        x: commonX,
        y: reference.y - element.height
      };
      break;

    case _enums.bottom:
      offsets = {
        x: commonX,
        y: reference.y + reference.height
      };
      break;

    case _enums.right:
      offsets = {
        x: reference.x + reference.width,
        y: commonY
      };
      break;

    case _enums.left:
      offsets = {
        x: reference.x - element.width,
        y: commonY
      };
      break;

    default:
      offsets = {
        x: reference.x,
        y: reference.y
      };
  }

  var mainAxis = basePlacement ? (0, _getMainAxisFromPlacement["default"])(basePlacement) : null;

  if (mainAxis != null) {
    var len = mainAxis === 'y' ? 'height' : 'width';

    switch (variation) {
      case _enums.start:
        offsets[mainAxis] = offsets[mainAxis] - (reference[len] / 2 - element[len] / 2);
        break;

      case _enums.end:
        offsets[mainAxis] = offsets[mainAxis] + (reference[len] / 2 - element[len] / 2);
        break;

      default:
    }
  }

  return offsets;
}

/***/ }),

/***/ 68369:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.default = debounce;

function debounce(fn) {
  var pending;
  return function () {
    if (!pending) {
      pending = new Promise(function (resolve) {
        Promise.resolve().then(function () {
          pending = undefined;
          resolve(fn());
        });
      });
    }

    return pending;
  };
}

/***/ }),

/***/ 19574:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.default = detectOverflow;

var _getBoundingClientRect = _interopRequireDefault(__webpack_require__(94294));

var _getClippingRect = _interopRequireDefault(__webpack_require__(48162));

var _getDocumentElement = _interopRequireDefault(__webpack_require__(58386));

var _computeOffsets = _interopRequireDefault(__webpack_require__(17175));

var _rectToClientRect = _interopRequireDefault(__webpack_require__(20935));

var _enums = __webpack_require__(56728);

var _instanceOf = __webpack_require__(67458);

var _mergePaddingObject = _interopRequireDefault(__webpack_require__(32055));

var _expandToHashMap = _interopRequireDefault(__webpack_require__(76294));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

// eslint-disable-next-line import/no-unused-modules
function detectOverflow(state, options) {
  if (options === void 0) {
    options = {};
  }

  var _options = options,
      _options$placement = _options.placement,
      placement = _options$placement === void 0 ? state.placement : _options$placement,
      _options$boundary = _options.boundary,
      boundary = _options$boundary === void 0 ? _enums.clippingParents : _options$boundary,
      _options$rootBoundary = _options.rootBoundary,
      rootBoundary = _options$rootBoundary === void 0 ? _enums.viewport : _options$rootBoundary,
      _options$elementConte = _options.elementContext,
      elementContext = _options$elementConte === void 0 ? _enums.popper : _options$elementConte,
      _options$altBoundary = _options.altBoundary,
      altBoundary = _options$altBoundary === void 0 ? false : _options$altBoundary,
      _options$padding = _options.padding,
      padding = _options$padding === void 0 ? 0 : _options$padding;
  var paddingObject = (0, _mergePaddingObject["default"])(typeof padding !== 'number' ? padding : (0, _expandToHashMap["default"])(padding, _enums.basePlacements));
  var altContext = elementContext === _enums.popper ? _enums.reference : _enums.popper;
  var referenceElement = state.elements.reference;
  var popperRect = state.rects.popper;
  var element = state.elements[altBoundary ? altContext : elementContext];
  var clippingClientRect = (0, _getClippingRect["default"])((0, _instanceOf.isElement)(element) ? element : element.contextElement || (0, _getDocumentElement["default"])(state.elements.popper), boundary, rootBoundary);
  var referenceClientRect = (0, _getBoundingClientRect["default"])(referenceElement);
  var popperOffsets = (0, _computeOffsets["default"])({
    reference: referenceClientRect,
    element: popperRect,
    strategy: 'absolute',
    placement: placement
  });
  var popperClientRect = (0, _rectToClientRect["default"])(Object.assign({}, popperRect, popperOffsets));
  var elementClientRect = elementContext === _enums.popper ? popperClientRect : referenceClientRect; // positive = overflowing the clipping rect
  // 0 or negative = within the clipping rect

  var overflowOffsets = {
    top: clippingClientRect.top - elementClientRect.top + paddingObject.top,
    bottom: elementClientRect.bottom - clippingClientRect.bottom + paddingObject.bottom,
    left: clippingClientRect.left - elementClientRect.left + paddingObject.left,
    right: elementClientRect.right - clippingClientRect.right + paddingObject.right
  };
  var offsetData = state.modifiersData.offset; // Offsets can be applied only to the popper element

  if (elementContext === _enums.popper && offsetData) {
    var offset = offsetData[placement];
    Object.keys(overflowOffsets).forEach(function (key) {
      var multiply = [_enums.right, _enums.bottom].indexOf(key) >= 0 ? 1 : -1;
      var axis = [_enums.top, _enums.bottom].indexOf(key) >= 0 ? 'y' : 'x';
      overflowOffsets[key] += offset[axis] * multiply;
    });
  }

  return overflowOffsets;
}

/***/ }),

/***/ 76294:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.default = expandToHashMap;

function expandToHashMap(value, keys) {
  return keys.reduce(function (hashMap, key) {
    hashMap[key] = value;
    return hashMap;
  }, {});
}

/***/ }),

/***/ 41434:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.default = format;

function format(str) {
  for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    args[_key - 1] = arguments[_key];
  }

  return [].concat(args).reduce(function (p, c) {
    return p.replace(/%s/, c);
  }, str);
}

/***/ }),

/***/ 82898:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.default = getAltAxis;

function getAltAxis(axis) {
  return axis === 'x' ? 'y' : 'x';
}

/***/ }),

/***/ 22709:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.default = getBasePlacement;

var _enums = __webpack_require__(56728);

function getBasePlacement(placement) {
  return placement.split('-')[0];
}

/***/ }),

/***/ 41801:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.default = getFreshSideObject;

function getFreshSideObject() {
  return {
    top: 0,
    right: 0,
    bottom: 0,
    left: 0
  };
}

/***/ }),

/***/ 16615:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.default = getMainAxisFromPlacement;

function getMainAxisFromPlacement(placement) {
  return ['top', 'bottom'].indexOf(placement) >= 0 ? 'x' : 'y';
}

/***/ }),

/***/ 70045:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.default = getOppositePlacement;
var hash = {
  left: 'right',
  right: 'left',
  bottom: 'top',
  top: 'bottom'
};

function getOppositePlacement(placement) {
  return placement.replace(/left|right|bottom|top/g, function (matched) {
    return hash[matched];
  });
}

/***/ }),

/***/ 28916:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.default = getOppositeVariationPlacement;
var hash = {
  start: 'end',
  end: 'start'
};

function getOppositeVariationPlacement(placement) {
  return placement.replace(/start|end/g, function (matched) {
    return hash[matched];
  });
}

/***/ }),

/***/ 45412:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.default = getVariation;

function getVariation(placement) {
  return placement.split('-')[1];
}

/***/ }),

/***/ 62836:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.round = exports.min = exports.max = void 0;
var max = Math.max;
exports.max = max;
var min = Math.min;
exports.min = min;
var round = Math.round;
exports.round = round;

/***/ }),

/***/ 36284:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.default = mergeByName;

function mergeByName(modifiers) {
  var merged = modifiers.reduce(function (merged, current) {
    var existing = merged[current.name];
    merged[current.name] = existing ? Object.assign({}, existing, current, {
      options: Object.assign({}, existing.options, current.options),
      data: Object.assign({}, existing.data, current.data)
    }) : current;
    return merged;
  }, {}); // IE11 does not support Object.values

  return Object.keys(merged).map(function (key) {
    return merged[key];
  });
}

/***/ }),

/***/ 32055:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.default = mergePaddingObject;

var _getFreshSideObject = _interopRequireDefault(__webpack_require__(41801));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

function mergePaddingObject(paddingObject) {
  return Object.assign({}, (0, _getFreshSideObject["default"])(), paddingObject);
}

/***/ }),

/***/ 45700:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.default = orderModifiers;

var _enums = __webpack_require__(56728);

// source: https://stackoverflow.com/questions/49875255
function order(modifiers) {
  var map = new Map();
  var visited = new Set();
  var result = [];
  modifiers.forEach(function (modifier) {
    map.set(modifier.name, modifier);
  }); // On visiting object, check for its dependencies and visit them recursively

  function sort(modifier) {
    visited.add(modifier.name);
    var requires = [].concat(modifier.requires || [], modifier.requiresIfExists || []);
    requires.forEach(function (dep) {
      if (!visited.has(dep)) {
        var depModifier = map.get(dep);

        if (depModifier) {
          sort(depModifier);
        }
      }
    });
    result.push(modifier);
  }

  modifiers.forEach(function (modifier) {
    if (!visited.has(modifier.name)) {
      // check for visited object
      sort(modifier);
    }
  });
  return result;
}

function orderModifiers(modifiers) {
  // order based on dependencies
  var orderedModifiers = order(modifiers); // order based on phase

  return _enums.modifierPhases.reduce(function (acc, phase) {
    return acc.concat(orderedModifiers.filter(function (modifier) {
      return modifier.phase === phase;
    }));
  }, []);
}

/***/ }),

/***/ 20935:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.default = rectToClientRect;

function rectToClientRect(rect) {
  return Object.assign({}, rect, {
    left: rect.x,
    top: rect.y,
    right: rect.x + rect.width,
    bottom: rect.y + rect.height
  });
}

/***/ }),

/***/ 25435:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.default = uniqueBy;

function uniqueBy(arr, fn) {
  var identifiers = new Set();
  return arr.filter(function (item) {
    var identifier = fn(item);

    if (!identifiers.has(identifier)) {
      identifiers.add(identifier);
      return true;
    }
  });
}

/***/ }),

/***/ 38574:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.default = validateModifiers;

var _format = _interopRequireDefault(__webpack_require__(41434));

var _enums = __webpack_require__(56728);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

var INVALID_MODIFIER_ERROR = 'Popper: modifier "%s" provided an invalid %s property, expected %s but got %s';
var MISSING_DEPENDENCY_ERROR = 'Popper: modifier "%s" requires "%s", but "%s" modifier is not available';
var VALID_PROPERTIES = (/* unused pure expression or super */ null && (['name', 'enabled', 'phase', 'fn', 'effect', 'requires', 'options']));

function validateModifiers(modifiers) {
  modifiers.forEach(function (modifier) {
    Object.keys(modifier).forEach(function (key) {
      switch (key) {
        case 'name':
          if (typeof modifier.name !== 'string') {}

          break;

        case 'enabled':
          if (typeof modifier.enabled !== 'boolean') {}

        case 'phase':
          if (_enums.modifierPhases.indexOf(modifier.phase) < 0) {}

          break;

        case 'fn':
          if (typeof modifier.fn !== 'function') {}

          break;

        case 'effect':
          if (typeof modifier.effect !== 'function') {}

          break;

        case 'requires':
          if (!Array.isArray(modifier.requires)) {}

          break;

        case 'requiresIfExists':
          if (!Array.isArray(modifier.requiresIfExists)) {}

          break;

        case 'options':
        case 'data':
          break;

        default:
      }

      modifier.requires && modifier.requires.forEach(function (requirement) {
        if (modifiers.find(function (mod) {
          return mod.name === requirement;
        }) == null) {}
      });
    });
  });
}

/***/ }),

/***/ 52534:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.default = within;

var _math = __webpack_require__(62836);

function within(min, value, max) {
  return (0, _math.max)(min, (0, _math.min)(value, max));
}

/***/ }),

/***/ 83923:
/***/ (function(module) {

"use strict";


module.exports = function (it) {
  if (typeof it != 'function') {
    throw TypeError(String(it) + ' is not a function');
  }

  return it;
};

/***/ }),

/***/ 1372:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var isObject = __webpack_require__(35611);

module.exports = function (it) {
  if (!isObject(it) && it !== null) {
    throw TypeError("Can't set " + String(it) + ' as a prototype');
  }

  return it;
};

/***/ }),

/***/ 36910:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var wellKnownSymbol = __webpack_require__(17657);

var create = __webpack_require__(83681);

var definePropertyModule = __webpack_require__(74217);

var UNSCOPABLES = wellKnownSymbol('unscopables');
var ArrayPrototype = Array.prototype; // Array.prototype[@@unscopables]
// https://tc39.es/ecma262/#sec-array.prototype-@@unscopables

if (ArrayPrototype[UNSCOPABLES] == undefined) {
  definePropertyModule.f(ArrayPrototype, UNSCOPABLES, {
    configurable: true,
    value: create(null)
  });
} // add a key to Array.prototype[@@unscopables]


module.exports = function (key) {
  ArrayPrototype[UNSCOPABLES][key] = true;
};

/***/ }),

/***/ 69953:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var charAt = __webpack_require__(37015).charAt; // `AdvanceStringIndex` abstract operation
// https://tc39.es/ecma262/#sec-advancestringindex


module.exports = function (S, index, unicode) {
  return index + (unicode ? charAt(S, index).length : 1);
};

/***/ }),

/***/ 50997:
/***/ (function(module) {

"use strict";


module.exports = function (it, Constructor, name) {
  if (!(it instanceof Constructor)) {
    throw TypeError('Incorrect ' + (name ? name + ' ' : '') + 'invocation');
  }

  return it;
};

/***/ }),

/***/ 63518:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var isObject = __webpack_require__(35611);

module.exports = function (it) {
  if (!isObject(it)) {
    throw TypeError(String(it) + ' is not an object');
  }

  return it;
};

/***/ }),

/***/ 32977:
/***/ (function(module) {

"use strict";


// eslint-disable-next-line es/no-typed-arrays -- safe
module.exports = typeof ArrayBuffer !== 'undefined' && typeof DataView !== 'undefined';

/***/ }),

/***/ 19216:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var NATIVE_ARRAY_BUFFER = __webpack_require__(32977);

var DESCRIPTORS = __webpack_require__(75592);

var global = __webpack_require__(84253);

var isObject = __webpack_require__(35611);

var has = __webpack_require__(28919);

var classof = __webpack_require__(47953);

var createNonEnumerableProperty = __webpack_require__(52188);

var redefine = __webpack_require__(11807);

var defineProperty = __webpack_require__(74217).f;

var getPrototypeOf = __webpack_require__(6863);

var setPrototypeOf = __webpack_require__(18444);

var wellKnownSymbol = __webpack_require__(17657);

var uid = __webpack_require__(41080);

var Int8Array = global.Int8Array;
var Int8ArrayPrototype = Int8Array && Int8Array.prototype;
var Uint8ClampedArray = global.Uint8ClampedArray;
var Uint8ClampedArrayPrototype = Uint8ClampedArray && Uint8ClampedArray.prototype;
var TypedArray = Int8Array && getPrototypeOf(Int8Array);
var TypedArrayPrototype = Int8ArrayPrototype && getPrototypeOf(Int8ArrayPrototype);
var ObjectPrototype = Object.prototype;
var isPrototypeOf = ObjectPrototype.isPrototypeOf;
var TO_STRING_TAG = wellKnownSymbol('toStringTag');
var TYPED_ARRAY_TAG = uid('TYPED_ARRAY_TAG');
var TYPED_ARRAY_CONSTRUCTOR = uid('TYPED_ARRAY_CONSTRUCTOR'); // Fixing native typed arrays in Opera Presto crashes the browser, see #595

var NATIVE_ARRAY_BUFFER_VIEWS = NATIVE_ARRAY_BUFFER && !!setPrototypeOf && classof(global.opera) !== 'Opera';
var TYPED_ARRAY_TAG_REQIRED = false;
var NAME, Constructor, Prototype;
var TypedArrayConstructorsList = {
  Int8Array: 1,
  Uint8Array: 1,
  Uint8ClampedArray: 1,
  Int16Array: 2,
  Uint16Array: 2,
  Int32Array: 4,
  Uint32Array: 4,
  Float32Array: 4,
  Float64Array: 8
};
var BigIntArrayConstructorsList = {
  BigInt64Array: 8,
  BigUint64Array: 8
};

var isView = function () {
  function isView(it) {
    if (!isObject(it)) return false;
    var klass = classof(it);
    return klass === 'DataView' || has(TypedArrayConstructorsList, klass) || has(BigIntArrayConstructorsList, klass);
  }

  return isView;
}();

var isTypedArray = function isTypedArray(it) {
  if (!isObject(it)) return false;
  var klass = classof(it);
  return has(TypedArrayConstructorsList, klass) || has(BigIntArrayConstructorsList, klass);
};

var aTypedArray = function aTypedArray(it) {
  if (isTypedArray(it)) return it;
  throw TypeError('Target is not a typed array');
};

var aTypedArrayConstructor = function aTypedArrayConstructor(C) {
  if (setPrototypeOf && !isPrototypeOf.call(TypedArray, C)) {
    throw TypeError('Target is not a typed array constructor');
  }

  return C;
};

var exportTypedArrayMethod = function exportTypedArrayMethod(KEY, property, forced) {
  if (!DESCRIPTORS) return;
  if (forced) for (var ARRAY in TypedArrayConstructorsList) {
    var TypedArrayConstructor = global[ARRAY];
    if (TypedArrayConstructor && has(TypedArrayConstructor.prototype, KEY)) try {
      delete TypedArrayConstructor.prototype[KEY];
    } catch (error) {
      /* empty */
    }
  }

  if (!TypedArrayPrototype[KEY] || forced) {
    redefine(TypedArrayPrototype, KEY, forced ? property : NATIVE_ARRAY_BUFFER_VIEWS && Int8ArrayPrototype[KEY] || property);
  }
};

var exportTypedArrayStaticMethod = function exportTypedArrayStaticMethod(KEY, property, forced) {
  var ARRAY, TypedArrayConstructor;
  if (!DESCRIPTORS) return;

  if (setPrototypeOf) {
    if (forced) for (ARRAY in TypedArrayConstructorsList) {
      TypedArrayConstructor = global[ARRAY];
      if (TypedArrayConstructor && has(TypedArrayConstructor, KEY)) try {
        delete TypedArrayConstructor[KEY];
      } catch (error) {
        /* empty */
      }
    }

    if (!TypedArray[KEY] || forced) {
      // V8 ~ Chrome 49-50 `%TypedArray%` methods are non-writable non-configurable
      try {
        return redefine(TypedArray, KEY, forced ? property : NATIVE_ARRAY_BUFFER_VIEWS && TypedArray[KEY] || property);
      } catch (error) {
        /* empty */
      }
    } else return;
  }

  for (ARRAY in TypedArrayConstructorsList) {
    TypedArrayConstructor = global[ARRAY];

    if (TypedArrayConstructor && (!TypedArrayConstructor[KEY] || forced)) {
      redefine(TypedArrayConstructor, KEY, property);
    }
  }
};

for (NAME in TypedArrayConstructorsList) {
  Constructor = global[NAME];
  Prototype = Constructor && Constructor.prototype;
  if (Prototype) createNonEnumerableProperty(Prototype, TYPED_ARRAY_CONSTRUCTOR, Constructor);else NATIVE_ARRAY_BUFFER_VIEWS = false;
}

for (NAME in BigIntArrayConstructorsList) {
  Constructor = global[NAME];
  Prototype = Constructor && Constructor.prototype;
  if (Prototype) createNonEnumerableProperty(Prototype, TYPED_ARRAY_CONSTRUCTOR, Constructor);
} // WebKit bug - typed arrays constructors prototype is Object.prototype


if (!NATIVE_ARRAY_BUFFER_VIEWS || typeof TypedArray != 'function' || TypedArray === Function.prototype) {
  // eslint-disable-next-line no-shadow -- safe
  TypedArray = function () {
    function TypedArray() {
      throw TypeError('Incorrect invocation');
    }

    return TypedArray;
  }();

  if (NATIVE_ARRAY_BUFFER_VIEWS) for (NAME in TypedArrayConstructorsList) {
    if (global[NAME]) setPrototypeOf(global[NAME], TypedArray);
  }
}

if (!NATIVE_ARRAY_BUFFER_VIEWS || !TypedArrayPrototype || TypedArrayPrototype === ObjectPrototype) {
  TypedArrayPrototype = TypedArray.prototype;
  if (NATIVE_ARRAY_BUFFER_VIEWS) for (NAME in TypedArrayConstructorsList) {
    if (global[NAME]) setPrototypeOf(global[NAME].prototype, TypedArrayPrototype);
  }
} // WebKit bug - one more object in Uint8ClampedArray prototype chain


if (NATIVE_ARRAY_BUFFER_VIEWS && getPrototypeOf(Uint8ClampedArrayPrototype) !== TypedArrayPrototype) {
  setPrototypeOf(Uint8ClampedArrayPrototype, TypedArrayPrototype);
}

if (DESCRIPTORS && !has(TypedArrayPrototype, TO_STRING_TAG)) {
  TYPED_ARRAY_TAG_REQIRED = true;
  defineProperty(TypedArrayPrototype, TO_STRING_TAG, {
    get: function () {
      function get() {
        return isObject(this) ? this[TYPED_ARRAY_TAG] : undefined;
      }

      return get;
    }()
  });

  for (NAME in TypedArrayConstructorsList) {
    if (global[NAME]) {
      createNonEnumerableProperty(global[NAME], TYPED_ARRAY_TAG, NAME);
    }
  }
}

module.exports = {
  NATIVE_ARRAY_BUFFER_VIEWS: NATIVE_ARRAY_BUFFER_VIEWS,
  TYPED_ARRAY_CONSTRUCTOR: TYPED_ARRAY_CONSTRUCTOR,
  TYPED_ARRAY_TAG: TYPED_ARRAY_TAG_REQIRED && TYPED_ARRAY_TAG,
  aTypedArray: aTypedArray,
  aTypedArrayConstructor: aTypedArrayConstructor,
  exportTypedArrayMethod: exportTypedArrayMethod,
  exportTypedArrayStaticMethod: exportTypedArrayStaticMethod,
  isView: isView,
  isTypedArray: isTypedArray,
  TypedArray: TypedArray,
  TypedArrayPrototype: TypedArrayPrototype
};

/***/ }),

/***/ 80246:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var global = __webpack_require__(84253);

var DESCRIPTORS = __webpack_require__(75592);

var NATIVE_ARRAY_BUFFER = __webpack_require__(32977);

var createNonEnumerableProperty = __webpack_require__(52188);

var redefineAll = __webpack_require__(3993);

var fails = __webpack_require__(69691);

var anInstance = __webpack_require__(50997);

var toInteger = __webpack_require__(3997);

var toLength = __webpack_require__(39570);

var toIndex = __webpack_require__(77559);

var IEEE754 = __webpack_require__(16567);

var getPrototypeOf = __webpack_require__(6863);

var setPrototypeOf = __webpack_require__(18444);

var getOwnPropertyNames = __webpack_require__(78870).f;

var defineProperty = __webpack_require__(74217).f;

var arrayFill = __webpack_require__(22051);

var setToStringTag = __webpack_require__(29108);

var InternalStateModule = __webpack_require__(79947);

var getInternalState = InternalStateModule.get;
var setInternalState = InternalStateModule.set;
var ARRAY_BUFFER = 'ArrayBuffer';
var DATA_VIEW = 'DataView';
var PROTOTYPE = 'prototype';
var WRONG_LENGTH = 'Wrong length';
var WRONG_INDEX = 'Wrong index';
var NativeArrayBuffer = global[ARRAY_BUFFER];
var $ArrayBuffer = NativeArrayBuffer;
var $DataView = global[DATA_VIEW];
var $DataViewPrototype = $DataView && $DataView[PROTOTYPE];
var ObjectPrototype = Object.prototype;
var RangeError = global.RangeError;
var packIEEE754 = IEEE754.pack;
var unpackIEEE754 = IEEE754.unpack;

var packInt8 = function packInt8(number) {
  return [number & 0xFF];
};

var packInt16 = function packInt16(number) {
  return [number & 0xFF, number >> 8 & 0xFF];
};

var packInt32 = function packInt32(number) {
  return [number & 0xFF, number >> 8 & 0xFF, number >> 16 & 0xFF, number >> 24 & 0xFF];
};

var unpackInt32 = function unpackInt32(buffer) {
  return buffer[3] << 24 | buffer[2] << 16 | buffer[1] << 8 | buffer[0];
};

var packFloat32 = function packFloat32(number) {
  return packIEEE754(number, 23, 4);
};

var packFloat64 = function packFloat64(number) {
  return packIEEE754(number, 52, 8);
};

var addGetter = function addGetter(Constructor, key) {
  defineProperty(Constructor[PROTOTYPE], key, {
    get: function () {
      function get() {
        return getInternalState(this)[key];
      }

      return get;
    }()
  });
};

var get = function get(view, count, index, isLittleEndian) {
  var intIndex = toIndex(index);
  var store = getInternalState(view);
  if (intIndex + count > store.byteLength) throw RangeError(WRONG_INDEX);
  var bytes = getInternalState(store.buffer).bytes;
  var start = intIndex + store.byteOffset;
  var pack = bytes.slice(start, start + count);
  return isLittleEndian ? pack : pack.reverse();
};

var set = function set(view, count, index, conversion, value, isLittleEndian) {
  var intIndex = toIndex(index);
  var store = getInternalState(view);
  if (intIndex + count > store.byteLength) throw RangeError(WRONG_INDEX);
  var bytes = getInternalState(store.buffer).bytes;
  var start = intIndex + store.byteOffset;
  var pack = conversion(+value);

  for (var i = 0; i < count; i++) {
    bytes[start + i] = pack[isLittleEndian ? i : count - i - 1];
  }
};

if (!NATIVE_ARRAY_BUFFER) {
  $ArrayBuffer = function () {
    function ArrayBuffer(length) {
      anInstance(this, $ArrayBuffer, ARRAY_BUFFER);
      var byteLength = toIndex(length);
      setInternalState(this, {
        bytes: arrayFill.call(new Array(byteLength), 0),
        byteLength: byteLength
      });
      if (!DESCRIPTORS) this.byteLength = byteLength;
    }

    return ArrayBuffer;
  }();

  $DataView = function () {
    function DataView(buffer, byteOffset, byteLength) {
      anInstance(this, $DataView, DATA_VIEW);
      anInstance(buffer, $ArrayBuffer, DATA_VIEW);
      var bufferLength = getInternalState(buffer).byteLength;
      var offset = toInteger(byteOffset);
      if (offset < 0 || offset > bufferLength) throw RangeError('Wrong offset');
      byteLength = byteLength === undefined ? bufferLength - offset : toLength(byteLength);
      if (offset + byteLength > bufferLength) throw RangeError(WRONG_LENGTH);
      setInternalState(this, {
        buffer: buffer,
        byteLength: byteLength,
        byteOffset: offset
      });

      if (!DESCRIPTORS) {
        this.buffer = buffer;
        this.byteLength = byteLength;
        this.byteOffset = offset;
      }
    }

    return DataView;
  }();

  if (DESCRIPTORS) {
    addGetter($ArrayBuffer, 'byteLength');
    addGetter($DataView, 'buffer');
    addGetter($DataView, 'byteLength');
    addGetter($DataView, 'byteOffset');
  }

  redefineAll($DataView[PROTOTYPE], {
    getInt8: function () {
      function getInt8(byteOffset) {
        return get(this, 1, byteOffset)[0] << 24 >> 24;
      }

      return getInt8;
    }(),
    getUint8: function () {
      function getUint8(byteOffset) {
        return get(this, 1, byteOffset)[0];
      }

      return getUint8;
    }(),
    getInt16: function () {
      function getInt16(byteOffset
      /* , littleEndian */
      ) {
        var bytes = get(this, 2, byteOffset, arguments.length > 1 ? arguments[1] : undefined);
        return (bytes[1] << 8 | bytes[0]) << 16 >> 16;
      }

      return getInt16;
    }(),
    getUint16: function () {
      function getUint16(byteOffset
      /* , littleEndian */
      ) {
        var bytes = get(this, 2, byteOffset, arguments.length > 1 ? arguments[1] : undefined);
        return bytes[1] << 8 | bytes[0];
      }

      return getUint16;
    }(),
    getInt32: function () {
      function getInt32(byteOffset
      /* , littleEndian */
      ) {
        return unpackInt32(get(this, 4, byteOffset, arguments.length > 1 ? arguments[1] : undefined));
      }

      return getInt32;
    }(),
    getUint32: function () {
      function getUint32(byteOffset
      /* , littleEndian */
      ) {
        return unpackInt32(get(this, 4, byteOffset, arguments.length > 1 ? arguments[1] : undefined)) >>> 0;
      }

      return getUint32;
    }(),
    getFloat32: function () {
      function getFloat32(byteOffset
      /* , littleEndian */
      ) {
        return unpackIEEE754(get(this, 4, byteOffset, arguments.length > 1 ? arguments[1] : undefined), 23);
      }

      return getFloat32;
    }(),
    getFloat64: function () {
      function getFloat64(byteOffset
      /* , littleEndian */
      ) {
        return unpackIEEE754(get(this, 8, byteOffset, arguments.length > 1 ? arguments[1] : undefined), 52);
      }

      return getFloat64;
    }(),
    setInt8: function () {
      function setInt8(byteOffset, value) {
        set(this, 1, byteOffset, packInt8, value);
      }

      return setInt8;
    }(),
    setUint8: function () {
      function setUint8(byteOffset, value) {
        set(this, 1, byteOffset, packInt8, value);
      }

      return setUint8;
    }(),
    setInt16: function () {
      function setInt16(byteOffset, value
      /* , littleEndian */
      ) {
        set(this, 2, byteOffset, packInt16, value, arguments.length > 2 ? arguments[2] : undefined);
      }

      return setInt16;
    }(),
    setUint16: function () {
      function setUint16(byteOffset, value
      /* , littleEndian */
      ) {
        set(this, 2, byteOffset, packInt16, value, arguments.length > 2 ? arguments[2] : undefined);
      }

      return setUint16;
    }(),
    setInt32: function () {
      function setInt32(byteOffset, value
      /* , littleEndian */
      ) {
        set(this, 4, byteOffset, packInt32, value, arguments.length > 2 ? arguments[2] : undefined);
      }

      return setInt32;
    }(),
    setUint32: function () {
      function setUint32(byteOffset, value
      /* , littleEndian */
      ) {
        set(this, 4, byteOffset, packInt32, value, arguments.length > 2 ? arguments[2] : undefined);
      }

      return setUint32;
    }(),
    setFloat32: function () {
      function setFloat32(byteOffset, value
      /* , littleEndian */
      ) {
        set(this, 4, byteOffset, packFloat32, value, arguments.length > 2 ? arguments[2] : undefined);
      }

      return setFloat32;
    }(),
    setFloat64: function () {
      function setFloat64(byteOffset, value
      /* , littleEndian */
      ) {
        set(this, 8, byteOffset, packFloat64, value, arguments.length > 2 ? arguments[2] : undefined);
      }

      return setFloat64;
    }()
  });
} else {
  /* eslint-disable no-new -- required for testing */
  if (!fails(function () {
    NativeArrayBuffer(1);
  }) || !fails(function () {
    new NativeArrayBuffer(-1);
  }) || fails(function () {
    new NativeArrayBuffer();
    new NativeArrayBuffer(1.5);
    new NativeArrayBuffer(NaN);
    return NativeArrayBuffer.name != ARRAY_BUFFER;
  })) {
    /* eslint-enable no-new -- required for testing */
    $ArrayBuffer = function () {
      function ArrayBuffer(length) {
        anInstance(this, $ArrayBuffer);
        return new NativeArrayBuffer(toIndex(length));
      }

      return ArrayBuffer;
    }();

    var ArrayBufferPrototype = $ArrayBuffer[PROTOTYPE] = NativeArrayBuffer[PROTOTYPE];

    for (var keys = getOwnPropertyNames(NativeArrayBuffer), j = 0, key; keys.length > j;) {
      if (!((key = keys[j++]) in $ArrayBuffer)) {
        createNonEnumerableProperty($ArrayBuffer, key, NativeArrayBuffer[key]);
      }
    }

    ArrayBufferPrototype.constructor = $ArrayBuffer;
  } // WebKit bug - the same parent prototype for typed arrays and data view


  if (setPrototypeOf && getPrototypeOf($DataViewPrototype) !== ObjectPrototype) {
    setPrototypeOf($DataViewPrototype, ObjectPrototype);
  } // iOS Safari 7.x bug


  var testView = new $DataView(new $ArrayBuffer(2));
  var $setInt8 = $DataViewPrototype.setInt8;
  testView.setInt8(0, 2147483648);
  testView.setInt8(1, 2147483649);
  if (testView.getInt8(0) || !testView.getInt8(1)) redefineAll($DataViewPrototype, {
    setInt8: function () {
      function setInt8(byteOffset, value) {
        $setInt8.call(this, byteOffset, value << 24 >> 24);
      }

      return setInt8;
    }(),
    setUint8: function () {
      function setUint8(byteOffset, value) {
        $setInt8.call(this, byteOffset, value << 24 >> 24);
      }

      return setUint8;
    }()
  }, {
    unsafe: true
  });
}

setToStringTag($ArrayBuffer, ARRAY_BUFFER);
setToStringTag($DataView, DATA_VIEW);
module.exports = {
  ArrayBuffer: $ArrayBuffer,
  DataView: $DataView
};

/***/ }),

/***/ 59571:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var toObject = __webpack_require__(12104);

var toAbsoluteIndex = __webpack_require__(55205);

var toLength = __webpack_require__(39570);

var min = Math.min; // `Array.prototype.copyWithin` method implementation
// https://tc39.es/ecma262/#sec-array.prototype.copywithin
// eslint-disable-next-line es/no-array-prototype-copywithin -- safe

module.exports = [].copyWithin || function () {
  function copyWithin(target
  /* = 0 */
  , start
  /* = 0, end = @length */
  ) {
    var O = toObject(this);
    var len = toLength(O.length);
    var to = toAbsoluteIndex(target, len);
    var from = toAbsoluteIndex(start, len);
    var end = arguments.length > 2 ? arguments[2] : undefined;
    var count = min((end === undefined ? len : toAbsoluteIndex(end, len)) - from, len - to);
    var inc = 1;

    if (from < to && to < from + count) {
      inc = -1;
      from += count - 1;
      to += count - 1;
    }

    while (count-- > 0) {
      if (from in O) O[to] = O[from];else delete O[to];
      to += inc;
      from += inc;
    }

    return O;
  }

  return copyWithin;
}();

/***/ }),

/***/ 22051:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var toObject = __webpack_require__(12104);

var toAbsoluteIndex = __webpack_require__(55205);

var toLength = __webpack_require__(39570); // `Array.prototype.fill` method implementation
// https://tc39.es/ecma262/#sec-array.prototype.fill


module.exports = function () {
  function fill(value
  /* , start = 0, end = @length */
  ) {
    var O = toObject(this);
    var length = toLength(O.length);
    var argumentsLength = arguments.length;
    var index = toAbsoluteIndex(argumentsLength > 1 ? arguments[1] : undefined, length);
    var end = argumentsLength > 2 ? arguments[2] : undefined;
    var endPos = end === undefined ? length : toAbsoluteIndex(end, length);

    while (endPos > index) {
      O[index++] = value;
    }

    return O;
  }

  return fill;
}();

/***/ }),

/***/ 90369:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $forEach = __webpack_require__(69033).forEach;

var arrayMethodIsStrict = __webpack_require__(37902);

var STRICT_METHOD = arrayMethodIsStrict('forEach'); // `Array.prototype.forEach` method implementation
// https://tc39.es/ecma262/#sec-array.prototype.foreach

module.exports = !STRICT_METHOD ? function () {
  function forEach(callbackfn
  /* , thisArg */
  ) {
    return $forEach(this, callbackfn, arguments.length > 1 ? arguments[1] : undefined); // eslint-disable-next-line es/no-array-prototype-foreach -- safe
  }

  return forEach;
}() : [].forEach;

/***/ }),

/***/ 11955:
/***/ (function(module) {

"use strict";


module.exports = function (Constructor, list) {
  var index = 0;
  var length = list.length;
  var result = new Constructor(length);

  while (length > index) {
    result[index] = list[index++];
  }

  return result;
};

/***/ }),

/***/ 28213:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var bind = __webpack_require__(74271);

var toObject = __webpack_require__(12104);

var callWithSafeIterationClosing = __webpack_require__(32648);

var isArrayIteratorMethod = __webpack_require__(14195);

var toLength = __webpack_require__(39570);

var createProperty = __webpack_require__(39241);

var getIteratorMethod = __webpack_require__(3170); // `Array.from` method implementation
// https://tc39.es/ecma262/#sec-array.from


module.exports = function () {
  function from(arrayLike
  /* , mapfn = undefined, thisArg = undefined */
  ) {
    var O = toObject(arrayLike);
    var C = typeof this == 'function' ? this : Array;
    var argumentsLength = arguments.length;
    var mapfn = argumentsLength > 1 ? arguments[1] : undefined;
    var mapping = mapfn !== undefined;
    var iteratorMethod = getIteratorMethod(O);
    var index = 0;
    var length, result, step, iterator, next, value;
    if (mapping) mapfn = bind(mapfn, argumentsLength > 2 ? arguments[2] : undefined, 2); // if the target is not iterable or it's an array with the default iterator - use a simple case

    if (iteratorMethod != undefined && !(C == Array && isArrayIteratorMethod(iteratorMethod))) {
      iterator = iteratorMethod.call(O);
      next = iterator.next;
      result = new C();

      for (; !(step = next.call(iterator)).done; index++) {
        value = mapping ? callWithSafeIterationClosing(iterator, mapfn, [step.value, index], true) : step.value;
        createProperty(result, index, value);
      }
    } else {
      length = toLength(O.length);
      result = new C(length);

      for (; length > index; index++) {
        value = mapping ? mapfn(O[index], index) : O[index];
        createProperty(result, index, value);
      }
    }

    result.length = index;
    return result;
  }

  return from;
}();

/***/ }),

/***/ 25945:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var toIndexedObject = __webpack_require__(26553);

var toLength = __webpack_require__(39570);

var toAbsoluteIndex = __webpack_require__(55205); // `Array.prototype.{ indexOf, includes }` methods implementation


var createMethod = function createMethod(IS_INCLUDES) {
  return function ($this, el, fromIndex) {
    var O = toIndexedObject($this);
    var length = toLength(O.length);
    var index = toAbsoluteIndex(fromIndex, length);
    var value; // Array#includes uses SameValueZero equality algorithm
    // eslint-disable-next-line no-self-compare -- NaN check

    if (IS_INCLUDES && el != el) while (length > index) {
      value = O[index++]; // eslint-disable-next-line no-self-compare -- NaN check

      if (value != value) return true; // Array#indexOf ignores holes, Array#includes - not
    } else for (; length > index; index++) {
      if ((IS_INCLUDES || index in O) && O[index] === el) return IS_INCLUDES || index || 0;
    }
    return !IS_INCLUDES && -1;
  };
};

module.exports = {
  // `Array.prototype.includes` method
  // https://tc39.es/ecma262/#sec-array.prototype.includes
  includes: createMethod(true),
  // `Array.prototype.indexOf` method
  // https://tc39.es/ecma262/#sec-array.prototype.indexof
  indexOf: createMethod(false)
};

/***/ }),

/***/ 69033:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var bind = __webpack_require__(74271);

var IndexedObject = __webpack_require__(50990);

var toObject = __webpack_require__(12104);

var toLength = __webpack_require__(39570);

var arraySpeciesCreate = __webpack_require__(77454);

var push = [].push; // `Array.prototype.{ forEach, map, filter, some, every, find, findIndex, filterReject }` methods implementation

var createMethod = function createMethod(TYPE) {
  var IS_MAP = TYPE == 1;
  var IS_FILTER = TYPE == 2;
  var IS_SOME = TYPE == 3;
  var IS_EVERY = TYPE == 4;
  var IS_FIND_INDEX = TYPE == 6;
  var IS_FILTER_REJECT = TYPE == 7;
  var NO_HOLES = TYPE == 5 || IS_FIND_INDEX;
  return function ($this, callbackfn, that, specificCreate) {
    var O = toObject($this);
    var self = IndexedObject(O);
    var boundFunction = bind(callbackfn, that, 3);
    var length = toLength(self.length);
    var index = 0;
    var create = specificCreate || arraySpeciesCreate;
    var target = IS_MAP ? create($this, length) : IS_FILTER || IS_FILTER_REJECT ? create($this, 0) : undefined;
    var value, result;

    for (; length > index; index++) {
      if (NO_HOLES || index in self) {
        value = self[index];
        result = boundFunction(value, index, O);

        if (TYPE) {
          if (IS_MAP) target[index] = result; // map
          else if (result) switch (TYPE) {
            case 3:
              return true;
            // some

            case 5:
              return value;
            // find

            case 6:
              return index;
            // findIndex

            case 2:
              push.call(target, value);
            // filter
          } else switch (TYPE) {
            case 4:
              return false;
            // every

            case 7:
              push.call(target, value);
            // filterReject
          }
        }
      }
    }

    return IS_FIND_INDEX ? -1 : IS_SOME || IS_EVERY ? IS_EVERY : target;
  };
};

module.exports = {
  // `Array.prototype.forEach` method
  // https://tc39.es/ecma262/#sec-array.prototype.foreach
  forEach: createMethod(0),
  // `Array.prototype.map` method
  // https://tc39.es/ecma262/#sec-array.prototype.map
  map: createMethod(1),
  // `Array.prototype.filter` method
  // https://tc39.es/ecma262/#sec-array.prototype.filter
  filter: createMethod(2),
  // `Array.prototype.some` method
  // https://tc39.es/ecma262/#sec-array.prototype.some
  some: createMethod(3),
  // `Array.prototype.every` method
  // https://tc39.es/ecma262/#sec-array.prototype.every
  every: createMethod(4),
  // `Array.prototype.find` method
  // https://tc39.es/ecma262/#sec-array.prototype.find
  find: createMethod(5),
  // `Array.prototype.findIndex` method
  // https://tc39.es/ecma262/#sec-array.prototype.findIndex
  findIndex: createMethod(6),
  // `Array.prototype.filterReject` method
  // https://github.com/tc39/proposal-array-filtering
  filterReject: createMethod(7)
};

/***/ }),

/***/ 41183:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";

/* eslint-disable es/no-array-prototype-lastindexof -- safe */

var toIndexedObject = __webpack_require__(26553);

var toInteger = __webpack_require__(3997);

var toLength = __webpack_require__(39570);

var arrayMethodIsStrict = __webpack_require__(37902);

var min = Math.min;
var $lastIndexOf = [].lastIndexOf;
var NEGATIVE_ZERO = !!$lastIndexOf && 1 / [1].lastIndexOf(1, -0) < 0;
var STRICT_METHOD = arrayMethodIsStrict('lastIndexOf');
var FORCED = NEGATIVE_ZERO || !STRICT_METHOD; // `Array.prototype.lastIndexOf` method implementation
// https://tc39.es/ecma262/#sec-array.prototype.lastindexof

module.exports = FORCED ? function () {
  function lastIndexOf(searchElement
  /* , fromIndex = @[*-1] */
  ) {
    // convert -0 to +0
    if (NEGATIVE_ZERO) return $lastIndexOf.apply(this, arguments) || 0;
    var O = toIndexedObject(this);
    var length = toLength(O.length);
    var index = length - 1;
    if (arguments.length > 1) index = min(index, toInteger(arguments[1]));
    if (index < 0) index = length + index;

    for (; index >= 0; index--) {
      if (index in O && O[index] === searchElement) return index || 0;
    }

    return -1;
  }

  return lastIndexOf;
}() : $lastIndexOf;

/***/ }),

/***/ 93537:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var fails = __webpack_require__(69691);

var wellKnownSymbol = __webpack_require__(17657);

var V8_VERSION = __webpack_require__(12338);

var SPECIES = wellKnownSymbol('species');

module.exports = function (METHOD_NAME) {
  // We can't use this feature detection in V8 since it causes
  // deoptimization and serious performance degradation
  // https://github.com/zloirock/core-js/issues/677
  return V8_VERSION >= 51 || !fails(function () {
    var array = [];
    var constructor = array.constructor = {};

    constructor[SPECIES] = function () {
      return {
        foo: 1
      };
    };

    return array[METHOD_NAME](Boolean).foo !== 1;
  });
};

/***/ }),

/***/ 37902:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var fails = __webpack_require__(69691);

module.exports = function (METHOD_NAME, argument) {
  var method = [][METHOD_NAME];
  return !!method && fails(function () {
    // eslint-disable-next-line no-useless-call,no-throw-literal -- required for testing
    method.call(null, argument || function () {
      throw 1;
    }, 1);
  });
};

/***/ }),

/***/ 28249:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var aFunction = __webpack_require__(83923);

var toObject = __webpack_require__(12104);

var IndexedObject = __webpack_require__(50990);

var toLength = __webpack_require__(39570); // `Array.prototype.{ reduce, reduceRight }` methods implementation


var createMethod = function createMethod(IS_RIGHT) {
  return function (that, callbackfn, argumentsLength, memo) {
    aFunction(callbackfn);
    var O = toObject(that);
    var self = IndexedObject(O);
    var length = toLength(O.length);
    var index = IS_RIGHT ? length - 1 : 0;
    var i = IS_RIGHT ? -1 : 1;
    if (argumentsLength < 2) while (true) {
      if (index in self) {
        memo = self[index];
        index += i;
        break;
      }

      index += i;

      if (IS_RIGHT ? index < 0 : length <= index) {
        throw TypeError('Reduce of empty array with no initial value');
      }
    }

    for (; IS_RIGHT ? index >= 0 : length > index; index += i) {
      if (index in self) {
        memo = callbackfn(memo, self[index], index, O);
      }
    }

    return memo;
  };
};

module.exports = {
  // `Array.prototype.reduce` method
  // https://tc39.es/ecma262/#sec-array.prototype.reduce
  left: createMethod(false),
  // `Array.prototype.reduceRight` method
  // https://tc39.es/ecma262/#sec-array.prototype.reduceright
  right: createMethod(true)
};

/***/ }),

/***/ 58737:
/***/ (function(module) {

"use strict";


// TODO: use something more complex like timsort?
var floor = Math.floor;

var mergeSort = function mergeSort(array, comparefn) {
  var length = array.length;
  var middle = floor(length / 2);
  return length < 8 ? insertionSort(array, comparefn) : merge(mergeSort(array.slice(0, middle), comparefn), mergeSort(array.slice(middle), comparefn), comparefn);
};

var insertionSort = function insertionSort(array, comparefn) {
  var length = array.length;
  var i = 1;
  var element, j;

  while (i < length) {
    j = i;
    element = array[i];

    while (j && comparefn(array[j - 1], element) > 0) {
      array[j] = array[--j];
    }

    if (j !== i++) array[j] = element;
  }

  return array;
};

var merge = function merge(left, right, comparefn) {
  var llength = left.length;
  var rlength = right.length;
  var lindex = 0;
  var rindex = 0;
  var result = [];

  while (lindex < llength || rindex < rlength) {
    if (lindex < llength && rindex < rlength) {
      result.push(comparefn(left[lindex], right[rindex]) <= 0 ? left[lindex++] : right[rindex++]);
    } else {
      result.push(lindex < llength ? left[lindex++] : right[rindex++]);
    }
  }

  return result;
};

module.exports = mergeSort;

/***/ }),

/***/ 73612:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var isObject = __webpack_require__(35611);

var isArray = __webpack_require__(89999);

var wellKnownSymbol = __webpack_require__(17657);

var SPECIES = wellKnownSymbol('species'); // a part of `ArraySpeciesCreate` abstract operation
// https://tc39.es/ecma262/#sec-arrayspeciescreate

module.exports = function (originalArray) {
  var C;

  if (isArray(originalArray)) {
    C = originalArray.constructor; // cross-realm fallback

    if (typeof C == 'function' && (C === Array || isArray(C.prototype))) C = undefined;else if (isObject(C)) {
      C = C[SPECIES];
      if (C === null) C = undefined;
    }
  }

  return C === undefined ? Array : C;
};

/***/ }),

/***/ 77454:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var arraySpeciesConstructor = __webpack_require__(73612); // `ArraySpeciesCreate` abstract operation
// https://tc39.es/ecma262/#sec-arrayspeciescreate


module.exports = function (originalArray, length) {
  return new (arraySpeciesConstructor(originalArray))(length === 0 ? 0 : length);
};

/***/ }),

/***/ 32648:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var anObject = __webpack_require__(63518);

var iteratorClose = __webpack_require__(61781); // call something on iterator step with safe closing on error


module.exports = function (iterator, fn, value, ENTRIES) {
  try {
    return ENTRIES ? fn(anObject(value)[0], value[1]) : fn(value);
  } catch (error) {
    iteratorClose(iterator);
    throw error;
  }
};

/***/ }),

/***/ 68762:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var wellKnownSymbol = __webpack_require__(17657);

var ITERATOR = wellKnownSymbol('iterator');
var SAFE_CLOSING = false;

try {
  var called = 0;
  var iteratorWithReturn = {
    next: function () {
      function next() {
        return {
          done: !!called++
        };
      }

      return next;
    }(),
    'return': function () {
      function _return() {
        SAFE_CLOSING = true;
      }

      return _return;
    }()
  };

  iteratorWithReturn[ITERATOR] = function () {
    return this;
  }; // eslint-disable-next-line es/no-array-from, no-throw-literal -- required for testing


  Array.from(iteratorWithReturn, function () {
    throw 2;
  });
} catch (error) {
  /* empty */
}

module.exports = function (exec, SKIP_CLOSING) {
  if (!SKIP_CLOSING && !SAFE_CLOSING) return false;
  var ITERATION_SUPPORT = false;

  try {
    var object = {};

    object[ITERATOR] = function () {
      return {
        next: function () {
          function next() {
            return {
              done: ITERATION_SUPPORT = true
            };
          }

          return next;
        }()
      };
    };

    exec(object);
  } catch (error) {
    /* empty */
  }

  return ITERATION_SUPPORT;
};

/***/ }),

/***/ 77000:
/***/ (function(module) {

"use strict";


var toString = {}.toString;

module.exports = function (it) {
  return toString.call(it).slice(8, -1);
};

/***/ }),

/***/ 47953:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var TO_STRING_TAG_SUPPORT = __webpack_require__(64910);

var classofRaw = __webpack_require__(77000);

var wellKnownSymbol = __webpack_require__(17657);

var TO_STRING_TAG = wellKnownSymbol('toStringTag'); // ES3 wrong here

var CORRECT_ARGUMENTS = classofRaw(function () {
  return arguments;
}()) == 'Arguments'; // fallback for IE11 Script Access Denied error

var tryGet = function tryGet(it, key) {
  try {
    return it[key];
  } catch (error) {
    /* empty */
  }
}; // getting tag from ES6+ `Object.prototype.toString`


module.exports = TO_STRING_TAG_SUPPORT ? classofRaw : function (it) {
  var O, tag, result;
  return it === undefined ? 'Undefined' : it === null ? 'Null' // @@toStringTag case
  : typeof (tag = tryGet(O = Object(it), TO_STRING_TAG)) == 'string' ? tag // builtinTag case
  : CORRECT_ARGUMENTS ? classofRaw(O) // ES3 arguments fallback
  : (result = classofRaw(O)) == 'Object' && typeof O.callee == 'function' ? 'Arguments' : result;
};

/***/ }),

/***/ 74568:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var defineProperty = __webpack_require__(74217).f;

var create = __webpack_require__(83681);

var redefineAll = __webpack_require__(3993);

var bind = __webpack_require__(74271);

var anInstance = __webpack_require__(50997);

var iterate = __webpack_require__(56342);

var defineIterator = __webpack_require__(71179);

var setSpecies = __webpack_require__(64157);

var DESCRIPTORS = __webpack_require__(75592);

var fastKey = __webpack_require__(40421).fastKey;

var InternalStateModule = __webpack_require__(79947);

var setInternalState = InternalStateModule.set;
var internalStateGetterFor = InternalStateModule.getterFor;
module.exports = {
  getConstructor: function () {
    function getConstructor(wrapper, CONSTRUCTOR_NAME, IS_MAP, ADDER) {
      var C = wrapper(function (that, iterable) {
        anInstance(that, C, CONSTRUCTOR_NAME);
        setInternalState(that, {
          type: CONSTRUCTOR_NAME,
          index: create(null),
          first: undefined,
          last: undefined,
          size: 0
        });
        if (!DESCRIPTORS) that.size = 0;
        if (iterable != undefined) iterate(iterable, that[ADDER], {
          that: that,
          AS_ENTRIES: IS_MAP
        });
      });
      var getInternalState = internalStateGetterFor(CONSTRUCTOR_NAME);

      var define = function () {
        function define(that, key, value) {
          var state = getInternalState(that);
          var entry = getEntry(that, key);
          var previous, index; // change existing entry

          if (entry) {
            entry.value = value; // create new entry
          } else {
            state.last = entry = {
              index: index = fastKey(key, true),
              key: key,
              value: value,
              previous: previous = state.last,
              next: undefined,
              removed: false
            };
            if (!state.first) state.first = entry;
            if (previous) previous.next = entry;
            if (DESCRIPTORS) state.size++;else that.size++; // add to index

            if (index !== 'F') state.index[index] = entry;
          }

          return that;
        }

        return define;
      }();

      var getEntry = function () {
        function getEntry(that, key) {
          var state = getInternalState(that); // fast case

          var index = fastKey(key);
          var entry;
          if (index !== 'F') return state.index[index]; // frozen object case

          for (entry = state.first; entry; entry = entry.next) {
            if (entry.key == key) return entry;
          }
        }

        return getEntry;
      }();

      redefineAll(C.prototype, {
        // `{ Map, Set }.prototype.clear()` methods
        // https://tc39.es/ecma262/#sec-map.prototype.clear
        // https://tc39.es/ecma262/#sec-set.prototype.clear
        clear: function () {
          function clear() {
            var that = this;
            var state = getInternalState(that);
            var data = state.index;
            var entry = state.first;

            while (entry) {
              entry.removed = true;
              if (entry.previous) entry.previous = entry.previous.next = undefined;
              delete data[entry.index];
              entry = entry.next;
            }

            state.first = state.last = undefined;
            if (DESCRIPTORS) state.size = 0;else that.size = 0;
          }

          return clear;
        }(),
        // `{ Map, Set }.prototype.delete(key)` methods
        // https://tc39.es/ecma262/#sec-map.prototype.delete
        // https://tc39.es/ecma262/#sec-set.prototype.delete
        'delete': function () {
          function _delete(key) {
            var that = this;
            var state = getInternalState(that);
            var entry = getEntry(that, key);

            if (entry) {
              var next = entry.next;
              var prev = entry.previous;
              delete state.index[entry.index];
              entry.removed = true;
              if (prev) prev.next = next;
              if (next) next.previous = prev;
              if (state.first == entry) state.first = next;
              if (state.last == entry) state.last = prev;
              if (DESCRIPTORS) state.size--;else that.size--;
            }

            return !!entry;
          }

          return _delete;
        }(),
        // `{ Map, Set }.prototype.forEach(callbackfn, thisArg = undefined)` methods
        // https://tc39.es/ecma262/#sec-map.prototype.foreach
        // https://tc39.es/ecma262/#sec-set.prototype.foreach
        forEach: function () {
          function forEach(callbackfn
          /* , that = undefined */
          ) {
            var state = getInternalState(this);
            var boundFunction = bind(callbackfn, arguments.length > 1 ? arguments[1] : undefined, 3);
            var entry;

            while (entry = entry ? entry.next : state.first) {
              boundFunction(entry.value, entry.key, this); // revert to the last existing entry

              while (entry && entry.removed) {
                entry = entry.previous;
              }
            }
          }

          return forEach;
        }(),
        // `{ Map, Set}.prototype.has(key)` methods
        // https://tc39.es/ecma262/#sec-map.prototype.has
        // https://tc39.es/ecma262/#sec-set.prototype.has
        has: function () {
          function has(key) {
            return !!getEntry(this, key);
          }

          return has;
        }()
      });
      redefineAll(C.prototype, IS_MAP ? {
        // `Map.prototype.get(key)` method
        // https://tc39.es/ecma262/#sec-map.prototype.get
        get: function () {
          function get(key) {
            var entry = getEntry(this, key);
            return entry && entry.value;
          }

          return get;
        }(),
        // `Map.prototype.set(key, value)` method
        // https://tc39.es/ecma262/#sec-map.prototype.set
        set: function () {
          function set(key, value) {
            return define(this, key === 0 ? 0 : key, value);
          }

          return set;
        }()
      } : {
        // `Set.prototype.add(value)` method
        // https://tc39.es/ecma262/#sec-set.prototype.add
        add: function () {
          function add(value) {
            return define(this, value = value === 0 ? 0 : value, value);
          }

          return add;
        }()
      });
      if (DESCRIPTORS) defineProperty(C.prototype, 'size', {
        get: function () {
          function get() {
            return getInternalState(this).size;
          }

          return get;
        }()
      });
      return C;
    }

    return getConstructor;
  }(),
  setStrong: function () {
    function setStrong(C, CONSTRUCTOR_NAME, IS_MAP) {
      var ITERATOR_NAME = CONSTRUCTOR_NAME + ' Iterator';
      var getInternalCollectionState = internalStateGetterFor(CONSTRUCTOR_NAME);
      var getInternalIteratorState = internalStateGetterFor(ITERATOR_NAME); // `{ Map, Set }.prototype.{ keys, values, entries, @@iterator }()` methods
      // https://tc39.es/ecma262/#sec-map.prototype.entries
      // https://tc39.es/ecma262/#sec-map.prototype.keys
      // https://tc39.es/ecma262/#sec-map.prototype.values
      // https://tc39.es/ecma262/#sec-map.prototype-@@iterator
      // https://tc39.es/ecma262/#sec-set.prototype.entries
      // https://tc39.es/ecma262/#sec-set.prototype.keys
      // https://tc39.es/ecma262/#sec-set.prototype.values
      // https://tc39.es/ecma262/#sec-set.prototype-@@iterator

      defineIterator(C, CONSTRUCTOR_NAME, function (iterated, kind) {
        setInternalState(this, {
          type: ITERATOR_NAME,
          target: iterated,
          state: getInternalCollectionState(iterated),
          kind: kind,
          last: undefined
        });
      }, function () {
        var state = getInternalIteratorState(this);
        var kind = state.kind;
        var entry = state.last; // revert to the last existing entry

        while (entry && entry.removed) {
          entry = entry.previous;
        } // get next entry


        if (!state.target || !(state.last = entry = entry ? entry.next : state.state.first)) {
          // or finish the iteration
          state.target = undefined;
          return {
            value: undefined,
            done: true
          };
        } // return step by kind


        if (kind == 'keys') return {
          value: entry.key,
          done: false
        };
        if (kind == 'values') return {
          value: entry.value,
          done: false
        };
        return {
          value: [entry.key, entry.value],
          done: false
        };
      }, IS_MAP ? 'entries' : 'values', !IS_MAP, true); // `{ Map, Set }.prototype[@@species]` accessors
      // https://tc39.es/ecma262/#sec-get-map-@@species
      // https://tc39.es/ecma262/#sec-get-set-@@species

      setSpecies(CONSTRUCTOR_NAME);
    }

    return setStrong;
  }()
};

/***/ }),

/***/ 57396:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var redefineAll = __webpack_require__(3993);

var getWeakData = __webpack_require__(40421).getWeakData;

var anObject = __webpack_require__(63518);

var isObject = __webpack_require__(35611);

var anInstance = __webpack_require__(50997);

var iterate = __webpack_require__(56342);

var ArrayIterationModule = __webpack_require__(69033);

var $has = __webpack_require__(28919);

var InternalStateModule = __webpack_require__(79947);

var setInternalState = InternalStateModule.set;
var internalStateGetterFor = InternalStateModule.getterFor;
var find = ArrayIterationModule.find;
var findIndex = ArrayIterationModule.findIndex;
var id = 0; // fallback for uncaught frozen keys

var uncaughtFrozenStore = function uncaughtFrozenStore(store) {
  return store.frozen || (store.frozen = new UncaughtFrozenStore());
};

var UncaughtFrozenStore = function UncaughtFrozenStore() {
  this.entries = [];
};

var findUncaughtFrozen = function findUncaughtFrozen(store, key) {
  return find(store.entries, function (it) {
    return it[0] === key;
  });
};

UncaughtFrozenStore.prototype = {
  get: function () {
    function get(key) {
      var entry = findUncaughtFrozen(this, key);
      if (entry) return entry[1];
    }

    return get;
  }(),
  has: function () {
    function has(key) {
      return !!findUncaughtFrozen(this, key);
    }

    return has;
  }(),
  set: function () {
    function set(key, value) {
      var entry = findUncaughtFrozen(this, key);
      if (entry) entry[1] = value;else this.entries.push([key, value]);
    }

    return set;
  }(),
  'delete': function () {
    function _delete(key) {
      var index = findIndex(this.entries, function (it) {
        return it[0] === key;
      });
      if (~index) this.entries.splice(index, 1);
      return !!~index;
    }

    return _delete;
  }()
};
module.exports = {
  getConstructor: function () {
    function getConstructor(wrapper, CONSTRUCTOR_NAME, IS_MAP, ADDER) {
      var C = wrapper(function (that, iterable) {
        anInstance(that, C, CONSTRUCTOR_NAME);
        setInternalState(that, {
          type: CONSTRUCTOR_NAME,
          id: id++,
          frozen: undefined
        });
        if (iterable != undefined) iterate(iterable, that[ADDER], {
          that: that,
          AS_ENTRIES: IS_MAP
        });
      });
      var getInternalState = internalStateGetterFor(CONSTRUCTOR_NAME);

      var define = function () {
        function define(that, key, value) {
          var state = getInternalState(that);
          var data = getWeakData(anObject(key), true);
          if (data === true) uncaughtFrozenStore(state).set(key, value);else data[state.id] = value;
          return that;
        }

        return define;
      }();

      redefineAll(C.prototype, {
        // `{ WeakMap, WeakSet }.prototype.delete(key)` methods
        // https://tc39.es/ecma262/#sec-weakmap.prototype.delete
        // https://tc39.es/ecma262/#sec-weakset.prototype.delete
        'delete': function () {
          function _delete(key) {
            var state = getInternalState(this);
            if (!isObject(key)) return false;
            var data = getWeakData(key);
            if (data === true) return uncaughtFrozenStore(state)['delete'](key);
            return data && $has(data, state.id) && delete data[state.id];
          }

          return _delete;
        }(),
        // `{ WeakMap, WeakSet }.prototype.has(key)` methods
        // https://tc39.es/ecma262/#sec-weakmap.prototype.has
        // https://tc39.es/ecma262/#sec-weakset.prototype.has
        has: function () {
          function has(key) {
            var state = getInternalState(this);
            if (!isObject(key)) return false;
            var data = getWeakData(key);
            if (data === true) return uncaughtFrozenStore(state).has(key);
            return data && $has(data, state.id);
          }

          return has;
        }()
      });
      redefineAll(C.prototype, IS_MAP ? {
        // `WeakMap.prototype.get(key)` method
        // https://tc39.es/ecma262/#sec-weakmap.prototype.get
        get: function () {
          function get(key) {
            var state = getInternalState(this);

            if (isObject(key)) {
              var data = getWeakData(key);
              if (data === true) return uncaughtFrozenStore(state).get(key);
              return data ? data[state.id] : undefined;
            }
          }

          return get;
        }(),
        // `WeakMap.prototype.set(key, value)` method
        // https://tc39.es/ecma262/#sec-weakmap.prototype.set
        set: function () {
          function set(key, value) {
            return define(this, key, value);
          }

          return set;
        }()
      } : {
        // `WeakSet.prototype.add(value)` method
        // https://tc39.es/ecma262/#sec-weakset.prototype.add
        add: function () {
          function add(value) {
            return define(this, value, true);
          }

          return add;
        }()
      });
      return C;
    }

    return getConstructor;
  }()
};

/***/ }),

/***/ 42006:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var global = __webpack_require__(84253);

var isForced = __webpack_require__(41441);

var redefine = __webpack_require__(11807);

var InternalMetadataModule = __webpack_require__(40421);

var iterate = __webpack_require__(56342);

var anInstance = __webpack_require__(50997);

var isObject = __webpack_require__(35611);

var fails = __webpack_require__(69691);

var checkCorrectnessOfIteration = __webpack_require__(68762);

var setToStringTag = __webpack_require__(29108);

var inheritIfRequired = __webpack_require__(22285);

module.exports = function (CONSTRUCTOR_NAME, wrapper, common) {
  var IS_MAP = CONSTRUCTOR_NAME.indexOf('Map') !== -1;
  var IS_WEAK = CONSTRUCTOR_NAME.indexOf('Weak') !== -1;
  var ADDER = IS_MAP ? 'set' : 'add';
  var NativeConstructor = global[CONSTRUCTOR_NAME];
  var NativePrototype = NativeConstructor && NativeConstructor.prototype;
  var Constructor = NativeConstructor;
  var exported = {};

  var fixMethod = function fixMethod(KEY) {
    var nativeMethod = NativePrototype[KEY];
    redefine(NativePrototype, KEY, KEY == 'add' ? function () {
      function add(value) {
        nativeMethod.call(this, value === 0 ? 0 : value);
        return this;
      }

      return add;
    }() : KEY == 'delete' ? function (key) {
      return IS_WEAK && !isObject(key) ? false : nativeMethod.call(this, key === 0 ? 0 : key);
    } : KEY == 'get' ? function () {
      function get(key) {
        return IS_WEAK && !isObject(key) ? undefined : nativeMethod.call(this, key === 0 ? 0 : key);
      }

      return get;
    }() : KEY == 'has' ? function () {
      function has(key) {
        return IS_WEAK && !isObject(key) ? false : nativeMethod.call(this, key === 0 ? 0 : key);
      }

      return has;
    }() : function () {
      function set(key, value) {
        nativeMethod.call(this, key === 0 ? 0 : key, value);
        return this;
      }

      return set;
    }());
  };

  var REPLACE = isForced(CONSTRUCTOR_NAME, typeof NativeConstructor != 'function' || !(IS_WEAK || NativePrototype.forEach && !fails(function () {
    new NativeConstructor().entries().next();
  })));

  if (REPLACE) {
    // create collection constructor
    Constructor = common.getConstructor(wrapper, CONSTRUCTOR_NAME, IS_MAP, ADDER);
    InternalMetadataModule.enable();
  } else if (isForced(CONSTRUCTOR_NAME, true)) {
    var instance = new Constructor(); // early implementations not supports chaining

    var HASNT_CHAINING = instance[ADDER](IS_WEAK ? {} : -0, 1) != instance; // V8 ~ Chromium 40- weak-collections throws on primitives, but should return false

    var THROWS_ON_PRIMITIVES = fails(function () {
      instance.has(1);
    }); // most early implementations doesn't supports iterables, most modern - not close it correctly
    // eslint-disable-next-line no-new -- required for testing

    var ACCEPT_ITERABLES = checkCorrectnessOfIteration(function (iterable) {
      new NativeConstructor(iterable);
    }); // for early implementations -0 and +0 not the same

    var BUGGY_ZERO = !IS_WEAK && fails(function () {
      // V8 ~ Chromium 42- fails only with 5+ elements
      var $instance = new NativeConstructor();
      var index = 5;

      while (index--) {
        $instance[ADDER](index, index);
      }

      return !$instance.has(-0);
    });

    if (!ACCEPT_ITERABLES) {
      Constructor = wrapper(function (dummy, iterable) {
        anInstance(dummy, Constructor, CONSTRUCTOR_NAME);
        var that = inheritIfRequired(new NativeConstructor(), dummy, Constructor);
        if (iterable != undefined) iterate(iterable, that[ADDER], {
          that: that,
          AS_ENTRIES: IS_MAP
        });
        return that;
      });
      Constructor.prototype = NativePrototype;
      NativePrototype.constructor = Constructor;
    }

    if (THROWS_ON_PRIMITIVES || BUGGY_ZERO) {
      fixMethod('delete');
      fixMethod('has');
      IS_MAP && fixMethod('get');
    }

    if (BUGGY_ZERO || HASNT_CHAINING) fixMethod(ADDER); // weak collections should not contains .clear method

    if (IS_WEAK && NativePrototype.clear) delete NativePrototype.clear;
  }

  exported[CONSTRUCTOR_NAME] = Constructor;
  $({
    global: true,
    forced: Constructor != NativeConstructor
  }, exported);
  setToStringTag(Constructor, CONSTRUCTOR_NAME);
  if (!IS_WEAK) common.setStrong(Constructor, CONSTRUCTOR_NAME, IS_MAP);
  return Constructor;
};

/***/ }),

/***/ 49957:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var has = __webpack_require__(28919);

var ownKeys = __webpack_require__(28482);

var getOwnPropertyDescriptorModule = __webpack_require__(22245);

var definePropertyModule = __webpack_require__(74217);

module.exports = function (target, source) {
  var keys = ownKeys(source);
  var defineProperty = definePropertyModule.f;
  var getOwnPropertyDescriptor = getOwnPropertyDescriptorModule.f;

  for (var i = 0; i < keys.length; i++) {
    var key = keys[i];
    if (!has(target, key)) defineProperty(target, key, getOwnPropertyDescriptor(source, key));
  }
};

/***/ }),

/***/ 93937:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var wellKnownSymbol = __webpack_require__(17657);

var MATCH = wellKnownSymbol('match');

module.exports = function (METHOD_NAME) {
  var regexp = /./;

  try {
    '/./'[METHOD_NAME](regexp);
  } catch (error1) {
    try {
      regexp[MATCH] = false;
      return '/./'[METHOD_NAME](regexp);
    } catch (error2) {
      /* empty */
    }
  }

  return false;
};

/***/ }),

/***/ 28954:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var fails = __webpack_require__(69691);

module.exports = !fails(function () {
  function F() {
    /* empty */
  }

  F.prototype.constructor = null; // eslint-disable-next-line es/no-object-getprototypeof -- required for testing

  return Object.getPrototypeOf(new F()) !== F.prototype;
});

/***/ }),

/***/ 34141:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var requireObjectCoercible = __webpack_require__(44184);

var toString = __webpack_require__(92082);

var quot = /"/g; // `CreateHTML` abstract operation
// https://tc39.es/ecma262/#sec-createhtml

module.exports = function (string, tag, attribute, value) {
  var S = toString(requireObjectCoercible(string));
  var p1 = '<' + tag;
  if (attribute !== '') p1 += ' ' + attribute + '="' + toString(value).replace(quot, '&quot;') + '"';
  return p1 + '>' + S + '</' + tag + '>';
};

/***/ }),

/***/ 38415:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var IteratorPrototype = __webpack_require__(39881).IteratorPrototype;

var create = __webpack_require__(83681);

var createPropertyDescriptor = __webpack_require__(39160);

var setToStringTag = __webpack_require__(29108);

var Iterators = __webpack_require__(82723);

var returnThis = function returnThis() {
  return this;
};

module.exports = function (IteratorConstructor, NAME, next) {
  var TO_STRING_TAG = NAME + ' Iterator';
  IteratorConstructor.prototype = create(IteratorPrototype, {
    next: createPropertyDescriptor(1, next)
  });
  setToStringTag(IteratorConstructor, TO_STRING_TAG, false, true);
  Iterators[TO_STRING_TAG] = returnThis;
  return IteratorConstructor;
};

/***/ }),

/***/ 52188:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var DESCRIPTORS = __webpack_require__(75592);

var definePropertyModule = __webpack_require__(74217);

var createPropertyDescriptor = __webpack_require__(39160);

module.exports = DESCRIPTORS ? function (object, key, value) {
  return definePropertyModule.f(object, key, createPropertyDescriptor(1, value));
} : function (object, key, value) {
  object[key] = value;
  return object;
};

/***/ }),

/***/ 39160:
/***/ (function(module) {

"use strict";


module.exports = function (bitmap, value) {
  return {
    enumerable: !(bitmap & 1),
    configurable: !(bitmap & 2),
    writable: !(bitmap & 4),
    value: value
  };
};

/***/ }),

/***/ 39241:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var toPropertyKey = __webpack_require__(14613);

var definePropertyModule = __webpack_require__(74217);

var createPropertyDescriptor = __webpack_require__(39160);

module.exports = function (object, key, value) {
  var propertyKey = toPropertyKey(key);
  if (propertyKey in object) definePropertyModule.f(object, propertyKey, createPropertyDescriptor(0, value));else object[propertyKey] = value;
};

/***/ }),

/***/ 79873:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var fails = __webpack_require__(69691);

var padStart = __webpack_require__(47497).start;

var abs = Math.abs;
var DatePrototype = Date.prototype;
var getTime = DatePrototype.getTime;
var nativeDateToISOString = DatePrototype.toISOString; // `Date.prototype.toISOString` method implementation
// https://tc39.es/ecma262/#sec-date.prototype.toisostring
// PhantomJS / old WebKit fails here:

module.exports = fails(function () {
  return nativeDateToISOString.call(new Date(-5e13 - 1)) != '0385-07-25T07:06:39.999Z';
}) || !fails(function () {
  nativeDateToISOString.call(new Date(NaN));
}) ? function () {
  function toISOString() {
    if (!isFinite(getTime.call(this))) throw RangeError('Invalid time value');
    var date = this;
    var year = date.getUTCFullYear();
    var milliseconds = date.getUTCMilliseconds();
    var sign = year < 0 ? '-' : year > 9999 ? '+' : '';
    return sign + padStart(abs(year), sign ? 6 : 4, 0) + '-' + padStart(date.getUTCMonth() + 1, 2, 0) + '-' + padStart(date.getUTCDate(), 2, 0) + 'T' + padStart(date.getUTCHours(), 2, 0) + ':' + padStart(date.getUTCMinutes(), 2, 0) + ':' + padStart(date.getUTCSeconds(), 2, 0) + '.' + padStart(milliseconds, 3, 0) + 'Z';
  }

  return toISOString;
}() : nativeDateToISOString;

/***/ }),

/***/ 57763:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var anObject = __webpack_require__(63518);

var ordinaryToPrimitive = __webpack_require__(28734); // `Date.prototype[@@toPrimitive](hint)` method implementation
// https://tc39.es/ecma262/#sec-date.prototype-@@toprimitive


module.exports = function (hint) {
  anObject(this);
  if (hint === 'string' || hint === 'default') hint = 'string';else if (hint !== 'number') throw TypeError('Incorrect hint');
  return ordinaryToPrimitive(this, hint);
};

/***/ }),

/***/ 71179:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var createIteratorConstructor = __webpack_require__(38415);

var getPrototypeOf = __webpack_require__(6863);

var setPrototypeOf = __webpack_require__(18444);

var setToStringTag = __webpack_require__(29108);

var createNonEnumerableProperty = __webpack_require__(52188);

var redefine = __webpack_require__(11807);

var wellKnownSymbol = __webpack_require__(17657);

var IS_PURE = __webpack_require__(1986);

var Iterators = __webpack_require__(82723);

var IteratorsCore = __webpack_require__(39881);

var IteratorPrototype = IteratorsCore.IteratorPrototype;
var BUGGY_SAFARI_ITERATORS = IteratorsCore.BUGGY_SAFARI_ITERATORS;
var ITERATOR = wellKnownSymbol('iterator');
var KEYS = 'keys';
var VALUES = 'values';
var ENTRIES = 'entries';

var returnThis = function returnThis() {
  return this;
};

module.exports = function (Iterable, NAME, IteratorConstructor, next, DEFAULT, IS_SET, FORCED) {
  createIteratorConstructor(IteratorConstructor, NAME, next);

  var getIterationMethod = function getIterationMethod(KIND) {
    if (KIND === DEFAULT && defaultIterator) return defaultIterator;
    if (!BUGGY_SAFARI_ITERATORS && KIND in IterablePrototype) return IterablePrototype[KIND];

    switch (KIND) {
      case KEYS:
        return function () {
          function keys() {
            return new IteratorConstructor(this, KIND);
          }

          return keys;
        }();

      case VALUES:
        return function () {
          function values() {
            return new IteratorConstructor(this, KIND);
          }

          return values;
        }();

      case ENTRIES:
        return function () {
          function entries() {
            return new IteratorConstructor(this, KIND);
          }

          return entries;
        }();
    }

    return function () {
      return new IteratorConstructor(this);
    };
  };

  var TO_STRING_TAG = NAME + ' Iterator';
  var INCORRECT_VALUES_NAME = false;
  var IterablePrototype = Iterable.prototype;
  var nativeIterator = IterablePrototype[ITERATOR] || IterablePrototype['@@iterator'] || DEFAULT && IterablePrototype[DEFAULT];
  var defaultIterator = !BUGGY_SAFARI_ITERATORS && nativeIterator || getIterationMethod(DEFAULT);
  var anyNativeIterator = NAME == 'Array' ? IterablePrototype.entries || nativeIterator : nativeIterator;
  var CurrentIteratorPrototype, methods, KEY; // fix native

  if (anyNativeIterator) {
    CurrentIteratorPrototype = getPrototypeOf(anyNativeIterator.call(new Iterable()));

    if (IteratorPrototype !== Object.prototype && CurrentIteratorPrototype.next) {
      if (!IS_PURE && getPrototypeOf(CurrentIteratorPrototype) !== IteratorPrototype) {
        if (setPrototypeOf) {
          setPrototypeOf(CurrentIteratorPrototype, IteratorPrototype);
        } else if (typeof CurrentIteratorPrototype[ITERATOR] != 'function') {
          createNonEnumerableProperty(CurrentIteratorPrototype, ITERATOR, returnThis);
        }
      } // Set @@toStringTag to native iterators


      setToStringTag(CurrentIteratorPrototype, TO_STRING_TAG, true, true);
      if (IS_PURE) Iterators[TO_STRING_TAG] = returnThis;
    }
  } // fix Array.prototype.{ values, @@iterator }.name in V8 / FF


  if (DEFAULT == VALUES && nativeIterator && nativeIterator.name !== VALUES) {
    INCORRECT_VALUES_NAME = true;

    defaultIterator = function () {
      function values() {
        return nativeIterator.call(this);
      }

      return values;
    }();
  } // define iterator


  if ((!IS_PURE || FORCED) && IterablePrototype[ITERATOR] !== defaultIterator) {
    createNonEnumerableProperty(IterablePrototype, ITERATOR, defaultIterator);
  }

  Iterators[NAME] = defaultIterator; // export additional methods

  if (DEFAULT) {
    methods = {
      values: getIterationMethod(VALUES),
      keys: IS_SET ? defaultIterator : getIterationMethod(KEYS),
      entries: getIterationMethod(ENTRIES)
    };
    if (FORCED) for (KEY in methods) {
      if (BUGGY_SAFARI_ITERATORS || INCORRECT_VALUES_NAME || !(KEY in IterablePrototype)) {
        redefine(IterablePrototype, KEY, methods[KEY]);
      }
    } else $({
      target: NAME,
      proto: true,
      forced: BUGGY_SAFARI_ITERATORS || INCORRECT_VALUES_NAME
    }, methods);
  }

  return methods;
};

/***/ }),

/***/ 55282:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var path = __webpack_require__(65781);

var has = __webpack_require__(28919);

var wrappedWellKnownSymbolModule = __webpack_require__(3444);

var defineProperty = __webpack_require__(74217).f;

module.exports = function (NAME) {
  var Symbol = path.Symbol || (path.Symbol = {});
  if (!has(Symbol, NAME)) defineProperty(Symbol, NAME, {
    value: wrappedWellKnownSymbolModule.f(NAME)
  });
};

/***/ }),

/***/ 75592:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var fails = __webpack_require__(69691); // Detect IE8's incomplete defineProperty implementation


module.exports = !fails(function () {
  // eslint-disable-next-line es/no-object-defineproperty -- required for testing
  return Object.defineProperty({}, 1, {
    get: function () {
      function get() {
        return 7;
      }

      return get;
    }()
  })[1] != 7;
});

/***/ }),

/***/ 21477:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var global = __webpack_require__(84253);

var isObject = __webpack_require__(35611);

var document = global.document; // typeof document.createElement is 'object' in old IE

var EXISTS = isObject(document) && isObject(document.createElement);

module.exports = function (it) {
  return EXISTS ? document.createElement(it) : {};
};

/***/ }),

/***/ 20967:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var userAgent = __webpack_require__(72319);

var firefox = userAgent.match(/firefox\/(\d+)/i);
module.exports = !!firefox && +firefox[1];

/***/ }),

/***/ 46392:
/***/ (function(module) {

"use strict";


module.exports = typeof window == 'object';

/***/ }),

/***/ 85749:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var UA = __webpack_require__(72319);

module.exports = /MSIE|Trident/.test(UA);

/***/ }),

/***/ 20037:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var userAgent = __webpack_require__(72319);

var global = __webpack_require__(84253);

module.exports = /iphone|ipod|ipad/i.test(userAgent) && global.Pebble !== undefined;

/***/ }),

/***/ 24542:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var userAgent = __webpack_require__(72319);

module.exports = /(?:iphone|ipod|ipad).*applewebkit/i.test(userAgent);

/***/ }),

/***/ 41488:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var classof = __webpack_require__(77000);

var global = __webpack_require__(84253);

module.exports = classof(global.process) == 'process';

/***/ }),

/***/ 47501:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var userAgent = __webpack_require__(72319);

module.exports = /web0s(?!.*chrome)/i.test(userAgent);

/***/ }),

/***/ 72319:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var getBuiltIn = __webpack_require__(69808);

module.exports = getBuiltIn('navigator', 'userAgent') || '';

/***/ }),

/***/ 12338:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var global = __webpack_require__(84253);

var userAgent = __webpack_require__(72319);

var process = global.process;
var Deno = global.Deno;
var versions = process && process.versions || Deno && Deno.version;
var v8 = versions && versions.v8;
var match, version;

if (v8) {
  match = v8.split('.');
  version = match[0] < 4 ? 1 : match[0] + match[1];
} else if (userAgent) {
  match = userAgent.match(/Edge\/(\d+)/);

  if (!match || match[1] >= 74) {
    match = userAgent.match(/Chrome\/(\d+)/);
    if (match) version = match[1];
  }
}

module.exports = version && +version;

/***/ }),

/***/ 12200:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var userAgent = __webpack_require__(72319);

var webkit = userAgent.match(/AppleWebKit\/(\d+)\./);
module.exports = !!webkit && +webkit[1];

/***/ }),

/***/ 23344:
/***/ (function(module) {

"use strict";


// IE8- don't enum bug keys
module.exports = ['constructor', 'hasOwnProperty', 'isPrototypeOf', 'propertyIsEnumerable', 'toLocaleString', 'toString', 'valueOf'];

/***/ }),

/***/ 56174:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var global = __webpack_require__(84253);

var getOwnPropertyDescriptor = __webpack_require__(22245).f;

var createNonEnumerableProperty = __webpack_require__(52188);

var redefine = __webpack_require__(11807);

var setGlobal = __webpack_require__(30889);

var copyConstructorProperties = __webpack_require__(49957);

var isForced = __webpack_require__(41441);
/*
  options.target      - name of the target object
  options.global      - target is the global object
  options.stat        - export as static methods of target
  options.proto       - export as prototype methods of target
  options.real        - real prototype method for the `pure` version
  options.forced      - export even if the native feature is available
  options.bind        - bind methods to the target, required for the `pure` version
  options.wrap        - wrap constructors to preventing global pollution, required for the `pure` version
  options.unsafe      - use the simple assignment of property instead of delete + defineProperty
  options.sham        - add a flag to not completely full polyfills
  options.enumerable  - export as enumerable property
  options.noTargetGet - prevent calling a getter on target
*/


module.exports = function (options, source) {
  var TARGET = options.target;
  var GLOBAL = options.global;
  var STATIC = options.stat;
  var FORCED, target, key, targetProperty, sourceProperty, descriptor;

  if (GLOBAL) {
    target = global;
  } else if (STATIC) {
    target = global[TARGET] || setGlobal(TARGET, {});
  } else {
    target = (global[TARGET] || {}).prototype;
  }

  if (target) for (key in source) {
    sourceProperty = source[key];

    if (options.noTargetGet) {
      descriptor = getOwnPropertyDescriptor(target, key);
      targetProperty = descriptor && descriptor.value;
    } else targetProperty = target[key];

    FORCED = isForced(GLOBAL ? key : TARGET + (STATIC ? '.' : '#') + key, options.forced); // contained in target

    if (!FORCED && targetProperty !== undefined) {
      if (typeof sourceProperty === typeof targetProperty) continue;
      copyConstructorProperties(sourceProperty, targetProperty);
    } // add a flag to not completely full polyfills


    if (options.sham || targetProperty && targetProperty.sham) {
      createNonEnumerableProperty(sourceProperty, 'sham', true);
    } // extend global


    redefine(target, key, sourceProperty, options);
  }
};

/***/ }),

/***/ 69691:
/***/ (function(module) {

"use strict";


module.exports = function (exec) {
  try {
    return !!exec();
  } catch (error) {
    return true;
  }
};

/***/ }),

/***/ 68880:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";
 // TODO: Remove from `core-js@4` since it's moved to entry points

__webpack_require__(12654);

var redefine = __webpack_require__(11807);

var regexpExec = __webpack_require__(71841);

var fails = __webpack_require__(69691);

var wellKnownSymbol = __webpack_require__(17657);

var createNonEnumerableProperty = __webpack_require__(52188);

var SPECIES = wellKnownSymbol('species');
var RegExpPrototype = RegExp.prototype;

module.exports = function (KEY, exec, FORCED, SHAM) {
  var SYMBOL = wellKnownSymbol(KEY);
  var DELEGATES_TO_SYMBOL = !fails(function () {
    // String methods call symbol-named RegEp methods
    var O = {};

    O[SYMBOL] = function () {
      return 7;
    };

    return ''[KEY](O) != 7;
  });
  var DELEGATES_TO_EXEC = DELEGATES_TO_SYMBOL && !fails(function () {
    // Symbol-named RegExp methods call .exec
    var execCalled = false;
    var re = /a/;

    if (KEY === 'split') {
      // We can't use real regex here since it causes deoptimization
      // and serious performance degradation in V8
      // https://github.com/zloirock/core-js/issues/306
      re = {}; // RegExp[@@split] doesn't call the regex's exec method, but first creates
      // a new one. We need to return the patched regex when creating the new one.

      re.constructor = {};

      re.constructor[SPECIES] = function () {
        return re;
      };

      re.flags = '';
      re[SYMBOL] = /./[SYMBOL];
    }

    re.exec = function () {
      execCalled = true;
      return null;
    };

    re[SYMBOL]('');
    return !execCalled;
  });

  if (!DELEGATES_TO_SYMBOL || !DELEGATES_TO_EXEC || FORCED) {
    var nativeRegExpMethod = /./[SYMBOL];
    var methods = exec(SYMBOL, ''[KEY], function (nativeMethod, regexp, str, arg2, forceStringMethod) {
      var $exec = regexp.exec;

      if ($exec === regexpExec || $exec === RegExpPrototype.exec) {
        if (DELEGATES_TO_SYMBOL && !forceStringMethod) {
          // The native String method already delegates to @@method (this
          // polyfilled function), leasing to infinite recursion.
          // We avoid it by directly calling the native @@method method.
          return {
            done: true,
            value: nativeRegExpMethod.call(regexp, str, arg2)
          };
        }

        return {
          done: true,
          value: nativeMethod.call(str, regexp, arg2)
        };
      }

      return {
        done: false
      };
    });
    redefine(String.prototype, KEY, methods[0]);
    redefine(RegExpPrototype, SYMBOL, methods[1]);
  }

  if (SHAM) createNonEnumerableProperty(RegExpPrototype[SYMBOL], 'sham', true);
};

/***/ }),

/***/ 904:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var isArray = __webpack_require__(89999);

var toLength = __webpack_require__(39570);

var bind = __webpack_require__(74271); // `FlattenIntoArray` abstract operation
// https://tc39.github.io/proposal-flatMap/#sec-FlattenIntoArray


var flattenIntoArray = function flattenIntoArray(target, original, source, sourceLen, start, depth, mapper, thisArg) {
  var targetIndex = start;
  var sourceIndex = 0;
  var mapFn = mapper ? bind(mapper, thisArg, 3) : false;
  var element;

  while (sourceIndex < sourceLen) {
    if (sourceIndex in source) {
      element = mapFn ? mapFn(source[sourceIndex], sourceIndex, original) : source[sourceIndex];

      if (depth > 0 && isArray(element)) {
        targetIndex = flattenIntoArray(target, original, element, toLength(element.length), targetIndex, depth - 1) - 1;
      } else {
        if (targetIndex >= 0x1FFFFFFFFFFFFF) throw TypeError('Exceed the acceptable array length');
        target[targetIndex] = element;
      }

      targetIndex++;
    }

    sourceIndex++;
  }

  return targetIndex;
};

module.exports = flattenIntoArray;

/***/ }),

/***/ 7056:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var fails = __webpack_require__(69691);

module.exports = !fails(function () {
  // eslint-disable-next-line es/no-object-isextensible, es/no-object-preventextensions -- required for testing
  return Object.isExtensible(Object.preventExtensions({}));
});

/***/ }),

/***/ 74271:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var aFunction = __webpack_require__(83923); // optional / simple context binding


module.exports = function (fn, that, length) {
  aFunction(fn);
  if (that === undefined) return fn;

  switch (length) {
    case 0:
      return function () {
        return fn.call(that);
      };

    case 1:
      return function (a) {
        return fn.call(that, a);
      };

    case 2:
      return function (a, b) {
        return fn.call(that, a, b);
      };

    case 3:
      return function (a, b, c) {
        return fn.call(that, a, b, c);
      };
  }

  return function () {
    return fn.apply(that, arguments);
  };
};

/***/ }),

/***/ 68719:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var aFunction = __webpack_require__(83923);

var isObject = __webpack_require__(35611);

var slice = [].slice;
var factories = {};

var construct = function construct(C, argsLength, args) {
  if (!(argsLength in factories)) {
    for (var list = [], i = 0; i < argsLength; i++) {
      list[i] = 'a[' + i + ']';
    } // eslint-disable-next-line no-new-func -- we have no proper alternatives, IE8- only


    factories[argsLength] = Function('C,a', 'return new C(' + list.join(',') + ')');
  }

  return factories[argsLength](C, args);
}; // `Function.prototype.bind` method implementation
// https://tc39.es/ecma262/#sec-function.prototype.bind


module.exports = Function.bind || function () {
  function bind(that
  /* , ...args */
  ) {
    var fn = aFunction(this);
    var partArgs = slice.call(arguments, 1);

    var boundFunction = function () {
      function bound() {
        var args = partArgs.concat(slice.call(arguments));
        return this instanceof boundFunction ? construct(fn, args.length, args) : fn.apply(that, args);
      }

      return bound;
    }();

    if (isObject(fn.prototype)) boundFunction.prototype = fn.prototype;
    return boundFunction;
  }

  return bind;
}();

/***/ }),

/***/ 69808:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var global = __webpack_require__(84253);

var aFunction = function aFunction(variable) {
  return typeof variable == 'function' ? variable : undefined;
};

module.exports = function (namespace, method) {
  return arguments.length < 2 ? aFunction(global[namespace]) : global[namespace] && global[namespace][method];
};

/***/ }),

/***/ 3170:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var classof = __webpack_require__(47953);

var Iterators = __webpack_require__(82723);

var wellKnownSymbol = __webpack_require__(17657);

var ITERATOR = wellKnownSymbol('iterator');

module.exports = function (it) {
  if (it != undefined) return it[ITERATOR] || it['@@iterator'] || Iterators[classof(it)];
};

/***/ }),

/***/ 55444:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var toObject = __webpack_require__(12104);

var floor = Math.floor;
var replace = ''.replace;
var SUBSTITUTION_SYMBOLS = /\$([$&'`]|\d{1,2}|<[^>]*>)/g;
var SUBSTITUTION_SYMBOLS_NO_NAMED = /\$([$&'`]|\d{1,2})/g; // `GetSubstitution` abstract operation
// https://tc39.es/ecma262/#sec-getsubstitution

module.exports = function (matched, str, position, captures, namedCaptures, replacement) {
  var tailPos = position + matched.length;
  var m = captures.length;
  var symbols = SUBSTITUTION_SYMBOLS_NO_NAMED;

  if (namedCaptures !== undefined) {
    namedCaptures = toObject(namedCaptures);
    symbols = SUBSTITUTION_SYMBOLS;
  }

  return replace.call(replacement, symbols, function (match, ch) {
    var capture;

    switch (ch.charAt(0)) {
      case '$':
        return '$';

      case '&':
        return matched;

      case '`':
        return str.slice(0, position);

      case "'":
        return str.slice(tailPos);

      case '<':
        capture = namedCaptures[ch.slice(1, -1)];
        break;

      default:
        // \d\d?
        var n = +ch;
        if (n === 0) return match;

        if (n > m) {
          var f = floor(n / 10);
          if (f === 0) return match;
          if (f <= m) return captures[f - 1] === undefined ? ch.charAt(1) : captures[f - 1] + ch.charAt(1);
          return match;
        }

        capture = captures[n - 1];
    }

    return capture === undefined ? '' : capture;
  });
};

/***/ }),

/***/ 84253:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var check = function check(it) {
  return it && it.Math == Math && it;
}; // https://github.com/zloirock/core-js/issues/86#issuecomment-115759028


module.exports = // eslint-disable-next-line es/no-global-this -- safe
check(typeof globalThis == 'object' && globalThis) || check(typeof window == 'object' && window) || // eslint-disable-next-line no-restricted-globals -- safe
check(typeof self == 'object' && self) || check(typeof __webpack_require__.g == 'object' && __webpack_require__.g) || // eslint-disable-next-line no-new-func -- fallback
function () {
  return this;
}() || Function('return this')();

/***/ }),

/***/ 28919:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var toObject = __webpack_require__(12104);

var hasOwnProperty = {}.hasOwnProperty;

module.exports = Object.hasOwn || function () {
  function hasOwn(it, key) {
    return hasOwnProperty.call(toObject(it), key);
  }

  return hasOwn;
}();

/***/ }),

/***/ 90311:
/***/ (function(module) {

"use strict";


module.exports = {};

/***/ }),

/***/ 21419:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var global = __webpack_require__(84253);

module.exports = function (a, b) {
  var console = global.console;

  if (console && console.error) {
    arguments.length === 1 ? console.error(a) : console.error(a, b);
  }
};

/***/ }),

/***/ 2369:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var getBuiltIn = __webpack_require__(69808);

module.exports = getBuiltIn('document', 'documentElement');

/***/ }),

/***/ 78251:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var DESCRIPTORS = __webpack_require__(75592);

var fails = __webpack_require__(69691);

var createElement = __webpack_require__(21477); // Thank's IE8 for his funny defineProperty


module.exports = !DESCRIPTORS && !fails(function () {
  // eslint-disable-next-line es/no-object-defineproperty -- requied for testing
  return Object.defineProperty(createElement('div'), 'a', {
    get: function () {
      function get() {
        return 7;
      }

      return get;
    }()
  }).a != 7;
});

/***/ }),

/***/ 16567:
/***/ (function(module) {

"use strict";


// IEEE754 conversions based on https://github.com/feross/ieee754
var abs = Math.abs;
var pow = Math.pow;
var floor = Math.floor;
var log = Math.log;
var LN2 = Math.LN2;

var pack = function pack(number, mantissaLength, bytes) {
  var buffer = new Array(bytes);
  var exponentLength = bytes * 8 - mantissaLength - 1;
  var eMax = (1 << exponentLength) - 1;
  var eBias = eMax >> 1;
  var rt = mantissaLength === 23 ? pow(2, -24) - pow(2, -77) : 0;
  var sign = number < 0 || number === 0 && 1 / number < 0 ? 1 : 0;
  var index = 0;
  var exponent, mantissa, c;
  number = abs(number); // eslint-disable-next-line no-self-compare -- NaN check

  if (number != number || number === Infinity) {
    // eslint-disable-next-line no-self-compare -- NaN check
    mantissa = number != number ? 1 : 0;
    exponent = eMax;
  } else {
    exponent = floor(log(number) / LN2);

    if (number * (c = pow(2, -exponent)) < 1) {
      exponent--;
      c *= 2;
    }

    if (exponent + eBias >= 1) {
      number += rt / c;
    } else {
      number += rt * pow(2, 1 - eBias);
    }

    if (number * c >= 2) {
      exponent++;
      c /= 2;
    }

    if (exponent + eBias >= eMax) {
      mantissa = 0;
      exponent = eMax;
    } else if (exponent + eBias >= 1) {
      mantissa = (number * c - 1) * pow(2, mantissaLength);
      exponent = exponent + eBias;
    } else {
      mantissa = number * pow(2, eBias - 1) * pow(2, mantissaLength);
      exponent = 0;
    }
  }

  for (; mantissaLength >= 8; buffer[index++] = mantissa & 255, mantissa /= 256, mantissaLength -= 8) {
    ;
  }

  exponent = exponent << mantissaLength | mantissa;
  exponentLength += mantissaLength;

  for (; exponentLength > 0; buffer[index++] = exponent & 255, exponent /= 256, exponentLength -= 8) {
    ;
  }

  buffer[--index] |= sign * 128;
  return buffer;
};

var unpack = function unpack(buffer, mantissaLength) {
  var bytes = buffer.length;
  var exponentLength = bytes * 8 - mantissaLength - 1;
  var eMax = (1 << exponentLength) - 1;
  var eBias = eMax >> 1;
  var nBits = exponentLength - 7;
  var index = bytes - 1;
  var sign = buffer[index--];
  var exponent = sign & 127;
  var mantissa;
  sign >>= 7;

  for (; nBits > 0; exponent = exponent * 256 + buffer[index], index--, nBits -= 8) {
    ;
  }

  mantissa = exponent & (1 << -nBits) - 1;
  exponent >>= -nBits;
  nBits += mantissaLength;

  for (; nBits > 0; mantissa = mantissa * 256 + buffer[index], index--, nBits -= 8) {
    ;
  }

  if (exponent === 0) {
    exponent = 1 - eBias;
  } else if (exponent === eMax) {
    return mantissa ? NaN : sign ? -Infinity : Infinity;
  } else {
    mantissa = mantissa + pow(2, mantissaLength);
    exponent = exponent - eBias;
  }

  return (sign ? -1 : 1) * mantissa * pow(2, exponent - mantissaLength);
};

module.exports = {
  pack: pack,
  unpack: unpack
};

/***/ }),

/***/ 50990:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var fails = __webpack_require__(69691);

var classof = __webpack_require__(77000);

var split = ''.split; // fallback for non-array-like ES3 and non-enumerable old V8 strings

module.exports = fails(function () {
  // throws an error in rhino, see https://github.com/mozilla/rhino/issues/346
  // eslint-disable-next-line no-prototype-builtins -- safe
  return !Object('z').propertyIsEnumerable(0);
}) ? function (it) {
  return classof(it) == 'String' ? split.call(it, '') : Object(it);
} : Object;

/***/ }),

/***/ 22285:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var isObject = __webpack_require__(35611);

var setPrototypeOf = __webpack_require__(18444); // makes subclassing work correct for wrapped built-ins


module.exports = function ($this, dummy, Wrapper) {
  var NewTarget, NewTargetPrototype;
  if ( // it can work only with native `setPrototypeOf`
  setPrototypeOf && // we haven't completely correct pre-ES6 way for getting `new.target`, so use this
  typeof (NewTarget = dummy.constructor) == 'function' && NewTarget !== Wrapper && isObject(NewTargetPrototype = NewTarget.prototype) && NewTargetPrototype !== Wrapper.prototype) setPrototypeOf($this, NewTargetPrototype);
  return $this;
};

/***/ }),

/***/ 89853:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var store = __webpack_require__(55935);

var functionToString = Function.toString; // this helper broken in `core-js@3.4.1-3.4.4`, so we can't use `shared` helper

if (typeof store.inspectSource != 'function') {
  store.inspectSource = function (it) {
    return functionToString.call(it);
  };
}

module.exports = store.inspectSource;

/***/ }),

/***/ 40421:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var hiddenKeys = __webpack_require__(90311);

var isObject = __webpack_require__(35611);

var has = __webpack_require__(28919);

var defineProperty = __webpack_require__(74217).f;

var getOwnPropertyNamesModule = __webpack_require__(78870);

var getOwnPropertyNamesExternalModule = __webpack_require__(59995);

var uid = __webpack_require__(41080);

var FREEZING = __webpack_require__(7056);

var REQUIRED = false;
var METADATA = uid('meta');
var id = 0; // eslint-disable-next-line es/no-object-isextensible -- safe

var isExtensible = Object.isExtensible || function () {
  return true;
};

var setMetadata = function setMetadata(it) {
  defineProperty(it, METADATA, {
    value: {
      objectID: 'O' + id++,
      // object ID
      weakData: {} // weak collections IDs

    }
  });
};

var fastKey = function fastKey(it, create) {
  // return a primitive with prefix
  if (!isObject(it)) return typeof it == 'symbol' ? it : (typeof it == 'string' ? 'S' : 'P') + it;

  if (!has(it, METADATA)) {
    // can't set metadata to uncaught frozen object
    if (!isExtensible(it)) return 'F'; // not necessary to add metadata

    if (!create) return 'E'; // add missing metadata

    setMetadata(it); // return object ID
  }

  return it[METADATA].objectID;
};

var getWeakData = function getWeakData(it, create) {
  if (!has(it, METADATA)) {
    // can't set metadata to uncaught frozen object
    if (!isExtensible(it)) return true; // not necessary to add metadata

    if (!create) return false; // add missing metadata

    setMetadata(it); // return the store of weak collections IDs
  }

  return it[METADATA].weakData;
}; // add metadata on freeze-family methods calling


var onFreeze = function onFreeze(it) {
  if (FREEZING && REQUIRED && isExtensible(it) && !has(it, METADATA)) setMetadata(it);
  return it;
};

var enable = function enable() {
  meta.enable = function () {
    /* empty */
  };

  REQUIRED = true;
  var getOwnPropertyNames = getOwnPropertyNamesModule.f;
  var splice = [].splice;
  var test = {};
  test[METADATA] = 1; // prevent exposing of metadata key

  if (getOwnPropertyNames(test).length) {
    getOwnPropertyNamesModule.f = function (it) {
      var result = getOwnPropertyNames(it);

      for (var i = 0, length = result.length; i < length; i++) {
        if (result[i] === METADATA) {
          splice.call(result, i, 1);
          break;
        }
      }

      return result;
    };

    $({
      target: 'Object',
      stat: true,
      forced: true
    }, {
      getOwnPropertyNames: getOwnPropertyNamesExternalModule.f
    });
  }
};

var meta = module.exports = {
  enable: enable,
  fastKey: fastKey,
  getWeakData: getWeakData,
  onFreeze: onFreeze
};
hiddenKeys[METADATA] = true;

/***/ }),

/***/ 79947:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var NATIVE_WEAK_MAP = __webpack_require__(82911);

var global = __webpack_require__(84253);

var isObject = __webpack_require__(35611);

var createNonEnumerableProperty = __webpack_require__(52188);

var objectHas = __webpack_require__(28919);

var shared = __webpack_require__(55935);

var sharedKey = __webpack_require__(85152);

var hiddenKeys = __webpack_require__(90311);

var OBJECT_ALREADY_INITIALIZED = 'Object already initialized';
var WeakMap = global.WeakMap;
var set, get, has;

var enforce = function enforce(it) {
  return has(it) ? get(it) : set(it, {});
};

var getterFor = function getterFor(TYPE) {
  return function (it) {
    var state;

    if (!isObject(it) || (state = get(it)).type !== TYPE) {
      throw TypeError('Incompatible receiver, ' + TYPE + ' required');
    }

    return state;
  };
};

if (NATIVE_WEAK_MAP || shared.state) {
  var store = shared.state || (shared.state = new WeakMap());
  var wmget = store.get;
  var wmhas = store.has;
  var wmset = store.set;

  set = function set(it, metadata) {
    if (wmhas.call(store, it)) throw new TypeError(OBJECT_ALREADY_INITIALIZED);
    metadata.facade = it;
    wmset.call(store, it, metadata);
    return metadata;
  };

  get = function get(it) {
    return wmget.call(store, it) || {};
  };

  has = function has(it) {
    return wmhas.call(store, it);
  };
} else {
  var STATE = sharedKey('state');
  hiddenKeys[STATE] = true;

  set = function set(it, metadata) {
    if (objectHas(it, STATE)) throw new TypeError(OBJECT_ALREADY_INITIALIZED);
    metadata.facade = it;
    createNonEnumerableProperty(it, STATE, metadata);
    return metadata;
  };

  get = function get(it) {
    return objectHas(it, STATE) ? it[STATE] : {};
  };

  has = function has(it) {
    return objectHas(it, STATE);
  };
}

module.exports = {
  set: set,
  get: get,
  has: has,
  enforce: enforce,
  getterFor: getterFor
};

/***/ }),

/***/ 14195:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var wellKnownSymbol = __webpack_require__(17657);

var Iterators = __webpack_require__(82723);

var ITERATOR = wellKnownSymbol('iterator');
var ArrayPrototype = Array.prototype; // check on default Array iterator

module.exports = function (it) {
  return it !== undefined && (Iterators.Array === it || ArrayPrototype[ITERATOR] === it);
};

/***/ }),

/***/ 89999:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var classof = __webpack_require__(77000); // `IsArray` abstract operation
// https://tc39.es/ecma262/#sec-isarray
// eslint-disable-next-line es/no-array-isarray -- safe


module.exports = Array.isArray || function () {
  function isArray(arg) {
    return classof(arg) == 'Array';
  }

  return isArray;
}();

/***/ }),

/***/ 41441:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var fails = __webpack_require__(69691);

var replacement = /#|\.prototype\./;

var isForced = function isForced(feature, detection) {
  var value = data[normalize(feature)];
  return value == POLYFILL ? true : value == NATIVE ? false : typeof detection == 'function' ? fails(detection) : !!detection;
};

var normalize = isForced.normalize = function (string) {
  return String(string).replace(replacement, '.').toLowerCase();
};

var data = isForced.data = {};
var NATIVE = isForced.NATIVE = 'N';
var POLYFILL = isForced.POLYFILL = 'P';
module.exports = isForced;

/***/ }),

/***/ 93624:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var isObject = __webpack_require__(35611);

var floor = Math.floor; // `Number.isInteger` method implementation
// https://tc39.es/ecma262/#sec-number.isinteger

module.exports = function () {
  function isInteger(it) {
    return !isObject(it) && isFinite(it) && floor(it) === it;
  }

  return isInteger;
}();

/***/ }),

/***/ 35611:
/***/ (function(module) {

"use strict";


module.exports = function (it) {
  return typeof it === 'object' ? it !== null : typeof it === 'function';
};

/***/ }),

/***/ 1986:
/***/ (function(module) {

"use strict";


module.exports = false;

/***/ }),

/***/ 86133:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var isObject = __webpack_require__(35611);

var classof = __webpack_require__(77000);

var wellKnownSymbol = __webpack_require__(17657);

var MATCH = wellKnownSymbol('match'); // `IsRegExp` abstract operation
// https://tc39.es/ecma262/#sec-isregexp

module.exports = function (it) {
  var isRegExp;
  return isObject(it) && ((isRegExp = it[MATCH]) !== undefined ? !!isRegExp : classof(it) == 'RegExp');
};

/***/ }),

/***/ 78253:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var getBuiltIn = __webpack_require__(69808);

var USE_SYMBOL_AS_UID = __webpack_require__(40647);

module.exports = USE_SYMBOL_AS_UID ? function (it) {
  return typeof it == 'symbol';
} : function (it) {
  var $Symbol = getBuiltIn('Symbol');
  return typeof $Symbol == 'function' && Object(it) instanceof $Symbol;
};

/***/ }),

/***/ 56342:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var anObject = __webpack_require__(63518);

var isArrayIteratorMethod = __webpack_require__(14195);

var toLength = __webpack_require__(39570);

var bind = __webpack_require__(74271);

var getIteratorMethod = __webpack_require__(3170);

var iteratorClose = __webpack_require__(61781);

var Result = function Result(stopped, result) {
  this.stopped = stopped;
  this.result = result;
};

module.exports = function (iterable, unboundFunction, options) {
  var that = options && options.that;
  var AS_ENTRIES = !!(options && options.AS_ENTRIES);
  var IS_ITERATOR = !!(options && options.IS_ITERATOR);
  var INTERRUPTED = !!(options && options.INTERRUPTED);
  var fn = bind(unboundFunction, that, 1 + AS_ENTRIES + INTERRUPTED);
  var iterator, iterFn, index, length, result, next, step;

  var stop = function stop(condition) {
    if (iterator) iteratorClose(iterator);
    return new Result(true, condition);
  };

  var callFn = function callFn(value) {
    if (AS_ENTRIES) {
      anObject(value);
      return INTERRUPTED ? fn(value[0], value[1], stop) : fn(value[0], value[1]);
    }

    return INTERRUPTED ? fn(value, stop) : fn(value);
  };

  if (IS_ITERATOR) {
    iterator = iterable;
  } else {
    iterFn = getIteratorMethod(iterable);
    if (typeof iterFn != 'function') throw TypeError('Target is not iterable'); // optimisation for array iterators

    if (isArrayIteratorMethod(iterFn)) {
      for (index = 0, length = toLength(iterable.length); length > index; index++) {
        result = callFn(iterable[index]);
        if (result && result instanceof Result) return result;
      }

      return new Result(false);
    }

    iterator = iterFn.call(iterable);
  }

  next = iterator.next;

  while (!(step = next.call(iterator)).done) {
    try {
      result = callFn(step.value);
    } catch (error) {
      iteratorClose(iterator);
      throw error;
    }

    if (typeof result == 'object' && result && result instanceof Result) return result;
  }

  return new Result(false);
};

/***/ }),

/***/ 61781:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var anObject = __webpack_require__(63518);

module.exports = function (iterator) {
  var returnMethod = iterator['return'];

  if (returnMethod !== undefined) {
    return anObject(returnMethod.call(iterator)).value;
  }
};

/***/ }),

/***/ 39881:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var fails = __webpack_require__(69691);

var getPrototypeOf = __webpack_require__(6863);

var createNonEnumerableProperty = __webpack_require__(52188);

var has = __webpack_require__(28919);

var wellKnownSymbol = __webpack_require__(17657);

var IS_PURE = __webpack_require__(1986);

var ITERATOR = wellKnownSymbol('iterator');
var BUGGY_SAFARI_ITERATORS = false;

var returnThis = function returnThis() {
  return this;
}; // `%IteratorPrototype%` object
// https://tc39.es/ecma262/#sec-%iteratorprototype%-object


var IteratorPrototype, PrototypeOfArrayIteratorPrototype, arrayIterator;
/* eslint-disable es/no-array-prototype-keys -- safe */

if ([].keys) {
  arrayIterator = [].keys(); // Safari 8 has buggy iterators w/o `next`

  if (!('next' in arrayIterator)) BUGGY_SAFARI_ITERATORS = true;else {
    PrototypeOfArrayIteratorPrototype = getPrototypeOf(getPrototypeOf(arrayIterator));
    if (PrototypeOfArrayIteratorPrototype !== Object.prototype) IteratorPrototype = PrototypeOfArrayIteratorPrototype;
  }
}

var NEW_ITERATOR_PROTOTYPE = IteratorPrototype == undefined || fails(function () {
  var test = {}; // FF44- legacy iterators case

  return IteratorPrototype[ITERATOR].call(test) !== test;
});
if (NEW_ITERATOR_PROTOTYPE) IteratorPrototype = {}; // `%IteratorPrototype%[@@iterator]()` method
// https://tc39.es/ecma262/#sec-%iteratorprototype%-@@iterator

if ((!IS_PURE || NEW_ITERATOR_PROTOTYPE) && !has(IteratorPrototype, ITERATOR)) {
  createNonEnumerableProperty(IteratorPrototype, ITERATOR, returnThis);
}

module.exports = {
  IteratorPrototype: IteratorPrototype,
  BUGGY_SAFARI_ITERATORS: BUGGY_SAFARI_ITERATORS
};

/***/ }),

/***/ 82723:
/***/ (function(module) {

"use strict";


module.exports = {};

/***/ }),

/***/ 19997:
/***/ (function(module) {

"use strict";


// eslint-disable-next-line es/no-math-expm1 -- safe
var $expm1 = Math.expm1;
var exp = Math.exp; // `Math.expm1` method implementation
// https://tc39.es/ecma262/#sec-math.expm1

module.exports = !$expm1 // Old FF bug
|| $expm1(10) > 22025.465794806719 || $expm1(10) < 22025.4657948067165168 // Tor Browser bug
|| $expm1(-2e-17) != -2e-17 ? function () {
  function expm1(x) {
    return (x = +x) == 0 ? x : x > -1e-6 && x < 1e-6 ? x + x * x / 2 : exp(x) - 1;
  }

  return expm1;
}() : $expm1;

/***/ }),

/***/ 43334:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var sign = __webpack_require__(24305);

var abs = Math.abs;
var pow = Math.pow;
var EPSILON = pow(2, -52);
var EPSILON32 = pow(2, -23);
var MAX32 = pow(2, 127) * (2 - EPSILON32);
var MIN32 = pow(2, -126);

var roundTiesToEven = function roundTiesToEven(n) {
  return n + 1 / EPSILON - 1 / EPSILON;
}; // `Math.fround` method implementation
// https://tc39.es/ecma262/#sec-math.fround
// eslint-disable-next-line es/no-math-fround -- safe


module.exports = Math.fround || function () {
  function fround(x) {
    var $abs = abs(x);
    var $sign = sign(x);
    var a, result;
    if ($abs < MIN32) return $sign * roundTiesToEven($abs / MIN32 / EPSILON32) * MIN32 * EPSILON32;
    a = (1 + EPSILON32 / EPSILON) * $abs;
    result = a - (a - $abs); // eslint-disable-next-line no-self-compare -- NaN check

    if (result > MAX32 || result != result) return $sign * Infinity;
    return $sign * result;
  }

  return fround;
}();

/***/ }),

/***/ 83659:
/***/ (function(module) {

"use strict";


var log = Math.log; // `Math.log1p` method implementation
// https://tc39.es/ecma262/#sec-math.log1p
// eslint-disable-next-line es/no-math-log1p -- safe

module.exports = Math.log1p || function () {
  function log1p(x) {
    return (x = +x) > -1e-8 && x < 1e-8 ? x - x * x / 2 : log(1 + x);
  }

  return log1p;
}();

/***/ }),

/***/ 24305:
/***/ (function(module) {

"use strict";


// `Math.sign` method implementation
// https://tc39.es/ecma262/#sec-math.sign
// eslint-disable-next-line es/no-math-sign -- safe
module.exports = Math.sign || function () {
  function sign(x) {
    // eslint-disable-next-line no-self-compare -- NaN check
    return (x = +x) == 0 || x != x ? x : x < 0 ? -1 : 1;
  }

  return sign;
}();

/***/ }),

/***/ 80909:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var global = __webpack_require__(84253);

var getOwnPropertyDescriptor = __webpack_require__(22245).f;

var macrotask = __webpack_require__(33453).set;

var IS_IOS = __webpack_require__(24542);

var IS_IOS_PEBBLE = __webpack_require__(20037);

var IS_WEBOS_WEBKIT = __webpack_require__(47501);

var IS_NODE = __webpack_require__(41488);

var MutationObserver = global.MutationObserver || global.WebKitMutationObserver;
var document = global.document;
var process = global.process;
var Promise = global.Promise; // Node.js 11 shows ExperimentalWarning on getting `queueMicrotask`

var queueMicrotaskDescriptor = getOwnPropertyDescriptor(global, 'queueMicrotask');
var queueMicrotask = queueMicrotaskDescriptor && queueMicrotaskDescriptor.value;
var flush, head, last, notify, toggle, node, promise, then; // modern engines have queueMicrotask method

if (!queueMicrotask) {
  flush = function flush() {
    var parent, fn;
    if (IS_NODE && (parent = process.domain)) parent.exit();

    while (head) {
      fn = head.fn;
      head = head.next;

      try {
        fn();
      } catch (error) {
        if (head) notify();else last = undefined;
        throw error;
      }
    }

    last = undefined;
    if (parent) parent.enter();
  }; // browsers with MutationObserver, except iOS - https://github.com/zloirock/core-js/issues/339
  // also except WebOS Webkit https://github.com/zloirock/core-js/issues/898


  if (!IS_IOS && !IS_NODE && !IS_WEBOS_WEBKIT && MutationObserver && document) {
    toggle = true;
    node = document.createTextNode('');
    new MutationObserver(flush).observe(node, {
      characterData: true
    });

    notify = function notify() {
      node.data = toggle = !toggle;
    }; // environments with maybe non-completely correct, but existent Promise

  } else if (!IS_IOS_PEBBLE && Promise && Promise.resolve) {
    // Promise.resolve without an argument throws an error in LG WebOS 2
    promise = Promise.resolve(undefined); // workaround of WebKit ~ iOS Safari 10.1 bug

    promise.constructor = Promise;
    then = promise.then;

    notify = function notify() {
      then.call(promise, flush);
    }; // Node.js without promises

  } else if (IS_NODE) {
    notify = function notify() {
      process.nextTick(flush);
    }; // for other environments - macrotask based on:
    // - setImmediate
    // - MessageChannel
    // - window.postMessag
    // - onreadystatechange
    // - setTimeout

  } else {
    notify = function notify() {
      // strange IE + webpack dev server bug - use .call(global)
      macrotask.call(global, flush);
    };
  }
}

module.exports = queueMicrotask || function (fn) {
  var task = {
    fn: fn,
    next: undefined
  };
  if (last) last.next = task;

  if (!head) {
    head = task;
    notify();
  }

  last = task;
};

/***/ }),

/***/ 4302:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var global = __webpack_require__(84253);

module.exports = global.Promise;

/***/ }),

/***/ 54772:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


/* eslint-disable es/no-symbol -- required for testing */
var V8_VERSION = __webpack_require__(12338);

var fails = __webpack_require__(69691); // eslint-disable-next-line es/no-object-getownpropertysymbols -- required for testing


module.exports = !!Object.getOwnPropertySymbols && !fails(function () {
  var symbol = Symbol(); // Chrome 38 Symbol has incorrect toString conversion
  // `get-own-property-symbols` polyfill symbols converted to object are not Symbol instances

  return !String(symbol) || !(Object(symbol) instanceof Symbol) || // Chrome 38-40 symbols are not inherited from DOM collections prototypes to instances
  !Symbol.sham && V8_VERSION && V8_VERSION < 41;
});

/***/ }),

/***/ 82911:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var global = __webpack_require__(84253);

var inspectSource = __webpack_require__(89853);

var WeakMap = global.WeakMap;
module.exports = typeof WeakMap === 'function' && /native code/.test(inspectSource(WeakMap));

/***/ }),

/***/ 82101:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var aFunction = __webpack_require__(83923);

var PromiseCapability = function PromiseCapability(C) {
  var resolve, reject;
  this.promise = new C(function ($$resolve, $$reject) {
    if (resolve !== undefined || reject !== undefined) throw TypeError('Bad Promise constructor');
    resolve = $$resolve;
    reject = $$reject;
  });
  this.resolve = aFunction(resolve);
  this.reject = aFunction(reject);
}; // `NewPromiseCapability` abstract operation
// https://tc39.es/ecma262/#sec-newpromisecapability


module.exports.f = function (C) {
  return new PromiseCapability(C);
};

/***/ }),

/***/ 18774:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var isRegExp = __webpack_require__(86133);

module.exports = function (it) {
  if (isRegExp(it)) {
    throw TypeError("The method doesn't accept regular expressions");
  }

  return it;
};

/***/ }),

/***/ 64630:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var global = __webpack_require__(84253);

var globalIsFinite = global.isFinite; // `Number.isFinite` method
// https://tc39.es/ecma262/#sec-number.isfinite
// eslint-disable-next-line es/no-number-isfinite -- safe

module.exports = Number.isFinite || function () {
  function isFinite(it) {
    return typeof it == 'number' && globalIsFinite(it);
  }

  return isFinite;
}();

/***/ }),

/***/ 78025:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var global = __webpack_require__(84253);

var toString = __webpack_require__(92082);

var trim = __webpack_require__(84703).trim;

var whitespaces = __webpack_require__(65181);

var $parseFloat = global.parseFloat;
var FORCED = 1 / $parseFloat(whitespaces + '-0') !== -Infinity; // `parseFloat` method
// https://tc39.es/ecma262/#sec-parsefloat-string

module.exports = FORCED ? function () {
  function parseFloat(string) {
    var trimmedString = trim(toString(string));
    var result = $parseFloat(trimmedString);
    return result === 0 && trimmedString.charAt(0) == '-' ? -0 : result;
  }

  return parseFloat;
}() : $parseFloat;

/***/ }),

/***/ 55092:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var global = __webpack_require__(84253);

var toString = __webpack_require__(92082);

var trim = __webpack_require__(84703).trim;

var whitespaces = __webpack_require__(65181);

var $parseInt = global.parseInt;
var hex = /^[+-]?0[Xx]/;
var FORCED = $parseInt(whitespaces + '08') !== 8 || $parseInt(whitespaces + '0x16') !== 22; // `parseInt` method
// https://tc39.es/ecma262/#sec-parseint-string-radix

module.exports = FORCED ? function () {
  function parseInt(string, radix) {
    var S = trim(toString(string));
    return $parseInt(S, radix >>> 0 || (hex.test(S) ? 16 : 10));
  }

  return parseInt;
}() : $parseInt;

/***/ }),

/***/ 64905:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var DESCRIPTORS = __webpack_require__(75592);

var fails = __webpack_require__(69691);

var objectKeys = __webpack_require__(84547);

var getOwnPropertySymbolsModule = __webpack_require__(21061);

var propertyIsEnumerableModule = __webpack_require__(77659);

var toObject = __webpack_require__(12104);

var IndexedObject = __webpack_require__(50990); // eslint-disable-next-line es/no-object-assign -- safe


var $assign = Object.assign; // eslint-disable-next-line es/no-object-defineproperty -- required for testing

var defineProperty = Object.defineProperty; // `Object.assign` method
// https://tc39.es/ecma262/#sec-object.assign

module.exports = !$assign || fails(function () {
  // should have correct order of operations (Edge bug)
  if (DESCRIPTORS && $assign({
    b: 1
  }, $assign(defineProperty({}, 'a', {
    enumerable: true,
    get: function () {
      function get() {
        defineProperty(this, 'b', {
          value: 3,
          enumerable: false
        });
      }

      return get;
    }()
  }), {
    b: 2
  })).b !== 1) return true; // should work with symbols and should have deterministic property order (V8 bug)

  var A = {};
  var B = {}; // eslint-disable-next-line es/no-symbol -- safe

  var symbol = Symbol();
  var alphabet = 'abcdefghijklmnopqrst';
  A[symbol] = 7;
  alphabet.split('').forEach(function (chr) {
    B[chr] = chr;
  });
  return $assign({}, A)[symbol] != 7 || objectKeys($assign({}, B)).join('') != alphabet;
}) ? function () {
  function assign(target, source) {
    // eslint-disable-line no-unused-vars -- required for `.length`
    var T = toObject(target);
    var argumentsLength = arguments.length;
    var index = 1;
    var getOwnPropertySymbols = getOwnPropertySymbolsModule.f;
    var propertyIsEnumerable = propertyIsEnumerableModule.f;

    while (argumentsLength > index) {
      var S = IndexedObject(arguments[index++]);
      var keys = getOwnPropertySymbols ? objectKeys(S).concat(getOwnPropertySymbols(S)) : objectKeys(S);
      var length = keys.length;
      var j = 0;
      var key;

      while (length > j) {
        key = keys[j++];
        if (!DESCRIPTORS || propertyIsEnumerable.call(S, key)) T[key] = S[key];
      }
    }

    return T;
  }

  return assign;
}() : $assign;

/***/ }),

/***/ 83681:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


/* global ActiveXObject -- old IE, WSH */
var anObject = __webpack_require__(63518);

var defineProperties = __webpack_require__(91543);

var enumBugKeys = __webpack_require__(23344);

var hiddenKeys = __webpack_require__(90311);

var html = __webpack_require__(2369);

var documentCreateElement = __webpack_require__(21477);

var sharedKey = __webpack_require__(85152);

var GT = '>';
var LT = '<';
var PROTOTYPE = 'prototype';
var SCRIPT = 'script';
var IE_PROTO = sharedKey('IE_PROTO');

var EmptyConstructor = function EmptyConstructor() {
  /* empty */
};

var scriptTag = function scriptTag(content) {
  return LT + SCRIPT + GT + content + LT + '/' + SCRIPT + GT;
}; // Create object with fake `null` prototype: use ActiveX Object with cleared prototype


var NullProtoObjectViaActiveX = function NullProtoObjectViaActiveX(activeXDocument) {
  activeXDocument.write(scriptTag(''));
  activeXDocument.close();
  var temp = activeXDocument.parentWindow.Object;
  activeXDocument = null; // avoid memory leak

  return temp;
}; // Create object with fake `null` prototype: use iframe Object with cleared prototype


var NullProtoObjectViaIFrame = function NullProtoObjectViaIFrame() {
  // Thrash, waste and sodomy: IE GC bug
  var iframe = documentCreateElement('iframe');
  var JS = 'java' + SCRIPT + ':';
  var iframeDocument;

  if (iframe.style) {
    iframe.style.display = 'none';
    html.appendChild(iframe); // https://github.com/zloirock/core-js/issues/475

    iframe.src = String(JS);
    iframeDocument = iframe.contentWindow.document;
    iframeDocument.open();
    iframeDocument.write(scriptTag('document.F=Object'));
    iframeDocument.close();
    return iframeDocument.F;
  }
}; // Check for document.domain and active x support
// No need to use active x approach when document.domain is not set
// see https://github.com/es-shims/es5-shim/issues/150
// variation of https://github.com/kitcambridge/es5-shim/commit/4f738ac066346
// avoid IE GC bug


var activeXDocument;

var _NullProtoObject = function NullProtoObject() {
  try {
    activeXDocument = new ActiveXObject('htmlfile');
  } catch (error) {
    /* ignore */
  }

  _NullProtoObject = document.domain && activeXDocument ? NullProtoObjectViaActiveX(activeXDocument) : // old IE
  NullProtoObjectViaIFrame() || NullProtoObjectViaActiveX(activeXDocument); // WSH

  var length = enumBugKeys.length;

  while (length--) {
    delete _NullProtoObject[PROTOTYPE][enumBugKeys[length]];
  }

  return _NullProtoObject();
};

hiddenKeys[IE_PROTO] = true; // `Object.create` method
// https://tc39.es/ecma262/#sec-object.create

module.exports = Object.create || function () {
  function create(O, Properties) {
    var result;

    if (O !== null) {
      EmptyConstructor[PROTOTYPE] = anObject(O);
      result = new EmptyConstructor();
      EmptyConstructor[PROTOTYPE] = null; // add "__proto__" for Object.getPrototypeOf polyfill

      result[IE_PROTO] = O;
    } else result = _NullProtoObject();

    return Properties === undefined ? result : defineProperties(result, Properties);
  }

  return create;
}();

/***/ }),

/***/ 91543:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var DESCRIPTORS = __webpack_require__(75592);

var definePropertyModule = __webpack_require__(74217);

var anObject = __webpack_require__(63518);

var objectKeys = __webpack_require__(84547); // `Object.defineProperties` method
// https://tc39.es/ecma262/#sec-object.defineproperties
// eslint-disable-next-line es/no-object-defineproperties -- safe


module.exports = DESCRIPTORS ? Object.defineProperties : function () {
  function defineProperties(O, Properties) {
    anObject(O);
    var keys = objectKeys(Properties);
    var length = keys.length;
    var index = 0;
    var key;

    while (length > index) {
      definePropertyModule.f(O, key = keys[index++], Properties[key]);
    }

    return O;
  }

  return defineProperties;
}();

/***/ }),

/***/ 74217:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


var DESCRIPTORS = __webpack_require__(75592);

var IE8_DOM_DEFINE = __webpack_require__(78251);

var anObject = __webpack_require__(63518);

var toPropertyKey = __webpack_require__(14613); // eslint-disable-next-line es/no-object-defineproperty -- safe


var $defineProperty = Object.defineProperty; // `Object.defineProperty` method
// https://tc39.es/ecma262/#sec-object.defineproperty

exports.f = DESCRIPTORS ? $defineProperty : function () {
  function defineProperty(O, P, Attributes) {
    anObject(O);
    P = toPropertyKey(P);
    anObject(Attributes);
    if (IE8_DOM_DEFINE) try {
      return $defineProperty(O, P, Attributes);
    } catch (error) {
      /* empty */
    }
    if ('get' in Attributes || 'set' in Attributes) throw TypeError('Accessors not supported');
    if ('value' in Attributes) O[P] = Attributes.value;
    return O;
  }

  return defineProperty;
}();

/***/ }),

/***/ 22245:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


var DESCRIPTORS = __webpack_require__(75592);

var propertyIsEnumerableModule = __webpack_require__(77659);

var createPropertyDescriptor = __webpack_require__(39160);

var toIndexedObject = __webpack_require__(26553);

var toPropertyKey = __webpack_require__(14613);

var has = __webpack_require__(28919);

var IE8_DOM_DEFINE = __webpack_require__(78251); // eslint-disable-next-line es/no-object-getownpropertydescriptor -- safe


var $getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor; // `Object.getOwnPropertyDescriptor` method
// https://tc39.es/ecma262/#sec-object.getownpropertydescriptor

exports.f = DESCRIPTORS ? $getOwnPropertyDescriptor : function () {
  function getOwnPropertyDescriptor(O, P) {
    O = toIndexedObject(O);
    P = toPropertyKey(P);
    if (IE8_DOM_DEFINE) try {
      return $getOwnPropertyDescriptor(O, P);
    } catch (error) {
      /* empty */
    }
    if (has(O, P)) return createPropertyDescriptor(!propertyIsEnumerableModule.f.call(O, P), O[P]);
  }

  return getOwnPropertyDescriptor;
}();

/***/ }),

/***/ 59995:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


/* eslint-disable es/no-object-getownpropertynames -- safe */
var toIndexedObject = __webpack_require__(26553);

var $getOwnPropertyNames = __webpack_require__(78870).f;

var toString = {}.toString;
var windowNames = typeof window == 'object' && window && Object.getOwnPropertyNames ? Object.getOwnPropertyNames(window) : [];

var getWindowNames = function getWindowNames(it) {
  try {
    return $getOwnPropertyNames(it);
  } catch (error) {
    return windowNames.slice();
  }
}; // fallback for IE11 buggy Object.getOwnPropertyNames with iframe and window


module.exports.f = function () {
  function getOwnPropertyNames(it) {
    return windowNames && toString.call(it) == '[object Window]' ? getWindowNames(it) : $getOwnPropertyNames(toIndexedObject(it));
  }

  return getOwnPropertyNames;
}();

/***/ }),

/***/ 78870:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


var internalObjectKeys = __webpack_require__(3613);

var enumBugKeys = __webpack_require__(23344);

var hiddenKeys = enumBugKeys.concat('length', 'prototype'); // `Object.getOwnPropertyNames` method
// https://tc39.es/ecma262/#sec-object.getownpropertynames
// eslint-disable-next-line es/no-object-getownpropertynames -- safe

exports.f = Object.getOwnPropertyNames || function () {
  function getOwnPropertyNames(O) {
    return internalObjectKeys(O, hiddenKeys);
  }

  return getOwnPropertyNames;
}();

/***/ }),

/***/ 21061:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


// eslint-disable-next-line es/no-object-getownpropertysymbols -- safe
exports.f = Object.getOwnPropertySymbols;

/***/ }),

/***/ 6863:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var has = __webpack_require__(28919);

var toObject = __webpack_require__(12104);

var sharedKey = __webpack_require__(85152);

var CORRECT_PROTOTYPE_GETTER = __webpack_require__(28954);

var IE_PROTO = sharedKey('IE_PROTO');
var ObjectPrototype = Object.prototype; // `Object.getPrototypeOf` method
// https://tc39.es/ecma262/#sec-object.getprototypeof
// eslint-disable-next-line es/no-object-getprototypeof -- safe

module.exports = CORRECT_PROTOTYPE_GETTER ? Object.getPrototypeOf : function (O) {
  O = toObject(O);
  if (has(O, IE_PROTO)) return O[IE_PROTO];

  if (typeof O.constructor == 'function' && O instanceof O.constructor) {
    return O.constructor.prototype;
  }

  return O instanceof Object ? ObjectPrototype : null;
};

/***/ }),

/***/ 3613:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var has = __webpack_require__(28919);

var toIndexedObject = __webpack_require__(26553);

var indexOf = __webpack_require__(25945).indexOf;

var hiddenKeys = __webpack_require__(90311);

module.exports = function (object, names) {
  var O = toIndexedObject(object);
  var i = 0;
  var result = [];
  var key;

  for (key in O) {
    !has(hiddenKeys, key) && has(O, key) && result.push(key);
  } // Don't enum bug & hidden keys


  while (names.length > i) {
    if (has(O, key = names[i++])) {
      ~indexOf(result, key) || result.push(key);
    }
  }

  return result;
};

/***/ }),

/***/ 84547:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var internalObjectKeys = __webpack_require__(3613);

var enumBugKeys = __webpack_require__(23344); // `Object.keys` method
// https://tc39.es/ecma262/#sec-object.keys
// eslint-disable-next-line es/no-object-keys -- safe


module.exports = Object.keys || function () {
  function keys(O) {
    return internalObjectKeys(O, enumBugKeys);
  }

  return keys;
}();

/***/ }),

/***/ 77659:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


var $propertyIsEnumerable = {}.propertyIsEnumerable; // eslint-disable-next-line es/no-object-getownpropertydescriptor -- safe

var getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor; // Nashorn ~ JDK8 bug

var NASHORN_BUG = getOwnPropertyDescriptor && !$propertyIsEnumerable.call({
  1: 2
}, 1); // `Object.prototype.propertyIsEnumerable` method implementation
// https://tc39.es/ecma262/#sec-object.prototype.propertyisenumerable

exports.f = NASHORN_BUG ? function () {
  function propertyIsEnumerable(V) {
    var descriptor = getOwnPropertyDescriptor(this, V);
    return !!descriptor && descriptor.enumerable;
  }

  return propertyIsEnumerable;
}() : $propertyIsEnumerable;

/***/ }),

/***/ 47339:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var IS_PURE = __webpack_require__(1986);

var global = __webpack_require__(84253);

var fails = __webpack_require__(69691);

var WEBKIT = __webpack_require__(12200); // Forced replacement object prototype accessors methods


module.exports = IS_PURE || !fails(function () {
  // This feature detection crashes old WebKit
  // https://github.com/zloirock/core-js/issues/232
  if (WEBKIT && WEBKIT < 535) return;
  var key = Math.random(); // In FF throws only define methods
  // eslint-disable-next-line no-undef, no-useless-call -- required for testing

  __defineSetter__.call(null, key, function () {
    /* empty */
  });

  delete global[key];
});

/***/ }),

/***/ 18444:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


/* eslint-disable no-proto -- safe */
var anObject = __webpack_require__(63518);

var aPossiblePrototype = __webpack_require__(1372); // `Object.setPrototypeOf` method
// https://tc39.es/ecma262/#sec-object.setprototypeof
// Works with __proto__ only. Old v8 can't work with null proto objects.
// eslint-disable-next-line es/no-object-setprototypeof -- safe


module.exports = Object.setPrototypeOf || ('__proto__' in {} ? function () {
  var CORRECT_SETTER = false;
  var test = {};
  var setter;

  try {
    // eslint-disable-next-line es/no-object-getownpropertydescriptor -- safe
    setter = Object.getOwnPropertyDescriptor(Object.prototype, '__proto__').set;
    setter.call(test, []);
    CORRECT_SETTER = test instanceof Array;
  } catch (error) {
    /* empty */
  }

  return function () {
    function setPrototypeOf(O, proto) {
      anObject(O);
      aPossiblePrototype(proto);
      if (CORRECT_SETTER) setter.call(O, proto);else O.__proto__ = proto;
      return O;
    }

    return setPrototypeOf;
  }();
}() : undefined);

/***/ }),

/***/ 30000:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var DESCRIPTORS = __webpack_require__(75592);

var objectKeys = __webpack_require__(84547);

var toIndexedObject = __webpack_require__(26553);

var propertyIsEnumerable = __webpack_require__(77659).f; // `Object.{ entries, values }` methods implementation


var createMethod = function createMethod(TO_ENTRIES) {
  return function (it) {
    var O = toIndexedObject(it);
    var keys = objectKeys(O);
    var length = keys.length;
    var i = 0;
    var result = [];
    var key;

    while (length > i) {
      key = keys[i++];

      if (!DESCRIPTORS || propertyIsEnumerable.call(O, key)) {
        result.push(TO_ENTRIES ? [key, O[key]] : O[key]);
      }
    }

    return result;
  };
};

module.exports = {
  // `Object.entries` method
  // https://tc39.es/ecma262/#sec-object.entries
  entries: createMethod(true),
  // `Object.values` method
  // https://tc39.es/ecma262/#sec-object.values
  values: createMethod(false)
};

/***/ }),

/***/ 13966:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var TO_STRING_TAG_SUPPORT = __webpack_require__(64910);

var classof = __webpack_require__(47953); // `Object.prototype.toString` method implementation
// https://tc39.es/ecma262/#sec-object.prototype.tostring


module.exports = TO_STRING_TAG_SUPPORT ? {}.toString : function () {
  function toString() {
    return '[object ' + classof(this) + ']';
  }

  return toString;
}();

/***/ }),

/***/ 28734:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var isObject = __webpack_require__(35611); // `OrdinaryToPrimitive` abstract operation
// https://tc39.es/ecma262/#sec-ordinarytoprimitive


module.exports = function (input, pref) {
  var fn, val;
  if (pref === 'string' && typeof (fn = input.toString) == 'function' && !isObject(val = fn.call(input))) return val;
  if (typeof (fn = input.valueOf) == 'function' && !isObject(val = fn.call(input))) return val;
  if (pref !== 'string' && typeof (fn = input.toString) == 'function' && !isObject(val = fn.call(input))) return val;
  throw TypeError("Can't convert object to primitive value");
};

/***/ }),

/***/ 28482:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var getBuiltIn = __webpack_require__(69808);

var getOwnPropertyNamesModule = __webpack_require__(78870);

var getOwnPropertySymbolsModule = __webpack_require__(21061);

var anObject = __webpack_require__(63518); // all object keys, includes non-enumerable and symbols


module.exports = getBuiltIn('Reflect', 'ownKeys') || function () {
  function ownKeys(it) {
    var keys = getOwnPropertyNamesModule.f(anObject(it));
    var getOwnPropertySymbols = getOwnPropertySymbolsModule.f;
    return getOwnPropertySymbols ? keys.concat(getOwnPropertySymbols(it)) : keys;
  }

  return ownKeys;
}();

/***/ }),

/***/ 65781:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var global = __webpack_require__(84253);

module.exports = global;

/***/ }),

/***/ 47673:
/***/ (function(module) {

"use strict";


module.exports = function (exec) {
  try {
    return {
      error: false,
      value: exec()
    };
  } catch (error) {
    return {
      error: true,
      value: error
    };
  }
};

/***/ }),

/***/ 48141:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var anObject = __webpack_require__(63518);

var isObject = __webpack_require__(35611);

var newPromiseCapability = __webpack_require__(82101);

module.exports = function (C, x) {
  anObject(C);
  if (isObject(x) && x.constructor === C) return x;
  var promiseCapability = newPromiseCapability.f(C);
  var resolve = promiseCapability.resolve;
  resolve(x);
  return promiseCapability.promise;
};

/***/ }),

/***/ 3993:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var redefine = __webpack_require__(11807);

module.exports = function (target, src, options) {
  for (var key in src) {
    redefine(target, key, src[key], options);
  }

  return target;
};

/***/ }),

/***/ 11807:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var global = __webpack_require__(84253);

var createNonEnumerableProperty = __webpack_require__(52188);

var has = __webpack_require__(28919);

var setGlobal = __webpack_require__(30889);

var inspectSource = __webpack_require__(89853);

var InternalStateModule = __webpack_require__(79947);

var getInternalState = InternalStateModule.get;
var enforceInternalState = InternalStateModule.enforce;
var TEMPLATE = String(String).split('String');
(module.exports = function (O, key, value, options) {
  var unsafe = options ? !!options.unsafe : false;
  var simple = options ? !!options.enumerable : false;
  var noTargetGet = options ? !!options.noTargetGet : false;
  var state;

  if (typeof value == 'function') {
    if (typeof key == 'string' && !has(value, 'name')) {
      createNonEnumerableProperty(value, 'name', key);
    }

    state = enforceInternalState(value);

    if (!state.source) {
      state.source = TEMPLATE.join(typeof key == 'string' ? key : '');
    }
  }

  if (O === global) {
    if (simple) O[key] = value;else setGlobal(key, value);
    return;
  } else if (!unsafe) {
    delete O[key];
  } else if (!noTargetGet && O[key]) {
    simple = true;
  }

  if (simple) O[key] = value;else createNonEnumerableProperty(O, key, value); // add fake Function#toString for correct work wrapped methods / constructors with methods like LoDash isNative
})(Function.prototype, 'toString', function () {
  function toString() {
    return typeof this == 'function' && getInternalState(this).source || inspectSource(this);
  }

  return toString;
}());

/***/ }),

/***/ 5641:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var classof = __webpack_require__(77000);

var regexpExec = __webpack_require__(71841); // `RegExpExec` abstract operation
// https://tc39.es/ecma262/#sec-regexpexec


module.exports = function (R, S) {
  var exec = R.exec;

  if (typeof exec === 'function') {
    var result = exec.call(R, S);

    if (typeof result !== 'object') {
      throw TypeError('RegExp exec method returned something other than an Object or null');
    }

    return result;
  }

  if (classof(R) !== 'RegExp') {
    throw TypeError('RegExp#exec called on incompatible receiver');
  }

  return regexpExec.call(R, S);
};

/***/ }),

/***/ 71841:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";

/* eslint-disable regexp/no-assertion-capturing-group, regexp/no-empty-group, regexp/no-lazy-ends -- testing */

/* eslint-disable regexp/no-useless-quantifier -- testing */

var toString = __webpack_require__(92082);

var regexpFlags = __webpack_require__(5105);

var stickyHelpers = __webpack_require__(1864);

var shared = __webpack_require__(27364);

var create = __webpack_require__(83681);

var getInternalState = __webpack_require__(79947).get;

var UNSUPPORTED_DOT_ALL = __webpack_require__(8368);

var UNSUPPORTED_NCG = __webpack_require__(2229);

var nativeExec = RegExp.prototype.exec;
var nativeReplace = shared('native-string-replace', String.prototype.replace);
var patchedExec = nativeExec;

var UPDATES_LAST_INDEX_WRONG = function () {
  var re1 = /a/;
  var re2 = /b*/g;
  nativeExec.call(re1, 'a');
  nativeExec.call(re2, 'a');
  return re1.lastIndex !== 0 || re2.lastIndex !== 0;
}();

var UNSUPPORTED_Y = stickyHelpers.UNSUPPORTED_Y || stickyHelpers.BROKEN_CARET; // nonparticipating capturing group, copied from es5-shim's String#split patch.

var NPCG_INCLUDED = /()??/.exec('')[1] !== undefined;
var PATCH = UPDATES_LAST_INDEX_WRONG || NPCG_INCLUDED || UNSUPPORTED_Y || UNSUPPORTED_DOT_ALL || UNSUPPORTED_NCG;

if (PATCH) {
  // eslint-disable-next-line max-statements -- TODO
  patchedExec = function () {
    function exec(string) {
      var re = this;
      var state = getInternalState(re);
      var str = toString(string);
      var raw = state.raw;
      var result, reCopy, lastIndex, match, i, object, group;

      if (raw) {
        raw.lastIndex = re.lastIndex;
        result = patchedExec.call(raw, str);
        re.lastIndex = raw.lastIndex;
        return result;
      }

      var groups = state.groups;
      var sticky = UNSUPPORTED_Y && re.sticky;
      var flags = regexpFlags.call(re);
      var source = re.source;
      var charsAdded = 0;
      var strCopy = str;

      if (sticky) {
        flags = flags.replace('y', '');

        if (flags.indexOf('g') === -1) {
          flags += 'g';
        }

        strCopy = str.slice(re.lastIndex); // Support anchored sticky behavior.

        if (re.lastIndex > 0 && (!re.multiline || re.multiline && str.charAt(re.lastIndex - 1) !== '\n')) {
          source = '(?: ' + source + ')';
          strCopy = ' ' + strCopy;
          charsAdded++;
        } // ^(? + rx + ) is needed, in combination with some str slicing, to
        // simulate the 'y' flag.


        reCopy = new RegExp('^(?:' + source + ')', flags);
      }

      if (NPCG_INCLUDED) {
        reCopy = new RegExp('^' + source + '$(?!\\s)', flags);
      }

      if (UPDATES_LAST_INDEX_WRONG) lastIndex = re.lastIndex;
      match = nativeExec.call(sticky ? reCopy : re, strCopy);

      if (sticky) {
        if (match) {
          match.input = match.input.slice(charsAdded);
          match[0] = match[0].slice(charsAdded);
          match.index = re.lastIndex;
          re.lastIndex += match[0].length;
        } else re.lastIndex = 0;
      } else if (UPDATES_LAST_INDEX_WRONG && match) {
        re.lastIndex = re.global ? match.index + match[0].length : lastIndex;
      }

      if (NPCG_INCLUDED && match && match.length > 1) {
        // Fix browsers whose `exec` methods don't consistently return `undefined`
        // for NPCG, like IE8. NOTE: This doesn' work for /(.?)?/
        nativeReplace.call(match[0], reCopy, function () {
          for (i = 1; i < arguments.length - 2; i++) {
            if (arguments[i] === undefined) match[i] = undefined;
          }
        });
      }

      if (match && groups) {
        match.groups = object = create(null);

        for (i = 0; i < groups.length; i++) {
          group = groups[i];
          object[group[0]] = match[group[1]];
        }
      }

      return match;
    }

    return exec;
  }();
}

module.exports = patchedExec;

/***/ }),

/***/ 5105:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var anObject = __webpack_require__(63518); // `RegExp.prototype.flags` getter implementation
// https://tc39.es/ecma262/#sec-get-regexp.prototype.flags


module.exports = function () {
  var that = anObject(this);
  var result = '';
  if (that.global) result += 'g';
  if (that.ignoreCase) result += 'i';
  if (that.multiline) result += 'm';
  if (that.dotAll) result += 's';
  if (that.unicode) result += 'u';
  if (that.sticky) result += 'y';
  return result;
};

/***/ }),

/***/ 1864:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


var fails = __webpack_require__(69691); // babel-minify transpiles RegExp('a', 'y') -> /a/y and it causes SyntaxError,


var RE = function RE(s, f) {
  return RegExp(s, f);
};

exports.UNSUPPORTED_Y = fails(function () {
  var re = RE('a', 'y');
  re.lastIndex = 2;
  return re.exec('abcd') != null;
});
exports.BROKEN_CARET = fails(function () {
  // https://bugzilla.mozilla.org/show_bug.cgi?id=773687
  var re = RE('^r', 'gy');
  re.lastIndex = 2;
  return re.exec('str') != null;
});

/***/ }),

/***/ 8368:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var fails = __webpack_require__(69691);

module.exports = fails(function () {
  // babel-minify transpiles RegExp('.', 's') -> /./s and it causes SyntaxError
  var re = RegExp('.', (typeof '').charAt(0));
  return !(re.dotAll && re.exec('\n') && re.flags === 's');
});

/***/ }),

/***/ 2229:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var fails = __webpack_require__(69691);

module.exports = fails(function () {
  // babel-minify transpiles RegExp('.', 'g') -> /./g and it causes SyntaxError
  var re = RegExp('(?<a>b)', (typeof '').charAt(5));
  return re.exec('b').groups.a !== 'b' || 'b'.replace(re, '$<a>c') !== 'bc';
});

/***/ }),

/***/ 44184:
/***/ (function(module) {

"use strict";


// `RequireObjectCoercible` abstract operation
// https://tc39.es/ecma262/#sec-requireobjectcoercible
module.exports = function (it) {
  if (it == undefined) throw TypeError("Can't call method on " + it);
  return it;
};

/***/ }),

/***/ 61398:
/***/ (function(module) {

"use strict";


// `SameValue` abstract operation
// https://tc39.es/ecma262/#sec-samevalue
// eslint-disable-next-line es/no-object-is -- safe
module.exports = Object.is || function () {
  function is(x, y) {
    // eslint-disable-next-line no-self-compare -- NaN check
    return x === y ? x !== 0 || 1 / x === 1 / y : x != x && y != y;
  }

  return is;
}();

/***/ }),

/***/ 30889:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var global = __webpack_require__(84253);

module.exports = function (key, value) {
  try {
    // eslint-disable-next-line es/no-object-defineproperty -- safe
    Object.defineProperty(global, key, {
      value: value,
      configurable: true,
      writable: true
    });
  } catch (error) {
    global[key] = value;
  }

  return value;
};

/***/ }),

/***/ 64157:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var getBuiltIn = __webpack_require__(69808);

var definePropertyModule = __webpack_require__(74217);

var wellKnownSymbol = __webpack_require__(17657);

var DESCRIPTORS = __webpack_require__(75592);

var SPECIES = wellKnownSymbol('species');

module.exports = function (CONSTRUCTOR_NAME) {
  var Constructor = getBuiltIn(CONSTRUCTOR_NAME);
  var defineProperty = definePropertyModule.f;

  if (DESCRIPTORS && Constructor && !Constructor[SPECIES]) {
    defineProperty(Constructor, SPECIES, {
      configurable: true,
      get: function () {
        function get() {
          return this;
        }

        return get;
      }()
    });
  }
};

/***/ }),

/***/ 29108:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var defineProperty = __webpack_require__(74217).f;

var has = __webpack_require__(28919);

var wellKnownSymbol = __webpack_require__(17657);

var TO_STRING_TAG = wellKnownSymbol('toStringTag');

module.exports = function (it, TAG, STATIC) {
  if (it && !has(it = STATIC ? it : it.prototype, TO_STRING_TAG)) {
    defineProperty(it, TO_STRING_TAG, {
      configurable: true,
      value: TAG
    });
  }
};

/***/ }),

/***/ 85152:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var shared = __webpack_require__(27364);

var uid = __webpack_require__(41080);

var keys = shared('keys');

module.exports = function (key) {
  return keys[key] || (keys[key] = uid(key));
};

/***/ }),

/***/ 55935:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var global = __webpack_require__(84253);

var setGlobal = __webpack_require__(30889);

var SHARED = '__core-js_shared__';
var store = global[SHARED] || setGlobal(SHARED, {});
module.exports = store;

/***/ }),

/***/ 27364:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var IS_PURE = __webpack_require__(1986);

var store = __webpack_require__(55935);

(module.exports = function (key, value) {
  return store[key] || (store[key] = value !== undefined ? value : {});
})('versions', []).push({
  version: '3.16.1',
  mode: IS_PURE ? 'pure' : 'global',
  copyright: '© 2021 Denis Pushkarev (zloirock.ru)'
});

/***/ }),

/***/ 24345:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var anObject = __webpack_require__(63518);

var aFunction = __webpack_require__(83923);

var wellKnownSymbol = __webpack_require__(17657);

var SPECIES = wellKnownSymbol('species'); // `SpeciesConstructor` abstract operation
// https://tc39.es/ecma262/#sec-speciesconstructor

module.exports = function (O, defaultConstructor) {
  var C = anObject(O).constructor;
  var S;
  return C === undefined || (S = anObject(C)[SPECIES]) == undefined ? defaultConstructor : aFunction(S);
};

/***/ }),

/***/ 26096:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var fails = __webpack_require__(69691); // check the existence of a method, lowercase
// of a tag and escaping quotes in arguments


module.exports = function (METHOD_NAME) {
  return fails(function () {
    var test = ''[METHOD_NAME]('"');
    return test !== test.toLowerCase() || test.split('"').length > 3;
  });
};

/***/ }),

/***/ 37015:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var toInteger = __webpack_require__(3997);

var toString = __webpack_require__(92082);

var requireObjectCoercible = __webpack_require__(44184); // `String.prototype.codePointAt` methods implementation


var createMethod = function createMethod(CONVERT_TO_STRING) {
  return function ($this, pos) {
    var S = toString(requireObjectCoercible($this));
    var position = toInteger(pos);
    var size = S.length;
    var first, second;
    if (position < 0 || position >= size) return CONVERT_TO_STRING ? '' : undefined;
    first = S.charCodeAt(position);
    return first < 0xD800 || first > 0xDBFF || position + 1 === size || (second = S.charCodeAt(position + 1)) < 0xDC00 || second > 0xDFFF ? CONVERT_TO_STRING ? S.charAt(position) : first : CONVERT_TO_STRING ? S.slice(position, position + 2) : (first - 0xD800 << 10) + (second - 0xDC00) + 0x10000;
  };
};

module.exports = {
  // `String.prototype.codePointAt` method
  // https://tc39.es/ecma262/#sec-string.prototype.codepointat
  codeAt: createMethod(false),
  // `String.prototype.at` method
  // https://github.com/mathiasbynens/String.prototype.at
  charAt: createMethod(true)
};

/***/ }),

/***/ 43331:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


// https://github.com/zloirock/core-js/issues/280
var userAgent = __webpack_require__(72319); // eslint-disable-next-line unicorn/no-unsafe-regex -- safe


module.exports = /Version\/10(?:\.\d+){1,2}(?: [\w./]+)?(?: Mobile\/\w+)? Safari\//.test(userAgent);

/***/ }),

/***/ 47497:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


// https://github.com/tc39/proposal-string-pad-start-end
var toLength = __webpack_require__(39570);

var toString = __webpack_require__(92082);

var repeat = __webpack_require__(9811);

var requireObjectCoercible = __webpack_require__(44184);

var ceil = Math.ceil; // `String.prototype.{ padStart, padEnd }` methods implementation

var createMethod = function createMethod(IS_END) {
  return function ($this, maxLength, fillString) {
    var S = toString(requireObjectCoercible($this));
    var stringLength = S.length;
    var fillStr = fillString === undefined ? ' ' : toString(fillString);
    var intMaxLength = toLength(maxLength);
    var fillLen, stringFiller;
    if (intMaxLength <= stringLength || fillStr == '') return S;
    fillLen = intMaxLength - stringLength;
    stringFiller = repeat.call(fillStr, ceil(fillLen / fillStr.length));
    if (stringFiller.length > fillLen) stringFiller = stringFiller.slice(0, fillLen);
    return IS_END ? S + stringFiller : stringFiller + S;
  };
};

module.exports = {
  // `String.prototype.padStart` method
  // https://tc39.es/ecma262/#sec-string.prototype.padstart
  start: createMethod(false),
  // `String.prototype.padEnd` method
  // https://tc39.es/ecma262/#sec-string.prototype.padend
  end: createMethod(true)
};

/***/ }),

/***/ 9811:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var toInteger = __webpack_require__(3997);

var toString = __webpack_require__(92082);

var requireObjectCoercible = __webpack_require__(44184); // `String.prototype.repeat` method implementation
// https://tc39.es/ecma262/#sec-string.prototype.repeat


module.exports = function () {
  function repeat(count) {
    var str = toString(requireObjectCoercible(this));
    var result = '';
    var n = toInteger(count);
    if (n < 0 || n == Infinity) throw RangeError('Wrong number of repetitions');

    for (; n > 0; (n >>>= 1) && (str += str)) {
      if (n & 1) result += str;
    }

    return result;
  }

  return repeat;
}();

/***/ }),

/***/ 3035:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var fails = __webpack_require__(69691);

var whitespaces = __webpack_require__(65181);

var non = "\u200B\x85\u180E"; // check that a method works with the correct list
// of whitespaces and has a correct name

module.exports = function (METHOD_NAME) {
  return fails(function () {
    return !!whitespaces[METHOD_NAME]() || non[METHOD_NAME]() != non || whitespaces[METHOD_NAME].name !== METHOD_NAME;
  });
};

/***/ }),

/***/ 84703:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var requireObjectCoercible = __webpack_require__(44184);

var toString = __webpack_require__(92082);

var whitespaces = __webpack_require__(65181);

var whitespace = '[' + whitespaces + ']';
var ltrim = RegExp('^' + whitespace + whitespace + '*');
var rtrim = RegExp(whitespace + whitespace + '*$'); // `String.prototype.{ trim, trimStart, trimEnd, trimLeft, trimRight }` methods implementation

var createMethod = function createMethod(TYPE) {
  return function ($this) {
    var string = toString(requireObjectCoercible($this));
    if (TYPE & 1) string = string.replace(ltrim, '');
    if (TYPE & 2) string = string.replace(rtrim, '');
    return string;
  };
};

module.exports = {
  // `String.prototype.{ trimLeft, trimStart }` methods
  // https://tc39.es/ecma262/#sec-string.prototype.trimstart
  start: createMethod(1),
  // `String.prototype.{ trimRight, trimEnd }` methods
  // https://tc39.es/ecma262/#sec-string.prototype.trimend
  end: createMethod(2),
  // `String.prototype.trim` method
  // https://tc39.es/ecma262/#sec-string.prototype.trim
  trim: createMethod(3)
};

/***/ }),

/***/ 33453:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var global = __webpack_require__(84253);

var fails = __webpack_require__(69691);

var bind = __webpack_require__(74271);

var html = __webpack_require__(2369);

var createElement = __webpack_require__(21477);

var IS_IOS = __webpack_require__(24542);

var IS_NODE = __webpack_require__(41488);

var set = global.setImmediate;
var clear = global.clearImmediate;
var process = global.process;
var MessageChannel = global.MessageChannel;
var Dispatch = global.Dispatch;
var counter = 0;
var queue = {};
var ONREADYSTATECHANGE = 'onreadystatechange';
var location, defer, channel, port;

try {
  // Deno throws a ReferenceError on `location` access without `--location` flag
  location = global.location;
} catch (error) {
  /* empty */
}

var run = function run(id) {
  // eslint-disable-next-line no-prototype-builtins -- safe
  if (queue.hasOwnProperty(id)) {
    var fn = queue[id];
    delete queue[id];
    fn();
  }
};

var runner = function runner(id) {
  return function () {
    run(id);
  };
};

var listener = function listener(event) {
  run(event.data);
};

var post = function post(id) {
  // old engines have not location.origin
  global.postMessage(String(id), location.protocol + '//' + location.host);
}; // Node.js 0.9+ & IE10+ has setImmediate, otherwise:


if (!set || !clear) {
  set = function () {
    function setImmediate(fn) {
      var args = [];
      var argumentsLength = arguments.length;
      var i = 1;

      while (argumentsLength > i) {
        args.push(arguments[i++]);
      }

      queue[++counter] = function () {
        // eslint-disable-next-line no-new-func -- spec requirement
        (typeof fn == 'function' ? fn : Function(fn)).apply(undefined, args);
      };

      defer(counter);
      return counter;
    }

    return setImmediate;
  }();

  clear = function () {
    function clearImmediate(id) {
      delete queue[id];
    }

    return clearImmediate;
  }(); // Node.js 0.8-


  if (IS_NODE) {
    defer = function defer(id) {
      process.nextTick(runner(id));
    }; // Sphere (JS game engine) Dispatch API

  } else if (Dispatch && Dispatch.now) {
    defer = function defer(id) {
      Dispatch.now(runner(id));
    }; // Browsers with MessageChannel, includes WebWorkers
    // except iOS - https://github.com/zloirock/core-js/issues/624

  } else if (MessageChannel && !IS_IOS) {
    channel = new MessageChannel();
    port = channel.port2;
    channel.port1.onmessage = listener;
    defer = bind(port.postMessage, port, 1); // Browsers with postMessage, skip WebWorkers
    // IE8 has postMessage, but it's sync & typeof its postMessage is 'object'
  } else if (global.addEventListener && typeof postMessage == 'function' && !global.importScripts && location && location.protocol !== 'file:' && !fails(post)) {
    defer = post;
    global.addEventListener('message', listener, false); // IE8-
  } else if (ONREADYSTATECHANGE in createElement('script')) {
    defer = function defer(id) {
      html.appendChild(createElement('script'))[ONREADYSTATECHANGE] = function () {
        html.removeChild(this);
        run(id);
      };
    }; // Rest old browsers

  } else {
    defer = function defer(id) {
      setTimeout(runner(id), 0);
    };
  }
}

module.exports = {
  set: set,
  clear: clear
};

/***/ }),

/***/ 81687:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var classof = __webpack_require__(77000); // `thisNumberValue` abstract operation
// https://tc39.es/ecma262/#sec-thisnumbervalue


module.exports = function (value) {
  if (typeof value != 'number' && classof(value) != 'Number') {
    throw TypeError('Incorrect invocation');
  }

  return +value;
};

/***/ }),

/***/ 55205:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var toInteger = __webpack_require__(3997);

var max = Math.max;
var min = Math.min; // Helper for a popular repeating case of the spec:
// Let integer be ? ToInteger(index).
// If integer < 0, let result be max((length + integer), 0); else let result be min(integer, length).

module.exports = function (index, length) {
  var integer = toInteger(index);
  return integer < 0 ? max(integer + length, 0) : min(integer, length);
};

/***/ }),

/***/ 77559:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var toInteger = __webpack_require__(3997);

var toLength = __webpack_require__(39570); // `ToIndex` abstract operation
// https://tc39.es/ecma262/#sec-toindex


module.exports = function (it) {
  if (it === undefined) return 0;
  var number = toInteger(it);
  var length = toLength(number);
  if (number !== length) throw RangeError('Wrong length or index');
  return length;
};

/***/ }),

/***/ 26553:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


// toObject with fallback for non-array-like ES3 strings
var IndexedObject = __webpack_require__(50990);

var requireObjectCoercible = __webpack_require__(44184);

module.exports = function (it) {
  return IndexedObject(requireObjectCoercible(it));
};

/***/ }),

/***/ 3997:
/***/ (function(module) {

"use strict";


var ceil = Math.ceil;
var floor = Math.floor; // `ToInteger` abstract operation
// https://tc39.es/ecma262/#sec-tointeger

module.exports = function (argument) {
  return isNaN(argument = +argument) ? 0 : (argument > 0 ? floor : ceil)(argument);
};

/***/ }),

/***/ 39570:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var toInteger = __webpack_require__(3997);

var min = Math.min; // `ToLength` abstract operation
// https://tc39.es/ecma262/#sec-tolength

module.exports = function (argument) {
  return argument > 0 ? min(toInteger(argument), 0x1FFFFFFFFFFFFF) : 0; // 2 ** 53 - 1 == 9007199254740991
};

/***/ }),

/***/ 12104:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var requireObjectCoercible = __webpack_require__(44184); // `ToObject` abstract operation
// https://tc39.es/ecma262/#sec-toobject


module.exports = function (argument) {
  return Object(requireObjectCoercible(argument));
};

/***/ }),

/***/ 95740:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var toPositiveInteger = __webpack_require__(80425);

module.exports = function (it, BYTES) {
  var offset = toPositiveInteger(it);
  if (offset % BYTES) throw RangeError('Wrong offset');
  return offset;
};

/***/ }),

/***/ 80425:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var toInteger = __webpack_require__(3997);

module.exports = function (it) {
  var result = toInteger(it);
  if (result < 0) throw RangeError("The argument can't be less than 0");
  return result;
};

/***/ }),

/***/ 94673:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var isObject = __webpack_require__(35611);

var isSymbol = __webpack_require__(78253);

var ordinaryToPrimitive = __webpack_require__(28734);

var wellKnownSymbol = __webpack_require__(17657);

var TO_PRIMITIVE = wellKnownSymbol('toPrimitive'); // `ToPrimitive` abstract operation
// https://tc39.es/ecma262/#sec-toprimitive

module.exports = function (input, pref) {
  if (!isObject(input) || isSymbol(input)) return input;
  var exoticToPrim = input[TO_PRIMITIVE];
  var result;

  if (exoticToPrim !== undefined) {
    if (pref === undefined) pref = 'default';
    result = exoticToPrim.call(input, pref);
    if (!isObject(result) || isSymbol(result)) return result;
    throw TypeError("Can't convert object to primitive value");
  }

  if (pref === undefined) pref = 'number';
  return ordinaryToPrimitive(input, pref);
};

/***/ }),

/***/ 14613:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var toPrimitive = __webpack_require__(94673);

var isSymbol = __webpack_require__(78253); // `ToPropertyKey` abstract operation
// https://tc39.es/ecma262/#sec-topropertykey


module.exports = function (argument) {
  var key = toPrimitive(argument, 'string');
  return isSymbol(key) ? key : String(key);
};

/***/ }),

/***/ 64910:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var wellKnownSymbol = __webpack_require__(17657);

var TO_STRING_TAG = wellKnownSymbol('toStringTag');
var test = {};
test[TO_STRING_TAG] = 'z';
module.exports = String(test) === '[object z]';

/***/ }),

/***/ 92082:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var isSymbol = __webpack_require__(78253);

module.exports = function (argument) {
  if (isSymbol(argument)) throw TypeError('Cannot convert a Symbol value to a string');
  return String(argument);
};

/***/ }),

/***/ 17576:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var global = __webpack_require__(84253);

var DESCRIPTORS = __webpack_require__(75592);

var TYPED_ARRAYS_CONSTRUCTORS_REQUIRES_WRAPPERS = __webpack_require__(29023);

var ArrayBufferViewCore = __webpack_require__(19216);

var ArrayBufferModule = __webpack_require__(80246);

var anInstance = __webpack_require__(50997);

var createPropertyDescriptor = __webpack_require__(39160);

var createNonEnumerableProperty = __webpack_require__(52188);

var isInteger = __webpack_require__(93624);

var toLength = __webpack_require__(39570);

var toIndex = __webpack_require__(77559);

var toOffset = __webpack_require__(95740);

var toPropertyKey = __webpack_require__(14613);

var has = __webpack_require__(28919);

var classof = __webpack_require__(47953);

var isObject = __webpack_require__(35611);

var isSymbol = __webpack_require__(78253);

var create = __webpack_require__(83681);

var setPrototypeOf = __webpack_require__(18444);

var getOwnPropertyNames = __webpack_require__(78870).f;

var typedArrayFrom = __webpack_require__(76182);

var forEach = __webpack_require__(69033).forEach;

var setSpecies = __webpack_require__(64157);

var definePropertyModule = __webpack_require__(74217);

var getOwnPropertyDescriptorModule = __webpack_require__(22245);

var InternalStateModule = __webpack_require__(79947);

var inheritIfRequired = __webpack_require__(22285);

var getInternalState = InternalStateModule.get;
var setInternalState = InternalStateModule.set;
var nativeDefineProperty = definePropertyModule.f;
var nativeGetOwnPropertyDescriptor = getOwnPropertyDescriptorModule.f;
var round = Math.round;
var RangeError = global.RangeError;
var ArrayBuffer = ArrayBufferModule.ArrayBuffer;
var DataView = ArrayBufferModule.DataView;
var NATIVE_ARRAY_BUFFER_VIEWS = ArrayBufferViewCore.NATIVE_ARRAY_BUFFER_VIEWS;
var TYPED_ARRAY_CONSTRUCTOR = ArrayBufferViewCore.TYPED_ARRAY_CONSTRUCTOR;
var TYPED_ARRAY_TAG = ArrayBufferViewCore.TYPED_ARRAY_TAG;
var TypedArray = ArrayBufferViewCore.TypedArray;
var TypedArrayPrototype = ArrayBufferViewCore.TypedArrayPrototype;
var aTypedArrayConstructor = ArrayBufferViewCore.aTypedArrayConstructor;
var isTypedArray = ArrayBufferViewCore.isTypedArray;
var BYTES_PER_ELEMENT = 'BYTES_PER_ELEMENT';
var WRONG_LENGTH = 'Wrong length';

var fromList = function fromList(C, list) {
  var index = 0;
  var length = list.length;
  var result = new (aTypedArrayConstructor(C))(length);

  while (length > index) {
    result[index] = list[index++];
  }

  return result;
};

var addGetter = function addGetter(it, key) {
  nativeDefineProperty(it, key, {
    get: function () {
      function get() {
        return getInternalState(this)[key];
      }

      return get;
    }()
  });
};

var isArrayBuffer = function isArrayBuffer(it) {
  var klass;
  return it instanceof ArrayBuffer || (klass = classof(it)) == 'ArrayBuffer' || klass == 'SharedArrayBuffer';
};

var isTypedArrayIndex = function isTypedArrayIndex(target, key) {
  return isTypedArray(target) && !isSymbol(key) && key in target && isInteger(+key) && key >= 0;
};

var wrappedGetOwnPropertyDescriptor = function () {
  function getOwnPropertyDescriptor(target, key) {
    key = toPropertyKey(key);
    return isTypedArrayIndex(target, key) ? createPropertyDescriptor(2, target[key]) : nativeGetOwnPropertyDescriptor(target, key);
  }

  return getOwnPropertyDescriptor;
}();

var wrappedDefineProperty = function () {
  function defineProperty(target, key, descriptor) {
    key = toPropertyKey(key);

    if (isTypedArrayIndex(target, key) && isObject(descriptor) && has(descriptor, 'value') && !has(descriptor, 'get') && !has(descriptor, 'set') // TODO: add validation descriptor w/o calling accessors
    && !descriptor.configurable && (!has(descriptor, 'writable') || descriptor.writable) && (!has(descriptor, 'enumerable') || descriptor.enumerable)) {
      target[key] = descriptor.value;
      return target;
    }

    return nativeDefineProperty(target, key, descriptor);
  }

  return defineProperty;
}();

if (DESCRIPTORS) {
  if (!NATIVE_ARRAY_BUFFER_VIEWS) {
    getOwnPropertyDescriptorModule.f = wrappedGetOwnPropertyDescriptor;
    definePropertyModule.f = wrappedDefineProperty;
    addGetter(TypedArrayPrototype, 'buffer');
    addGetter(TypedArrayPrototype, 'byteOffset');
    addGetter(TypedArrayPrototype, 'byteLength');
    addGetter(TypedArrayPrototype, 'length');
  }

  $({
    target: 'Object',
    stat: true,
    forced: !NATIVE_ARRAY_BUFFER_VIEWS
  }, {
    getOwnPropertyDescriptor: wrappedGetOwnPropertyDescriptor,
    defineProperty: wrappedDefineProperty
  });

  module.exports = function (TYPE, wrapper, CLAMPED) {
    var BYTES = TYPE.match(/\d+$/)[0] / 8;
    var CONSTRUCTOR_NAME = TYPE + (CLAMPED ? 'Clamped' : '') + 'Array';
    var GETTER = 'get' + TYPE;
    var SETTER = 'set' + TYPE;
    var NativeTypedArrayConstructor = global[CONSTRUCTOR_NAME];
    var TypedArrayConstructor = NativeTypedArrayConstructor;
    var TypedArrayConstructorPrototype = TypedArrayConstructor && TypedArrayConstructor.prototype;
    var exported = {};

    var getter = function getter(that, index) {
      var data = getInternalState(that);
      return data.view[GETTER](index * BYTES + data.byteOffset, true);
    };

    var setter = function setter(that, index, value) {
      var data = getInternalState(that);
      if (CLAMPED) value = (value = round(value)) < 0 ? 0 : value > 0xFF ? 0xFF : value & 0xFF;
      data.view[SETTER](index * BYTES + data.byteOffset, value, true);
    };

    var addElement = function addElement(that, index) {
      nativeDefineProperty(that, index, {
        get: function () {
          function get() {
            return getter(this, index);
          }

          return get;
        }(),
        set: function () {
          function set(value) {
            return setter(this, index, value);
          }

          return set;
        }(),
        enumerable: true
      });
    };

    if (!NATIVE_ARRAY_BUFFER_VIEWS) {
      TypedArrayConstructor = wrapper(function (that, data, offset, $length) {
        anInstance(that, TypedArrayConstructor, CONSTRUCTOR_NAME);
        var index = 0;
        var byteOffset = 0;
        var buffer, byteLength, length;

        if (!isObject(data)) {
          length = toIndex(data);
          byteLength = length * BYTES;
          buffer = new ArrayBuffer(byteLength);
        } else if (isArrayBuffer(data)) {
          buffer = data;
          byteOffset = toOffset(offset, BYTES);
          var $len = data.byteLength;

          if ($length === undefined) {
            if ($len % BYTES) throw RangeError(WRONG_LENGTH);
            byteLength = $len - byteOffset;
            if (byteLength < 0) throw RangeError(WRONG_LENGTH);
          } else {
            byteLength = toLength($length) * BYTES;
            if (byteLength + byteOffset > $len) throw RangeError(WRONG_LENGTH);
          }

          length = byteLength / BYTES;
        } else if (isTypedArray(data)) {
          return fromList(TypedArrayConstructor, data);
        } else {
          return typedArrayFrom.call(TypedArrayConstructor, data);
        }

        setInternalState(that, {
          buffer: buffer,
          byteOffset: byteOffset,
          byteLength: byteLength,
          length: length,
          view: new DataView(buffer)
        });

        while (index < length) {
          addElement(that, index++);
        }
      });
      if (setPrototypeOf) setPrototypeOf(TypedArrayConstructor, TypedArray);
      TypedArrayConstructorPrototype = TypedArrayConstructor.prototype = create(TypedArrayPrototype);
    } else if (TYPED_ARRAYS_CONSTRUCTORS_REQUIRES_WRAPPERS) {
      TypedArrayConstructor = wrapper(function (dummy, data, typedArrayOffset, $length) {
        anInstance(dummy, TypedArrayConstructor, CONSTRUCTOR_NAME);
        return inheritIfRequired(function () {
          if (!isObject(data)) return new NativeTypedArrayConstructor(toIndex(data));
          if (isArrayBuffer(data)) return $length !== undefined ? new NativeTypedArrayConstructor(data, toOffset(typedArrayOffset, BYTES), $length) : typedArrayOffset !== undefined ? new NativeTypedArrayConstructor(data, toOffset(typedArrayOffset, BYTES)) : new NativeTypedArrayConstructor(data);
          if (isTypedArray(data)) return fromList(TypedArrayConstructor, data);
          return typedArrayFrom.call(TypedArrayConstructor, data);
        }(), dummy, TypedArrayConstructor);
      });
      if (setPrototypeOf) setPrototypeOf(TypedArrayConstructor, TypedArray);
      forEach(getOwnPropertyNames(NativeTypedArrayConstructor), function (key) {
        if (!(key in TypedArrayConstructor)) {
          createNonEnumerableProperty(TypedArrayConstructor, key, NativeTypedArrayConstructor[key]);
        }
      });
      TypedArrayConstructor.prototype = TypedArrayConstructorPrototype;
    }

    if (TypedArrayConstructorPrototype.constructor !== TypedArrayConstructor) {
      createNonEnumerableProperty(TypedArrayConstructorPrototype, 'constructor', TypedArrayConstructor);
    }

    createNonEnumerableProperty(TypedArrayConstructorPrototype, TYPED_ARRAY_CONSTRUCTOR, TypedArrayConstructor);

    if (TYPED_ARRAY_TAG) {
      createNonEnumerableProperty(TypedArrayConstructorPrototype, TYPED_ARRAY_TAG, CONSTRUCTOR_NAME);
    }

    exported[CONSTRUCTOR_NAME] = TypedArrayConstructor;
    $({
      global: true,
      forced: TypedArrayConstructor != NativeTypedArrayConstructor,
      sham: !NATIVE_ARRAY_BUFFER_VIEWS
    }, exported);

    if (!(BYTES_PER_ELEMENT in TypedArrayConstructor)) {
      createNonEnumerableProperty(TypedArrayConstructor, BYTES_PER_ELEMENT, BYTES);
    }

    if (!(BYTES_PER_ELEMENT in TypedArrayConstructorPrototype)) {
      createNonEnumerableProperty(TypedArrayConstructorPrototype, BYTES_PER_ELEMENT, BYTES);
    }

    setSpecies(CONSTRUCTOR_NAME);
  };
} else module.exports = function () {
  /* empty */
};

/***/ }),

/***/ 29023:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


/* eslint-disable no-new -- required for testing */
var global = __webpack_require__(84253);

var fails = __webpack_require__(69691);

var checkCorrectnessOfIteration = __webpack_require__(68762);

var NATIVE_ARRAY_BUFFER_VIEWS = __webpack_require__(19216).NATIVE_ARRAY_BUFFER_VIEWS;

var ArrayBuffer = global.ArrayBuffer;
var Int8Array = global.Int8Array;
module.exports = !NATIVE_ARRAY_BUFFER_VIEWS || !fails(function () {
  Int8Array(1);
}) || !fails(function () {
  new Int8Array(-1);
}) || !checkCorrectnessOfIteration(function (iterable) {
  new Int8Array();
  new Int8Array(null);
  new Int8Array(1.5);
  new Int8Array(iterable);
}, true) || fails(function () {
  // Safari (11+) bug - a reason why even Safari 13 should load a typed array polyfill
  return new Int8Array(new ArrayBuffer(2), 1, undefined).length !== 1;
});

/***/ }),

/***/ 16688:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var arrayFromConstructorAndList = __webpack_require__(11955);

var typedArraySpeciesConstructor = __webpack_require__(25198);

module.exports = function (instance, list) {
  return arrayFromConstructorAndList(typedArraySpeciesConstructor(instance), list);
};

/***/ }),

/***/ 76182:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var toObject = __webpack_require__(12104);

var toLength = __webpack_require__(39570);

var getIteratorMethod = __webpack_require__(3170);

var isArrayIteratorMethod = __webpack_require__(14195);

var bind = __webpack_require__(74271);

var aTypedArrayConstructor = __webpack_require__(19216).aTypedArrayConstructor;

module.exports = function () {
  function from(source
  /* , mapfn, thisArg */
  ) {
    var O = toObject(source);
    var argumentsLength = arguments.length;
    var mapfn = argumentsLength > 1 ? arguments[1] : undefined;
    var mapping = mapfn !== undefined;
    var iteratorMethod = getIteratorMethod(O);
    var i, length, result, step, iterator, next;

    if (iteratorMethod != undefined && !isArrayIteratorMethod(iteratorMethod)) {
      iterator = iteratorMethod.call(O);
      next = iterator.next;
      O = [];

      while (!(step = next.call(iterator)).done) {
        O.push(step.value);
      }
    }

    if (mapping && argumentsLength > 2) {
      mapfn = bind(mapfn, arguments[2], 2);
    }

    length = toLength(O.length);
    result = new (aTypedArrayConstructor(this))(length);

    for (i = 0; length > i; i++) {
      result[i] = mapping ? mapfn(O[i], i) : O[i];
    }

    return result;
  }

  return from;
}();

/***/ }),

/***/ 25198:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var ArrayBufferViewCore = __webpack_require__(19216);

var speciesConstructor = __webpack_require__(24345);

var TYPED_ARRAY_CONSTRUCTOR = ArrayBufferViewCore.TYPED_ARRAY_CONSTRUCTOR;
var aTypedArrayConstructor = ArrayBufferViewCore.aTypedArrayConstructor; // a part of `TypedArraySpeciesCreate` abstract operation
// https://tc39.es/ecma262/#typedarray-species-create

module.exports = function (originalArray) {
  return aTypedArrayConstructor(speciesConstructor(originalArray, originalArray[TYPED_ARRAY_CONSTRUCTOR]));
};

/***/ }),

/***/ 41080:
/***/ (function(module) {

"use strict";


var id = 0;
var postfix = Math.random();

module.exports = function (key) {
  return 'Symbol(' + String(key === undefined ? '' : key) + ')_' + (++id + postfix).toString(36);
};

/***/ }),

/***/ 40647:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


/* eslint-disable es/no-symbol -- required for testing */
var NATIVE_SYMBOL = __webpack_require__(54772);

module.exports = NATIVE_SYMBOL && !Symbol.sham && typeof Symbol.iterator == 'symbol';

/***/ }),

/***/ 3444:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


var wellKnownSymbol = __webpack_require__(17657);

exports.f = wellKnownSymbol;

/***/ }),

/***/ 17657:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var global = __webpack_require__(84253);

var shared = __webpack_require__(27364);

var has = __webpack_require__(28919);

var uid = __webpack_require__(41080);

var NATIVE_SYMBOL = __webpack_require__(54772);

var USE_SYMBOL_AS_UID = __webpack_require__(40647);

var WellKnownSymbolsStore = shared('wks');
var Symbol = global.Symbol;
var createWellKnownSymbol = USE_SYMBOL_AS_UID ? Symbol : Symbol && Symbol.withoutSetter || uid;

module.exports = function (name) {
  if (!has(WellKnownSymbolsStore, name) || !(NATIVE_SYMBOL || typeof WellKnownSymbolsStore[name] == 'string')) {
    if (NATIVE_SYMBOL && has(Symbol, name)) {
      WellKnownSymbolsStore[name] = Symbol[name];
    } else {
      WellKnownSymbolsStore[name] = createWellKnownSymbol('Symbol.' + name);
    }
  }

  return WellKnownSymbolsStore[name];
};

/***/ }),

/***/ 65181:
/***/ (function(module) {

"use strict";


// a string of all valid unicode whitespaces
module.exports = "\t\n\x0B\f\r \xA0\u1680\u2000\u2001\u2002" + "\u2003\u2004\u2005\u2006\u2007\u2008\u2009\u200A\u202F\u205F\u3000\u2028\u2029\uFEFF";

/***/ }),

/***/ 15196:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var global = __webpack_require__(84253);

var arrayBufferModule = __webpack_require__(80246);

var setSpecies = __webpack_require__(64157);

var ARRAY_BUFFER = 'ArrayBuffer';
var ArrayBuffer = arrayBufferModule[ARRAY_BUFFER];
var NativeArrayBuffer = global[ARRAY_BUFFER]; // `ArrayBuffer` constructor
// https://tc39.es/ecma262/#sec-arraybuffer-constructor

$({
  global: true,
  forced: NativeArrayBuffer !== ArrayBuffer
}, {
  ArrayBuffer: ArrayBuffer
});
setSpecies(ARRAY_BUFFER);

/***/ }),

/***/ 95993:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var ArrayBufferViewCore = __webpack_require__(19216);

var NATIVE_ARRAY_BUFFER_VIEWS = ArrayBufferViewCore.NATIVE_ARRAY_BUFFER_VIEWS; // `ArrayBuffer.isView` method
// https://tc39.es/ecma262/#sec-arraybuffer.isview

$({
  target: 'ArrayBuffer',
  stat: true,
  forced: !NATIVE_ARRAY_BUFFER_VIEWS
}, {
  isView: ArrayBufferViewCore.isView
});

/***/ }),

/***/ 25079:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var fails = __webpack_require__(69691);

var ArrayBufferModule = __webpack_require__(80246);

var anObject = __webpack_require__(63518);

var toAbsoluteIndex = __webpack_require__(55205);

var toLength = __webpack_require__(39570);

var speciesConstructor = __webpack_require__(24345);

var ArrayBuffer = ArrayBufferModule.ArrayBuffer;
var DataView = ArrayBufferModule.DataView;
var nativeArrayBufferSlice = ArrayBuffer.prototype.slice;
var INCORRECT_SLICE = fails(function () {
  return !new ArrayBuffer(2).slice(1, undefined).byteLength;
}); // `ArrayBuffer.prototype.slice` method
// https://tc39.es/ecma262/#sec-arraybuffer.prototype.slice

$({
  target: 'ArrayBuffer',
  proto: true,
  unsafe: true,
  forced: INCORRECT_SLICE
}, {
  slice: function () {
    function slice(start, end) {
      if (nativeArrayBufferSlice !== undefined && end === undefined) {
        return nativeArrayBufferSlice.call(anObject(this), start); // FF fix
      }

      var length = anObject(this).byteLength;
      var first = toAbsoluteIndex(start, length);
      var fin = toAbsoluteIndex(end === undefined ? length : end, length);
      var result = new (speciesConstructor(this, ArrayBuffer))(toLength(fin - first));
      var viewSource = new DataView(this);
      var viewTarget = new DataView(result);
      var index = 0;

      while (first < fin) {
        viewTarget.setUint8(index++, viewSource.getUint8(first++));
      }

      return result;
    }

    return slice;
  }()
});

/***/ }),

/***/ 24835:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var fails = __webpack_require__(69691);

var isArray = __webpack_require__(89999);

var isObject = __webpack_require__(35611);

var toObject = __webpack_require__(12104);

var toLength = __webpack_require__(39570);

var createProperty = __webpack_require__(39241);

var arraySpeciesCreate = __webpack_require__(77454);

var arrayMethodHasSpeciesSupport = __webpack_require__(93537);

var wellKnownSymbol = __webpack_require__(17657);

var V8_VERSION = __webpack_require__(12338);

var IS_CONCAT_SPREADABLE = wellKnownSymbol('isConcatSpreadable');
var MAX_SAFE_INTEGER = 0x1FFFFFFFFFFFFF;
var MAXIMUM_ALLOWED_INDEX_EXCEEDED = 'Maximum allowed index exceeded'; // We can't use this feature detection in V8 since it causes
// deoptimization and serious performance degradation
// https://github.com/zloirock/core-js/issues/679

var IS_CONCAT_SPREADABLE_SUPPORT = V8_VERSION >= 51 || !fails(function () {
  var array = [];
  array[IS_CONCAT_SPREADABLE] = false;
  return array.concat()[0] !== array;
});
var SPECIES_SUPPORT = arrayMethodHasSpeciesSupport('concat');

var isConcatSpreadable = function isConcatSpreadable(O) {
  if (!isObject(O)) return false;
  var spreadable = O[IS_CONCAT_SPREADABLE];
  return spreadable !== undefined ? !!spreadable : isArray(O);
};

var FORCED = !IS_CONCAT_SPREADABLE_SUPPORT || !SPECIES_SUPPORT; // `Array.prototype.concat` method
// https://tc39.es/ecma262/#sec-array.prototype.concat
// with adding support of @@isConcatSpreadable and @@species

$({
  target: 'Array',
  proto: true,
  forced: FORCED
}, {
  // eslint-disable-next-line no-unused-vars -- required for `.length`
  concat: function () {
    function concat(arg) {
      var O = toObject(this);
      var A = arraySpeciesCreate(O, 0);
      var n = 0;
      var i, k, length, len, E;

      for (i = -1, length = arguments.length; i < length; i++) {
        E = i === -1 ? O : arguments[i];

        if (isConcatSpreadable(E)) {
          len = toLength(E.length);
          if (n + len > MAX_SAFE_INTEGER) throw TypeError(MAXIMUM_ALLOWED_INDEX_EXCEEDED);

          for (k = 0; k < len; k++, n++) {
            if (k in E) createProperty(A, n, E[k]);
          }
        } else {
          if (n >= MAX_SAFE_INTEGER) throw TypeError(MAXIMUM_ALLOWED_INDEX_EXCEEDED);
          createProperty(A, n++, E);
        }
      }

      A.length = n;
      return A;
    }

    return concat;
  }()
});

/***/ }),

/***/ 14653:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var copyWithin = __webpack_require__(59571);

var addToUnscopables = __webpack_require__(36910); // `Array.prototype.copyWithin` method
// https://tc39.es/ecma262/#sec-array.prototype.copywithin


$({
  target: 'Array',
  proto: true
}, {
  copyWithin: copyWithin
}); // https://tc39.es/ecma262/#sec-array.prototype-@@unscopables

addToUnscopables('copyWithin');

/***/ }),

/***/ 58139:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var $every = __webpack_require__(69033).every;

var arrayMethodIsStrict = __webpack_require__(37902);

var STRICT_METHOD = arrayMethodIsStrict('every'); // `Array.prototype.every` method
// https://tc39.es/ecma262/#sec-array.prototype.every

$({
  target: 'Array',
  proto: true,
  forced: !STRICT_METHOD
}, {
  every: function () {
    function every(callbackfn
    /* , thisArg */
    ) {
      return $every(this, callbackfn, arguments.length > 1 ? arguments[1] : undefined);
    }

    return every;
  }()
});

/***/ }),

/***/ 34049:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var fill = __webpack_require__(22051);

var addToUnscopables = __webpack_require__(36910); // `Array.prototype.fill` method
// https://tc39.es/ecma262/#sec-array.prototype.fill


$({
  target: 'Array',
  proto: true
}, {
  fill: fill
}); // https://tc39.es/ecma262/#sec-array.prototype-@@unscopables

addToUnscopables('fill');

/***/ }),

/***/ 38356:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var $filter = __webpack_require__(69033).filter;

var arrayMethodHasSpeciesSupport = __webpack_require__(93537);

var HAS_SPECIES_SUPPORT = arrayMethodHasSpeciesSupport('filter'); // `Array.prototype.filter` method
// https://tc39.es/ecma262/#sec-array.prototype.filter
// with adding support of @@species

$({
  target: 'Array',
  proto: true,
  forced: !HAS_SPECIES_SUPPORT
}, {
  filter: function () {
    function filter(callbackfn
    /* , thisArg */
    ) {
      return $filter(this, callbackfn, arguments.length > 1 ? arguments[1] : undefined);
    }

    return filter;
  }()
});

/***/ }),

/***/ 31699:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var $findIndex = __webpack_require__(69033).findIndex;

var addToUnscopables = __webpack_require__(36910);

var FIND_INDEX = 'findIndex';
var SKIPS_HOLES = true; // Shouldn't skip holes

if (FIND_INDEX in []) Array(1)[FIND_INDEX](function () {
  SKIPS_HOLES = false;
}); // `Array.prototype.findIndex` method
// https://tc39.es/ecma262/#sec-array.prototype.findindex

$({
  target: 'Array',
  proto: true,
  forced: SKIPS_HOLES
}, {
  findIndex: function () {
    function findIndex(callbackfn
    /* , that = undefined */
    ) {
      return $findIndex(this, callbackfn, arguments.length > 1 ? arguments[1] : undefined);
    }

    return findIndex;
  }()
}); // https://tc39.es/ecma262/#sec-array.prototype-@@unscopables

addToUnscopables(FIND_INDEX);

/***/ }),

/***/ 72494:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var $find = __webpack_require__(69033).find;

var addToUnscopables = __webpack_require__(36910);

var FIND = 'find';
var SKIPS_HOLES = true; // Shouldn't skip holes

if (FIND in []) Array(1)[FIND](function () {
  SKIPS_HOLES = false;
}); // `Array.prototype.find` method
// https://tc39.es/ecma262/#sec-array.prototype.find

$({
  target: 'Array',
  proto: true,
  forced: SKIPS_HOLES
}, {
  find: function () {
    function find(callbackfn
    /* , that = undefined */
    ) {
      return $find(this, callbackfn, arguments.length > 1 ? arguments[1] : undefined);
    }

    return find;
  }()
}); // https://tc39.es/ecma262/#sec-array.prototype-@@unscopables

addToUnscopables(FIND);

/***/ }),

/***/ 81520:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var flattenIntoArray = __webpack_require__(904);

var toObject = __webpack_require__(12104);

var toLength = __webpack_require__(39570);

var aFunction = __webpack_require__(83923);

var arraySpeciesCreate = __webpack_require__(77454); // `Array.prototype.flatMap` method
// https://tc39.es/ecma262/#sec-array.prototype.flatmap


$({
  target: 'Array',
  proto: true
}, {
  flatMap: function () {
    function flatMap(callbackfn
    /* , thisArg */
    ) {
      var O = toObject(this);
      var sourceLen = toLength(O.length);
      var A;
      aFunction(callbackfn);
      A = arraySpeciesCreate(O, 0);
      A.length = flattenIntoArray(A, O, O, sourceLen, 0, 1, callbackfn, arguments.length > 1 ? arguments[1] : undefined);
      return A;
    }

    return flatMap;
  }()
});

/***/ }),

/***/ 3263:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var flattenIntoArray = __webpack_require__(904);

var toObject = __webpack_require__(12104);

var toLength = __webpack_require__(39570);

var toInteger = __webpack_require__(3997);

var arraySpeciesCreate = __webpack_require__(77454); // `Array.prototype.flat` method
// https://tc39.es/ecma262/#sec-array.prototype.flat


$({
  target: 'Array',
  proto: true
}, {
  flat: function () {
    function flat() {
      var depthArg = arguments.length ? arguments[0] : undefined;
      var O = toObject(this);
      var sourceLen = toLength(O.length);
      var A = arraySpeciesCreate(O, 0);
      A.length = flattenIntoArray(A, O, O, sourceLen, 0, depthArg === undefined ? 1 : toInteger(depthArg));
      return A;
    }

    return flat;
  }()
});

/***/ }),

/***/ 80036:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var forEach = __webpack_require__(90369); // `Array.prototype.forEach` method
// https://tc39.es/ecma262/#sec-array.prototype.foreach
// eslint-disable-next-line es/no-array-prototype-foreach -- safe


$({
  target: 'Array',
  proto: true,
  forced: [].forEach != forEach
}, {
  forEach: forEach
});

/***/ }),

/***/ 60515:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var from = __webpack_require__(28213);

var checkCorrectnessOfIteration = __webpack_require__(68762);

var INCORRECT_ITERATION = !checkCorrectnessOfIteration(function (iterable) {
  // eslint-disable-next-line es/no-array-from -- required for testing
  Array.from(iterable);
}); // `Array.from` method
// https://tc39.es/ecma262/#sec-array.from

$({
  target: 'Array',
  stat: true,
  forced: INCORRECT_ITERATION
}, {
  from: from
});

/***/ }),

/***/ 94330:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var $includes = __webpack_require__(25945).includes;

var addToUnscopables = __webpack_require__(36910); // `Array.prototype.includes` method
// https://tc39.es/ecma262/#sec-array.prototype.includes


$({
  target: 'Array',
  proto: true
}, {
  includes: function () {
    function includes(el
    /* , fromIndex = 0 */
    ) {
      return $includes(this, el, arguments.length > 1 ? arguments[1] : undefined);
    }

    return includes;
  }()
}); // https://tc39.es/ecma262/#sec-array.prototype-@@unscopables

addToUnscopables('includes');

/***/ }),

/***/ 96553:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";

/* eslint-disable es/no-array-prototype-indexof -- required for testing */

var $ = __webpack_require__(56174);

var $indexOf = __webpack_require__(25945).indexOf;

var arrayMethodIsStrict = __webpack_require__(37902);

var nativeIndexOf = [].indexOf;
var NEGATIVE_ZERO = !!nativeIndexOf && 1 / [1].indexOf(1, -0) < 0;
var STRICT_METHOD = arrayMethodIsStrict('indexOf'); // `Array.prototype.indexOf` method
// https://tc39.es/ecma262/#sec-array.prototype.indexof

$({
  target: 'Array',
  proto: true,
  forced: NEGATIVE_ZERO || !STRICT_METHOD
}, {
  indexOf: function () {
    function indexOf(searchElement
    /* , fromIndex = 0 */
    ) {
      return NEGATIVE_ZERO // convert -0 to +0
      ? nativeIndexOf.apply(this, arguments) || 0 : $indexOf(this, searchElement, arguments.length > 1 ? arguments[1] : undefined);
    }

    return indexOf;
  }()
});

/***/ }),

/***/ 99139:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var isArray = __webpack_require__(89999); // `Array.isArray` method
// https://tc39.es/ecma262/#sec-array.isarray


$({
  target: 'Array',
  stat: true
}, {
  isArray: isArray
});

/***/ }),

/***/ 98215:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var toIndexedObject = __webpack_require__(26553);

var addToUnscopables = __webpack_require__(36910);

var Iterators = __webpack_require__(82723);

var InternalStateModule = __webpack_require__(79947);

var defineIterator = __webpack_require__(71179);

var ARRAY_ITERATOR = 'Array Iterator';
var setInternalState = InternalStateModule.set;
var getInternalState = InternalStateModule.getterFor(ARRAY_ITERATOR); // `Array.prototype.entries` method
// https://tc39.es/ecma262/#sec-array.prototype.entries
// `Array.prototype.keys` method
// https://tc39.es/ecma262/#sec-array.prototype.keys
// `Array.prototype.values` method
// https://tc39.es/ecma262/#sec-array.prototype.values
// `Array.prototype[@@iterator]` method
// https://tc39.es/ecma262/#sec-array.prototype-@@iterator
// `CreateArrayIterator` internal method
// https://tc39.es/ecma262/#sec-createarrayiterator

module.exports = defineIterator(Array, 'Array', function (iterated, kind) {
  setInternalState(this, {
    type: ARRAY_ITERATOR,
    target: toIndexedObject(iterated),
    // target
    index: 0,
    // next index
    kind: kind // kind

  }); // `%ArrayIteratorPrototype%.next` method
  // https://tc39.es/ecma262/#sec-%arrayiteratorprototype%.next
}, function () {
  var state = getInternalState(this);
  var target = state.target;
  var kind = state.kind;
  var index = state.index++;

  if (!target || index >= target.length) {
    state.target = undefined;
    return {
      value: undefined,
      done: true
    };
  }

  if (kind == 'keys') return {
    value: index,
    done: false
  };
  if (kind == 'values') return {
    value: target[index],
    done: false
  };
  return {
    value: [index, target[index]],
    done: false
  };
}, 'values'); // argumentsList[@@iterator] is %ArrayProto_values%
// https://tc39.es/ecma262/#sec-createunmappedargumentsobject
// https://tc39.es/ecma262/#sec-createmappedargumentsobject

Iterators.Arguments = Iterators.Array; // https://tc39.es/ecma262/#sec-array.prototype-@@unscopables

addToUnscopables('keys');
addToUnscopables('values');
addToUnscopables('entries');

/***/ }),

/***/ 32617:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var IndexedObject = __webpack_require__(50990);

var toIndexedObject = __webpack_require__(26553);

var arrayMethodIsStrict = __webpack_require__(37902);

var nativeJoin = [].join;
var ES3_STRINGS = IndexedObject != Object;
var STRICT_METHOD = arrayMethodIsStrict('join', ','); // `Array.prototype.join` method
// https://tc39.es/ecma262/#sec-array.prototype.join

$({
  target: 'Array',
  proto: true,
  forced: ES3_STRINGS || !STRICT_METHOD
}, {
  join: function () {
    function join(separator) {
      return nativeJoin.call(toIndexedObject(this), separator === undefined ? ',' : separator);
    }

    return join;
  }()
});

/***/ }),

/***/ 80186:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var lastIndexOf = __webpack_require__(41183); // `Array.prototype.lastIndexOf` method
// https://tc39.es/ecma262/#sec-array.prototype.lastindexof
// eslint-disable-next-line es/no-array-prototype-lastindexof -- required for testing


$({
  target: 'Array',
  proto: true,
  forced: lastIndexOf !== [].lastIndexOf
}, {
  lastIndexOf: lastIndexOf
});

/***/ }),

/***/ 31149:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var $map = __webpack_require__(69033).map;

var arrayMethodHasSpeciesSupport = __webpack_require__(93537);

var HAS_SPECIES_SUPPORT = arrayMethodHasSpeciesSupport('map'); // `Array.prototype.map` method
// https://tc39.es/ecma262/#sec-array.prototype.map
// with adding support of @@species

$({
  target: 'Array',
  proto: true,
  forced: !HAS_SPECIES_SUPPORT
}, {
  map: function () {
    function map(callbackfn
    /* , thisArg */
    ) {
      return $map(this, callbackfn, arguments.length > 1 ? arguments[1] : undefined);
    }

    return map;
  }()
});

/***/ }),

/***/ 71827:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var fails = __webpack_require__(69691);

var createProperty = __webpack_require__(39241);

var ISNT_GENERIC = fails(function () {
  function F() {
    /* empty */
  } // eslint-disable-next-line es/no-array-of -- required for testing


  return !(Array.of.call(F) instanceof F);
}); // `Array.of` method
// https://tc39.es/ecma262/#sec-array.of
// WebKit Array.of isn't generic

$({
  target: 'Array',
  stat: true,
  forced: ISNT_GENERIC
}, {
  of: function () {
    function of() {
      var index = 0;
      var argumentsLength = arguments.length;
      var result = new (typeof this == 'function' ? this : Array)(argumentsLength);

      while (argumentsLength > index) {
        createProperty(result, index, arguments[index++]);
      }

      result.length = argumentsLength;
      return result;
    }

    return of;
  }()
});

/***/ }),

/***/ 32099:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var $reduceRight = __webpack_require__(28249).right;

var arrayMethodIsStrict = __webpack_require__(37902);

var CHROME_VERSION = __webpack_require__(12338);

var IS_NODE = __webpack_require__(41488);

var STRICT_METHOD = arrayMethodIsStrict('reduceRight'); // Chrome 80-82 has a critical bug
// https://bugs.chromium.org/p/chromium/issues/detail?id=1049982

var CHROME_BUG = !IS_NODE && CHROME_VERSION > 79 && CHROME_VERSION < 83; // `Array.prototype.reduceRight` method
// https://tc39.es/ecma262/#sec-array.prototype.reduceright

$({
  target: 'Array',
  proto: true,
  forced: !STRICT_METHOD || CHROME_BUG
}, {
  reduceRight: function () {
    function reduceRight(callbackfn
    /* , initialValue */
    ) {
      return $reduceRight(this, callbackfn, arguments.length, arguments.length > 1 ? arguments[1] : undefined);
    }

    return reduceRight;
  }()
});

/***/ }),

/***/ 79770:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var $reduce = __webpack_require__(28249).left;

var arrayMethodIsStrict = __webpack_require__(37902);

var CHROME_VERSION = __webpack_require__(12338);

var IS_NODE = __webpack_require__(41488);

var STRICT_METHOD = arrayMethodIsStrict('reduce'); // Chrome 80-82 has a critical bug
// https://bugs.chromium.org/p/chromium/issues/detail?id=1049982

var CHROME_BUG = !IS_NODE && CHROME_VERSION > 79 && CHROME_VERSION < 83; // `Array.prototype.reduce` method
// https://tc39.es/ecma262/#sec-array.prototype.reduce

$({
  target: 'Array',
  proto: true,
  forced: !STRICT_METHOD || CHROME_BUG
}, {
  reduce: function () {
    function reduce(callbackfn
    /* , initialValue */
    ) {
      return $reduce(this, callbackfn, arguments.length, arguments.length > 1 ? arguments[1] : undefined);
    }

    return reduce;
  }()
});

/***/ }),

/***/ 72667:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var isArray = __webpack_require__(89999);

var nativeReverse = [].reverse;
var test = [1, 2]; // `Array.prototype.reverse` method
// https://tc39.es/ecma262/#sec-array.prototype.reverse
// fix for Safari 12.0 bug
// https://bugs.webkit.org/show_bug.cgi?id=188794

$({
  target: 'Array',
  proto: true,
  forced: String(test) === String(test.reverse())
}, {
  reverse: function () {
    function reverse() {
      // eslint-disable-next-line no-self-assign -- dirty hack
      if (isArray(this)) this.length = this.length;
      return nativeReverse.call(this);
    }

    return reverse;
  }()
});

/***/ }),

/***/ 94920:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var isObject = __webpack_require__(35611);

var isArray = __webpack_require__(89999);

var toAbsoluteIndex = __webpack_require__(55205);

var toLength = __webpack_require__(39570);

var toIndexedObject = __webpack_require__(26553);

var createProperty = __webpack_require__(39241);

var wellKnownSymbol = __webpack_require__(17657);

var arrayMethodHasSpeciesSupport = __webpack_require__(93537);

var HAS_SPECIES_SUPPORT = arrayMethodHasSpeciesSupport('slice');
var SPECIES = wellKnownSymbol('species');
var nativeSlice = [].slice;
var max = Math.max; // `Array.prototype.slice` method
// https://tc39.es/ecma262/#sec-array.prototype.slice
// fallback for not array-like ES3 strings and DOM objects

$({
  target: 'Array',
  proto: true,
  forced: !HAS_SPECIES_SUPPORT
}, {
  slice: function () {
    function slice(start, end) {
      var O = toIndexedObject(this);
      var length = toLength(O.length);
      var k = toAbsoluteIndex(start, length);
      var fin = toAbsoluteIndex(end === undefined ? length : end, length); // inline `ArraySpeciesCreate` for usage native `Array#slice` where it's possible

      var Constructor, result, n;

      if (isArray(O)) {
        Constructor = O.constructor; // cross-realm fallback

        if (typeof Constructor == 'function' && (Constructor === Array || isArray(Constructor.prototype))) {
          Constructor = undefined;
        } else if (isObject(Constructor)) {
          Constructor = Constructor[SPECIES];
          if (Constructor === null) Constructor = undefined;
        }

        if (Constructor === Array || Constructor === undefined) {
          return nativeSlice.call(O, k, fin);
        }
      }

      result = new (Constructor === undefined ? Array : Constructor)(max(fin - k, 0));

      for (n = 0; k < fin; k++, n++) {
        if (k in O) createProperty(result, n, O[k]);
      }

      result.length = n;
      return result;
    }

    return slice;
  }()
});

/***/ }),

/***/ 64683:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var $some = __webpack_require__(69033).some;

var arrayMethodIsStrict = __webpack_require__(37902);

var STRICT_METHOD = arrayMethodIsStrict('some'); // `Array.prototype.some` method
// https://tc39.es/ecma262/#sec-array.prototype.some

$({
  target: 'Array',
  proto: true,
  forced: !STRICT_METHOD
}, {
  some: function () {
    function some(callbackfn
    /* , thisArg */
    ) {
      return $some(this, callbackfn, arguments.length > 1 ? arguments[1] : undefined);
    }

    return some;
  }()
});

/***/ }),

/***/ 26189:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var aFunction = __webpack_require__(83923);

var toObject = __webpack_require__(12104);

var toLength = __webpack_require__(39570);

var toString = __webpack_require__(92082);

var fails = __webpack_require__(69691);

var internalSort = __webpack_require__(58737);

var arrayMethodIsStrict = __webpack_require__(37902);

var FF = __webpack_require__(20967);

var IE_OR_EDGE = __webpack_require__(85749);

var V8 = __webpack_require__(12338);

var WEBKIT = __webpack_require__(12200);

var test = [];
var nativeSort = test.sort; // IE8-

var FAILS_ON_UNDEFINED = fails(function () {
  test.sort(undefined);
}); // V8 bug

var FAILS_ON_NULL = fails(function () {
  test.sort(null);
}); // Old WebKit

var STRICT_METHOD = arrayMethodIsStrict('sort');
var STABLE_SORT = !fails(function () {
  // feature detection can be too slow, so check engines versions
  if (V8) return V8 < 70;
  if (FF && FF > 3) return;
  if (IE_OR_EDGE) return true;
  if (WEBKIT) return WEBKIT < 603;
  var result = '';
  var code, chr, value, index; // generate an array with more 512 elements (Chakra and old V8 fails only in this case)

  for (code = 65; code < 76; code++) {
    chr = String.fromCharCode(code);

    switch (code) {
      case 66:
      case 69:
      case 70:
      case 72:
        value = 3;
        break;

      case 68:
      case 71:
        value = 4;
        break;

      default:
        value = 2;
    }

    for (index = 0; index < 47; index++) {
      test.push({
        k: chr + index,
        v: value
      });
    }
  }

  test.sort(function (a, b) {
    return b.v - a.v;
  });

  for (index = 0; index < test.length; index++) {
    chr = test[index].k.charAt(0);
    if (result.charAt(result.length - 1) !== chr) result += chr;
  }

  return result !== 'DGBEFHACIJK';
});
var FORCED = FAILS_ON_UNDEFINED || !FAILS_ON_NULL || !STRICT_METHOD || !STABLE_SORT;

var getSortCompare = function getSortCompare(comparefn) {
  return function (x, y) {
    if (y === undefined) return -1;
    if (x === undefined) return 1;
    if (comparefn !== undefined) return +comparefn(x, y) || 0;
    return toString(x) > toString(y) ? 1 : -1;
  };
}; // `Array.prototype.sort` method
// https://tc39.es/ecma262/#sec-array.prototype.sort


$({
  target: 'Array',
  proto: true,
  forced: FORCED
}, {
  sort: function () {
    function sort(comparefn) {
      if (comparefn !== undefined) aFunction(comparefn);
      var array = toObject(this);
      if (STABLE_SORT) return comparefn === undefined ? nativeSort.call(array) : nativeSort.call(array, comparefn);
      var items = [];
      var arrayLength = toLength(array.length);
      var itemsLength, index;

      for (index = 0; index < arrayLength; index++) {
        if (index in array) items.push(array[index]);
      }

      items = internalSort(items, getSortCompare(comparefn));
      itemsLength = items.length;
      index = 0;

      while (index < itemsLength) {
        array[index] = items[index++];
      }

      while (index < arrayLength) {
        delete array[index++];
      }

      return array;
    }

    return sort;
  }()
});

/***/ }),

/***/ 41185:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var setSpecies = __webpack_require__(64157); // `Array[@@species]` getter
// https://tc39.es/ecma262/#sec-get-array-@@species


setSpecies('Array');

/***/ }),

/***/ 51008:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var toAbsoluteIndex = __webpack_require__(55205);

var toInteger = __webpack_require__(3997);

var toLength = __webpack_require__(39570);

var toObject = __webpack_require__(12104);

var arraySpeciesCreate = __webpack_require__(77454);

var createProperty = __webpack_require__(39241);

var arrayMethodHasSpeciesSupport = __webpack_require__(93537);

var HAS_SPECIES_SUPPORT = arrayMethodHasSpeciesSupport('splice');
var max = Math.max;
var min = Math.min;
var MAX_SAFE_INTEGER = 0x1FFFFFFFFFFFFF;
var MAXIMUM_ALLOWED_LENGTH_EXCEEDED = 'Maximum allowed length exceeded'; // `Array.prototype.splice` method
// https://tc39.es/ecma262/#sec-array.prototype.splice
// with adding support of @@species

$({
  target: 'Array',
  proto: true,
  forced: !HAS_SPECIES_SUPPORT
}, {
  splice: function () {
    function splice(start, deleteCount
    /* , ...items */
    ) {
      var O = toObject(this);
      var len = toLength(O.length);
      var actualStart = toAbsoluteIndex(start, len);
      var argumentsLength = arguments.length;
      var insertCount, actualDeleteCount, A, k, from, to;

      if (argumentsLength === 0) {
        insertCount = actualDeleteCount = 0;
      } else if (argumentsLength === 1) {
        insertCount = 0;
        actualDeleteCount = len - actualStart;
      } else {
        insertCount = argumentsLength - 2;
        actualDeleteCount = min(max(toInteger(deleteCount), 0), len - actualStart);
      }

      if (len + insertCount - actualDeleteCount > MAX_SAFE_INTEGER) {
        throw TypeError(MAXIMUM_ALLOWED_LENGTH_EXCEEDED);
      }

      A = arraySpeciesCreate(O, actualDeleteCount);

      for (k = 0; k < actualDeleteCount; k++) {
        from = actualStart + k;
        if (from in O) createProperty(A, k, O[from]);
      }

      A.length = actualDeleteCount;

      if (insertCount < actualDeleteCount) {
        for (k = actualStart; k < len - actualDeleteCount; k++) {
          from = k + actualDeleteCount;
          to = k + insertCount;
          if (from in O) O[to] = O[from];else delete O[to];
        }

        for (k = len; k > len - actualDeleteCount + insertCount; k--) {
          delete O[k - 1];
        }
      } else if (insertCount > actualDeleteCount) {
        for (k = len - actualDeleteCount; k > actualStart; k--) {
          from = k + actualDeleteCount - 1;
          to = k + insertCount - 1;
          if (from in O) O[to] = O[from];else delete O[to];
        }
      }

      for (k = 0; k < insertCount; k++) {
        O[k + actualStart] = arguments[k + 2];
      }

      O.length = len - actualDeleteCount + insertCount;
      return A;
    }

    return splice;
  }()
});

/***/ }),

/***/ 86000:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


// this method was added to unscopables after implementation
// in popular engines, so it's moved to a separate module
var addToUnscopables = __webpack_require__(36910); // https://tc39.es/ecma262/#sec-array.prototype-@@unscopables


addToUnscopables('flatMap');

/***/ }),

/***/ 47112:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


// this method was added to unscopables after implementation
// in popular engines, so it's moved to a separate module
var addToUnscopables = __webpack_require__(36910); // https://tc39.es/ecma262/#sec-array.prototype-@@unscopables


addToUnscopables('flat');

/***/ }),

/***/ 87143:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var ArrayBufferModule = __webpack_require__(80246);

var NATIVE_ARRAY_BUFFER = __webpack_require__(32977); // `DataView` constructor
// https://tc39.es/ecma262/#sec-dataview-constructor


$({
  global: true,
  forced: !NATIVE_ARRAY_BUFFER
}, {
  DataView: ArrayBufferModule.DataView
});

/***/ }),

/***/ 72499:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174); // `Date.now` method
// https://tc39.es/ecma262/#sec-date.now


$({
  target: 'Date',
  stat: true
}, {
  now: function () {
    function now() {
      return new Date().getTime();
    }

    return now;
  }()
});

/***/ }),

/***/ 73676:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var toISOString = __webpack_require__(79873); // `Date.prototype.toISOString` method
// https://tc39.es/ecma262/#sec-date.prototype.toisostring
// PhantomJS / old WebKit has a broken implementations


$({
  target: 'Date',
  proto: true,
  forced: Date.prototype.toISOString !== toISOString
}, {
  toISOString: toISOString
});

/***/ }),

/***/ 76746:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var fails = __webpack_require__(69691);

var toObject = __webpack_require__(12104);

var toPrimitive = __webpack_require__(94673);

var FORCED = fails(function () {
  return new Date(NaN).toJSON() !== null || Date.prototype.toJSON.call({
    toISOString: function () {
      function toISOString() {
        return 1;
      }

      return toISOString;
    }()
  }) !== 1;
}); // `Date.prototype.toJSON` method
// https://tc39.es/ecma262/#sec-date.prototype.tojson

$({
  target: 'Date',
  proto: true,
  forced: FORCED
}, {
  // eslint-disable-next-line no-unused-vars -- required for `.length`
  toJSON: function () {
    function toJSON(key) {
      var O = toObject(this);
      var pv = toPrimitive(O, 'number');
      return typeof pv == 'number' && !isFinite(pv) ? null : O.toISOString();
    }

    return toJSON;
  }()
});

/***/ }),

/***/ 19:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var createNonEnumerableProperty = __webpack_require__(52188);

var dateToPrimitive = __webpack_require__(57763);

var wellKnownSymbol = __webpack_require__(17657);

var TO_PRIMITIVE = wellKnownSymbol('toPrimitive');
var DatePrototype = Date.prototype; // `Date.prototype[@@toPrimitive]` method
// https://tc39.es/ecma262/#sec-date.prototype-@@toprimitive

if (!(TO_PRIMITIVE in DatePrototype)) {
  createNonEnumerableProperty(DatePrototype, TO_PRIMITIVE, dateToPrimitive);
}

/***/ }),

/***/ 17038:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var redefine = __webpack_require__(11807);

var DatePrototype = Date.prototype;
var INVALID_DATE = 'Invalid Date';
var TO_STRING = 'toString';
var nativeDateToString = DatePrototype[TO_STRING];
var getTime = DatePrototype.getTime; // `Date.prototype.toString` method
// https://tc39.es/ecma262/#sec-date.prototype.tostring

if (String(new Date(NaN)) != INVALID_DATE) {
  redefine(DatePrototype, TO_STRING, function () {
    function toString() {
      var value = getTime.call(this); // eslint-disable-next-line no-self-compare -- NaN check

      return value === value ? nativeDateToString.call(this) : INVALID_DATE;
    }

    return toString;
  }());
}

/***/ }),

/***/ 46360:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var bind = __webpack_require__(68719); // `Function.prototype.bind` method
// https://tc39.es/ecma262/#sec-function.prototype.bind


$({
  target: 'Function',
  proto: true
}, {
  bind: bind
});

/***/ }),

/***/ 16835:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var isObject = __webpack_require__(35611);

var definePropertyModule = __webpack_require__(74217);

var getPrototypeOf = __webpack_require__(6863);

var wellKnownSymbol = __webpack_require__(17657);

var HAS_INSTANCE = wellKnownSymbol('hasInstance');
var FunctionPrototype = Function.prototype; // `Function.prototype[@@hasInstance]` method
// https://tc39.es/ecma262/#sec-function.prototype-@@hasinstance

if (!(HAS_INSTANCE in FunctionPrototype)) {
  definePropertyModule.f(FunctionPrototype, HAS_INSTANCE, {
    value: function () {
      function value(O) {
        if (typeof this != 'function' || !isObject(O)) return false;
        if (!isObject(this.prototype)) return O instanceof this; // for environment w/o native `@@hasInstance` logic enough `instanceof`, but add this:

        while (O = getPrototypeOf(O)) {
          if (this.prototype === O) return true;
        }

        return false;
      }

      return value;
    }()
  });
}

/***/ }),

/***/ 96683:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var DESCRIPTORS = __webpack_require__(75592);

var defineProperty = __webpack_require__(74217).f;

var FunctionPrototype = Function.prototype;
var FunctionPrototypeToString = FunctionPrototype.toString;
var nameRE = /^\s*function ([^ (]*)/;
var NAME = 'name'; // Function instances `.name` property
// https://tc39.es/ecma262/#sec-function-instances-name

if (DESCRIPTORS && !(NAME in FunctionPrototype)) {
  defineProperty(FunctionPrototype, NAME, {
    configurable: true,
    get: function () {
      function get() {
        try {
          return FunctionPrototypeToString.call(this).match(nameRE)[1];
        } catch (error) {
          return '';
        }
      }

      return get;
    }()
  });
}

/***/ }),

/***/ 7756:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var global = __webpack_require__(84253);

var setToStringTag = __webpack_require__(29108); // JSON[@@toStringTag] property
// https://tc39.es/ecma262/#sec-json-@@tostringtag


setToStringTag(global.JSON, 'JSON', true);

/***/ }),

/***/ 21842:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var collection = __webpack_require__(42006);

var collectionStrong = __webpack_require__(74568); // `Map` constructor
// https://tc39.es/ecma262/#sec-map-objects


module.exports = collection('Map', function (init) {
  return function () {
    function Map() {
      return init(this, arguments.length ? arguments[0] : undefined);
    }

    return Map;
  }();
}, collectionStrong);

/***/ }),

/***/ 1613:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var log1p = __webpack_require__(83659); // eslint-disable-next-line es/no-math-acosh -- required for testing


var $acosh = Math.acosh;
var log = Math.log;
var sqrt = Math.sqrt;
var LN2 = Math.LN2;
var FORCED = !$acosh // V8 bug: https://code.google.com/p/v8/issues/detail?id=3509
|| Math.floor($acosh(Number.MAX_VALUE)) != 710 // Tor Browser bug: Math.acosh(Infinity) -> NaN
|| $acosh(Infinity) != Infinity; // `Math.acosh` method
// https://tc39.es/ecma262/#sec-math.acosh

$({
  target: 'Math',
  stat: true,
  forced: FORCED
}, {
  acosh: function () {
    function acosh(x) {
      return (x = +x) < 1 ? NaN : x > 94906265.62425156 ? log(x) + LN2 : log1p(x - 1 + sqrt(x - 1) * sqrt(x + 1));
    }

    return acosh;
  }()
});

/***/ }),

/***/ 65115:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174); // eslint-disable-next-line es/no-math-asinh -- required for testing


var $asinh = Math.asinh;
var log = Math.log;
var sqrt = Math.sqrt;

function asinh(x) {
  return !isFinite(x = +x) || x == 0 ? x : x < 0 ? -asinh(-x) : log(x + sqrt(x * x + 1));
} // `Math.asinh` method
// https://tc39.es/ecma262/#sec-math.asinh
// Tor Browser bug: Math.asinh(0) -> -0


$({
  target: 'Math',
  stat: true,
  forced: !($asinh && 1 / $asinh(0) > 0)
}, {
  asinh: asinh
});

/***/ }),

/***/ 27115:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174); // eslint-disable-next-line es/no-math-atanh -- required for testing


var $atanh = Math.atanh;
var log = Math.log; // `Math.atanh` method
// https://tc39.es/ecma262/#sec-math.atanh
// Tor Browser bug: Math.atanh(-0) -> 0

$({
  target: 'Math',
  stat: true,
  forced: !($atanh && 1 / $atanh(-0) < 0)
}, {
  atanh: function () {
    function atanh(x) {
      return (x = +x) == 0 ? x : log((1 + x) / (1 - x)) / 2;
    }

    return atanh;
  }()
});

/***/ }),

/***/ 13280:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var sign = __webpack_require__(24305);

var abs = Math.abs;
var pow = Math.pow; // `Math.cbrt` method
// https://tc39.es/ecma262/#sec-math.cbrt

$({
  target: 'Math',
  stat: true
}, {
  cbrt: function () {
    function cbrt(x) {
      return sign(x = +x) * pow(abs(x), 1 / 3);
    }

    return cbrt;
  }()
});

/***/ }),

/***/ 21019:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var floor = Math.floor;
var log = Math.log;
var LOG2E = Math.LOG2E; // `Math.clz32` method
// https://tc39.es/ecma262/#sec-math.clz32

$({
  target: 'Math',
  stat: true
}, {
  clz32: function () {
    function clz32(x) {
      return (x >>>= 0) ? 31 - floor(log(x + 0.5) * LOG2E) : 32;
    }

    return clz32;
  }()
});

/***/ }),

/***/ 66619:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var expm1 = __webpack_require__(19997); // eslint-disable-next-line es/no-math-cosh -- required for testing


var $cosh = Math.cosh;
var abs = Math.abs;
var E = Math.E; // `Math.cosh` method
// https://tc39.es/ecma262/#sec-math.cosh

$({
  target: 'Math',
  stat: true,
  forced: !$cosh || $cosh(710) === Infinity
}, {
  cosh: function () {
    function cosh(x) {
      var t = expm1(abs(x) - 1) + 1;
      return (t + 1 / (t * E * E)) * (E / 2);
    }

    return cosh;
  }()
});

/***/ }),

/***/ 75469:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var expm1 = __webpack_require__(19997); // `Math.expm1` method
// https://tc39.es/ecma262/#sec-math.expm1
// eslint-disable-next-line es/no-math-expm1 -- required for testing


$({
  target: 'Math',
  stat: true,
  forced: expm1 != Math.expm1
}, {
  expm1: expm1
});

/***/ }),

/***/ 90967:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var fround = __webpack_require__(43334); // `Math.fround` method
// https://tc39.es/ecma262/#sec-math.fround


$({
  target: 'Math',
  stat: true
}, {
  fround: fround
});

/***/ }),

/***/ 35022:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174); // eslint-disable-next-line es/no-math-hypot -- required for testing


var $hypot = Math.hypot;
var abs = Math.abs;
var sqrt = Math.sqrt; // Chrome 77 bug
// https://bugs.chromium.org/p/v8/issues/detail?id=9546

var BUGGY = !!$hypot && $hypot(Infinity, NaN) !== Infinity; // `Math.hypot` method
// https://tc39.es/ecma262/#sec-math.hypot

$({
  target: 'Math',
  stat: true,
  forced: BUGGY
}, {
  // eslint-disable-next-line no-unused-vars -- required for `.length`
  hypot: function () {
    function hypot(value1, value2) {
      var sum = 0;
      var i = 0;
      var aLen = arguments.length;
      var larg = 0;
      var arg, div;

      while (i < aLen) {
        arg = abs(arguments[i++]);

        if (larg < arg) {
          div = larg / arg;
          sum = sum * div * div + 1;
          larg = arg;
        } else if (arg > 0) {
          div = arg / larg;
          sum += div * div;
        } else sum += arg;
      }

      return larg === Infinity ? Infinity : larg * sqrt(sum);
    }

    return hypot;
  }()
});

/***/ }),

/***/ 22604:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var fails = __webpack_require__(69691); // eslint-disable-next-line es/no-math-imul -- required for testing


var $imul = Math.imul;
var FORCED = fails(function () {
  return $imul(0xFFFFFFFF, 5) != -5 || $imul.length != 2;
}); // `Math.imul` method
// https://tc39.es/ecma262/#sec-math.imul
// some WebKit versions fails with big numbers, some has wrong arity

$({
  target: 'Math',
  stat: true,
  forced: FORCED
}, {
  imul: function () {
    function imul(x, y) {
      var UINT16 = 0xFFFF;
      var xn = +x;
      var yn = +y;
      var xl = UINT16 & xn;
      var yl = UINT16 & yn;
      return 0 | xl * yl + ((UINT16 & xn >>> 16) * yl + xl * (UINT16 & yn >>> 16) << 16 >>> 0);
    }

    return imul;
  }()
});

/***/ }),

/***/ 41843:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var log = Math.log;
var LOG10E = Math.LOG10E; // `Math.log10` method
// https://tc39.es/ecma262/#sec-math.log10

$({
  target: 'Math',
  stat: true
}, {
  log10: function () {
    function log10(x) {
      return log(x) * LOG10E;
    }

    return log10;
  }()
});

/***/ }),

/***/ 86810:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var log1p = __webpack_require__(83659); // `Math.log1p` method
// https://tc39.es/ecma262/#sec-math.log1p


$({
  target: 'Math',
  stat: true
}, {
  log1p: log1p
});

/***/ }),

/***/ 54670:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var log = Math.log;
var LN2 = Math.LN2; // `Math.log2` method
// https://tc39.es/ecma262/#sec-math.log2

$({
  target: 'Math',
  stat: true
}, {
  log2: function () {
    function log2(x) {
      return log(x) / LN2;
    }

    return log2;
  }()
});

/***/ }),

/***/ 16580:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var sign = __webpack_require__(24305); // `Math.sign` method
// https://tc39.es/ecma262/#sec-math.sign


$({
  target: 'Math',
  stat: true
}, {
  sign: sign
});

/***/ }),

/***/ 31304:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var fails = __webpack_require__(69691);

var expm1 = __webpack_require__(19997);

var abs = Math.abs;
var exp = Math.exp;
var E = Math.E;
var FORCED = fails(function () {
  // eslint-disable-next-line es/no-math-sinh -- required for testing
  return Math.sinh(-2e-17) != -2e-17;
}); // `Math.sinh` method
// https://tc39.es/ecma262/#sec-math.sinh
// V8 near Chromium 38 has a problem with very small numbers

$({
  target: 'Math',
  stat: true,
  forced: FORCED
}, {
  sinh: function () {
    function sinh(x) {
      return abs(x = +x) < 1 ? (expm1(x) - expm1(-x)) / 2 : (exp(x - 1) - exp(-x - 1)) * (E / 2);
    }

    return sinh;
  }()
});

/***/ }),

/***/ 13078:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var expm1 = __webpack_require__(19997);

var exp = Math.exp; // `Math.tanh` method
// https://tc39.es/ecma262/#sec-math.tanh

$({
  target: 'Math',
  stat: true
}, {
  tanh: function () {
    function tanh(x) {
      var a = expm1(x = +x);
      var b = expm1(-x);
      return a == Infinity ? 1 : b == Infinity ? -1 : (a - b) / (exp(x) + exp(-x));
    }

    return tanh;
  }()
});

/***/ }),

/***/ 45128:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var setToStringTag = __webpack_require__(29108); // Math[@@toStringTag] property
// https://tc39.es/ecma262/#sec-math-@@tostringtag


setToStringTag(Math, 'Math', true);

/***/ }),

/***/ 31938:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var ceil = Math.ceil;
var floor = Math.floor; // `Math.trunc` method
// https://tc39.es/ecma262/#sec-math.trunc

$({
  target: 'Math',
  stat: true
}, {
  trunc: function () {
    function trunc(it) {
      return (it > 0 ? floor : ceil)(it);
    }

    return trunc;
  }()
});

/***/ }),

/***/ 90958:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var DESCRIPTORS = __webpack_require__(75592);

var global = __webpack_require__(84253);

var isForced = __webpack_require__(41441);

var redefine = __webpack_require__(11807);

var has = __webpack_require__(28919);

var classof = __webpack_require__(77000);

var inheritIfRequired = __webpack_require__(22285);

var isSymbol = __webpack_require__(78253);

var toPrimitive = __webpack_require__(94673);

var fails = __webpack_require__(69691);

var create = __webpack_require__(83681);

var getOwnPropertyNames = __webpack_require__(78870).f;

var getOwnPropertyDescriptor = __webpack_require__(22245).f;

var defineProperty = __webpack_require__(74217).f;

var trim = __webpack_require__(84703).trim;

var NUMBER = 'Number';
var NativeNumber = global[NUMBER];
var NumberPrototype = NativeNumber.prototype; // Opera ~12 has broken Object#toString

var BROKEN_CLASSOF = classof(create(NumberPrototype)) == NUMBER; // `ToNumber` abstract operation
// https://tc39.es/ecma262/#sec-tonumber

var toNumber = function toNumber(argument) {
  if (isSymbol(argument)) throw TypeError('Cannot convert a Symbol value to a number');
  var it = toPrimitive(argument, 'number');
  var first, third, radix, maxCode, digits, length, index, code;

  if (typeof it == 'string' && it.length > 2) {
    it = trim(it);
    first = it.charCodeAt(0);

    if (first === 43 || first === 45) {
      third = it.charCodeAt(2);
      if (third === 88 || third === 120) return NaN; // Number('+0x1') should be NaN, old V8 fix
    } else if (first === 48) {
      switch (it.charCodeAt(1)) {
        case 66:
        case 98:
          radix = 2;
          maxCode = 49;
          break;
        // fast equal of /^0b[01]+$/i

        case 79:
        case 111:
          radix = 8;
          maxCode = 55;
          break;
        // fast equal of /^0o[0-7]+$/i

        default:
          return +it;
      }

      digits = it.slice(2);
      length = digits.length;

      for (index = 0; index < length; index++) {
        code = digits.charCodeAt(index); // parseInt parses a string to a first unavailable symbol
        // but ToNumber should return NaN if a string contains unavailable symbols

        if (code < 48 || code > maxCode) return NaN;
      }

      return parseInt(digits, radix);
    }
  }

  return +it;
}; // `Number` constructor
// https://tc39.es/ecma262/#sec-number-constructor


if (isForced(NUMBER, !NativeNumber(' 0o1') || !NativeNumber('0b1') || NativeNumber('+0x1'))) {
  var NumberWrapper = function () {
    function Number(value) {
      var it = arguments.length < 1 ? 0 : value;
      var dummy = this;
      return dummy instanceof NumberWrapper // check on 1..constructor(foo) case
      && (BROKEN_CLASSOF ? fails(function () {
        NumberPrototype.valueOf.call(dummy);
      }) : classof(dummy) != NUMBER) ? inheritIfRequired(new NativeNumber(toNumber(it)), dummy, NumberWrapper) : toNumber(it);
    }

    return Number;
  }();

  for (var keys = DESCRIPTORS ? getOwnPropertyNames(NativeNumber) : ( // ES3:
  'MAX_VALUE,MIN_VALUE,NaN,NEGATIVE_INFINITY,POSITIVE_INFINITY,' + // ES2015 (in case, if modules with ES2015 Number statics required before):
  'EPSILON,isFinite,isInteger,isNaN,isSafeInteger,MAX_SAFE_INTEGER,' + 'MIN_SAFE_INTEGER,parseFloat,parseInt,isInteger,' + // ESNext
  'fromString,range').split(','), j = 0, key; keys.length > j; j++) {
    if (has(NativeNumber, key = keys[j]) && !has(NumberWrapper, key)) {
      defineProperty(NumberWrapper, key, getOwnPropertyDescriptor(NativeNumber, key));
    }
  }

  NumberWrapper.prototype = NumberPrototype;
  NumberPrototype.constructor = NumberWrapper;
  redefine(global, NUMBER, NumberWrapper);
}

/***/ }),

/***/ 88885:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174); // `Number.EPSILON` constant
// https://tc39.es/ecma262/#sec-number.epsilon


$({
  target: 'Number',
  stat: true
}, {
  EPSILON: Math.pow(2, -52)
});

/***/ }),

/***/ 45017:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var numberIsFinite = __webpack_require__(64630); // `Number.isFinite` method
// https://tc39.es/ecma262/#sec-number.isfinite


$({
  target: 'Number',
  stat: true
}, {
  isFinite: numberIsFinite
});

/***/ }),

/***/ 59743:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var isInteger = __webpack_require__(93624); // `Number.isInteger` method
// https://tc39.es/ecma262/#sec-number.isinteger


$({
  target: 'Number',
  stat: true
}, {
  isInteger: isInteger
});

/***/ }),

/***/ 47560:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174); // `Number.isNaN` method
// https://tc39.es/ecma262/#sec-number.isnan


$({
  target: 'Number',
  stat: true
}, {
  isNaN: function () {
    function isNaN(number) {
      // eslint-disable-next-line no-self-compare -- NaN check
      return number != number;
    }

    return isNaN;
  }()
});

/***/ }),

/***/ 31569:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var isInteger = __webpack_require__(93624);

var abs = Math.abs; // `Number.isSafeInteger` method
// https://tc39.es/ecma262/#sec-number.issafeinteger

$({
  target: 'Number',
  stat: true
}, {
  isSafeInteger: function () {
    function isSafeInteger(number) {
      return isInteger(number) && abs(number) <= 0x1FFFFFFFFFFFFF;
    }

    return isSafeInteger;
  }()
});

/***/ }),

/***/ 6134:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174); // `Number.MAX_SAFE_INTEGER` constant
// https://tc39.es/ecma262/#sec-number.max_safe_integer


$({
  target: 'Number',
  stat: true
}, {
  MAX_SAFE_INTEGER: 0x1FFFFFFFFFFFFF
});

/***/ }),

/***/ 12622:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174); // `Number.MIN_SAFE_INTEGER` constant
// https://tc39.es/ecma262/#sec-number.min_safe_integer


$({
  target: 'Number',
  stat: true
}, {
  MIN_SAFE_INTEGER: -0x1FFFFFFFFFFFFF
});

/***/ }),

/***/ 60852:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var parseFloat = __webpack_require__(78025); // `Number.parseFloat` method
// https://tc39.es/ecma262/#sec-number.parseFloat
// eslint-disable-next-line es/no-number-parsefloat -- required for testing


$({
  target: 'Number',
  stat: true,
  forced: Number.parseFloat != parseFloat
}, {
  parseFloat: parseFloat
});

/***/ }),

/***/ 99085:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var parseInt = __webpack_require__(55092); // `Number.parseInt` method
// https://tc39.es/ecma262/#sec-number.parseint
// eslint-disable-next-line es/no-number-parseint -- required for testing


$({
  target: 'Number',
  stat: true,
  forced: Number.parseInt != parseInt
}, {
  parseInt: parseInt
});

/***/ }),

/***/ 75180:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var toInteger = __webpack_require__(3997);

var thisNumberValue = __webpack_require__(81687);

var repeat = __webpack_require__(9811);

var fails = __webpack_require__(69691);

var nativeToFixed = 1.0.toFixed;
var floor = Math.floor;

var pow = function pow(x, n, acc) {
  return n === 0 ? acc : n % 2 === 1 ? pow(x, n - 1, acc * x) : pow(x * x, n / 2, acc);
};

var log = function log(x) {
  var n = 0;
  var x2 = x;

  while (x2 >= 4096) {
    n += 12;
    x2 /= 4096;
  }

  while (x2 >= 2) {
    n += 1;
    x2 /= 2;
  }

  return n;
};

var multiply = function multiply(data, n, c) {
  var index = -1;
  var c2 = c;

  while (++index < 6) {
    c2 += n * data[index];
    data[index] = c2 % 1e7;
    c2 = floor(c2 / 1e7);
  }
};

var divide = function divide(data, n) {
  var index = 6;
  var c = 0;

  while (--index >= 0) {
    c += data[index];
    data[index] = floor(c / n);
    c = c % n * 1e7;
  }
};

var dataToString = function dataToString(data) {
  var index = 6;
  var s = '';

  while (--index >= 0) {
    if (s !== '' || index === 0 || data[index] !== 0) {
      var t = String(data[index]);
      s = s === '' ? t : s + repeat.call('0', 7 - t.length) + t;
    }
  }

  return s;
};

var FORCED = nativeToFixed && (0.00008.toFixed(3) !== '0.000' || 0.9.toFixed(0) !== '1' || 1.255.toFixed(2) !== '1.25' || 1000000000000000128.0.toFixed(0) !== '1000000000000000128') || !fails(function () {
  // V8 ~ Android 4.3-
  nativeToFixed.call({});
}); // `Number.prototype.toFixed` method
// https://tc39.es/ecma262/#sec-number.prototype.tofixed

$({
  target: 'Number',
  proto: true,
  forced: FORCED
}, {
  toFixed: function () {
    function toFixed(fractionDigits) {
      var number = thisNumberValue(this);
      var fractDigits = toInteger(fractionDigits);
      var data = [0, 0, 0, 0, 0, 0];
      var sign = '';
      var result = '0';
      var e, z, j, k;
      if (fractDigits < 0 || fractDigits > 20) throw RangeError('Incorrect fraction digits'); // eslint-disable-next-line no-self-compare -- NaN check

      if (number != number) return 'NaN';
      if (number <= -1e21 || number >= 1e21) return String(number);

      if (number < 0) {
        sign = '-';
        number = -number;
      }

      if (number > 1e-21) {
        e = log(number * pow(2, 69, 1)) - 69;
        z = e < 0 ? number * pow(2, -e, 1) : number / pow(2, e, 1);
        z *= 0x10000000000000;
        e = 52 - e;

        if (e > 0) {
          multiply(data, 0, z);
          j = fractDigits;

          while (j >= 7) {
            multiply(data, 1e7, 0);
            j -= 7;
          }

          multiply(data, pow(10, j, 1), 0);
          j = e - 1;

          while (j >= 23) {
            divide(data, 1 << 23);
            j -= 23;
          }

          divide(data, 1 << j);
          multiply(data, 1, 1);
          divide(data, 2);
          result = dataToString(data);
        } else {
          multiply(data, 0, z);
          multiply(data, 1 << -e, 0);
          result = dataToString(data) + repeat.call('0', fractDigits);
        }
      }

      if (fractDigits > 0) {
        k = result.length;
        result = sign + (k <= fractDigits ? '0.' + repeat.call('0', fractDigits - k) + result : result.slice(0, k - fractDigits) + '.' + result.slice(k - fractDigits));
      } else {
        result = sign + result;
      }

      return result;
    }

    return toFixed;
  }()
});

/***/ }),

/***/ 79437:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var fails = __webpack_require__(69691);

var thisNumberValue = __webpack_require__(81687);

var nativeToPrecision = 1.0.toPrecision;
var FORCED = fails(function () {
  // IE7-
  return nativeToPrecision.call(1, undefined) !== '1';
}) || !fails(function () {
  // V8 ~ Android 4.3-
  nativeToPrecision.call({});
}); // `Number.prototype.toPrecision` method
// https://tc39.es/ecma262/#sec-number.prototype.toprecision

$({
  target: 'Number',
  proto: true,
  forced: FORCED
}, {
  toPrecision: function () {
    function toPrecision(precision) {
      return precision === undefined ? nativeToPrecision.call(thisNumberValue(this)) : nativeToPrecision.call(thisNumberValue(this), precision);
    }

    return toPrecision;
  }()
});

/***/ }),

/***/ 75370:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var assign = __webpack_require__(64905); // `Object.assign` method
// https://tc39.es/ecma262/#sec-object.assign
// eslint-disable-next-line es/no-object-assign -- required for testing


$({
  target: 'Object',
  stat: true,
  forced: Object.assign !== assign
}, {
  assign: assign
});

/***/ }),

/***/ 61022:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var DESCRIPTORS = __webpack_require__(75592);

var create = __webpack_require__(83681); // `Object.create` method
// https://tc39.es/ecma262/#sec-object.create


$({
  target: 'Object',
  stat: true,
  sham: !DESCRIPTORS
}, {
  create: create
});

/***/ }),

/***/ 50580:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var DESCRIPTORS = __webpack_require__(75592);

var FORCED = __webpack_require__(47339);

var toObject = __webpack_require__(12104);

var aFunction = __webpack_require__(83923);

var definePropertyModule = __webpack_require__(74217); // `Object.prototype.__defineGetter__` method
// https://tc39.es/ecma262/#sec-object.prototype.__defineGetter__


if (DESCRIPTORS) {
  $({
    target: 'Object',
    proto: true,
    forced: FORCED
  }, {
    __defineGetter__: function () {
      function __defineGetter__(P, getter) {
        definePropertyModule.f(toObject(this), P, {
          get: aFunction(getter),
          enumerable: true,
          configurable: true
        });
      }

      return __defineGetter__;
    }()
  });
}

/***/ }),

/***/ 5301:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var DESCRIPTORS = __webpack_require__(75592);

var defineProperties = __webpack_require__(91543); // `Object.defineProperties` method
// https://tc39.es/ecma262/#sec-object.defineproperties


$({
  target: 'Object',
  stat: true,
  forced: !DESCRIPTORS,
  sham: !DESCRIPTORS
}, {
  defineProperties: defineProperties
});

/***/ }),

/***/ 76438:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var DESCRIPTORS = __webpack_require__(75592);

var objectDefinePropertyModile = __webpack_require__(74217); // `Object.defineProperty` method
// https://tc39.es/ecma262/#sec-object.defineproperty


$({
  target: 'Object',
  stat: true,
  forced: !DESCRIPTORS,
  sham: !DESCRIPTORS
}, {
  defineProperty: objectDefinePropertyModile.f
});

/***/ }),

/***/ 97307:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var DESCRIPTORS = __webpack_require__(75592);

var FORCED = __webpack_require__(47339);

var toObject = __webpack_require__(12104);

var aFunction = __webpack_require__(83923);

var definePropertyModule = __webpack_require__(74217); // `Object.prototype.__defineSetter__` method
// https://tc39.es/ecma262/#sec-object.prototype.__defineSetter__


if (DESCRIPTORS) {
  $({
    target: 'Object',
    proto: true,
    forced: FORCED
  }, {
    __defineSetter__: function () {
      function __defineSetter__(P, setter) {
        definePropertyModule.f(toObject(this), P, {
          set: aFunction(setter),
          enumerable: true,
          configurable: true
        });
      }

      return __defineSetter__;
    }()
  });
}

/***/ }),

/***/ 34016:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var $entries = __webpack_require__(30000).entries; // `Object.entries` method
// https://tc39.es/ecma262/#sec-object.entries


$({
  target: 'Object',
  stat: true
}, {
  entries: function () {
    function entries(O) {
      return $entries(O);
    }

    return entries;
  }()
});

/***/ }),

/***/ 86944:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var FREEZING = __webpack_require__(7056);

var fails = __webpack_require__(69691);

var isObject = __webpack_require__(35611);

var onFreeze = __webpack_require__(40421).onFreeze; // eslint-disable-next-line es/no-object-freeze -- safe


var $freeze = Object.freeze;
var FAILS_ON_PRIMITIVES = fails(function () {
  $freeze(1);
}); // `Object.freeze` method
// https://tc39.es/ecma262/#sec-object.freeze

$({
  target: 'Object',
  stat: true,
  forced: FAILS_ON_PRIMITIVES,
  sham: !FREEZING
}, {
  freeze: function () {
    function freeze(it) {
      return $freeze && isObject(it) ? $freeze(onFreeze(it)) : it;
    }

    return freeze;
  }()
});

/***/ }),

/***/ 60687:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var iterate = __webpack_require__(56342);

var createProperty = __webpack_require__(39241); // `Object.fromEntries` method
// https://github.com/tc39/proposal-object-from-entries


$({
  target: 'Object',
  stat: true
}, {
  fromEntries: function () {
    function fromEntries(iterable) {
      var obj = {};
      iterate(iterable, function (k, v) {
        createProperty(obj, k, v);
      }, {
        AS_ENTRIES: true
      });
      return obj;
    }

    return fromEntries;
  }()
});

/***/ }),

/***/ 86957:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var fails = __webpack_require__(69691);

var toIndexedObject = __webpack_require__(26553);

var nativeGetOwnPropertyDescriptor = __webpack_require__(22245).f;

var DESCRIPTORS = __webpack_require__(75592);

var FAILS_ON_PRIMITIVES = fails(function () {
  nativeGetOwnPropertyDescriptor(1);
});
var FORCED = !DESCRIPTORS || FAILS_ON_PRIMITIVES; // `Object.getOwnPropertyDescriptor` method
// https://tc39.es/ecma262/#sec-object.getownpropertydescriptor

$({
  target: 'Object',
  stat: true,
  forced: FORCED,
  sham: !DESCRIPTORS
}, {
  getOwnPropertyDescriptor: function () {
    function getOwnPropertyDescriptor(it, key) {
      return nativeGetOwnPropertyDescriptor(toIndexedObject(it), key);
    }

    return getOwnPropertyDescriptor;
  }()
});

/***/ }),

/***/ 59163:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var DESCRIPTORS = __webpack_require__(75592);

var ownKeys = __webpack_require__(28482);

var toIndexedObject = __webpack_require__(26553);

var getOwnPropertyDescriptorModule = __webpack_require__(22245);

var createProperty = __webpack_require__(39241); // `Object.getOwnPropertyDescriptors` method
// https://tc39.es/ecma262/#sec-object.getownpropertydescriptors


$({
  target: 'Object',
  stat: true,
  sham: !DESCRIPTORS
}, {
  getOwnPropertyDescriptors: function () {
    function getOwnPropertyDescriptors(object) {
      var O = toIndexedObject(object);
      var getOwnPropertyDescriptor = getOwnPropertyDescriptorModule.f;
      var keys = ownKeys(O);
      var result = {};
      var index = 0;
      var key, descriptor;

      while (keys.length > index) {
        descriptor = getOwnPropertyDescriptor(O, key = keys[index++]);
        if (descriptor !== undefined) createProperty(result, key, descriptor);
      }

      return result;
    }

    return getOwnPropertyDescriptors;
  }()
});

/***/ }),

/***/ 34128:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var fails = __webpack_require__(69691);

var getOwnPropertyNames = __webpack_require__(59995).f; // eslint-disable-next-line es/no-object-getownpropertynames -- required for testing


var FAILS_ON_PRIMITIVES = fails(function () {
  return !Object.getOwnPropertyNames(1);
}); // `Object.getOwnPropertyNames` method
// https://tc39.es/ecma262/#sec-object.getownpropertynames

$({
  target: 'Object',
  stat: true,
  forced: FAILS_ON_PRIMITIVES
}, {
  getOwnPropertyNames: getOwnPropertyNames
});

/***/ }),

/***/ 24267:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var fails = __webpack_require__(69691);

var toObject = __webpack_require__(12104);

var nativeGetPrototypeOf = __webpack_require__(6863);

var CORRECT_PROTOTYPE_GETTER = __webpack_require__(28954);

var FAILS_ON_PRIMITIVES = fails(function () {
  nativeGetPrototypeOf(1);
}); // `Object.getPrototypeOf` method
// https://tc39.es/ecma262/#sec-object.getprototypeof

$({
  target: 'Object',
  stat: true,
  forced: FAILS_ON_PRIMITIVES,
  sham: !CORRECT_PROTOTYPE_GETTER
}, {
  getPrototypeOf: function () {
    function getPrototypeOf(it) {
      return nativeGetPrototypeOf(toObject(it));
    }

    return getPrototypeOf;
  }()
});

/***/ }),

/***/ 4239:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var fails = __webpack_require__(69691);

var isObject = __webpack_require__(35611); // eslint-disable-next-line es/no-object-isextensible -- safe


var $isExtensible = Object.isExtensible;
var FAILS_ON_PRIMITIVES = fails(function () {
  $isExtensible(1);
}); // `Object.isExtensible` method
// https://tc39.es/ecma262/#sec-object.isextensible

$({
  target: 'Object',
  stat: true,
  forced: FAILS_ON_PRIMITIVES
}, {
  isExtensible: function () {
    function isExtensible(it) {
      return isObject(it) ? $isExtensible ? $isExtensible(it) : true : false;
    }

    return isExtensible;
  }()
});

/***/ }),

/***/ 8381:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var fails = __webpack_require__(69691);

var isObject = __webpack_require__(35611); // eslint-disable-next-line es/no-object-isfrozen -- safe


var $isFrozen = Object.isFrozen;
var FAILS_ON_PRIMITIVES = fails(function () {
  $isFrozen(1);
}); // `Object.isFrozen` method
// https://tc39.es/ecma262/#sec-object.isfrozen

$({
  target: 'Object',
  stat: true,
  forced: FAILS_ON_PRIMITIVES
}, {
  isFrozen: function () {
    function isFrozen(it) {
      return isObject(it) ? $isFrozen ? $isFrozen(it) : false : true;
    }

    return isFrozen;
  }()
});

/***/ }),

/***/ 63669:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var fails = __webpack_require__(69691);

var isObject = __webpack_require__(35611); // eslint-disable-next-line es/no-object-issealed -- safe


var $isSealed = Object.isSealed;
var FAILS_ON_PRIMITIVES = fails(function () {
  $isSealed(1);
}); // `Object.isSealed` method
// https://tc39.es/ecma262/#sec-object.issealed

$({
  target: 'Object',
  stat: true,
  forced: FAILS_ON_PRIMITIVES
}, {
  isSealed: function () {
    function isSealed(it) {
      return isObject(it) ? $isSealed ? $isSealed(it) : false : true;
    }

    return isSealed;
  }()
});

/***/ }),

/***/ 39816:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var is = __webpack_require__(61398); // `Object.is` method
// https://tc39.es/ecma262/#sec-object.is


$({
  target: 'Object',
  stat: true
}, {
  is: is
});

/***/ }),

/***/ 25779:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var toObject = __webpack_require__(12104);

var nativeKeys = __webpack_require__(84547);

var fails = __webpack_require__(69691);

var FAILS_ON_PRIMITIVES = fails(function () {
  nativeKeys(1);
}); // `Object.keys` method
// https://tc39.es/ecma262/#sec-object.keys

$({
  target: 'Object',
  stat: true,
  forced: FAILS_ON_PRIMITIVES
}, {
  keys: function () {
    function keys(it) {
      return nativeKeys(toObject(it));
    }

    return keys;
  }()
});

/***/ }),

/***/ 84580:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var DESCRIPTORS = __webpack_require__(75592);

var FORCED = __webpack_require__(47339);

var toObject = __webpack_require__(12104);

var toPropertyKey = __webpack_require__(14613);

var getPrototypeOf = __webpack_require__(6863);

var getOwnPropertyDescriptor = __webpack_require__(22245).f; // `Object.prototype.__lookupGetter__` method
// https://tc39.es/ecma262/#sec-object.prototype.__lookupGetter__


if (DESCRIPTORS) {
  $({
    target: 'Object',
    proto: true,
    forced: FORCED
  }, {
    __lookupGetter__: function () {
      function __lookupGetter__(P) {
        var O = toObject(this);
        var key = toPropertyKey(P);
        var desc;

        do {
          if (desc = getOwnPropertyDescriptor(O, key)) return desc.get;
        } while (O = getPrototypeOf(O));
      }

      return __lookupGetter__;
    }()
  });
}

/***/ }),

/***/ 33198:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var DESCRIPTORS = __webpack_require__(75592);

var FORCED = __webpack_require__(47339);

var toObject = __webpack_require__(12104);

var toPropertyKey = __webpack_require__(14613);

var getPrototypeOf = __webpack_require__(6863);

var getOwnPropertyDescriptor = __webpack_require__(22245).f; // `Object.prototype.__lookupSetter__` method
// https://tc39.es/ecma262/#sec-object.prototype.__lookupSetter__


if (DESCRIPTORS) {
  $({
    target: 'Object',
    proto: true,
    forced: FORCED
  }, {
    __lookupSetter__: function () {
      function __lookupSetter__(P) {
        var O = toObject(this);
        var key = toPropertyKey(P);
        var desc;

        do {
          if (desc = getOwnPropertyDescriptor(O, key)) return desc.set;
        } while (O = getPrototypeOf(O));
      }

      return __lookupSetter__;
    }()
  });
}

/***/ }),

/***/ 71895:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var isObject = __webpack_require__(35611);

var onFreeze = __webpack_require__(40421).onFreeze;

var FREEZING = __webpack_require__(7056);

var fails = __webpack_require__(69691); // eslint-disable-next-line es/no-object-preventextensions -- safe


var $preventExtensions = Object.preventExtensions;
var FAILS_ON_PRIMITIVES = fails(function () {
  $preventExtensions(1);
}); // `Object.preventExtensions` method
// https://tc39.es/ecma262/#sec-object.preventextensions

$({
  target: 'Object',
  stat: true,
  forced: FAILS_ON_PRIMITIVES,
  sham: !FREEZING
}, {
  preventExtensions: function () {
    function preventExtensions(it) {
      return $preventExtensions && isObject(it) ? $preventExtensions(onFreeze(it)) : it;
    }

    return preventExtensions;
  }()
});

/***/ }),

/***/ 44910:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var isObject = __webpack_require__(35611);

var onFreeze = __webpack_require__(40421).onFreeze;

var FREEZING = __webpack_require__(7056);

var fails = __webpack_require__(69691); // eslint-disable-next-line es/no-object-seal -- safe


var $seal = Object.seal;
var FAILS_ON_PRIMITIVES = fails(function () {
  $seal(1);
}); // `Object.seal` method
// https://tc39.es/ecma262/#sec-object.seal

$({
  target: 'Object',
  stat: true,
  forced: FAILS_ON_PRIMITIVES,
  sham: !FREEZING
}, {
  seal: function () {
    function seal(it) {
      return $seal && isObject(it) ? $seal(onFreeze(it)) : it;
    }

    return seal;
  }()
});

/***/ }),

/***/ 95962:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var setPrototypeOf = __webpack_require__(18444); // `Object.setPrototypeOf` method
// https://tc39.es/ecma262/#sec-object.setprototypeof


$({
  target: 'Object',
  stat: true
}, {
  setPrototypeOf: setPrototypeOf
});

/***/ }),

/***/ 73769:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var TO_STRING_TAG_SUPPORT = __webpack_require__(64910);

var redefine = __webpack_require__(11807);

var toString = __webpack_require__(13966); // `Object.prototype.toString` method
// https://tc39.es/ecma262/#sec-object.prototype.tostring


if (!TO_STRING_TAG_SUPPORT) {
  redefine(Object.prototype, 'toString', toString, {
    unsafe: true
  });
}

/***/ }),

/***/ 14962:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var $values = __webpack_require__(30000).values; // `Object.values` method
// https://tc39.es/ecma262/#sec-object.values


$({
  target: 'Object',
  stat: true
}, {
  values: function () {
    function values(O) {
      return $values(O);
    }

    return values;
  }()
});

/***/ }),

/***/ 80345:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var parseFloatImplementation = __webpack_require__(78025); // `parseFloat` method
// https://tc39.es/ecma262/#sec-parsefloat-string


$({
  global: true,
  forced: parseFloat != parseFloatImplementation
}, {
  parseFloat: parseFloatImplementation
});

/***/ }),

/***/ 18438:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var parseIntImplementation = __webpack_require__(55092); // `parseInt` method
// https://tc39.es/ecma262/#sec-parseint-string-radix


$({
  global: true,
  forced: parseInt != parseIntImplementation
}, {
  parseInt: parseIntImplementation
});

/***/ }),

/***/ 46356:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var IS_PURE = __webpack_require__(1986);

var NativePromise = __webpack_require__(4302);

var fails = __webpack_require__(69691);

var getBuiltIn = __webpack_require__(69808);

var speciesConstructor = __webpack_require__(24345);

var promiseResolve = __webpack_require__(48141);

var redefine = __webpack_require__(11807); // Safari bug https://bugs.webkit.org/show_bug.cgi?id=200829


var NON_GENERIC = !!NativePromise && fails(function () {
  NativePromise.prototype['finally'].call({
    then: function () {
      function then() {
        /* empty */
      }

      return then;
    }()
  }, function () {
    /* empty */
  });
}); // `Promise.prototype.finally` method
// https://tc39.es/ecma262/#sec-promise.prototype.finally

$({
  target: 'Promise',
  proto: true,
  real: true,
  forced: NON_GENERIC
}, {
  'finally': function () {
    function _finally(onFinally) {
      var C = speciesConstructor(this, getBuiltIn('Promise'));
      var isFunction = typeof onFinally == 'function';
      return this.then(isFunction ? function (x) {
        return promiseResolve(C, onFinally()).then(function () {
          return x;
        });
      } : onFinally, isFunction ? function (e) {
        return promiseResolve(C, onFinally()).then(function () {
          throw e;
        });
      } : onFinally);
    }

    return _finally;
  }()
}); // makes sure that native promise-based APIs `Promise#finally` properly works with patched `Promise#then`

if (!IS_PURE && typeof NativePromise == 'function') {
  var method = getBuiltIn('Promise').prototype['finally'];

  if (NativePromise.prototype['finally'] !== method) {
    redefine(NativePromise.prototype, 'finally', method, {
      unsafe: true
    });
  }
}

/***/ }),

/***/ 70552:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var IS_PURE = __webpack_require__(1986);

var global = __webpack_require__(84253);

var getBuiltIn = __webpack_require__(69808);

var NativePromise = __webpack_require__(4302);

var redefine = __webpack_require__(11807);

var redefineAll = __webpack_require__(3993);

var setPrototypeOf = __webpack_require__(18444);

var setToStringTag = __webpack_require__(29108);

var setSpecies = __webpack_require__(64157);

var isObject = __webpack_require__(35611);

var aFunction = __webpack_require__(83923);

var anInstance = __webpack_require__(50997);

var inspectSource = __webpack_require__(89853);

var iterate = __webpack_require__(56342);

var checkCorrectnessOfIteration = __webpack_require__(68762);

var speciesConstructor = __webpack_require__(24345);

var task = __webpack_require__(33453).set;

var microtask = __webpack_require__(80909);

var promiseResolve = __webpack_require__(48141);

var hostReportErrors = __webpack_require__(21419);

var newPromiseCapabilityModule = __webpack_require__(82101);

var perform = __webpack_require__(47673);

var InternalStateModule = __webpack_require__(79947);

var isForced = __webpack_require__(41441);

var wellKnownSymbol = __webpack_require__(17657);

var IS_BROWSER = __webpack_require__(46392);

var IS_NODE = __webpack_require__(41488);

var V8_VERSION = __webpack_require__(12338);

var SPECIES = wellKnownSymbol('species');
var PROMISE = 'Promise';
var getInternalState = InternalStateModule.get;
var setInternalState = InternalStateModule.set;
var getInternalPromiseState = InternalStateModule.getterFor(PROMISE);
var NativePromisePrototype = NativePromise && NativePromise.prototype;
var PromiseConstructor = NativePromise;
var PromiseConstructorPrototype = NativePromisePrototype;
var TypeError = global.TypeError;
var document = global.document;
var process = global.process;
var newPromiseCapability = newPromiseCapabilityModule.f;
var newGenericPromiseCapability = newPromiseCapability;
var DISPATCH_EVENT = !!(document && document.createEvent && global.dispatchEvent);
var NATIVE_REJECTION_EVENT = typeof PromiseRejectionEvent == 'function';
var UNHANDLED_REJECTION = 'unhandledrejection';
var REJECTION_HANDLED = 'rejectionhandled';
var PENDING = 0;
var FULFILLED = 1;
var REJECTED = 2;
var HANDLED = 1;
var UNHANDLED = 2;
var SUBCLASSING = false;
var Internal, OwnPromiseCapability, PromiseWrapper, nativeThen;
var FORCED = isForced(PROMISE, function () {
  var PROMISE_CONSTRUCTOR_SOURCE = inspectSource(PromiseConstructor);
  var GLOBAL_CORE_JS_PROMISE = PROMISE_CONSTRUCTOR_SOURCE !== String(PromiseConstructor); // V8 6.6 (Node 10 and Chrome 66) have a bug with resolving custom thenables
  // https://bugs.chromium.org/p/chromium/issues/detail?id=830565
  // We can't detect it synchronously, so just check versions

  if (!GLOBAL_CORE_JS_PROMISE && V8_VERSION === 66) return true; // We need Promise#finally in the pure version for preventing prototype pollution

  if (IS_PURE && !PromiseConstructorPrototype['finally']) return true; // We can't use @@species feature detection in V8 since it causes
  // deoptimization and performance degradation
  // https://github.com/zloirock/core-js/issues/679

  if (V8_VERSION >= 51 && /native code/.test(PROMISE_CONSTRUCTOR_SOURCE)) return false; // Detect correctness of subclassing with @@species support

  var promise = new PromiseConstructor(function (resolve) {
    resolve(1);
  });

  var FakePromise = function FakePromise(exec) {
    exec(function () {
      /* empty */
    }, function () {
      /* empty */
    });
  };

  var constructor = promise.constructor = {};
  constructor[SPECIES] = FakePromise;
  SUBCLASSING = promise.then(function () {
    /* empty */
  }) instanceof FakePromise;
  if (!SUBCLASSING) return true; // Unhandled rejections tracking support, NodeJS Promise without it fails @@species test

  return !GLOBAL_CORE_JS_PROMISE && IS_BROWSER && !NATIVE_REJECTION_EVENT;
});
var INCORRECT_ITERATION = FORCED || !checkCorrectnessOfIteration(function (iterable) {
  PromiseConstructor.all(iterable)['catch'](function () {
    /* empty */
  });
}); // helpers

var isThenable = function isThenable(it) {
  var then;
  return isObject(it) && typeof (then = it.then) == 'function' ? then : false;
};

var notify = function notify(state, isReject) {
  if (state.notified) return;
  state.notified = true;
  var chain = state.reactions;
  microtask(function () {
    var value = state.value;
    var ok = state.state == FULFILLED;
    var index = 0; // variable length - can't use forEach

    while (chain.length > index) {
      var reaction = chain[index++];
      var handler = ok ? reaction.ok : reaction.fail;
      var resolve = reaction.resolve;
      var reject = reaction.reject;
      var domain = reaction.domain;
      var result, then, exited;

      try {
        if (handler) {
          if (!ok) {
            if (state.rejection === UNHANDLED) onHandleUnhandled(state);
            state.rejection = HANDLED;
          }

          if (handler === true) result = value;else {
            if (domain) domain.enter();
            result = handler(value); // can throw

            if (domain) {
              domain.exit();
              exited = true;
            }
          }

          if (result === reaction.promise) {
            reject(TypeError('Promise-chain cycle'));
          } else if (then = isThenable(result)) {
            then.call(result, resolve, reject);
          } else resolve(result);
        } else reject(value);
      } catch (error) {
        if (domain && !exited) domain.exit();
        reject(error);
      }
    }

    state.reactions = [];
    state.notified = false;
    if (isReject && !state.rejection) onUnhandled(state);
  });
};

var dispatchEvent = function dispatchEvent(name, promise, reason) {
  var event, handler;

  if (DISPATCH_EVENT) {
    event = document.createEvent('Event');
    event.promise = promise;
    event.reason = reason;
    event.initEvent(name, false, true);
    global.dispatchEvent(event);
  } else event = {
    promise: promise,
    reason: reason
  };

  if (!NATIVE_REJECTION_EVENT && (handler = global['on' + name])) handler(event);else if (name === UNHANDLED_REJECTION) hostReportErrors('Unhandled promise rejection', reason);
};

var onUnhandled = function onUnhandled(state) {
  task.call(global, function () {
    var promise = state.facade;
    var value = state.value;
    var IS_UNHANDLED = isUnhandled(state);
    var result;

    if (IS_UNHANDLED) {
      result = perform(function () {
        if (IS_NODE) {
          process.emit('unhandledRejection', value, promise);
        } else dispatchEvent(UNHANDLED_REJECTION, promise, value);
      }); // Browsers should not trigger `rejectionHandled` event if it was handled here, NodeJS - should

      state.rejection = IS_NODE || isUnhandled(state) ? UNHANDLED : HANDLED;
      if (result.error) throw result.value;
    }
  });
};

var isUnhandled = function isUnhandled(state) {
  return state.rejection !== HANDLED && !state.parent;
};

var onHandleUnhandled = function onHandleUnhandled(state) {
  task.call(global, function () {
    var promise = state.facade;

    if (IS_NODE) {
      process.emit('rejectionHandled', promise);
    } else dispatchEvent(REJECTION_HANDLED, promise, state.value);
  });
};

var bind = function bind(fn, state, unwrap) {
  return function (value) {
    fn(state, value, unwrap);
  };
};

var internalReject = function internalReject(state, value, unwrap) {
  if (state.done) return;
  state.done = true;
  if (unwrap) state = unwrap;
  state.value = value;
  state.state = REJECTED;
  notify(state, true);
};

var internalResolve = function internalResolve(state, value, unwrap) {
  if (state.done) return;
  state.done = true;
  if (unwrap) state = unwrap;

  try {
    if (state.facade === value) throw TypeError("Promise can't be resolved itself");
    var then = isThenable(value);

    if (then) {
      microtask(function () {
        var wrapper = {
          done: false
        };

        try {
          then.call(value, bind(internalResolve, wrapper, state), bind(internalReject, wrapper, state));
        } catch (error) {
          internalReject(wrapper, error, state);
        }
      });
    } else {
      state.value = value;
      state.state = FULFILLED;
      notify(state, false);
    }
  } catch (error) {
    internalReject({
      done: false
    }, error, state);
  }
}; // constructor polyfill


if (FORCED) {
  // 25.4.3.1 Promise(executor)
  PromiseConstructor = function () {
    function Promise(executor) {
      anInstance(this, PromiseConstructor, PROMISE);
      aFunction(executor);
      Internal.call(this);
      var state = getInternalState(this);

      try {
        executor(bind(internalResolve, state), bind(internalReject, state));
      } catch (error) {
        internalReject(state, error);
      }
    }

    return Promise;
  }();

  PromiseConstructorPrototype = PromiseConstructor.prototype; // eslint-disable-next-line no-unused-vars -- required for `.length`

  Internal = function () {
    function Promise(executor) {
      setInternalState(this, {
        type: PROMISE,
        done: false,
        notified: false,
        parent: false,
        reactions: [],
        rejection: false,
        state: PENDING,
        value: undefined
      });
    }

    return Promise;
  }();

  Internal.prototype = redefineAll(PromiseConstructorPrototype, {
    // `Promise.prototype.then` method
    // https://tc39.es/ecma262/#sec-promise.prototype.then
    then: function () {
      function then(onFulfilled, onRejected) {
        var state = getInternalPromiseState(this);
        var reaction = newPromiseCapability(speciesConstructor(this, PromiseConstructor));
        reaction.ok = typeof onFulfilled == 'function' ? onFulfilled : true;
        reaction.fail = typeof onRejected == 'function' && onRejected;
        reaction.domain = IS_NODE ? process.domain : undefined;
        state.parent = true;
        state.reactions.push(reaction);
        if (state.state != PENDING) notify(state, false);
        return reaction.promise;
      }

      return then;
    }(),
    // `Promise.prototype.catch` method
    // https://tc39.es/ecma262/#sec-promise.prototype.catch
    'catch': function () {
      function _catch(onRejected) {
        return this.then(undefined, onRejected);
      }

      return _catch;
    }()
  });

  OwnPromiseCapability = function OwnPromiseCapability() {
    var promise = new Internal();
    var state = getInternalState(promise);
    this.promise = promise;
    this.resolve = bind(internalResolve, state);
    this.reject = bind(internalReject, state);
  };

  newPromiseCapabilityModule.f = newPromiseCapability = function newPromiseCapability(C) {
    return C === PromiseConstructor || C === PromiseWrapper ? new OwnPromiseCapability(C) : newGenericPromiseCapability(C);
  };

  if (!IS_PURE && typeof NativePromise == 'function' && NativePromisePrototype !== Object.prototype) {
    nativeThen = NativePromisePrototype.then;

    if (!SUBCLASSING) {
      // make `Promise#then` return a polyfilled `Promise` for native promise-based APIs
      redefine(NativePromisePrototype, 'then', function () {
        function then(onFulfilled, onRejected) {
          var that = this;
          return new PromiseConstructor(function (resolve, reject) {
            nativeThen.call(that, resolve, reject);
          }).then(onFulfilled, onRejected); // https://github.com/zloirock/core-js/issues/640
        }

        return then;
      }(), {
        unsafe: true
      }); // makes sure that native promise-based APIs `Promise#catch` properly works with patched `Promise#then`

      redefine(NativePromisePrototype, 'catch', PromiseConstructorPrototype['catch'], {
        unsafe: true
      });
    } // make `.constructor === Promise` work for native promise-based APIs


    try {
      delete NativePromisePrototype.constructor;
    } catch (error) {
      /* empty */
    } // make `instanceof Promise` work for native promise-based APIs


    if (setPrototypeOf) {
      setPrototypeOf(NativePromisePrototype, PromiseConstructorPrototype);
    }
  }
}

$({
  global: true,
  wrap: true,
  forced: FORCED
}, {
  Promise: PromiseConstructor
});
setToStringTag(PromiseConstructor, PROMISE, false, true);
setSpecies(PROMISE);
PromiseWrapper = getBuiltIn(PROMISE); // statics

$({
  target: PROMISE,
  stat: true,
  forced: FORCED
}, {
  // `Promise.reject` method
  // https://tc39.es/ecma262/#sec-promise.reject
  reject: function () {
    function reject(r) {
      var capability = newPromiseCapability(this);
      capability.reject.call(undefined, r);
      return capability.promise;
    }

    return reject;
  }()
});
$({
  target: PROMISE,
  stat: true,
  forced: IS_PURE || FORCED
}, {
  // `Promise.resolve` method
  // https://tc39.es/ecma262/#sec-promise.resolve
  resolve: function () {
    function resolve(x) {
      return promiseResolve(IS_PURE && this === PromiseWrapper ? PromiseConstructor : this, x);
    }

    return resolve;
  }()
});
$({
  target: PROMISE,
  stat: true,
  forced: INCORRECT_ITERATION
}, {
  // `Promise.all` method
  // https://tc39.es/ecma262/#sec-promise.all
  all: function () {
    function all(iterable) {
      var C = this;
      var capability = newPromiseCapability(C);
      var resolve = capability.resolve;
      var reject = capability.reject;
      var result = perform(function () {
        var $promiseResolve = aFunction(C.resolve);
        var values = [];
        var counter = 0;
        var remaining = 1;
        iterate(iterable, function (promise) {
          var index = counter++;
          var alreadyCalled = false;
          values.push(undefined);
          remaining++;
          $promiseResolve.call(C, promise).then(function (value) {
            if (alreadyCalled) return;
            alreadyCalled = true;
            values[index] = value;
            --remaining || resolve(values);
          }, reject);
        });
        --remaining || resolve(values);
      });
      if (result.error) reject(result.value);
      return capability.promise;
    }

    return all;
  }(),
  // `Promise.race` method
  // https://tc39.es/ecma262/#sec-promise.race
  race: function () {
    function race(iterable) {
      var C = this;
      var capability = newPromiseCapability(C);
      var reject = capability.reject;
      var result = perform(function () {
        var $promiseResolve = aFunction(C.resolve);
        iterate(iterable, function (promise) {
          $promiseResolve.call(C, promise).then(capability.resolve, reject);
        });
      });
      if (result.error) reject(result.value);
      return capability.promise;
    }

    return race;
  }()
});

/***/ }),

/***/ 57128:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var getBuiltIn = __webpack_require__(69808);

var aFunction = __webpack_require__(83923);

var anObject = __webpack_require__(63518);

var fails = __webpack_require__(69691);

var nativeApply = getBuiltIn('Reflect', 'apply');
var functionApply = Function.apply; // MS Edge argumentsList argument is optional

var OPTIONAL_ARGUMENTS_LIST = !fails(function () {
  nativeApply(function () {
    /* empty */
  });
}); // `Reflect.apply` method
// https://tc39.es/ecma262/#sec-reflect.apply

$({
  target: 'Reflect',
  stat: true,
  forced: OPTIONAL_ARGUMENTS_LIST
}, {
  apply: function () {
    function apply(target, thisArgument, argumentsList) {
      aFunction(target);
      anObject(argumentsList);
      return nativeApply ? nativeApply(target, thisArgument, argumentsList) : functionApply.call(target, thisArgument, argumentsList);
    }

    return apply;
  }()
});

/***/ }),

/***/ 83588:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var getBuiltIn = __webpack_require__(69808);

var aFunction = __webpack_require__(83923);

var anObject = __webpack_require__(63518);

var isObject = __webpack_require__(35611);

var create = __webpack_require__(83681);

var bind = __webpack_require__(68719);

var fails = __webpack_require__(69691);

var nativeConstruct = getBuiltIn('Reflect', 'construct'); // `Reflect.construct` method
// https://tc39.es/ecma262/#sec-reflect.construct
// MS Edge supports only 2 arguments and argumentsList argument is optional
// FF Nightly sets third argument as `new.target`, but does not create `this` from it

var NEW_TARGET_BUG = fails(function () {
  function F() {
    /* empty */
  }

  return !(nativeConstruct(function () {
    /* empty */
  }, [], F) instanceof F);
});
var ARGS_BUG = !fails(function () {
  nativeConstruct(function () {
    /* empty */
  });
});
var FORCED = NEW_TARGET_BUG || ARGS_BUG;
$({
  target: 'Reflect',
  stat: true,
  forced: FORCED,
  sham: FORCED
}, {
  construct: function () {
    function construct(Target, args
    /* , newTarget */
    ) {
      aFunction(Target);
      anObject(args);
      var newTarget = arguments.length < 3 ? Target : aFunction(arguments[2]);
      if (ARGS_BUG && !NEW_TARGET_BUG) return nativeConstruct(Target, args, newTarget);

      if (Target == newTarget) {
        // w/o altered newTarget, optimization for 0-4 arguments
        switch (args.length) {
          case 0:
            return new Target();

          case 1:
            return new Target(args[0]);

          case 2:
            return new Target(args[0], args[1]);

          case 3:
            return new Target(args[0], args[1], args[2]);

          case 4:
            return new Target(args[0], args[1], args[2], args[3]);
        } // w/o altered newTarget, lot of arguments case


        var $args = [null];
        $args.push.apply($args, args);
        return new (bind.apply(Target, $args))();
      } // with altered newTarget, not support built-in constructors


      var proto = newTarget.prototype;
      var instance = create(isObject(proto) ? proto : Object.prototype);
      var result = Function.apply.call(Target, instance, args);
      return isObject(result) ? result : instance;
    }

    return construct;
  }()
});

/***/ }),

/***/ 47057:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var DESCRIPTORS = __webpack_require__(75592);

var anObject = __webpack_require__(63518);

var toPropertyKey = __webpack_require__(14613);

var definePropertyModule = __webpack_require__(74217);

var fails = __webpack_require__(69691); // MS Edge has broken Reflect.defineProperty - throwing instead of returning false


var ERROR_INSTEAD_OF_FALSE = fails(function () {
  // eslint-disable-next-line es/no-reflect -- required for testing
  Reflect.defineProperty(definePropertyModule.f({}, 1, {
    value: 1
  }), 1, {
    value: 2
  });
}); // `Reflect.defineProperty` method
// https://tc39.es/ecma262/#sec-reflect.defineproperty

$({
  target: 'Reflect',
  stat: true,
  forced: ERROR_INSTEAD_OF_FALSE,
  sham: !DESCRIPTORS
}, {
  defineProperty: function () {
    function defineProperty(target, propertyKey, attributes) {
      anObject(target);
      var key = toPropertyKey(propertyKey);
      anObject(attributes);

      try {
        definePropertyModule.f(target, key, attributes);
        return true;
      } catch (error) {
        return false;
      }
    }

    return defineProperty;
  }()
});

/***/ }),

/***/ 67092:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var anObject = __webpack_require__(63518);

var getOwnPropertyDescriptor = __webpack_require__(22245).f; // `Reflect.deleteProperty` method
// https://tc39.es/ecma262/#sec-reflect.deleteproperty


$({
  target: 'Reflect',
  stat: true
}, {
  deleteProperty: function () {
    function deleteProperty(target, propertyKey) {
      var descriptor = getOwnPropertyDescriptor(anObject(target), propertyKey);
      return descriptor && !descriptor.configurable ? false : delete target[propertyKey];
    }

    return deleteProperty;
  }()
});

/***/ }),

/***/ 69203:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var DESCRIPTORS = __webpack_require__(75592);

var anObject = __webpack_require__(63518);

var getOwnPropertyDescriptorModule = __webpack_require__(22245); // `Reflect.getOwnPropertyDescriptor` method
// https://tc39.es/ecma262/#sec-reflect.getownpropertydescriptor


$({
  target: 'Reflect',
  stat: true,
  sham: !DESCRIPTORS
}, {
  getOwnPropertyDescriptor: function () {
    function getOwnPropertyDescriptor(target, propertyKey) {
      return getOwnPropertyDescriptorModule.f(anObject(target), propertyKey);
    }

    return getOwnPropertyDescriptor;
  }()
});

/***/ }),

/***/ 27879:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var anObject = __webpack_require__(63518);

var objectGetPrototypeOf = __webpack_require__(6863);

var CORRECT_PROTOTYPE_GETTER = __webpack_require__(28954); // `Reflect.getPrototypeOf` method
// https://tc39.es/ecma262/#sec-reflect.getprototypeof


$({
  target: 'Reflect',
  stat: true,
  sham: !CORRECT_PROTOTYPE_GETTER
}, {
  getPrototypeOf: function () {
    function getPrototypeOf(target) {
      return objectGetPrototypeOf(anObject(target));
    }

    return getPrototypeOf;
  }()
});

/***/ }),

/***/ 59478:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var isObject = __webpack_require__(35611);

var anObject = __webpack_require__(63518);

var has = __webpack_require__(28919);

var getOwnPropertyDescriptorModule = __webpack_require__(22245);

var getPrototypeOf = __webpack_require__(6863); // `Reflect.get` method
// https://tc39.es/ecma262/#sec-reflect.get


function get(target, propertyKey
/* , receiver */
) {
  var receiver = arguments.length < 3 ? target : arguments[2];
  var descriptor, prototype;
  if (anObject(target) === receiver) return target[propertyKey];
  if (descriptor = getOwnPropertyDescriptorModule.f(target, propertyKey)) return has(descriptor, 'value') ? descriptor.value : descriptor.get === undefined ? undefined : descriptor.get.call(receiver);
  if (isObject(prototype = getPrototypeOf(target))) return get(prototype, propertyKey, receiver);
}

$({
  target: 'Reflect',
  stat: true
}, {
  get: get
});

/***/ }),

/***/ 71507:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174); // `Reflect.has` method
// https://tc39.es/ecma262/#sec-reflect.has


$({
  target: 'Reflect',
  stat: true
}, {
  has: function () {
    function has(target, propertyKey) {
      return propertyKey in target;
    }

    return has;
  }()
});

/***/ }),

/***/ 39021:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var anObject = __webpack_require__(63518); // eslint-disable-next-line es/no-object-isextensible -- safe


var objectIsExtensible = Object.isExtensible; // `Reflect.isExtensible` method
// https://tc39.es/ecma262/#sec-reflect.isextensible

$({
  target: 'Reflect',
  stat: true
}, {
  isExtensible: function () {
    function isExtensible(target) {
      anObject(target);
      return objectIsExtensible ? objectIsExtensible(target) : true;
    }

    return isExtensible;
  }()
});

/***/ }),

/***/ 8641:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var ownKeys = __webpack_require__(28482); // `Reflect.ownKeys` method
// https://tc39.es/ecma262/#sec-reflect.ownkeys


$({
  target: 'Reflect',
  stat: true
}, {
  ownKeys: ownKeys
});

/***/ }),

/***/ 83064:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var getBuiltIn = __webpack_require__(69808);

var anObject = __webpack_require__(63518);

var FREEZING = __webpack_require__(7056); // `Reflect.preventExtensions` method
// https://tc39.es/ecma262/#sec-reflect.preventextensions


$({
  target: 'Reflect',
  stat: true,
  sham: !FREEZING
}, {
  preventExtensions: function () {
    function preventExtensions(target) {
      anObject(target);

      try {
        var objectPreventExtensions = getBuiltIn('Object', 'preventExtensions');
        if (objectPreventExtensions) objectPreventExtensions(target);
        return true;
      } catch (error) {
        return false;
      }
    }

    return preventExtensions;
  }()
});

/***/ }),

/***/ 33362:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var anObject = __webpack_require__(63518);

var aPossiblePrototype = __webpack_require__(1372);

var objectSetPrototypeOf = __webpack_require__(18444); // `Reflect.setPrototypeOf` method
// https://tc39.es/ecma262/#sec-reflect.setprototypeof


if (objectSetPrototypeOf) $({
  target: 'Reflect',
  stat: true
}, {
  setPrototypeOf: function () {
    function setPrototypeOf(target, proto) {
      anObject(target);
      aPossiblePrototype(proto);

      try {
        objectSetPrototypeOf(target, proto);
        return true;
      } catch (error) {
        return false;
      }
    }

    return setPrototypeOf;
  }()
});

/***/ }),

/***/ 93115:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var anObject = __webpack_require__(63518);

var isObject = __webpack_require__(35611);

var has = __webpack_require__(28919);

var fails = __webpack_require__(69691);

var definePropertyModule = __webpack_require__(74217);

var getOwnPropertyDescriptorModule = __webpack_require__(22245);

var getPrototypeOf = __webpack_require__(6863);

var createPropertyDescriptor = __webpack_require__(39160); // `Reflect.set` method
// https://tc39.es/ecma262/#sec-reflect.set


function set(target, propertyKey, V
/* , receiver */
) {
  var receiver = arguments.length < 4 ? target : arguments[3];
  var ownDescriptor = getOwnPropertyDescriptorModule.f(anObject(target), propertyKey);
  var existingDescriptor, prototype;

  if (!ownDescriptor) {
    if (isObject(prototype = getPrototypeOf(target))) {
      return set(prototype, propertyKey, V, receiver);
    }

    ownDescriptor = createPropertyDescriptor(0);
  }

  if (has(ownDescriptor, 'value')) {
    if (ownDescriptor.writable === false || !isObject(receiver)) return false;

    if (existingDescriptor = getOwnPropertyDescriptorModule.f(receiver, propertyKey)) {
      if (existingDescriptor.get || existingDescriptor.set || existingDescriptor.writable === false) return false;
      existingDescriptor.value = V;
      definePropertyModule.f(receiver, propertyKey, existingDescriptor);
    } else definePropertyModule.f(receiver, propertyKey, createPropertyDescriptor(0, V));

    return true;
  }

  return ownDescriptor.set === undefined ? false : (ownDescriptor.set.call(receiver, V), true);
} // MS Edge 17-18 Reflect.set allows setting the property to object
// with non-writable property on the prototype


var MS_EDGE_BUG = fails(function () {
  var Constructor = function Constructor() {
    /* empty */
  };

  var object = definePropertyModule.f(new Constructor(), 'a', {
    configurable: true
  }); // eslint-disable-next-line es/no-reflect -- required for testing

  return Reflect.set(Constructor.prototype, 'a', 1, object) !== false;
});
$({
  target: 'Reflect',
  stat: true,
  forced: MS_EDGE_BUG
}, {
  set: set
});

/***/ }),

/***/ 45090:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var DESCRIPTORS = __webpack_require__(75592);

var global = __webpack_require__(84253);

var isForced = __webpack_require__(41441);

var inheritIfRequired = __webpack_require__(22285);

var createNonEnumerableProperty = __webpack_require__(52188);

var defineProperty = __webpack_require__(74217).f;

var getOwnPropertyNames = __webpack_require__(78870).f;

var isRegExp = __webpack_require__(86133);

var toString = __webpack_require__(92082);

var getFlags = __webpack_require__(5105);

var stickyHelpers = __webpack_require__(1864);

var redefine = __webpack_require__(11807);

var fails = __webpack_require__(69691);

var has = __webpack_require__(28919);

var enforceInternalState = __webpack_require__(79947).enforce;

var setSpecies = __webpack_require__(64157);

var wellKnownSymbol = __webpack_require__(17657);

var UNSUPPORTED_DOT_ALL = __webpack_require__(8368);

var UNSUPPORTED_NCG = __webpack_require__(2229);

var MATCH = wellKnownSymbol('match');
var NativeRegExp = global.RegExp;
var RegExpPrototype = NativeRegExp.prototype; // TODO: Use only propper RegExpIdentifierName

var IS_NCG = /^\?<[^\s\d!#%&*+<=>@^][^\s!#%&*+<=>@^]*>/;
var re1 = /a/g;
var re2 = /a/g; // "new" should create a new object, old webkit bug

var CORRECT_NEW = new NativeRegExp(re1) !== re1;
var UNSUPPORTED_Y = stickyHelpers.UNSUPPORTED_Y;
var BASE_FORCED = DESCRIPTORS && (!CORRECT_NEW || UNSUPPORTED_Y || UNSUPPORTED_DOT_ALL || UNSUPPORTED_NCG || fails(function () {
  re2[MATCH] = false; // RegExp constructor can alter flags and IsRegExp works correct with @@match

  return NativeRegExp(re1) != re1 || NativeRegExp(re2) == re2 || NativeRegExp(re1, 'i') != '/a/i';
}));

var handleDotAll = function handleDotAll(string) {
  var length = string.length;
  var index = 0;
  var result = '';
  var brackets = false;
  var chr;

  for (; index <= length; index++) {
    chr = string.charAt(index);

    if (chr === '\\') {
      result += chr + string.charAt(++index);
      continue;
    }

    if (!brackets && chr === '.') {
      result += '[\\s\\S]';
    } else {
      if (chr === '[') {
        brackets = true;
      } else if (chr === ']') {
        brackets = false;
      }

      result += chr;
    }
  }

  return result;
};

var handleNCG = function handleNCG(string) {
  var length = string.length;
  var index = 0;
  var result = '';
  var named = [];
  var names = {};
  var brackets = false;
  var ncg = false;
  var groupid = 0;
  var groupname = '';
  var chr;

  for (; index <= length; index++) {
    chr = string.charAt(index);

    if (chr === '\\') {
      chr = chr + string.charAt(++index);
    } else if (chr === ']') {
      brackets = false;
    } else if (!brackets) switch (true) {
      case chr === '[':
        brackets = true;
        break;

      case chr === '(':
        if (IS_NCG.test(string.slice(index + 1))) {
          index += 2;
          ncg = true;
        }

        result += chr;
        groupid++;
        continue;

      case chr === '>' && ncg:
        if (groupname === '' || has(names, groupname)) {
          throw new SyntaxError('Invalid capture group name');
        }

        names[groupname] = true;
        named.push([groupname, groupid]);
        ncg = false;
        groupname = '';
        continue;
    }

    if (ncg) groupname += chr;else result += chr;
  }

  return [result, named];
}; // `RegExp` constructor
// https://tc39.es/ecma262/#sec-regexp-constructor


if (isForced('RegExp', BASE_FORCED)) {
  var RegExpWrapper = function () {
    function RegExp(pattern, flags) {
      var thisIsRegExp = this instanceof RegExpWrapper;
      var patternIsRegExp = isRegExp(pattern);
      var flagsAreUndefined = flags === undefined;
      var groups = [];
      var rawPattern = pattern;
      var rawFlags, dotAll, sticky, handled, result, state;

      if (!thisIsRegExp && patternIsRegExp && flagsAreUndefined && pattern.constructor === RegExpWrapper) {
        return pattern;
      }

      if (patternIsRegExp || pattern instanceof RegExpWrapper) {
        pattern = pattern.source;
        if (flagsAreUndefined) flags = 'flags' in rawPattern ? rawPattern.flags : getFlags.call(rawPattern);
      }

      pattern = pattern === undefined ? '' : toString(pattern);
      flags = flags === undefined ? '' : toString(flags);
      rawPattern = pattern;

      if (UNSUPPORTED_DOT_ALL && 'dotAll' in re1) {
        dotAll = !!flags && flags.indexOf('s') > -1;
        if (dotAll) flags = flags.replace(/s/g, '');
      }

      rawFlags = flags;

      if (UNSUPPORTED_Y && 'sticky' in re1) {
        sticky = !!flags && flags.indexOf('y') > -1;
        if (sticky) flags = flags.replace(/y/g, '');
      }

      if (UNSUPPORTED_NCG) {
        handled = handleNCG(pattern);
        pattern = handled[0];
        groups = handled[1];
      }

      result = inheritIfRequired(NativeRegExp(pattern, flags), thisIsRegExp ? this : RegExpPrototype, RegExpWrapper);

      if (dotAll || sticky || groups.length) {
        state = enforceInternalState(result);

        if (dotAll) {
          state.dotAll = true;
          state.raw = RegExpWrapper(handleDotAll(pattern), rawFlags);
        }

        if (sticky) state.sticky = true;
        if (groups.length) state.groups = groups;
      }

      if (pattern !== rawPattern) try {
        // fails in old engines, but we have no alternatives for unsupported regex syntax
        createNonEnumerableProperty(result, 'source', rawPattern === '' ? '(?:)' : rawPattern);
      } catch (error) {
        /* empty */
      }
      return result;
    }

    return RegExp;
  }();

  var proxy = function proxy(key) {
    key in RegExpWrapper || defineProperty(RegExpWrapper, key, {
      configurable: true,
      get: function () {
        function get() {
          return NativeRegExp[key];
        }

        return get;
      }(),
      set: function () {
        function set(it) {
          NativeRegExp[key] = it;
        }

        return set;
      }()
    });
  };

  for (var keys = getOwnPropertyNames(NativeRegExp), index = 0; keys.length > index;) {
    proxy(keys[index++]);
  }

  RegExpPrototype.constructor = RegExpWrapper;
  RegExpWrapper.prototype = RegExpPrototype;
  redefine(global, 'RegExp', RegExpWrapper);
} // https://tc39.es/ecma262/#sec-get-regexp-@@species


setSpecies('RegExp');

/***/ }),

/***/ 12654:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var exec = __webpack_require__(71841); // `RegExp.prototype.exec` method
// https://tc39.es/ecma262/#sec-regexp.prototype.exec


$({
  target: 'RegExp',
  proto: true,
  forced: /./.exec !== exec
}, {
  exec: exec
});

/***/ }),

/***/ 58870:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var DESCRIPTORS = __webpack_require__(75592);

var objectDefinePropertyModule = __webpack_require__(74217);

var regExpFlags = __webpack_require__(5105);

var fails = __webpack_require__(69691);

var FORCED = DESCRIPTORS && fails(function () {
  // eslint-disable-next-line es/no-object-getownpropertydescriptor -- safe
  return Object.getOwnPropertyDescriptor(RegExp.prototype, 'flags').get.call({
    dotAll: true,
    sticky: true
  }) !== 'sy';
}); // `RegExp.prototype.flags` getter
// https://tc39.es/ecma262/#sec-get-regexp.prototype.flags

if (FORCED) objectDefinePropertyModule.f(RegExp.prototype, 'flags', {
  configurable: true,
  get: regExpFlags
});

/***/ }),

/***/ 76581:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var redefine = __webpack_require__(11807);

var anObject = __webpack_require__(63518);

var $toString = __webpack_require__(92082);

var fails = __webpack_require__(69691);

var flags = __webpack_require__(5105);

var TO_STRING = 'toString';
var RegExpPrototype = RegExp.prototype;
var nativeToString = RegExpPrototype[TO_STRING];
var NOT_GENERIC = fails(function () {
  return nativeToString.call({
    source: 'a',
    flags: 'b'
  }) != '/a/b';
}); // FF44- RegExp#toString has a wrong name

var INCORRECT_NAME = nativeToString.name != TO_STRING; // `RegExp.prototype.toString` method
// https://tc39.es/ecma262/#sec-regexp.prototype.tostring

if (NOT_GENERIC || INCORRECT_NAME) {
  redefine(RegExp.prototype, TO_STRING, function () {
    function toString() {
      var R = anObject(this);
      var p = $toString(R.source);
      var rf = R.flags;
      var f = $toString(rf === undefined && R instanceof RegExp && !('flags' in RegExpPrototype) ? flags.call(R) : rf);
      return '/' + p + '/' + f;
    }

    return toString;
  }(), {
    unsafe: true
  });
}

/***/ }),

/***/ 4567:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var collection = __webpack_require__(42006);

var collectionStrong = __webpack_require__(74568); // `Set` constructor
// https://tc39.es/ecma262/#sec-set-objects


module.exports = collection('Set', function (init) {
  return function () {
    function Set() {
      return init(this, arguments.length ? arguments[0] : undefined);
    }

    return Set;
  }();
}, collectionStrong);

/***/ }),

/***/ 67586:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var createHTML = __webpack_require__(34141);

var forcedStringHTMLMethod = __webpack_require__(26096); // `String.prototype.anchor` method
// https://tc39.es/ecma262/#sec-string.prototype.anchor


$({
  target: 'String',
  proto: true,
  forced: forcedStringHTMLMethod('anchor')
}, {
  anchor: function () {
    function anchor(name) {
      return createHTML(this, 'a', 'name', name);
    }

    return anchor;
  }()
});

/***/ }),

/***/ 7743:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var createHTML = __webpack_require__(34141);

var forcedStringHTMLMethod = __webpack_require__(26096); // `String.prototype.big` method
// https://tc39.es/ecma262/#sec-string.prototype.big


$({
  target: 'String',
  proto: true,
  forced: forcedStringHTMLMethod('big')
}, {
  big: function () {
    function big() {
      return createHTML(this, 'big', '', '');
    }

    return big;
  }()
});

/***/ }),

/***/ 15890:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var createHTML = __webpack_require__(34141);

var forcedStringHTMLMethod = __webpack_require__(26096); // `String.prototype.blink` method
// https://tc39.es/ecma262/#sec-string.prototype.blink


$({
  target: 'String',
  proto: true,
  forced: forcedStringHTMLMethod('blink')
}, {
  blink: function () {
    function blink() {
      return createHTML(this, 'blink', '', '');
    }

    return blink;
  }()
});

/***/ }),

/***/ 34651:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var createHTML = __webpack_require__(34141);

var forcedStringHTMLMethod = __webpack_require__(26096); // `String.prototype.bold` method
// https://tc39.es/ecma262/#sec-string.prototype.bold


$({
  target: 'String',
  proto: true,
  forced: forcedStringHTMLMethod('bold')
}, {
  bold: function () {
    function bold() {
      return createHTML(this, 'b', '', '');
    }

    return bold;
  }()
});

/***/ }),

/***/ 28638:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var codeAt = __webpack_require__(37015).codeAt; // `String.prototype.codePointAt` method
// https://tc39.es/ecma262/#sec-string.prototype.codepointat


$({
  target: 'String',
  proto: true
}, {
  codePointAt: function () {
    function codePointAt(pos) {
      return codeAt(this, pos);
    }

    return codePointAt;
  }()
});

/***/ }),

/***/ 60314:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var getOwnPropertyDescriptor = __webpack_require__(22245).f;

var toLength = __webpack_require__(39570);

var toString = __webpack_require__(92082);

var notARegExp = __webpack_require__(18774);

var requireObjectCoercible = __webpack_require__(44184);

var correctIsRegExpLogic = __webpack_require__(93937);

var IS_PURE = __webpack_require__(1986); // eslint-disable-next-line es/no-string-prototype-endswith -- safe


var $endsWith = ''.endsWith;
var min = Math.min;
var CORRECT_IS_REGEXP_LOGIC = correctIsRegExpLogic('endsWith'); // https://github.com/zloirock/core-js/pull/702

var MDN_POLYFILL_BUG = !IS_PURE && !CORRECT_IS_REGEXP_LOGIC && !!function () {
  var descriptor = getOwnPropertyDescriptor(String.prototype, 'endsWith');
  return descriptor && !descriptor.writable;
}(); // `String.prototype.endsWith` method
// https://tc39.es/ecma262/#sec-string.prototype.endswith

$({
  target: 'String',
  proto: true,
  forced: !MDN_POLYFILL_BUG && !CORRECT_IS_REGEXP_LOGIC
}, {
  endsWith: function () {
    function endsWith(searchString
    /* , endPosition = @length */
    ) {
      var that = toString(requireObjectCoercible(this));
      notARegExp(searchString);
      var endPosition = arguments.length > 1 ? arguments[1] : undefined;
      var len = toLength(that.length);
      var end = endPosition === undefined ? len : min(toLength(endPosition), len);
      var search = toString(searchString);
      return $endsWith ? $endsWith.call(that, search, end) : that.slice(end - search.length, end) === search;
    }

    return endsWith;
  }()
});

/***/ }),

/***/ 18037:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var createHTML = __webpack_require__(34141);

var forcedStringHTMLMethod = __webpack_require__(26096); // `String.prototype.fixed` method
// https://tc39.es/ecma262/#sec-string.prototype.fixed


$({
  target: 'String',
  proto: true,
  forced: forcedStringHTMLMethod('fixed')
}, {
  fixed: function () {
    function fixed() {
      return createHTML(this, 'tt', '', '');
    }

    return fixed;
  }()
});

/***/ }),

/***/ 48415:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var createHTML = __webpack_require__(34141);

var forcedStringHTMLMethod = __webpack_require__(26096); // `String.prototype.fontcolor` method
// https://tc39.es/ecma262/#sec-string.prototype.fontcolor


$({
  target: 'String',
  proto: true,
  forced: forcedStringHTMLMethod('fontcolor')
}, {
  fontcolor: function () {
    function fontcolor(color) {
      return createHTML(this, 'font', 'color', color);
    }

    return fontcolor;
  }()
});

/***/ }),

/***/ 54950:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var createHTML = __webpack_require__(34141);

var forcedStringHTMLMethod = __webpack_require__(26096); // `String.prototype.fontsize` method
// https://tc39.es/ecma262/#sec-string.prototype.fontsize


$({
  target: 'String',
  proto: true,
  forced: forcedStringHTMLMethod('fontsize')
}, {
  fontsize: function () {
    function fontsize(size) {
      return createHTML(this, 'font', 'size', size);
    }

    return fontsize;
  }()
});

/***/ }),

/***/ 71595:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var toAbsoluteIndex = __webpack_require__(55205);

var fromCharCode = String.fromCharCode; // eslint-disable-next-line es/no-string-fromcodepoint -- required for testing

var $fromCodePoint = String.fromCodePoint; // length should be 1, old FF problem

var INCORRECT_LENGTH = !!$fromCodePoint && $fromCodePoint.length != 1; // `String.fromCodePoint` method
// https://tc39.es/ecma262/#sec-string.fromcodepoint

$({
  target: 'String',
  stat: true,
  forced: INCORRECT_LENGTH
}, {
  // eslint-disable-next-line no-unused-vars -- required for `.length`
  fromCodePoint: function () {
    function fromCodePoint(x) {
      var elements = [];
      var length = arguments.length;
      var i = 0;
      var code;

      while (length > i) {
        code = +arguments[i++];
        if (toAbsoluteIndex(code, 0x10FFFF) !== code) throw RangeError(code + ' is not a valid code point');
        elements.push(code < 0x10000 ? fromCharCode(code) : fromCharCode(((code -= 0x10000) >> 10) + 0xD800, code % 0x400 + 0xDC00));
      }

      return elements.join('');
    }

    return fromCodePoint;
  }()
});

/***/ }),

/***/ 98954:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var notARegExp = __webpack_require__(18774);

var requireObjectCoercible = __webpack_require__(44184);

var toString = __webpack_require__(92082);

var correctIsRegExpLogic = __webpack_require__(93937); // `String.prototype.includes` method
// https://tc39.es/ecma262/#sec-string.prototype.includes


$({
  target: 'String',
  proto: true,
  forced: !correctIsRegExpLogic('includes')
}, {
  includes: function () {
    function includes(searchString
    /* , position = 0 */
    ) {
      return !!~toString(requireObjectCoercible(this)).indexOf(toString(notARegExp(searchString)), arguments.length > 1 ? arguments[1] : undefined);
    }

    return includes;
  }()
});

/***/ }),

/***/ 76102:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var createHTML = __webpack_require__(34141);

var forcedStringHTMLMethod = __webpack_require__(26096); // `String.prototype.italics` method
// https://tc39.es/ecma262/#sec-string.prototype.italics


$({
  target: 'String',
  proto: true,
  forced: forcedStringHTMLMethod('italics')
}, {
  italics: function () {
    function italics() {
      return createHTML(this, 'i', '', '');
    }

    return italics;
  }()
});

/***/ }),

/***/ 91332:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var charAt = __webpack_require__(37015).charAt;

var toString = __webpack_require__(92082);

var InternalStateModule = __webpack_require__(79947);

var defineIterator = __webpack_require__(71179);

var STRING_ITERATOR = 'String Iterator';
var setInternalState = InternalStateModule.set;
var getInternalState = InternalStateModule.getterFor(STRING_ITERATOR); // `String.prototype[@@iterator]` method
// https://tc39.es/ecma262/#sec-string.prototype-@@iterator

defineIterator(String, 'String', function (iterated) {
  setInternalState(this, {
    type: STRING_ITERATOR,
    string: toString(iterated),
    index: 0
  }); // `%StringIteratorPrototype%.next` method
  // https://tc39.es/ecma262/#sec-%stringiteratorprototype%.next
}, function () {
  function next() {
    var state = getInternalState(this);
    var string = state.string;
    var index = state.index;
    var point;
    if (index >= string.length) return {
      value: undefined,
      done: true
    };
    point = charAt(string, index);
    state.index += point.length;
    return {
      value: point,
      done: false
    };
  }

  return next;
}());

/***/ }),

/***/ 47816:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var createHTML = __webpack_require__(34141);

var forcedStringHTMLMethod = __webpack_require__(26096); // `String.prototype.link` method
// https://tc39.es/ecma262/#sec-string.prototype.link


$({
  target: 'String',
  proto: true,
  forced: forcedStringHTMLMethod('link')
}, {
  link: function () {
    function link(url) {
      return createHTML(this, 'a', 'href', url);
    }

    return link;
  }()
});

/***/ }),

/***/ 79445:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var fixRegExpWellKnownSymbolLogic = __webpack_require__(68880);

var anObject = __webpack_require__(63518);

var toLength = __webpack_require__(39570);

var toString = __webpack_require__(92082);

var requireObjectCoercible = __webpack_require__(44184);

var advanceStringIndex = __webpack_require__(69953);

var regExpExec = __webpack_require__(5641); // @@match logic


fixRegExpWellKnownSymbolLogic('match', function (MATCH, nativeMatch, maybeCallNative) {
  return [// `String.prototype.match` method
  // https://tc39.es/ecma262/#sec-string.prototype.match
  function () {
    function match(regexp) {
      var O = requireObjectCoercible(this);
      var matcher = regexp == undefined ? undefined : regexp[MATCH];
      return matcher !== undefined ? matcher.call(regexp, O) : new RegExp(regexp)[MATCH](toString(O));
    }

    return match;
  }(), // `RegExp.prototype[@@match]` method
  // https://tc39.es/ecma262/#sec-regexp.prototype-@@match
  function (string) {
    var rx = anObject(this);
    var S = toString(string);
    var res = maybeCallNative(nativeMatch, rx, S);
    if (res.done) return res.value;
    if (!rx.global) return regExpExec(rx, S);
    var fullUnicode = rx.unicode;
    rx.lastIndex = 0;
    var A = [];
    var n = 0;
    var result;

    while ((result = regExpExec(rx, S)) !== null) {
      var matchStr = toString(result[0]);
      A[n] = matchStr;
      if (matchStr === '') rx.lastIndex = advanceStringIndex(S, toLength(rx.lastIndex), fullUnicode);
      n++;
    }

    return n === 0 ? null : A;
  }];
});

/***/ }),

/***/ 36493:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var $padEnd = __webpack_require__(47497).end;

var WEBKIT_BUG = __webpack_require__(43331); // `String.prototype.padEnd` method
// https://tc39.es/ecma262/#sec-string.prototype.padend


$({
  target: 'String',
  proto: true,
  forced: WEBKIT_BUG
}, {
  padEnd: function () {
    function padEnd(maxLength
    /* , fillString = ' ' */
    ) {
      return $padEnd(this, maxLength, arguments.length > 1 ? arguments[1] : undefined);
    }

    return padEnd;
  }()
});

/***/ }),

/***/ 24476:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var $padStart = __webpack_require__(47497).start;

var WEBKIT_BUG = __webpack_require__(43331); // `String.prototype.padStart` method
// https://tc39.es/ecma262/#sec-string.prototype.padstart


$({
  target: 'String',
  proto: true,
  forced: WEBKIT_BUG
}, {
  padStart: function () {
    function padStart(maxLength
    /* , fillString = ' ' */
    ) {
      return $padStart(this, maxLength, arguments.length > 1 ? arguments[1] : undefined);
    }

    return padStart;
  }()
});

/***/ }),

/***/ 78192:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var toIndexedObject = __webpack_require__(26553);

var toLength = __webpack_require__(39570);

var toString = __webpack_require__(92082); // `String.raw` method
// https://tc39.es/ecma262/#sec-string.raw


$({
  target: 'String',
  stat: true
}, {
  raw: function () {
    function raw(template) {
      var rawTemplate = toIndexedObject(template.raw);
      var literalSegments = toLength(rawTemplate.length);
      var argumentsLength = arguments.length;
      var elements = [];
      var i = 0;

      while (literalSegments > i) {
        elements.push(toString(rawTemplate[i++]));
        if (i < argumentsLength) elements.push(toString(arguments[i]));
      }

      return elements.join('');
    }

    return raw;
  }()
});

/***/ }),

/***/ 58660:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var repeat = __webpack_require__(9811); // `String.prototype.repeat` method
// https://tc39.es/ecma262/#sec-string.prototype.repeat


$({
  target: 'String',
  proto: true
}, {
  repeat: repeat
});

/***/ }),

/***/ 56592:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var fixRegExpWellKnownSymbolLogic = __webpack_require__(68880);

var fails = __webpack_require__(69691);

var anObject = __webpack_require__(63518);

var toInteger = __webpack_require__(3997);

var toLength = __webpack_require__(39570);

var toString = __webpack_require__(92082);

var requireObjectCoercible = __webpack_require__(44184);

var advanceStringIndex = __webpack_require__(69953);

var getSubstitution = __webpack_require__(55444);

var regExpExec = __webpack_require__(5641);

var wellKnownSymbol = __webpack_require__(17657);

var REPLACE = wellKnownSymbol('replace');
var max = Math.max;
var min = Math.min;

var maybeToString = function maybeToString(it) {
  return it === undefined ? it : String(it);
}; // IE <= 11 replaces $0 with the whole match, as if it was $&
// https://stackoverflow.com/questions/6024666/getting-ie-to-replace-a-regex-with-the-literal-string-0


var REPLACE_KEEPS_$0 = function () {
  // eslint-disable-next-line regexp/prefer-escape-replacement-dollar-char -- required for testing
  return 'a'.replace(/./, '$0') === '$0';
}(); // Safari <= 13.0.3(?) substitutes nth capture where n>m with an empty string


var REGEXP_REPLACE_SUBSTITUTES_UNDEFINED_CAPTURE = function () {
  if (/./[REPLACE]) {
    return /./[REPLACE]('a', '$0') === '';
  }

  return false;
}();

var REPLACE_SUPPORTS_NAMED_GROUPS = !fails(function () {
  var re = /./;

  re.exec = function () {
    var result = [];
    result.groups = {
      a: '7'
    };
    return result;
  };

  return ''.replace(re, '$<a>') !== '7';
}); // @@replace logic

fixRegExpWellKnownSymbolLogic('replace', function (_, nativeReplace, maybeCallNative) {
  var UNSAFE_SUBSTITUTE = REGEXP_REPLACE_SUBSTITUTES_UNDEFINED_CAPTURE ? '$' : '$0';
  return [// `String.prototype.replace` method
  // https://tc39.es/ecma262/#sec-string.prototype.replace
  function () {
    function replace(searchValue, replaceValue) {
      var O = requireObjectCoercible(this);
      var replacer = searchValue == undefined ? undefined : searchValue[REPLACE];
      return replacer !== undefined ? replacer.call(searchValue, O, replaceValue) : nativeReplace.call(toString(O), searchValue, replaceValue);
    }

    return replace;
  }(), // `RegExp.prototype[@@replace]` method
  // https://tc39.es/ecma262/#sec-regexp.prototype-@@replace
  function (string, replaceValue) {
    var rx = anObject(this);
    var S = toString(string);

    if (typeof replaceValue === 'string' && replaceValue.indexOf(UNSAFE_SUBSTITUTE) === -1 && replaceValue.indexOf('$<') === -1) {
      var res = maybeCallNative(nativeReplace, rx, S, replaceValue);
      if (res.done) return res.value;
    }

    var functionalReplace = typeof replaceValue === 'function';
    if (!functionalReplace) replaceValue = toString(replaceValue);
    var global = rx.global;

    if (global) {
      var fullUnicode = rx.unicode;
      rx.lastIndex = 0;
    }

    var results = [];

    while (true) {
      var result = regExpExec(rx, S);
      if (result === null) break;
      results.push(result);
      if (!global) break;
      var matchStr = toString(result[0]);
      if (matchStr === '') rx.lastIndex = advanceStringIndex(S, toLength(rx.lastIndex), fullUnicode);
    }

    var accumulatedResult = '';
    var nextSourcePosition = 0;

    for (var i = 0; i < results.length; i++) {
      result = results[i];
      var matched = toString(result[0]);
      var position = max(min(toInteger(result.index), S.length), 0);
      var captures = []; // NOTE: This is equivalent to
      //   captures = result.slice(1).map(maybeToString)
      // but for some reason `nativeSlice.call(result, 1, result.length)` (called in
      // the slice polyfill when slicing native arrays) "doesn't work" in safari 9 and
      // causes a crash (https://pastebin.com/N21QzeQA) when trying to debug it.

      for (var j = 1; j < result.length; j++) {
        captures.push(maybeToString(result[j]));
      }

      var namedCaptures = result.groups;

      if (functionalReplace) {
        var replacerArgs = [matched].concat(captures, position, S);
        if (namedCaptures !== undefined) replacerArgs.push(namedCaptures);
        var replacement = toString(replaceValue.apply(undefined, replacerArgs));
      } else {
        replacement = getSubstitution(matched, S, position, captures, namedCaptures, replaceValue);
      }

      if (position >= nextSourcePosition) {
        accumulatedResult += S.slice(nextSourcePosition, position) + replacement;
        nextSourcePosition = position + matched.length;
      }
    }

    return accumulatedResult + S.slice(nextSourcePosition);
  }];
}, !REPLACE_SUPPORTS_NAMED_GROUPS || !REPLACE_KEEPS_$0 || REGEXP_REPLACE_SUBSTITUTES_UNDEFINED_CAPTURE);

/***/ }),

/***/ 69957:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var fixRegExpWellKnownSymbolLogic = __webpack_require__(68880);

var anObject = __webpack_require__(63518);

var requireObjectCoercible = __webpack_require__(44184);

var sameValue = __webpack_require__(61398);

var toString = __webpack_require__(92082);

var regExpExec = __webpack_require__(5641); // @@search logic


fixRegExpWellKnownSymbolLogic('search', function (SEARCH, nativeSearch, maybeCallNative) {
  return [// `String.prototype.search` method
  // https://tc39.es/ecma262/#sec-string.prototype.search
  function () {
    function search(regexp) {
      var O = requireObjectCoercible(this);
      var searcher = regexp == undefined ? undefined : regexp[SEARCH];
      return searcher !== undefined ? searcher.call(regexp, O) : new RegExp(regexp)[SEARCH](toString(O));
    }

    return search;
  }(), // `RegExp.prototype[@@search]` method
  // https://tc39.es/ecma262/#sec-regexp.prototype-@@search
  function (string) {
    var rx = anObject(this);
    var S = toString(string);
    var res = maybeCallNative(nativeSearch, rx, S);
    if (res.done) return res.value;
    var previousLastIndex = rx.lastIndex;
    if (!sameValue(previousLastIndex, 0)) rx.lastIndex = 0;
    var result = regExpExec(rx, S);
    if (!sameValue(rx.lastIndex, previousLastIndex)) rx.lastIndex = previousLastIndex;
    return result === null ? -1 : result.index;
  }];
});

/***/ }),

/***/ 30037:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var createHTML = __webpack_require__(34141);

var forcedStringHTMLMethod = __webpack_require__(26096); // `String.prototype.small` method
// https://tc39.es/ecma262/#sec-string.prototype.small


$({
  target: 'String',
  proto: true,
  forced: forcedStringHTMLMethod('small')
}, {
  small: function () {
    function small() {
      return createHTML(this, 'small', '', '');
    }

    return small;
  }()
});

/***/ }),

/***/ 9747:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var fixRegExpWellKnownSymbolLogic = __webpack_require__(68880);

var isRegExp = __webpack_require__(86133);

var anObject = __webpack_require__(63518);

var requireObjectCoercible = __webpack_require__(44184);

var speciesConstructor = __webpack_require__(24345);

var advanceStringIndex = __webpack_require__(69953);

var toLength = __webpack_require__(39570);

var toString = __webpack_require__(92082);

var callRegExpExec = __webpack_require__(5641);

var regexpExec = __webpack_require__(71841);

var stickyHelpers = __webpack_require__(1864);

var fails = __webpack_require__(69691);

var UNSUPPORTED_Y = stickyHelpers.UNSUPPORTED_Y;
var arrayPush = [].push;
var min = Math.min;
var MAX_UINT32 = 0xFFFFFFFF; // Chrome 51 has a buggy "split" implementation when RegExp#exec !== nativeExec
// Weex JS has frozen built-in prototypes, so use try / catch wrapper

var SPLIT_WORKS_WITH_OVERWRITTEN_EXEC = !fails(function () {
  // eslint-disable-next-line regexp/no-empty-group -- required for testing
  var re = /(?:)/;
  var originalExec = re.exec;

  re.exec = function () {
    return originalExec.apply(this, arguments);
  };

  var result = 'ab'.split(re);
  return result.length !== 2 || result[0] !== 'a' || result[1] !== 'b';
}); // @@split logic

fixRegExpWellKnownSymbolLogic('split', function (SPLIT, nativeSplit, maybeCallNative) {
  var internalSplit;

  if ('abbc'.split(/(b)*/)[1] == 'c' || // eslint-disable-next-line regexp/no-empty-group -- required for testing
  'test'.split(/(?:)/, -1).length != 4 || 'ab'.split(/(?:ab)*/).length != 2 || '.'.split(/(.?)(.?)/).length != 4 || // eslint-disable-next-line regexp/no-assertion-capturing-group, regexp/no-empty-group -- required for testing
  '.'.split(/()()/).length > 1 || ''.split(/.?/).length) {
    // based on es5-shim implementation, need to rework it
    internalSplit = function internalSplit(separator, limit) {
      var string = toString(requireObjectCoercible(this));
      var lim = limit === undefined ? MAX_UINT32 : limit >>> 0;
      if (lim === 0) return [];
      if (separator === undefined) return [string]; // If `separator` is not a regex, use native split

      if (!isRegExp(separator)) {
        return nativeSplit.call(string, separator, lim);
      }

      var output = [];
      var flags = (separator.ignoreCase ? 'i' : '') + (separator.multiline ? 'm' : '') + (separator.unicode ? 'u' : '') + (separator.sticky ? 'y' : '');
      var lastLastIndex = 0; // Make `global` and avoid `lastIndex` issues by working with a copy

      var separatorCopy = new RegExp(separator.source, flags + 'g');
      var match, lastIndex, lastLength;

      while (match = regexpExec.call(separatorCopy, string)) {
        lastIndex = separatorCopy.lastIndex;

        if (lastIndex > lastLastIndex) {
          output.push(string.slice(lastLastIndex, match.index));
          if (match.length > 1 && match.index < string.length) arrayPush.apply(output, match.slice(1));
          lastLength = match[0].length;
          lastLastIndex = lastIndex;
          if (output.length >= lim) break;
        }

        if (separatorCopy.lastIndex === match.index) separatorCopy.lastIndex++; // Avoid an infinite loop
      }

      if (lastLastIndex === string.length) {
        if (lastLength || !separatorCopy.test('')) output.push('');
      } else output.push(string.slice(lastLastIndex));

      return output.length > lim ? output.slice(0, lim) : output;
    }; // Chakra, V8

  } else if ('0'.split(undefined, 0).length) {
    internalSplit = function internalSplit(separator, limit) {
      return separator === undefined && limit === 0 ? [] : nativeSplit.call(this, separator, limit);
    };
  } else internalSplit = nativeSplit;

  return [// `String.prototype.split` method
  // https://tc39.es/ecma262/#sec-string.prototype.split
  function () {
    function split(separator, limit) {
      var O = requireObjectCoercible(this);
      var splitter = separator == undefined ? undefined : separator[SPLIT];
      return splitter !== undefined ? splitter.call(separator, O, limit) : internalSplit.call(toString(O), separator, limit);
    }

    return split;
  }(), // `RegExp.prototype[@@split]` method
  // https://tc39.es/ecma262/#sec-regexp.prototype-@@split
  //
  // NOTE: This cannot be properly polyfilled in engines that don't support
  // the 'y' flag.
  function (string, limit) {
    var rx = anObject(this);
    var S = toString(string);
    var res = maybeCallNative(internalSplit, rx, S, limit, internalSplit !== nativeSplit);
    if (res.done) return res.value;
    var C = speciesConstructor(rx, RegExp);
    var unicodeMatching = rx.unicode;
    var flags = (rx.ignoreCase ? 'i' : '') + (rx.multiline ? 'm' : '') + (rx.unicode ? 'u' : '') + (UNSUPPORTED_Y ? 'g' : 'y'); // ^(? + rx + ) is needed, in combination with some S slicing, to
    // simulate the 'y' flag.

    var splitter = new C(UNSUPPORTED_Y ? '^(?:' + rx.source + ')' : rx, flags);
    var lim = limit === undefined ? MAX_UINT32 : limit >>> 0;
    if (lim === 0) return [];
    if (S.length === 0) return callRegExpExec(splitter, S) === null ? [S] : [];
    var p = 0;
    var q = 0;
    var A = [];

    while (q < S.length) {
      splitter.lastIndex = UNSUPPORTED_Y ? 0 : q;
      var z = callRegExpExec(splitter, UNSUPPORTED_Y ? S.slice(q) : S);
      var e;

      if (z === null || (e = min(toLength(splitter.lastIndex + (UNSUPPORTED_Y ? q : 0)), S.length)) === p) {
        q = advanceStringIndex(S, q, unicodeMatching);
      } else {
        A.push(S.slice(p, q));
        if (A.length === lim) return A;

        for (var i = 1; i <= z.length - 1; i++) {
          A.push(z[i]);
          if (A.length === lim) return A;
        }

        q = p = e;
      }
    }

    A.push(S.slice(p));
    return A;
  }];
}, !SPLIT_WORKS_WITH_OVERWRITTEN_EXEC, UNSUPPORTED_Y);

/***/ }),

/***/ 13578:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var getOwnPropertyDescriptor = __webpack_require__(22245).f;

var toLength = __webpack_require__(39570);

var toString = __webpack_require__(92082);

var notARegExp = __webpack_require__(18774);

var requireObjectCoercible = __webpack_require__(44184);

var correctIsRegExpLogic = __webpack_require__(93937);

var IS_PURE = __webpack_require__(1986); // eslint-disable-next-line es/no-string-prototype-startswith -- safe


var $startsWith = ''.startsWith;
var min = Math.min;
var CORRECT_IS_REGEXP_LOGIC = correctIsRegExpLogic('startsWith'); // https://github.com/zloirock/core-js/pull/702

var MDN_POLYFILL_BUG = !IS_PURE && !CORRECT_IS_REGEXP_LOGIC && !!function () {
  var descriptor = getOwnPropertyDescriptor(String.prototype, 'startsWith');
  return descriptor && !descriptor.writable;
}(); // `String.prototype.startsWith` method
// https://tc39.es/ecma262/#sec-string.prototype.startswith

$({
  target: 'String',
  proto: true,
  forced: !MDN_POLYFILL_BUG && !CORRECT_IS_REGEXP_LOGIC
}, {
  startsWith: function () {
    function startsWith(searchString
    /* , position = 0 */
    ) {
      var that = toString(requireObjectCoercible(this));
      notARegExp(searchString);
      var index = toLength(min(arguments.length > 1 ? arguments[1] : undefined, that.length));
      var search = toString(searchString);
      return $startsWith ? $startsWith.call(that, search, index) : that.slice(index, index + search.length) === search;
    }

    return startsWith;
  }()
});

/***/ }),

/***/ 54705:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var createHTML = __webpack_require__(34141);

var forcedStringHTMLMethod = __webpack_require__(26096); // `String.prototype.strike` method
// https://tc39.es/ecma262/#sec-string.prototype.strike


$({
  target: 'String',
  proto: true,
  forced: forcedStringHTMLMethod('strike')
}, {
  strike: function () {
    function strike() {
      return createHTML(this, 'strike', '', '');
    }

    return strike;
  }()
});

/***/ }),

/***/ 75831:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var createHTML = __webpack_require__(34141);

var forcedStringHTMLMethod = __webpack_require__(26096); // `String.prototype.sub` method
// https://tc39.es/ecma262/#sec-string.prototype.sub


$({
  target: 'String',
  proto: true,
  forced: forcedStringHTMLMethod('sub')
}, {
  sub: function () {
    function sub() {
      return createHTML(this, 'sub', '', '');
    }

    return sub;
  }()
});

/***/ }),

/***/ 96958:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var createHTML = __webpack_require__(34141);

var forcedStringHTMLMethod = __webpack_require__(26096); // `String.prototype.sup` method
// https://tc39.es/ecma262/#sec-string.prototype.sup


$({
  target: 'String',
  proto: true,
  forced: forcedStringHTMLMethod('sup')
}, {
  sup: function () {
    function sup() {
      return createHTML(this, 'sup', '', '');
    }

    return sup;
  }()
});

/***/ }),

/***/ 19209:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var $trimEnd = __webpack_require__(84703).end;

var forcedStringTrimMethod = __webpack_require__(3035);

var FORCED = forcedStringTrimMethod('trimEnd');
var trimEnd = FORCED ? function () {
  function trimEnd() {
    return $trimEnd(this); // eslint-disable-next-line es/no-string-prototype-trimstart-trimend -- safe
  }

  return trimEnd;
}() : ''.trimEnd; // `String.prototype.{ trimEnd, trimRight }` methods
// https://tc39.es/ecma262/#sec-string.prototype.trimend
// https://tc39.es/ecma262/#String.prototype.trimright

$({
  target: 'String',
  proto: true,
  forced: FORCED
}, {
  trimEnd: trimEnd,
  trimRight: trimEnd
});

/***/ }),

/***/ 56737:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var $trimStart = __webpack_require__(84703).start;

var forcedStringTrimMethod = __webpack_require__(3035);

var FORCED = forcedStringTrimMethod('trimStart');
var trimStart = FORCED ? function () {
  function trimStart() {
    return $trimStart(this); // eslint-disable-next-line es/no-string-prototype-trimstart-trimend -- safe
  }

  return trimStart;
}() : ''.trimStart; // `String.prototype.{ trimStart, trimLeft }` methods
// https://tc39.es/ecma262/#sec-string.prototype.trimstart
// https://tc39.es/ecma262/#String.prototype.trimleft

$({
  target: 'String',
  proto: true,
  forced: FORCED
}, {
  trimStart: trimStart,
  trimLeft: trimStart
});

/***/ }),

/***/ 3684:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var $trim = __webpack_require__(84703).trim;

var forcedStringTrimMethod = __webpack_require__(3035); // `String.prototype.trim` method
// https://tc39.es/ecma262/#sec-string.prototype.trim


$({
  target: 'String',
  proto: true,
  forced: forcedStringTrimMethod('trim')
}, {
  trim: function () {
    function trim() {
      return $trim(this);
    }

    return trim;
  }()
});

/***/ }),

/***/ 69656:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var defineWellKnownSymbol = __webpack_require__(55282); // `Symbol.asyncIterator` well-known symbol
// https://tc39.es/ecma262/#sec-symbol.asynciterator


defineWellKnownSymbol('asyncIterator');

/***/ }),

/***/ 43674:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";
// `Symbol.prototype.description` getter
// https://tc39.es/ecma262/#sec-symbol.prototype.description


var $ = __webpack_require__(56174);

var DESCRIPTORS = __webpack_require__(75592);

var global = __webpack_require__(84253);

var has = __webpack_require__(28919);

var isObject = __webpack_require__(35611);

var defineProperty = __webpack_require__(74217).f;

var copyConstructorProperties = __webpack_require__(49957);

var NativeSymbol = global.Symbol;

if (DESCRIPTORS && typeof NativeSymbol == 'function' && (!('description' in NativeSymbol.prototype) || // Safari 12 bug
NativeSymbol().description !== undefined)) {
  var EmptyStringDescriptionStore = {}; // wrap Symbol constructor for correct work with undefined description

  var SymbolWrapper = function () {
    function Symbol() {
      var description = arguments.length < 1 || arguments[0] === undefined ? undefined : String(arguments[0]);
      var result = this instanceof SymbolWrapper ? new NativeSymbol(description) // in Edge 13, String(Symbol(undefined)) === 'Symbol(undefined)'
      : description === undefined ? NativeSymbol() : NativeSymbol(description);
      if (description === '') EmptyStringDescriptionStore[result] = true;
      return result;
    }

    return Symbol;
  }();

  copyConstructorProperties(SymbolWrapper, NativeSymbol);
  var symbolPrototype = SymbolWrapper.prototype = NativeSymbol.prototype;
  symbolPrototype.constructor = SymbolWrapper;
  var symbolToString = symbolPrototype.toString;

  var _native = String(NativeSymbol('test')) == 'Symbol(test)';

  var regexp = /^Symbol\((.*)\)[^)]+$/;
  defineProperty(symbolPrototype, 'description', {
    configurable: true,
    get: function () {
      function description() {
        var symbol = isObject(this) ? this.valueOf() : this;
        var string = symbolToString.call(symbol);
        if (has(EmptyStringDescriptionStore, symbol)) return '';
        var desc = _native ? string.slice(7, -1) : string.replace(regexp, '$1');
        return desc === '' ? undefined : desc;
      }

      return description;
    }()
  });
  $({
    global: true,
    forced: true
  }, {
    Symbol: SymbolWrapper
  });
}

/***/ }),

/***/ 72411:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var defineWellKnownSymbol = __webpack_require__(55282); // `Symbol.hasInstance` well-known symbol
// https://tc39.es/ecma262/#sec-symbol.hasinstance


defineWellKnownSymbol('hasInstance');

/***/ }),

/***/ 24467:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var defineWellKnownSymbol = __webpack_require__(55282); // `Symbol.isConcatSpreadable` well-known symbol
// https://tc39.es/ecma262/#sec-symbol.isconcatspreadable


defineWellKnownSymbol('isConcatSpreadable');

/***/ }),

/***/ 37493:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var defineWellKnownSymbol = __webpack_require__(55282); // `Symbol.iterator` well-known symbol
// https://tc39.es/ecma262/#sec-symbol.iterator


defineWellKnownSymbol('iterator');

/***/ }),

/***/ 65379:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var global = __webpack_require__(84253);

var getBuiltIn = __webpack_require__(69808);

var IS_PURE = __webpack_require__(1986);

var DESCRIPTORS = __webpack_require__(75592);

var NATIVE_SYMBOL = __webpack_require__(54772);

var fails = __webpack_require__(69691);

var has = __webpack_require__(28919);

var isArray = __webpack_require__(89999);

var isObject = __webpack_require__(35611);

var isSymbol = __webpack_require__(78253);

var anObject = __webpack_require__(63518);

var toObject = __webpack_require__(12104);

var toIndexedObject = __webpack_require__(26553);

var toPropertyKey = __webpack_require__(14613);

var $toString = __webpack_require__(92082);

var createPropertyDescriptor = __webpack_require__(39160);

var nativeObjectCreate = __webpack_require__(83681);

var objectKeys = __webpack_require__(84547);

var getOwnPropertyNamesModule = __webpack_require__(78870);

var getOwnPropertyNamesExternal = __webpack_require__(59995);

var getOwnPropertySymbolsModule = __webpack_require__(21061);

var getOwnPropertyDescriptorModule = __webpack_require__(22245);

var definePropertyModule = __webpack_require__(74217);

var propertyIsEnumerableModule = __webpack_require__(77659);

var createNonEnumerableProperty = __webpack_require__(52188);

var redefine = __webpack_require__(11807);

var shared = __webpack_require__(27364);

var sharedKey = __webpack_require__(85152);

var hiddenKeys = __webpack_require__(90311);

var uid = __webpack_require__(41080);

var wellKnownSymbol = __webpack_require__(17657);

var wrappedWellKnownSymbolModule = __webpack_require__(3444);

var defineWellKnownSymbol = __webpack_require__(55282);

var setToStringTag = __webpack_require__(29108);

var InternalStateModule = __webpack_require__(79947);

var $forEach = __webpack_require__(69033).forEach;

var HIDDEN = sharedKey('hidden');
var SYMBOL = 'Symbol';
var PROTOTYPE = 'prototype';
var TO_PRIMITIVE = wellKnownSymbol('toPrimitive');
var setInternalState = InternalStateModule.set;
var getInternalState = InternalStateModule.getterFor(SYMBOL);
var ObjectPrototype = Object[PROTOTYPE];
var $Symbol = global.Symbol;
var $stringify = getBuiltIn('JSON', 'stringify');
var nativeGetOwnPropertyDescriptor = getOwnPropertyDescriptorModule.f;
var nativeDefineProperty = definePropertyModule.f;
var nativeGetOwnPropertyNames = getOwnPropertyNamesExternal.f;
var nativePropertyIsEnumerable = propertyIsEnumerableModule.f;
var AllSymbols = shared('symbols');
var ObjectPrototypeSymbols = shared('op-symbols');
var StringToSymbolRegistry = shared('string-to-symbol-registry');
var SymbolToStringRegistry = shared('symbol-to-string-registry');
var WellKnownSymbolsStore = shared('wks');
var QObject = global.QObject; // Don't use setters in Qt Script, https://github.com/zloirock/core-js/issues/173

var USE_SETTER = !QObject || !QObject[PROTOTYPE] || !QObject[PROTOTYPE].findChild; // fallback for old Android, https://code.google.com/p/v8/issues/detail?id=687

var setSymbolDescriptor = DESCRIPTORS && fails(function () {
  return nativeObjectCreate(nativeDefineProperty({}, 'a', {
    get: function () {
      function get() {
        return nativeDefineProperty(this, 'a', {
          value: 7
        }).a;
      }

      return get;
    }()
  })).a != 7;
}) ? function (O, P, Attributes) {
  var ObjectPrototypeDescriptor = nativeGetOwnPropertyDescriptor(ObjectPrototype, P);
  if (ObjectPrototypeDescriptor) delete ObjectPrototype[P];
  nativeDefineProperty(O, P, Attributes);

  if (ObjectPrototypeDescriptor && O !== ObjectPrototype) {
    nativeDefineProperty(ObjectPrototype, P, ObjectPrototypeDescriptor);
  }
} : nativeDefineProperty;

var wrap = function wrap(tag, description) {
  var symbol = AllSymbols[tag] = nativeObjectCreate($Symbol[PROTOTYPE]);
  setInternalState(symbol, {
    type: SYMBOL,
    tag: tag,
    description: description
  });
  if (!DESCRIPTORS) symbol.description = description;
  return symbol;
};

var $defineProperty = function () {
  function defineProperty(O, P, Attributes) {
    if (O === ObjectPrototype) $defineProperty(ObjectPrototypeSymbols, P, Attributes);
    anObject(O);
    var key = toPropertyKey(P);
    anObject(Attributes);

    if (has(AllSymbols, key)) {
      if (!Attributes.enumerable) {
        if (!has(O, HIDDEN)) nativeDefineProperty(O, HIDDEN, createPropertyDescriptor(1, {}));
        O[HIDDEN][key] = true;
      } else {
        if (has(O, HIDDEN) && O[HIDDEN][key]) O[HIDDEN][key] = false;
        Attributes = nativeObjectCreate(Attributes, {
          enumerable: createPropertyDescriptor(0, false)
        });
      }

      return setSymbolDescriptor(O, key, Attributes);
    }

    return nativeDefineProperty(O, key, Attributes);
  }

  return defineProperty;
}();

var $defineProperties = function () {
  function defineProperties(O, Properties) {
    anObject(O);
    var properties = toIndexedObject(Properties);
    var keys = objectKeys(properties).concat($getOwnPropertySymbols(properties));
    $forEach(keys, function (key) {
      if (!DESCRIPTORS || $propertyIsEnumerable.call(properties, key)) $defineProperty(O, key, properties[key]);
    });
    return O;
  }

  return defineProperties;
}();

var $create = function () {
  function create(O, Properties) {
    return Properties === undefined ? nativeObjectCreate(O) : $defineProperties(nativeObjectCreate(O), Properties);
  }

  return create;
}();

var $propertyIsEnumerable = function () {
  function propertyIsEnumerable(V) {
    var P = toPropertyKey(V);
    var enumerable = nativePropertyIsEnumerable.call(this, P);
    if (this === ObjectPrototype && has(AllSymbols, P) && !has(ObjectPrototypeSymbols, P)) return false;
    return enumerable || !has(this, P) || !has(AllSymbols, P) || has(this, HIDDEN) && this[HIDDEN][P] ? enumerable : true;
  }

  return propertyIsEnumerable;
}();

var $getOwnPropertyDescriptor = function () {
  function getOwnPropertyDescriptor(O, P) {
    var it = toIndexedObject(O);
    var key = toPropertyKey(P);
    if (it === ObjectPrototype && has(AllSymbols, key) && !has(ObjectPrototypeSymbols, key)) return;
    var descriptor = nativeGetOwnPropertyDescriptor(it, key);

    if (descriptor && has(AllSymbols, key) && !(has(it, HIDDEN) && it[HIDDEN][key])) {
      descriptor.enumerable = true;
    }

    return descriptor;
  }

  return getOwnPropertyDescriptor;
}();

var $getOwnPropertyNames = function () {
  function getOwnPropertyNames(O) {
    var names = nativeGetOwnPropertyNames(toIndexedObject(O));
    var result = [];
    $forEach(names, function (key) {
      if (!has(AllSymbols, key) && !has(hiddenKeys, key)) result.push(key);
    });
    return result;
  }

  return getOwnPropertyNames;
}();

var $getOwnPropertySymbols = function () {
  function getOwnPropertySymbols(O) {
    var IS_OBJECT_PROTOTYPE = O === ObjectPrototype;
    var names = nativeGetOwnPropertyNames(IS_OBJECT_PROTOTYPE ? ObjectPrototypeSymbols : toIndexedObject(O));
    var result = [];
    $forEach(names, function (key) {
      if (has(AllSymbols, key) && (!IS_OBJECT_PROTOTYPE || has(ObjectPrototype, key))) {
        result.push(AllSymbols[key]);
      }
    });
    return result;
  }

  return getOwnPropertySymbols;
}(); // `Symbol` constructor
// https://tc39.es/ecma262/#sec-symbol-constructor


if (!NATIVE_SYMBOL) {
  $Symbol = function () {
    function Symbol() {
      if (this instanceof $Symbol) throw TypeError('Symbol is not a constructor');
      var description = !arguments.length || arguments[0] === undefined ? undefined : $toString(arguments[0]);
      var tag = uid(description);

      var setter = function () {
        function setter(value) {
          if (this === ObjectPrototype) setter.call(ObjectPrototypeSymbols, value);
          if (has(this, HIDDEN) && has(this[HIDDEN], tag)) this[HIDDEN][tag] = false;
          setSymbolDescriptor(this, tag, createPropertyDescriptor(1, value));
        }

        return setter;
      }();

      if (DESCRIPTORS && USE_SETTER) setSymbolDescriptor(ObjectPrototype, tag, {
        configurable: true,
        set: setter
      });
      return wrap(tag, description);
    }

    return Symbol;
  }();

  redefine($Symbol[PROTOTYPE], 'toString', function () {
    function toString() {
      return getInternalState(this).tag;
    }

    return toString;
  }());
  redefine($Symbol, 'withoutSetter', function (description) {
    return wrap(uid(description), description);
  });
  propertyIsEnumerableModule.f = $propertyIsEnumerable;
  definePropertyModule.f = $defineProperty;
  getOwnPropertyDescriptorModule.f = $getOwnPropertyDescriptor;
  getOwnPropertyNamesModule.f = getOwnPropertyNamesExternal.f = $getOwnPropertyNames;
  getOwnPropertySymbolsModule.f = $getOwnPropertySymbols;

  wrappedWellKnownSymbolModule.f = function (name) {
    return wrap(wellKnownSymbol(name), name);
  };

  if (DESCRIPTORS) {
    // https://github.com/tc39/proposal-Symbol-description
    nativeDefineProperty($Symbol[PROTOTYPE], 'description', {
      configurable: true,
      get: function () {
        function description() {
          return getInternalState(this).description;
        }

        return description;
      }()
    });

    if (!IS_PURE) {
      redefine(ObjectPrototype, 'propertyIsEnumerable', $propertyIsEnumerable, {
        unsafe: true
      });
    }
  }
}

$({
  global: true,
  wrap: true,
  forced: !NATIVE_SYMBOL,
  sham: !NATIVE_SYMBOL
}, {
  Symbol: $Symbol
});
$forEach(objectKeys(WellKnownSymbolsStore), function (name) {
  defineWellKnownSymbol(name);
});
$({
  target: SYMBOL,
  stat: true,
  forced: !NATIVE_SYMBOL
}, {
  // `Symbol.for` method
  // https://tc39.es/ecma262/#sec-symbol.for
  'for': function () {
    function _for(key) {
      var string = $toString(key);
      if (has(StringToSymbolRegistry, string)) return StringToSymbolRegistry[string];
      var symbol = $Symbol(string);
      StringToSymbolRegistry[string] = symbol;
      SymbolToStringRegistry[symbol] = string;
      return symbol;
    }

    return _for;
  }(),
  // `Symbol.keyFor` method
  // https://tc39.es/ecma262/#sec-symbol.keyfor
  keyFor: function () {
    function keyFor(sym) {
      if (!isSymbol(sym)) throw TypeError(sym + ' is not a symbol');
      if (has(SymbolToStringRegistry, sym)) return SymbolToStringRegistry[sym];
    }

    return keyFor;
  }(),
  useSetter: function () {
    function useSetter() {
      USE_SETTER = true;
    }

    return useSetter;
  }(),
  useSimple: function () {
    function useSimple() {
      USE_SETTER = false;
    }

    return useSimple;
  }()
});
$({
  target: 'Object',
  stat: true,
  forced: !NATIVE_SYMBOL,
  sham: !DESCRIPTORS
}, {
  // `Object.create` method
  // https://tc39.es/ecma262/#sec-object.create
  create: $create,
  // `Object.defineProperty` method
  // https://tc39.es/ecma262/#sec-object.defineproperty
  defineProperty: $defineProperty,
  // `Object.defineProperties` method
  // https://tc39.es/ecma262/#sec-object.defineproperties
  defineProperties: $defineProperties,
  // `Object.getOwnPropertyDescriptor` method
  // https://tc39.es/ecma262/#sec-object.getownpropertydescriptors
  getOwnPropertyDescriptor: $getOwnPropertyDescriptor
});
$({
  target: 'Object',
  stat: true,
  forced: !NATIVE_SYMBOL
}, {
  // `Object.getOwnPropertyNames` method
  // https://tc39.es/ecma262/#sec-object.getownpropertynames
  getOwnPropertyNames: $getOwnPropertyNames,
  // `Object.getOwnPropertySymbols` method
  // https://tc39.es/ecma262/#sec-object.getownpropertysymbols
  getOwnPropertySymbols: $getOwnPropertySymbols
}); // Chrome 38 and 39 `Object.getOwnPropertySymbols` fails on primitives
// https://bugs.chromium.org/p/v8/issues/detail?id=3443

$({
  target: 'Object',
  stat: true,
  forced: fails(function () {
    getOwnPropertySymbolsModule.f(1);
  })
}, {
  getOwnPropertySymbols: function () {
    function getOwnPropertySymbols(it) {
      return getOwnPropertySymbolsModule.f(toObject(it));
    }

    return getOwnPropertySymbols;
  }()
}); // `JSON.stringify` method behavior with symbols
// https://tc39.es/ecma262/#sec-json.stringify

if ($stringify) {
  var FORCED_JSON_STRINGIFY = !NATIVE_SYMBOL || fails(function () {
    var symbol = $Symbol(); // MS Edge converts symbol values to JSON as {}

    return $stringify([symbol]) != '[null]' // WebKit converts symbol values to JSON as null
    || $stringify({
      a: symbol
    }) != '{}' // V8 throws on boxed symbols
    || $stringify(Object(symbol)) != '{}';
  });
  $({
    target: 'JSON',
    stat: true,
    forced: FORCED_JSON_STRINGIFY
  }, {
    // eslint-disable-next-line no-unused-vars -- required for `.length`
    stringify: function () {
      function stringify(it, replacer, space) {
        var args = [it];
        var index = 1;
        var $replacer;

        while (arguments.length > index) {
          args.push(arguments[index++]);
        }

        $replacer = replacer;
        if (!isObject(replacer) && it === undefined || isSymbol(it)) return; // IE8 returns string on undefined

        if (!isArray(replacer)) replacer = function () {
          function replacer(key, value) {
            if (typeof $replacer == 'function') value = $replacer.call(this, key, value);
            if (!isSymbol(value)) return value;
          }

          return replacer;
        }();
        args[1] = replacer;
        return $stringify.apply(null, args);
      }

      return stringify;
    }()
  });
} // `Symbol.prototype[@@toPrimitive]` method
// https://tc39.es/ecma262/#sec-symbol.prototype-@@toprimitive


if (!$Symbol[PROTOTYPE][TO_PRIMITIVE]) {
  createNonEnumerableProperty($Symbol[PROTOTYPE], TO_PRIMITIVE, $Symbol[PROTOTYPE].valueOf);
} // `Symbol.prototype[@@toStringTag]` property
// https://tc39.es/ecma262/#sec-symbol.prototype-@@tostringtag


setToStringTag($Symbol, SYMBOL);
hiddenKeys[HIDDEN] = true;

/***/ }),

/***/ 87420:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var defineWellKnownSymbol = __webpack_require__(55282); // `Symbol.match` well-known symbol
// https://tc39.es/ecma262/#sec-symbol.match


defineWellKnownSymbol('match');

/***/ }),

/***/ 9480:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var defineWellKnownSymbol = __webpack_require__(55282); // `Symbol.replace` well-known symbol
// https://tc39.es/ecma262/#sec-symbol.replace


defineWellKnownSymbol('replace');

/***/ }),

/***/ 66171:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var defineWellKnownSymbol = __webpack_require__(55282); // `Symbol.search` well-known symbol
// https://tc39.es/ecma262/#sec-symbol.search


defineWellKnownSymbol('search');

/***/ }),

/***/ 53276:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var defineWellKnownSymbol = __webpack_require__(55282); // `Symbol.species` well-known symbol
// https://tc39.es/ecma262/#sec-symbol.species


defineWellKnownSymbol('species');

/***/ }),

/***/ 71852:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var defineWellKnownSymbol = __webpack_require__(55282); // `Symbol.split` well-known symbol
// https://tc39.es/ecma262/#sec-symbol.split


defineWellKnownSymbol('split');

/***/ }),

/***/ 59045:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var defineWellKnownSymbol = __webpack_require__(55282); // `Symbol.toPrimitive` well-known symbol
// https://tc39.es/ecma262/#sec-symbol.toprimitive


defineWellKnownSymbol('toPrimitive');

/***/ }),

/***/ 70887:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var defineWellKnownSymbol = __webpack_require__(55282); // `Symbol.toStringTag` well-known symbol
// https://tc39.es/ecma262/#sec-symbol.tostringtag


defineWellKnownSymbol('toStringTag');

/***/ }),

/***/ 72906:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var defineWellKnownSymbol = __webpack_require__(55282); // `Symbol.unscopables` well-known symbol
// https://tc39.es/ecma262/#sec-symbol.unscopables


defineWellKnownSymbol('unscopables');

/***/ }),

/***/ 64346:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var ArrayBufferViewCore = __webpack_require__(19216);

var $copyWithin = __webpack_require__(59571);

var aTypedArray = ArrayBufferViewCore.aTypedArray;
var exportTypedArrayMethod = ArrayBufferViewCore.exportTypedArrayMethod; // `%TypedArray%.prototype.copyWithin` method
// https://tc39.es/ecma262/#sec-%typedarray%.prototype.copywithin

exportTypedArrayMethod('copyWithin', function () {
  function copyWithin(target, start
  /* , end */
  ) {
    return $copyWithin.call(aTypedArray(this), target, start, arguments.length > 2 ? arguments[2] : undefined);
  }

  return copyWithin;
}());

/***/ }),

/***/ 27553:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var ArrayBufferViewCore = __webpack_require__(19216);

var $every = __webpack_require__(69033).every;

var aTypedArray = ArrayBufferViewCore.aTypedArray;
var exportTypedArrayMethod = ArrayBufferViewCore.exportTypedArrayMethod; // `%TypedArray%.prototype.every` method
// https://tc39.es/ecma262/#sec-%typedarray%.prototype.every

exportTypedArrayMethod('every', function () {
  function every(callbackfn
  /* , thisArg */
  ) {
    return $every(aTypedArray(this), callbackfn, arguments.length > 1 ? arguments[1] : undefined);
  }

  return every;
}());

/***/ }),

/***/ 49007:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var ArrayBufferViewCore = __webpack_require__(19216);

var $fill = __webpack_require__(22051);

var aTypedArray = ArrayBufferViewCore.aTypedArray;
var exportTypedArrayMethod = ArrayBufferViewCore.exportTypedArrayMethod; // `%TypedArray%.prototype.fill` method
// https://tc39.es/ecma262/#sec-%typedarray%.prototype.fill
// eslint-disable-next-line no-unused-vars -- required for `.length`

exportTypedArrayMethod('fill', function () {
  function fill(value
  /* , start, end */
  ) {
    return $fill.apply(aTypedArray(this), arguments);
  }

  return fill;
}());

/***/ }),

/***/ 17621:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var ArrayBufferViewCore = __webpack_require__(19216);

var $filter = __webpack_require__(69033).filter;

var fromSpeciesAndList = __webpack_require__(16688);

var aTypedArray = ArrayBufferViewCore.aTypedArray;
var exportTypedArrayMethod = ArrayBufferViewCore.exportTypedArrayMethod; // `%TypedArray%.prototype.filter` method
// https://tc39.es/ecma262/#sec-%typedarray%.prototype.filter

exportTypedArrayMethod('filter', function () {
  function filter(callbackfn
  /* , thisArg */
  ) {
    var list = $filter(aTypedArray(this), callbackfn, arguments.length > 1 ? arguments[1] : undefined);
    return fromSpeciesAndList(this, list);
  }

  return filter;
}());

/***/ }),

/***/ 20500:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var ArrayBufferViewCore = __webpack_require__(19216);

var $findIndex = __webpack_require__(69033).findIndex;

var aTypedArray = ArrayBufferViewCore.aTypedArray;
var exportTypedArrayMethod = ArrayBufferViewCore.exportTypedArrayMethod; // `%TypedArray%.prototype.findIndex` method
// https://tc39.es/ecma262/#sec-%typedarray%.prototype.findindex

exportTypedArrayMethod('findIndex', function () {
  function findIndex(predicate
  /* , thisArg */
  ) {
    return $findIndex(aTypedArray(this), predicate, arguments.length > 1 ? arguments[1] : undefined);
  }

  return findIndex;
}());

/***/ }),

/***/ 71078:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var ArrayBufferViewCore = __webpack_require__(19216);

var $find = __webpack_require__(69033).find;

var aTypedArray = ArrayBufferViewCore.aTypedArray;
var exportTypedArrayMethod = ArrayBufferViewCore.exportTypedArrayMethod; // `%TypedArray%.prototype.find` method
// https://tc39.es/ecma262/#sec-%typedarray%.prototype.find

exportTypedArrayMethod('find', function () {
  function find(predicate
  /* , thisArg */
  ) {
    return $find(aTypedArray(this), predicate, arguments.length > 1 ? arguments[1] : undefined);
  }

  return find;
}());

/***/ }),

/***/ 75344:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var createTypedArrayConstructor = __webpack_require__(17576); // `Float32Array` constructor
// https://tc39.es/ecma262/#sec-typedarray-objects


createTypedArrayConstructor('Float32', function (init) {
  return function () {
    function Float32Array(data, byteOffset, length) {
      return init(this, data, byteOffset, length);
    }

    return Float32Array;
  }();
});

/***/ }),

/***/ 87853:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var createTypedArrayConstructor = __webpack_require__(17576); // `Float64Array` constructor
// https://tc39.es/ecma262/#sec-typedarray-objects


createTypedArrayConstructor('Float64', function (init) {
  return function () {
    function Float64Array(data, byteOffset, length) {
      return init(this, data, byteOffset, length);
    }

    return Float64Array;
  }();
});

/***/ }),

/***/ 85923:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var ArrayBufferViewCore = __webpack_require__(19216);

var $forEach = __webpack_require__(69033).forEach;

var aTypedArray = ArrayBufferViewCore.aTypedArray;
var exportTypedArrayMethod = ArrayBufferViewCore.exportTypedArrayMethod; // `%TypedArray%.prototype.forEach` method
// https://tc39.es/ecma262/#sec-%typedarray%.prototype.foreach

exportTypedArrayMethod('forEach', function () {
  function forEach(callbackfn
  /* , thisArg */
  ) {
    $forEach(aTypedArray(this), callbackfn, arguments.length > 1 ? arguments[1] : undefined);
  }

  return forEach;
}());

/***/ }),

/***/ 38956:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var TYPED_ARRAYS_CONSTRUCTORS_REQUIRES_WRAPPERS = __webpack_require__(29023);

var exportTypedArrayStaticMethod = __webpack_require__(19216).exportTypedArrayStaticMethod;

var typedArrayFrom = __webpack_require__(76182); // `%TypedArray%.from` method
// https://tc39.es/ecma262/#sec-%typedarray%.from


exportTypedArrayStaticMethod('from', typedArrayFrom, TYPED_ARRAYS_CONSTRUCTORS_REQUIRES_WRAPPERS);

/***/ }),

/***/ 49264:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var ArrayBufferViewCore = __webpack_require__(19216);

var $includes = __webpack_require__(25945).includes;

var aTypedArray = ArrayBufferViewCore.aTypedArray;
var exportTypedArrayMethod = ArrayBufferViewCore.exportTypedArrayMethod; // `%TypedArray%.prototype.includes` method
// https://tc39.es/ecma262/#sec-%typedarray%.prototype.includes

exportTypedArrayMethod('includes', function () {
  function includes(searchElement
  /* , fromIndex */
  ) {
    return $includes(aTypedArray(this), searchElement, arguments.length > 1 ? arguments[1] : undefined);
  }

  return includes;
}());

/***/ }),

/***/ 70448:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var ArrayBufferViewCore = __webpack_require__(19216);

var $indexOf = __webpack_require__(25945).indexOf;

var aTypedArray = ArrayBufferViewCore.aTypedArray;
var exportTypedArrayMethod = ArrayBufferViewCore.exportTypedArrayMethod; // `%TypedArray%.prototype.indexOf` method
// https://tc39.es/ecma262/#sec-%typedarray%.prototype.indexof

exportTypedArrayMethod('indexOf', function () {
  function indexOf(searchElement
  /* , fromIndex */
  ) {
    return $indexOf(aTypedArray(this), searchElement, arguments.length > 1 ? arguments[1] : undefined);
  }

  return indexOf;
}());

/***/ }),

/***/ 1150:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var createTypedArrayConstructor = __webpack_require__(17576); // `Int16Array` constructor
// https://tc39.es/ecma262/#sec-typedarray-objects


createTypedArrayConstructor('Int16', function (init) {
  return function () {
    function Int16Array(data, byteOffset, length) {
      return init(this, data, byteOffset, length);
    }

    return Int16Array;
  }();
});

/***/ }),

/***/ 50444:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var createTypedArrayConstructor = __webpack_require__(17576); // `Int32Array` constructor
// https://tc39.es/ecma262/#sec-typedarray-objects


createTypedArrayConstructor('Int32', function (init) {
  return function () {
    function Int32Array(data, byteOffset, length) {
      return init(this, data, byteOffset, length);
    }

    return Int32Array;
  }();
});

/***/ }),

/***/ 25865:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var createTypedArrayConstructor = __webpack_require__(17576); // `Int8Array` constructor
// https://tc39.es/ecma262/#sec-typedarray-objects


createTypedArrayConstructor('Int8', function (init) {
  return function () {
    function Int8Array(data, byteOffset, length) {
      return init(this, data, byteOffset, length);
    }

    return Int8Array;
  }();
});

/***/ }),

/***/ 5775:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var global = __webpack_require__(84253);

var ArrayBufferViewCore = __webpack_require__(19216);

var ArrayIterators = __webpack_require__(98215);

var wellKnownSymbol = __webpack_require__(17657);

var ITERATOR = wellKnownSymbol('iterator');
var Uint8Array = global.Uint8Array;
var arrayValues = ArrayIterators.values;
var arrayKeys = ArrayIterators.keys;
var arrayEntries = ArrayIterators.entries;
var aTypedArray = ArrayBufferViewCore.aTypedArray;
var exportTypedArrayMethod = ArrayBufferViewCore.exportTypedArrayMethod;
var nativeTypedArrayIterator = Uint8Array && Uint8Array.prototype[ITERATOR];
var CORRECT_ITER_NAME = !!nativeTypedArrayIterator && (nativeTypedArrayIterator.name == 'values' || nativeTypedArrayIterator.name == undefined);

var typedArrayValues = function () {
  function values() {
    return arrayValues.call(aTypedArray(this));
  }

  return values;
}(); // `%TypedArray%.prototype.entries` method
// https://tc39.es/ecma262/#sec-%typedarray%.prototype.entries


exportTypedArrayMethod('entries', function () {
  function entries() {
    return arrayEntries.call(aTypedArray(this));
  }

  return entries;
}()); // `%TypedArray%.prototype.keys` method
// https://tc39.es/ecma262/#sec-%typedarray%.prototype.keys

exportTypedArrayMethod('keys', function () {
  function keys() {
    return arrayKeys.call(aTypedArray(this));
  }

  return keys;
}()); // `%TypedArray%.prototype.values` method
// https://tc39.es/ecma262/#sec-%typedarray%.prototype.values

exportTypedArrayMethod('values', typedArrayValues, !CORRECT_ITER_NAME); // `%TypedArray%.prototype[@@iterator]` method
// https://tc39.es/ecma262/#sec-%typedarray%.prototype-@@iterator

exportTypedArrayMethod(ITERATOR, typedArrayValues, !CORRECT_ITER_NAME);

/***/ }),

/***/ 45164:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var ArrayBufferViewCore = __webpack_require__(19216);

var aTypedArray = ArrayBufferViewCore.aTypedArray;
var exportTypedArrayMethod = ArrayBufferViewCore.exportTypedArrayMethod;
var $join = [].join; // `%TypedArray%.prototype.join` method
// https://tc39.es/ecma262/#sec-%typedarray%.prototype.join
// eslint-disable-next-line no-unused-vars -- required for `.length`

exportTypedArrayMethod('join', function () {
  function join(separator) {
    return $join.apply(aTypedArray(this), arguments);
  }

  return join;
}());

/***/ }),

/***/ 83762:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var ArrayBufferViewCore = __webpack_require__(19216);

var $lastIndexOf = __webpack_require__(41183);

var aTypedArray = ArrayBufferViewCore.aTypedArray;
var exportTypedArrayMethod = ArrayBufferViewCore.exportTypedArrayMethod; // `%TypedArray%.prototype.lastIndexOf` method
// https://tc39.es/ecma262/#sec-%typedarray%.prototype.lastindexof
// eslint-disable-next-line no-unused-vars -- required for `.length`

exportTypedArrayMethod('lastIndexOf', function () {
  function lastIndexOf(searchElement
  /* , fromIndex */
  ) {
    return $lastIndexOf.apply(aTypedArray(this), arguments);
  }

  return lastIndexOf;
}());

/***/ }),

/***/ 67265:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var ArrayBufferViewCore = __webpack_require__(19216);

var $map = __webpack_require__(69033).map;

var typedArraySpeciesConstructor = __webpack_require__(25198);

var aTypedArray = ArrayBufferViewCore.aTypedArray;
var exportTypedArrayMethod = ArrayBufferViewCore.exportTypedArrayMethod; // `%TypedArray%.prototype.map` method
// https://tc39.es/ecma262/#sec-%typedarray%.prototype.map

exportTypedArrayMethod('map', function () {
  function map(mapfn
  /* , thisArg */
  ) {
    return $map(aTypedArray(this), mapfn, arguments.length > 1 ? arguments[1] : undefined, function (O, length) {
      return new (typedArraySpeciesConstructor(O))(length);
    });
  }

  return map;
}());

/***/ }),

/***/ 56058:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var ArrayBufferViewCore = __webpack_require__(19216);

var TYPED_ARRAYS_CONSTRUCTORS_REQUIRES_WRAPPERS = __webpack_require__(29023);

var aTypedArrayConstructor = ArrayBufferViewCore.aTypedArrayConstructor;
var exportTypedArrayStaticMethod = ArrayBufferViewCore.exportTypedArrayStaticMethod; // `%TypedArray%.of` method
// https://tc39.es/ecma262/#sec-%typedarray%.of

exportTypedArrayStaticMethod('of', function () {
  function of() {
    var index = 0;
    var length = arguments.length;
    var result = new (aTypedArrayConstructor(this))(length);

    while (length > index) {
      result[index] = arguments[index++];
    }

    return result;
  }

  return of;
}(), TYPED_ARRAYS_CONSTRUCTORS_REQUIRES_WRAPPERS);

/***/ }),

/***/ 23535:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var ArrayBufferViewCore = __webpack_require__(19216);

var $reduceRight = __webpack_require__(28249).right;

var aTypedArray = ArrayBufferViewCore.aTypedArray;
var exportTypedArrayMethod = ArrayBufferViewCore.exportTypedArrayMethod; // `%TypedArray%.prototype.reduceRicht` method
// https://tc39.es/ecma262/#sec-%typedarray%.prototype.reduceright

exportTypedArrayMethod('reduceRight', function () {
  function reduceRight(callbackfn
  /* , initialValue */
  ) {
    return $reduceRight(aTypedArray(this), callbackfn, arguments.length, arguments.length > 1 ? arguments[1] : undefined);
  }

  return reduceRight;
}());

/***/ }),

/***/ 87001:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var ArrayBufferViewCore = __webpack_require__(19216);

var $reduce = __webpack_require__(28249).left;

var aTypedArray = ArrayBufferViewCore.aTypedArray;
var exportTypedArrayMethod = ArrayBufferViewCore.exportTypedArrayMethod; // `%TypedArray%.prototype.reduce` method
// https://tc39.es/ecma262/#sec-%typedarray%.prototype.reduce

exportTypedArrayMethod('reduce', function () {
  function reduce(callbackfn
  /* , initialValue */
  ) {
    return $reduce(aTypedArray(this), callbackfn, arguments.length, arguments.length > 1 ? arguments[1] : undefined);
  }

  return reduce;
}());

/***/ }),

/***/ 18617:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var ArrayBufferViewCore = __webpack_require__(19216);

var aTypedArray = ArrayBufferViewCore.aTypedArray;
var exportTypedArrayMethod = ArrayBufferViewCore.exportTypedArrayMethod;
var floor = Math.floor; // `%TypedArray%.prototype.reverse` method
// https://tc39.es/ecma262/#sec-%typedarray%.prototype.reverse

exportTypedArrayMethod('reverse', function () {
  function reverse() {
    var that = this;
    var length = aTypedArray(that).length;
    var middle = floor(length / 2);
    var index = 0;
    var value;

    while (index < middle) {
      value = that[index];
      that[index++] = that[--length];
      that[length] = value;
    }

    return that;
  }

  return reverse;
}());

/***/ }),

/***/ 70595:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var ArrayBufferViewCore = __webpack_require__(19216);

var toLength = __webpack_require__(39570);

var toOffset = __webpack_require__(95740);

var toObject = __webpack_require__(12104);

var fails = __webpack_require__(69691);

var aTypedArray = ArrayBufferViewCore.aTypedArray;
var exportTypedArrayMethod = ArrayBufferViewCore.exportTypedArrayMethod;
var FORCED = fails(function () {
  // eslint-disable-next-line es/no-typed-arrays -- required for testing
  new Int8Array(1).set({});
}); // `%TypedArray%.prototype.set` method
// https://tc39.es/ecma262/#sec-%typedarray%.prototype.set

exportTypedArrayMethod('set', function () {
  function set(arrayLike
  /* , offset */
  ) {
    aTypedArray(this);
    var offset = toOffset(arguments.length > 1 ? arguments[1] : undefined, 1);
    var length = this.length;
    var src = toObject(arrayLike);
    var len = toLength(src.length);
    var index = 0;
    if (len + offset > length) throw RangeError('Wrong length');

    while (index < len) {
      this[offset + index] = src[index++];
    }
  }

  return set;
}(), FORCED);

/***/ }),

/***/ 76820:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var ArrayBufferViewCore = __webpack_require__(19216);

var typedArraySpeciesConstructor = __webpack_require__(25198);

var fails = __webpack_require__(69691);

var aTypedArray = ArrayBufferViewCore.aTypedArray;
var exportTypedArrayMethod = ArrayBufferViewCore.exportTypedArrayMethod;
var $slice = [].slice;
var FORCED = fails(function () {
  // eslint-disable-next-line es/no-typed-arrays -- required for testing
  new Int8Array(1).slice();
}); // `%TypedArray%.prototype.slice` method
// https://tc39.es/ecma262/#sec-%typedarray%.prototype.slice

exportTypedArrayMethod('slice', function () {
  function slice(start, end) {
    var list = $slice.call(aTypedArray(this), start, end);
    var C = typedArraySpeciesConstructor(this);
    var index = 0;
    var length = list.length;
    var result = new C(length);

    while (length > index) {
      result[index] = list[index++];
    }

    return result;
  }

  return slice;
}(), FORCED);

/***/ }),

/***/ 33357:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var ArrayBufferViewCore = __webpack_require__(19216);

var $some = __webpack_require__(69033).some;

var aTypedArray = ArrayBufferViewCore.aTypedArray;
var exportTypedArrayMethod = ArrayBufferViewCore.exportTypedArrayMethod; // `%TypedArray%.prototype.some` method
// https://tc39.es/ecma262/#sec-%typedarray%.prototype.some

exportTypedArrayMethod('some', function () {
  function some(callbackfn
  /* , thisArg */
  ) {
    return $some(aTypedArray(this), callbackfn, arguments.length > 1 ? arguments[1] : undefined);
  }

  return some;
}());

/***/ }),

/***/ 38215:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var ArrayBufferViewCore = __webpack_require__(19216);

var global = __webpack_require__(84253);

var fails = __webpack_require__(69691);

var aFunction = __webpack_require__(83923);

var toLength = __webpack_require__(39570);

var internalSort = __webpack_require__(58737);

var FF = __webpack_require__(20967);

var IE_OR_EDGE = __webpack_require__(85749);

var V8 = __webpack_require__(12338);

var WEBKIT = __webpack_require__(12200);

var aTypedArray = ArrayBufferViewCore.aTypedArray;
var exportTypedArrayMethod = ArrayBufferViewCore.exportTypedArrayMethod;
var Uint16Array = global.Uint16Array;
var nativeSort = Uint16Array && Uint16Array.prototype.sort; // WebKit

var ACCEPT_INCORRECT_ARGUMENTS = !!nativeSort && !fails(function () {
  var array = new Uint16Array(2);
  array.sort(null);
  array.sort({});
});
var STABLE_SORT = !!nativeSort && !fails(function () {
  // feature detection can be too slow, so check engines versions
  if (V8) return V8 < 74;
  if (FF) return FF < 67;
  if (IE_OR_EDGE) return true;
  if (WEBKIT) return WEBKIT < 602;
  var array = new Uint16Array(516);
  var expected = Array(516);
  var index, mod;

  for (index = 0; index < 516; index++) {
    mod = index % 4;
    array[index] = 515 - index;
    expected[index] = index - 2 * mod + 3;
  }

  array.sort(function (a, b) {
    return (a / 4 | 0) - (b / 4 | 0);
  });

  for (index = 0; index < 516; index++) {
    if (array[index] !== expected[index]) return true;
  }
});

var getSortCompare = function getSortCompare(comparefn) {
  return function (x, y) {
    if (comparefn !== undefined) return +comparefn(x, y) || 0; // eslint-disable-next-line no-self-compare -- NaN check

    if (y !== y) return -1; // eslint-disable-next-line no-self-compare -- NaN check

    if (x !== x) return 1;
    if (x === 0 && y === 0) return 1 / x > 0 && 1 / y < 0 ? 1 : -1;
    return x > y;
  };
}; // `%TypedArray%.prototype.sort` method
// https://tc39.es/ecma262/#sec-%typedarray%.prototype.sort


exportTypedArrayMethod('sort', function () {
  function sort(comparefn) {
    var array = this;
    if (comparefn !== undefined) aFunction(comparefn);
    if (STABLE_SORT) return nativeSort.call(array, comparefn);
    aTypedArray(array);
    var arrayLength = toLength(array.length);
    var items = Array(arrayLength);
    var index;

    for (index = 0; index < arrayLength; index++) {
      items[index] = array[index];
    }

    items = internalSort(array, getSortCompare(comparefn));

    for (index = 0; index < arrayLength; index++) {
      array[index] = items[index];
    }

    return array;
  }

  return sort;
}(), !STABLE_SORT || ACCEPT_INCORRECT_ARGUMENTS);

/***/ }),

/***/ 37281:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var ArrayBufferViewCore = __webpack_require__(19216);

var toLength = __webpack_require__(39570);

var toAbsoluteIndex = __webpack_require__(55205);

var typedArraySpeciesConstructor = __webpack_require__(25198);

var aTypedArray = ArrayBufferViewCore.aTypedArray;
var exportTypedArrayMethod = ArrayBufferViewCore.exportTypedArrayMethod; // `%TypedArray%.prototype.subarray` method
// https://tc39.es/ecma262/#sec-%typedarray%.prototype.subarray

exportTypedArrayMethod('subarray', function () {
  function subarray(begin, end) {
    var O = aTypedArray(this);
    var length = O.length;
    var beginIndex = toAbsoluteIndex(begin, length);
    var C = typedArraySpeciesConstructor(O);
    return new C(O.buffer, O.byteOffset + beginIndex * O.BYTES_PER_ELEMENT, toLength((end === undefined ? length : toAbsoluteIndex(end, length)) - beginIndex));
  }

  return subarray;
}());

/***/ }),

/***/ 99584:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var global = __webpack_require__(84253);

var ArrayBufferViewCore = __webpack_require__(19216);

var fails = __webpack_require__(69691);

var Int8Array = global.Int8Array;
var aTypedArray = ArrayBufferViewCore.aTypedArray;
var exportTypedArrayMethod = ArrayBufferViewCore.exportTypedArrayMethod;
var $toLocaleString = [].toLocaleString;
var $slice = [].slice; // iOS Safari 6.x fails here

var TO_LOCALE_STRING_BUG = !!Int8Array && fails(function () {
  $toLocaleString.call(new Int8Array(1));
});
var FORCED = fails(function () {
  return [1, 2].toLocaleString() != new Int8Array([1, 2]).toLocaleString();
}) || !fails(function () {
  Int8Array.prototype.toLocaleString.call([1, 2]);
}); // `%TypedArray%.prototype.toLocaleString` method
// https://tc39.es/ecma262/#sec-%typedarray%.prototype.tolocalestring

exportTypedArrayMethod('toLocaleString', function () {
  function toLocaleString() {
    return $toLocaleString.apply(TO_LOCALE_STRING_BUG ? $slice.call(aTypedArray(this)) : aTypedArray(this), arguments);
  }

  return toLocaleString;
}(), FORCED);

/***/ }),

/***/ 43582:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var exportTypedArrayMethod = __webpack_require__(19216).exportTypedArrayMethod;

var fails = __webpack_require__(69691);

var global = __webpack_require__(84253);

var Uint8Array = global.Uint8Array;
var Uint8ArrayPrototype = Uint8Array && Uint8Array.prototype || {};
var arrayToString = [].toString;
var arrayJoin = [].join;

if (fails(function () {
  arrayToString.call({});
})) {
  arrayToString = function () {
    function toString() {
      return arrayJoin.call(this);
    }

    return toString;
  }();
}

var IS_NOT_ARRAY_METHOD = Uint8ArrayPrototype.toString != arrayToString; // `%TypedArray%.prototype.toString` method
// https://tc39.es/ecma262/#sec-%typedarray%.prototype.tostring

exportTypedArrayMethod('toString', arrayToString, IS_NOT_ARRAY_METHOD);

/***/ }),

/***/ 37650:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var createTypedArrayConstructor = __webpack_require__(17576); // `Uint16Array` constructor
// https://tc39.es/ecma262/#sec-typedarray-objects


createTypedArrayConstructor('Uint16', function (init) {
  return function () {
    function Uint16Array(data, byteOffset, length) {
      return init(this, data, byteOffset, length);
    }

    return Uint16Array;
  }();
});

/***/ }),

/***/ 12424:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var createTypedArrayConstructor = __webpack_require__(17576); // `Uint32Array` constructor
// https://tc39.es/ecma262/#sec-typedarray-objects


createTypedArrayConstructor('Uint32', function (init) {
  return function () {
    function Uint32Array(data, byteOffset, length) {
      return init(this, data, byteOffset, length);
    }

    return Uint32Array;
  }();
});

/***/ }),

/***/ 11220:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var createTypedArrayConstructor = __webpack_require__(17576); // `Uint8Array` constructor
// https://tc39.es/ecma262/#sec-typedarray-objects


createTypedArrayConstructor('Uint8', function (init) {
  return function () {
    function Uint8Array(data, byteOffset, length) {
      return init(this, data, byteOffset, length);
    }

    return Uint8Array;
  }();
});

/***/ }),

/***/ 6636:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var createTypedArrayConstructor = __webpack_require__(17576); // `Uint8ClampedArray` constructor
// https://tc39.es/ecma262/#sec-typedarray-objects


createTypedArrayConstructor('Uint8', function (init) {
  return function () {
    function Uint8ClampedArray(data, byteOffset, length) {
      return init(this, data, byteOffset, length);
    }

    return Uint8ClampedArray;
  }();
}, true);

/***/ }),

/***/ 15378:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var global = __webpack_require__(84253);

var redefineAll = __webpack_require__(3993);

var InternalMetadataModule = __webpack_require__(40421);

var collection = __webpack_require__(42006);

var collectionWeak = __webpack_require__(57396);

var isObject = __webpack_require__(35611);

var enforceIternalState = __webpack_require__(79947).enforce;

var NATIVE_WEAK_MAP = __webpack_require__(82911);

var IS_IE11 = !global.ActiveXObject && 'ActiveXObject' in global; // eslint-disable-next-line es/no-object-isextensible -- safe

var isExtensible = Object.isExtensible;
var InternalWeakMap;

var wrapper = function wrapper(init) {
  return function () {
    function WeakMap() {
      return init(this, arguments.length ? arguments[0] : undefined);
    }

    return WeakMap;
  }();
}; // `WeakMap` constructor
// https://tc39.es/ecma262/#sec-weakmap-constructor


var $WeakMap = module.exports = collection('WeakMap', wrapper, collectionWeak); // IE11 WeakMap frozen keys fix
// We can't use feature detection because it crash some old IE builds
// https://github.com/zloirock/core-js/issues/485

if (NATIVE_WEAK_MAP && IS_IE11) {
  InternalWeakMap = collectionWeak.getConstructor(wrapper, 'WeakMap', true);
  InternalMetadataModule.enable();
  var WeakMapPrototype = $WeakMap.prototype;
  var nativeDelete = WeakMapPrototype['delete'];
  var nativeHas = WeakMapPrototype.has;
  var nativeGet = WeakMapPrototype.get;
  var nativeSet = WeakMapPrototype.set;
  redefineAll(WeakMapPrototype, {
    'delete': function () {
      function _delete(key) {
        if (isObject(key) && !isExtensible(key)) {
          var state = enforceIternalState(this);
          if (!state.frozen) state.frozen = new InternalWeakMap();
          return nativeDelete.call(this, key) || state.frozen['delete'](key);
        }

        return nativeDelete.call(this, key);
      }

      return _delete;
    }(),
    has: function () {
      function has(key) {
        if (isObject(key) && !isExtensible(key)) {
          var state = enforceIternalState(this);
          if (!state.frozen) state.frozen = new InternalWeakMap();
          return nativeHas.call(this, key) || state.frozen.has(key);
        }

        return nativeHas.call(this, key);
      }

      return has;
    }(),
    get: function () {
      function get(key) {
        if (isObject(key) && !isExtensible(key)) {
          var state = enforceIternalState(this);
          if (!state.frozen) state.frozen = new InternalWeakMap();
          return nativeHas.call(this, key) ? nativeGet.call(this, key) : state.frozen.get(key);
        }

        return nativeGet.call(this, key);
      }

      return get;
    }(),
    set: function () {
      function set(key, value) {
        if (isObject(key) && !isExtensible(key)) {
          var state = enforceIternalState(this);
          if (!state.frozen) state.frozen = new InternalWeakMap();
          nativeHas.call(this, key) ? nativeSet.call(this, key, value) : state.frozen.set(key, value);
        } else nativeSet.call(this, key, value);

        return this;
      }

      return set;
    }()
  });
}

/***/ }),

/***/ 31213:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var collection = __webpack_require__(42006);

var collectionWeak = __webpack_require__(57396); // `WeakSet` constructor
// https://tc39.es/ecma262/#sec-weakset-constructor


collection('WeakSet', function (init) {
  return function () {
    function WeakSet() {
      return init(this, arguments.length ? arguments[0] : undefined);
    }

    return WeakSet;
  }();
}, collectionWeak);

/***/ }),

/***/ 19275:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var global = __webpack_require__(84253);

var task = __webpack_require__(33453);

var FORCED = !global.setImmediate || !global.clearImmediate; // http://w3c.github.io/setImmediate/

$({
  global: true,
  bind: true,
  enumerable: true,
  forced: FORCED
}, {
  // `setImmediate` method
  // http://w3c.github.io/setImmediate/#si-setImmediate
  setImmediate: task.set,
  // `clearImmediate` method
  // http://w3c.github.io/setImmediate/#si-clearImmediate
  clearImmediate: task.clear
});

/***/ }),

/***/ 59:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var global = __webpack_require__(84253);

var microtask = __webpack_require__(80909);

var IS_NODE = __webpack_require__(41488);

var process = global.process; // `queueMicrotask` method
// https://html.spec.whatwg.org/multipage/timers-and-user-prompts.html#dom-queuemicrotask

$({
  global: true,
  enumerable: true,
  noTargetGet: true
}, {
  queueMicrotask: function () {
    function queueMicrotask(fn) {
      var domain = IS_NODE && process.domain;
      microtask(domain ? domain.bind(fn) : fn);
    }

    return queueMicrotask;
  }()
});

/***/ }),

/***/ 43849:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var $ = __webpack_require__(56174);

var global = __webpack_require__(84253);

var userAgent = __webpack_require__(72319);

var slice = [].slice;
var MSIE = /MSIE .\./.test(userAgent); // <- dirty ie9- check

var wrap = function wrap(scheduler) {
  return function (handler, timeout
  /* , ...arguments */
  ) {
    var boundArgs = arguments.length > 2;
    var args = boundArgs ? slice.call(arguments, 2) : undefined;
    return scheduler(boundArgs ? function () {
      // eslint-disable-next-line no-new-func -- spec requirement
      (typeof handler == 'function' ? handler : Function(handler)).apply(this, args);
    } : handler, timeout);
  };
}; // ie9- setTimeout & setInterval additional parameters fix
// https://html.spec.whatwg.org/multipage/timers-and-user-prompts.html#timers


$({
  global: true,
  bind: true,
  forced: MSIE
}, {
  // `setTimeout` method
  // https://html.spec.whatwg.org/multipage/timers-and-user-prompts.html#dom-settimeout
  setTimeout: wrap(global.setTimeout),
  // `setInterval` method
  // https://html.spec.whatwg.org/multipage/timers-and-user-prompts.html#dom-setinterval
  setInterval: wrap(global.setInterval)
});

/***/ }),

/***/ 42950:
/***/ (function(module) {

"use strict";


/*! @license DOMPurify 2.3.1 | (c) Cure53 and other contributors | Released under the Apache license 2.0 and Mozilla Public License 2.0 | github.com/cure53/DOMPurify/blob/2.3.1/LICENSE */
(function (global, factory) {
   true ? module.exports = factory() : 0;
})(void 0, function () {
  'use strict';

  function _toConsumableArray(arr) {
    if (Array.isArray(arr)) {
      for (var i = 0, arr2 = Array(arr.length); i < arr.length; i++) {
        arr2[i] = arr[i];
      }

      return arr2;
    } else {
      return Array.from(arr);
    }
  }

  var hasOwnProperty = Object.hasOwnProperty,
      setPrototypeOf = Object.setPrototypeOf,
      isFrozen = Object.isFrozen,
      getPrototypeOf = Object.getPrototypeOf,
      getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;
  var freeze = Object.freeze,
      seal = Object.seal,
      create = Object.create; // eslint-disable-line import/no-mutable-exports

  var _ref = typeof Reflect !== 'undefined' && Reflect,
      apply = _ref.apply,
      construct = _ref.construct;

  if (!apply) {
    apply = function () {
      function apply(fun, thisValue, args) {
        return fun.apply(thisValue, args);
      }

      return apply;
    }();
  }

  if (!freeze) {
    freeze = function () {
      function freeze(x) {
        return x;
      }

      return freeze;
    }();
  }

  if (!seal) {
    seal = function () {
      function seal(x) {
        return x;
      }

      return seal;
    }();
  }

  if (!construct) {
    construct = function () {
      function construct(Func, args) {
        return new (Function.prototype.bind.apply(Func, [null].concat(_toConsumableArray(args))))();
      }

      return construct;
    }();
  }

  var arrayForEach = unapply(Array.prototype.forEach);
  var arrayPop = unapply(Array.prototype.pop);
  var arrayPush = unapply(Array.prototype.push);
  var stringToLowerCase = unapply(String.prototype.toLowerCase);
  var stringMatch = unapply(String.prototype.match);
  var stringReplace = unapply(String.prototype.replace);
  var stringIndexOf = unapply(String.prototype.indexOf);
  var stringTrim = unapply(String.prototype.trim);
  var regExpTest = unapply(RegExp.prototype.test);
  var typeErrorCreate = unconstruct(TypeError);

  function unapply(func) {
    return function (thisArg) {
      for (var _len = arguments.length, args = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
        args[_key - 1] = arguments[_key];
      }

      return apply(func, thisArg, args);
    };
  }

  function unconstruct(func) {
    return function () {
      for (var _len2 = arguments.length, args = Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
        args[_key2] = arguments[_key2];
      }

      return construct(func, args);
    };
  }
  /* Add properties to a lookup table */


  function addToSet(set, array) {
    if (setPrototypeOf) {
      // Make 'in' and truthy checks like Boolean(set.constructor)
      // independent of any properties defined on Object.prototype.
      // Prevent prototype setters from intercepting set as a this value.
      setPrototypeOf(set, null);
    }

    var l = array.length;

    while (l--) {
      var element = array[l];

      if (typeof element === 'string') {
        var lcElement = stringToLowerCase(element);

        if (lcElement !== element) {
          // Config presets (e.g. tags.js, attrs.js) are immutable.
          if (!isFrozen(array)) {
            array[l] = lcElement;
          }

          element = lcElement;
        }
      }

      set[element] = true;
    }

    return set;
  }
  /* Shallow clone an object */


  function clone(object) {
    var newObject = create(null);
    var property = void 0;

    for (property in object) {
      if (apply(hasOwnProperty, object, [property])) {
        newObject[property] = object[property];
      }
    }

    return newObject;
  }
  /* IE10 doesn't support __lookupGetter__ so lets'
   * simulate it. It also automatically checks
   * if the prop is function or getter and behaves
   * accordingly. */


  function lookupGetter(object, prop) {
    while (object !== null) {
      var desc = getOwnPropertyDescriptor(object, prop);

      if (desc) {
        if (desc.get) {
          return unapply(desc.get);
        }

        if (typeof desc.value === 'function') {
          return unapply(desc.value);
        }
      }

      object = getPrototypeOf(object);
    }

    function fallbackValue(element) {
      return null;
    }

    return fallbackValue;
  }

  var html = freeze(['a', 'abbr', 'acronym', 'address', 'area', 'article', 'aside', 'audio', 'b', 'bdi', 'bdo', 'big', 'blink', 'blockquote', 'body', 'br', 'button', 'canvas', 'caption', 'center', 'cite', 'code', 'col', 'colgroup', 'content', 'data', 'datalist', 'dd', 'decorator', 'del', 'details', 'dfn', 'dialog', 'dir', 'div', 'dl', 'dt', 'element', 'em', 'fieldset', 'figcaption', 'figure', 'font', 'footer', 'form', 'h1', 'h2', 'h3', 'h4', 'h5', 'h6', 'head', 'header', 'hgroup', 'hr', 'html', 'i', 'img', 'input', 'ins', 'kbd', 'label', 'legend', 'li', 'main', 'map', 'mark', 'marquee', 'menu', 'menuitem', 'meter', 'nav', 'nobr', 'ol', 'optgroup', 'option', 'output', 'p', 'picture', 'pre', 'progress', 'q', 'rp', 'rt', 'ruby', 's', 'samp', 'section', 'select', 'shadow', 'small', 'source', 'spacer', 'span', 'strike', 'strong', 'style', 'sub', 'summary', 'sup', 'table', 'tbody', 'td', 'template', 'textarea', 'tfoot', 'th', 'thead', 'time', 'tr', 'track', 'tt', 'u', 'ul', 'var', 'video', 'wbr']); // SVG

  var svg = freeze(['svg', 'a', 'altglyph', 'altglyphdef', 'altglyphitem', 'animatecolor', 'animatemotion', 'animatetransform', 'circle', 'clippath', 'defs', 'desc', 'ellipse', 'filter', 'font', 'g', 'glyph', 'glyphref', 'hkern', 'image', 'line', 'lineargradient', 'marker', 'mask', 'metadata', 'mpath', 'path', 'pattern', 'polygon', 'polyline', 'radialgradient', 'rect', 'stop', 'style', 'switch', 'symbol', 'text', 'textpath', 'title', 'tref', 'tspan', 'view', 'vkern']);
  var svgFilters = freeze(['feBlend', 'feColorMatrix', 'feComponentTransfer', 'feComposite', 'feConvolveMatrix', 'feDiffuseLighting', 'feDisplacementMap', 'feDistantLight', 'feFlood', 'feFuncA', 'feFuncB', 'feFuncG', 'feFuncR', 'feGaussianBlur', 'feMerge', 'feMergeNode', 'feMorphology', 'feOffset', 'fePointLight', 'feSpecularLighting', 'feSpotLight', 'feTile', 'feTurbulence']); // List of SVG elements that are disallowed by default.
  // We still need to know them so that we can do namespace
  // checks properly in case one wants to add them to
  // allow-list.

  var svgDisallowed = freeze(['animate', 'color-profile', 'cursor', 'discard', 'fedropshadow', 'feimage', 'font-face', 'font-face-format', 'font-face-name', 'font-face-src', 'font-face-uri', 'foreignobject', 'hatch', 'hatchpath', 'mesh', 'meshgradient', 'meshpatch', 'meshrow', 'missing-glyph', 'script', 'set', 'solidcolor', 'unknown', 'use']);
  var mathMl = freeze(['math', 'menclose', 'merror', 'mfenced', 'mfrac', 'mglyph', 'mi', 'mlabeledtr', 'mmultiscripts', 'mn', 'mo', 'mover', 'mpadded', 'mphantom', 'mroot', 'mrow', 'ms', 'mspace', 'msqrt', 'mstyle', 'msub', 'msup', 'msubsup', 'mtable', 'mtd', 'mtext', 'mtr', 'munder', 'munderover']); // Similarly to SVG, we want to know all MathML elements,
  // even those that we disallow by default.

  var mathMlDisallowed = freeze(['maction', 'maligngroup', 'malignmark', 'mlongdiv', 'mscarries', 'mscarry', 'msgroup', 'mstack', 'msline', 'msrow', 'semantics', 'annotation', 'annotation-xml', 'mprescripts', 'none']);
  var text = freeze(['#text']);
  var html$1 = freeze(['accept', 'action', 'align', 'alt', 'autocapitalize', 'autocomplete', 'autopictureinpicture', 'autoplay', 'background', 'bgcolor', 'border', 'capture', 'cellpadding', 'cellspacing', 'checked', 'cite', 'class', 'clear', 'color', 'cols', 'colspan', 'controls', 'controlslist', 'coords', 'crossorigin', 'datetime', 'decoding', 'default', 'dir', 'disabled', 'disablepictureinpicture', 'disableremoteplayback', 'download', 'draggable', 'enctype', 'enterkeyhint', 'face', 'for', 'headers', 'height', 'hidden', 'high', 'href', 'hreflang', 'id', 'inputmode', 'integrity', 'ismap', 'kind', 'label', 'lang', 'list', 'loading', 'loop', 'low', 'max', 'maxlength', 'media', 'method', 'min', 'minlength', 'multiple', 'muted', 'name', 'noshade', 'novalidate', 'nowrap', 'open', 'optimum', 'pattern', 'placeholder', 'playsinline', 'poster', 'preload', 'pubdate', 'radiogroup', 'readonly', 'rel', 'required', 'rev', 'reversed', 'role', 'rows', 'rowspan', 'spellcheck', 'scope', 'selected', 'shape', 'size', 'sizes', 'span', 'srclang', 'start', 'src', 'srcset', 'step', 'style', 'summary', 'tabindex', 'title', 'translate', 'type', 'usemap', 'valign', 'value', 'width', 'xmlns', 'slot']);
  var svg$1 = freeze(['accent-height', 'accumulate', 'additive', 'alignment-baseline', 'ascent', 'attributename', 'attributetype', 'azimuth', 'basefrequency', 'baseline-shift', 'begin', 'bias', 'by', 'class', 'clip', 'clippathunits', 'clip-path', 'clip-rule', 'color', 'color-interpolation', 'color-interpolation-filters', 'color-profile', 'color-rendering', 'cx', 'cy', 'd', 'dx', 'dy', 'diffuseconstant', 'direction', 'display', 'divisor', 'dur', 'edgemode', 'elevation', 'end', 'fill', 'fill-opacity', 'fill-rule', 'filter', 'filterunits', 'flood-color', 'flood-opacity', 'font-family', 'font-size', 'font-size-adjust', 'font-stretch', 'font-style', 'font-variant', 'font-weight', 'fx', 'fy', 'g1', 'g2', 'glyph-name', 'glyphref', 'gradientunits', 'gradienttransform', 'height', 'href', 'id', 'image-rendering', 'in', 'in2', 'k', 'k1', 'k2', 'k3', 'k4', 'kerning', 'keypoints', 'keysplines', 'keytimes', 'lang', 'lengthadjust', 'letter-spacing', 'kernelmatrix', 'kernelunitlength', 'lighting-color', 'local', 'marker-end', 'marker-mid', 'marker-start', 'markerheight', 'markerunits', 'markerwidth', 'maskcontentunits', 'maskunits', 'max', 'mask', 'media', 'method', 'mode', 'min', 'name', 'numoctaves', 'offset', 'operator', 'opacity', 'order', 'orient', 'orientation', 'origin', 'overflow', 'paint-order', 'path', 'pathlength', 'patterncontentunits', 'patterntransform', 'patternunits', 'points', 'preservealpha', 'preserveaspectratio', 'primitiveunits', 'r', 'rx', 'ry', 'radius', 'refx', 'refy', 'repeatcount', 'repeatdur', 'restart', 'result', 'rotate', 'scale', 'seed', 'shape-rendering', 'specularconstant', 'specularexponent', 'spreadmethod', 'startoffset', 'stddeviation', 'stitchtiles', 'stop-color', 'stop-opacity', 'stroke-dasharray', 'stroke-dashoffset', 'stroke-linecap', 'stroke-linejoin', 'stroke-miterlimit', 'stroke-opacity', 'stroke', 'stroke-width', 'style', 'surfacescale', 'systemlanguage', 'tabindex', 'targetx', 'targety', 'transform', 'text-anchor', 'text-decoration', 'text-rendering', 'textlength', 'type', 'u1', 'u2', 'unicode', 'values', 'viewbox', 'visibility', 'version', 'vert-adv-y', 'vert-origin-x', 'vert-origin-y', 'width', 'word-spacing', 'wrap', 'writing-mode', 'xchannelselector', 'ychannelselector', 'x', 'x1', 'x2', 'xmlns', 'y', 'y1', 'y2', 'z', 'zoomandpan']);
  var mathMl$1 = freeze(['accent', 'accentunder', 'align', 'bevelled', 'close', 'columnsalign', 'columnlines', 'columnspan', 'denomalign', 'depth', 'dir', 'display', 'displaystyle', 'encoding', 'fence', 'frame', 'height', 'href', 'id', 'largeop', 'length', 'linethickness', 'lspace', 'lquote', 'mathbackground', 'mathcolor', 'mathsize', 'mathvariant', 'maxsize', 'minsize', 'movablelimits', 'notation', 'numalign', 'open', 'rowalign', 'rowlines', 'rowspacing', 'rowspan', 'rspace', 'rquote', 'scriptlevel', 'scriptminsize', 'scriptsizemultiplier', 'selection', 'separator', 'separators', 'stretchy', 'subscriptshift', 'supscriptshift', 'symmetric', 'voffset', 'width', 'xmlns']);
  var xml = freeze(['xlink:href', 'xml:id', 'xlink:title', 'xml:space', 'xmlns:xlink']); // eslint-disable-next-line unicorn/better-regex

  var MUSTACHE_EXPR = seal(/\{\{[\s\S]*|[\s\S]*\}\}/gm); // Specify template detection regex for SAFE_FOR_TEMPLATES mode

  var ERB_EXPR = seal(/<%[\s\S]*|[\s\S]*%>/gm);
  var DATA_ATTR = seal(/^data-[\-\w.\u00B7-\uFFFF]/); // eslint-disable-line no-useless-escape

  var ARIA_ATTR = seal(/^aria-[\-\w]+$/); // eslint-disable-line no-useless-escape

  var IS_ALLOWED_URI = seal(/^(?:(?:(?:f|ht)tps?|mailto|tel|callto|cid|xmpp):|[^a-z]|[a-z+.\-]+(?:[^a-z+.\-:]|$))/i // eslint-disable-line no-useless-escape
  );
  var IS_SCRIPT_OR_DATA = seal(/^(?:\w+script|data):/i);
  var ATTR_WHITESPACE = seal(/[\u0000-\u0020\u00A0\u1680\u180E\u2000-\u2029\u205F\u3000]/g // eslint-disable-line no-control-regex
  );

  var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) {
    return typeof obj;
  } : function (obj) {
    return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj;
  };

  function _toConsumableArray$1(arr) {
    if (Array.isArray(arr)) {
      for (var i = 0, arr2 = Array(arr.length); i < arr.length; i++) {
        arr2[i] = arr[i];
      }

      return arr2;
    } else {
      return Array.from(arr);
    }
  }

  var getGlobal = function () {
    function getGlobal() {
      return typeof window === 'undefined' ? null : window;
    }

    return getGlobal;
  }();
  /**
   * Creates a no-op policy for internal use only.
   * Don't export this function outside this module!
   * @param {?TrustedTypePolicyFactory} trustedTypes The policy factory.
   * @param {Document} document The document object (to determine policy name suffix)
   * @return {?TrustedTypePolicy} The policy created (or null, if Trusted Types
   * are not supported).
   */


  var _createTrustedTypesPolicy = function () {
    function _createTrustedTypesPolicy(trustedTypes, document) {
      if ((typeof trustedTypes === 'undefined' ? 'undefined' : _typeof(trustedTypes)) !== 'object' || typeof trustedTypes.createPolicy !== 'function') {
        return null;
      } // Allow the callers to control the unique policy name
      // by adding a data-tt-policy-suffix to the script element with the DOMPurify.
      // Policy creation with duplicate names throws in Trusted Types.


      var suffix = null;
      var ATTR_NAME = 'data-tt-policy-suffix';

      if (document.currentScript && document.currentScript.hasAttribute(ATTR_NAME)) {
        suffix = document.currentScript.getAttribute(ATTR_NAME);
      }

      var policyName = 'dompurify' + (suffix ? '#' + suffix : '');

      try {
        return trustedTypes.createPolicy(policyName, {
          createHTML: function () {
            function createHTML(html$$1) {
              return html$$1;
            }

            return createHTML;
          }()
        });
      } catch (_) {
        // Policy creation failed (most likely another DOMPurify script has
        // already run). Skip creating the policy, as this will only cause errors
        // if TT are enforced.
        return null;
      }
    }

    return _createTrustedTypesPolicy;
  }();

  function createDOMPurify() {
    var window = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : getGlobal();

    var DOMPurify = function () {
      function DOMPurify(root) {
        return createDOMPurify(root);
      }

      return DOMPurify;
    }();
    /**
     * Version label, exposed for easier checks
     * if DOMPurify is up to date or not
     */


    DOMPurify.version = '2.3.1';
    /**
     * Array of elements that DOMPurify removed during sanitation.
     * Empty if nothing was removed.
     */

    DOMPurify.removed = [];

    if (!window || !window.document || window.document.nodeType !== 9) {
      // Not running in a browser, provide a factory function
      // so that you can pass your own Window
      DOMPurify.isSupported = false;
      return DOMPurify;
    }

    var originalDocument = window.document;
    var document = window.document;
    var DocumentFragment = window.DocumentFragment,
        HTMLTemplateElement = window.HTMLTemplateElement,
        Node = window.Node,
        Element = window.Element,
        NodeFilter = window.NodeFilter,
        _window$NamedNodeMap = window.NamedNodeMap,
        NamedNodeMap = _window$NamedNodeMap === undefined ? window.NamedNodeMap || window.MozNamedAttrMap : _window$NamedNodeMap,
        Text = window.Text,
        Comment = window.Comment,
        DOMParser = window.DOMParser,
        trustedTypes = window.trustedTypes;
    var ElementPrototype = Element.prototype;
    var cloneNode = lookupGetter(ElementPrototype, 'cloneNode');
    var getNextSibling = lookupGetter(ElementPrototype, 'nextSibling');
    var getChildNodes = lookupGetter(ElementPrototype, 'childNodes');
    var getParentNode = lookupGetter(ElementPrototype, 'parentNode'); // As per issue #47, the web-components registry is inherited by a
    // new document created via createHTMLDocument. As per the spec
    // (http://w3c.github.io/webcomponents/spec/custom/#creating-and-passing-registries)
    // a new empty registry is used when creating a template contents owner
    // document, so we use that as our parent document to ensure nothing
    // is inherited.

    if (typeof HTMLTemplateElement === 'function') {
      var template = document.createElement('template');

      if (template.content && template.content.ownerDocument) {
        document = template.content.ownerDocument;
      }
    }

    var trustedTypesPolicy = _createTrustedTypesPolicy(trustedTypes, originalDocument);

    var emptyHTML = trustedTypesPolicy && RETURN_TRUSTED_TYPE ? trustedTypesPolicy.createHTML('') : '';
    var _document = document,
        implementation = _document.implementation,
        createNodeIterator = _document.createNodeIterator,
        createDocumentFragment = _document.createDocumentFragment,
        getElementsByTagName = _document.getElementsByTagName;
    var importNode = originalDocument.importNode;
    var documentMode = {};

    try {
      documentMode = clone(document).documentMode ? document.documentMode : {};
    } catch (_) {}

    var hooks = {};
    /**
     * Expose whether this browser supports running the full DOMPurify.
     */

    DOMPurify.isSupported = typeof getParentNode === 'function' && implementation && typeof implementation.createHTMLDocument !== 'undefined' && documentMode !== 9;
    var MUSTACHE_EXPR$$1 = MUSTACHE_EXPR,
        ERB_EXPR$$1 = ERB_EXPR,
        DATA_ATTR$$1 = DATA_ATTR,
        ARIA_ATTR$$1 = ARIA_ATTR,
        IS_SCRIPT_OR_DATA$$1 = IS_SCRIPT_OR_DATA,
        ATTR_WHITESPACE$$1 = ATTR_WHITESPACE;
    var IS_ALLOWED_URI$$1 = IS_ALLOWED_URI;
    /**
     * We consider the elements and attributes below to be safe. Ideally
     * don't add any new ones but feel free to remove unwanted ones.
     */

    /* allowed element names */

    var ALLOWED_TAGS = null;
    var DEFAULT_ALLOWED_TAGS = addToSet({}, [].concat(_toConsumableArray$1(html), _toConsumableArray$1(svg), _toConsumableArray$1(svgFilters), _toConsumableArray$1(mathMl), _toConsumableArray$1(text)));
    /* Allowed attribute names */

    var ALLOWED_ATTR = null;
    var DEFAULT_ALLOWED_ATTR = addToSet({}, [].concat(_toConsumableArray$1(html$1), _toConsumableArray$1(svg$1), _toConsumableArray$1(mathMl$1), _toConsumableArray$1(xml)));
    /* Explicitly forbidden tags (overrides ALLOWED_TAGS/ADD_TAGS) */

    var FORBID_TAGS = null;
    /* Explicitly forbidden attributes (overrides ALLOWED_ATTR/ADD_ATTR) */

    var FORBID_ATTR = null;
    /* Decide if ARIA attributes are okay */

    var ALLOW_ARIA_ATTR = true;
    /* Decide if custom data attributes are okay */

    var ALLOW_DATA_ATTR = true;
    /* Decide if unknown protocols are okay */

    var ALLOW_UNKNOWN_PROTOCOLS = false;
    /* Output should be safe for common template engines.
     * This means, DOMPurify removes data attributes, mustaches and ERB
     */

    var SAFE_FOR_TEMPLATES = false;
    /* Decide if document with <html>... should be returned */

    var WHOLE_DOCUMENT = false;
    /* Track whether config is already set on this instance of DOMPurify. */

    var SET_CONFIG = false;
    /* Decide if all elements (e.g. style, script) must be children of
     * document.body. By default, browsers might move them to document.head */

    var FORCE_BODY = false;
    /* Decide if a DOM `HTMLBodyElement` should be returned, instead of a html
     * string (or a TrustedHTML object if Trusted Types are supported).
     * If `WHOLE_DOCUMENT` is enabled a `HTMLHtmlElement` will be returned instead
     */

    var RETURN_DOM = false;
    /* Decide if a DOM `DocumentFragment` should be returned, instead of a html
     * string  (or a TrustedHTML object if Trusted Types are supported) */

    var RETURN_DOM_FRAGMENT = false;
    /* If `RETURN_DOM` or `RETURN_DOM_FRAGMENT` is enabled, decide if the returned DOM
     * `Node` is imported into the current `Document`. If this flag is not enabled the
     * `Node` will belong (its ownerDocument) to a fresh `HTMLDocument`, created by
     * DOMPurify.
     *
     * This defaults to `true` starting DOMPurify 2.2.0. Note that setting it to `false`
     * might cause XSS from attacks hidden in closed shadowroots in case the browser
     * supports Declarative Shadow: DOM https://web.dev/declarative-shadow-dom/
     */

    var RETURN_DOM_IMPORT = true;
    /* Try to return a Trusted Type object instead of a string, return a string in
     * case Trusted Types are not supported  */

    var RETURN_TRUSTED_TYPE = false;
    /* Output should be free from DOM clobbering attacks? */

    var SANITIZE_DOM = true;
    /* Keep element content when removing element? */

    var KEEP_CONTENT = true;
    /* If a `Node` is passed to sanitize(), then performs sanitization in-place instead
     * of importing it into a new Document and returning a sanitized copy */

    var IN_PLACE = false;
    /* Allow usage of profiles like html, svg and mathMl */

    var USE_PROFILES = {};
    /* Tags to ignore content of when KEEP_CONTENT is true */

    var FORBID_CONTENTS = null;
    var DEFAULT_FORBID_CONTENTS = addToSet({}, ['annotation-xml', 'audio', 'colgroup', 'desc', 'foreignobject', 'head', 'iframe', 'math', 'mi', 'mn', 'mo', 'ms', 'mtext', 'noembed', 'noframes', 'noscript', 'plaintext', 'script', 'style', 'svg', 'template', 'thead', 'title', 'video', 'xmp']);
    /* Tags that are safe for data: URIs */

    var DATA_URI_TAGS = null;
    var DEFAULT_DATA_URI_TAGS = addToSet({}, ['audio', 'video', 'img', 'source', 'image', 'track']);
    /* Attributes safe for values like "javascript:" */

    var URI_SAFE_ATTRIBUTES = null;
    var DEFAULT_URI_SAFE_ATTRIBUTES = addToSet({}, ['alt', 'class', 'for', 'id', 'label', 'name', 'pattern', 'placeholder', 'role', 'summary', 'title', 'value', 'style', 'xmlns']);
    var MATHML_NAMESPACE = 'http://www.w3.org/1998/Math/MathML';
    var SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
    var HTML_NAMESPACE = 'http://www.w3.org/1999/xhtml';
    /* Document namespace */

    var NAMESPACE = HTML_NAMESPACE;
    var IS_EMPTY_INPUT = false;
    /* Keep a reference to config to pass to hooks */

    var CONFIG = null;
    /* Ideally, do not touch anything below this line */

    /* ______________________________________________ */

    var formElement = document.createElement('form');
    /**
     * _parseConfig
     *
     * @param  {Object} cfg optional config literal
     */
    // eslint-disable-next-line complexity

    var _parseConfig = function () {
      function _parseConfig(cfg) {
        if (CONFIG && CONFIG === cfg) {
          return;
        }
        /* Shield configuration object from tampering */


        if (!cfg || (typeof cfg === 'undefined' ? 'undefined' : _typeof(cfg)) !== 'object') {
          cfg = {};
        }
        /* Shield configuration object from prototype pollution */


        cfg = clone(cfg);
        /* Set configuration parameters */

        ALLOWED_TAGS = 'ALLOWED_TAGS' in cfg ? addToSet({}, cfg.ALLOWED_TAGS) : DEFAULT_ALLOWED_TAGS;
        ALLOWED_ATTR = 'ALLOWED_ATTR' in cfg ? addToSet({}, cfg.ALLOWED_ATTR) : DEFAULT_ALLOWED_ATTR;
        URI_SAFE_ATTRIBUTES = 'ADD_URI_SAFE_ATTR' in cfg ? addToSet(clone(DEFAULT_URI_SAFE_ATTRIBUTES), cfg.ADD_URI_SAFE_ATTR) : DEFAULT_URI_SAFE_ATTRIBUTES;
        DATA_URI_TAGS = 'ADD_DATA_URI_TAGS' in cfg ? addToSet(clone(DEFAULT_DATA_URI_TAGS), cfg.ADD_DATA_URI_TAGS) : DEFAULT_DATA_URI_TAGS;
        FORBID_CONTENTS = 'FORBID_CONTENTS' in cfg ? addToSet({}, cfg.FORBID_CONTENTS) : DEFAULT_FORBID_CONTENTS;
        FORBID_TAGS = 'FORBID_TAGS' in cfg ? addToSet({}, cfg.FORBID_TAGS) : {};
        FORBID_ATTR = 'FORBID_ATTR' in cfg ? addToSet({}, cfg.FORBID_ATTR) : {};
        USE_PROFILES = 'USE_PROFILES' in cfg ? cfg.USE_PROFILES : false;
        ALLOW_ARIA_ATTR = cfg.ALLOW_ARIA_ATTR !== false; // Default true

        ALLOW_DATA_ATTR = cfg.ALLOW_DATA_ATTR !== false; // Default true

        ALLOW_UNKNOWN_PROTOCOLS = cfg.ALLOW_UNKNOWN_PROTOCOLS || false; // Default false

        SAFE_FOR_TEMPLATES = cfg.SAFE_FOR_TEMPLATES || false; // Default false

        WHOLE_DOCUMENT = cfg.WHOLE_DOCUMENT || false; // Default false

        RETURN_DOM = cfg.RETURN_DOM || false; // Default false

        RETURN_DOM_FRAGMENT = cfg.RETURN_DOM_FRAGMENT || false; // Default false

        RETURN_DOM_IMPORT = cfg.RETURN_DOM_IMPORT !== false; // Default true

        RETURN_TRUSTED_TYPE = cfg.RETURN_TRUSTED_TYPE || false; // Default false

        FORCE_BODY = cfg.FORCE_BODY || false; // Default false

        SANITIZE_DOM = cfg.SANITIZE_DOM !== false; // Default true

        KEEP_CONTENT = cfg.KEEP_CONTENT !== false; // Default true

        IN_PLACE = cfg.IN_PLACE || false; // Default false

        IS_ALLOWED_URI$$1 = cfg.ALLOWED_URI_REGEXP || IS_ALLOWED_URI$$1;
        NAMESPACE = cfg.NAMESPACE || HTML_NAMESPACE;

        if (SAFE_FOR_TEMPLATES) {
          ALLOW_DATA_ATTR = false;
        }

        if (RETURN_DOM_FRAGMENT) {
          RETURN_DOM = true;
        }
        /* Parse profile info */


        if (USE_PROFILES) {
          ALLOWED_TAGS = addToSet({}, [].concat(_toConsumableArray$1(text)));
          ALLOWED_ATTR = [];

          if (USE_PROFILES.html === true) {
            addToSet(ALLOWED_TAGS, html);
            addToSet(ALLOWED_ATTR, html$1);
          }

          if (USE_PROFILES.svg === true) {
            addToSet(ALLOWED_TAGS, svg);
            addToSet(ALLOWED_ATTR, svg$1);
            addToSet(ALLOWED_ATTR, xml);
          }

          if (USE_PROFILES.svgFilters === true) {
            addToSet(ALLOWED_TAGS, svgFilters);
            addToSet(ALLOWED_ATTR, svg$1);
            addToSet(ALLOWED_ATTR, xml);
          }

          if (USE_PROFILES.mathMl === true) {
            addToSet(ALLOWED_TAGS, mathMl);
            addToSet(ALLOWED_ATTR, mathMl$1);
            addToSet(ALLOWED_ATTR, xml);
          }
        }
        /* Merge configuration parameters */


        if (cfg.ADD_TAGS) {
          if (ALLOWED_TAGS === DEFAULT_ALLOWED_TAGS) {
            ALLOWED_TAGS = clone(ALLOWED_TAGS);
          }

          addToSet(ALLOWED_TAGS, cfg.ADD_TAGS);
        }

        if (cfg.ADD_ATTR) {
          if (ALLOWED_ATTR === DEFAULT_ALLOWED_ATTR) {
            ALLOWED_ATTR = clone(ALLOWED_ATTR);
          }

          addToSet(ALLOWED_ATTR, cfg.ADD_ATTR);
        }

        if (cfg.ADD_URI_SAFE_ATTR) {
          addToSet(URI_SAFE_ATTRIBUTES, cfg.ADD_URI_SAFE_ATTR);
        }

        if (cfg.FORBID_CONTENTS) {
          if (FORBID_CONTENTS === DEFAULT_FORBID_CONTENTS) {
            FORBID_CONTENTS = clone(FORBID_CONTENTS);
          }

          addToSet(FORBID_CONTENTS, cfg.FORBID_CONTENTS);
        }
        /* Add #text in case KEEP_CONTENT is set to true */


        if (KEEP_CONTENT) {
          ALLOWED_TAGS['#text'] = true;
        }
        /* Add html, head and body to ALLOWED_TAGS in case WHOLE_DOCUMENT is true */


        if (WHOLE_DOCUMENT) {
          addToSet(ALLOWED_TAGS, ['html', 'head', 'body']);
        }
        /* Add tbody to ALLOWED_TAGS in case tables are permitted, see #286, #365 */


        if (ALLOWED_TAGS.table) {
          addToSet(ALLOWED_TAGS, ['tbody']);
          delete FORBID_TAGS.tbody;
        } // Prevent further manipulation of configuration.
        // Not available in IE8, Safari 5, etc.


        if (freeze) {
          freeze(cfg);
        }

        CONFIG = cfg;
      }

      return _parseConfig;
    }();

    var MATHML_TEXT_INTEGRATION_POINTS = addToSet({}, ['mi', 'mo', 'mn', 'ms', 'mtext']);
    var HTML_INTEGRATION_POINTS = addToSet({}, ['foreignobject', 'desc', 'title', 'annotation-xml']);
    /* Keep track of all possible SVG and MathML tags
     * so that we can perform the namespace checks
     * correctly. */

    var ALL_SVG_TAGS = addToSet({}, svg);
    addToSet(ALL_SVG_TAGS, svgFilters);
    addToSet(ALL_SVG_TAGS, svgDisallowed);
    var ALL_MATHML_TAGS = addToSet({}, mathMl);
    addToSet(ALL_MATHML_TAGS, mathMlDisallowed);
    /**
     *
     *
     * @param  {Element} element a DOM element whose namespace is being checked
     * @returns {boolean} Return false if the element has a
     *  namespace that a spec-compliant parser would never
     *  return. Return true otherwise.
     */

    var _checkValidNamespace = function () {
      function _checkValidNamespace(element) {
        var parent = getParentNode(element); // In JSDOM, if we're inside shadow DOM, then parentNode
        // can be null. We just simulate parent in this case.

        if (!parent || !parent.tagName) {
          parent = {
            namespaceURI: HTML_NAMESPACE,
            tagName: 'template'
          };
        }

        var tagName = stringToLowerCase(element.tagName);
        var parentTagName = stringToLowerCase(parent.tagName);

        if (element.namespaceURI === SVG_NAMESPACE) {
          // The only way to switch from HTML namespace to SVG
          // is via <svg>. If it happens via any other tag, then
          // it should be killed.
          if (parent.namespaceURI === HTML_NAMESPACE) {
            return tagName === 'svg';
          } // The only way to switch from MathML to SVG is via
          // svg if parent is either <annotation-xml> or MathML
          // text integration points.


          if (parent.namespaceURI === MATHML_NAMESPACE) {
            return tagName === 'svg' && (parentTagName === 'annotation-xml' || MATHML_TEXT_INTEGRATION_POINTS[parentTagName]);
          } // We only allow elements that are defined in SVG
          // spec. All others are disallowed in SVG namespace.


          return Boolean(ALL_SVG_TAGS[tagName]);
        }

        if (element.namespaceURI === MATHML_NAMESPACE) {
          // The only way to switch from HTML namespace to MathML
          // is via <math>. If it happens via any other tag, then
          // it should be killed.
          if (parent.namespaceURI === HTML_NAMESPACE) {
            return tagName === 'math';
          } // The only way to switch from SVG to MathML is via
          // <math> and HTML integration points


          if (parent.namespaceURI === SVG_NAMESPACE) {
            return tagName === 'math' && HTML_INTEGRATION_POINTS[parentTagName];
          } // We only allow elements that are defined in MathML
          // spec. All others are disallowed in MathML namespace.


          return Boolean(ALL_MATHML_TAGS[tagName]);
        }

        if (element.namespaceURI === HTML_NAMESPACE) {
          // The only way to switch from SVG to HTML is via
          // HTML integration points, and from MathML to HTML
          // is via MathML text integration points
          if (parent.namespaceURI === SVG_NAMESPACE && !HTML_INTEGRATION_POINTS[parentTagName]) {
            return false;
          }

          if (parent.namespaceURI === MATHML_NAMESPACE && !MATHML_TEXT_INTEGRATION_POINTS[parentTagName]) {
            return false;
          } // Certain elements are allowed in both SVG and HTML
          // namespace. We need to specify them explicitly
          // so that they don't get erronously deleted from
          // HTML namespace.


          var commonSvgAndHTMLElements = addToSet({}, ['title', 'style', 'font', 'a', 'script']); // We disallow tags that are specific for MathML
          // or SVG and should never appear in HTML namespace

          return !ALL_MATHML_TAGS[tagName] && (commonSvgAndHTMLElements[tagName] || !ALL_SVG_TAGS[tagName]);
        } // The code should never reach this place (this means
        // that the element somehow got namespace that is not
        // HTML, SVG or MathML). Return false just in case.


        return false;
      }

      return _checkValidNamespace;
    }();
    /**
     * _forceRemove
     *
     * @param  {Node} node a DOM node
     */


    var _forceRemove = function () {
      function _forceRemove(node) {
        arrayPush(DOMPurify.removed, {
          element: node
        });

        try {
          // eslint-disable-next-line unicorn/prefer-dom-node-remove
          node.parentNode.removeChild(node);
        } catch (_) {
          try {
            node.outerHTML = emptyHTML;
          } catch (_) {
            node.remove();
          }
        }
      }

      return _forceRemove;
    }();
    /**
     * _removeAttribute
     *
     * @param  {String} name an Attribute name
     * @param  {Node} node a DOM node
     */


    var _removeAttribute = function () {
      function _removeAttribute(name, node) {
        try {
          arrayPush(DOMPurify.removed, {
            attribute: node.getAttributeNode(name),
            from: node
          });
        } catch (_) {
          arrayPush(DOMPurify.removed, {
            attribute: null,
            from: node
          });
        }

        node.removeAttribute(name); // We void attribute values for unremovable "is"" attributes

        if (name === 'is' && !ALLOWED_ATTR[name]) {
          if (RETURN_DOM || RETURN_DOM_FRAGMENT) {
            try {
              _forceRemove(node);
            } catch (_) {}
          } else {
            try {
              node.setAttribute(name, '');
            } catch (_) {}
          }
        }
      }

      return _removeAttribute;
    }();
    /**
     * _initDocument
     *
     * @param  {String} dirty a string of dirty markup
     * @return {Document} a DOM, filled with the dirty markup
     */


    var _initDocument = function () {
      function _initDocument(dirty) {
        /* Create a HTML document */
        var doc = void 0;
        var leadingWhitespace = void 0;

        if (FORCE_BODY) {
          dirty = '<remove></remove>' + dirty;
        } else {
          /* If FORCE_BODY isn't used, leading whitespace needs to be preserved manually */
          var matches = stringMatch(dirty, /^[\r\n\t ]+/);
          leadingWhitespace = matches && matches[0];
        }

        var dirtyPayload = trustedTypesPolicy ? trustedTypesPolicy.createHTML(dirty) : dirty;
        /*
         * Use the DOMParser API by default, fallback later if needs be
         * DOMParser not work for svg when has multiple root element.
         */

        if (NAMESPACE === HTML_NAMESPACE) {
          try {
            doc = new DOMParser().parseFromString(dirtyPayload, 'text/html');
          } catch (_) {}
        }
        /* Use createHTMLDocument in case DOMParser is not available */


        if (!doc || !doc.documentElement) {
          doc = implementation.createDocument(NAMESPACE, 'template', null);

          try {
            doc.documentElement.innerHTML = IS_EMPTY_INPUT ? '' : dirtyPayload;
          } catch (_) {// Syntax error if dirtyPayload is invalid xml
          }
        }

        var body = doc.body || doc.documentElement;

        if (dirty && leadingWhitespace) {
          body.insertBefore(document.createTextNode(leadingWhitespace), body.childNodes[0] || null);
        }
        /* Work on whole document or just its body */


        if (NAMESPACE === HTML_NAMESPACE) {
          return getElementsByTagName.call(doc, WHOLE_DOCUMENT ? 'html' : 'body')[0];
        }

        return WHOLE_DOCUMENT ? doc.documentElement : body;
      }

      return _initDocument;
    }();
    /**
     * _createIterator
     *
     * @param  {Document} root document/fragment to create iterator for
     * @return {Iterator} iterator instance
     */


    var _createIterator = function () {
      function _createIterator(root) {
        return createNodeIterator.call(root.ownerDocument || root, root, NodeFilter.SHOW_ELEMENT | NodeFilter.SHOW_COMMENT | NodeFilter.SHOW_TEXT, null, false);
      }

      return _createIterator;
    }();
    /**
     * _isClobbered
     *
     * @param  {Node} elm element to check for clobbering attacks
     * @return {Boolean} true if clobbered, false if safe
     */


    var _isClobbered = function () {
      function _isClobbered(elm) {
        if (elm instanceof Text || elm instanceof Comment) {
          return false;
        }

        if (typeof elm.nodeName !== 'string' || typeof elm.textContent !== 'string' || typeof elm.removeChild !== 'function' || !(elm.attributes instanceof NamedNodeMap) || typeof elm.removeAttribute !== 'function' || typeof elm.setAttribute !== 'function' || typeof elm.namespaceURI !== 'string' || typeof elm.insertBefore !== 'function') {
          return true;
        }

        return false;
      }

      return _isClobbered;
    }();
    /**
     * _isNode
     *
     * @param  {Node} obj object to check whether it's a DOM node
     * @return {Boolean} true is object is a DOM node
     */


    var _isNode = function () {
      function _isNode(object) {
        return (typeof Node === 'undefined' ? 'undefined' : _typeof(Node)) === 'object' ? object instanceof Node : object && (typeof object === 'undefined' ? 'undefined' : _typeof(object)) === 'object' && typeof object.nodeType === 'number' && typeof object.nodeName === 'string';
      }

      return _isNode;
    }();
    /**
     * _executeHook
     * Execute user configurable hooks
     *
     * @param  {String} entryPoint  Name of the hook's entry point
     * @param  {Node} currentNode node to work on with the hook
     * @param  {Object} data additional hook parameters
     */


    var _executeHook = function () {
      function _executeHook(entryPoint, currentNode, data) {
        if (!hooks[entryPoint]) {
          return;
        }

        arrayForEach(hooks[entryPoint], function (hook) {
          hook.call(DOMPurify, currentNode, data, CONFIG);
        });
      }

      return _executeHook;
    }();
    /**
     * _sanitizeElements
     *
     * @protect nodeName
     * @protect textContent
     * @protect removeChild
     *
     * @param   {Node} currentNode to check for permission to exist
     * @return  {Boolean} true if node was killed, false if left alive
     */


    var _sanitizeElements = function () {
      function _sanitizeElements(currentNode) {
        var content = void 0;
        /* Execute a hook if present */

        _executeHook('beforeSanitizeElements', currentNode, null);
        /* Check if element is clobbered or can clobber */


        if (_isClobbered(currentNode)) {
          _forceRemove(currentNode);

          return true;
        }
        /* Check if tagname contains Unicode */


        if (stringMatch(currentNode.nodeName, /[\u0080-\uFFFF]/)) {
          _forceRemove(currentNode);

          return true;
        }
        /* Now let's check the element's type and name */


        var tagName = stringToLowerCase(currentNode.nodeName);
        /* Execute a hook if present */

        _executeHook('uponSanitizeElement', currentNode, {
          tagName: tagName,
          allowedTags: ALLOWED_TAGS
        });
        /* Detect mXSS attempts abusing namespace confusion */


        if (!_isNode(currentNode.firstElementChild) && (!_isNode(currentNode.content) || !_isNode(currentNode.content.firstElementChild)) && regExpTest(/<[/\w]/g, currentNode.innerHTML) && regExpTest(/<[/\w]/g, currentNode.textContent)) {
          _forceRemove(currentNode);

          return true;
        }
        /* Mitigate a problem with templates inside select */


        if (tagName === 'select' && regExpTest(/<template/i, currentNode.innerHTML)) {
          _forceRemove(currentNode);

          return true;
        }
        /* Remove element if anything forbids its presence */


        if (!ALLOWED_TAGS[tagName] || FORBID_TAGS[tagName]) {
          /* Keep content except for bad-listed elements */
          if (KEEP_CONTENT && !FORBID_CONTENTS[tagName]) {
            var parentNode = getParentNode(currentNode) || currentNode.parentNode;
            var childNodes = getChildNodes(currentNode) || currentNode.childNodes;

            if (childNodes && parentNode) {
              var childCount = childNodes.length;

              for (var i = childCount - 1; i >= 0; --i) {
                parentNode.insertBefore(cloneNode(childNodes[i], true), getNextSibling(currentNode));
              }
            }
          }

          _forceRemove(currentNode);

          return true;
        }
        /* Check whether element has a valid namespace */


        if (currentNode instanceof Element && !_checkValidNamespace(currentNode)) {
          _forceRemove(currentNode);

          return true;
        }

        if ((tagName === 'noscript' || tagName === 'noembed') && regExpTest(/<\/no(script|embed)/i, currentNode.innerHTML)) {
          _forceRemove(currentNode);

          return true;
        }
        /* Sanitize element content to be template-safe */


        if (SAFE_FOR_TEMPLATES && currentNode.nodeType === 3) {
          /* Get the element's text content */
          content = currentNode.textContent;
          content = stringReplace(content, MUSTACHE_EXPR$$1, ' ');
          content = stringReplace(content, ERB_EXPR$$1, ' ');

          if (currentNode.textContent !== content) {
            arrayPush(DOMPurify.removed, {
              element: currentNode.cloneNode()
            });
            currentNode.textContent = content;
          }
        }
        /* Execute a hook if present */


        _executeHook('afterSanitizeElements', currentNode, null);

        return false;
      }

      return _sanitizeElements;
    }();
    /**
     * _isValidAttribute
     *
     * @param  {string} lcTag Lowercase tag name of containing element.
     * @param  {string} lcName Lowercase attribute name.
     * @param  {string} value Attribute value.
     * @return {Boolean} Returns true if `value` is valid, otherwise false.
     */
    // eslint-disable-next-line complexity


    var _isValidAttribute = function () {
      function _isValidAttribute(lcTag, lcName, value) {
        /* Make sure attribute cannot clobber */
        if (SANITIZE_DOM && (lcName === 'id' || lcName === 'name') && (value in document || value in formElement)) {
          return false;
        }
        /* Allow valid data-* attributes: At least one character after "-"
            (https://html.spec.whatwg.org/multipage/dom.html#embedding-custom-non-visible-data-with-the-data-*-attributes)
            XML-compatible (https://html.spec.whatwg.org/multipage/infrastructure.html#xml-compatible and http://www.w3.org/TR/xml/#d0e804)
            We don't need to check the value; it's always URI safe. */


        if (ALLOW_DATA_ATTR && !FORBID_ATTR[lcName] && regExpTest(DATA_ATTR$$1, lcName)) ;else if (ALLOW_ARIA_ATTR && regExpTest(ARIA_ATTR$$1, lcName)) ;else if (!ALLOWED_ATTR[lcName] || FORBID_ATTR[lcName]) {
          return false;
          /* Check value is safe. First, is attr inert? If so, is safe */
        } else if (URI_SAFE_ATTRIBUTES[lcName]) ;else if (regExpTest(IS_ALLOWED_URI$$1, stringReplace(value, ATTR_WHITESPACE$$1, ''))) ;else if ((lcName === 'src' || lcName === 'xlink:href' || lcName === 'href') && lcTag !== 'script' && stringIndexOf(value, 'data:') === 0 && DATA_URI_TAGS[lcTag]) ;else if (ALLOW_UNKNOWN_PROTOCOLS && !regExpTest(IS_SCRIPT_OR_DATA$$1, stringReplace(value, ATTR_WHITESPACE$$1, ''))) ;else if (!value) ;else {
          return false;
        }
        return true;
      }

      return _isValidAttribute;
    }();
    /**
     * _sanitizeAttributes
     *
     * @protect attributes
     * @protect nodeName
     * @protect removeAttribute
     * @protect setAttribute
     *
     * @param  {Node} currentNode to sanitize
     */


    var _sanitizeAttributes = function () {
      function _sanitizeAttributes(currentNode) {
        var attr = void 0;
        var value = void 0;
        var lcName = void 0;
        var l = void 0;
        /* Execute a hook if present */

        _executeHook('beforeSanitizeAttributes', currentNode, null);

        var attributes = currentNode.attributes;
        /* Check if we have attributes; if not we might have a text node */

        if (!attributes) {
          return;
        }

        var hookEvent = {
          attrName: '',
          attrValue: '',
          keepAttr: true,
          allowedAttributes: ALLOWED_ATTR
        };
        l = attributes.length;
        /* Go backwards over all attributes; safely remove bad ones */

        while (l--) {
          attr = attributes[l];
          var _attr = attr,
              name = _attr.name,
              namespaceURI = _attr.namespaceURI;
          value = stringTrim(attr.value);
          lcName = stringToLowerCase(name);
          /* Execute a hook if present */

          hookEvent.attrName = lcName;
          hookEvent.attrValue = value;
          hookEvent.keepAttr = true;
          hookEvent.forceKeepAttr = undefined; // Allows developers to see this is a property they can set

          _executeHook('uponSanitizeAttribute', currentNode, hookEvent);

          value = hookEvent.attrValue;
          /* Did the hooks approve of the attribute? */

          if (hookEvent.forceKeepAttr) {
            continue;
          }
          /* Remove attribute */


          _removeAttribute(name, currentNode);
          /* Did the hooks approve of the attribute? */


          if (!hookEvent.keepAttr) {
            continue;
          }
          /* Work around a security issue in jQuery 3.0 */


          if (regExpTest(/\/>/i, value)) {
            _removeAttribute(name, currentNode);

            continue;
          }
          /* Sanitize attribute content to be template-safe */


          if (SAFE_FOR_TEMPLATES) {
            value = stringReplace(value, MUSTACHE_EXPR$$1, ' ');
            value = stringReplace(value, ERB_EXPR$$1, ' ');
          }
          /* Is `value` valid for this attribute? */


          var lcTag = currentNode.nodeName.toLowerCase();

          if (!_isValidAttribute(lcTag, lcName, value)) {
            continue;
          }
          /* Handle invalid data-* attribute set by try-catching it */


          try {
            if (namespaceURI) {
              currentNode.setAttributeNS(namespaceURI, name, value);
            } else {
              /* Fallback to setAttribute() for browser-unrecognized namespaces e.g. "x-schema". */
              currentNode.setAttribute(name, value);
            }

            arrayPop(DOMPurify.removed);
          } catch (_) {}
        }
        /* Execute a hook if present */


        _executeHook('afterSanitizeAttributes', currentNode, null);
      }

      return _sanitizeAttributes;
    }();
    /**
     * _sanitizeShadowDOM
     *
     * @param  {DocumentFragment} fragment to iterate over recursively
     */


    var _sanitizeShadowDOM = function () {
      function _sanitizeShadowDOM(fragment) {
        var shadowNode = void 0;

        var shadowIterator = _createIterator(fragment);
        /* Execute a hook if present */


        _executeHook('beforeSanitizeShadowDOM', fragment, null);

        while (shadowNode = shadowIterator.nextNode()) {
          /* Execute a hook if present */
          _executeHook('uponSanitizeShadowNode', shadowNode, null);
          /* Sanitize tags and elements */


          if (_sanitizeElements(shadowNode)) {
            continue;
          }
          /* Deep shadow DOM detected */


          if (shadowNode.content instanceof DocumentFragment) {
            _sanitizeShadowDOM(shadowNode.content);
          }
          /* Check attributes, sanitize if necessary */


          _sanitizeAttributes(shadowNode);
        }
        /* Execute a hook if present */


        _executeHook('afterSanitizeShadowDOM', fragment, null);
      }

      return _sanitizeShadowDOM;
    }();
    /**
     * Sanitize
     * Public method providing core sanitation functionality
     *
     * @param {String|Node} dirty string or DOM node
     * @param {Object} configuration object
     */
    // eslint-disable-next-line complexity


    DOMPurify.sanitize = function (dirty, cfg) {
      var body = void 0;
      var importedNode = void 0;
      var currentNode = void 0;
      var oldNode = void 0;
      var returnNode = void 0;
      /* Make sure we have a string to sanitize.
        DO NOT return early, as this will return the wrong type if
        the user has requested a DOM object rather than a string */

      IS_EMPTY_INPUT = !dirty;

      if (IS_EMPTY_INPUT) {
        dirty = '<!-->';
      }
      /* Stringify, in case dirty is an object */


      if (typeof dirty !== 'string' && !_isNode(dirty)) {
        // eslint-disable-next-line no-negated-condition
        if (typeof dirty.toString !== 'function') {
          throw typeErrorCreate('toString is not a function');
        } else {
          dirty = dirty.toString();

          if (typeof dirty !== 'string') {
            throw typeErrorCreate('dirty is not a string, aborting');
          }
        }
      }
      /* Check we can run. Otherwise fall back or ignore */


      if (!DOMPurify.isSupported) {
        if (_typeof(window.toStaticHTML) === 'object' || typeof window.toStaticHTML === 'function') {
          if (typeof dirty === 'string') {
            return window.toStaticHTML(dirty);
          }

          if (_isNode(dirty)) {
            return window.toStaticHTML(dirty.outerHTML);
          }
        }

        return dirty;
      }
      /* Assign config vars */


      if (!SET_CONFIG) {
        _parseConfig(cfg);
      }
      /* Clean up removed elements */


      DOMPurify.removed = [];
      /* Check if dirty is correctly typed for IN_PLACE */

      if (typeof dirty === 'string') {
        IN_PLACE = false;
      }

      if (IN_PLACE) ;else if (dirty instanceof Node) {
        /* If dirty is a DOM element, append to an empty document to avoid
           elements being stripped by the parser */
        body = _initDocument('<!---->');
        importedNode = body.ownerDocument.importNode(dirty, true);

        if (importedNode.nodeType === 1 && importedNode.nodeName === 'BODY') {
          /* Node is already a body, use as is */
          body = importedNode;
        } else if (importedNode.nodeName === 'HTML') {
          body = importedNode;
        } else {
          // eslint-disable-next-line unicorn/prefer-dom-node-append
          body.appendChild(importedNode);
        }
      } else {
        /* Exit directly if we have nothing to do */
        if (!RETURN_DOM && !SAFE_FOR_TEMPLATES && !WHOLE_DOCUMENT && // eslint-disable-next-line unicorn/prefer-includes
        dirty.indexOf('<') === -1) {
          return trustedTypesPolicy && RETURN_TRUSTED_TYPE ? trustedTypesPolicy.createHTML(dirty) : dirty;
        }
        /* Initialize the document to work on */


        body = _initDocument(dirty);
        /* Check we have a DOM node from the data */

        if (!body) {
          return RETURN_DOM ? null : emptyHTML;
        }
      }
      /* Remove first element node (ours) if FORCE_BODY is set */

      if (body && FORCE_BODY) {
        _forceRemove(body.firstChild);
      }
      /* Get node iterator */


      var nodeIterator = _createIterator(IN_PLACE ? dirty : body);
      /* Now start iterating over the created document */


      while (currentNode = nodeIterator.nextNode()) {
        /* Fix IE's strange behavior with manipulated textNodes #89 */
        if (currentNode.nodeType === 3 && currentNode === oldNode) {
          continue;
        }
        /* Sanitize tags and elements */


        if (_sanitizeElements(currentNode)) {
          continue;
        }
        /* Shadow DOM detected, sanitize it */


        if (currentNode.content instanceof DocumentFragment) {
          _sanitizeShadowDOM(currentNode.content);
        }
        /* Check attributes, sanitize if necessary */


        _sanitizeAttributes(currentNode);

        oldNode = currentNode;
      }

      oldNode = null;
      /* If we sanitized `dirty` in-place, return it. */

      if (IN_PLACE) {
        return dirty;
      }
      /* Return sanitized string or DOM */


      if (RETURN_DOM) {
        if (RETURN_DOM_FRAGMENT) {
          returnNode = createDocumentFragment.call(body.ownerDocument);

          while (body.firstChild) {
            // eslint-disable-next-line unicorn/prefer-dom-node-append
            returnNode.appendChild(body.firstChild);
          }
        } else {
          returnNode = body;
        }

        if (RETURN_DOM_IMPORT) {
          /*
            AdoptNode() is not used because internal state is not reset
            (e.g. the past names map of a HTMLFormElement), this is safe
            in theory but we would rather not risk another attack vector.
            The state that is cloned by importNode() is explicitly defined
            by the specs.
          */
          returnNode = importNode.call(originalDocument, returnNode, true);
        }

        return returnNode;
      }

      var serializedHTML = WHOLE_DOCUMENT ? body.outerHTML : body.innerHTML;
      /* Sanitize final string template-safe */

      if (SAFE_FOR_TEMPLATES) {
        serializedHTML = stringReplace(serializedHTML, MUSTACHE_EXPR$$1, ' ');
        serializedHTML = stringReplace(serializedHTML, ERB_EXPR$$1, ' ');
      }

      return trustedTypesPolicy && RETURN_TRUSTED_TYPE ? trustedTypesPolicy.createHTML(serializedHTML) : serializedHTML;
    };
    /**
     * Public method to set the configuration once
     * setConfig
     *
     * @param {Object} cfg configuration object
     */


    DOMPurify.setConfig = function (cfg) {
      _parseConfig(cfg);

      SET_CONFIG = true;
    };
    /**
     * Public method to remove the configuration
     * clearConfig
     *
     */


    DOMPurify.clearConfig = function () {
      CONFIG = null;
      SET_CONFIG = false;
    };
    /**
     * Public method to check if an attribute value is valid.
     * Uses last set config, if any. Otherwise, uses config defaults.
     * isValidAttribute
     *
     * @param  {string} tag Tag name of containing element.
     * @param  {string} attr Attribute name.
     * @param  {string} value Attribute value.
     * @return {Boolean} Returns true if `value` is valid. Otherwise, returns false.
     */


    DOMPurify.isValidAttribute = function (tag, attr, value) {
      /* Initialize shared config vars if necessary. */
      if (!CONFIG) {
        _parseConfig({});
      }

      var lcTag = stringToLowerCase(tag);
      var lcName = stringToLowerCase(attr);
      return _isValidAttribute(lcTag, lcName, value);
    };
    /**
     * AddHook
     * Public method to add DOMPurify hooks
     *
     * @param {String} entryPoint entry point for the hook to add
     * @param {Function} hookFunction function to execute
     */


    DOMPurify.addHook = function (entryPoint, hookFunction) {
      if (typeof hookFunction !== 'function') {
        return;
      }

      hooks[entryPoint] = hooks[entryPoint] || [];
      arrayPush(hooks[entryPoint], hookFunction);
    };
    /**
     * RemoveHook
     * Public method to remove a DOMPurify hook at a given entryPoint
     * (pops it from the stack of hooks if more are present)
     *
     * @param {String} entryPoint entry point for the hook to remove
     */


    DOMPurify.removeHook = function (entryPoint) {
      if (hooks[entryPoint]) {
        arrayPop(hooks[entryPoint]);
      }
    };
    /**
     * RemoveHooks
     * Public method to remove all DOMPurify hooks at a given entryPoint
     *
     * @param  {String} entryPoint entry point for the hooks to remove
     */


    DOMPurify.removeHooks = function (entryPoint) {
      if (hooks[entryPoint]) {
        hooks[entryPoint] = [];
      }
    };
    /**
     * RemoveAllHooks
     * Public method to remove all DOMPurify hooks
     *
     */


    DOMPurify.removeAllHooks = function () {
      hooks = {};
    };

    return DOMPurify;
  }

  var purify = createDOMPurify();
  return purify;
});

/***/ }),

/***/ 92376:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports._CI = createClassComponentInstance;
exports._HI = normalizeRoot;
exports._M = mount;
exports._MCCC = mountClassComponentCallbacks;
exports._ME = mountElement;
exports._MFCC = mountFunctionalComponentCallbacks;
exports._MP = mountProps;
exports._MR = mountRef;
exports._RFC = renderFunctionalComponent;
exports.__render = __render;
exports.createComponentVNode = createComponentVNode;
exports.createFragment = createFragment;
exports.createPortal = createPortal;
exports.createRef = createRef;
exports.createRenderer = createRenderer;
exports.createTextVNode = createTextVNode;
exports.createVNode = createVNode;
exports.directClone = directClone;
exports.findDOMfromVNode = findDOMfromVNode;
exports.forwardRef = forwardRef;
exports.getFlagsForElementVnode = getFlagsForElementVnode;
exports.linkEvent = linkEvent;
exports.normalizeProps = normalizeProps;
exports.render = render;
exports.rerender = rerender;
exports.version = exports.options = exports.Fragment = exports.EMPTY_OBJ = exports.Component = void 0;
var isArray = Array.isArray;

function isStringOrNumber(o) {
  var type = typeof o;
  return type === 'string' || type === 'number';
}

function isNullOrUndef(o) {
  return o === void 0 || o === null;
}

function isInvalid(o) {
  return o === null || o === false || o === true || o === void 0;
}

function isFunction(o) {
  return typeof o === 'function';
}

function isString(o) {
  return typeof o === 'string';
}

function isNumber(o) {
  return typeof o === 'number';
}

function isNull(o) {
  return o === null;
}

function isUndefined(o) {
  return o === void 0;
}

function combineFrom(first, second) {
  var out = {};

  if (first) {
    for (var key in first) {
      out[key] = first[key];
    }
  }

  if (second) {
    for (var key$1 in second) {
      out[key$1] = second[key$1];
    }
  }

  return out;
}
/**
 * Links given data to event as first parameter
 * @param {*} data data to be linked, it will be available in function as first parameter
 * @param {Function} event Function to be called when event occurs
 * @returns {{data: *, event: Function}}
 */


function linkEvent(data, event) {
  if (isFunction(event)) {
    return {
      data: data,
      event: event
    };
  }

  return null; // Return null when event is invalid, to avoid creating unnecessary event handlers
} // object.event should always be function, otherwise its badly created object.


function isLinkEventObject(o) {
  return !isNull(o) && typeof o === 'object';
} // We need EMPTY_OBJ defined in one place.
// Its used for comparison so we cant inline it into shared


var EMPTY_OBJ = {};
exports.EMPTY_OBJ = EMPTY_OBJ;
var Fragment = '$F';
exports.Fragment = Fragment;

function normalizeEventName(name) {
  return name.substr(2).toLowerCase();
}

function appendChild(parentDOM, dom) {
  parentDOM.appendChild(dom);
}

function insertOrAppend(parentDOM, newNode, nextNode) {
  if (isNull(nextNode)) {
    appendChild(parentDOM, newNode);
  } else {
    parentDOM.insertBefore(newNode, nextNode);
  }
}

function documentCreateElement(tag, isSVG) {
  if (isSVG) {
    return document.createElementNS('http://www.w3.org/2000/svg', tag);
  }

  return document.createElement(tag);
}

function replaceChild(parentDOM, newDom, lastDom) {
  parentDOM.replaceChild(newDom, lastDom);
}

function removeChild(parentDOM, childNode) {
  parentDOM.removeChild(childNode);
}

function callAll(arrayFn) {
  for (var i = 0; i < arrayFn.length; i++) {
    arrayFn[i]();
  }
}

function findChildVNode(vNode, startEdge, flags) {
  var children = vNode.children;

  if (flags & 4
  /* ComponentClass */
  ) {
    return children.$LI;
  }

  if (flags & 8192
  /* Fragment */
  ) {
    return vNode.childFlags === 2
    /* HasVNodeChildren */
    ? children : children[startEdge ? 0 : children.length - 1];
  }

  return children;
}

function findDOMfromVNode(vNode, startEdge) {
  var flags;

  while (vNode) {
    flags = vNode.flags;

    if (flags & 2033
    /* DOMRef */
    ) {
      return vNode.dom;
    }

    vNode = findChildVNode(vNode, startEdge, flags);
  }

  return null;
}

function removeVNodeDOM(vNode, parentDOM) {
  do {
    var flags = vNode.flags;

    if (flags & 2033
    /* DOMRef */
    ) {
      removeChild(parentDOM, vNode.dom);
      return;
    }

    var children = vNode.children;

    if (flags & 4
    /* ComponentClass */
    ) {
      vNode = children.$LI;
    }

    if (flags & 8
    /* ComponentFunction */
    ) {
      vNode = children;
    }

    if (flags & 8192
    /* Fragment */
    ) {
      if (vNode.childFlags === 2
      /* HasVNodeChildren */
      ) {
        vNode = children;
      } else {
        for (var i = 0, len = children.length; i < len; ++i) {
          removeVNodeDOM(children[i], parentDOM);
        }

        return;
      }
    }
  } while (vNode);
}

function moveVNodeDOM(vNode, parentDOM, nextNode) {
  do {
    var flags = vNode.flags;

    if (flags & 2033
    /* DOMRef */
    ) {
      insertOrAppend(parentDOM, vNode.dom, nextNode);
      return;
    }

    var children = vNode.children;

    if (flags & 4
    /* ComponentClass */
    ) {
      vNode = children.$LI;
    }

    if (flags & 8
    /* ComponentFunction */
    ) {
      vNode = children;
    }

    if (flags & 8192
    /* Fragment */
    ) {
      if (vNode.childFlags === 2
      /* HasVNodeChildren */
      ) {
        vNode = children;
      } else {
        for (var i = 0, len = children.length; i < len; ++i) {
          moveVNodeDOM(children[i], parentDOM, nextNode);
        }

        return;
      }
    }
  } while (vNode);
}

function createDerivedState(instance, nextProps, state) {
  if (instance.constructor.getDerivedStateFromProps) {
    return combineFrom(state, instance.constructor.getDerivedStateFromProps(nextProps, state));
  }

  return state;
}

var renderCheck = {
  v: false
};
var options = {
  componentComparator: null,
  createVNode: null,
  renderComplete: null
};
exports.options = options;

function setTextContent(dom, children) {
  dom.textContent = children;
} // Calling this function assumes, nextValue is linkEvent


function isLastValueSameLinkEvent(lastValue, nextValue) {
  return isLinkEventObject(lastValue) && lastValue.event === nextValue.event && lastValue.data === nextValue.data;
}

function mergeUnsetProperties(to, from) {
  for (var propName in from) {
    if (isUndefined(to[propName])) {
      to[propName] = from[propName];
    }
  }

  return to;
}

function safeCall1(method, arg1) {
  return !!isFunction(method) && (method(arg1), true);
}

var keyPrefix = '$';

function V(childFlags, children, className, flags, key, props, ref, type) {
  this.childFlags = childFlags;
  this.children = children;
  this.className = className;
  this.dom = null;
  this.flags = flags;
  this.key = key === void 0 ? null : key;
  this.props = props === void 0 ? null : props;
  this.ref = ref === void 0 ? null : ref;
  this.type = type;
}

function createVNode(flags, type, className, children, childFlags, props, key, ref) {
  var childFlag = childFlags === void 0 ? 1
  /* HasInvalidChildren */
  : childFlags;
  var vNode = new V(childFlag, children, className, flags, key, props, ref, type);

  if (options.createVNode) {
    options.createVNode(vNode);
  }

  if (childFlag === 0
  /* UnknownChildren */
  ) {
    normalizeChildren(vNode, vNode.children);
  }

  return vNode;
}

function mergeDefaultHooks(flags, type, ref) {
  if (flags & 4
  /* ComponentClass */
  ) {
    return ref;
  }

  var defaultHooks = (flags & 32768
  /* ForwardRef */
  ? type.render : type).defaultHooks;

  if (isNullOrUndef(defaultHooks)) {
    return ref;
  }

  if (isNullOrUndef(ref)) {
    return defaultHooks;
  }

  return mergeUnsetProperties(ref, defaultHooks);
}

function mergeDefaultProps(flags, type, props) {
  // set default props
  var defaultProps = (flags & 32768
  /* ForwardRef */
  ? type.render : type).defaultProps;

  if (isNullOrUndef(defaultProps)) {
    return props;
  }

  if (isNullOrUndef(props)) {
    return combineFrom(defaultProps, null);
  }

  return mergeUnsetProperties(props, defaultProps);
}

function resolveComponentFlags(flags, type) {
  if (flags & 12
  /* ComponentKnown */
  ) {
    return flags;
  }

  if (type.prototype && type.prototype.render) {
    return 4
    /* ComponentClass */
    ;
  }

  if (type.render) {
    return 32776
    /* ForwardRefComponent */
    ;
  }

  return 8
  /* ComponentFunction */
  ;
}

function createComponentVNode(flags, type, props, key, ref) {
  flags = resolveComponentFlags(flags, type);
  var vNode = new V(1
  /* HasInvalidChildren */
  , null, null, flags, key, mergeDefaultProps(flags, type, props), mergeDefaultHooks(flags, type, ref), type);

  if (options.createVNode) {
    options.createVNode(vNode);
  }

  return vNode;
}

function createTextVNode(text, key) {
  return new V(1
  /* HasInvalidChildren */
  , isNullOrUndef(text) || text === true || text === false ? '' : text, null, 16
  /* Text */
  , key, null, null, null);
}

function createFragment(children, childFlags, key) {
  var fragment = createVNode(8192
  /* Fragment */
  , 8192
  /* Fragment */
  , null, children, childFlags, null, key, null);

  switch (fragment.childFlags) {
    case 1
    /* HasInvalidChildren */
    :
      fragment.children = createVoidVNode();
      fragment.childFlags = 2
      /* HasVNodeChildren */
      ;
      break;

    case 16
    /* HasTextChildren */
    :
      fragment.children = [createTextVNode(children)];
      fragment.childFlags = 4
      /* HasNonKeyedChildren */
      ;
      break;
  }

  return fragment;
}

function normalizeProps(vNode) {
  var props = vNode.props;

  if (props) {
    var flags = vNode.flags;

    if (flags & 481
    /* Element */
    ) {
      if (props.children !== void 0 && isNullOrUndef(vNode.children)) {
        normalizeChildren(vNode, props.children);
      }

      if (props.className !== void 0) {
        vNode.className = props.className || null;
        props.className = undefined;
      }
    }

    if (props.key !== void 0) {
      vNode.key = props.key;
      props.key = undefined;
    }

    if (props.ref !== void 0) {
      if (flags & 8
      /* ComponentFunction */
      ) {
        vNode.ref = combineFrom(vNode.ref, props.ref);
      } else {
        vNode.ref = props.ref;
      }

      props.ref = undefined;
    }
  }

  return vNode;
}
/*
 * Fragment is different than normal vNode,
 * because when it needs to be cloned we need to clone its children too
 * But not normalize, because otherwise those possibly get KEY and re-mount
 */


function cloneFragment(vNodeToClone) {
  var oldChildren = vNodeToClone.children;
  var childFlags = vNodeToClone.childFlags;
  return createFragment(childFlags === 2
  /* HasVNodeChildren */
  ? directClone(oldChildren) : oldChildren.map(directClone), childFlags, vNodeToClone.key);
}

function directClone(vNodeToClone) {
  var flags = vNodeToClone.flags & -16385
  /* ClearInUse */
  ;
  var props = vNodeToClone.props;

  if (flags & 14
  /* Component */
  ) {
    if (!isNull(props)) {
      var propsToClone = props;
      props = {};

      for (var key in propsToClone) {
        props[key] = propsToClone[key];
      }
    }
  }

  if ((flags & 8192
  /* Fragment */
  ) === 0) {
    return new V(vNodeToClone.childFlags, vNodeToClone.children, vNodeToClone.className, flags, vNodeToClone.key, props, vNodeToClone.ref, vNodeToClone.type);
  }

  return cloneFragment(vNodeToClone);
}

function createVoidVNode() {
  return createTextVNode('', null);
}

function createPortal(children, container) {
  var normalizedRoot = normalizeRoot(children);
  return createVNode(1024
  /* Portal */
  , 1024
  /* Portal */
  , null, normalizedRoot, 0
  /* UnknownChildren */
  , null, normalizedRoot.key, container);
}

function _normalizeVNodes(nodes, result, index, currentKey) {
  for (var len = nodes.length; index < len; index++) {
    var n = nodes[index];

    if (!isInvalid(n)) {
      var newKey = currentKey + keyPrefix + index;

      if (isArray(n)) {
        _normalizeVNodes(n, result, 0, newKey);
      } else {
        if (isStringOrNumber(n)) {
          n = createTextVNode(n, newKey);
        } else {
          var oldKey = n.key;
          var isPrefixedKey = isString(oldKey) && oldKey[0] === keyPrefix;

          if (n.flags & 81920
          /* InUseOrNormalized */
          || isPrefixedKey) {
            n = directClone(n);
          }

          n.flags |= 65536
          /* Normalized */
          ;

          if (!isPrefixedKey) {
            if (isNull(oldKey)) {
              n.key = newKey;
            } else {
              n.key = currentKey + oldKey;
            }
          } else if (oldKey.substring(0, currentKey.length) !== currentKey) {
            n.key = currentKey + oldKey;
          }
        }

        result.push(n);
      }
    }
  }
}

function getFlagsForElementVnode(type) {
  switch (type) {
    case 'svg':
      return 32
      /* SvgElement */
      ;

    case 'input':
      return 64
      /* InputElement */
      ;

    case 'select':
      return 256
      /* SelectElement */
      ;

    case 'textarea':
      return 128
      /* TextareaElement */
      ;

    case Fragment:
      return 8192
      /* Fragment */
      ;

    default:
      return 1
      /* HtmlElement */
      ;
  }
}

function normalizeChildren(vNode, children) {
  var newChildren;
  var newChildFlags = 1
  /* HasInvalidChildren */
  ; // Don't change children to match strict equal (===) true in patching

  if (isInvalid(children)) {
    newChildren = children;
  } else if (isStringOrNumber(children)) {
    newChildFlags = 16
    /* HasTextChildren */
    ;
    newChildren = children;
  } else if (isArray(children)) {
    var len = children.length;

    for (var i = 0; i < len; ++i) {
      var n = children[i];

      if (isInvalid(n) || isArray(n)) {
        newChildren = newChildren || children.slice(0, i);

        _normalizeVNodes(children, newChildren, i, '');

        break;
      } else if (isStringOrNumber(n)) {
        newChildren = newChildren || children.slice(0, i);
        newChildren.push(createTextVNode(n, keyPrefix + i));
      } else {
        var key = n.key;
        var needsCloning = (n.flags & 81920
        /* InUseOrNormalized */
        ) > 0;
        var isNullKey = isNull(key);
        var isPrefixed = isString(key) && key[0] === keyPrefix;

        if (needsCloning || isNullKey || isPrefixed) {
          newChildren = newChildren || children.slice(0, i);

          if (needsCloning || isPrefixed) {
            n = directClone(n);
          }

          if (isNullKey || isPrefixed) {
            n.key = keyPrefix + i;
          }

          newChildren.push(n);
        } else if (newChildren) {
          newChildren.push(n);
        }

        n.flags |= 65536
        /* Normalized */
        ;
      }
    }

    newChildren = newChildren || children;

    if (newChildren.length === 0) {
      newChildFlags = 1
      /* HasInvalidChildren */
      ;
    } else {
      newChildFlags = 8
      /* HasKeyedChildren */
      ;
    }
  } else {
    newChildren = children;
    newChildren.flags |= 65536
    /* Normalized */
    ;

    if (children.flags & 81920
    /* InUseOrNormalized */
    ) {
      newChildren = directClone(children);
    }

    newChildFlags = 2
    /* HasVNodeChildren */
    ;
  }

  vNode.children = newChildren;
  vNode.childFlags = newChildFlags;
  return vNode;
}

function normalizeRoot(input) {
  if (isInvalid(input) || isStringOrNumber(input)) {
    return createTextVNode(input, null);
  }

  if (isArray(input)) {
    return createFragment(input, 0
    /* UnknownChildren */
    , null);
  }

  return input.flags & 16384
  /* InUse */
  ? directClone(input) : input;
}

var xlinkNS = 'http://www.w3.org/1999/xlink';
var xmlNS = 'http://www.w3.org/XML/1998/namespace';
var namespaces = {
  'xlink:actuate': xlinkNS,
  'xlink:arcrole': xlinkNS,
  'xlink:href': xlinkNS,
  'xlink:role': xlinkNS,
  'xlink:show': xlinkNS,
  'xlink:title': xlinkNS,
  'xlink:type': xlinkNS,
  'xml:base': xmlNS,
  'xml:lang': xmlNS,
  'xml:space': xmlNS
};

function getDelegatedEventObject(v) {
  return {
    onClick: v,
    onDblClick: v,
    onFocusIn: v,
    onFocusOut: v,
    onKeyDown: v,
    onKeyPress: v,
    onKeyUp: v,
    onMouseDown: v,
    onMouseMove: v,
    onMouseUp: v,
    onTouchEnd: v,
    onTouchMove: v,
    onTouchStart: v
  };
}

var attachedEventCounts = getDelegatedEventObject(0);
var attachedEvents = getDelegatedEventObject(null);
var syntheticEvents = getDelegatedEventObject(true);

function updateOrAddSyntheticEvent(name, dom) {
  var eventsObject = dom.$EV;

  if (!eventsObject) {
    eventsObject = dom.$EV = getDelegatedEventObject(null);
  }

  if (!eventsObject[name]) {
    if (++attachedEventCounts[name] === 1) {
      attachedEvents[name] = attachEventToDocument(name);
    }
  }

  return eventsObject;
}

function unmountSyntheticEvent(name, dom) {
  var eventsObject = dom.$EV;

  if (eventsObject && eventsObject[name]) {
    if (--attachedEventCounts[name] === 0) {
      document.removeEventListener(normalizeEventName(name), attachedEvents[name]);
      attachedEvents[name] = null;
    }

    eventsObject[name] = null;
  }
}

function handleSyntheticEvent(name, lastEvent, nextEvent, dom) {
  if (isFunction(nextEvent)) {
    updateOrAddSyntheticEvent(name, dom)[name] = nextEvent;
  } else if (isLinkEventObject(nextEvent)) {
    if (isLastValueSameLinkEvent(lastEvent, nextEvent)) {
      return;
    }

    updateOrAddSyntheticEvent(name, dom)[name] = nextEvent;
  } else {
    unmountSyntheticEvent(name, dom);
  }
} // When browsers fully support event.composedPath we could loop it through instead of using parentNode property


function getTargetNode(event) {
  return isFunction(event.composedPath) ? event.composedPath()[0] : event.target;
}

function dispatchEvents(event, isClick, name, eventData) {
  var dom = getTargetNode(event);

  do {
    // Html Nodes can be nested fe: span inside button in that scenario browser does not handle disabled attribute on parent,
    // because the event listener is on document.body
    // Don't process clicks on disabled elements
    if (isClick && dom.disabled) {
      return;
    }

    var eventsObject = dom.$EV;

    if (eventsObject) {
      var currentEvent = eventsObject[name];

      if (currentEvent) {
        // linkEvent object
        eventData.dom = dom;
        currentEvent.event ? currentEvent.event(currentEvent.data, event) : currentEvent(event);

        if (event.cancelBubble) {
          return;
        }
      }
    }

    dom = dom.parentNode;
  } while (!isNull(dom));
}

function stopPropagation() {
  this.cancelBubble = true;

  if (!this.immediatePropagationStopped) {
    this.stopImmediatePropagation();
  }
}

function isDefaultPrevented() {
  return this.defaultPrevented;
}

function isPropagationStopped() {
  return this.cancelBubble;
}

function extendEventProperties(event) {
  // Event data needs to be object to save reference to currentTarget getter
  var eventData = {
    dom: document
  };
  event.isDefaultPrevented = isDefaultPrevented;
  event.isPropagationStopped = isPropagationStopped;
  event.stopPropagation = stopPropagation;
  Object.defineProperty(event, 'currentTarget', {
    configurable: true,
    get: function () {
      function get() {
        return eventData.dom;
      }

      return get;
    }()
  });
  return eventData;
}

function rootClickEvent(name) {
  return function (event) {
    if (event.button !== 0) {
      // Firefox incorrectly triggers click event for mid/right mouse buttons.
      // This bug has been active for 17 years.
      // https://bugzilla.mozilla.org/show_bug.cgi?id=184051
      event.stopPropagation();
      return;
    }

    dispatchEvents(event, true, name, extendEventProperties(event));
  };
}

function rootEvent(name) {
  return function (event) {
    dispatchEvents(event, false, name, extendEventProperties(event));
  };
}

function attachEventToDocument(name) {
  var attachedEvent = name === 'onClick' || name === 'onDblClick' ? rootClickEvent(name) : rootEvent(name);
  document.addEventListener(normalizeEventName(name), attachedEvent);
  return attachedEvent;
}

function isSameInnerHTML(dom, innerHTML) {
  var tempdom = document.createElement('i');
  tempdom.innerHTML = innerHTML;
  return tempdom.innerHTML === dom.innerHTML;
}

function triggerEventListener(props, methodName, e) {
  if (props[methodName]) {
    var listener = props[methodName];

    if (listener.event) {
      listener.event(listener.data, e);
    } else {
      listener(e);
    }
  } else {
    var nativeListenerName = methodName.toLowerCase();

    if (props[nativeListenerName]) {
      props[nativeListenerName](e);
    }
  }
}

function createWrappedFunction(methodName, applyValue) {
  var fnMethod = function fnMethod(e) {
    var vNode = this.$V; // If vNode is gone by the time event fires, no-op

    if (!vNode) {
      return;
    }

    var props = vNode.props || EMPTY_OBJ;
    var dom = vNode.dom;

    if (isString(methodName)) {
      triggerEventListener(props, methodName, e);
    } else {
      for (var i = 0; i < methodName.length; ++i) {
        triggerEventListener(props, methodName[i], e);
      }
    }

    if (isFunction(applyValue)) {
      var newVNode = this.$V;
      var newProps = newVNode.props || EMPTY_OBJ;
      applyValue(newProps, dom, false, newVNode);
    }
  };

  Object.defineProperty(fnMethod, 'wrapped', {
    configurable: false,
    enumerable: false,
    value: true,
    writable: false
  });
  return fnMethod;
}

function attachEvent(dom, eventName, handler) {
  var previousKey = "$" + eventName;
  var previousArgs = dom[previousKey];

  if (previousArgs) {
    if (previousArgs[1].wrapped) {
      return;
    }

    dom.removeEventListener(previousArgs[0], previousArgs[1]);
    dom[previousKey] = null;
  }

  if (isFunction(handler)) {
    dom.addEventListener(eventName, handler);
    dom[previousKey] = [eventName, handler];
  }
}

function isCheckedType(type) {
  return type === 'checkbox' || type === 'radio';
}

var onTextInputChange = createWrappedFunction('onInput', applyValueInput);
var wrappedOnChange = createWrappedFunction(['onClick', 'onChange'], applyValueInput);
/* tslint:disable-next-line:no-empty */

function emptywrapper(event) {
  event.stopPropagation();
}

emptywrapper.wrapped = true;

function inputEvents(dom, nextPropsOrEmpty) {
  if (isCheckedType(nextPropsOrEmpty.type)) {
    attachEvent(dom, 'change', wrappedOnChange);
    attachEvent(dom, 'click', emptywrapper);
  } else {
    attachEvent(dom, 'input', onTextInputChange);
  }
}

function applyValueInput(nextPropsOrEmpty, dom) {
  var type = nextPropsOrEmpty.type;
  var value = nextPropsOrEmpty.value;
  var checked = nextPropsOrEmpty.checked;
  var multiple = nextPropsOrEmpty.multiple;
  var defaultValue = nextPropsOrEmpty.defaultValue;
  var hasValue = !isNullOrUndef(value);

  if (type && type !== dom.type) {
    dom.setAttribute('type', type);
  }

  if (!isNullOrUndef(multiple) && multiple !== dom.multiple) {
    dom.multiple = multiple;
  }

  if (!isNullOrUndef(defaultValue) && !hasValue) {
    dom.defaultValue = defaultValue + '';
  }

  if (isCheckedType(type)) {
    if (hasValue) {
      dom.value = value;
    }

    if (!isNullOrUndef(checked)) {
      dom.checked = checked;
    }
  } else {
    if (hasValue && dom.value !== value) {
      dom.defaultValue = value;
      dom.value = value;
    } else if (!isNullOrUndef(checked)) {
      dom.checked = checked;
    }
  }
}

function updateChildOptions(vNode, value) {
  if (vNode.type === 'option') {
    updateChildOption(vNode, value);
  } else {
    var children = vNode.children;
    var flags = vNode.flags;

    if (flags & 4
    /* ComponentClass */
    ) {
      updateChildOptions(children.$LI, value);
    } else if (flags & 8
    /* ComponentFunction */
    ) {
      updateChildOptions(children, value);
    } else if (vNode.childFlags === 2
    /* HasVNodeChildren */
    ) {
      updateChildOptions(children, value);
    } else if (vNode.childFlags & 12
    /* MultipleChildren */
    ) {
      for (var i = 0, len = children.length; i < len; ++i) {
        updateChildOptions(children[i], value);
      }
    }
  }
}

function updateChildOption(vNode, value) {
  var props = vNode.props || EMPTY_OBJ;
  var dom = vNode.dom; // we do this as multiple may have changed

  dom.value = props.value;

  if (props.value === value || isArray(value) && value.indexOf(props.value) !== -1) {
    dom.selected = true;
  } else if (!isNullOrUndef(value) || !isNullOrUndef(props.selected)) {
    dom.selected = props.selected || false;
  }
}

var onSelectChange = createWrappedFunction('onChange', applyValueSelect);

function selectEvents(dom) {
  attachEvent(dom, 'change', onSelectChange);
}

function applyValueSelect(nextPropsOrEmpty, dom, mounting, vNode) {
  var multiplePropInBoolean = Boolean(nextPropsOrEmpty.multiple);

  if (!isNullOrUndef(nextPropsOrEmpty.multiple) && multiplePropInBoolean !== dom.multiple) {
    dom.multiple = multiplePropInBoolean;
  }

  var index = nextPropsOrEmpty.selectedIndex;

  if (index === -1) {
    dom.selectedIndex = -1;
  }

  var childFlags = vNode.childFlags;

  if (childFlags !== 1
  /* HasInvalidChildren */
  ) {
    var value = nextPropsOrEmpty.value;

    if (isNumber(index) && index > -1 && dom.options[index]) {
      value = dom.options[index].value;
    }

    if (mounting && isNullOrUndef(value)) {
      value = nextPropsOrEmpty.defaultValue;
    }

    updateChildOptions(vNode, value);
  }
}

var onTextareaInputChange = createWrappedFunction('onInput', applyValueTextArea);
var wrappedOnChange$1 = createWrappedFunction('onChange');

function textAreaEvents(dom, nextPropsOrEmpty) {
  attachEvent(dom, 'input', onTextareaInputChange);

  if (nextPropsOrEmpty.onChange) {
    attachEvent(dom, 'change', wrappedOnChange$1);
  }
}

function applyValueTextArea(nextPropsOrEmpty, dom, mounting) {
  var value = nextPropsOrEmpty.value;
  var domValue = dom.value;

  if (isNullOrUndef(value)) {
    if (mounting) {
      var defaultValue = nextPropsOrEmpty.defaultValue;

      if (!isNullOrUndef(defaultValue) && defaultValue !== domValue) {
        dom.defaultValue = defaultValue;
        dom.value = defaultValue;
      }
    }
  } else if (domValue !== value) {
    /* There is value so keep it controlled */
    dom.defaultValue = value;
    dom.value = value;
  }
}

function processElement(flags, vNode, dom, nextPropsOrEmpty, mounting, isControlled) {
  if (flags & 64
  /* InputElement */
  ) {
    applyValueInput(nextPropsOrEmpty, dom);
  } else if (flags & 256
  /* SelectElement */
  ) {
    applyValueSelect(nextPropsOrEmpty, dom, mounting, vNode);
  } else if (flags & 128
  /* TextareaElement */
  ) {
    applyValueTextArea(nextPropsOrEmpty, dom, mounting);
  }

  if (isControlled) {
    dom.$V = vNode;
  }
}

function addFormElementEventHandlers(flags, dom, nextPropsOrEmpty) {
  if (flags & 64
  /* InputElement */
  ) {
    inputEvents(dom, nextPropsOrEmpty);
  } else if (flags & 256
  /* SelectElement */
  ) {
    selectEvents(dom);
  } else if (flags & 128
  /* TextareaElement */
  ) {
    textAreaEvents(dom, nextPropsOrEmpty);
  }
}

function isControlledFormElement(nextPropsOrEmpty) {
  return nextPropsOrEmpty.type && isCheckedType(nextPropsOrEmpty.type) ? !isNullOrUndef(nextPropsOrEmpty.checked) : !isNullOrUndef(nextPropsOrEmpty.value);
}

function createRef() {
  return {
    current: null
  };
}

function forwardRef(render) {
  // @ts-ignore
  return {
    render: render
  };
}

function unmountRef(ref) {
  if (ref) {
    if (!safeCall1(ref, null) && ref.current) {
      ref.current = null;
    }
  }
}

function mountRef(ref, value, lifecycle) {
  if (ref && (isFunction(ref) || ref.current !== void 0)) {
    lifecycle.push(function () {
      if (!safeCall1(ref, value) && ref.current !== void 0) {
        ref.current = value;
      }
    });
  }
}

function remove(vNode, parentDOM) {
  unmount(vNode);
  removeVNodeDOM(vNode, parentDOM);
}

function unmount(vNode) {
  var flags = vNode.flags;
  var children = vNode.children;
  var ref;

  if (flags & 481
  /* Element */
  ) {
    ref = vNode.ref;
    var props = vNode.props;
    unmountRef(ref);
    var childFlags = vNode.childFlags;

    if (!isNull(props)) {
      var keys = Object.keys(props);

      for (var i = 0, len = keys.length; i < len; i++) {
        var key = keys[i];

        if (syntheticEvents[key]) {
          unmountSyntheticEvent(key, vNode.dom);
        }
      }
    }

    if (childFlags & 12
    /* MultipleChildren */
    ) {
      unmountAllChildren(children);
    } else if (childFlags === 2
    /* HasVNodeChildren */
    ) {
      unmount(children);
    }
  } else if (children) {
    if (flags & 4
    /* ComponentClass */
    ) {
      if (isFunction(children.componentWillUnmount)) {
        children.componentWillUnmount();
      }

      unmountRef(vNode.ref);
      children.$UN = true;
      unmount(children.$LI);
    } else if (flags & 8
    /* ComponentFunction */
    ) {
      ref = vNode.ref;

      if (!isNullOrUndef(ref) && isFunction(ref.onComponentWillUnmount)) {
        ref.onComponentWillUnmount(findDOMfromVNode(vNode, true), vNode.props || EMPTY_OBJ);
      }

      unmount(children);
    } else if (flags & 1024
    /* Portal */
    ) {
      remove(children, vNode.ref);
    } else if (flags & 8192
    /* Fragment */
    ) {
      if (vNode.childFlags & 12
      /* MultipleChildren */
      ) {
        unmountAllChildren(children);
      }
    }
  }
}

function unmountAllChildren(children) {
  for (var i = 0, len = children.length; i < len; ++i) {
    unmount(children[i]);
  }
}

function clearDOM(dom) {
  // Optimization for clearing dom
  dom.textContent = '';
}

function removeAllChildren(dom, vNode, children) {
  unmountAllChildren(children);

  if (vNode.flags & 8192
  /* Fragment */
  ) {
    removeVNodeDOM(vNode, dom);
  } else {
    clearDOM(dom);
  }
}

function wrapLinkEvent(nextValue) {
  // This variable makes sure there is no "this" context in callback
  var ev = nextValue.event;
  return function (e) {
    ev(nextValue.data, e);
  };
}

function patchEvent(name, lastValue, nextValue, dom) {
  if (isLinkEventObject(nextValue)) {
    if (isLastValueSameLinkEvent(lastValue, nextValue)) {
      return;
    }

    nextValue = wrapLinkEvent(nextValue);
  }

  attachEvent(dom, normalizeEventName(name), nextValue);
} // We are assuming here that we come from patchProp routine
// -nextAttrValue cannot be null or undefined


function patchStyle(lastAttrValue, nextAttrValue, dom) {
  if (isNullOrUndef(nextAttrValue)) {
    dom.removeAttribute('style');
    return;
  }

  var domStyle = dom.style;
  var style;
  var value;

  if (isString(nextAttrValue)) {
    domStyle.cssText = nextAttrValue;
    return;
  }

  if (!isNullOrUndef(lastAttrValue) && !isString(lastAttrValue)) {
    for (style in nextAttrValue) {
      // do not add a hasOwnProperty check here, it affects performance
      value = nextAttrValue[style];

      if (value !== lastAttrValue[style]) {
        domStyle.setProperty(style, value);
      }
    }

    for (style in lastAttrValue) {
      if (isNullOrUndef(nextAttrValue[style])) {
        domStyle.removeProperty(style);
      }
    }
  } else {
    for (style in nextAttrValue) {
      value = nextAttrValue[style];
      domStyle.setProperty(style, value);
    }
  }
}

function patchDangerInnerHTML(lastValue, nextValue, lastVNode, dom) {
  var lastHtml = lastValue && lastValue.__html || '';
  var nextHtml = nextValue && nextValue.__html || '';

  if (lastHtml !== nextHtml) {
    if (!isNullOrUndef(nextHtml) && !isSameInnerHTML(dom, nextHtml)) {
      if (!isNull(lastVNode)) {
        if (lastVNode.childFlags & 12
        /* MultipleChildren */
        ) {
          unmountAllChildren(lastVNode.children);
        } else if (lastVNode.childFlags === 2
        /* HasVNodeChildren */
        ) {
          unmount(lastVNode.children);
        }

        lastVNode.children = null;
        lastVNode.childFlags = 1
        /* HasInvalidChildren */
        ;
      }

      dom.innerHTML = nextHtml;
    }
  }
}

function patchProp(prop, lastValue, nextValue, dom, isSVG, hasControlledValue, lastVNode) {
  switch (prop) {
    case 'children':
    case 'childrenType':
    case 'className':
    case 'defaultValue':
    case 'key':
    case 'multiple':
    case 'ref':
    case 'selectedIndex':
      break;

    case 'autoFocus':
      dom.autofocus = !!nextValue;
      break;

    case 'allowfullscreen':
    case 'autoplay':
    case 'capture':
    case 'checked':
    case 'controls':
    case 'default':
    case 'disabled':
    case 'hidden':
    case 'indeterminate':
    case 'loop':
    case 'muted':
    case 'novalidate':
    case 'open':
    case 'readOnly':
    case 'required':
    case 'reversed':
    case 'scoped':
    case 'seamless':
    case 'selected':
      dom[prop] = !!nextValue;
      break;

    case 'defaultChecked':
    case 'value':
    case 'volume':
      if (hasControlledValue && prop === 'value') {
        break;
      }

      var value = isNullOrUndef(nextValue) ? '' : nextValue;

      if (dom[prop] !== value) {
        dom[prop] = value;
      }

      break;

    case 'style':
      patchStyle(lastValue, nextValue, dom);
      break;

    case 'dangerouslySetInnerHTML':
      patchDangerInnerHTML(lastValue, nextValue, lastVNode, dom);
      break;

    default:
      if (syntheticEvents[prop]) {
        handleSyntheticEvent(prop, lastValue, nextValue, dom);
      } else if (prop.charCodeAt(0) === 111 && prop.charCodeAt(1) === 110) {
        patchEvent(prop, lastValue, nextValue, dom);
      } else if (isNullOrUndef(nextValue)) {
        dom.removeAttribute(prop);
      } else if (isSVG && namespaces[prop]) {
        // We optimize for isSVG being false
        // If we end up in this path we can read property again
        dom.setAttributeNS(namespaces[prop], prop, nextValue);
      } else {
        dom.setAttribute(prop, nextValue);
      }

      break;
  }
}

function mountProps(vNode, flags, props, dom, isSVG) {
  var hasControlledValue = false;
  var isFormElement = (flags & 448
  /* FormElement */
  ) > 0;

  if (isFormElement) {
    hasControlledValue = isControlledFormElement(props);

    if (hasControlledValue) {
      addFormElementEventHandlers(flags, dom, props);
    }
  }

  for (var prop in props) {
    // do not add a hasOwnProperty check here, it affects performance
    patchProp(prop, null, props[prop], dom, isSVG, hasControlledValue, null);
  }

  if (isFormElement) {
    processElement(flags, vNode, dom, props, true, hasControlledValue);
  }
}

function renderNewInput(instance, props, context) {
  var nextInput = normalizeRoot(instance.render(props, instance.state, context));
  var childContext = context;

  if (isFunction(instance.getChildContext)) {
    childContext = combineFrom(context, instance.getChildContext());
  }

  instance.$CX = childContext;
  return nextInput;
}

function createClassComponentInstance(vNode, Component, props, context, isSVG, lifecycle) {
  var instance = new Component(props, context);
  var usesNewAPI = instance.$N = Boolean(Component.getDerivedStateFromProps || instance.getSnapshotBeforeUpdate);
  instance.$SVG = isSVG;
  instance.$L = lifecycle;
  vNode.children = instance;
  instance.$BS = false;
  instance.context = context;

  if (instance.props === EMPTY_OBJ) {
    instance.props = props;
  }

  if (!usesNewAPI) {
    if (isFunction(instance.componentWillMount)) {
      instance.$BR = true;
      instance.componentWillMount();
      var pending = instance.$PS;

      if (!isNull(pending)) {
        var state = instance.state;

        if (isNull(state)) {
          instance.state = pending;
        } else {
          for (var key in pending) {
            state[key] = pending[key];
          }
        }

        instance.$PS = null;
      }

      instance.$BR = false;
    }
  } else {
    instance.state = createDerivedState(instance, props, instance.state);
  }

  instance.$LI = renderNewInput(instance, props, context);
  return instance;
}

function renderFunctionalComponent(vNode, context) {
  var props = vNode.props || EMPTY_OBJ;
  return vNode.flags & 32768
  /* ForwardRef */
  ? vNode.type.render(props, vNode.ref, context) : vNode.type(props, context);
}

function mount(vNode, parentDOM, context, isSVG, nextNode, lifecycle) {
  var flags = vNode.flags |= 16384
  /* InUse */
  ;

  if (flags & 481
  /* Element */
  ) {
    mountElement(vNode, parentDOM, context, isSVG, nextNode, lifecycle);
  } else if (flags & 4
  /* ComponentClass */
  ) {
    mountClassComponent(vNode, parentDOM, context, isSVG, nextNode, lifecycle);
  } else if (flags & 8
  /* ComponentFunction */
  ) {
    mountFunctionalComponent(vNode, parentDOM, context, isSVG, nextNode, lifecycle);
    mountFunctionalComponentCallbacks(vNode, lifecycle);
  } else if (flags & 512
  /* Void */
  || flags & 16
  /* Text */
  ) {
    mountText(vNode, parentDOM, nextNode);
  } else if (flags & 8192
  /* Fragment */
  ) {
    mountFragment(vNode, context, parentDOM, isSVG, nextNode, lifecycle);
  } else if (flags & 1024
  /* Portal */
  ) {
    mountPortal(vNode, context, parentDOM, nextNode, lifecycle);
  } else ;
}

function mountPortal(vNode, context, parentDOM, nextNode, lifecycle) {
  mount(vNode.children, vNode.ref, context, false, null, lifecycle);
  var placeHolderVNode = createVoidVNode();
  mountText(placeHolderVNode, parentDOM, nextNode);
  vNode.dom = placeHolderVNode.dom;
}

function mountFragment(vNode, context, parentDOM, isSVG, nextNode, lifecycle) {
  var children = vNode.children;
  var childFlags = vNode.childFlags; // When fragment is optimized for multiple children, check if there is no children and change flag to invalid
  // This is the only normalization always done, to keep optimization flags API same for fragments and regular elements

  if (childFlags & 12
  /* MultipleChildren */
  && children.length === 0) {
    childFlags = vNode.childFlags = 2
    /* HasVNodeChildren */
    ;
    children = vNode.children = createVoidVNode();
  }

  if (childFlags === 2
  /* HasVNodeChildren */
  ) {
    mount(children, parentDOM, nextNode, isSVG, nextNode, lifecycle);
  } else {
    mountArrayChildren(children, parentDOM, context, isSVG, nextNode, lifecycle);
  }
}

function mountText(vNode, parentDOM, nextNode) {
  var dom = vNode.dom = document.createTextNode(vNode.children);

  if (!isNull(parentDOM)) {
    insertOrAppend(parentDOM, dom, nextNode);
  }
}

function mountElement(vNode, parentDOM, context, isSVG, nextNode, lifecycle) {
  var flags = vNode.flags;
  var props = vNode.props;
  var className = vNode.className;
  var childFlags = vNode.childFlags;
  var dom = vNode.dom = documentCreateElement(vNode.type, isSVG = isSVG || (flags & 32
  /* SvgElement */
  ) > 0);
  var children = vNode.children;

  if (!isNullOrUndef(className) && className !== '') {
    if (isSVG) {
      dom.setAttribute('class', className);
    } else {
      dom.className = className;
    }
  }

  if (childFlags === 16
  /* HasTextChildren */
  ) {
    setTextContent(dom, children);
  } else if (childFlags !== 1
  /* HasInvalidChildren */
  ) {
    var childrenIsSVG = isSVG && vNode.type !== 'foreignObject';

    if (childFlags === 2
    /* HasVNodeChildren */
    ) {
      if (children.flags & 16384
      /* InUse */
      ) {
        vNode.children = children = directClone(children);
      }

      mount(children, dom, context, childrenIsSVG, null, lifecycle);
    } else if (childFlags === 8
    /* HasKeyedChildren */
    || childFlags === 4
    /* HasNonKeyedChildren */
    ) {
      mountArrayChildren(children, dom, context, childrenIsSVG, null, lifecycle);
    }
  }

  if (!isNull(parentDOM)) {
    insertOrAppend(parentDOM, dom, nextNode);
  }

  if (!isNull(props)) {
    mountProps(vNode, flags, props, dom, isSVG);
  }

  mountRef(vNode.ref, dom, lifecycle);
}

function mountArrayChildren(children, dom, context, isSVG, nextNode, lifecycle) {
  for (var i = 0; i < children.length; ++i) {
    var child = children[i];

    if (child.flags & 16384
    /* InUse */
    ) {
      children[i] = child = directClone(child);
    }

    mount(child, dom, context, isSVG, nextNode, lifecycle);
  }
}

function mountClassComponent(vNode, parentDOM, context, isSVG, nextNode, lifecycle) {
  var instance = createClassComponentInstance(vNode, vNode.type, vNode.props || EMPTY_OBJ, context, isSVG, lifecycle);
  mount(instance.$LI, parentDOM, instance.$CX, isSVG, nextNode, lifecycle);
  mountClassComponentCallbacks(vNode.ref, instance, lifecycle);
}

function mountFunctionalComponent(vNode, parentDOM, context, isSVG, nextNode, lifecycle) {
  mount(vNode.children = normalizeRoot(renderFunctionalComponent(vNode, context)), parentDOM, context, isSVG, nextNode, lifecycle);
}

function createClassMountCallback(instance) {
  return function () {
    instance.componentDidMount();
  };
}

function mountClassComponentCallbacks(ref, instance, lifecycle) {
  mountRef(ref, instance, lifecycle);

  if (isFunction(instance.componentDidMount)) {
    lifecycle.push(createClassMountCallback(instance));
  }
}

function createOnMountCallback(ref, vNode) {
  return function () {
    ref.onComponentDidMount(findDOMfromVNode(vNode, true), vNode.props || EMPTY_OBJ);
  };
}

function mountFunctionalComponentCallbacks(vNode, lifecycle) {
  var ref = vNode.ref;

  if (!isNullOrUndef(ref)) {
    safeCall1(ref.onComponentWillMount, vNode.props || EMPTY_OBJ);

    if (isFunction(ref.onComponentDidMount)) {
      lifecycle.push(createOnMountCallback(ref, vNode));
    }
  }
}

function replaceWithNewNode(lastVNode, nextVNode, parentDOM, context, isSVG, lifecycle) {
  unmount(lastVNode);

  if ((nextVNode.flags & lastVNode.flags & 2033
  /* DOMRef */
  ) !== 0) {
    mount(nextVNode, null, context, isSVG, null, lifecycle); // Single DOM operation, when we have dom references available

    replaceChild(parentDOM, nextVNode.dom, lastVNode.dom);
  } else {
    mount(nextVNode, parentDOM, context, isSVG, findDOMfromVNode(lastVNode, true), lifecycle);
    removeVNodeDOM(lastVNode, parentDOM);
  }
}

function patch(lastVNode, nextVNode, parentDOM, context, isSVG, nextNode, lifecycle) {
  var nextFlags = nextVNode.flags |= 16384
  /* InUse */
  ;

  if (lastVNode.flags !== nextFlags || lastVNode.type !== nextVNode.type || lastVNode.key !== nextVNode.key || nextFlags & 2048
  /* ReCreate */
  ) {
    if (lastVNode.flags & 16384
    /* InUse */
    ) {
      replaceWithNewNode(lastVNode, nextVNode, parentDOM, context, isSVG, lifecycle);
    } else {
      // Last vNode is not in use, it has crashed at application level. Just mount nextVNode and ignore last one
      mount(nextVNode, parentDOM, context, isSVG, nextNode, lifecycle);
    }
  } else if (nextFlags & 481
  /* Element */
  ) {
    patchElement(lastVNode, nextVNode, context, isSVG, nextFlags, lifecycle);
  } else if (nextFlags & 4
  /* ComponentClass */
  ) {
    patchClassComponent(lastVNode, nextVNode, parentDOM, context, isSVG, nextNode, lifecycle);
  } else if (nextFlags & 8
  /* ComponentFunction */
  ) {
    patchFunctionalComponent(lastVNode, nextVNode, parentDOM, context, isSVG, nextNode, lifecycle);
  } else if (nextFlags & 16
  /* Text */
  ) {
    patchText(lastVNode, nextVNode);
  } else if (nextFlags & 512
  /* Void */
  ) {
    nextVNode.dom = lastVNode.dom;
  } else if (nextFlags & 8192
  /* Fragment */
  ) {
    patchFragment(lastVNode, nextVNode, parentDOM, context, isSVG, lifecycle);
  } else {
    patchPortal(lastVNode, nextVNode, context, lifecycle);
  }
}

function patchSingleTextChild(lastChildren, nextChildren, parentDOM) {
  if (lastChildren !== nextChildren) {
    if (lastChildren !== '') {
      parentDOM.firstChild.nodeValue = nextChildren;
    } else {
      setTextContent(parentDOM, nextChildren);
    }
  }
}

function patchContentEditableChildren(dom, nextChildren) {
  if (dom.textContent !== nextChildren) {
    dom.textContent = nextChildren;
  }
}

function patchFragment(lastVNode, nextVNode, parentDOM, context, isSVG, lifecycle) {
  var lastChildren = lastVNode.children;
  var nextChildren = nextVNode.children;
  var lastChildFlags = lastVNode.childFlags;
  var nextChildFlags = nextVNode.childFlags;
  var nextNode = null; // When fragment is optimized for multiple children, check if there is no children and change flag to invalid
  // This is the only normalization always done, to keep optimization flags API same for fragments and regular elements

  if (nextChildFlags & 12
  /* MultipleChildren */
  && nextChildren.length === 0) {
    nextChildFlags = nextVNode.childFlags = 2
    /* HasVNodeChildren */
    ;
    nextChildren = nextVNode.children = createVoidVNode();
  }

  var nextIsSingle = (nextChildFlags & 2
  /* HasVNodeChildren */
  ) !== 0;

  if (lastChildFlags & 12
  /* MultipleChildren */
  ) {
    var lastLen = lastChildren.length; // We need to know Fragment's edge node when

    if ( // It uses keyed algorithm
    lastChildFlags & 8
    /* HasKeyedChildren */
    && nextChildFlags & 8
    /* HasKeyedChildren */
    || // It transforms from many to single
    nextIsSingle || !nextIsSingle && nextChildren.length > lastLen) {
      // When fragment has multiple children there is always at least one vNode
      nextNode = findDOMfromVNode(lastChildren[lastLen - 1], false).nextSibling;
    }
  }

  patchChildren(lastChildFlags, nextChildFlags, lastChildren, nextChildren, parentDOM, context, isSVG, nextNode, lastVNode, lifecycle);
}

function patchPortal(lastVNode, nextVNode, context, lifecycle) {
  var lastContainer = lastVNode.ref;
  var nextContainer = nextVNode.ref;
  var nextChildren = nextVNode.children;
  patchChildren(lastVNode.childFlags, nextVNode.childFlags, lastVNode.children, nextChildren, lastContainer, context, false, null, lastVNode, lifecycle);
  nextVNode.dom = lastVNode.dom;

  if (lastContainer !== nextContainer && !isInvalid(nextChildren)) {
    var node = nextChildren.dom;
    removeChild(lastContainer, node);
    appendChild(nextContainer, node);
  }
}

function patchElement(lastVNode, nextVNode, context, isSVG, nextFlags, lifecycle) {
  var dom = nextVNode.dom = lastVNode.dom;
  var lastProps = lastVNode.props;
  var nextProps = nextVNode.props;
  var isFormElement = false;
  var hasControlledValue = false;
  var nextPropsOrEmpty;
  isSVG = isSVG || (nextFlags & 32
  /* SvgElement */
  ) > 0; // inlined patchProps  -- starts --

  if (lastProps !== nextProps) {
    var lastPropsOrEmpty = lastProps || EMPTY_OBJ;
    nextPropsOrEmpty = nextProps || EMPTY_OBJ;

    if (nextPropsOrEmpty !== EMPTY_OBJ) {
      isFormElement = (nextFlags & 448
      /* FormElement */
      ) > 0;

      if (isFormElement) {
        hasControlledValue = isControlledFormElement(nextPropsOrEmpty);
      }

      for (var prop in nextPropsOrEmpty) {
        var lastValue = lastPropsOrEmpty[prop];
        var nextValue = nextPropsOrEmpty[prop];

        if (lastValue !== nextValue) {
          patchProp(prop, lastValue, nextValue, dom, isSVG, hasControlledValue, lastVNode);
        }
      }
    }

    if (lastPropsOrEmpty !== EMPTY_OBJ) {
      for (var prop$1 in lastPropsOrEmpty) {
        if (isNullOrUndef(nextPropsOrEmpty[prop$1]) && !isNullOrUndef(lastPropsOrEmpty[prop$1])) {
          patchProp(prop$1, lastPropsOrEmpty[prop$1], null, dom, isSVG, hasControlledValue, lastVNode);
        }
      }
    }
  }

  var nextChildren = nextVNode.children;
  var nextClassName = nextVNode.className; // inlined patchProps  -- ends --

  if (lastVNode.className !== nextClassName) {
    if (isNullOrUndef(nextClassName)) {
      dom.removeAttribute('class');
    } else if (isSVG) {
      dom.setAttribute('class', nextClassName);
    } else {
      dom.className = nextClassName;
    }
  }

  if (nextFlags & 4096
  /* ContentEditable */
  ) {
    patchContentEditableChildren(dom, nextChildren);
  } else {
    patchChildren(lastVNode.childFlags, nextVNode.childFlags, lastVNode.children, nextChildren, dom, context, isSVG && nextVNode.type !== 'foreignObject', null, lastVNode, lifecycle);
  }

  if (isFormElement) {
    processElement(nextFlags, nextVNode, dom, nextPropsOrEmpty, false, hasControlledValue);
  }

  var nextRef = nextVNode.ref;
  var lastRef = lastVNode.ref;

  if (lastRef !== nextRef) {
    unmountRef(lastRef);
    mountRef(nextRef, dom, lifecycle);
  }
}

function replaceOneVNodeWithMultipleVNodes(lastChildren, nextChildren, parentDOM, context, isSVG, lifecycle) {
  unmount(lastChildren);
  mountArrayChildren(nextChildren, parentDOM, context, isSVG, findDOMfromVNode(lastChildren, true), lifecycle);
  removeVNodeDOM(lastChildren, parentDOM);
}

function patchChildren(lastChildFlags, nextChildFlags, lastChildren, nextChildren, parentDOM, context, isSVG, nextNode, parentVNode, lifecycle) {
  switch (lastChildFlags) {
    case 2
    /* HasVNodeChildren */
    :
      switch (nextChildFlags) {
        case 2
        /* HasVNodeChildren */
        :
          patch(lastChildren, nextChildren, parentDOM, context, isSVG, nextNode, lifecycle);
          break;

        case 1
        /* HasInvalidChildren */
        :
          remove(lastChildren, parentDOM);
          break;

        case 16
        /* HasTextChildren */
        :
          unmount(lastChildren);
          setTextContent(parentDOM, nextChildren);
          break;

        default:
          replaceOneVNodeWithMultipleVNodes(lastChildren, nextChildren, parentDOM, context, isSVG, lifecycle);
          break;
      }

      break;

    case 1
    /* HasInvalidChildren */
    :
      switch (nextChildFlags) {
        case 2
        /* HasVNodeChildren */
        :
          mount(nextChildren, parentDOM, context, isSVG, nextNode, lifecycle);
          break;

        case 1
        /* HasInvalidChildren */
        :
          break;

        case 16
        /* HasTextChildren */
        :
          setTextContent(parentDOM, nextChildren);
          break;

        default:
          mountArrayChildren(nextChildren, parentDOM, context, isSVG, nextNode, lifecycle);
          break;
      }

      break;

    case 16
    /* HasTextChildren */
    :
      switch (nextChildFlags) {
        case 16
        /* HasTextChildren */
        :
          patchSingleTextChild(lastChildren, nextChildren, parentDOM);
          break;

        case 2
        /* HasVNodeChildren */
        :
          clearDOM(parentDOM);
          mount(nextChildren, parentDOM, context, isSVG, nextNode, lifecycle);
          break;

        case 1
        /* HasInvalidChildren */
        :
          clearDOM(parentDOM);
          break;

        default:
          clearDOM(parentDOM);
          mountArrayChildren(nextChildren, parentDOM, context, isSVG, nextNode, lifecycle);
          break;
      }

      break;

    default:
      switch (nextChildFlags) {
        case 16
        /* HasTextChildren */
        :
          unmountAllChildren(lastChildren);
          setTextContent(parentDOM, nextChildren);
          break;

        case 2
        /* HasVNodeChildren */
        :
          removeAllChildren(parentDOM, parentVNode, lastChildren);
          mount(nextChildren, parentDOM, context, isSVG, nextNode, lifecycle);
          break;

        case 1
        /* HasInvalidChildren */
        :
          removeAllChildren(parentDOM, parentVNode, lastChildren);
          break;

        default:
          var lastLength = lastChildren.length | 0;
          var nextLength = nextChildren.length | 0; // Fast path's for both algorithms

          if (lastLength === 0) {
            if (nextLength > 0) {
              mountArrayChildren(nextChildren, parentDOM, context, isSVG, nextNode, lifecycle);
            }
          } else if (nextLength === 0) {
            removeAllChildren(parentDOM, parentVNode, lastChildren);
          } else if (nextChildFlags === 8
          /* HasKeyedChildren */
          && lastChildFlags === 8
          /* HasKeyedChildren */
          ) {
            patchKeyedChildren(lastChildren, nextChildren, parentDOM, context, isSVG, lastLength, nextLength, nextNode, parentVNode, lifecycle);
          } else {
            patchNonKeyedChildren(lastChildren, nextChildren, parentDOM, context, isSVG, lastLength, nextLength, nextNode, lifecycle);
          }

          break;
      }

      break;
  }
}

function createDidUpdate(instance, lastProps, lastState, snapshot, lifecycle) {
  lifecycle.push(function () {
    instance.componentDidUpdate(lastProps, lastState, snapshot);
  });
}

function updateClassComponent(instance, nextState, nextProps, parentDOM, context, isSVG, force, nextNode, lifecycle) {
  var lastState = instance.state;
  var lastProps = instance.props;
  var usesNewAPI = Boolean(instance.$N);
  var hasSCU = isFunction(instance.shouldComponentUpdate);

  if (usesNewAPI) {
    nextState = createDerivedState(instance, nextProps, nextState !== lastState ? combineFrom(lastState, nextState) : nextState);
  }

  if (force || !hasSCU || hasSCU && instance.shouldComponentUpdate(nextProps, nextState, context)) {
    if (!usesNewAPI && isFunction(instance.componentWillUpdate)) {
      instance.componentWillUpdate(nextProps, nextState, context);
    }

    instance.props = nextProps;
    instance.state = nextState;
    instance.context = context;
    var snapshot = null;
    var nextInput = renderNewInput(instance, nextProps, context);

    if (usesNewAPI && isFunction(instance.getSnapshotBeforeUpdate)) {
      snapshot = instance.getSnapshotBeforeUpdate(lastProps, lastState);
    }

    patch(instance.$LI, nextInput, parentDOM, instance.$CX, isSVG, nextNode, lifecycle); // Dont update Last input, until patch has been succesfully executed

    instance.$LI = nextInput;

    if (isFunction(instance.componentDidUpdate)) {
      createDidUpdate(instance, lastProps, lastState, snapshot, lifecycle);
    }
  } else {
    instance.props = nextProps;
    instance.state = nextState;
    instance.context = context;
  }
}

function patchClassComponent(lastVNode, nextVNode, parentDOM, context, isSVG, nextNode, lifecycle) {
  var instance = nextVNode.children = lastVNode.children; // If Component has crashed, ignore it to stay functional

  if (isNull(instance)) {
    return;
  }

  instance.$L = lifecycle;
  var nextProps = nextVNode.props || EMPTY_OBJ;
  var nextRef = nextVNode.ref;
  var lastRef = lastVNode.ref;
  var nextState = instance.state;

  if (!instance.$N) {
    if (isFunction(instance.componentWillReceiveProps)) {
      instance.$BR = true;
      instance.componentWillReceiveProps(nextProps, context); // If instance component was removed during its own update do nothing.

      if (instance.$UN) {
        return;
      }

      instance.$BR = false;
    }

    if (!isNull(instance.$PS)) {
      nextState = combineFrom(nextState, instance.$PS);
      instance.$PS = null;
    }
  }

  updateClassComponent(instance, nextState, nextProps, parentDOM, context, isSVG, false, nextNode, lifecycle);

  if (lastRef !== nextRef) {
    unmountRef(lastRef);
    mountRef(nextRef, instance, lifecycle);
  }
}

function patchFunctionalComponent(lastVNode, nextVNode, parentDOM, context, isSVG, nextNode, lifecycle) {
  var shouldUpdate = true;
  var nextProps = nextVNode.props || EMPTY_OBJ;
  var nextRef = nextVNode.ref;
  var lastProps = lastVNode.props;
  var nextHooksDefined = !isNullOrUndef(nextRef);
  var lastInput = lastVNode.children;

  if (nextHooksDefined && isFunction(nextRef.onComponentShouldUpdate)) {
    shouldUpdate = nextRef.onComponentShouldUpdate(lastProps, nextProps);
  }

  if (shouldUpdate !== false) {
    if (nextHooksDefined && isFunction(nextRef.onComponentWillUpdate)) {
      nextRef.onComponentWillUpdate(lastProps, nextProps);
    }

    var nextInput = normalizeRoot(renderFunctionalComponent(nextVNode, context));
    patch(lastInput, nextInput, parentDOM, context, isSVG, nextNode, lifecycle);
    nextVNode.children = nextInput;

    if (nextHooksDefined && isFunction(nextRef.onComponentDidUpdate)) {
      nextRef.onComponentDidUpdate(lastProps, nextProps);
    }
  } else {
    nextVNode.children = lastInput;
  }
}

function patchText(lastVNode, nextVNode) {
  var nextText = nextVNode.children;
  var dom = nextVNode.dom = lastVNode.dom;

  if (nextText !== lastVNode.children) {
    dom.nodeValue = nextText;
  }
}

function patchNonKeyedChildren(lastChildren, nextChildren, dom, context, isSVG, lastChildrenLength, nextChildrenLength, nextNode, lifecycle) {
  var commonLength = lastChildrenLength > nextChildrenLength ? nextChildrenLength : lastChildrenLength;
  var i = 0;
  var nextChild;
  var lastChild;

  for (; i < commonLength; ++i) {
    nextChild = nextChildren[i];
    lastChild = lastChildren[i];

    if (nextChild.flags & 16384
    /* InUse */
    ) {
      nextChild = nextChildren[i] = directClone(nextChild);
    }

    patch(lastChild, nextChild, dom, context, isSVG, nextNode, lifecycle);
    lastChildren[i] = nextChild;
  }

  if (lastChildrenLength < nextChildrenLength) {
    for (i = commonLength; i < nextChildrenLength; ++i) {
      nextChild = nextChildren[i];

      if (nextChild.flags & 16384
      /* InUse */
      ) {
        nextChild = nextChildren[i] = directClone(nextChild);
      }

      mount(nextChild, dom, context, isSVG, nextNode, lifecycle);
    }
  } else if (lastChildrenLength > nextChildrenLength) {
    for (i = commonLength; i < lastChildrenLength; ++i) {
      remove(lastChildren[i], dom);
    }
  }
}

function patchKeyedChildren(a, b, dom, context, isSVG, aLength, bLength, outerEdge, parentVNode, lifecycle) {
  var aEnd = aLength - 1;
  var bEnd = bLength - 1;
  var j = 0;
  var aNode = a[j];
  var bNode = b[j];
  var nextPos;
  var nextNode; // Step 1
  // tslint:disable-next-line

  outer: {
    // Sync nodes with the same key at the beginning.
    while (aNode.key === bNode.key) {
      if (bNode.flags & 16384
      /* InUse */
      ) {
        b[j] = bNode = directClone(bNode);
      }

      patch(aNode, bNode, dom, context, isSVG, outerEdge, lifecycle);
      a[j] = bNode;
      ++j;

      if (j > aEnd || j > bEnd) {
        break outer;
      }

      aNode = a[j];
      bNode = b[j];
    }

    aNode = a[aEnd];
    bNode = b[bEnd]; // Sync nodes with the same key at the end.

    while (aNode.key === bNode.key) {
      if (bNode.flags & 16384
      /* InUse */
      ) {
        b[bEnd] = bNode = directClone(bNode);
      }

      patch(aNode, bNode, dom, context, isSVG, outerEdge, lifecycle);
      a[aEnd] = bNode;
      aEnd--;
      bEnd--;

      if (j > aEnd || j > bEnd) {
        break outer;
      }

      aNode = a[aEnd];
      bNode = b[bEnd];
    }
  }

  if (j > aEnd) {
    if (j <= bEnd) {
      nextPos = bEnd + 1;
      nextNode = nextPos < bLength ? findDOMfromVNode(b[nextPos], true) : outerEdge;

      while (j <= bEnd) {
        bNode = b[j];

        if (bNode.flags & 16384
        /* InUse */
        ) {
          b[j] = bNode = directClone(bNode);
        }

        ++j;
        mount(bNode, dom, context, isSVG, nextNode, lifecycle);
      }
    }
  } else if (j > bEnd) {
    while (j <= aEnd) {
      remove(a[j++], dom);
    }
  } else {
    patchKeyedChildrenComplex(a, b, context, aLength, bLength, aEnd, bEnd, j, dom, isSVG, outerEdge, parentVNode, lifecycle);
  }
}

function patchKeyedChildrenComplex(a, b, context, aLength, bLength, aEnd, bEnd, j, dom, isSVG, outerEdge, parentVNode, lifecycle) {
  var aNode;
  var bNode;
  var nextPos;
  var i = 0;
  var aStart = j;
  var bStart = j;
  var aLeft = aEnd - j + 1;
  var bLeft = bEnd - j + 1;
  var sources = new Int32Array(bLeft + 1); // Keep track if its possible to remove whole DOM using textContent = '';

  var canRemoveWholeContent = aLeft === aLength;
  var moved = false;
  var pos = 0;
  var patched = 0; // When sizes are small, just loop them through

  if (bLength < 4 || (aLeft | bLeft) < 32) {
    for (i = aStart; i <= aEnd; ++i) {
      aNode = a[i];

      if (patched < bLeft) {
        for (j = bStart; j <= bEnd; j++) {
          bNode = b[j];

          if (aNode.key === bNode.key) {
            sources[j - bStart] = i + 1;

            if (canRemoveWholeContent) {
              canRemoveWholeContent = false;

              while (aStart < i) {
                remove(a[aStart++], dom);
              }
            }

            if (pos > j) {
              moved = true;
            } else {
              pos = j;
            }

            if (bNode.flags & 16384
            /* InUse */
            ) {
              b[j] = bNode = directClone(bNode);
            }

            patch(aNode, bNode, dom, context, isSVG, outerEdge, lifecycle);
            ++patched;
            break;
          }
        }

        if (!canRemoveWholeContent && j > bEnd) {
          remove(aNode, dom);
        }
      } else if (!canRemoveWholeContent) {
        remove(aNode, dom);
      }
    }
  } else {
    var keyIndex = {}; // Map keys by their index

    for (i = bStart; i <= bEnd; ++i) {
      keyIndex[b[i].key] = i;
    } // Try to patch same keys


    for (i = aStart; i <= aEnd; ++i) {
      aNode = a[i];

      if (patched < bLeft) {
        j = keyIndex[aNode.key];

        if (j !== void 0) {
          if (canRemoveWholeContent) {
            canRemoveWholeContent = false;

            while (i > aStart) {
              remove(a[aStart++], dom);
            }
          }

          sources[j - bStart] = i + 1;

          if (pos > j) {
            moved = true;
          } else {
            pos = j;
          }

          bNode = b[j];

          if (bNode.flags & 16384
          /* InUse */
          ) {
            b[j] = bNode = directClone(bNode);
          }

          patch(aNode, bNode, dom, context, isSVG, outerEdge, lifecycle);
          ++patched;
        } else if (!canRemoveWholeContent) {
          remove(aNode, dom);
        }
      } else if (!canRemoveWholeContent) {
        remove(aNode, dom);
      }
    }
  } // fast-path: if nothing patched remove all old and add all new


  if (canRemoveWholeContent) {
    removeAllChildren(dom, parentVNode, a);
    mountArrayChildren(b, dom, context, isSVG, outerEdge, lifecycle);
  } else if (moved) {
    var seq = lis_algorithm(sources);
    j = seq.length - 1;

    for (i = bLeft - 1; i >= 0; i--) {
      if (sources[i] === 0) {
        pos = i + bStart;
        bNode = b[pos];

        if (bNode.flags & 16384
        /* InUse */
        ) {
          b[pos] = bNode = directClone(bNode);
        }

        nextPos = pos + 1;
        mount(bNode, dom, context, isSVG, nextPos < bLength ? findDOMfromVNode(b[nextPos], true) : outerEdge, lifecycle);
      } else if (j < 0 || i !== seq[j]) {
        pos = i + bStart;
        bNode = b[pos];
        nextPos = pos + 1;
        moveVNodeDOM(bNode, dom, nextPos < bLength ? findDOMfromVNode(b[nextPos], true) : outerEdge);
      } else {
        j--;
      }
    }
  } else if (patched !== bLeft) {
    // when patched count doesn't match b length we need to insert those new ones
    // loop backwards so we can use insertBefore
    for (i = bLeft - 1; i >= 0; i--) {
      if (sources[i] === 0) {
        pos = i + bStart;
        bNode = b[pos];

        if (bNode.flags & 16384
        /* InUse */
        ) {
          b[pos] = bNode = directClone(bNode);
        }

        nextPos = pos + 1;
        mount(bNode, dom, context, isSVG, nextPos < bLength ? findDOMfromVNode(b[nextPos], true) : outerEdge, lifecycle);
      }
    }
  }
}

var result;
var p;
var maxLen = 0; // https://en.wikipedia.org/wiki/Longest_increasing_subsequence

function lis_algorithm(arr) {
  var arrI = 0;
  var i = 0;
  var j = 0;
  var k = 0;
  var u = 0;
  var v = 0;
  var c = 0;
  var len = arr.length;

  if (len > maxLen) {
    maxLen = len;
    result = new Int32Array(len);
    p = new Int32Array(len);
  }

  for (; i < len; ++i) {
    arrI = arr[i];

    if (arrI !== 0) {
      j = result[k];

      if (arr[j] < arrI) {
        p[i] = j;
        result[++k] = i;
        continue;
      }

      u = 0;
      v = k;

      while (u < v) {
        c = u + v >> 1;

        if (arr[result[c]] < arrI) {
          u = c + 1;
        } else {
          v = c;
        }
      }

      if (arrI < arr[result[u]]) {
        if (u > 0) {
          p[i] = result[u - 1];
        }

        result[u] = i;
      }
    }
  }

  u = k + 1;
  var seq = new Int32Array(u);
  v = result[u - 1];

  while (u-- > 0) {
    seq[u] = v;
    v = p[v];
    result[u] = 0;
  }

  return seq;
}

var hasDocumentAvailable = typeof document !== 'undefined';

if (hasDocumentAvailable) {
  /*
   * Defining $EV and $V properties on Node.prototype
   * fixes v8 "wrong map" de-optimization
   */
  if (window.Node) {
    Node.prototype.$EV = null;
    Node.prototype.$V = null;
  }
}

function __render(input, parentDOM, callback, context) {
  var lifecycle = [];
  var rootInput = parentDOM.$V;
  renderCheck.v = true;

  if (isNullOrUndef(rootInput)) {
    if (!isNullOrUndef(input)) {
      if (input.flags & 16384
      /* InUse */
      ) {
        input = directClone(input);
      }

      mount(input, parentDOM, context, false, null, lifecycle);
      parentDOM.$V = input;
      rootInput = input;
    }
  } else {
    if (isNullOrUndef(input)) {
      remove(rootInput, parentDOM);
      parentDOM.$V = null;
    } else {
      if (input.flags & 16384
      /* InUse */
      ) {
        input = directClone(input);
      }

      patch(rootInput, input, parentDOM, context, false, null, lifecycle);
      rootInput = parentDOM.$V = input;
    }
  }

  callAll(lifecycle);
  renderCheck.v = false;

  if (isFunction(callback)) {
    callback();
  }

  if (isFunction(options.renderComplete)) {
    options.renderComplete(rootInput, parentDOM);
  }
}

function render(input, parentDOM, callback, context) {
  if (callback === void 0) callback = null;
  if (context === void 0) context = EMPTY_OBJ;

  __render(input, parentDOM, callback, context);
}

function createRenderer(parentDOM) {
  return function () {
    function renderer(lastInput, nextInput, callback, context) {
      if (!parentDOM) {
        parentDOM = lastInput;
      }

      render(nextInput, parentDOM, callback, context);
    }

    return renderer;
  }();
}

var QUEUE = [];
var nextTick = typeof Promise !== 'undefined' ? Promise.resolve().then.bind(Promise.resolve()) : function (a) {
  window.setTimeout(a, 0);
};
var microTaskPending = false;

function queueStateChanges(component, newState, callback, force) {
  var pending = component.$PS;

  if (isFunction(newState)) {
    newState = newState(pending ? combineFrom(component.state, pending) : component.state, component.props, component.context);
  }

  if (isNullOrUndef(pending)) {
    component.$PS = newState;
  } else {
    for (var stateKey in newState) {
      pending[stateKey] = newState[stateKey];
    }
  }

  if (!component.$BR) {
    if (!renderCheck.v) {
      if (QUEUE.length === 0) {
        applyState(component, force);

        if (isFunction(callback)) {
          callback.call(component);
        }

        return;
      }
    }

    if (QUEUE.indexOf(component) === -1) {
      QUEUE.push(component);
    }

    if (force) {
      component.$F = true;
    }

    if (!microTaskPending) {
      microTaskPending = true;
      nextTick(rerender);
    }

    if (isFunction(callback)) {
      var QU = component.$QU;

      if (!QU) {
        QU = component.$QU = [];
      }

      QU.push(callback);
    }
  } else if (isFunction(callback)) {
    component.$L.push(callback.bind(component));
  }
}

function callSetStateCallbacks(component) {
  var queue = component.$QU;

  for (var i = 0; i < queue.length; ++i) {
    queue[i].call(component);
  }

  component.$QU = null;
}

function rerender() {
  var component;
  microTaskPending = false;

  while (component = QUEUE.shift()) {
    if (!component.$UN) {
      var force = component.$F;
      component.$F = false;
      applyState(component, force);

      if (component.$QU) {
        callSetStateCallbacks(component);
      }
    }
  }
}

function applyState(component, force) {
  if (force || !component.$BR) {
    var pendingState = component.$PS;
    component.$PS = null;
    var lifecycle = [];
    renderCheck.v = true;
    updateClassComponent(component, combineFrom(component.state, pendingState), component.props, findDOMfromVNode(component.$LI, true).parentNode, component.context, component.$SVG, force, null, lifecycle);
    callAll(lifecycle);
    renderCheck.v = false;
  } else {
    component.state = component.$PS;
    component.$PS = null;
  }
}

var Component = function () {
  function Component(props, context) {
    // Public
    this.state = null; // Internal properties

    this.$BR = false; // BLOCK RENDER

    this.$BS = true; // BLOCK STATE

    this.$PS = null; // PENDING STATE (PARTIAL or FULL)

    this.$LI = null; // LAST INPUT

    this.$UN = false; // UNMOUNTED

    this.$CX = null; // CHILDCONTEXT

    this.$QU = null; // QUEUE

    this.$N = false; // Uses new lifecycle API Flag

    this.$L = null; // Current lifecycle of this component

    this.$SVG = false; // Flag to keep track if component is inside SVG tree

    this.$F = false; // Force update flag

    this.props = props || EMPTY_OBJ;
    this.context = context || EMPTY_OBJ; // context should not be mutable
  }

  return Component;
}();

exports.Component = Component;

Component.prototype.forceUpdate = function () {
  function forceUpdate(callback) {
    if (this.$UN) {
      return;
    } // Do not allow double render during force update


    queueStateChanges(this, {}, callback, true);
  }

  return forceUpdate;
}();

Component.prototype.setState = function () {
  function setState(newState, callback) {
    if (this.$UN) {
      return;
    }

    if (!this.$BS) {
      queueStateChanges(this, newState, callback, false);
    }
  }

  return setState;
}();

Component.prototype.render = function () {
  function render(_nextProps, _nextState, _nextContext) {
    return null;
  }

  return render;
}();

var version = "7.4.8";
exports.version = version;

/***/ }),

/***/ 96323:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;

var _indexEsm = __webpack_require__(92376);

Object.keys(_indexEsm).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  if (key in exports && exports[key] === _indexEsm[key]) return;
  exports[key] = _indexEsm[key];
});

if (false) {}

/***/ }),

/***/ 10648:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.VNodeFlags = exports.ChildFlags = void 0;

/* If editing these values check babel-plugin-also */
var VNodeFlags;
exports.VNodeFlags = VNodeFlags;

(function (VNodeFlags) {
  /* First set of bits define shape of vNode */
  VNodeFlags[VNodeFlags["HtmlElement"] = 1] = "HtmlElement";
  VNodeFlags[VNodeFlags["ComponentUnknown"] = 2] = "ComponentUnknown";
  VNodeFlags[VNodeFlags["ComponentClass"] = 4] = "ComponentClass";
  VNodeFlags[VNodeFlags["ComponentFunction"] = 8] = "ComponentFunction";
  VNodeFlags[VNodeFlags["Text"] = 16] = "Text";
  /* Special flags */

  VNodeFlags[VNodeFlags["SvgElement"] = 32] = "SvgElement";
  VNodeFlags[VNodeFlags["InputElement"] = 64] = "InputElement";
  VNodeFlags[VNodeFlags["TextareaElement"] = 128] = "TextareaElement";
  VNodeFlags[VNodeFlags["SelectElement"] = 256] = "SelectElement";
  VNodeFlags[VNodeFlags["Void"] = 512] = "Void";
  VNodeFlags[VNodeFlags["Portal"] = 1024] = "Portal";
  VNodeFlags[VNodeFlags["ReCreate"] = 2048] = "ReCreate";
  VNodeFlags[VNodeFlags["ContentEditable"] = 4096] = "ContentEditable";
  VNodeFlags[VNodeFlags["Fragment"] = 8192] = "Fragment";
  VNodeFlags[VNodeFlags["InUse"] = 16384] = "InUse";
  VNodeFlags[VNodeFlags["ForwardRef"] = 32768] = "ForwardRef";
  VNodeFlags[VNodeFlags["Normalized"] = 65536] = "Normalized";
  /* Masks */

  VNodeFlags[VNodeFlags["ForwardRefComponent"] = 32776] = "ForwardRefComponent";
  VNodeFlags[VNodeFlags["FormElement"] = 448] = "FormElement";
  VNodeFlags[VNodeFlags["Element"] = 481] = "Element";
  VNodeFlags[VNodeFlags["Component"] = 14] = "Component";
  VNodeFlags[VNodeFlags["DOMRef"] = 2033] = "DOMRef";
  VNodeFlags[VNodeFlags["InUseOrNormalized"] = 81920] = "InUseOrNormalized";
  VNodeFlags[VNodeFlags["ClearInUse"] = -16385] = "ClearInUse";
  VNodeFlags[VNodeFlags["ComponentKnown"] = 12] = "ComponentKnown";
})(VNodeFlags || (exports.VNodeFlags = VNodeFlags = {})); // Combinations are not possible, its bitwise only to reduce vNode size


var ChildFlags;
exports.ChildFlags = ChildFlags;

(function (ChildFlags) {
  ChildFlags[ChildFlags["UnknownChildren"] = 0] = "UnknownChildren";
  /* Second set of bits define shape of children */

  ChildFlags[ChildFlags["HasInvalidChildren"] = 1] = "HasInvalidChildren";
  ChildFlags[ChildFlags["HasVNodeChildren"] = 2] = "HasVNodeChildren";
  ChildFlags[ChildFlags["HasNonKeyedChildren"] = 4] = "HasNonKeyedChildren";
  ChildFlags[ChildFlags["HasKeyedChildren"] = 8] = "HasKeyedChildren";
  ChildFlags[ChildFlags["HasTextChildren"] = 16] = "HasTextChildren";
  ChildFlags[ChildFlags["MultipleChildren"] = 12] = "MultipleChildren";
})(ChildFlags || (exports.ChildFlags = ChildFlags = {}));

/***/ }),

/***/ 69838:
/***/ (function(module) {

"use strict";


/**
 * Copyright (c) 2014-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
var runtime = function (exports) {
  "use strict";

  var Op = Object.prototype;
  var hasOwn = Op.hasOwnProperty;
  var undefined; // More compressible than void 0.

  var $Symbol = typeof Symbol === "function" ? Symbol : {};
  var iteratorSymbol = $Symbol.iterator || "@@iterator";
  var asyncIteratorSymbol = $Symbol.asyncIterator || "@@asyncIterator";
  var toStringTagSymbol = $Symbol.toStringTag || "@@toStringTag";

  function define(obj, key, value) {
    Object.defineProperty(obj, key, {
      value: value,
      enumerable: true,
      configurable: true,
      writable: true
    });
    return obj[key];
  }

  try {
    // IE 8 has a broken Object.defineProperty that only works on DOM objects.
    define({}, "");
  } catch (err) {
    define = function define(obj, key, value) {
      return obj[key] = value;
    };
  }

  function wrap(innerFn, outerFn, self, tryLocsList) {
    // If outerFn provided and outerFn.prototype is a Generator, then outerFn.prototype instanceof Generator.
    var protoGenerator = outerFn && outerFn.prototype instanceof Generator ? outerFn : Generator;
    var generator = Object.create(protoGenerator.prototype);
    var context = new Context(tryLocsList || []); // The ._invoke method unifies the implementations of the .next,
    // .throw, and .return methods.

    generator._invoke = makeInvokeMethod(innerFn, self, context);
    return generator;
  }

  exports.wrap = wrap; // Try/catch helper to minimize deoptimizations. Returns a completion
  // record like context.tryEntries[i].completion. This interface could
  // have been (and was previously) designed to take a closure to be
  // invoked without arguments, but in all the cases we care about we
  // already have an existing method we want to call, so there's no need
  // to create a new function object. We can even get away with assuming
  // the method takes exactly one argument, since that happens to be true
  // in every case, so we don't have to touch the arguments object. The
  // only additional allocation required is the completion record, which
  // has a stable shape and so hopefully should be cheap to allocate.

  function tryCatch(fn, obj, arg) {
    try {
      return {
        type: "normal",
        arg: fn.call(obj, arg)
      };
    } catch (err) {
      return {
        type: "throw",
        arg: err
      };
    }
  }

  var GenStateSuspendedStart = "suspendedStart";
  var GenStateSuspendedYield = "suspendedYield";
  var GenStateExecuting = "executing";
  var GenStateCompleted = "completed"; // Returning this object from the innerFn has the same effect as
  // breaking out of the dispatch switch statement.

  var ContinueSentinel = {}; // Dummy constructor functions that we use as the .constructor and
  // .constructor.prototype properties for functions that return Generator
  // objects. For full spec compliance, you may wish to configure your
  // minifier not to mangle the names of these two functions.

  function Generator() {}

  function GeneratorFunction() {}

  function GeneratorFunctionPrototype() {} // This is a polyfill for %IteratorPrototype% for environments that
  // don't natively support it.


  var IteratorPrototype = {};
  define(IteratorPrototype, iteratorSymbol, function () {
    return this;
  });
  var getProto = Object.getPrototypeOf;
  var NativeIteratorPrototype = getProto && getProto(getProto(values([])));

  if (NativeIteratorPrototype && NativeIteratorPrototype !== Op && hasOwn.call(NativeIteratorPrototype, iteratorSymbol)) {
    // This environment has a native %IteratorPrototype%; use it instead
    // of the polyfill.
    IteratorPrototype = NativeIteratorPrototype;
  }

  var Gp = GeneratorFunctionPrototype.prototype = Generator.prototype = Object.create(IteratorPrototype);
  GeneratorFunction.prototype = GeneratorFunctionPrototype;
  define(Gp, "constructor", GeneratorFunctionPrototype);
  define(GeneratorFunctionPrototype, "constructor", GeneratorFunction);
  GeneratorFunction.displayName = define(GeneratorFunctionPrototype, toStringTagSymbol, "GeneratorFunction"); // Helper for defining the .next, .throw, and .return methods of the
  // Iterator interface in terms of a single ._invoke method.

  function defineIteratorMethods(prototype) {
    ["next", "throw", "return"].forEach(function (method) {
      define(prototype, method, function (arg) {
        return this._invoke(method, arg);
      });
    });
  }

  exports.isGeneratorFunction = function (genFun) {
    var ctor = typeof genFun === "function" && genFun.constructor;
    return ctor ? ctor === GeneratorFunction || // For the native GeneratorFunction constructor, the best we can
    // do is to check its .name property.
    (ctor.displayName || ctor.name) === "GeneratorFunction" : false;
  };

  exports.mark = function (genFun) {
    if (Object.setPrototypeOf) {
      Object.setPrototypeOf(genFun, GeneratorFunctionPrototype);
    } else {
      genFun.__proto__ = GeneratorFunctionPrototype;
      define(genFun, toStringTagSymbol, "GeneratorFunction");
    }

    genFun.prototype = Object.create(Gp);
    return genFun;
  }; // Within the body of any async function, `await x` is transformed to
  // `yield regeneratorRuntime.awrap(x)`, so that the runtime can test
  // `hasOwn.call(value, "__await")` to determine if the yielded value is
  // meant to be awaited.


  exports.awrap = function (arg) {
    return {
      __await: arg
    };
  };

  function AsyncIterator(generator, PromiseImpl) {
    function invoke(method, arg, resolve, reject) {
      var record = tryCatch(generator[method], generator, arg);

      if (record.type === "throw") {
        reject(record.arg);
      } else {
        var result = record.arg;
        var value = result.value;

        if (value && typeof value === "object" && hasOwn.call(value, "__await")) {
          return PromiseImpl.resolve(value.__await).then(function (value) {
            invoke("next", value, resolve, reject);
          }, function (err) {
            invoke("throw", err, resolve, reject);
          });
        }

        return PromiseImpl.resolve(value).then(function (unwrapped) {
          // When a yielded Promise is resolved, its final value becomes
          // the .value of the Promise<{value,done}> result for the
          // current iteration.
          result.value = unwrapped;
          resolve(result);
        }, function (error) {
          // If a rejected Promise was yielded, throw the rejection back
          // into the async generator function so it can be handled there.
          return invoke("throw", error, resolve, reject);
        });
      }
    }

    var previousPromise;

    function enqueue(method, arg) {
      function callInvokeWithMethodAndArg() {
        return new PromiseImpl(function (resolve, reject) {
          invoke(method, arg, resolve, reject);
        });
      }

      return previousPromise = // If enqueue has been called before, then we want to wait until
      // all previous Promises have been resolved before calling invoke,
      // so that results are always delivered in the correct order. If
      // enqueue has not been called before, then it is important to
      // call invoke immediately, without waiting on a callback to fire,
      // so that the async generator function has the opportunity to do
      // any necessary setup in a predictable way. This predictability
      // is why the Promise constructor synchronously invokes its
      // executor callback, and why async functions synchronously
      // execute code before the first await. Since we implement simple
      // async functions in terms of async generators, it is especially
      // important to get this right, even though it requires care.
      previousPromise ? previousPromise.then(callInvokeWithMethodAndArg, // Avoid propagating failures to Promises returned by later
      // invocations of the iterator.
      callInvokeWithMethodAndArg) : callInvokeWithMethodAndArg();
    } // Define the unified helper method that is used to implement .next,
    // .throw, and .return (see defineIteratorMethods).


    this._invoke = enqueue;
  }

  defineIteratorMethods(AsyncIterator.prototype);
  define(AsyncIterator.prototype, asyncIteratorSymbol, function () {
    return this;
  });
  exports.AsyncIterator = AsyncIterator; // Note that simple async functions are implemented on top of
  // AsyncIterator objects; they just return a Promise for the value of
  // the final result produced by the iterator.

  exports.async = function (innerFn, outerFn, self, tryLocsList, PromiseImpl) {
    if (PromiseImpl === void 0) PromiseImpl = Promise;
    var iter = new AsyncIterator(wrap(innerFn, outerFn, self, tryLocsList), PromiseImpl);
    return exports.isGeneratorFunction(outerFn) ? iter // If outerFn is a generator, return the full iterator.
    : iter.next().then(function (result) {
      return result.done ? result.value : iter.next();
    });
  };

  function makeInvokeMethod(innerFn, self, context) {
    var state = GenStateSuspendedStart;
    return function () {
      function invoke(method, arg) {
        if (state === GenStateExecuting) {
          throw new Error("Generator is already running");
        }

        if (state === GenStateCompleted) {
          if (method === "throw") {
            throw arg;
          } // Be forgiving, per 25.3.3.3.3 of the spec:
          // https://people.mozilla.org/~jorendorff/es6-draft.html#sec-generatorresume


          return doneResult();
        }

        context.method = method;
        context.arg = arg;

        while (true) {
          var delegate = context.delegate;

          if (delegate) {
            var delegateResult = maybeInvokeDelegate(delegate, context);

            if (delegateResult) {
              if (delegateResult === ContinueSentinel) continue;
              return delegateResult;
            }
          }

          if (context.method === "next") {
            // Setting context._sent for legacy support of Babel's
            // function.sent implementation.
            context.sent = context._sent = context.arg;
          } else if (context.method === "throw") {
            if (state === GenStateSuspendedStart) {
              state = GenStateCompleted;
              throw context.arg;
            }

            context.dispatchException(context.arg);
          } else if (context.method === "return") {
            context.abrupt("return", context.arg);
          }

          state = GenStateExecuting;
          var record = tryCatch(innerFn, self, context);

          if (record.type === "normal") {
            // If an exception is thrown from innerFn, we leave state ===
            // GenStateExecuting and loop back for another invocation.
            state = context.done ? GenStateCompleted : GenStateSuspendedYield;

            if (record.arg === ContinueSentinel) {
              continue;
            }

            return {
              value: record.arg,
              done: context.done
            };
          } else if (record.type === "throw") {
            state = GenStateCompleted; // Dispatch the exception by looping back around to the
            // context.dispatchException(context.arg) call above.

            context.method = "throw";
            context.arg = record.arg;
          }
        }
      }

      return invoke;
    }();
  } // Call delegate.iterator[context.method](context.arg) and handle the
  // result, either by returning a { value, done } result from the
  // delegate iterator, or by modifying context.method and context.arg,
  // setting context.delegate to null, and returning the ContinueSentinel.


  function maybeInvokeDelegate(delegate, context) {
    var method = delegate.iterator[context.method];

    if (method === undefined) {
      // A .throw or .return when the delegate iterator has no .throw
      // method always terminates the yield* loop.
      context.delegate = null;

      if (context.method === "throw") {
        // Note: ["return"] must be used for ES3 parsing compatibility.
        if (delegate.iterator["return"]) {
          // If the delegate iterator has a return method, give it a
          // chance to clean up.
          context.method = "return";
          context.arg = undefined;
          maybeInvokeDelegate(delegate, context);

          if (context.method === "throw") {
            // If maybeInvokeDelegate(context) changed context.method from
            // "return" to "throw", let that override the TypeError below.
            return ContinueSentinel;
          }
        }

        context.method = "throw";
        context.arg = new TypeError("The iterator does not provide a 'throw' method");
      }

      return ContinueSentinel;
    }

    var record = tryCatch(method, delegate.iterator, context.arg);

    if (record.type === "throw") {
      context.method = "throw";
      context.arg = record.arg;
      context.delegate = null;
      return ContinueSentinel;
    }

    var info = record.arg;

    if (!info) {
      context.method = "throw";
      context.arg = new TypeError("iterator result is not an object");
      context.delegate = null;
      return ContinueSentinel;
    }

    if (info.done) {
      // Assign the result of the finished delegate to the temporary
      // variable specified by delegate.resultName (see delegateYield).
      context[delegate.resultName] = info.value; // Resume execution at the desired location (see delegateYield).

      context.next = delegate.nextLoc; // If context.method was "throw" but the delegate handled the
      // exception, let the outer generator proceed normally. If
      // context.method was "next", forget context.arg since it has been
      // "consumed" by the delegate iterator. If context.method was
      // "return", allow the original .return call to continue in the
      // outer generator.

      if (context.method !== "return") {
        context.method = "next";
        context.arg = undefined;
      }
    } else {
      // Re-yield the result returned by the delegate method.
      return info;
    } // The delegate iterator is finished, so forget it and continue with
    // the outer generator.


    context.delegate = null;
    return ContinueSentinel;
  } // Define Generator.prototype.{next,throw,return} in terms of the
  // unified ._invoke helper method.


  defineIteratorMethods(Gp);
  define(Gp, toStringTagSymbol, "Generator"); // A Generator should always return itself as the iterator object when the
  // @@iterator function is called on it. Some browsers' implementations of the
  // iterator prototype chain incorrectly implement this, causing the Generator
  // object to not be returned from this call. This ensures that doesn't happen.
  // See https://github.com/facebook/regenerator/issues/274 for more details.

  define(Gp, iteratorSymbol, function () {
    return this;
  });
  define(Gp, "toString", function () {
    return "[object Generator]";
  });

  function pushTryEntry(locs) {
    var entry = {
      tryLoc: locs[0]
    };

    if (1 in locs) {
      entry.catchLoc = locs[1];
    }

    if (2 in locs) {
      entry.finallyLoc = locs[2];
      entry.afterLoc = locs[3];
    }

    this.tryEntries.push(entry);
  }

  function resetTryEntry(entry) {
    var record = entry.completion || {};
    record.type = "normal";
    delete record.arg;
    entry.completion = record;
  }

  function Context(tryLocsList) {
    // The root entry object (effectively a try statement without a catch
    // or a finally block) gives us a place to store values thrown from
    // locations where there is no enclosing try statement.
    this.tryEntries = [{
      tryLoc: "root"
    }];
    tryLocsList.forEach(pushTryEntry, this);
    this.reset(true);
  }

  exports.keys = function (object) {
    var keys = [];

    for (var key in object) {
      keys.push(key);
    }

    keys.reverse(); // Rather than returning an object with a next method, we keep
    // things simple and return the next function itself.

    return function () {
      function next() {
        while (keys.length) {
          var key = keys.pop();

          if (key in object) {
            next.value = key;
            next.done = false;
            return next;
          }
        } // To avoid creating an additional object, we just hang the .value
        // and .done properties off the next function object itself. This
        // also ensures that the minifier will not anonymize the function.


        next.done = true;
        return next;
      }

      return next;
    }();
  };

  function values(iterable) {
    if (iterable) {
      var iteratorMethod = iterable[iteratorSymbol];

      if (iteratorMethod) {
        return iteratorMethod.call(iterable);
      }

      if (typeof iterable.next === "function") {
        return iterable;
      }

      if (!isNaN(iterable.length)) {
        var i = -1,
            next = function () {
          function next() {
            while (++i < iterable.length) {
              if (hasOwn.call(iterable, i)) {
                next.value = iterable[i];
                next.done = false;
                return next;
              }
            }

            next.value = undefined;
            next.done = true;
            return next;
          }

          return next;
        }();

        return next.next = next;
      }
    } // Return an iterator with no values.


    return {
      next: doneResult
    };
  }

  exports.values = values;

  function doneResult() {
    return {
      value: undefined,
      done: true
    };
  }

  Context.prototype = {
    constructor: Context,
    reset: function () {
      function reset(skipTempReset) {
        this.prev = 0;
        this.next = 0; // Resetting context._sent for legacy support of Babel's
        // function.sent implementation.

        this.sent = this._sent = undefined;
        this.done = false;
        this.delegate = null;
        this.method = "next";
        this.arg = undefined;
        this.tryEntries.forEach(resetTryEntry);

        if (!skipTempReset) {
          for (var name in this) {
            // Not sure about the optimal order of these conditions:
            if (name.charAt(0) === "t" && hasOwn.call(this, name) && !isNaN(+name.slice(1))) {
              this[name] = undefined;
            }
          }
        }
      }

      return reset;
    }(),
    stop: function () {
      function stop() {
        this.done = true;
        var rootEntry = this.tryEntries[0];
        var rootRecord = rootEntry.completion;

        if (rootRecord.type === "throw") {
          throw rootRecord.arg;
        }

        return this.rval;
      }

      return stop;
    }(),
    dispatchException: function () {
      function dispatchException(exception) {
        if (this.done) {
          throw exception;
        }

        var context = this;

        function handle(loc, caught) {
          record.type = "throw";
          record.arg = exception;
          context.next = loc;

          if (caught) {
            // If the dispatched exception was caught by a catch block,
            // then let that catch block handle the exception normally.
            context.method = "next";
            context.arg = undefined;
          }

          return !!caught;
        }

        for (var i = this.tryEntries.length - 1; i >= 0; --i) {
          var entry = this.tryEntries[i];
          var record = entry.completion;

          if (entry.tryLoc === "root") {
            // Exception thrown outside of any try block that could handle
            // it, so set the completion value of the entire function to
            // throw the exception.
            return handle("end");
          }

          if (entry.tryLoc <= this.prev) {
            var hasCatch = hasOwn.call(entry, "catchLoc");
            var hasFinally = hasOwn.call(entry, "finallyLoc");

            if (hasCatch && hasFinally) {
              if (this.prev < entry.catchLoc) {
                return handle(entry.catchLoc, true);
              } else if (this.prev < entry.finallyLoc) {
                return handle(entry.finallyLoc);
              }
            } else if (hasCatch) {
              if (this.prev < entry.catchLoc) {
                return handle(entry.catchLoc, true);
              }
            } else if (hasFinally) {
              if (this.prev < entry.finallyLoc) {
                return handle(entry.finallyLoc);
              }
            } else {
              throw new Error("try statement without catch or finally");
            }
          }
        }
      }

      return dispatchException;
    }(),
    abrupt: function () {
      function abrupt(type, arg) {
        for (var i = this.tryEntries.length - 1; i >= 0; --i) {
          var entry = this.tryEntries[i];

          if (entry.tryLoc <= this.prev && hasOwn.call(entry, "finallyLoc") && this.prev < entry.finallyLoc) {
            var finallyEntry = entry;
            break;
          }
        }

        if (finallyEntry && (type === "break" || type === "continue") && finallyEntry.tryLoc <= arg && arg <= finallyEntry.finallyLoc) {
          // Ignore the finally entry if control is not jumping to a
          // location outside the try/catch block.
          finallyEntry = null;
        }

        var record = finallyEntry ? finallyEntry.completion : {};
        record.type = type;
        record.arg = arg;

        if (finallyEntry) {
          this.method = "next";
          this.next = finallyEntry.finallyLoc;
          return ContinueSentinel;
        }

        return this.complete(record);
      }

      return abrupt;
    }(),
    complete: function () {
      function complete(record, afterLoc) {
        if (record.type === "throw") {
          throw record.arg;
        }

        if (record.type === "break" || record.type === "continue") {
          this.next = record.arg;
        } else if (record.type === "return") {
          this.rval = this.arg = record.arg;
          this.method = "return";
          this.next = "end";
        } else if (record.type === "normal" && afterLoc) {
          this.next = afterLoc;
        }

        return ContinueSentinel;
      }

      return complete;
    }(),
    finish: function () {
      function finish(finallyLoc) {
        for (var i = this.tryEntries.length - 1; i >= 0; --i) {
          var entry = this.tryEntries[i];

          if (entry.finallyLoc === finallyLoc) {
            this.complete(entry.completion, entry.afterLoc);
            resetTryEntry(entry);
            return ContinueSentinel;
          }
        }
      }

      return finish;
    }(),
    "catch": function () {
      function _catch(tryLoc) {
        for (var i = this.tryEntries.length - 1; i >= 0; --i) {
          var entry = this.tryEntries[i];

          if (entry.tryLoc === tryLoc) {
            var record = entry.completion;

            if (record.type === "throw") {
              var thrown = record.arg;
              resetTryEntry(entry);
            }

            return thrown;
          }
        } // The context.catch method must only be called with a location
        // argument that corresponds to a known catch block.


        throw new Error("illegal catch attempt");
      }

      return _catch;
    }(),
    delegateYield: function () {
      function delegateYield(iterable, resultName, nextLoc) {
        this.delegate = {
          iterator: values(iterable),
          resultName: resultName,
          nextLoc: nextLoc
        };

        if (this.method === "next") {
          // Deliberately forget the last sent value so that we don't
          // accidentally pass it on to the delegate.
          this.arg = undefined;
        }

        return ContinueSentinel;
      }

      return delegateYield;
    }()
  }; // Regardless of whether this script is executing as a CommonJS module
  // or not, return the runtime object so that we can declare the variable
  // regeneratorRuntime in the outer scope, which allows this module to be
  // injected easily by `bin/regenerator --include-runtime script.js`.

  return exports;
}( // If this script is executing as a CommonJS module, use module.exports
// as the regeneratorRuntime namespace. Otherwise create a new empty
// object. Either way, the resulting object will be used to initialize
// the regeneratorRuntime variable at the top of this file.
 true ? module.exports : 0);

try {
  regeneratorRuntime = runtime;
} catch (accidentalStrictMode) {
  // This module should not be running in strict mode, so the above
  // assignment should always work unless something is misconfigured. Just
  // in case runtime.js accidentally runs in strict mode, in modern engines
  // we can explicitly access globalThis. In older engines we can escape
  // strict mode using a global Function call. This could conceivably fail
  // if a Content Security Policy forbids using Function, but in that case
  // the proper solution is to fix the accidental strict mode problem. If
  // you've misconfigured your bundler to force strict mode and applied a
  // CSP to forbid Function, and you're not willing to fix either of those
  // problems, please detail your unique predicament in a GitHub issue.
  if (typeof globalThis === "object") {
    globalThis.regeneratorRuntime = runtime;
  } else {
    Function("r", "regeneratorRuntime = r")(runtime);
  }
}

/***/ }),

/***/ 96250:
/***/ (function() {

"use strict";


self.fetch || (self.fetch = function (e, n) {
  return n = n || {}, new Promise(function (t, s) {
    var r = new XMLHttpRequest(),
        o = [],
        u = [],
        i = {},
        a = function a() {
      return {
        ok: 2 == (r.status / 100 | 0),
        statusText: r.statusText,
        status: r.status,
        url: r.responseURL,
        text: function () {
          function text() {
            return Promise.resolve(r.responseText);
          }

          return text;
        }(),
        json: function () {
          function json() {
            return Promise.resolve(r.responseText).then(JSON.parse);
          }

          return json;
        }(),
        blob: function () {
          function blob() {
            return Promise.resolve(new Blob([r.response]));
          }

          return blob;
        }(),
        clone: a,
        headers: {
          keys: function () {
            function keys() {
              return o;
            }

            return keys;
          }(),
          entries: function () {
            function entries() {
              return u;
            }

            return entries;
          }(),
          get: function () {
            function get(e) {
              return i[e.toLowerCase()];
            }

            return get;
          }(),
          has: function () {
            function has(e) {
              return e.toLowerCase() in i;
            }

            return has;
          }()
        }
      };
    };

    for (var c in r.open(n.method || "get", e, !0), r.onload = function () {
      r.getAllResponseHeaders().replace(/^(.*?):[^\S\n]*([\s\S]*?)$/gm, function (e, n, t) {
        o.push(n = n.toLowerCase()), u.push([n, t]), i[n] = i[n] ? i[n] + "," + t : t;
      }), t(a());
    }, r.onerror = s, r.withCredentials = "include" == n.credentials, n.headers) {
      r.setRequestHeader(c, n.headers[c]);
    }

    r.send(n.body || null);
  });
});

/***/ }),

/***/ 1397:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.zipWith = exports.zip = exports.uniq = exports.uniqBy = exports.reduce = exports.range = exports.sort = exports.sortBy = exports.map = exports.filter = exports.toKeyedArray = exports.toArray = void 0;

/**
 * @file
 * @copyright 2020 Aleksej Komarov
 * @license MIT
 */

/**
 * Converts a given collection to an array.
 *
 * - Arrays are returned unmodified;
 * - If object was provided, keys will be discarded;
 * - Everything else will result in an empty array.
 *
 * @returns {any[]}
 */
var toArray = function toArray(collection) {
  if (Array.isArray(collection)) {
    return collection;
  }

  if (typeof collection === 'object') {
    var _hasOwnProperty = Object.prototype.hasOwnProperty;
    var result = [];

    for (var i in collection) {
      if (_hasOwnProperty.call(collection, i)) {
        result.push(collection[i]);
      }
    }

    return result;
  }

  return [];
};
/**
 * Converts a given object to an array, and appends a key to every
 * object inside of that array.
 *
 * Example input (object):
 * ```
 * {
 *   'Foo': { info: 'Hello world!' },
 *   'Bar': { info: 'Hello world!' },
 * }
 * ```
 *
 * Example output (array):
 * ```
 * [
 *   { key: 'Foo', info: 'Hello world!' },
 *   { key: 'Bar', info: 'Hello world!' },
 * ]
 * ```
 *
 * @template T
 * @param {{ [key: string]: T }} obj Object, or in DM terms, an assoc array
 * @param {string} keyProp Property, to which key will be assigned
 * @returns {T[]} Array of keyed objects
 */


exports.toArray = toArray;

var toKeyedArray = function toKeyedArray(obj, keyProp) {
  if (keyProp === void 0) {
    keyProp = 'key';
  }

  return map(function (item, key) {
    var _Object$assign;

    return Object.assign((_Object$assign = {}, _Object$assign[keyProp] = key, _Object$assign), item);
  })(obj);
};
/**
 * Iterates over elements of collection, returning an array of all elements
 * iteratee returns truthy for. The predicate is invoked with three
 * arguments: (value, index|key, collection).
 *
 * If collection is 'null' or 'undefined', it will be returned "as is"
 * without emitting any errors (which can be useful in some cases).
 *
 * @returns {any[]}
 */


exports.toKeyedArray = toKeyedArray;

var filter = function filter(iterateeFn) {
  return function (collection) {
    if (collection === null || collection === undefined) {
      return collection;
    }

    if (Array.isArray(collection)) {
      var result = [];

      for (var i = 0; i < collection.length; i++) {
        var item = collection[i];

        if (iterateeFn(item, i, collection)) {
          result.push(item);
        }
      }

      return result;
    }

    throw new Error("filter() can't iterate on type " + typeof collection);
  };
};
/**
 * Creates an array of values by running each element in collection
 * thru an iteratee function. The iteratee is invoked with three
 * arguments: (value, index|key, collection).
 *
 * If collection is 'null' or 'undefined', it will be returned "as is"
 * without emitting any errors (which can be useful in some cases).
 *
 * @returns {any[]}
 */


exports.filter = filter;

var map = function map(iterateeFn) {
  return function (collection) {
    if (collection === null || collection === undefined) {
      return collection;
    }

    if (Array.isArray(collection)) {
      var result = [];

      for (var i = 0; i < collection.length; i++) {
        result.push(iterateeFn(collection[i], i, collection));
      }

      return result;
    }

    if (typeof collection === 'object') {
      var _hasOwnProperty2 = Object.prototype.hasOwnProperty;
      var _result = [];

      for (var _i in collection) {
        if (_hasOwnProperty2.call(collection, _i)) {
          _result.push(iterateeFn(collection[_i], _i, collection));
        }
      }

      return _result;
    }

    throw new Error("map() can't iterate on type " + typeof collection);
  };
};

exports.map = map;

var COMPARATOR = function COMPARATOR(objA, objB) {
  var criteriaA = objA.criteria;
  var criteriaB = objB.criteria;
  var length = criteriaA.length;

  for (var i = 0; i < length; i++) {
    var a = criteriaA[i];
    var b = criteriaB[i];

    if (a < b) {
      return -1;
    }

    if (a > b) {
      return 1;
    }
  }

  return 0;
};
/**
 * Creates an array of elements, sorted in ascending order by the results
 * of running each element in a collection thru each iteratee.
 *
 * Iteratees are called with one argument (value).
 *
 * @returns {any[]}
 */


var sortBy = function sortBy() {
  for (var _len = arguments.length, iterateeFns = new Array(_len), _key = 0; _key < _len; _key++) {
    iterateeFns[_key] = arguments[_key];
  }

  return function (array) {
    if (!Array.isArray(array)) {
      return array;
    }

    var length = array.length; // Iterate over the array to collect criteria to sort it by

    var mappedArray = [];

    var _loop = function _loop(i) {
      var value = array[i];
      mappedArray.push({
        criteria: iterateeFns.map(function (fn) {
          return fn(value);
        }),
        value: value
      });
    };

    for (var i = 0; i < length; i++) {
      _loop(i);
    } // Sort criteria using the base comparator


    mappedArray.sort(COMPARATOR); // Unwrap values

    while (length--) {
      mappedArray[length] = mappedArray[length].value;
    }

    return mappedArray;
  };
};

exports.sortBy = sortBy;
var sort = sortBy();
/**
 * Returns a range of numbers from start to end, exclusively.
 * For example, range(0, 5) will return [0, 1, 2, 3, 4].
 */

exports.sort = sort;

var range = function range(start, end) {
  return new Array(end - start).fill(null).map(function (_, index) {
    return index + start;
  });
};
/**
 * A fast implementation of reduce.
 */


exports.range = range;

var reduce = function reduce(reducerFn, initialValue) {
  return function (array) {
    var length = array.length;
    var i;
    var result;

    if (initialValue === undefined) {
      i = 1;
      result = array[0];
    } else {
      i = 0;
      result = initialValue;
    }

    for (; i < length; i++) {
      result = reducerFn(result, array[i], i, array);
    }

    return result;
  };
};
/**
 * Creates a duplicate-free version of an array, using SameValueZero for
 * equality comparisons, in which only the first occurrence of each element
 * is kept. The order of result values is determined by the order they occur
 * in the array.
 *
 * It accepts iteratee which is invoked for each element in array to generate
 * the criterion by which uniqueness is computed. The order of result values
 * is determined by the order they occur in the array. The iteratee is
 * invoked with one argument: value.
 */

/* eslint-disable indent */


exports.reduce = reduce;

var uniqBy = function uniqBy(iterateeFn) {
  return function (array) {
    var length = array.length;
    var result = [];
    var seen = iterateeFn ? [] : result;
    var index = -1;

    outer: while (++index < length) {
      var _value = array[index];
      var computed = iterateeFn ? iterateeFn(_value) : _value;
      _value = _value !== 0 ? _value : 0;

      if (computed === computed) {
        var seenIndex = seen.length;

        while (seenIndex--) {
          if (seen[seenIndex] === computed) {
            continue outer;
          }
        }

        if (iterateeFn) {
          seen.push(computed);
        }

        result.push(_value);
      } else if (!seen.includes(computed)) {
        if (seen !== result) {
          seen.push(computed);
        }

        result.push(_value);
      }
    }

    return result;
  };
};
/* eslint-enable indent */


exports.uniqBy = uniqBy;
var uniq = uniqBy();
exports.uniq = uniq;

/**
 * Creates an array of grouped elements, the first of which contains
 * the first elements of the given arrays, the second of which contains
 * the second elements of the given arrays, and so on.
 */
var zip = function zip() {
  for (var _len2 = arguments.length, arrays = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
    arrays[_key2] = arguments[_key2];
  }

  if (arrays.length === 0) {
    return;
  }

  var numArrays = arrays.length;
  var numValues = arrays[0].length;
  var result = [];

  for (var valueIndex = 0; valueIndex < numValues; valueIndex++) {
    var entry = [];

    for (var arrayIndex = 0; arrayIndex < numArrays; arrayIndex++) {
      entry.push(arrays[arrayIndex][valueIndex]);
    }

    result.push(entry);
  }

  return result;
};
/**
 * This method is like "zip" except that it accepts iteratee to
 * specify how grouped values should be combined. The iteratee is
 * invoked with the elements of each group.
 *
 * @returns {any[]}
 */


exports.zip = zip;

var zipWith = function zipWith(iterateeFn) {
  return function () {
    return map(function (values) {
      return iterateeFn.apply(void 0, values);
    })(zip.apply(void 0, arguments));
  };
};

exports.zipWith = zipWith;

/***/ }),

/***/ 52844:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.EventEmitter = void 0;

/**
 * @file
 * @copyright 2020 Aleksej Komarov
 * @license MIT
 */
var EventEmitter = /*#__PURE__*/function () {
  function EventEmitter() {
    this.listeners = {};
  }

  var _proto = EventEmitter.prototype;

  _proto.on = function () {
    function on(name, listener) {
      this.listeners[name] = this.listeners[name] || [];
      this.listeners[name].push(listener);
    }

    return on;
  }();

  _proto.off = function () {
    function off(name, listener) {
      var listeners = this.listeners[name];

      if (!listeners) {
        throw new Error("There is no listeners for \"" + name + "\"");
      }

      this.listeners[name] = listeners.filter(function (existingListener) {
        return existingListener !== listener;
      });
    }

    return off;
  }();

  _proto.emit = function () {
    function emit(name) {
      var listeners = this.listeners[name];

      if (!listeners) {
        return;
      }

      for (var _len = arguments.length, params = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
        params[_key - 1] = arguments[_key];
      }

      for (var i = 0, len = listeners.length; i < len; i += 1) {
        var listener = listeners[i];
        listener.apply(void 0, params);
      }
    }

    return emit;
  }();

  _proto.clear = function () {
    function clear() {
      this.listeners = {};
    }

    return clear;
  }();

  return EventEmitter;
}();

exports.EventEmitter = EventEmitter;

/***/ }),

/***/ 32416:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.compose = exports.flow = void 0;

function _createForOfIteratorHelperLoose(o, allowArrayLike) { var it = typeof Symbol !== "undefined" && o[Symbol.iterator] || o["@@iterator"]; if (it) return (it = it.call(o)).next.bind(it); if (Array.isArray(o) || (it = _unsupportedIterableToArray(o)) || allowArrayLike && o && typeof o.length === "number") { if (it) o = it; var i = 0; return function () { if (i >= o.length) return { done: true }; return { done: false, value: o[i++] }; }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }

function _unsupportedIterableToArray(o, minLen) { if (!o) return; if (typeof o === "string") return _arrayLikeToArray(o, minLen); var n = Object.prototype.toString.call(o).slice(8, -1); if (n === "Object" && o.constructor) n = o.constructor.name; if (n === "Map" || n === "Set") return Array.from(o); if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen); }

function _arrayLikeToArray(arr, len) { if (len == null || len > arr.length) len = arr.length; for (var i = 0, arr2 = new Array(len); i < len; i++) { arr2[i] = arr[i]; } return arr2; }

/**
 * @file
 * @copyright 2020 Aleksej Komarov
 * @license MIT
 */

/**
 * Creates a function that returns the result of invoking the given
 * functions, where each successive invocation is supplied the return
 * value of the previous.
 */
var flow = function flow() {
  for (var _len = arguments.length, funcs = new Array(_len), _key = 0; _key < _len; _key++) {
    funcs[_key] = arguments[_key];
  }

  return function (input) {
    var output = input;

    for (var _len2 = arguments.length, rest = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++) {
      rest[_key2 - 1] = arguments[_key2];
    }

    for (var _iterator = _createForOfIteratorHelperLoose(funcs), _step; !(_step = _iterator()).done;) {
      var func = _step.value;

      // Recurse into the array of functions
      if (Array.isArray(func)) {
        output = flow.apply(void 0, func).apply(void 0, [output].concat(rest));
      } else if (func) {
        output = func.apply(void 0, [output].concat(rest));
      }
    }

    return output;
  };
};
/**
 * Composes single-argument functions from right to left.
 *
 * All functions might accept a context in form of additional arguments.
 * If the resulting function is called with more than 1 argument, rest of
 * the arguments are passed to all functions unchanged.
 *
 * @param {...Function} funcs The functions to compose
 * @returns {Function} A function obtained by composing the argument functions
 * from right to left. For example, compose(f, g, h) is identical to doing
 * (input, ...rest) => f(g(h(input, ...rest), ...rest), ...rest)
 */


exports.flow = flow;

var compose = function compose() {
  for (var _len3 = arguments.length, funcs = new Array(_len3), _key3 = 0; _key3 < _len3; _key3++) {
    funcs[_key3] = arguments[_key3];
  }

  if (funcs.length === 0) {
    return function (arg) {
      return arg;
    };
  }

  if (funcs.length === 1) {
    return funcs[0];
  }

  return funcs.reduce(function (a, b) {
    return function (value) {
      for (var _len4 = arguments.length, rest = new Array(_len4 > 1 ? _len4 - 1 : 0), _key4 = 1; _key4 < _len4; _key4++) {
        rest[_key4 - 1] = arguments[_key4];
      }

      return a.apply(void 0, [b.apply(void 0, [value].concat(rest))].concat(rest));
    };
  });
};

exports.compose = compose;

/***/ }),

/***/ 57890:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.KEY_QUOTE = exports.KEY_RIGHT_BRACKET = exports.KEY_BACKSLASH = exports.KEY_LEFT_BRACKET = exports.KEY_SLASH = exports.KEY_PERIOD = exports.KEY_MINUS = exports.KEY_COMMA = exports.KEY_EQUAL = exports.KEY_SEMICOLON = exports.KEY_F12 = exports.KEY_F11 = exports.KEY_F10 = exports.KEY_F9 = exports.KEY_F8 = exports.KEY_F7 = exports.KEY_F6 = exports.KEY_F5 = exports.KEY_F4 = exports.KEY_F3 = exports.KEY_F2 = exports.KEY_F1 = exports.KEY_Z = exports.KEY_Y = exports.KEY_X = exports.KEY_W = exports.KEY_V = exports.KEY_U = exports.KEY_T = exports.KEY_S = exports.KEY_R = exports.KEY_Q = exports.KEY_P = exports.KEY_O = exports.KEY_N = exports.KEY_M = exports.KEY_L = exports.KEY_K = exports.KEY_J = exports.KEY_I = exports.KEY_H = exports.KEY_G = exports.KEY_F = exports.KEY_E = exports.KEY_D = exports.KEY_C = exports.KEY_B = exports.KEY_A = exports.KEY_9 = exports.KEY_8 = exports.KEY_7 = exports.KEY_6 = exports.KEY_5 = exports.KEY_4 = exports.KEY_3 = exports.KEY_2 = exports.KEY_1 = exports.KEY_0 = exports.KEY_DELETE = exports.KEY_INSERT = exports.KEY_DOWN = exports.KEY_RIGHT = exports.KEY_UP = exports.KEY_LEFT = exports.KEY_HOME = exports.KEY_END = exports.KEY_PAGEDOWN = exports.KEY_PAGEUP = exports.KEY_SPACE = exports.KEY_ESCAPE = exports.KEY_CAPSLOCK = exports.KEY_PAUSE = exports.KEY_ALT = exports.KEY_CTRL = exports.KEY_SHIFT = exports.KEY_ENTER = exports.KEY_TAB = exports.KEY_BACKSPACE = void 0;

/**
 * All possible browser keycodes, in one file.
 *
 * @file
 * @copyright 2020 Aleksej Komarov
 * @license MIT
 */
var KEY_BACKSPACE = 8;
exports.KEY_BACKSPACE = KEY_BACKSPACE;
var KEY_TAB = 9;
exports.KEY_TAB = KEY_TAB;
var KEY_ENTER = 13;
exports.KEY_ENTER = KEY_ENTER;
var KEY_SHIFT = 16;
exports.KEY_SHIFT = KEY_SHIFT;
var KEY_CTRL = 17;
exports.KEY_CTRL = KEY_CTRL;
var KEY_ALT = 18;
exports.KEY_ALT = KEY_ALT;
var KEY_PAUSE = 19;
exports.KEY_PAUSE = KEY_PAUSE;
var KEY_CAPSLOCK = 20;
exports.KEY_CAPSLOCK = KEY_CAPSLOCK;
var KEY_ESCAPE = 27;
exports.KEY_ESCAPE = KEY_ESCAPE;
var KEY_SPACE = 32;
exports.KEY_SPACE = KEY_SPACE;
var KEY_PAGEUP = 33;
exports.KEY_PAGEUP = KEY_PAGEUP;
var KEY_PAGEDOWN = 34;
exports.KEY_PAGEDOWN = KEY_PAGEDOWN;
var KEY_END = 35;
exports.KEY_END = KEY_END;
var KEY_HOME = 36;
exports.KEY_HOME = KEY_HOME;
var KEY_LEFT = 37;
exports.KEY_LEFT = KEY_LEFT;
var KEY_UP = 38;
exports.KEY_UP = KEY_UP;
var KEY_RIGHT = 39;
exports.KEY_RIGHT = KEY_RIGHT;
var KEY_DOWN = 40;
exports.KEY_DOWN = KEY_DOWN;
var KEY_INSERT = 45;
exports.KEY_INSERT = KEY_INSERT;
var KEY_DELETE = 46;
exports.KEY_DELETE = KEY_DELETE;
var KEY_0 = 48;
exports.KEY_0 = KEY_0;
var KEY_1 = 49;
exports.KEY_1 = KEY_1;
var KEY_2 = 50;
exports.KEY_2 = KEY_2;
var KEY_3 = 51;
exports.KEY_3 = KEY_3;
var KEY_4 = 52;
exports.KEY_4 = KEY_4;
var KEY_5 = 53;
exports.KEY_5 = KEY_5;
var KEY_6 = 54;
exports.KEY_6 = KEY_6;
var KEY_7 = 55;
exports.KEY_7 = KEY_7;
var KEY_8 = 56;
exports.KEY_8 = KEY_8;
var KEY_9 = 57;
exports.KEY_9 = KEY_9;
var KEY_A = 65;
exports.KEY_A = KEY_A;
var KEY_B = 66;
exports.KEY_B = KEY_B;
var KEY_C = 67;
exports.KEY_C = KEY_C;
var KEY_D = 68;
exports.KEY_D = KEY_D;
var KEY_E = 69;
exports.KEY_E = KEY_E;
var KEY_F = 70;
exports.KEY_F = KEY_F;
var KEY_G = 71;
exports.KEY_G = KEY_G;
var KEY_H = 72;
exports.KEY_H = KEY_H;
var KEY_I = 73;
exports.KEY_I = KEY_I;
var KEY_J = 74;
exports.KEY_J = KEY_J;
var KEY_K = 75;
exports.KEY_K = KEY_K;
var KEY_L = 76;
exports.KEY_L = KEY_L;
var KEY_M = 77;
exports.KEY_M = KEY_M;
var KEY_N = 78;
exports.KEY_N = KEY_N;
var KEY_O = 79;
exports.KEY_O = KEY_O;
var KEY_P = 80;
exports.KEY_P = KEY_P;
var KEY_Q = 81;
exports.KEY_Q = KEY_Q;
var KEY_R = 82;
exports.KEY_R = KEY_R;
var KEY_S = 83;
exports.KEY_S = KEY_S;
var KEY_T = 84;
exports.KEY_T = KEY_T;
var KEY_U = 85;
exports.KEY_U = KEY_U;
var KEY_V = 86;
exports.KEY_V = KEY_V;
var KEY_W = 87;
exports.KEY_W = KEY_W;
var KEY_X = 88;
exports.KEY_X = KEY_X;
var KEY_Y = 89;
exports.KEY_Y = KEY_Y;
var KEY_Z = 90;
exports.KEY_Z = KEY_Z;
var KEY_F1 = 112;
exports.KEY_F1 = KEY_F1;
var KEY_F2 = 113;
exports.KEY_F2 = KEY_F2;
var KEY_F3 = 114;
exports.KEY_F3 = KEY_F3;
var KEY_F4 = 115;
exports.KEY_F4 = KEY_F4;
var KEY_F5 = 116;
exports.KEY_F5 = KEY_F5;
var KEY_F6 = 117;
exports.KEY_F6 = KEY_F6;
var KEY_F7 = 118;
exports.KEY_F7 = KEY_F7;
var KEY_F8 = 119;
exports.KEY_F8 = KEY_F8;
var KEY_F9 = 120;
exports.KEY_F9 = KEY_F9;
var KEY_F10 = 121;
exports.KEY_F10 = KEY_F10;
var KEY_F11 = 122;
exports.KEY_F11 = KEY_F11;
var KEY_F12 = 123;
exports.KEY_F12 = KEY_F12;
var KEY_SEMICOLON = 186;
exports.KEY_SEMICOLON = KEY_SEMICOLON;
var KEY_EQUAL = 187;
exports.KEY_EQUAL = KEY_EQUAL;
var KEY_COMMA = 188;
exports.KEY_COMMA = KEY_COMMA;
var KEY_MINUS = 189;
exports.KEY_MINUS = KEY_MINUS;
var KEY_PERIOD = 190;
exports.KEY_PERIOD = KEY_PERIOD;
var KEY_SLASH = 191;
exports.KEY_SLASH = KEY_SLASH;
var KEY_LEFT_BRACKET = 219;
exports.KEY_LEFT_BRACKET = KEY_LEFT_BRACKET;
var KEY_BACKSLASH = 220;
exports.KEY_BACKSLASH = KEY_BACKSLASH;
var KEY_RIGHT_BRACKET = 221;
exports.KEY_RIGHT_BRACKET = KEY_RIGHT_BRACKET;
var KEY_QUOTE = 222;
exports.KEY_QUOTE = KEY_QUOTE;

/***/ }),

/***/ 94831:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.isEscape = exports.KEY = void 0;

/**
 * ### Key codes.
 * event.keyCode is deprecated, use this reference instead.
 *
 * Handles modifier keys (Shift, Alt, Control) and arrow keys.
 *
 * For alphabetical keys, use the actual character (e.g. 'a') instead of the key code.
 * Don't access Esc or Escape directly, use isEscape() instead
 *
 * Something isn't here that you want? Just add it:
 * @url https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent/key/Key_Values
 * @usage
 * ```ts
 * import { KEY } from 'tgui/common/keys';
 *
 * if (event.key === KEY.Enter) {
 *   // do something
 * }
 * ```
 *
 *
 */
var KEY;
/**
 * ### isEscape
 *
 * Checks if the user has hit the 'ESC' key on their keyboard.
 * There's a weirdness in BYOND where this could be either the string
 * 'Escape' or 'Esc' depending on the browser. This function handles
 * both cases.
 *
 * @param key - the key to check, typically from event.key
 * @returns true if key is Escape or Esc, false otherwise
 */

exports.KEY = KEY;

(function (KEY) {
  KEY["Alt"] = "Alt";
  KEY["Backspace"] = "Backspace";
  KEY["Control"] = "Control";
  KEY["Delete"] = "Delete";
  KEY["Down"] = "ArrowDown";
  KEY["End"] = "End";
  KEY["Enter"] = "Enter";
  KEY["Esc"] = "Esc";
  KEY["Escape"] = "Escape";
  KEY["Home"] = "Home";
  KEY["Insert"] = "Insert";
  KEY["Left"] = "ArrowLeft";
  KEY["PageDown"] = "PageDown";
  KEY["PageUp"] = "PageUp";
  KEY["Right"] = "ArrowRight";
  KEY["Shift"] = "Shift";
  KEY["Space"] = " ";
  KEY["Tab"] = "Tab";
  KEY["Up"] = "ArrowUp";
})(KEY || (exports.KEY = KEY = {}));

var isEscape = function isEscape(key) {
  return key === KEY.Esc || key === KEY.Escape;
};

exports.isEscape = isEscape;

/***/ }),

/***/ 97622:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.numberOfDecimalDigits = exports.keyOfMatchingRange = exports.inRange = exports.toFixed = exports.round = exports.scale = exports.clamp01 = exports.clamp = void 0;

/**
 * @file
 * @copyright 2020 Aleksej Komarov
 * @license MIT
 */

/**
 * Limits a number to the range between 'min' and 'max'.
 */
var clamp = function clamp(value, min, max) {
  return value < min ? min : value > max ? max : value;
};
/**
 * Limits a number between 0 and 1.
 */


exports.clamp = clamp;

var clamp01 = function clamp01(value) {
  return value < 0 ? 0 : value > 1 ? 1 : value;
};
/**
 * Scales a number to fit into the range between min and max.
 */


exports.clamp01 = clamp01;

var scale = function scale(value, min, max) {
  return (value - min) / (max - min);
};
/**
 * Robust number rounding.
 *
 * Adapted from Locutus, see: http://locutus.io/php/math/round/
 *
 * @param  {number} value
 * @param  {number} precision
 * @return {number}
 */


exports.scale = scale;

var round = function round(value, precision) {
  if (!value || isNaN(value)) {
    return value;
  } // helper variables


  var m, f, isHalf, sgn; // making sure precision is integer

  precision |= 0;
  m = Math.pow(10, precision);
  value *= m; // sign of the number

  sgn = +(value > 0) | -(value < 0); // isHalf = value % 1 === 0.5 * sgn;

  isHalf = Math.abs(value % 1) >= 0.4999999999854481;
  f = Math.floor(value);

  if (isHalf) {
    // rounds .5 away from zero
    value = f + (sgn > 0);
  }

  return (isHalf ? value : Math.round(value)) / m;
};
/**
 * Returns a string representing a number in fixed point notation.
 */


exports.round = round;

var toFixed = function toFixed(value, fractionDigits) {
  if (fractionDigits === void 0) {
    fractionDigits = 0;
  }

  return Number(value).toFixed(Math.max(fractionDigits, 0));
};
/**
 * Checks whether a value is within the provided range.
 *
 * Range is an array of two numbers, for example: [0, 15].
 */


exports.toFixed = toFixed;

var inRange = function inRange(value, range) {
  return range && value >= range[0] && value <= range[1];
};
/**
 * Walks over the object with ranges, comparing value against every range,
 * and returns the key of the first matching range.
 *
 * Range is an array of two numbers, for example: [0, 15].
 */


exports.inRange = inRange;

var keyOfMatchingRange = function keyOfMatchingRange(value, ranges) {
  for (var _i = 0, _Object$keys = Object.keys(ranges); _i < _Object$keys.length; _i++) {
    var rangeName = _Object$keys[_i];
    var range = ranges[rangeName];

    if (inRange(value, range)) {
      return rangeName;
    }
  }
};
/**
 * Get number of digits following the decimal point in a number
 */


exports.keyOfMatchingRange = keyOfMatchingRange;

var numberOfDecimalDigits = function numberOfDecimalDigits(value) {
  if (Math.floor(value) !== value) {
    return value.toString().split('.')[1].length || 0;
  }

  return 0;
};

exports.numberOfDecimalDigits = numberOfDecimalDigits;

/***/ }),

/***/ 28440:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.perf = void 0;

var _window$performance;

/**
 * Ghetto performance measurement tools.
 *
 * Uses NODE_ENV to remove itself from production builds.
 *
 * @file
 * @copyright 2020 Aleksej Komarov
 * @license MIT
 */
var FPS = 60;
var FRAME_DURATION = 1000 / FPS; // True if Performance API is supported

var supportsPerf = !!((_window$performance = window.performance) != null && _window$performance.now); // High precision markers

var hpMarkersByName = {}; // Low precision markers

var lpMarkersByName = {};
/**
 * Marks a certain spot in the code for later measurements.
 */

var mark = function mark(name, timestamp) {
  if (false) {}
};
/**
 * Calculates and returns the difference between two markers as a string.
 *
 * Use logger.log() to print the measurement.
 */


var measure = function measure(markerNameA, markerNameB) {
  if (false) { var duration, markerB, markerA; }
};

var formatDuration = function formatDuration(duration) {
  var durationInFrames = duration / FRAME_DURATION;
  return duration.toFixed(duration < 10 ? 1 : 0) + 'ms ' + '(' + durationInFrames.toFixed(2) + ' frames)';
};

var perf = {
  mark: mark,
  measure: measure
};
exports.perf = perf;

/***/ }),

/***/ 1060:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.canRender = exports.pureComponentHooks = exports.shallowDiffers = exports.normalizeChildren = exports.classes = void 0;

/**
 * @file
 * @copyright 2020 Aleksej Komarov
 * @license MIT
 */

/**
 * Helper for conditionally adding/removing classes in React
 */
var classes = function classes(classNames) {
  var className = '';

  for (var i = 0; i < classNames.length; i++) {
    var part = classNames[i];

    if (typeof part === 'string') {
      className += part + ' ';
    }
  }

  return className;
};
/**
 * Normalizes children prop, so that it is always an array of VDom
 * elements.
 */


exports.classes = classes;

var normalizeChildren = function normalizeChildren(children) {
  if (Array.isArray(children)) {
    return children.flat().filter(function (value) {
      return value;
    });
  }

  if (typeof children === 'object') {
    return [children];
  }

  return [];
};
/**
 * Shallowly checks if two objects are different.
 * Credit: https://github.com/developit/preact-compat
 */


exports.normalizeChildren = normalizeChildren;

var shallowDiffers = function shallowDiffers(a, b) {
  var i;

  for (i in a) {
    if (!(i in b)) {
      return true;
    }
  }

  for (i in b) {
    if (a[i] !== b[i]) {
      return true;
    }
  }

  return false;
};
/**
 * Default inferno hooks for pure components.
 */


exports.shallowDiffers = shallowDiffers;
var pureComponentHooks = {
  onComponentShouldUpdate: function () {
    function onComponentShouldUpdate(lastProps, nextProps) {
      return shallowDiffers(lastProps, nextProps);
    }

    return onComponentShouldUpdate;
  }()
};
/**
 * A helper to determine whether the object is renderable by React.
 */

exports.pureComponentHooks = pureComponentHooks;

var canRender = function canRender(value) {
  return value !== undefined && value !== null && typeof value !== 'boolean';
};
/**
 * A common case in tgui, when you pass a value conditionally, these are
 * the types that can fall through the condition.
 */


exports.canRender = canRender;

/***/ }),

/***/ 465:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.useSelector = exports.useDispatch = exports.createAction = exports.combineReducers = exports.applyMiddleware = exports.createStore = void 0;

var _fp = __webpack_require__(32416);

function _createForOfIteratorHelperLoose(o, allowArrayLike) { var it = typeof Symbol !== "undefined" && o[Symbol.iterator] || o["@@iterator"]; if (it) return (it = it.call(o)).next.bind(it); if (Array.isArray(o) || (it = _unsupportedIterableToArray(o)) || allowArrayLike && o && typeof o.length === "number") { if (it) o = it; var i = 0; return function () { if (i >= o.length) return { done: true }; return { done: false, value: o[i++] }; }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }

function _unsupportedIterableToArray(o, minLen) { if (!o) return; if (typeof o === "string") return _arrayLikeToArray(o, minLen); var n = Object.prototype.toString.call(o).slice(8, -1); if (n === "Object" && o.constructor) n = o.constructor.name; if (n === "Map" || n === "Set") return Array.from(o); if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen); }

function _arrayLikeToArray(arr, len) { if (len == null || len > arr.length) len = arr.length; for (var i = 0, arr2 = new Array(len); i < len; i++) { arr2[i] = arr[i]; } return arr2; }

/**
 * Creates a Redux store.
 */
var createStore = function createStore(reducer, enhancer) {
  // Apply a store enhancer (applyMiddleware is one of them).
  if (enhancer) {
    return enhancer(createStore)(reducer);
  }

  var currentState;
  var listeners = [];

  var getState = function getState() {
    return currentState;
  };

  var subscribe = function subscribe(listener) {
    listeners.push(listener);
  };

  var dispatch = function dispatch(action) {
    currentState = reducer(currentState, action);

    for (var i = 0; i < listeners.length; i++) {
      listeners[i]();
    }
  }; // This creates the initial store by causing each reducer to be called
  // with an undefined state


  dispatch({
    type: '@@INIT'
  });
  return {
    dispatch: dispatch,
    subscribe: subscribe,
    getState: getState
  };
};
/**
 * Creates a store enhancer which applies middleware to all dispatched
 * actions.
 */


exports.createStore = createStore;

var applyMiddleware = function applyMiddleware() {
  for (var _len = arguments.length, middlewares = new Array(_len), _key = 0; _key < _len; _key++) {
    middlewares[_key] = arguments[_key];
  }

  return function (createStore) {
    return function (reducer) {
      for (var _len2 = arguments.length, args = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++) {
        args[_key2 - 1] = arguments[_key2];
      }

      var store = createStore.apply(void 0, [reducer].concat(args));

      var _dispatch = function dispatch() {
        throw new Error('Dispatching while constructing your middleware is not allowed.');
      };

      var storeApi = {
        getState: store.getState,
        dispatch: function () {
          function dispatch(action) {
            for (var _len3 = arguments.length, args = new Array(_len3 > 1 ? _len3 - 1 : 0), _key3 = 1; _key3 < _len3; _key3++) {
              args[_key3 - 1] = arguments[_key3];
            }

            return _dispatch.apply(void 0, [action].concat(args));
          }

          return dispatch;
        }()
      };
      var chain = middlewares.map(function (middleware) {
        return middleware(storeApi);
      });
      _dispatch = _fp.compose.apply(void 0, chain)(store.dispatch);
      return Object.assign({}, store, {
        dispatch: _dispatch
      });
    };
  };
};
/**
 * Combines reducers by running them in their own object namespaces as
 * defined in reducersObj paramter.
 *
 * Main difference from redux/combineReducers is that it preserves keys
 * in the state that are not present in the reducers object. This function
 * is also more flexible than the redux counterpart.
 */


exports.applyMiddleware = applyMiddleware;

var combineReducers = function combineReducers(reducersObj) {
  var keys = Object.keys(reducersObj);
  var hasChanged = false;
  return function (prevState, action) {
    if (prevState === void 0) {
      prevState = {};
    }

    var nextState = Object.assign({}, prevState);

    for (var _iterator = _createForOfIteratorHelperLoose(keys), _step; !(_step = _iterator()).done;) {
      var key = _step.value;
      var reducer = reducersObj[key];
      var prevDomainState = prevState[key];
      var nextDomainState = reducer(prevDomainState, action);

      if (prevDomainState !== nextDomainState) {
        hasChanged = true;
        nextState[key] = nextDomainState;
      }
    }

    return hasChanged ? nextState : prevState;
  };
};
/**
 * A utility function to create an action creator for the given action
 * type string. The action creator accepts a single argument, which will
 * be included in the action object as a field called payload. The action
 * creator function will also have its toString() overriden so that it
 * returns the action type, allowing it to be used in reducer logic that
 * is looking for that action type.
 *
 * @param {string} type The action type to use for created actions.
 * @param {any} prepare (optional) a method that takes any number of arguments
 * and returns { payload } or { payload, meta }. If this is given, the
 * resulting action creator will pass it's arguments to this method to
 * calculate payload & meta.
 *
 * @public
 */


exports.combineReducers = combineReducers;

var createAction = function createAction(type, prepare) {
  if (prepare === void 0) {
    prepare = null;
  }

  var actionCreator = function actionCreator() {
    if (!prepare) {
      return {
        type: type,
        payload: arguments.length <= 0 ? undefined : arguments[0]
      };
    }

    var prepared = prepare.apply(void 0, arguments);

    if (!prepared) {
      throw new Error('prepare function did not return an object');
    }

    var action = {
      type: type
    };

    if ('payload' in prepared) {
      action.payload = prepared.payload;
    }

    if ('meta' in prepared) {
      action.meta = prepared.meta;
    }

    return action;
  };

  actionCreator.toString = function () {
    return '' + type;
  };

  actionCreator.type = type;

  actionCreator.match = function (action) {
    return action.type === type;
  };

  return actionCreator;
}; // Implementation specific
// --------------------------------------------------------


exports.createAction = createAction;

var useDispatch = function useDispatch(context) {
  return context.store.dispatch;
};

exports.useDispatch = useDispatch;

var useSelector = function useSelector(context, selector) {
  return selector(context.store.getState());
};

exports.useSelector = useSelector;

/***/ }),

/***/ 7151:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.storage = exports.IMPL_INDEXED_DB = exports.IMPL_LOCAL_STORAGE = exports.IMPL_MEMORY = void 0;

function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { Promise.resolve(value).then(_next, _throw); } }

function _asyncToGenerator(fn) { return function () { var self = this, args = arguments; return new Promise(function (resolve, reject) { var gen = fn.apply(self, args); function _next(value) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value); } function _throw(err) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err); } _next(undefined); }); }; }

/**
 * Browser-agnostic abstraction of key-value web storage.
 *
 * @file
 * @copyright 2020 Aleksej Komarov
 * @license MIT
 */
var IMPL_MEMORY = 0;
exports.IMPL_MEMORY = IMPL_MEMORY;
var IMPL_LOCAL_STORAGE = 1;
exports.IMPL_LOCAL_STORAGE = IMPL_LOCAL_STORAGE;
var IMPL_INDEXED_DB = 2;
exports.IMPL_INDEXED_DB = IMPL_INDEXED_DB;
var INDEXED_DB_VERSION = 1;
var INDEXED_DB_NAME = 'tau-tgui';
var INDEXED_DB_STORE_NAME = 'storage-v1';
var READ_ONLY = 'readonly';
var READ_WRITE = 'readwrite';

var testGeneric = function testGeneric(testFn) {
  return function () {
    try {
      return Boolean(testFn());
    } catch (_unused) {
      return false;
    }
  };
}; // Localstorage can sometimes throw an error, even if DOM storage is not
// disabled in IE11 settings.
// See: https://superuser.com/questions/1080011


var testLocalStorage = testGeneric(function () {
  return window.localStorage && window.localStorage.getItem;
});
var testIndexedDb = testGeneric(function () {
  return (window.indexedDB || window.msIndexedDB) && (window.IDBTransaction || window.msIDBTransaction);
});

var MemoryBackend = /*#__PURE__*/function () {
  function MemoryBackend() {
    this.impl = IMPL_MEMORY;
    this.store = {};
  }

  var _proto = MemoryBackend.prototype;

  _proto.get = function () {
    function get(key) {
      return this.store[key];
    }

    return get;
  }();

  _proto.set = function () {
    function set(key, value) {
      this.store[key] = value;
    }

    return set;
  }();

  _proto.remove = function () {
    function remove(key) {
      this.store[key] = undefined;
    }

    return remove;
  }();

  _proto.clear = function () {
    function clear() {
      this.store = {};
    }

    return clear;
  }();

  return MemoryBackend;
}();

var LocalStorageBackend = /*#__PURE__*/function () {
  function LocalStorageBackend() {
    this.impl = IMPL_LOCAL_STORAGE;
  }

  var _proto2 = LocalStorageBackend.prototype;

  _proto2.get = function () {
    function get(key) {
      var value = localStorage.getItem(key);

      if (typeof value === 'string') {
        return JSON.parse(value);
      }
    }

    return get;
  }();

  _proto2.set = function () {
    function set(key, value) {
      localStorage.setItem(key, JSON.stringify(value));
    }

    return set;
  }();

  _proto2.remove = function () {
    function remove(key) {
      localStorage.removeItem(key);
    }

    return remove;
  }();

  _proto2.clear = function () {
    function clear() {
      localStorage.clear();
    }

    return clear;
  }();

  return LocalStorageBackend;
}();

var IndexedDbBackend = /*#__PURE__*/function () {
  function IndexedDbBackend() {
    this.impl = IMPL_INDEXED_DB;
    /** @type {Promise<IDBDatabase>} */

    this.dbPromise = new Promise(function (resolve, reject) {
      var indexedDB = window.indexedDB || window.msIndexedDB;
      var req = indexedDB.open(INDEXED_DB_NAME, INDEXED_DB_VERSION);

      req.onupgradeneeded = function () {
        try {
          req.result.createObjectStore(INDEXED_DB_STORE_NAME);
        } catch (err) {
          reject(new Error('Failed to upgrade IDB: ' + req.error));
        }
      };

      req.onsuccess = function () {
        return resolve(req.result);
      };

      req.onerror = function () {
        reject(new Error('Failed to open IDB: ' + req.error));
      };
    });
  }

  var _proto3 = IndexedDbBackend.prototype;

  _proto3.getStore = function () {
    function getStore(mode) {
      return this.dbPromise.then(function (db) {
        return db.transaction(INDEXED_DB_STORE_NAME, mode).objectStore(INDEXED_DB_STORE_NAME);
      });
    }

    return getStore;
  }();

  _proto3.get = /*#__PURE__*/function () {
    var _get = _asyncToGenerator( /*#__PURE__*/regeneratorRuntime.mark(function () {
      function _callee(key) {
        var store;
        return regeneratorRuntime.wrap(function () {
          function _callee$(_context) {
            while (1) {
              switch (_context.prev = _context.next) {
                case 0:
                  _context.next = 2;
                  return this.getStore(READ_ONLY);

                case 2:
                  store = _context.sent;
                  return _context.abrupt("return", new Promise(function (resolve, reject) {
                    var req = store.get(key);

                    req.onsuccess = function () {
                      return resolve(req.result);
                    };

                    req.onerror = function () {
                      return reject(req.error);
                    };
                  }));

                case 4:
                case "end":
                  return _context.stop();
              }
            }
          }

          return _callee$;
        }(), _callee, this);
      }

      return _callee;
    }()));

    function get(_x) {
      return _get.apply(this, arguments);
    }

    return get;
  }();

  _proto3.set = /*#__PURE__*/function () {
    var _set = _asyncToGenerator( /*#__PURE__*/regeneratorRuntime.mark(function () {
      function _callee2(key, value) {
        var store;
        return regeneratorRuntime.wrap(function () {
          function _callee2$(_context2) {
            while (1) {
              switch (_context2.prev = _context2.next) {
                case 0:
                  // The reason we don't _save_ null is because IE 10 does
                  // not support saving the `null` type in IndexedDB. How
                  // ironic, given the bug below!
                  // See: https://github.com/mozilla/localForage/issues/161
                  if (value === null) {
                    value = undefined;
                  } // NOTE: We deliberately make this operation transactionless


                  _context2.next = 3;
                  return this.getStore(READ_WRITE);

                case 3:
                  store = _context2.sent;
                  store.put(value, key);

                case 5:
                case "end":
                  return _context2.stop();
              }
            }
          }

          return _callee2$;
        }(), _callee2, this);
      }

      return _callee2;
    }()));

    function set(_x2, _x3) {
      return _set.apply(this, arguments);
    }

    return set;
  }();

  _proto3.remove = /*#__PURE__*/function () {
    var _remove = _asyncToGenerator( /*#__PURE__*/regeneratorRuntime.mark(function () {
      function _callee3(key) {
        var store;
        return regeneratorRuntime.wrap(function () {
          function _callee3$(_context3) {
            while (1) {
              switch (_context3.prev = _context3.next) {
                case 0:
                  _context3.next = 2;
                  return this.getStore(READ_WRITE);

                case 2:
                  store = _context3.sent;
                  store["delete"](key);

                case 4:
                case "end":
                  return _context3.stop();
              }
            }
          }

          return _callee3$;
        }(), _callee3, this);
      }

      return _callee3;
    }()));

    function remove(_x4) {
      return _remove.apply(this, arguments);
    }

    return remove;
  }();

  _proto3.clear = /*#__PURE__*/function () {
    var _clear = _asyncToGenerator( /*#__PURE__*/regeneratorRuntime.mark(function () {
      function _callee4() {
        var store;
        return regeneratorRuntime.wrap(function () {
          function _callee4$(_context4) {
            while (1) {
              switch (_context4.prev = _context4.next) {
                case 0:
                  _context4.next = 2;
                  return this.getStore(READ_WRITE);

                case 2:
                  store = _context4.sent;
                  store.clear();

                case 4:
                case "end":
                  return _context4.stop();
              }
            }
          }

          return _callee4$;
        }(), _callee4, this);
      }

      return _callee4;
    }()));

    function clear() {
      return _clear.apply(this, arguments);
    }

    return clear;
  }();

  return IndexedDbBackend;
}();
/**
 * Web Storage Proxy object, which selects the best backend available
 * depending on the environment.
 */


var StorageProxy = /*#__PURE__*/function () {
  function StorageProxy() {
    this.backendPromise = _asyncToGenerator( /*#__PURE__*/regeneratorRuntime.mark(function () {
      function _callee5() {
        var backend;
        return regeneratorRuntime.wrap(function () {
          function _callee5$(_context5) {
            while (1) {
              switch (_context5.prev = _context5.next) {
                case 0:
                  if (!testIndexedDb()) {
                    _context5.next = 10;
                    break;
                  }

                  _context5.prev = 1;
                  backend = new IndexedDbBackend();
                  _context5.next = 5;
                  return backend.dbPromise;

                case 5:
                  return _context5.abrupt("return", backend);

                case 8:
                  _context5.prev = 8;
                  _context5.t0 = _context5["catch"](1);

                case 10:
                  if (!testLocalStorage()) {
                    _context5.next = 12;
                    break;
                  }

                  return _context5.abrupt("return", new LocalStorageBackend());

                case 12:
                  return _context5.abrupt("return", new MemoryBackend());

                case 13:
                case "end":
                  return _context5.stop();
              }
            }
          }

          return _callee5$;
        }(), _callee5, null, [[1, 8]]);
      }

      return _callee5;
    }()))();
  }

  var _proto4 = StorageProxy.prototype;

  _proto4.get = /*#__PURE__*/function () {
    var _get2 = _asyncToGenerator( /*#__PURE__*/regeneratorRuntime.mark(function () {
      function _callee6(key) {
        var backend;
        return regeneratorRuntime.wrap(function () {
          function _callee6$(_context6) {
            while (1) {
              switch (_context6.prev = _context6.next) {
                case 0:
                  _context6.next = 2;
                  return this.backendPromise;

                case 2:
                  backend = _context6.sent;
                  return _context6.abrupt("return", backend.get(key));

                case 4:
                case "end":
                  return _context6.stop();
              }
            }
          }

          return _callee6$;
        }(), _callee6, this);
      }

      return _callee6;
    }()));

    function get(_x5) {
      return _get2.apply(this, arguments);
    }

    return get;
  }();

  _proto4.set = /*#__PURE__*/function () {
    var _set2 = _asyncToGenerator( /*#__PURE__*/regeneratorRuntime.mark(function () {
      function _callee7(key, value) {
        var backend;
        return regeneratorRuntime.wrap(function () {
          function _callee7$(_context7) {
            while (1) {
              switch (_context7.prev = _context7.next) {
                case 0:
                  _context7.next = 2;
                  return this.backendPromise;

                case 2:
                  backend = _context7.sent;
                  return _context7.abrupt("return", backend.set(key, value));

                case 4:
                case "end":
                  return _context7.stop();
              }
            }
          }

          return _callee7$;
        }(), _callee7, this);
      }

      return _callee7;
    }()));

    function set(_x6, _x7) {
      return _set2.apply(this, arguments);
    }

    return set;
  }();

  _proto4.remove = /*#__PURE__*/function () {
    var _remove2 = _asyncToGenerator( /*#__PURE__*/regeneratorRuntime.mark(function () {
      function _callee8(key) {
        var backend;
        return regeneratorRuntime.wrap(function () {
          function _callee8$(_context8) {
            while (1) {
              switch (_context8.prev = _context8.next) {
                case 0:
                  _context8.next = 2;
                  return this.backendPromise;

                case 2:
                  backend = _context8.sent;
                  return _context8.abrupt("return", backend.remove(key));

                case 4:
                case "end":
                  return _context8.stop();
              }
            }
          }

          return _callee8$;
        }(), _callee8, this);
      }

      return _callee8;
    }()));

    function remove(_x8) {
      return _remove2.apply(this, arguments);
    }

    return remove;
  }();

  _proto4.clear = /*#__PURE__*/function () {
    var _clear2 = _asyncToGenerator( /*#__PURE__*/regeneratorRuntime.mark(function () {
      function _callee9() {
        var backend;
        return regeneratorRuntime.wrap(function () {
          function _callee9$(_context9) {
            while (1) {
              switch (_context9.prev = _context9.next) {
                case 0:
                  _context9.next = 2;
                  return this.backendPromise;

                case 2:
                  backend = _context9.sent;
                  return _context9.abrupt("return", backend.clear());

                case 4:
                case "end":
                  return _context9.stop();
              }
            }
          }

          return _callee9$;
        }(), _callee9, this);
      }

      return _callee9;
    }()));

    function clear() {
      return _clear2.apply(this, arguments);
    }

    return clear;
  }();

  return StorageProxy;
}();

var storage = new StorageProxy();
exports.storage = storage;

/***/ }),

/***/ 21118:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.buildQueryString = exports.decodeHtmlEntities = exports.toTitleCase = exports.capitalize = exports.createSearch = exports.createGlobPattern = exports.multiline = void 0;

function _createForOfIteratorHelperLoose(o, allowArrayLike) { var it = typeof Symbol !== "undefined" && o[Symbol.iterator] || o["@@iterator"]; if (it) return (it = it.call(o)).next.bind(it); if (Array.isArray(o) || (it = _unsupportedIterableToArray(o)) || allowArrayLike && o && typeof o.length === "number") { if (it) o = it; var i = 0; return function () { if (i >= o.length) return { done: true }; return { done: false, value: o[i++] }; }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }

function _unsupportedIterableToArray(o, minLen) { if (!o) return; if (typeof o === "string") return _arrayLikeToArray(o, minLen); var n = Object.prototype.toString.call(o).slice(8, -1); if (n === "Object" && o.constructor) n = o.constructor.name; if (n === "Map" || n === "Set") return Array.from(o); if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen); }

function _arrayLikeToArray(arr, len) { if (len == null || len > arr.length) len = arr.length; for (var i = 0, arr2 = new Array(len); i < len; i++) { arr2[i] = arr[i]; } return arr2; }

/**
 * @file
 * @copyright 2020 Aleksej Komarov
 * @license MIT
 */

/**
 * Removes excess whitespace and indentation from the string.
 */
var multiline = function multiline(str) {
  if (Array.isArray(str)) {
    // Small stub to allow usage as a template tag
    return multiline(str.join(''));
  }

  var lines = str.split('\n'); // Determine base indentation

  var minIndent;

  for (var _iterator = _createForOfIteratorHelperLoose(lines), _step; !(_step = _iterator()).done;) {
    var line = _step.value;

    for (var indent = 0; indent < line.length; indent++) {
      var _char = line[indent];

      if (_char !== ' ') {
        if (minIndent === undefined || indent < minIndent) {
          minIndent = indent;
        }

        break;
      }
    }
  }

  if (!minIndent) {
    minIndent = 0;
  } // Remove this base indentation and trim the resulting string
  // from both ends.


  return lines.map(function (line) {
    return line.substr(minIndent).trimRight();
  }).join('\n').trim();
};
/**
 * Creates a glob pattern matcher.
 *
 * Matches strings with wildcards.
 *
 * Example: createGlobPattern('*@domain')('user@domain') === true
 */


exports.multiline = multiline;

var createGlobPattern = function createGlobPattern(pattern) {
  var escapeString = function escapeString(str) {
    return str.replace(/[|\\{}()[\]^$+*?.]/g, '\\$&');
  };

  var regex = new RegExp('^' + pattern.split(/\*+/).map(escapeString).join('.*') + '$');
  return function (str) {
    return regex.test(str);
  };
};
/**
 * Creates a search terms matcher.
 *
 * Returns true if given string matches the search text.
 *
 * @template T
 * @param {string} searchText
 * @param {(obj: T) => string} stringifier
 * @returns {(obj: T) => boolean}
 */


exports.createGlobPattern = createGlobPattern;

var createSearch = function createSearch(searchText, stringifier) {
  var preparedSearchText = searchText.toLowerCase().trim();
  return function (obj) {
    if (!preparedSearchText) {
      return true;
    }

    var str = stringifier ? stringifier(obj) : obj;

    if (!str) {
      return false;
    }

    return str.toLowerCase().includes(preparedSearchText);
  };
};

exports.createSearch = createSearch;

var capitalize = function capitalize(str) {
  // Handle array
  if (Array.isArray(str)) {
    return str.map(capitalize);
  } // Handle string


  return str.charAt(0).toUpperCase() + str.slice(1).toLowerCase();
};

exports.capitalize = capitalize;

var toTitleCase = function toTitleCase(str) {
  // Handle array
  if (Array.isArray(str)) {
    return str.map(toTitleCase);
  } // Pass non-string


  if (typeof str !== 'string') {
    return str;
  } // Handle string


  var WORDS_UPPER = ['Id', 'Tv'];
  var WORDS_LOWER = ['A', 'An', 'And', 'As', 'At', 'But', 'By', 'For', 'For', 'From', 'In', 'Into', 'Near', 'Nor', 'Of', 'On', 'Onto', 'Or', 'The', 'To', 'With'];
  var currentStr = str.replace(/([^\W_]+[^\s-]*) */g, function (str) {
    return str.charAt(0).toUpperCase() + str.substr(1).toLowerCase();
  });

  for (var _i = 0, _WORDS_LOWER = WORDS_LOWER; _i < _WORDS_LOWER.length; _i++) {
    var word = _WORDS_LOWER[_i];
    var regex = new RegExp('\\s' + word + '\\s', 'g');
    currentStr = currentStr.replace(regex, function (str) {
      return str.toLowerCase();
    });
  }

  for (var _i2 = 0, _WORDS_UPPER = WORDS_UPPER; _i2 < _WORDS_UPPER.length; _i2++) {
    var _word = _WORDS_UPPER[_i2];

    var _regex = new RegExp('\\b' + _word + '\\b', 'g');

    currentStr = currentStr.replace(_regex, function (str) {
      return str.toLowerCase();
    });
  }

  return currentStr;
};
/**
 * Decodes HTML entities, and removes unnecessary HTML tags.
 *
 * @param  {String} str Encoded HTML string
 * @return {String} Decoded HTML string
 */


exports.toTitleCase = toTitleCase;

var decodeHtmlEntities = function decodeHtmlEntities(str) {
  if (!str) {
    return str;
  }

  var translate_re = /&(nbsp|amp|quot|lt|gt|apos);/g;
  var translate = {
    nbsp: ' ',
    amp: '&',
    quot: '"',
    lt: '<',
    gt: '>',
    apos: '\''
  };
  return str // Newline tags
  .replace(/<br>/gi, '\n').replace(/<\/?[a-z0-9-_]+[^>]*>/gi, '') // Basic entities
  .replace(translate_re, function (match, entity) {
    return translate[entity];
  }) // Decimal entities
  .replace(/&#?([0-9]+);/gi, function (match, numStr) {
    var num = parseInt(numStr, 10);
    return String.fromCharCode(num);
  }) // Hex entities
  .replace(/&#x?([0-9a-f]+);/gi, function (match, numStr) {
    var num = parseInt(numStr, 16);
    return String.fromCharCode(num);
  });
};
/**
 * Converts an object into a query string,
 */


exports.decodeHtmlEntities = decodeHtmlEntities;

var buildQueryString = function buildQueryString(obj) {
  return Object.keys(obj).map(function (key) {
    return encodeURIComponent(key) + '=' + encodeURIComponent(obj[key]);
  }).join('&');
};

exports.buildQueryString = buildQueryString;

/***/ }),

/***/ 53538:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.sleep = exports.debounce = void 0;

/**
 * @file
 * @copyright 2020 Aleksej Komarov
 * @license MIT
 */

/**
 * Returns a function, that, as long as it continues to be invoked, will
 * not be triggered. The function will be called after it stops being
 * called for N milliseconds. If `immediate` is passed, trigger the
 * function on the leading edge, instead of the trailing.
 */
var debounce = function debounce(fn, time, immediate) {
  if (immediate === void 0) {
    immediate = false;
  }

  var timeout;
  return function () {
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }

    var later = function later() {
      timeout = null;

      if (!immediate) {
        fn.apply(void 0, args);
      }
    };

    var callNow = immediate && !timeout;
    clearTimeout(timeout);
    timeout = setTimeout(later, time);

    if (callNow) {
      fn.apply(void 0, args);
    }
  };
};
/**
 * Suspends an asynchronous function for N milliseconds.
 *
 * @param {number} time
 */


exports.debounce = debounce;

var sleep = function sleep(time) {
  return new Promise(function (resolve) {
    return setTimeout(resolve, time);
  });
};

exports.sleep = sleep;

/***/ }),

/***/ 67288:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.vecNormalize = exports.vecLength = exports.vecInverse = exports.vecScale = exports.vecDivide = exports.vecMultiply = exports.vecSubtract = exports.vecAdd = void 0;

var _collections = __webpack_require__(1397);

/**
 * N-dimensional vector manipulation functions.
 *
 * Vectors are plain number arrays, i.e. [x, y, z].
 *
 * @file
 * @copyright 2020 Aleksej Komarov
 * @license MIT
 */
var ADD = function ADD(a, b) {
  return a + b;
};

var SUB = function SUB(a, b) {
  return a - b;
};

var MUL = function MUL(a, b) {
  return a * b;
};

var DIV = function DIV(a, b) {
  return a / b;
};

var vecAdd = function vecAdd() {
  for (var _len = arguments.length, vecs = new Array(_len), _key = 0; _key < _len; _key++) {
    vecs[_key] = arguments[_key];
  }

  return (0, _collections.reduce)(function (a, b) {
    return (0, _collections.zipWith)(ADD)(a, b);
  })(vecs);
};

exports.vecAdd = vecAdd;

var vecSubtract = function vecSubtract() {
  for (var _len2 = arguments.length, vecs = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
    vecs[_key2] = arguments[_key2];
  }

  return (0, _collections.reduce)(function (a, b) {
    return (0, _collections.zipWith)(SUB)(a, b);
  })(vecs);
};

exports.vecSubtract = vecSubtract;

var vecMultiply = function vecMultiply() {
  for (var _len3 = arguments.length, vecs = new Array(_len3), _key3 = 0; _key3 < _len3; _key3++) {
    vecs[_key3] = arguments[_key3];
  }

  return (0, _collections.reduce)(function (a, b) {
    return (0, _collections.zipWith)(MUL)(a, b);
  })(vecs);
};

exports.vecMultiply = vecMultiply;

var vecDivide = function vecDivide() {
  for (var _len4 = arguments.length, vecs = new Array(_len4), _key4 = 0; _key4 < _len4; _key4++) {
    vecs[_key4] = arguments[_key4];
  }

  return (0, _collections.reduce)(function (a, b) {
    return (0, _collections.zipWith)(DIV)(a, b);
  })(vecs);
};

exports.vecDivide = vecDivide;

var vecScale = function vecScale(vec, n) {
  return (0, _collections.map)(function (x) {
    return x * n;
  })(vec);
};

exports.vecScale = vecScale;

var vecInverse = function vecInverse(vec) {
  return (0, _collections.map)(function (x) {
    return -x;
  })(vec);
};

exports.vecInverse = vecInverse;

var vecLength = function vecLength(vec) {
  return Math.sqrt((0, _collections.reduce)(ADD)((0, _collections.zipWith)(MUL)(vec, vec)));
};

exports.vecLength = vecLength;

var vecNormalize = function vecNormalize(vec) {
  return vecDivide(vec, vecLength(vec));
};

exports.vecNormalize = vecNormalize;

/***/ }),

/***/ 88280:
/***/ (function() {

"use strict";


/**
 * CSS Object Model patches
 *
 * Adapted from: https://github.com/shawnbot/aight
 *
 * @file
 * @copyright 2020 Aleksej Komarov
 * @license MIT
 */

/* eslint-disable */
(function (Proto) {
  'use strict';

  if (typeof Proto.setAttribute !== 'undefined') {
    var toAttr = function toAttr(prop) {
      return prop.replace(/-[a-z]/g, function (bit) {
        return bit[1].toUpperCase();
      });
    };

    Proto.setProperty = function (prop, value) {
      var attr = toAttr(prop);

      if (!value) {
        return this.removeAttribute(attr);
      }

      var str = String(value);
      return this.setAttribute(attr, str);
    };

    Proto.getPropertyValue = function (prop) {
      var attr = toAttr(prop);
      return this.getAttribute(attr) || null;
    };

    Proto.removeProperty = function (prop) {
      var attr = toAttr(prop);
      var value = this.getAttribute(attr);
      this.removeAttribute(attr);
      return value;
    };
  }
})(CSSStyleDeclaration.prototype);

/***/ }),

/***/ 15511:
/***/ (function() {

"use strict";


/**
 * @file
 * @copyright 2013 Andrea Giammarchi, WebReflection
 * @license MIT
 */

/* eslint-disable */
(function (window) {
  'use strict';
  /* jshint loopfunc: true, noempty: false*/
  // http://www.w3.org/TR/dom/#element

  function createDocumentFragment() {
    return document.createDocumentFragment();
  }

  function createElement(nodeName) {
    return document.createElement(nodeName);
  }

  function enoughArguments(length, name) {
    if (!length) throw new Error('Failed to construct ' + name + ': 1 argument required, but only 0 present.');
  }

  function mutationMacro(nodes) {
    if (nodes.length === 1) {
      return textNodeIfPrimitive(nodes[0]);
    }

    for (var fragment = createDocumentFragment(), list = slice.call(nodes), i = 0; i < nodes.length; i++) {
      fragment.appendChild(textNodeIfPrimitive(list[i]));
    }

    return fragment;
  }

  function textNodeIfPrimitive(node) {
    return typeof node === 'object' ? node : document.createTextNode(node);
  }

  for (var head, property, TemporaryPrototype, TemporaryTokenList, wrapVerifyToken, document = window.document, hOP = Object.prototype.hasOwnProperty, defineProperty = Object.defineProperty || function (object, property, descriptor) {
    if (hOP.call(descriptor, 'value')) {
      object[property] = descriptor.value;
    } else {
      if (hOP.call(descriptor, 'get')) object.__defineGetter__(property, descriptor.get);
      if (hOP.call(descriptor, 'set')) object.__defineSetter__(property, descriptor.set);
    }

    return object;
  }, indexOf = [].indexOf || function () {
    function indexOf(value) {
      var length = this.length;

      while (length--) {
        if (this[length] === value) {
          break;
        }
      }

      return length;
    }

    return indexOf;
  }(), // http://www.w3.org/TR/domcore/#domtokenlist
  verifyToken = function verifyToken(token) {
    if (!token) {
      throw 'SyntaxError';
    } else if (spaces.test(token)) {
      throw 'InvalidCharacterError';
    }

    return token;
  }, DOMTokenList = function DOMTokenList(node) {
    var noClassName = typeof node.className === 'undefined',
        className = noClassName ? node.getAttribute('class') || '' : node.className,
        isSVG = noClassName || typeof className === 'object',
        value = (isSVG ? noClassName ? className : className.baseVal : className).replace(trim, '');

    if (value.length) {
      properties.push.apply(this, value.split(spaces));
    }

    this._isSVG = isSVG;
    this._ = node;
  }, classListDescriptor = {
    get: function () {
      function get() {
        return new DOMTokenList(this);
      }

      return get;
    }(),
    set: function () {
      function set() {}

      return set;
    }()
  }, trim = /^\s+|\s+$/g, spaces = /\s+/, SPACE = '\x20', CLASS_LIST = 'classList', toggle = function () {
    function toggle(token, force) {
      if (this.contains(token)) {
        if (!force) {
          // force is not true (either false or omitted)
          this.remove(token);
        }
      } else if (force === undefined || force) {
        force = true;
        this.add(token);
      }

      return !!force;
    }

    return toggle;
  }(), DocumentFragmentPrototype = window.DocumentFragment && DocumentFragment.prototype, Node = window.Node, NodePrototype = (Node || Element).prototype, CharacterData = window.CharacterData || Node, CharacterDataPrototype = CharacterData && CharacterData.prototype, DocumentType = window.DocumentType, DocumentTypePrototype = DocumentType && DocumentType.prototype, ElementPrototype = (window.Element || Node || window.HTMLElement).prototype, HTMLSelectElement = window.HTMLSelectElement || createElement('select').constructor, selectRemove = HTMLSelectElement.prototype.remove, SVGElement = window.SVGElement, properties = ['matches', ElementPrototype.matchesSelector || ElementPrototype.webkitMatchesSelector || ElementPrototype.khtmlMatchesSelector || ElementPrototype.mozMatchesSelector || ElementPrototype.msMatchesSelector || ElementPrototype.oMatchesSelector || function () {
    function matches(selector) {
      var parentNode = this.parentNode;
      return !!parentNode && -1 < indexOf.call(parentNode.querySelectorAll(selector), this);
    }

    return matches;
  }(), 'closest', function () {
    function closest(selector) {
      var parentNode = this,
          matches;

      while ( // document has no .matches
      (matches = parentNode && parentNode.matches) && !parentNode.matches(selector)) {
        parentNode = parentNode.parentNode;
      }

      return matches ? parentNode : null;
    }

    return closest;
  }(), 'prepend', function () {
    function prepend() {
      var firstChild = this.firstChild,
          node = mutationMacro(arguments);

      if (firstChild) {
        this.insertBefore(node, firstChild);
      } else {
        this.appendChild(node);
      }
    }

    return prepend;
  }(), 'append', function () {
    function append() {
      this.appendChild(mutationMacro(arguments));
    }

    return append;
  }(), 'before', function () {
    function before() {
      var parentNode = this.parentNode;

      if (parentNode) {
        parentNode.insertBefore(mutationMacro(arguments), this);
      }
    }

    return before;
  }(), 'after', function () {
    function after() {
      var parentNode = this.parentNode,
          nextSibling = this.nextSibling,
          node = mutationMacro(arguments);

      if (parentNode) {
        if (nextSibling) {
          parentNode.insertBefore(node, nextSibling);
        } else {
          parentNode.appendChild(node);
        }
      }
    }

    return after;
  }(), // https://dom.spec.whatwg.org/#dom-element-toggleattribute
  'toggleAttribute', function () {
    function toggleAttribute(name, force) {
      var had = this.hasAttribute(name);

      if (1 < arguments.length) {
        if (had && !force) this.removeAttribute(name);else if (force && !had) this.setAttribute(name, "");
      } else if (had) this.removeAttribute(name);else this.setAttribute(name, "");

      return this.hasAttribute(name);
    }

    return toggleAttribute;
  }(), // WARNING - DEPRECATED - use .replaceWith() instead
  'replace', function () {
    function replace() {
      this.replaceWith.apply(this, arguments);
    }

    return replace;
  }(), 'replaceWith', function () {
    function replaceWith() {
      var parentNode = this.parentNode;

      if (parentNode) {
        parentNode.replaceChild(mutationMacro(arguments), this);
      }
    }

    return replaceWith;
  }(), 'remove', function () {
    function remove() {
      var parentNode = this.parentNode;

      if (parentNode) {
        parentNode.removeChild(this);
      }
    }

    return remove;
  }()], slice = properties.slice, i = properties.length; i; i -= 2) {
    property = properties[i - 2];

    if (!(property in ElementPrototype)) {
      ElementPrototype[property] = properties[i - 1];
    } // avoid unnecessary re-patch when the script is included
    // gazillion times without any reason whatsoever
    // https://github.com/WebReflection/dom4/pull/48


    if (property === 'remove' && !selectRemove._dom4) {
      // see https://github.com/WebReflection/dom4/issues/19
      (HTMLSelectElement.prototype[property] = function () {
        return 0 < arguments.length ? selectRemove.apply(this, arguments) : ElementPrototype.remove.call(this);
      })._dom4 = true;
    } // see https://github.com/WebReflection/dom4/issues/18


    if (/^(?:before|after|replace|replaceWith|remove)$/.test(property)) {
      if (CharacterData && !(property in CharacterDataPrototype)) {
        CharacterDataPrototype[property] = properties[i - 1];
      }

      if (DocumentType && !(property in DocumentTypePrototype)) {
        DocumentTypePrototype[property] = properties[i - 1];
      }
    } // see https://github.com/WebReflection/dom4/pull/26


    if (/^(?:append|prepend)$/.test(property)) {
      if (DocumentFragmentPrototype) {
        if (!(property in DocumentFragmentPrototype)) {
          DocumentFragmentPrototype[property] = properties[i - 1];
        }
      } else {
        try {
          createDocumentFragment().constructor.prototype[property] = properties[i - 1];
        } catch (o_O) {}
      }
    }
  } // most likely an IE9 only issue
  // see https://github.com/WebReflection/dom4/issues/6


  if (!createElement('a').matches('a')) {
    ElementPrototype[property] = function (matches) {
      return function (selector) {
        return matches.call(this.parentNode ? this : createDocumentFragment().appendChild(this), selector);
      };
    }(ElementPrototype[property]);
  } // used to fix both old webkit and SVG


  DOMTokenList.prototype = {
    length: 0,
    add: function () {
      function add() {
        for (var j = 0, token; j < arguments.length; j++) {
          token = arguments[j];

          if (!this.contains(token)) {
            properties.push.call(this, property);
          }
        }

        if (this._isSVG) {
          this._.setAttribute('class', '' + this);
        } else {
          this._.className = '' + this;
        }
      }

      return add;
    }(),
    contains: function (indexOf) {
      return function () {
        function contains(token) {
          i = indexOf.call(this, property = verifyToken(token));
          return -1 < i;
        }

        return contains;
      }();
    }([].indexOf || function (token) {
      i = this.length;

      while (i-- && this[i] !== token) {}

      return i;
    }),
    item: function () {
      function item(i) {
        return this[i] || null;
      }

      return item;
    }(),
    remove: function () {
      function remove() {
        for (var j = 0, token; j < arguments.length; j++) {
          token = arguments[j];

          if (this.contains(token)) {
            properties.splice.call(this, i, 1);
          }
        }

        if (this._isSVG) {
          this._.setAttribute('class', '' + this);
        } else {
          this._.className = '' + this;
        }
      }

      return remove;
    }(),
    toggle: toggle,
    toString: function () {
      function toString() {
        return properties.join.call(this, SPACE);
      }

      return toString;
    }()
  };

  if (SVGElement && !(CLASS_LIST in SVGElement.prototype)) {
    defineProperty(SVGElement.prototype, CLASS_LIST, classListDescriptor);
  } // http://www.w3.org/TR/dom/#domtokenlist
  // iOS 5.1 has completely screwed this property
  // classList in ElementPrototype is false
  // but it's actually there as getter


  if (!(CLASS_LIST in document.documentElement)) {
    defineProperty(ElementPrototype, CLASS_LIST, classListDescriptor);
  } else {
    // iOS 5.1 and Nokia ASHA do not support multiple add or remove
    // trying to detect and fix that in here
    TemporaryTokenList = createElement('div')[CLASS_LIST];
    TemporaryTokenList.add('a', 'b', 'a');

    if ('a\x20b' != TemporaryTokenList) {
      // no other way to reach original methods in iOS 5.1
      TemporaryPrototype = TemporaryTokenList.constructor.prototype;

      if (!('add' in TemporaryPrototype)) {
        // ASHA double fails in here
        TemporaryPrototype = window.TemporaryTokenList.prototype;
      }

      wrapVerifyToken = function wrapVerifyToken(original) {
        return function () {
          var i = 0;

          while (i < arguments.length) {
            original.call(this, arguments[i++]);
          }
        };
      };

      TemporaryPrototype.add = wrapVerifyToken(TemporaryPrototype.add);
      TemporaryPrototype.remove = wrapVerifyToken(TemporaryPrototype.remove); // toggle is broken too ^_^ ... let's fix it

      TemporaryPrototype.toggle = toggle;
    }
  }

  if (!('contains' in NodePrototype)) {
    defineProperty(NodePrototype, 'contains', {
      value: function () {
        function value(el) {
          while (el && el !== this) {
            el = el.parentNode;
          }

          return this === el;
        }

        return value;
      }()
    });
  }

  if (!('head' in document)) {
    defineProperty(document, 'head', {
      get: function () {
        function get() {
          return head || (head = document.getElementsByTagName('head')[0]);
        }

        return get;
      }()
    });
  } // requestAnimationFrame partial polyfill


  (function () {
    for (var raf, rAF = window.requestAnimationFrame, cAF = window.cancelAnimationFrame, prefixes = ['o', 'ms', 'moz', 'webkit'], i = prefixes.length; !cAF && i--;) {
      rAF = rAF || window[prefixes[i] + 'RequestAnimationFrame'];
      cAF = window[prefixes[i] + 'CancelAnimationFrame'] || window[prefixes[i] + 'CancelRequestAnimationFrame'];
    }

    if (!cAF) {
      // some FF apparently implemented rAF but no cAF
      if (rAF) {
        raf = rAF;

        rAF = function rAF(callback) {
          var goOn = true;
          raf(function () {
            if (goOn) callback.apply(this, arguments);
          });
          return function () {
            goOn = false;
          };
        };

        cAF = function cAF(id) {
          id();
        };
      } else {
        rAF = function rAF(callback) {
          return setTimeout(callback, 15, 15);
        };

        cAF = function cAF(id) {
          clearTimeout(id);
        };
      }
    }

    window.requestAnimationFrame = rAF;
    window.cancelAnimationFrame = cAF;
  })(); // http://www.w3.org/TR/dom/#customevent


  try {
    new window.CustomEvent('?');
  } catch (o_O) {
    window.CustomEvent = function (eventName, defaultInitDict) {
      // the infamous substitute
      function CustomEvent(type, eventInitDict) {
        /*jshint eqnull:true */
        var event = document.createEvent(eventName);

        if (typeof type != 'string') {
          throw new Error('An event name must be provided');
        }

        if (eventName == 'Event') {
          event.initCustomEvent = initCustomEvent;
        }

        if (eventInitDict == null) {
          eventInitDict = defaultInitDict;
        }

        event.initCustomEvent(type, eventInitDict.bubbles, eventInitDict.cancelable, eventInitDict.detail);
        return event;
      } // attached at runtime


      function initCustomEvent(type, bubbles, cancelable, detail) {
        /*jshint validthis:true*/
        this.initEvent(type, bubbles, cancelable);
        this.detail = detail;
      } // that's it


      return CustomEvent;
    }( // is this IE9 or IE10 ?
    // where CustomEvent is there
    // but not usable as construtor ?
    window.CustomEvent ? // use the CustomEvent interface in such case
    'CustomEvent' : 'Event', // otherwise the common compatible one
    {
      bubbles: false,
      cancelable: false,
      detail: null
    });
  } // window.Event as constructor


  try {
    new Event('_');
  } catch (o_O) {
    /* jshint -W022 */
    o_O = function ($Event) {
      function Event(type, init) {
        enoughArguments(arguments.length, 'Event');
        var out = document.createEvent('Event');
        if (!init) init = {};
        out.initEvent(type, !!init.bubbles, !!init.cancelable);
        return out;
      }

      Event.prototype = $Event.prototype;
      return Event;
    }(window.Event || function () {
      function Event() {}

      return Event;
    }());

    defineProperty(window, 'Event', {
      value: o_O
    }); // Android 4 gotcha

    if (Event !== o_O) Event = o_O;
  } // window.KeyboardEvent as constructor


  try {
    new KeyboardEvent('_', {});
  } catch (o_O) {
    /* jshint -W022 */
    o_O = function ($KeyboardEvent) {
      // code inspired by https://gist.github.com/termi/4654819
      var initType = 0,
          defaults = {
        "char": '',
        key: '',
        location: 0,
        ctrlKey: false,
        shiftKey: false,
        altKey: false,
        metaKey: false,
        altGraphKey: false,
        repeat: false,
        locale: navigator.language,
        detail: 0,
        bubbles: false,
        cancelable: false,
        keyCode: 0,
        charCode: 0,
        which: 0
      },
          eventType;

      try {
        var e = document.createEvent('KeyboardEvent');
        e.initKeyboardEvent('keyup', false, false, window, '+', 3, true, false, true, false, false);
        initType = (e.keyIdentifier || e.key) == '+' && (e.keyLocation || e.location) == 3 && (e.ctrlKey ? e.altKey ? 1 : 3 : e.shiftKey ? 2 : 4) || 9;
      } catch (o_O) {}

      eventType = 0 < initType ? 'KeyboardEvent' : 'Event';

      function getModifier(init) {
        for (var out = [], keys = ['ctrlKey', 'Control', 'shiftKey', 'Shift', 'altKey', 'Alt', 'metaKey', 'Meta', 'altGraphKey', 'AltGraph'], i = 0; i < keys.length; i += 2) {
          if (init[keys[i]]) out.push(keys[i + 1]);
        }

        return out.join(' ');
      }

      function withDefaults(target, source) {
        for (var key in source) {
          if (source.hasOwnProperty(key) && !source.hasOwnProperty.call(target, key)) target[key] = source[key];
        }

        return target;
      }

      function withInitValues(key, out, init) {
        try {
          out[key] = init[key];
        } catch (o_O) {}
      }

      function KeyboardEvent(type, init) {
        enoughArguments(arguments.length, 'KeyboardEvent');
        init = withDefaults(init || {}, defaults);
        var out = document.createEvent(eventType),
            ctrlKey = init.ctrlKey,
            shiftKey = init.shiftKey,
            altKey = init.altKey,
            metaKey = init.metaKey,
            altGraphKey = init.altGraphKey,
            modifiers = initType > 3 ? getModifier(init) : null,
            key = String(init.key),
            chr = String(init["char"]),
            location = init.location,
            keyCode = init.keyCode || (init.keyCode = key) && key.charCodeAt(0) || 0,
            charCode = init.charCode || (init.charCode = chr) && chr.charCodeAt(0) || 0,
            bubbles = init.bubbles,
            cancelable = init.cancelable,
            repeat = init.repeat,
            locale = init.locale,
            view = init.view || window,
            args;
        if (!init.which) init.which = init.keyCode;

        if ('initKeyEvent' in out) {
          out.initKeyEvent(type, bubbles, cancelable, view, ctrlKey, altKey, shiftKey, metaKey, keyCode, charCode);
        } else if (0 < initType && 'initKeyboardEvent' in out) {
          args = [type, bubbles, cancelable, view];

          switch (initType) {
            case 1:
              args.push(key, location, ctrlKey, shiftKey, altKey, metaKey, altGraphKey);
              break;

            case 2:
              args.push(ctrlKey, altKey, shiftKey, metaKey, keyCode, charCode);
              break;

            case 3:
              args.push(key, location, ctrlKey, altKey, shiftKey, metaKey, altGraphKey);
              break;

            case 4:
              args.push(key, location, modifiers, repeat, locale);
              break;

            default:
              args.push(char, key, location, modifiers, repeat, locale);
          }

          out.initKeyboardEvent.apply(out, args);
        } else {
          out.initEvent(type, bubbles, cancelable);
        }

        for (key in out) {
          if (defaults.hasOwnProperty(key) && out[key] !== init[key]) {
            withInitValues(key, out, init);
          }
        }

        return out;
      }

      KeyboardEvent.prototype = $KeyboardEvent.prototype;
      return KeyboardEvent;
    }(window.KeyboardEvent || function () {
      function KeyboardEvent() {}

      return KeyboardEvent;
    }());

    defineProperty(window, 'KeyboardEvent', {
      value: o_O
    }); // Android 4 gotcha

    if (KeyboardEvent !== o_O) KeyboardEvent = o_O;
  } // window.MouseEvent as constructor


  try {
    new MouseEvent('_', {});
  } catch (o_O) {
    /* jshint -W022 */
    o_O = function ($MouseEvent) {
      function MouseEvent(type, init) {
        enoughArguments(arguments.length, 'MouseEvent');
        var out = document.createEvent('MouseEvent');
        if (!init) init = {};
        out.initMouseEvent(type, !!init.bubbles, !!init.cancelable, init.view || window, init.detail || 1, init.screenX || 0, init.screenY || 0, init.clientX || 0, init.clientY || 0, !!init.ctrlKey, !!init.altKey, !!init.shiftKey, !!init.metaKey, init.button || 0, init.relatedTarget || null);
        return out;
      }

      MouseEvent.prototype = $MouseEvent.prototype;
      return MouseEvent;
    }(window.MouseEvent || function () {
      function MouseEvent() {}

      return MouseEvent;
    }());

    defineProperty(window, 'MouseEvent', {
      value: o_O
    }); // Android 4 gotcha

    if (MouseEvent !== o_O) MouseEvent = o_O;
  }

  if (!document.querySelectorAll('*').forEach) {
    (function () {
      function patch(what) {
        var querySelectorAll = what.querySelectorAll;

        what.querySelectorAll = function () {
          function qSA(css) {
            var result = querySelectorAll.call(this, css);
            result.forEach = Array.prototype.forEach;
            return result;
          }

          return qSA;
        }();
      }

      patch(document);
      patch(Element.prototype);
    })();
  }

  try {
    // https://drafts.csswg.org/selectors-4/#the-scope-pseudo
    document.querySelector(':scope *');
  } catch (o_O) {
    (function () {
      var dataScope = 'data-scope-' + (Math.random() * 1e9 >>> 0);
      var proto = Element.prototype;
      var querySelector = proto.querySelector;
      var querySelectorAll = proto.querySelectorAll;

      proto.querySelector = function () {
        function qS(css) {
          return find(this, querySelector, css);
        }

        return qS;
      }();

      proto.querySelectorAll = function () {
        function qSA(css) {
          return find(this, querySelectorAll, css);
        }

        return qSA;
      }();

      function find(node, method, css) {
        node.setAttribute(dataScope, null);
        var result = method.call(node, String(css).replace(/(^|,\s*)(:scope([ >]|$))/g, function ($0, $1, $2, $3) {
          return $1 + '[' + dataScope + ']' + ($3 || ' ');
        }));
        node.removeAttribute(dataScope);
        return result;
      }
    })();
  }
})(window);

(function (global) {
  'use strict'; // a WeakMap fallback for DOM nodes only used as key

  var DOMMap = global.WeakMap || function () {
    var counter = 0,
        dispatched = false,
        drop = false,
        value;

    function dispatch(key, ce, shouldDrop) {
      drop = shouldDrop;
      dispatched = false;
      value = undefined;
      key.dispatchEvent(ce);
    }

    function Handler(value) {
      this.value = value;
    }

    Handler.prototype.handleEvent = function () {
      function handleEvent(e) {
        dispatched = true;

        if (drop) {
          e.currentTarget.removeEventListener(e.type, this, false);
        } else {
          value = this.value;
        }
      }

      return handleEvent;
    }();

    function DOMMap() {
      counter++; // make id clashing highly improbable

      this.__ce__ = new Event('@DOMMap:' + counter + Math.random());
    }

    DOMMap.prototype = {
      'constructor': DOMMap,
      'delete': function () {
        function del(key) {
          return dispatch(key, this.__ce__, true), dispatched;
        }

        return del;
      }(),
      'get': function () {
        function get(key) {
          dispatch(key, this.__ce__, false);
          var v = value;
          value = undefined;
          return v;
        }

        return get;
      }(),
      'has': function () {
        function has(key) {
          return dispatch(key, this.__ce__, false), dispatched;
        }

        return has;
      }(),
      'set': function () {
        function set(key, value) {
          dispatch(key, this.__ce__, true);
          key.addEventListener(this.__ce__.type, new Handler(value), false);
          return this;
        }

        return set;
      }()
    };
    return DOMMap;
  }();

  function Dict() {}

  Dict.prototype = (Object.create || Object)(null); // https://dom.spec.whatwg.org/#interface-eventtarget

  function createEventListener(type, callback, options) {
    function eventListener(e) {
      if (eventListener.once) {
        e.currentTarget.removeEventListener(e.type, callback, eventListener);
        eventListener.removed = true;
      }

      if (eventListener.passive) {
        e.preventDefault = createEventListener.preventDefault;
      }

      if (typeof eventListener.callback === 'function') {
        /* jshint validthis: true */
        eventListener.callback.call(this, e);
      } else if (eventListener.callback) {
        eventListener.callback.handleEvent(e);
      }

      if (eventListener.passive) {
        delete e.preventDefault;
      }
    }

    eventListener.type = type;
    eventListener.callback = callback;
    eventListener.capture = !!options.capture;
    eventListener.passive = !!options.passive;
    eventListener.once = !!options.once; // currently pointless but specs say to use it, so ...

    eventListener.removed = false;
    return eventListener;
  }

  createEventListener.preventDefault = function () {
    function preventDefault() {}

    return preventDefault;
  }();

  var Event = global.CustomEvent,
      dE = global.dispatchEvent,
      aEL = global.addEventListener,
      rEL = global.removeEventListener,
      counter = 0,
      increment = function increment() {
    counter++;
  },
      indexOf = [].indexOf || function () {
    function indexOf(value) {
      var length = this.length;

      while (length--) {
        if (this[length] === value) {
          break;
        }
      }

      return length;
    }

    return indexOf;
  }(),
      getListenerKey = function getListenerKey(options) {
    return ''.concat(options.capture ? '1' : '0', options.passive ? '1' : '0', options.once ? '1' : '0');
  },
      augment;

  try {
    aEL('_', increment, {
      once: true
    });
    dE(new Event('_'));
    dE(new Event('_'));
    rEL('_', increment, {
      once: true
    });
  } catch (o_O) {}

  if (counter !== 1) {
    (function () {
      var dm = new DOMMap();

      function createAEL(aEL) {
        return function () {
          function addEventListener(type, handler, options) {
            if (options && typeof options !== 'boolean') {
              var info = dm.get(this),
                  key = getListenerKey(options),
                  i,
                  tmp,
                  wrap;
              if (!info) dm.set(this, info = new Dict());
              if (!(type in info)) info[type] = {
                handler: [],
                wrap: []
              };
              tmp = info[type];
              i = indexOf.call(tmp.handler, handler);

              if (i < 0) {
                i = tmp.handler.push(handler) - 1;
                tmp.wrap[i] = wrap = new Dict();
              } else {
                wrap = tmp.wrap[i];
              }

              if (!(key in wrap)) {
                wrap[key] = createEventListener(type, handler, options);
                aEL.call(this, type, wrap[key], wrap[key].capture);
              }
            } else {
              aEL.call(this, type, handler, options);
            }
          }

          return addEventListener;
        }();
      }

      function createREL(rEL) {
        return function () {
          function removeEventListener(type, handler, options) {
            if (options && typeof options !== 'boolean') {
              var info = dm.get(this),
                  key,
                  i,
                  tmp,
                  wrap;

              if (info && type in info) {
                tmp = info[type];
                i = indexOf.call(tmp.handler, handler);

                if (-1 < i) {
                  key = getListenerKey(options);
                  wrap = tmp.wrap[i];

                  if (key in wrap) {
                    rEL.call(this, type, wrap[key], wrap[key].capture);
                    delete wrap[key]; // return if there are other wraps

                    for (key in wrap) {
                      return;
                    } // otherwise remove all the things


                    tmp.handler.splice(i, 1);
                    tmp.wrap.splice(i, 1); // if there are no other handlers

                    if (tmp.handler.length === 0) // drop the info[type] entirely
                      delete info[type];
                  }
                }
              }
            } else {
              rEL.call(this, type, handler, options);
            }
          }

          return removeEventListener;
        }();
      }

      augment = function augment(Constructor) {
        if (!Constructor) return;
        var proto = Constructor.prototype;
        proto.addEventListener = createAEL(proto.addEventListener);
        proto.removeEventListener = createREL(proto.removeEventListener);
      };

      if (global.EventTarget) {
        augment(EventTarget);
      } else {
        augment(global.Text);
        augment(global.Element || global.HTMLElement);
        augment(global.HTMLDocument);
        augment(global.Window || {
          prototype: global
        });
        augment(global.XMLHttpRequest);
      }
    })();
  }
})(window);

/***/ }),

/***/ 25513:
/***/ (function(module) {

"use strict";


/**
 * @file
 * @copyright 2014 Alexander Farkas
 * @license MIT
 */

/* eslint-disable */
(function (window, document) {
  /*jshint evil:true */

  /** version */
  var version = '3.7.3';
  /** Preset options */

  var options = window.html5 || {};
  /** Used to skip problem elements */

  var reSkip = /^<|^(?:button|map|select|textarea|object|iframe|option|optgroup)$/i;
  /** Not all elements can be cloned in IE **/

  var saveClones = /^(?:a|b|code|div|fieldset|h1|h2|h3|h4|h5|h6|i|label|li|ol|p|q|span|strong|style|table|tbody|td|th|tr|ul)$/i;
  /** Detect whether the browser supports default html5 styles */

  var supportsHtml5Styles;
  /** Name of the expando, to work with multiple documents or to re-shiv one document */

  var expando = '_html5shiv';
  /** The id for the the documents expando */

  var expanID = 0;
  /** Cached data for each document */

  var expandoData = {};
  /** Detect whether the browser supports unknown elements */

  var supportsUnknownElements;

  (function () {
    try {
      var a = document.createElement('a');
      a.innerHTML = '<xyz></xyz>'; //if the hidden property is implemented we can assume, that the browser supports basic HTML5 Styles

      supportsHtml5Styles = 'hidden' in a;

      supportsUnknownElements = a.childNodes.length == 1 || function () {
        // assign a false positive if unable to shiv
        document.createElement('a');
        var frag = document.createDocumentFragment();
        return typeof frag.cloneNode == 'undefined' || typeof frag.createDocumentFragment == 'undefined' || typeof frag.createElement == 'undefined';
      }();
    } catch (e) {
      // assign a false positive if detection fails => unable to shiv
      supportsHtml5Styles = true;
      supportsUnknownElements = true;
    }
  })();
  /*--------------------------------------------------------------------------*/

  /**
   * Creates a style sheet with the given CSS text and adds it to the document.
   * @private
   * @param {Document} ownerDocument The document.
   * @param {String} cssText The CSS text.
   * @returns {StyleSheet} The style element.
   */


  function addStyleSheet(ownerDocument, cssText) {
    var p = ownerDocument.createElement('p'),
        parent = ownerDocument.getElementsByTagName('head')[0] || ownerDocument.documentElement;
    p.innerHTML = 'x<style>' + cssText + '</style>';
    return parent.insertBefore(p.lastChild, parent.firstChild);
  }
  /**
   * Returns the value of `html5.elements` as an array.
   * @private
   * @returns {Array} An array of shived element node names.
   */


  function getElements() {
    var elements = html5.elements;
    return typeof elements == 'string' ? elements.split(' ') : elements;
  }
  /**
   * Extends the built-in list of html5 elements
   * @memberOf html5
   * @param {String|Array} newElements whitespace separated list or array of new element names to shiv
   * @param {Document} ownerDocument The context document.
   */


  function addElements(newElements, ownerDocument) {
    var elements = html5.elements;

    if (typeof elements != 'string') {
      elements = elements.join(' ');
    }

    if (typeof newElements != 'string') {
      newElements = newElements.join(' ');
    }

    html5.elements = elements + ' ' + newElements;
    shivDocument(ownerDocument);
  }
  /**
  * Returns the data associated to the given document
  * @private
  * @param {Document} ownerDocument The document.
  * @returns {Object} An object of data.
  */


  function getExpandoData(ownerDocument) {
    var data = expandoData[ownerDocument[expando]];

    if (!data) {
      data = {};
      expanID++;
      ownerDocument[expando] = expanID;
      expandoData[expanID] = data;
    }

    return data;
  }
  /**
   * returns a shived element for the given nodeName and document
   * @memberOf html5
   * @param {String} nodeName name of the element
   * @param {Document|DocumentFragment} ownerDocument The context document.
   * @returns {Object} The shived element.
   */


  function createElement(nodeName, ownerDocument, data) {
    if (!ownerDocument) {
      ownerDocument = document;
    }

    if (supportsUnknownElements) {
      return ownerDocument.createElement(nodeName);
    }

    if (!data) {
      data = getExpandoData(ownerDocument);
    }

    var node;

    if (data.cache[nodeName]) {
      node = data.cache[nodeName].cloneNode();
    } else if (saveClones.test(nodeName)) {
      node = (data.cache[nodeName] = data.createElem(nodeName)).cloneNode();
    } else {
      node = data.createElem(nodeName);
    } // Avoid adding some elements to fragments in IE < 9 because
    // * Attributes like `name` or `type` cannot be set/changed once an element
    //   is inserted into a document/fragment
    // * Link elements with `src` attributes that are inaccessible, as with
    //   a 403 response, will cause the tab/window to crash
    // * Script elements appended to fragments will execute when their `src`
    //   or `text` property is set


    return node.canHaveChildren && !reSkip.test(nodeName) && !node.tagUrn ? data.frag.appendChild(node) : node;
  }
  /**
   * returns a shived DocumentFragment for the given document
   * @memberOf html5
   * @param {Document} ownerDocument The context document.
   * @returns {Object} The shived DocumentFragment.
   */


  function createDocumentFragment(ownerDocument, data) {
    if (!ownerDocument) {
      ownerDocument = document;
    }

    if (supportsUnknownElements) {
      return ownerDocument.createDocumentFragment();
    }

    data = data || getExpandoData(ownerDocument);
    var clone = data.frag.cloneNode(),
        i = 0,
        elems = getElements(),
        l = elems.length;

    for (; i < l; i++) {
      clone.createElement(elems[i]);
    }

    return clone;
  }
  /**
   * Shivs the `createElement` and `createDocumentFragment` methods of the document.
   * @private
   * @param {Document|DocumentFragment} ownerDocument The document.
   * @param {Object} data of the document.
   */


  function shivMethods(ownerDocument, data) {
    if (!data.cache) {
      data.cache = {};
      data.createElem = ownerDocument.createElement;
      data.createFrag = ownerDocument.createDocumentFragment;
      data.frag = data.createFrag();
    }

    ownerDocument.createElement = function (nodeName) {
      //abort shiv
      if (!html5.shivMethods) {
        return data.createElem(nodeName);
      }

      return createElement(nodeName, ownerDocument, data);
    };

    ownerDocument.createDocumentFragment = Function('h,f', 'return function(){' + 'var n=f.cloneNode(),c=n.createElement;' + 'h.shivMethods&&(' + // unroll the `createElement` calls
    getElements().join().replace(/[\w\-:]+/g, function (nodeName) {
      data.createElem(nodeName);
      data.frag.createElement(nodeName);
      return 'c("' + nodeName + '")';
    }) + ');return n}')(html5, data.frag);
  }
  /*--------------------------------------------------------------------------*/

  /**
   * Shivs the given document.
   * @memberOf html5
   * @param {Document} ownerDocument The document to shiv.
   * @returns {Document} The shived document.
   */


  function shivDocument(ownerDocument) {
    if (!ownerDocument) {
      ownerDocument = document;
    }

    var data = getExpandoData(ownerDocument);

    if (html5.shivCSS && !supportsHtml5Styles && !data.hasCSS) {
      data.hasCSS = !!addStyleSheet(ownerDocument, // corrects block display not defined in IE6/7/8/9
      'article,aside,dialog,figcaption,figure,footer,header,hgroup,main,nav,section{display:block}' + // adds styling not present in IE6/7/8/9
      'mark{background:#FF0;color:#000}' + // hides non-rendered elements
      'template{display:none}');
    }

    if (!supportsUnknownElements) {
      shivMethods(ownerDocument, data);
    }

    return ownerDocument;
  }
  /*--------------------------------------------------------------------------*/

  /**
   * The `html5` object is exposed so that more elements can be shived and
   * existing shiving can be detected on iframes.
   * @type Object
   * @example
   *
   * // options can be changed before the script is included
   * html5 = { 'elements': 'mark section', 'shivCSS': false, 'shivMethods': false };
   */


  var html5 = {
    /**
     * An array or space separated string of node names of the elements to shiv.
     * @memberOf html5
     * @type Array|String
     */
    'elements': options.elements || 'abbr article aside audio bdi canvas data datalist details dialog figcaption figure footer header hgroup main mark meter nav output picture progress section summary template time video',

    /**
     * current version of html5shiv
     */
    'version': version,

    /**
     * A flag to indicate that the HTML5 style sheet should be inserted.
     * @memberOf html5
     * @type Boolean
     */
    'shivCSS': options.shivCSS !== false,

    /**
     * Is equal to true if a browser supports creating unknown/HTML5 elements
     * @memberOf html5
     * @type boolean
     */
    'supportsUnknownElements': supportsUnknownElements,

    /**
     * A flag to indicate that the document's `createElement` and `createDocumentFragment`
     * methods should be overwritten.
     * @memberOf html5
     * @type Boolean
     */
    'shivMethods': options.shivMethods !== false,

    /**
     * A string to describe the type of `html5` object ("default" or "default print").
     * @memberOf html5
     * @type String
     */
    'type': 'default',
    // shivs the document according to the specified `html5` object options
    'shivDocument': shivDocument,
    //creates a shived element
    createElement: createElement,
    //creates a shived documentFragment
    createDocumentFragment: createDocumentFragment,
    //extends list of elements
    addElements: addElements
  };
  /*--------------------------------------------------------------------------*/
  // expose html5

  window.html5 = html5; // shiv the document

  shivDocument(document);

  if ( true && module.exports) {
    module.exports = html5;
  }
})(window, document);

/***/ }),

/***/ 72090:
/***/ (function() {

"use strict";


/**
 * @file
 * @copyright 2013 Andrea Giammarchi, WebReflection
 * @license MIT
 */

/* eslint-disable */
(function (window) {
  /*! (C) WebReflection Mit Style License */
  if (document.createEvent) return;

  var DUNNOABOUTDOMLOADED = true,
      READYEVENTDISPATCHED = false,
      ONREADYSTATECHANGE = 'onreadystatechange',
      DOMCONTENTLOADED = 'DOMContentLoaded',
      SECRET = '__IE8__' + Math.random(),
      // Object = window.Object,
  defineProperty = Object.defineProperty || // just in case ...
  function (object, property, descriptor) {
    object[property] = descriptor.value;
  },
      defineProperties = Object.defineProperties || // IE8 implemented defineProperty but not the plural...
  function (object, descriptors) {
    for (var key in descriptors) {
      if (hasOwnProperty.call(descriptors, key)) {
        try {
          defineProperty(object, key, descriptors[key]);
        } catch (o_O) {
          if (window.console) {}
        }
      }
    }
  },
      getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor,
      hasOwnProperty = Object.prototype.hasOwnProperty,
      // here IE7 will break like a charm
  ElementPrototype = window.Element.prototype,
      TextPrototype = window.Text.prototype,
      // none of above native constructors exist/are exposed
  possiblyNativeEvent = /^[a-z]+$/,
      // ^ actually could probably be just /^[a-z]+$/
  readyStateOK = /loaded|complete/,
      types = {},
      div = document.createElement('div'),
      html = document.documentElement,
      removeAttribute = html.removeAttribute,
      setAttribute = html.setAttribute,
      valueDesc = function valueDesc(value) {
    return {
      enumerable: true,
      writable: true,
      configurable: true,
      value: value
    };
  };

  function commonEventLoop(currentTarget, e, $handlers, synthetic) {
    for (var handler, continuePropagation, handlers = $handlers.slice(), evt = enrich(e, currentTarget), i = 0, length = handlers.length; i < length; i++) {
      handler = handlers[i];

      if (typeof handler === 'object') {
        if (typeof handler.handleEvent === 'function') {
          handler.handleEvent(evt);
        }
      } else {
        handler.call(currentTarget, evt);
      }

      if (evt.stoppedImmediatePropagation) break;
    }

    continuePropagation = !evt.stoppedPropagation;
    /*
    if (continuePropagation && !synthetic && !live(currentTarget)) {
      evt.cancelBubble = true;
    }
    */

    return synthetic && continuePropagation && currentTarget.parentNode ? currentTarget.parentNode.dispatchEvent(evt) : !evt.defaultPrevented;
  }

  function commonDescriptor(get, set) {
    return {
      // if you try with enumerable: true
      // IE8 will miserably fail
      configurable: true,
      get: get,
      set: set
    };
  }

  function commonTextContent(protoDest, protoSource, property) {
    var descriptor = getOwnPropertyDescriptor(protoSource || protoDest, property);
    defineProperty(protoDest, 'textContent', commonDescriptor(function () {
      return descriptor.get.call(this);
    }, function (textContent) {
      descriptor.set.call(this, textContent);
    }));
  }

  function enrich(e, currentTarget) {
    e.currentTarget = currentTarget;
    e.eventPhase = // AT_TARGET : BUBBLING_PHASE
    e.target === e.currentTarget ? 2 : 3;
    return e;
  }

  function find(array, value) {
    var i = array.length;

    while (i-- && array[i] !== value) {
      ;
    }

    return i;
  }

  function getTextContent() {
    if (this.tagName === 'BR') return '\n';
    var textNode = this.firstChild,
        arrayContent = [];

    while (textNode) {
      if (textNode.nodeType !== 8 && textNode.nodeType !== 7) {
        arrayContent.push(textNode.textContent);
      }

      textNode = textNode.nextSibling;
    }

    return arrayContent.join('');
  }

  function live(self) {
    return self.nodeType !== 9 && html.contains(self);
  }

  function onkeyup(e) {
    var evt = document.createEvent('Event');
    evt.initEvent('input', true, true);
    (e.srcElement || e.fromElement || document).dispatchEvent(evt);
  }

  function onReadyState(e) {
    if (!READYEVENTDISPATCHED && readyStateOK.test(document.readyState)) {
      READYEVENTDISPATCHED = !READYEVENTDISPATCHED;
      document.detachEvent(ONREADYSTATECHANGE, onReadyState);
      e = document.createEvent('Event');
      e.initEvent(DOMCONTENTLOADED, true, true);
      document.dispatchEvent(e);
    }
  }

  function getter(attr) {
    return function () {
      return html[attr] || document.body && document.body[attr] || 0;
    };
  }

  function setTextContent(textContent) {
    var node;

    while (node = this.lastChild) {
      this.removeChild(node);
    }
    /*jshint eqnull:true */


    if (textContent != null) {
      this.appendChild(document.createTextNode(textContent));
    }
  }

  function verify(self, e) {
    if (!e) {
      e = window.event;
    }

    if (!e.target) {
      e.target = e.srcElement || e.fromElement || document;
    }

    if (!e.timeStamp) {
      e.timeStamp = new Date().getTime();
    }

    return e;
  } // normalized textContent for:
  //  comment, script, style, text, title


  commonTextContent(window.HTMLCommentElement.prototype, ElementPrototype, 'nodeValue');
  commonTextContent(window.HTMLScriptElement.prototype, null, 'text');
  commonTextContent(TextPrototype, null, 'nodeValue');
  commonTextContent(window.HTMLTitleElement.prototype, null, 'text');
  defineProperty(window.HTMLStyleElement.prototype, 'textContent', function (descriptor) {
    return commonDescriptor(function () {
      return descriptor.get.call(this.styleSheet);
    }, function (textContent) {
      descriptor.set.call(this.styleSheet, textContent);
    });
  }(getOwnPropertyDescriptor(window.CSSStyleSheet.prototype, 'cssText')));
  var opacityre = /\b\s*alpha\s*\(\s*opacity\s*=\s*(\d+)\s*\)/;
  defineProperty(window.CSSStyleDeclaration.prototype, 'opacity', {
    get: function () {
      function get() {
        var m = this.filter.match(opacityre);
        return m ? (m[1] / 100).toString() : '';
      }

      return get;
    }(),
    set: function () {
      function set(value) {
        this.zoom = 1;
        var found = false;

        if (value < 1) {
          value = ' alpha(opacity=' + Math.round(value * 100) + ')';
        } else {
          value = '';
        }

        this.filter = this.filter.replace(opacityre, function () {
          found = true;
          return value;
        });

        if (!found && value) {
          this.filter += value;
        }
      }

      return set;
    }()
  });
  defineProperties(ElementPrototype, {
    // bonus
    textContent: {
      get: getTextContent,
      set: setTextContent
    },
    // http://www.w3.org/TR/ElementTraversal/#interface-elementTraversal
    firstElementChild: {
      get: function () {
        function get() {
          for (var childNodes = this.childNodes || [], i = 0, length = childNodes.length; i < length; i++) {
            if (childNodes[i].nodeType == 1) return childNodes[i];
          }
        }

        return get;
      }()
    },
    lastElementChild: {
      get: function () {
        function get() {
          for (var childNodes = this.childNodes || [], i = childNodes.length; i--;) {
            if (childNodes[i].nodeType == 1) return childNodes[i];
          }
        }

        return get;
      }()
    },
    oninput: {
      get: function () {
        function get() {
          return this._oninput || null;
        }

        return get;
      }(),
      set: function () {
        function set(oninput) {
          if (this._oninput) {
            this.removeEventListener('input', this._oninput);
            this._oninput = oninput;

            if (oninput) {
              this.addEventListener('input', oninput);
            }
          }
        }

        return set;
      }()
    },
    previousElementSibling: {
      get: function () {
        function get() {
          var previousElementSibling = this.previousSibling;

          while (previousElementSibling && previousElementSibling.nodeType != 1) {
            previousElementSibling = previousElementSibling.previousSibling;
          }

          return previousElementSibling;
        }

        return get;
      }()
    },
    nextElementSibling: {
      get: function () {
        function get() {
          var nextElementSibling = this.nextSibling;

          while (nextElementSibling && nextElementSibling.nodeType != 1) {
            nextElementSibling = nextElementSibling.nextSibling;
          }

          return nextElementSibling;
        }

        return get;
      }()
    },
    childElementCount: {
      get: function () {
        function get() {
          for (var count = 0, childNodes = this.childNodes || [], i = childNodes.length; i--; count += childNodes[i].nodeType == 1) {
            ;
          }

          return count;
        }

        return get;
      }()
    },

    /*
    // children would be an override
    // IE8 already supports them but with comments too
    // not just nodeType 1
    children: {
      get: function () {
        for(var
          children = [],
          childNodes = this.childNodes || [],
          i = 0, length = childNodes.length;
          i < length; i++
        ) {
          if (childNodes[i].nodeType == 1) {
            children.push(childNodes[i]);
          }
        }
        return children;
      }
    },
    */
    // DOM Level 2 EventTarget methods and events
    addEventListener: valueDesc(function (type, handler, capture) {
      if (typeof handler !== 'function' && typeof handler !== 'object') return;
      var self = this,
          ontype = 'on' + type,
          temple = self[SECRET] || defineProperty(self, SECRET, {
        value: {}
      })[SECRET],
          currentType = temple[ontype] || (temple[ontype] = {}),
          handlers = currentType.h || (currentType.h = []),
          e,
          attr;

      if (!hasOwnProperty.call(currentType, 'w')) {
        currentType.w = function (e) {
          // e[SECRET] is a silent notification needed to avoid
          // fired events during live test
          return e[SECRET] || commonEventLoop(self, verify(self, e), handlers, false);
        }; // if not detected yet


        if (!hasOwnProperty.call(types, ontype)) {
          // and potentially a native event
          if (possiblyNativeEvent.test(type)) {
            // do this heavy thing
            try {
              // TODO:  should I consider tagName too so that
              //        INPUT[ontype] could be different ?
              e = document.createEventObject(); // do not clone ever a node
              // specially a document one ...
              // use the secret to ignore them all

              e[SECRET] = true; // document a part if a node has never been
              // added to any other node, fireEvent might
              // behave very weirdly (read: trigger unspecified errors)

              if (self.nodeType != 9) {
                /*jshint eqnull:true */
                if (self.parentNode == null) {
                  div.appendChild(self);
                }

                if (attr = self.getAttribute(ontype)) {
                  removeAttribute.call(self, ontype);
                }
              }

              self.fireEvent(ontype, e);
              types[ontype] = true;
            } catch (meh) {
              types[ontype] = false;

              while (div.hasChildNodes()) {
                div.removeChild(div.firstChild);
              }
            }

            if (attr != null) {
              setAttribute.call(self, ontype, attr);
            }
          } else {
            // no need to bother since
            // 'x-event' ain't native for sure
            types[ontype] = false;
          }
        }

        if (currentType.n = types[ontype]) {
          self.attachEvent(ontype, currentType.w);
        }
      }

      if (find(handlers, handler) < 0) {
        handlers[capture ? 'unshift' : 'push'](handler);
      }

      if (type === 'input') {
        self.attachEvent('onkeyup', onkeyup);
      }
    }),
    dispatchEvent: valueDesc(function (e) {
      var self = this,
          ontype = 'on' + e.type,
          temple = self[SECRET],
          currentType = temple && temple[ontype],
          valid = !!currentType,
          parentNode;
      if (!e.target) e.target = self;
      return valid ? currentType.n
      /* && live(self) */
      ? self.fireEvent(ontype, e) : commonEventLoop(self, e, currentType.h, true) : (parentNode = self.parentNode) ? parentNode.dispatchEvent(e) : true, !e.defaultPrevented;
    }),
    removeEventListener: valueDesc(function (type, handler, capture) {
      if (typeof handler !== 'function' && typeof handler !== 'object') return;
      var self = this,
          ontype = 'on' + type,
          temple = self[SECRET],
          currentType = temple && temple[ontype],
          handlers = currentType && currentType.h,
          i = handlers ? find(handlers, handler) : -1;
      if (-1 < i) handlers.splice(i, 1);
    })
  });
  /* this is not needed in IE8
  defineProperties(window.HTMLSelectElement.prototype, {
    value: {
      get: function () {
        return this.options[this.selectedIndex].value;
      }
    }
  });
  //*/
  // EventTarget methods for Text nodes too

  defineProperties(TextPrototype, {
    addEventListener: valueDesc(ElementPrototype.addEventListener),
    dispatchEvent: valueDesc(ElementPrototype.dispatchEvent),
    removeEventListener: valueDesc(ElementPrototype.removeEventListener)
  });
  defineProperties(window.XMLHttpRequest.prototype, {
    addEventListener: valueDesc(function (type, handler, capture) {
      var self = this,
          ontype = 'on' + type,
          temple = self[SECRET] || defineProperty(self, SECRET, {
        value: {}
      })[SECRET],
          currentType = temple[ontype] || (temple[ontype] = {}),
          handlers = currentType.h || (currentType.h = []);

      if (find(handlers, handler) < 0) {
        if (!self[ontype]) {
          self[ontype] = function () {
            var e = document.createEvent('Event');
            e.initEvent(type, true, true);
            self.dispatchEvent(e);
          };
        }

        handlers[capture ? 'unshift' : 'push'](handler);
      }
    }),
    dispatchEvent: valueDesc(function (e) {
      var self = this,
          ontype = 'on' + e.type,
          temple = self[SECRET],
          currentType = temple && temple[ontype],
          valid = !!currentType;
      return valid && (currentType.n
      /* && live(self) */
      ? self.fireEvent(ontype, e) : commonEventLoop(self, e, currentType.h, true));
    }),
    removeEventListener: valueDesc(ElementPrototype.removeEventListener)
  });
  var buttonGetter = getOwnPropertyDescriptor(Event.prototype, 'button').get;
  defineProperties(window.Event.prototype, {
    bubbles: valueDesc(true),
    cancelable: valueDesc(true),
    preventDefault: valueDesc(function () {
      if (this.cancelable) {
        this.returnValue = false;
      }
    }),
    stopPropagation: valueDesc(function () {
      this.stoppedPropagation = true;
      this.cancelBubble = true;
    }),
    stopImmediatePropagation: valueDesc(function () {
      this.stoppedImmediatePropagation = true;
      this.stopPropagation();
    }),
    initEvent: valueDesc(function (type, bubbles, cancelable) {
      this.type = type;
      this.bubbles = !!bubbles;
      this.cancelable = !!cancelable;

      if (!this.bubbles) {
        this.stopPropagation();
      }
    }),
    pageX: {
      get: function () {
        function get() {
          return this._pageX || (this._pageX = this.clientX + window.scrollX - (html.clientLeft || 0));
        }

        return get;
      }()
    },
    pageY: {
      get: function () {
        function get() {
          return this._pageY || (this._pageY = this.clientY + window.scrollY - (html.clientTop || 0));
        }

        return get;
      }()
    },
    which: {
      get: function () {
        function get() {
          return this.keyCode ? this.keyCode : isNaN(this.button) ? undefined : this.button + 1;
        }

        return get;
      }()
    },
    charCode: {
      get: function () {
        function get() {
          return this.keyCode && this.type == 'keypress' ? this.keyCode : 0;
        }

        return get;
      }()
    },
    buttons: {
      get: function () {
        function get() {
          return buttonGetter.call(this);
        }

        return get;
      }()
    },
    button: {
      get: function () {
        function get() {
          var buttons = this.buttons;
          return buttons & 1 ? 0 : buttons & 2 ? 2 : buttons & 4 ? 1 : undefined;
        }

        return get;
      }()
    },
    defaultPrevented: {
      get: function () {
        function get() {
          // if preventDefault() was never called, or returnValue not given a value
          // then returnValue is undefined
          var returnValue = this.returnValue,
              undef;
          return !(returnValue === undef || returnValue);
        }

        return get;
      }()
    },
    relatedTarget: {
      get: function () {
        function get() {
          var type = this.type;

          if (type === 'mouseover') {
            return this.fromElement;
          } else if (type === 'mouseout') {
            return this.toElement;
          } else {
            return null;
          }
        }

        return get;
      }()
    }
  });
  defineProperties(window.HTMLDocument.prototype, {
    defaultView: {
      get: function () {
        function get() {
          return this.parentWindow;
        }

        return get;
      }()
    },
    textContent: {
      get: function () {
        function get() {
          return this.nodeType === 11 ? getTextContent.call(this) : null;
        }

        return get;
      }(),
      set: function () {
        function set(textContent) {
          if (this.nodeType === 11) {
            setTextContent.call(this, textContent);
          }
        }

        return set;
      }()
    },
    addEventListener: valueDesc(function (type, handler, capture) {
      var self = this;
      ElementPrototype.addEventListener.call(self, type, handler, capture); // NOTE:  it won't fire if already loaded, this is NOT a $.ready() shim!
      //        this behaves just like standard browsers

      if (DUNNOABOUTDOMLOADED && type === DOMCONTENTLOADED && !readyStateOK.test(self.readyState)) {
        DUNNOABOUTDOMLOADED = false;
        self.attachEvent(ONREADYSTATECHANGE, onReadyState);
        /* global top */

        if (window == top) {
          (function () {
            function gonna(e) {
              try {
                self.documentElement.doScroll('left');
                onReadyState();
              } catch (o_O) {
                setTimeout(gonna, 50);
              }
            }

            return gonna;
          })()();
        }
      }
    }),
    dispatchEvent: valueDesc(ElementPrototype.dispatchEvent),
    removeEventListener: valueDesc(ElementPrototype.removeEventListener),
    createEvent: valueDesc(function (Class) {
      var e;
      if (Class !== 'Event') throw new Error('unsupported ' + Class);
      e = document.createEventObject();
      e.timeStamp = new Date().getTime();
      return e;
    })
  });
  defineProperties(window.Window.prototype, {
    getComputedStyle: valueDesc(function () {
      var // partially grabbed from jQuery and Dean's hack
      notpixel = /^(?:[+-]?(?:\d*\.|)\d+(?:[eE][+-]?\d+|))(?!px)[a-z%]+$/,
          position = /^(top|right|bottom|left)$/,
          re = /\-([a-z])/g,
          place = function place(match, $1) {
        return $1.toUpperCase();
      };

      function ComputedStyle(_) {
        this._ = _;
      }

      ComputedStyle.prototype.getPropertyValue = function (name) {
        var el = this._,
            style = el.style,
            currentStyle = el.currentStyle,
            runtimeStyle = el.runtimeStyle,
            result,
            left,
            rtLeft;

        if (name == 'opacity') {
          return style.opacity || '1';
        }

        name = (name === 'float' ? 'style-float' : name).replace(re, place);
        result = currentStyle ? currentStyle[name] : style[name];

        if (notpixel.test(result) && !position.test(name)) {
          left = style.left;
          rtLeft = runtimeStyle && runtimeStyle.left;

          if (rtLeft) {
            runtimeStyle.left = currentStyle.left;
          }

          style.left = name === 'fontSize' ? '1em' : result;
          result = style.pixelLeft + 'px';
          style.left = left;

          if (rtLeft) {
            runtimeStyle.left = rtLeft;
          }
        }
        /*jshint eqnull:true */


        return result == null ? result : result + '' || 'auto';
      }; // unsupported


      function PseudoComputedStyle() {}

      PseudoComputedStyle.prototype.getPropertyValue = function () {
        return null;
      };

      return function (el, pseudo) {
        return pseudo ? new PseudoComputedStyle(el) : new ComputedStyle(el);
      };
    }()),
    addEventListener: valueDesc(function (type, handler, capture) {
      var self = window,
          ontype = 'on' + type,
          handlers;

      if (!self[ontype]) {
        self[ontype] = function (e) {
          return commonEventLoop(self, verify(self, e), handlers, false) && undefined;
        };
      }

      handlers = self[ontype][SECRET] || (self[ontype][SECRET] = []);

      if (find(handlers, handler) < 0) {
        handlers[capture ? 'unshift' : 'push'](handler);
      }
    }),
    dispatchEvent: valueDesc(function (e) {
      var method = window['on' + e.type];
      return method ? method.call(window, e) !== false && !e.defaultPrevented : true;
    }),
    removeEventListener: valueDesc(function (type, handler, capture) {
      var ontype = 'on' + type,
          handlers = (window[ontype] || Object)[SECRET],
          i = handlers ? find(handlers, handler) : -1;
      if (-1 < i) handlers.splice(i, 1);
    }),
    pageXOffset: {
      get: getter('scrollLeft')
    },
    pageYOffset: {
      get: getter('scrollTop')
    },
    scrollX: {
      get: getter('scrollLeft')
    },
    scrollY: {
      get: getter('scrollTop')
    },
    innerWidth: {
      get: getter('clientWidth')
    },
    innerHeight: {
      get: getter('clientHeight')
    }
  });
  window.HTMLElement = window.Element;

  (function (styleSheets, HTML5Element, i) {
    for (i = 0; i < HTML5Element.length; i++) {
      document.createElement(HTML5Element[i]);
    }

    if (!styleSheets.length) document.createStyleSheet('');
    styleSheets[0].addRule(HTML5Element.join(','), 'display:block;');
  })(document.styleSheets, ['header', 'nav', 'section', 'article', 'aside', 'footer']);

  (function () {
    if (document.createRange) return;

    document.createRange = function () {
      function createRange() {
        return new Range();
      }

      return createRange;
    }();

    function getContents(start, end) {
      var nodes = [start];

      while (start !== end) {
        nodes.push(start = start.nextSibling);
      }

      return nodes;
    }

    function Range() {}

    var proto = Range.prototype;

    proto.cloneContents = function () {
      function cloneContents() {
        for (var fragment = this._start.ownerDocument.createDocumentFragment(), nodes = getContents(this._start, this._end), i = 0, length = nodes.length; i < length; i++) {
          fragment.appendChild(nodes[i].cloneNode(true));
        }

        return fragment;
      }

      return cloneContents;
    }();

    proto.cloneRange = function () {
      function cloneRange() {
        var range = new Range();
        range._start = this._start;
        range._end = this._end;
        return range;
      }

      return cloneRange;
    }();

    proto.deleteContents = function () {
      function deleteContents() {
        for (var parentNode = this._start.parentNode, nodes = getContents(this._start, this._end), i = 0, length = nodes.length; i < length; i++) {
          parentNode.removeChild(nodes[i]);
        }
      }

      return deleteContents;
    }();

    proto.extractContents = function () {
      function extractContents() {
        for (var fragment = this._start.ownerDocument.createDocumentFragment(), nodes = getContents(this._start, this._end), i = 0, length = nodes.length; i < length; i++) {
          fragment.appendChild(nodes[i]);
        }

        return fragment;
      }

      return extractContents;
    }();

    proto.setEndAfter = function () {
      function setEndAfter(node) {
        this._end = node;
      }

      return setEndAfter;
    }();

    proto.setEndBefore = function () {
      function setEndBefore(node) {
        this._end = node.previousSibling;
      }

      return setEndBefore;
    }();

    proto.setStartAfter = function () {
      function setStartAfter(node) {
        this._start = node.nextSibling;
      }

      return setStartAfter;
    }();

    proto.setStartBefore = function () {
      function setStartBefore(node) {
        this._start = node;
      }

      return setStartBefore;
    }();
  })();
})(window);

/***/ }),

/***/ 37283:
/***/ (function() {

"use strict";


/**
 * @file
 * @copyright 2020 Aleksej Komarov
 * @license MIT
 */
// Inferno needs Int32Array, and it is not covered by core-js.
if (!window.Int32Array) {
  window.Int32Array = Array;
}

/***/ }),

/***/ 39646:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.assetMiddleware = exports.resolveAsset = void 0;

/**
 * @file
 * @copyright 2020 Aleksej Komarov
 * @license MIT
 */
var EXCLUDED_PATTERNS = [/v4shim/i];
var loadedMappings = {};

var resolveAsset = function resolveAsset(name) {
  return loadedMappings[name] || name;
};

exports.resolveAsset = resolveAsset;

var assetMiddleware = function assetMiddleware(store) {
  return function (next) {
    return function (action) {
      var type = action.type,
          payload = action.payload;

      if (type === 'asset/stylesheet') {
        Byond.loadCss(payload);
        return;
      }

      if (type === 'asset/mappings') {
        var _loop = function _loop() {
          var name = _Object$keys[_i];

          // Skip anything that matches excluded patterns
          if (EXCLUDED_PATTERNS.some(function (regex) {
            return regex.test(name);
          })) {
            return "continue";
          }

          var url = payload[name];
          var ext = name.split('.').pop();
          loadedMappings[name] = url;

          if (ext === 'css') {
            Byond.loadCss(url);
          }

          if (ext === 'js') {
            Byond.loadJs(url);
          }
        };

        for (var _i = 0, _Object$keys = Object.keys(payload); _i < _Object$keys.length; _i++) {
          var _ret = _loop();

          if (_ret === "continue") continue;
        }

        return;
      }

      next(action);
    };
  };
};

exports.assetMiddleware = assetMiddleware;

/***/ }),

/***/ 31246:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.useSharedState = exports.useLocalState = exports.useBackend = exports.selectBackend = exports.sendAct = exports.sendMessage = exports.backendMiddleware = exports.backendReducer = exports.backendSuspendSuccess = exports.backendSuspendStart = exports.backendSetSharedState = exports.backendUpdate = void 0;

var _perf = __webpack_require__(28440);

var _redux = __webpack_require__(465);

var _drag = __webpack_require__(41410);

var _focus = __webpack_require__(40509);

var _logging = __webpack_require__(79926);

var _renderer = __webpack_require__(77960);

var _excluded = ["payload"];

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

var logger = (0, _logging.createLogger)('backend');
var backendUpdate = (0, _redux.createAction)('backend/update');
exports.backendUpdate = backendUpdate;
var backendSetSharedState = (0, _redux.createAction)('backend/setSharedState');
exports.backendSetSharedState = backendSetSharedState;
var backendSuspendStart = (0, _redux.createAction)('backend/suspendStart');
exports.backendSuspendStart = backendSuspendStart;

var backendSuspendSuccess = function backendSuspendSuccess() {
  return {
    type: 'backend/suspendSuccess',
    payload: {
      timestamp: Date.now()
    }
  };
};

exports.backendSuspendSuccess = backendSuspendSuccess;
var initialState = {
  config: {},
  data: {},
  shared: {},
  // Start as suspended
  suspended: Date.now(),
  suspending: false
};

var backendReducer = function backendReducer(state, action) {
  if (state === void 0) {
    state = initialState;
  }

  var type = action.type,
      payload = action.payload;

  if (type === 'backend/update') {
    // Merge config
    var config = Object.assign({}, state.config, payload.config); // Merge data

    var data = Object.assign({}, state.data, payload.static_data, payload.data); // Merge shared states

    var shared = Object.assign({}, state.shared);

    if (payload.shared) {
      for (var _i = 0, _Object$keys = Object.keys(payload.shared); _i < _Object$keys.length; _i++) {
        var key = _Object$keys[_i];
        var value = payload.shared[key];

        if (value === '') {
          shared[key] = undefined;
        } else {
          shared[key] = JSON.parse(value);
        }
      }
    } // Return new state


    return Object.assign({}, state, {
      config: config,
      data: data,
      shared: shared,
      suspended: false
    });
  }

  if (type === 'backend/setSharedState') {
    var _Object$assign;

    var _key = payload.key,
        _nextState = payload.nextState;
    return Object.assign({}, state, {
      shared: Object.assign({}, state.shared, (_Object$assign = {}, _Object$assign[_key] = _nextState, _Object$assign))
    });
  }

  if (type === 'backend/suspendStart') {
    return Object.assign({}, state, {
      suspending: true
    });
  }

  if (type === 'backend/suspendSuccess') {
    var timestamp = payload.timestamp;
    return Object.assign({}, state, {
      data: {},
      shared: {},
      config: Object.assign({}, state.config, {
        title: '',
        status: 1
      }),
      suspending: false,
      suspended: timestamp
    });
  }

  return state;
};

exports.backendReducer = backendReducer;

var backendMiddleware = function backendMiddleware(store) {
  var fancyState;
  var suspendInterval;
  return function (next) {
    return function (action) {
      var _selectBackend = selectBackend(store.getState()),
          suspended = _selectBackend.suspended;

      var type = action.type,
          payload = action.payload;

      if (type === 'update') {
        store.dispatch(backendUpdate(payload));
        return;
      }

      if (type === 'suspend') {
        store.dispatch(backendSuspendSuccess());
        return;
      }

      if (type === 'ping') {
        sendMessage({
          type: 'pingReply'
        });
        return;
      }

      if (type === 'backend/suspendStart' && !suspendInterval) {
        logger.log("suspending (" + window.__windowId__ + ")"); // Keep sending suspend messages until it succeeds.
        // It may fail multiple times due to topic rate limiting.

        var suspendFn = function suspendFn() {
          return sendMessage({
            type: 'suspend'
          });
        };

        suspendFn();
        suspendInterval = setInterval(suspendFn, 2000);
      }

      if (type === 'backend/suspendSuccess') {
        (0, _renderer.suspendRenderer)();
        clearInterval(suspendInterval);
        suspendInterval = undefined;
        Byond.winset(window.__windowId__, {
          'is-visible': false
        });
        setImmediate(function () {
          return (0, _focus.focusMap)();
        });
      }

      if (type === 'backend/update') {
        var _payload$config, _payload$config$windo;

        var fancy = (_payload$config = payload.config) == null ? void 0 : (_payload$config$windo = _payload$config.window) == null ? void 0 : _payload$config$windo.fancy; // Initialize fancy state

        if (fancyState === undefined) {
          fancyState = fancy;
        } // React to changes in fancy
        else if (fancyState !== fancy) {
          logger.log('changing fancy mode to', fancy);
          fancyState = fancy;
          Byond.winset(window.__windowId__, {
            titlebar: !fancy,
            'can-resize': !fancy
          });
        }
      } // Resume on incoming update


      if (type === 'backend/update' && suspended) {
        // Show the payload
        logger.log('backend/update', payload); // Signal renderer that we have resumed

        (0, _renderer.resumeRenderer)(); // Setup drag

        (0, _drag.setupDrag)(); // We schedule this for the next tick here because resizing and unhiding
        // during the same tick will flash with a white background.

        setImmediate(function () {
          _perf.perf.mark('resume/start'); // Doublecheck if we are not re-suspended.


          var _selectBackend2 = selectBackend(store.getState()),
              suspended = _selectBackend2.suspended;

          if (suspended) {
            return;
          }

          Byond.winset(window.__windowId__, {
            'is-visible': true
          });

          _perf.perf.mark('resume/finish');

          if (false) {}
        });
      }

      return next(action);
    };
  };
};
/**
 * Sends a message to /datum/tgui_window.
 */


exports.backendMiddleware = backendMiddleware;

var sendMessage = function sendMessage(message) {
  if (message === void 0) {
    message = {};
  }

  var _message = message,
      payload = _message.payload,
      rest = _objectWithoutPropertiesLoose(_message, _excluded);

  var data = Object.assign({
    // Message identifying header
    tgui: 1,
    window_id: window.__windowId__
  }, rest); // JSON-encode the payload

  if (payload !== null && payload !== undefined) {
    data.payload = JSON.stringify(payload);
  }

  Byond.topic(data);
};
/**
 * Sends an action to `ui_act` on `src_object` that this tgui window
 * is associated with.
 */


exports.sendMessage = sendMessage;

var sendAct = function sendAct(action, payload) {
  if (payload === void 0) {
    payload = {};
  }

  // Validate that payload is an object
  var isObject = typeof payload === 'object' && payload !== null && !Array.isArray(payload);

  if (!isObject) {
    logger.error("Payload for act() must be an object, got this:", payload);
    return;
  }

  sendMessage({
    type: 'act/' + action,
    payload: payload
  });
};

exports.sendAct = sendAct;

/**
 * Selects a backend-related slice of Redux state
 */
var selectBackend = function selectBackend(state) {
  return state.backend || {};
};
/**
 * A React hook (sort of) for getting tgui state and related functions.
 *
 * This is supposed to be replaced with a real React Hook, which can only
 * be used in functional components.
 *
 * You can make
 */


exports.selectBackend = selectBackend;

var useBackend = function useBackend(context) {
  var store = context.store;
  var state = selectBackend(store.getState());
  return Object.assign({}, state, {
    act: sendAct
  });
};
/**
 * A tuple that contains the state and a setter function for it.
 */


exports.useBackend = useBackend;

/**
 * Allocates state on Redux store without sharing it with other clients.
 *
 * Use it when you want to have a stateful variable in your component
 * that persists between renders, but will be forgotten after you close
 * the UI.
 *
 * It is a lot more performant than `setSharedState`.
 *
 * @param context React context.
 * @param key Key which uniquely identifies this state in Redux store.
 * @param initialState Initializes your global variable with this value.
 */
var useLocalState = function useLocalState(context, key, initialState) {
  var _state$shared;

  var store = context.store;
  var state = selectBackend(store.getState());
  var sharedStates = (_state$shared = state.shared) != null ? _state$shared : {};
  var sharedState = key in sharedStates ? sharedStates[key] : initialState;
  return [sharedState, function (nextState) {
    store.dispatch(backendSetSharedState({
      key: key,
      nextState: typeof nextState === 'function' ? nextState(sharedState) : nextState
    }));
  }];
};
/**
 * Allocates state on Redux store, and **shares** it with other clients
 * in the game.
 *
 * Use it when you want to have a stateful variable in your component
 * that persists not only between renders, but also gets pushed to other
 * clients that observe this UI.
 *
 * This makes creation of observable s
 *
 * @param context React context.
 * @param key Key which uniquely identifies this state in Redux store.
 * @param initialState Initializes your global variable with this value.
 */


exports.useLocalState = useLocalState;

var useSharedState = function useSharedState(context, key, initialState) {
  var _state$shared2;

  var store = context.store;
  var state = selectBackend(store.getState());
  var sharedStates = (_state$shared2 = state.shared) != null ? _state$shared2 : {};
  var sharedState = key in sharedStates ? sharedStates[key] : initialState;
  return [sharedState, function (nextState) {
    sendMessage({
      type: 'setSharedState',
      key: key,
      value: JSON.stringify(typeof nextState === 'function' ? nextState(sharedState) : nextState) || ''
    });
  }];
};

exports.useSharedState = useSharedState;

/***/ }),

/***/ 13251:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.AnimatedNumber = void 0;

var _math = __webpack_require__(97622);

var _inferno = __webpack_require__(96323);

function _inheritsLoose(subClass, superClass) { subClass.prototype = Object.create(superClass.prototype); subClass.prototype.constructor = subClass; _setPrototypeOf(subClass, superClass); }

function _setPrototypeOf(o, p) { _setPrototypeOf = Object.setPrototypeOf || function () { function _setPrototypeOf(o, p) { o.__proto__ = p; return o; } return _setPrototypeOf; }(); return _setPrototypeOf(o, p); }

var FPS = 20;
var Q = 0.5;

var isSafeNumber = function isSafeNumber(value) {
  return typeof value === 'number' && Number.isFinite(value) && !Number.isNaN(value);
};

var AnimatedNumber = /*#__PURE__*/function (_Component) {
  _inheritsLoose(AnimatedNumber, _Component);

  function AnimatedNumber(props) {
    var _this;

    _this = _Component.call(this, props) || this;
    _this.timer = null;
    _this.state = {
      value: 0
    }; // Use provided initial state

    if (isSafeNumber(props.initial)) {
      _this.state.value = props.initial;
    } // Set initial state with value provided in props
    else if (isSafeNumber(props.value)) {
      _this.state.value = Number(props.value);
    }

    return _this;
  }

  var _proto = AnimatedNumber.prototype;

  _proto.tick = function () {
    function tick() {
      var props = this.props,
          state = this.state;
      var currentValue = Number(state.value);
      var targetValue = Number(props.value); // Avoid poisoning our state with infinities and NaN

      if (!isSafeNumber(targetValue)) {
        return;
      } // Smooth the value using an exponential moving average


      var value = currentValue * Q + targetValue * (1 - Q);
      this.setState({
        value: value
      });
    }

    return tick;
  }();

  _proto.componentDidMount = function () {
    function componentDidMount() {
      var _this2 = this;

      this.timer = setInterval(function () {
        return _this2.tick();
      }, 1000 / FPS);
    }

    return componentDidMount;
  }();

  _proto.componentWillUnmount = function () {
    function componentWillUnmount() {
      clearTimeout(this.timer);
    }

    return componentWillUnmount;
  }();

  _proto.render = function () {
    function render() {
      var props = this.props,
          state = this.state;
      var format = props.format,
          children = props.children;
      var currentValue = state.value;
      var targetValue = props.value; // Directly display values which can't be animated

      if (!isSafeNumber(targetValue)) {
        return targetValue || null;
      }

      var formattedValue; // Use custom formatter

      if (format) {
        formattedValue = format(currentValue);
      } // Fix our animated precision at target value's precision.
      else {
        var fraction = String(targetValue).split('.')[1];
        var precision = fraction ? fraction.length : 0;
        formattedValue = (0, _math.toFixed)(currentValue, (0, _math.clamp)(precision, 0, 8));
      } // Use a custom render function


      if (typeof children === 'function') {
        return children(formattedValue, currentValue);
      }

      return formattedValue;
    }

    return render;
  }();

  return AnimatedNumber;
}(_inferno.Component);

exports.AnimatedNumber = AnimatedNumber;

/***/ }),

/***/ 90297:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Blink = void 0;

var _inferno = __webpack_require__(96323);

function _inheritsLoose(subClass, superClass) { subClass.prototype = Object.create(superClass.prototype); subClass.prototype.constructor = subClass; _setPrototypeOf(subClass, superClass); }

function _setPrototypeOf(o, p) { _setPrototypeOf = Object.setPrototypeOf || function () { function _setPrototypeOf(o, p) { o.__proto__ = p; return o; } return _setPrototypeOf; }(); return _setPrototypeOf(o, p); }

var DEFAULT_BLINKING_INTERVAL = 1000;
var DEFAULT_BLINKING_TIME = 1000;

var Blink = /*#__PURE__*/function (_Component) {
  _inheritsLoose(Blink, _Component);

  function Blink() {
    var _this;

    _this = _Component.call(this) || this;
    _this.state = {
      hidden: false
    };
    return _this;
  }

  var _proto = Blink.prototype;

  _proto.createTimer = function () {
    function createTimer() {
      var _this2 = this;

      var _this$props = this.props,
          _this$props$interval = _this$props.interval,
          interval = _this$props$interval === void 0 ? DEFAULT_BLINKING_INTERVAL : _this$props$interval,
          _this$props$time = _this$props.time,
          time = _this$props$time === void 0 ? DEFAULT_BLINKING_TIME : _this$props$time;
      clearInterval(this.interval);
      clearTimeout(this.timer);
      this.setState({
        hidden: false
      });
      this.interval = setInterval(function () {
        _this2.setState({
          hidden: true
        });

        _this2.timer = setTimeout(function () {
          _this2.setState({
            hidden: false
          });
        }, time);
      }, interval + time);
    }

    return createTimer;
  }();

  _proto.componentDidMount = function () {
    function componentDidMount() {
      this.createTimer();
    }

    return componentDidMount;
  }();

  _proto.componentDidUpdate = function () {
    function componentDidUpdate(prevProps) {
      if (prevProps.interval !== this.props.interval || prevProps.time !== this.props.time) {
        this.createTimer();
      }
    }

    return componentDidUpdate;
  }();

  _proto.componentWillUnmount = function () {
    function componentWillUnmount() {
      clearInterval(this.interval);
      clearTimeout(this.timer);
    }

    return componentWillUnmount;
  }();

  _proto.render = function () {
    function render(props) {
      return (0, _inferno.createVNode)(1, "span", null, props.children, 0, {
        "style": {
          visibility: this.state.hidden ? "hidden" : "visible"
        }
      });
    }

    return render;
  }();

  return Blink;
}(_inferno.Component);

exports.Blink = Blink;

/***/ }),

/***/ 7149:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.BlockQuote = void 0;

var _inferno = __webpack_require__(96323);

var _react = __webpack_require__(1060);

var _Box = __webpack_require__(54425);

var _excluded = ["className"];

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

var BlockQuote = function BlockQuote(props) {
  var className = props.className,
      rest = _objectWithoutPropertiesLoose(props, _excluded);

  return (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, _Box.Box, Object.assign({
    "className": (0, _react.classes)(['BlockQuote', className])
  }, rest)));
};

exports.BlockQuote = BlockQuote;

/***/ }),

/***/ 54425:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Box = exports.computeBoxClassName = exports.computeBoxProps = exports.halfUnit = exports.unit = void 0;

var _react = __webpack_require__(1060);

var _inferno = __webpack_require__(96323);

var _infernoVnodeFlags = __webpack_require__(10648);

var _constants = __webpack_require__(84200);

var _excluded = ["as", "className", "children"];

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

/**
 * Coverts our rem-like spacing unit into a CSS unit.
 */
var unit = function unit(value) {
  if (typeof value === 'string') {
    // Transparently convert pixels into rem units
    if (value.endsWith('px') && !Byond.IS_LTE_IE8) {
      return parseFloat(value) / 12 + 'rem';
    }

    return value;
  }

  if (typeof value === 'number') {
    if (Byond.IS_LTE_IE8) {
      return value * 12 + 'px';
    }

    return value + 'rem';
  }
};
/**
 * Same as `unit`, but half the size for integers numbers.
 */


exports.unit = unit;

var halfUnit = function halfUnit(value) {
  if (typeof value === 'string') {
    return unit(value);
  }

  if (typeof value === 'number') {
    return unit(value * 0.5);
  }
};

exports.halfUnit = halfUnit;

var isColorCode = function isColorCode(str) {
  return !isColorClass(str);
};

var isColorClass = function isColorClass(str) {
  if (typeof str === 'string') {
    return _constants.CSS_COLORS.includes(str);
  }
};

var mapRawPropTo = function mapRawPropTo(attrName) {
  return function (style, value) {
    if (typeof value === 'number' || typeof value === 'string') {
      style[attrName] = value;
    }
  };
};

var mapUnitPropTo = function mapUnitPropTo(attrName, unit) {
  return function (style, value) {
    if (typeof value === 'number' || typeof value === 'string') {
      style[attrName] = unit(value);
    }
  };
};

var mapBooleanPropTo = function mapBooleanPropTo(attrName, attrValue) {
  return function (style, value) {
    if (value) {
      style[attrName] = attrValue;
    }
  };
};

var mapDirectionalUnitPropTo = function mapDirectionalUnitPropTo(attrName, unit, dirs) {
  return function (style, value) {
    if (typeof value === 'number' || typeof value === 'string') {
      for (var i = 0; i < dirs.length; i++) {
        style[attrName + '-' + dirs[i]] = unit(value);
      }
    }
  };
};

var mapColorPropTo = function mapColorPropTo(attrName) {
  return function (style, value) {
    if (isColorCode(value)) {
      style[attrName] = value;
    }
  };
};

var styleMapperByPropName = {
  // Direct mapping
  position: mapRawPropTo('position'),
  overflow: mapRawPropTo('overflow'),
  overflowX: mapRawPropTo('overflow-x'),
  overflowY: mapRawPropTo('overflow-y'),
  top: mapUnitPropTo('top', unit),
  bottom: mapUnitPropTo('bottom', unit),
  left: mapUnitPropTo('left', unit),
  right: mapUnitPropTo('right', unit),
  width: mapUnitPropTo('width', unit),
  minWidth: mapUnitPropTo('min-width', unit),
  maxWidth: mapUnitPropTo('max-width', unit),
  height: mapUnitPropTo('height', unit),
  minHeight: mapUnitPropTo('min-height', unit),
  maxHeight: mapUnitPropTo('max-height', unit),
  fontSize: mapUnitPropTo('font-size', unit),
  fontFamily: mapRawPropTo('font-family'),
  lineHeight: function () {
    function lineHeight(style, value) {
      if (typeof value === 'number') {
        style['line-height'] = value;
      } else if (typeof value === 'string') {
        style['line-height'] = unit(value);
      }
    }

    return lineHeight;
  }(),
  opacity: mapRawPropTo('opacity'),
  textAlign: mapRawPropTo('text-align'),
  verticalAlign: mapRawPropTo('vertical-align'),
  // Boolean props
  inline: mapBooleanPropTo('display', 'inline-block'),
  bold: mapBooleanPropTo('font-weight', 'bold'),
  italic: mapBooleanPropTo('font-style', 'italic'),
  nowrap: mapBooleanPropTo('white-space', 'nowrap'),
  preserveWhitespace: mapBooleanPropTo('white-space', 'pre-wrap'),
  // Margins
  m: mapDirectionalUnitPropTo('margin', halfUnit, ['top', 'bottom', 'left', 'right']),
  mx: mapDirectionalUnitPropTo('margin', halfUnit, ['left', 'right']),
  my: mapDirectionalUnitPropTo('margin', halfUnit, ['top', 'bottom']),
  mt: mapUnitPropTo('margin-top', halfUnit),
  mb: mapUnitPropTo('margin-bottom', halfUnit),
  ml: mapUnitPropTo('margin-left', halfUnit),
  mr: mapUnitPropTo('margin-right', halfUnit),
  // Margins
  p: mapDirectionalUnitPropTo('padding', halfUnit, ['top', 'bottom', 'left', 'right']),
  px: mapDirectionalUnitPropTo('padding', halfUnit, ['left', 'right']),
  py: mapDirectionalUnitPropTo('padding', halfUnit, ['top', 'bottom']),
  pt: mapUnitPropTo('padding-top', halfUnit),
  pb: mapUnitPropTo('padding-bottom', halfUnit),
  pl: mapUnitPropTo('padding-left', halfUnit),
  pr: mapUnitPropTo('padding-right', halfUnit),
  // Color props
  color: mapColorPropTo('color'),
  textColor: mapColorPropTo('color'),
  backgroundColor: mapColorPropTo('background-color'),
  // Utility props
  fillPositionedParent: function () {
    function fillPositionedParent(style, value) {
      if (value) {
        style['position'] = 'absolute';
        style['top'] = 0;
        style['bottom'] = 0;
        style['left'] = 0;
        style['right'] = 0;
      }
    }

    return fillPositionedParent;
  }()
};

var computeBoxProps = function computeBoxProps(props) {
  var computedProps = {};
  var computedStyles = {}; // Compute props

  for (var _i = 0, _Object$keys = Object.keys(props); _i < _Object$keys.length; _i++) {
    var propName = _Object$keys[_i];

    if (propName === 'style') {
      continue;
    } // IE8: onclick workaround


    if (Byond.IS_LTE_IE8 && propName === 'onClick') {
      computedProps.onclick = props[propName];
      continue;
    }

    var propValue = props[propName];
    var mapPropToStyle = styleMapperByPropName[propName];

    if (mapPropToStyle) {
      mapPropToStyle(computedStyles, propValue);
    } else {
      computedProps[propName] = propValue;
    }
  } // Concatenate styles


  var style = '';

  for (var _i2 = 0, _Object$keys2 = Object.keys(computedStyles); _i2 < _Object$keys2.length; _i2++) {
    var attrName = _Object$keys2[_i2];
    var attrValue = computedStyles[attrName];
    style += attrName + ':' + attrValue + ';';
  }

  if (props.style) {
    for (var _i3 = 0, _Object$keys3 = Object.keys(props.style); _i3 < _Object$keys3.length; _i3++) {
      var _attrName = _Object$keys3[_i3];
      var _attrValue = props.style[_attrName];
      style += _attrName + ':' + _attrValue + ';';
    }
  }

  if (style.length > 0) {
    computedProps.style = style;
  }

  return computedProps;
};

exports.computeBoxProps = computeBoxProps;

var computeBoxClassName = function computeBoxClassName(props) {
  var color = props.textColor || props.color;
  var backgroundColor = props.backgroundColor;
  return (0, _react.classes)([isColorClass(color) && 'color-' + color, isColorClass(backgroundColor) && 'color-bg-' + backgroundColor]);
};

exports.computeBoxClassName = computeBoxClassName;

var Box = function Box(props) {
  var _props$as = props.as,
      as = _props$as === void 0 ? 'div' : _props$as,
      className = props.className,
      children = props.children,
      rest = _objectWithoutPropertiesLoose(props, _excluded); // Render props


  if (typeof children === 'function') {
    return children(computeBoxProps(props));
  }

  var computedClassName = typeof className === 'string' ? className + ' ' + computeBoxClassName(rest) : computeBoxClassName(rest);
  var computedProps = computeBoxProps(rest); // Render a wrapper element

  return (0, _inferno.createVNode)(_infernoVnodeFlags.VNodeFlags.HtmlElement, as, computedClassName, children, _infernoVnodeFlags.ChildFlags.UnknownChildren, computedProps);
};

exports.Box = Box;
Box.defaultHooks = _react.pureComponentHooks;

/***/ }),

/***/ 25808:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.ButtonInput = exports.ButtonConfirm = exports.ButtonCheckbox = exports.Button = void 0;

var _inferno = __webpack_require__(96323);

var _keycodes = __webpack_require__(57890);

var _react = __webpack_require__(1060);

var _logging = __webpack_require__(79926);

var _Box = __webpack_require__(54425);

var _Icon = __webpack_require__(65743);

var _Tooltip = __webpack_require__(59463);

var _excluded = ["className", "fluid", "icon", "iconRotation", "iconSpin", "iconColor", "iconPosition", "color", "disabled", "selected", "tooltip", "tooltipPosition", "ellipsis", "compact", "circular", "content", "children", "onclick", "onClick"],
    _excluded2 = ["checked"],
    _excluded3 = ["confirmContent", "confirmColor", "confirmIcon", "icon", "color", "content", "onClick"],
    _excluded4 = ["fluid", "content", "icon", "iconRotation", "iconSpin", "tooltip", "tooltipPosition", "color", "placeholder", "maxLength"];

function _inheritsLoose(subClass, superClass) { subClass.prototype = Object.create(superClass.prototype); subClass.prototype.constructor = subClass; _setPrototypeOf(subClass, superClass); }

function _setPrototypeOf(o, p) { _setPrototypeOf = Object.setPrototypeOf || function () { function _setPrototypeOf(o, p) { o.__proto__ = p; return o; } return _setPrototypeOf; }(); return _setPrototypeOf(o, p); }

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

var logger = (0, _logging.createLogger)('Button');

var Button = function Button(props) {
  var className = props.className,
      fluid = props.fluid,
      icon = props.icon,
      iconRotation = props.iconRotation,
      iconSpin = props.iconSpin,
      iconColor = props.iconColor,
      iconPosition = props.iconPosition,
      color = props.color,
      disabled = props.disabled,
      selected = props.selected,
      tooltip = props.tooltip,
      tooltipPosition = props.tooltipPosition,
      ellipsis = props.ellipsis,
      compact = props.compact,
      circular = props.circular,
      content = props.content,
      children = props.children,
      onclick = props.onclick,
      _onClick = props.onClick,
      rest = _objectWithoutPropertiesLoose(props, _excluded);

  var hasContent = !!(content || children); // A warning about the lowercase onclick

  if (onclick) {
    logger.warn("Lowercase 'onclick' is not supported on Button and lowercase" + " prop names are discouraged in general. Please use a camelCase" + "'onClick' instead and read: " + "https://infernojs.org/docs/guides/event-handling");
  } // IE8: Use a lowercase "onclick" because synthetic events are fucked.
  // IE8: Use an "unselectable" prop because "user-select" doesn't work.


  var buttonContent = (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, _Box.Box, Object.assign({
    "className": (0, _react.classes)(['Button', fluid && 'Button--fluid', disabled && 'Button--disabled', selected && 'Button--selected', hasContent && 'Button--hasContent', ellipsis && 'Button--ellipsis', circular && 'Button--circular', compact && 'Button--compact', iconPosition && 'Button--iconPosition--' + iconPosition, color && typeof color === 'string' ? 'Button--color--' + color : 'Button--color--default', className]),
    "tabIndex": !disabled && '0',
    "unselectable": Byond.IS_LTE_IE8,
    "onClick": function () {
      function onClick(e) {
        if (!disabled && _onClick) {
          _onClick(e);
        }
      }

      return onClick;
    }(),
    "onKeyDown": function () {
      function onKeyDown(e) {
        var keyCode = window.event ? e.which : e.keyCode; // Simulate a click when pressing space or enter.

        if (keyCode === _keycodes.KEY_SPACE || keyCode === _keycodes.KEY_ENTER) {
          e.preventDefault();

          if (!disabled && _onClick) {
            _onClick(e);
          }

          return;
        } // Refocus layout on pressing escape.


        if (keyCode === _keycodes.KEY_ESCAPE) {
          e.preventDefault();
          return;
        }
      }

      return onKeyDown;
    }()
  }, rest, {
    children: [icon && iconPosition !== 'right' && (0, _inferno.createComponentVNode)(2, _Icon.Icon, {
      "name": icon,
      "color": iconColor,
      "rotation": iconRotation,
      "spin": iconSpin
    }), content, children, icon && iconPosition === 'right' && (0, _inferno.createComponentVNode)(2, _Icon.Icon, {
      "name": icon,
      "color": iconColor,
      "rotation": iconRotation,
      "spin": iconSpin
    })]
  })));

  if (tooltip) {
    buttonContent = (0, _inferno.createComponentVNode)(2, _Tooltip.Tooltip, {
      "content": tooltip,
      "position": tooltipPosition,
      children: buttonContent
    });
  }

  return buttonContent;
};

exports.Button = Button;
Button.defaultHooks = _react.pureComponentHooks;

var ButtonCheckbox = function ButtonCheckbox(props) {
  var checked = props.checked,
      rest = _objectWithoutPropertiesLoose(props, _excluded2);

  return (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, Button, Object.assign({
    "color": "transparent",
    "icon": checked ? 'check-square-o' : 'square-o',
    "selected": checked
  }, rest)));
};

exports.ButtonCheckbox = ButtonCheckbox;
Button.Checkbox = ButtonCheckbox;

var ButtonConfirm = /*#__PURE__*/function (_Component) {
  _inheritsLoose(ButtonConfirm, _Component);

  function ButtonConfirm() {
    var _this;

    _this = _Component.call(this) || this;
    _this.state = {
      clickedOnce: false
    };

    _this.handleClick = function () {
      if (_this.state.clickedOnce) {
        _this.setClickedOnce(false);
      }
    };

    return _this;
  }

  var _proto = ButtonConfirm.prototype;

  _proto.setClickedOnce = function () {
    function setClickedOnce(clickedOnce) {
      var _this2 = this;

      this.setState({
        clickedOnce: clickedOnce
      });

      if (clickedOnce) {
        setTimeout(function () {
          return window.addEventListener('click', _this2.handleClick);
        });
      } else {
        window.removeEventListener('click', this.handleClick);
      }
    }

    return setClickedOnce;
  }();

  _proto.render = function () {
    function render() {
      var _this3 = this;

      var _this$props = this.props,
          _this$props$confirmCo = _this$props.confirmContent,
          confirmContent = _this$props$confirmCo === void 0 ? "Confirm?" : _this$props$confirmCo,
          _this$props$confirmCo2 = _this$props.confirmColor,
          confirmColor = _this$props$confirmCo2 === void 0 ? "bad" : _this$props$confirmCo2,
          confirmIcon = _this$props.confirmIcon,
          icon = _this$props.icon,
          color = _this$props.color,
          content = _this$props.content,
          _onClick2 = _this$props.onClick,
          rest = _objectWithoutPropertiesLoose(_this$props, _excluded3);

      return (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, Button, Object.assign({
        "content": this.state.clickedOnce ? confirmContent : content,
        "icon": this.state.clickedOnce ? confirmIcon : icon,
        "color": this.state.clickedOnce ? confirmColor : color,
        "onClick": function () {
          function onClick() {
            return _this3.state.clickedOnce ? _onClick2() : _this3.setClickedOnce(true);
          }

          return onClick;
        }()
      }, rest)));
    }

    return render;
  }();

  return ButtonConfirm;
}(_inferno.Component);

exports.ButtonConfirm = ButtonConfirm;
Button.Confirm = ButtonConfirm;

var ButtonInput = /*#__PURE__*/function (_Component2) {
  _inheritsLoose(ButtonInput, _Component2);

  function ButtonInput() {
    var _this4;

    _this4 = _Component2.call(this) || this;
    _this4.inputRef = (0, _inferno.createRef)();
    _this4.state = {
      inInput: false
    };
    return _this4;
  }

  var _proto2 = ButtonInput.prototype;

  _proto2.setInInput = function () {
    function setInInput(inInput) {
      this.setState({
        inInput: inInput
      });

      if (this.inputRef) {
        var input = this.inputRef.current;

        if (inInput) {
          input.value = this.props.currentValue || "";

          try {
            input.focus();
            input.select();
          } catch (_unused) {}
        }
      }
    }

    return setInInput;
  }();

  _proto2.commitResult = function () {
    function commitResult(e) {
      if (this.inputRef) {
        var input = this.inputRef.current;
        var hasValue = input.value !== "";

        if (hasValue) {
          this.props.onCommit(e, input.value);
          return;
        } else {
          if (!this.props.defaultValue) {
            return;
          }

          this.props.onCommit(e, this.props.defaultValue);
        }
      }
    }

    return commitResult;
  }();

  _proto2.render = function () {
    function render() {
      var _this5 = this;

      var _this$props2 = this.props,
          fluid = _this$props2.fluid,
          content = _this$props2.content,
          icon = _this$props2.icon,
          iconRotation = _this$props2.iconRotation,
          iconSpin = _this$props2.iconSpin,
          tooltip = _this$props2.tooltip,
          tooltipPosition = _this$props2.tooltipPosition,
          _this$props2$color = _this$props2.color,
          color = _this$props2$color === void 0 ? 'default' : _this$props2$color,
          placeholder = _this$props2.placeholder,
          maxLength = _this$props2.maxLength,
          rest = _objectWithoutPropertiesLoose(_this$props2, _excluded4);

      var buttonContent = (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, _Box.Box, Object.assign({
        "className": (0, _react.classes)(['Button', fluid && 'Button--fluid', 'Button--color--' + color])
      }, rest, {
        "onClick": function () {
          function onClick() {
            return _this5.setInInput(true);
          }

          return onClick;
        }(),
        children: [icon && (0, _inferno.createComponentVNode)(2, _Icon.Icon, {
          "name": icon,
          "rotation": iconRotation,
          "spin": iconSpin
        }), (0, _inferno.createVNode)(1, "div", null, content, 0), (0, _inferno.createVNode)(64, "input", "NumberInput__input", null, 1, {
          "style": {
            'display': !this.state.inInput ? 'none' : undefined,
            'text-align': 'left'
          },
          "onBlur": function () {
            function onBlur(e) {
              if (!_this5.state.inInput) {
                return;
              }

              _this5.setInInput(false);

              _this5.commitResult(e);
            }

            return onBlur;
          }(),
          "onKeyDown": function () {
            function onKeyDown(e) {
              if (e.keyCode === _keycodes.KEY_ENTER) {
                _this5.setInInput(false);

                _this5.commitResult(e);

                return;
              }

              if (e.keyCode === _keycodes.KEY_ESCAPE) {
                _this5.setInInput(false);
              }
            }

            return onKeyDown;
          }()
        }, null, this.inputRef)]
      })));

      if (tooltip) {
        buttonContent = (0, _inferno.createComponentVNode)(2, _Tooltip.Tooltip, {
          "content": tooltip,
          "position": tooltipPosition,
          children: buttonContent
        });
      }

      return buttonContent;
    }

    return render;
  }();

  return ButtonInput;
}(_inferno.Component);

exports.ButtonInput = ButtonInput;
Button.Input = ButtonInput;

/***/ }),

/***/ 44051:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.ByondUi = void 0;

var _inferno = __webpack_require__(96323);

var _react = __webpack_require__(1060);

var _timer = __webpack_require__(53538);

var _logging = __webpack_require__(79926);

var _Box = __webpack_require__(54425);

var _excluded = ["params"],
    _excluded2 = ["params"],
    _excluded3 = ["params"];

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

function _inheritsLoose(subClass, superClass) { subClass.prototype = Object.create(superClass.prototype); subClass.prototype.constructor = subClass; _setPrototypeOf(subClass, superClass); }

function _setPrototypeOf(o, p) { _setPrototypeOf = Object.setPrototypeOf || function () { function _setPrototypeOf(o, p) { o.__proto__ = p; return o; } return _setPrototypeOf; }(); return _setPrototypeOf(o, p); }

var logger = (0, _logging.createLogger)('ByondUi'); // Stack of currently allocated BYOND UI element ids.

var byondUiStack = [];

var createByondUiElement = function createByondUiElement(elementId) {
  // Reserve an index in the stack
  var index = byondUiStack.length;
  byondUiStack.push(null); // Get a unique id

  var id = elementId || 'byondui_' + index;
  logger.log("allocated '" + id + "'"); // Return a control structure

  return {
    render: function () {
      function render(params) {
        logger.log("rendering '" + id + "'");
        byondUiStack[index] = id;
        Byond.winset(id, params);
      }

      return render;
    }(),
    unmount: function () {
      function unmount() {
        logger.log("unmounting '" + id + "'");
        byondUiStack[index] = null;
        Byond.winset(id, {
          parent: ''
        });
      }

      return unmount;
    }()
  };
};

window.addEventListener('beforeunload', function () {
  // Cleanly unmount all visible UI elements
  for (var index = 0; index < byondUiStack.length; index++) {
    var id = byondUiStack[index];

    if (typeof id === 'string') {
      logger.log("unmounting '" + id + "' (beforeunload)");
      byondUiStack[index] = null;
      Byond.winset(id, {
        parent: ''
      });
    }
  }
});
/**
 * Get the bounding box of the DOM element.
 */

var getBoundingBox = function getBoundingBox(element) {
  var rect = element.getBoundingClientRect();
  return {
    pos: [rect.left, rect.top],
    size: [rect.right - rect.left, rect.bottom - rect.top]
  };
};

var ByondUi = /*#__PURE__*/function (_Component) {
  _inheritsLoose(ByondUi, _Component);

  function ByondUi(props) {
    var _props$params;

    var _this;

    _this = _Component.call(this, props) || this;
    _this.containerRef = (0, _inferno.createRef)();
    _this.byondUiElement = createByondUiElement((_props$params = props.params) == null ? void 0 : _props$params.id);
    _this.handleResize = (0, _timer.debounce)(function () {
      _this.forceUpdate();
    }, 100);
    return _this;
  }

  var _proto = ByondUi.prototype;

  _proto.shouldComponentUpdate = function () {
    function shouldComponentUpdate(nextProps) {
      var _this$props = this.props,
          _this$props$params = _this$props.params,
          prevParams = _this$props$params === void 0 ? {} : _this$props$params,
          prevRest = _objectWithoutPropertiesLoose(_this$props, _excluded);

      var _nextProps$params = nextProps.params,
          nextParams = _nextProps$params === void 0 ? {} : _nextProps$params,
          nextRest = _objectWithoutPropertiesLoose(nextProps, _excluded2);

      return (0, _react.shallowDiffers)(prevParams, nextParams) || (0, _react.shallowDiffers)(prevRest, nextRest);
    }

    return shouldComponentUpdate;
  }();

  _proto.componentDidMount = function () {
    function componentDidMount() {
      // IE8: It probably works, but fuck you anyway.
      if (Byond.IS_LTE_IE10) {
        return;
      }

      window.addEventListener('resize', this.handleResize);
      this.componentDidUpdate();
      this.handleResize();
    }

    return componentDidMount;
  }();

  _proto.componentDidUpdate = function () {
    function componentDidUpdate() {
      // IE8: It probably works, but fuck you anyway.
      if (Byond.IS_LTE_IE10) {
        return;
      }

      var _this$props$params2 = this.props.params,
          params = _this$props$params2 === void 0 ? {} : _this$props$params2;
      var box = getBoundingBox(this.containerRef.current);
      logger.debug('bounding box', box);
      this.byondUiElement.render(Object.assign({
        parent: window.__windowId__
      }, params, {
        pos: box.pos[0] + ',' + box.pos[1],
        size: box.size[0] + 'x' + box.size[1]
      }));
    }

    return componentDidUpdate;
  }();

  _proto.componentWillUnmount = function () {
    function componentWillUnmount() {
      // IE8: It probably works, but fuck you anyway.
      if (Byond.IS_LTE_IE10) {
        return;
      }

      window.removeEventListener('resize', this.handleResize);
      this.byondUiElement.unmount();
    }

    return componentWillUnmount;
  }();

  _proto.render = function () {
    function render() {
      var _this$props2 = this.props,
          params = _this$props2.params,
          rest = _objectWithoutPropertiesLoose(_this$props2, _excluded3);

      var boxProps = (0, _Box.computeBoxProps)(rest);
      return (0, _inferno.normalizeProps)((0, _inferno.createVNode)(1, "div", null, (0, _inferno.createVNode)(1, "div", null, null, 1, {
        "style": {
          'min-height': '22px'
        }
      }), 0, Object.assign({}, boxProps), null, this.containerRef));
    }

    return render;
  }();

  return ByondUi;
}(_inferno.Component);

exports.ByondUi = ByondUi;

/***/ }),

/***/ 64726:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Chart = void 0;

var _inferno = __webpack_require__(96323);

var _collections = __webpack_require__(1397);

var _react = __webpack_require__(1060);

var _Box = __webpack_require__(54425);

var _excluded = ["data", "rangeX", "rangeY", "fillColor", "strokeColor", "strokeWidth"];

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

function _inheritsLoose(subClass, superClass) { subClass.prototype = Object.create(superClass.prototype); subClass.prototype.constructor = subClass; _setPrototypeOf(subClass, superClass); }

function _setPrototypeOf(o, p) { _setPrototypeOf = Object.setPrototypeOf || function () { function _setPrototypeOf(o, p) { o.__proto__ = p; return o; } return _setPrototypeOf; }(); return _setPrototypeOf(o, p); }

var normalizeData = function normalizeData(data, scale, rangeX, rangeY) {
  if (data.length === 0) {
    return [];
  }

  var min = (0, _collections.zipWith)(Math.min).apply(void 0, data);
  var max = (0, _collections.zipWith)(Math.max).apply(void 0, data);

  if (rangeX !== undefined) {
    min[0] = rangeX[0];
    max[0] = rangeX[1];
  }

  if (rangeY !== undefined) {
    min[1] = rangeY[0];
    max[1] = rangeY[1];
  }

  var normalized = (0, _collections.map)(function (point) {
    return (0, _collections.zipWith)(function (value, min, max, scale) {
      return (value - min) / (max - min) * scale;
    })(point, min, max, scale);
  })(data);
  return normalized;
};

var dataToPolylinePoints = function dataToPolylinePoints(data) {
  var points = '';

  for (var i = 0; i < data.length; i++) {
    var point = data[i];
    points += point[0] + ',' + point[1] + ' ';
  }

  return points;
};

var LineChart = /*#__PURE__*/function (_Component) {
  _inheritsLoose(LineChart, _Component);

  function LineChart(props) {
    var _this;

    _this = _Component.call(this, props) || this;
    _this.ref = (0, _inferno.createRef)();
    _this.state = {
      // Initial guess
      viewBox: [600, 200]
    };

    _this.handleResize = function () {
      var element = _this.ref.current;

      _this.setState({
        viewBox: [element.offsetWidth, element.offsetHeight]
      });
    };

    return _this;
  }

  var _proto = LineChart.prototype;

  _proto.componentDidMount = function () {
    function componentDidMount() {
      window.addEventListener('resize', this.handleResize);
      this.handleResize();
    }

    return componentDidMount;
  }();

  _proto.componentWillUnmount = function () {
    function componentWillUnmount() {
      window.removeEventListener('resize', this.handleResize);
    }

    return componentWillUnmount;
  }();

  _proto.render = function () {
    function render() {
      var _this2 = this;

      var _this$props = this.props,
          _this$props$data = _this$props.data,
          data = _this$props$data === void 0 ? [] : _this$props$data,
          rangeX = _this$props.rangeX,
          rangeY = _this$props.rangeY,
          _this$props$fillColor = _this$props.fillColor,
          fillColor = _this$props$fillColor === void 0 ? 'none' : _this$props$fillColor,
          _this$props$strokeCol = _this$props.strokeColor,
          strokeColor = _this$props$strokeCol === void 0 ? '#ffffff' : _this$props$strokeCol,
          _this$props$strokeWid = _this$props.strokeWidth,
          strokeWidth = _this$props$strokeWid === void 0 ? 2 : _this$props$strokeWid,
          rest = _objectWithoutPropertiesLoose(_this$props, _excluded);

      var viewBox = this.state.viewBox;
      var normalized = normalizeData(data, viewBox, rangeX, rangeY); // Push data outside viewBox and form a fillable polygon

      if (normalized.length > 0) {
        var first = normalized[0];
        var last = normalized[normalized.length - 1];
        normalized.push([viewBox[0] + strokeWidth, last[1]]);
        normalized.push([viewBox[0] + strokeWidth, -strokeWidth]);
        normalized.push([-strokeWidth, -strokeWidth]);
        normalized.push([-strokeWidth, first[1]]);
      }

      var points = dataToPolylinePoints(normalized);
      return (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, _Box.Box, Object.assign({
        "position": "relative"
      }, rest, {
        children: function () {
          function children(props) {
            return (0, _inferno.normalizeProps)((0, _inferno.createVNode)(1, "div", null, (0, _inferno.createVNode)(32, "svg", null, (0, _inferno.createVNode)(32, "polyline", null, null, 1, {
              "transform": "scale(1, -1) translate(0, -" + viewBox[1] + ")",
              "fill": fillColor,
              "stroke": strokeColor,
              "stroke-width": strokeWidth,
              "points": points
            }), 2, {
              "viewBox": "0 0 " + viewBox[0] + " " + viewBox[1],
              "preserveAspectRatio": "none",
              "style": {
                position: 'absolute',
                top: 0,
                left: 0,
                right: 0,
                bottom: 0,
                overflow: 'hidden'
              }
            }), 2, Object.assign({}, props), null, _this2.ref));
          }

          return children;
        }()
      })));
    }

    return render;
  }();

  return LineChart;
}(_inferno.Component);

LineChart.defaultHooks = _react.pureComponentHooks;

var Stub = function Stub(props) {
  return null;
}; // IE8: No inline svg support


var Chart = {
  Line: Byond.IS_LTE_IE8 ? Stub : LineChart
};
exports.Chart = Chart;

/***/ }),

/***/ 49751:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Collapsible = void 0;

var _inferno = __webpack_require__(96323);

var _Box = __webpack_require__(54425);

var _Button = __webpack_require__(25808);

var _excluded = ["children", "color", "title", "buttons"];

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

function _inheritsLoose(subClass, superClass) { subClass.prototype = Object.create(superClass.prototype); subClass.prototype.constructor = subClass; _setPrototypeOf(subClass, superClass); }

function _setPrototypeOf(o, p) { _setPrototypeOf = Object.setPrototypeOf || function () { function _setPrototypeOf(o, p) { o.__proto__ = p; return o; } return _setPrototypeOf; }(); return _setPrototypeOf(o, p); }

var Collapsible = /*#__PURE__*/function (_Component) {
  _inheritsLoose(Collapsible, _Component);

  function Collapsible(props) {
    var _this;

    _this = _Component.call(this, props) || this;
    var open = props.open;
    _this.state = {
      open: open || false
    };
    return _this;
  }

  var _proto = Collapsible.prototype;

  _proto.render = function () {
    function render() {
      var _this2 = this;

      var props = this.props;
      var open = this.state.open;

      var children = props.children,
          _props$color = props.color,
          color = _props$color === void 0 ? 'default' : _props$color,
          title = props.title,
          buttons = props.buttons,
          rest = _objectWithoutPropertiesLoose(props, _excluded);

      return (0, _inferno.createComponentVNode)(2, _Box.Box, {
        "mb": 1,
        children: [(0, _inferno.createVNode)(1, "div", "Table", [(0, _inferno.createVNode)(1, "div", "Table__cell", (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, _Button.Button, Object.assign({
          "fluid": true,
          "color": color,
          "icon": open ? 'chevron-down' : 'chevron-right',
          "onClick": function () {
            function onClick() {
              return _this2.setState({
                open: !open
              });
            }

            return onClick;
          }()
        }, rest, {
          children: title
        }))), 2), buttons && (0, _inferno.createVNode)(1, "div", "Table__cell Table__cell--collapsing", buttons, 0)], 0), open && (0, _inferno.createComponentVNode)(2, _Box.Box, {
          "mt": 1,
          children: children
        })]
      });
    }

    return render;
  }();

  return Collapsible;
}(_inferno.Component);

exports.Collapsible = Collapsible;

/***/ }),

/***/ 47233:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.ColorBox = void 0;

var _inferno = __webpack_require__(96323);

var _react = __webpack_require__(1060);

var _Box = __webpack_require__(54425);

var _excluded = ["content", "children", "className", "color", "backgroundColor"];

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

var ColorBox = function ColorBox(props) {
  var content = props.content,
      children = props.children,
      className = props.className,
      color = props.color,
      backgroundColor = props.backgroundColor,
      rest = _objectWithoutPropertiesLoose(props, _excluded);

  rest.color = content ? null : 'transparent';
  rest.backgroundColor = color || backgroundColor;
  return (0, _inferno.normalizeProps)((0, _inferno.createVNode)(1, "div", (0, _react.classes)(['ColorBox', className, (0, _Box.computeBoxClassName)(rest)]), content || '.', 0, Object.assign({}, (0, _Box.computeBoxProps)(rest))));
};

exports.ColorBox = ColorBox;
ColorBox.defaultHooks = _react.pureComponentHooks;

/***/ }),

/***/ 93913:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Dimmer = void 0;

var _inferno = __webpack_require__(96323);

var _react = __webpack_require__(1060);

var _Box = __webpack_require__(54425);

var _excluded = ["className", "children"];

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

var Dimmer = function Dimmer(props) {
  var className = props.className,
      children = props.children,
      rest = _objectWithoutPropertiesLoose(props, _excluded);

  return (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, _Box.Box, Object.assign({
    "className": (0, _react.classes)(['Dimmer'].concat(className))
  }, rest, {
    children: (0, _inferno.createVNode)(1, "div", "Dimmer__inner", children, 0)
  })));
};

exports.Dimmer = Dimmer;

/***/ }),

/***/ 22755:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Divider = void 0;

var _inferno = __webpack_require__(96323);

var _react = __webpack_require__(1060);

/**
 * @file
 * @copyright 2020 Aleksej Komarov
 * @license MIT
 */
var Divider = function Divider(props) {
  var vertical = props.vertical,
      hidden = props.hidden;
  return (0, _inferno.createVNode)(1, "div", (0, _react.classes)(['Divider', hidden && 'Divider--hidden', vertical ? 'Divider--vertical' : 'Divider--horizontal']));
};

exports.Divider = Divider;

/***/ }),

/***/ 42833:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.DraggableControl = void 0;

var _inferno = __webpack_require__(96323);

var _math = __webpack_require__(97622);

var _react = __webpack_require__(1060);

var _AnimatedNumber = __webpack_require__(13251);

function _inheritsLoose(subClass, superClass) { subClass.prototype = Object.create(superClass.prototype); subClass.prototype.constructor = subClass; _setPrototypeOf(subClass, superClass); }

function _setPrototypeOf(o, p) { _setPrototypeOf = Object.setPrototypeOf || function () { function _setPrototypeOf(o, p) { o.__proto__ = p; return o; } return _setPrototypeOf; }(); return _setPrototypeOf(o, p); }

var DEFAULT_UPDATE_RATE = 400;
/**
 * Reduces screen offset to a single number based on the matrix provided.
 */

var getScalarScreenOffset = function getScalarScreenOffset(e, matrix) {
  return e.screenX * matrix[0] + e.screenY * matrix[1];
};

var DraggableControl = /*#__PURE__*/function (_Component) {
  _inheritsLoose(DraggableControl, _Component);

  function DraggableControl(props) {
    var _this;

    _this = _Component.call(this, props) || this;
    _this.inputRef = (0, _inferno.createRef)();
    _this.state = {
      value: props.value,
      dragging: false,
      editing: false,
      internalValue: null,
      origin: null,
      suppressingFlicker: false
    }; // Suppresses flickering while the value propagates through the backend

    _this.flickerTimer = null;

    _this.suppressFlicker = function () {
      var suppressFlicker = _this.props.suppressFlicker;

      if (suppressFlicker > 0) {
        _this.setState({
          suppressingFlicker: true
        });

        clearTimeout(_this.flickerTimer);
        _this.flickerTimer = setTimeout(function () {
          return _this.setState({
            suppressingFlicker: false
          });
        }, suppressFlicker);
      }
    };

    _this.handleDragStart = function (e) {
      var _this$props = _this.props,
          value = _this$props.value,
          dragMatrix = _this$props.dragMatrix;
      var editing = _this.state.editing;

      if (editing) {
        return;
      }

      document.body.style['pointer-events'] = 'none';
      _this.ref = e.target;

      _this.setState({
        dragging: false,
        origin: getScalarScreenOffset(e, dragMatrix),
        value: value,
        internalValue: value
      });

      _this.timer = setTimeout(function () {
        _this.setState({
          dragging: true
        });
      }, 250);
      _this.dragInterval = setInterval(function () {
        var _this$state = _this.state,
            dragging = _this$state.dragging,
            value = _this$state.value;
        var onDrag = _this.props.onDrag;

        if (dragging && onDrag) {
          onDrag(e, value);
        }
      }, _this.props.updateRate || DEFAULT_UPDATE_RATE);
      document.addEventListener('mousemove', _this.handleDragMove);
      document.addEventListener('mouseup', _this.handleDragEnd);
    };

    _this.handleDragMove = function (e) {
      var _this$props2 = _this.props,
          minValue = _this$props2.minValue,
          maxValue = _this$props2.maxValue,
          step = _this$props2.step,
          stepPixelSize = _this$props2.stepPixelSize,
          dragMatrix = _this$props2.dragMatrix;

      _this.setState(function (prevState) {
        var state = Object.assign({}, prevState);
        var offset = getScalarScreenOffset(e, dragMatrix) - state.origin;

        if (prevState.dragging) {
          var stepOffset = Number.isFinite(minValue) ? minValue % step : 0; // Translate mouse movement to value
          // Give it some headroom (by increasing clamp range by 1 step)

          state.internalValue = (0, _math.clamp)(state.internalValue + offset * step / stepPixelSize, minValue - step, maxValue + step); // Clamp the final value

          state.value = (0, _math.clamp)(state.internalValue - state.internalValue % step + stepOffset, minValue, maxValue);
          state.origin = getScalarScreenOffset(e, dragMatrix);
        } else if (Math.abs(offset) > 4) {
          state.dragging = true;
        }

        return state;
      });
    };

    _this.handleDragEnd = function (e) {
      var _this$props3 = _this.props,
          onChange = _this$props3.onChange,
          onDrag = _this$props3.onDrag;
      var _this$state2 = _this.state,
          dragging = _this$state2.dragging,
          value = _this$state2.value,
          internalValue = _this$state2.internalValue;
      document.body.style['pointer-events'] = 'auto';
      clearTimeout(_this.timer);
      clearInterval(_this.dragInterval);

      _this.setState({
        dragging: false,
        editing: !dragging,
        origin: null
      });

      document.removeEventListener('mousemove', _this.handleDragMove);
      document.removeEventListener('mouseup', _this.handleDragEnd);

      if (dragging) {
        _this.suppressFlicker();

        if (onChange) {
          onChange(e, value);
        }

        if (onDrag) {
          onDrag(e, value);
        }
      } else if (_this.inputRef) {
        var input = _this.inputRef.current;
        input.value = internalValue; // IE8: Dies when trying to focus a hidden element
        // (Error: Object does not support this action)

        try {
          input.focus();
          input.select();
        } catch (_unused) {}
      }
    };

    return _this;
  }

  var _proto = DraggableControl.prototype;

  _proto.render = function () {
    function render() {
      var _this2 = this;

      var _this$state3 = this.state,
          dragging = _this$state3.dragging,
          editing = _this$state3.editing,
          intermediateValue = _this$state3.value,
          suppressingFlicker = _this$state3.suppressingFlicker;
      var _this$props4 = this.props,
          animated = _this$props4.animated,
          value = _this$props4.value,
          unit = _this$props4.unit,
          minValue = _this$props4.minValue,
          maxValue = _this$props4.maxValue,
          unclamped = _this$props4.unclamped,
          format = _this$props4.format,
          onChange = _this$props4.onChange,
          onDrag = _this$props4.onDrag,
          children = _this$props4.children,
          height = _this$props4.height,
          lineHeight = _this$props4.lineHeight,
          fontSize = _this$props4.fontSize;
      var displayValue = value;

      if (dragging || suppressingFlicker) {
        displayValue = intermediateValue;
      } // Setup a display element
      // Shows a formatted number based on what we are currently doing
      // with the draggable surface.


      var renderDisplayElement = function () {
        function renderDisplayElement(value) {
          return value + (unit ? ' ' + unit : '');
        }

        return renderDisplayElement;
      }();

      var displayElement = animated && !dragging && !suppressingFlicker && (0, _inferno.createComponentVNode)(2, _AnimatedNumber.AnimatedNumber, {
        "value": displayValue,
        "format": format,
        children: renderDisplayElement
      }) || renderDisplayElement(format ? format(displayValue) : displayValue); // Setup an input element
      // Handles direct input via the keyboard

      var inputElement = (0, _inferno.createVNode)(64, "input", "NumberInput__input", null, 1, {
        "style": {
          display: !editing ? 'none' : undefined,
          height: height,
          'line-height': lineHeight,
          'font-size': fontSize
        },
        "onBlur": function () {
          function onBlur(e) {
            if (!editing) {
              return;
            }

            var value;

            if (unclamped) {
              value = parseFloat(e.target.value);
            } else {
              value = (0, _math.clamp)(parseFloat(e.target.value), minValue, maxValue);
            }

            if (Number.isNaN(value)) {
              _this2.setState({
                editing: false
              });

              return;
            }

            _this2.setState({
              editing: false,
              value: value
            });

            _this2.suppressFlicker();

            if (onChange) {
              onChange(e, value);
            }

            if (onDrag) {
              onDrag(e, value);
            }
          }

          return onBlur;
        }(),
        "onKeyDown": function () {
          function onKeyDown(e) {
            if (e.keyCode === 13) {
              var _value;

              if (unclamped) {
                _value = parseFloat(e.target.value);
              } else {
                _value = (0, _math.clamp)(parseFloat(e.target.value), minValue, maxValue);
              }

              if (Number.isNaN(_value)) {
                _this2.setState({
                  editing: false
                });

                return;
              }

              _this2.setState({
                editing: false,
                value: _value
              });

              _this2.suppressFlicker();

              if (onChange) {
                onChange(e, _value);
              }

              if (onDrag) {
                onDrag(e, _value);
              }

              return;
            }

            if (e.keyCode === 27) {
              _this2.setState({
                editing: false
              });

              return;
            }
          }

          return onKeyDown;
        }()
      }, null, this.inputRef); // Return a part of the state for higher-level components to use.

      return children({
        dragging: dragging,
        editing: editing,
        value: value,
        displayValue: displayValue,
        displayElement: displayElement,
        inputElement: inputElement,
        handleDragStart: this.handleDragStart
      });
    }

    return render;
  }();

  return DraggableControl;
}(_inferno.Component);

exports.DraggableControl = DraggableControl;
DraggableControl.defaultHooks = _react.pureComponentHooks;
DraggableControl.defaultProps = {
  minValue: -Infinity,
  maxValue: +Infinity,
  step: 1,
  stepPixelSize: 1,
  suppressFlicker: 50,
  dragMatrix: [1, 0]
};

/***/ }),

/***/ 12489:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Dropdown = void 0;

var _inferno = __webpack_require__(96323);

var _react = __webpack_require__(1060);

var _Box = __webpack_require__(54425);

var _Icon = __webpack_require__(65743);

var _excluded = ["icon", "iconRotation", "iconSpin", "color", "over", "noscroll", "nochevron", "width", "onClick", "selected", "disabled", "displayText"],
    _excluded2 = ["className"];

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

function _inheritsLoose(subClass, superClass) { subClass.prototype = Object.create(superClass.prototype); subClass.prototype.constructor = subClass; _setPrototypeOf(subClass, superClass); }

function _setPrototypeOf(o, p) { _setPrototypeOf = Object.setPrototypeOf || function () { function _setPrototypeOf(o, p) { o.__proto__ = p; return o; } return _setPrototypeOf; }(); return _setPrototypeOf(o, p); }

var Dropdown = /*#__PURE__*/function (_Component) {
  _inheritsLoose(Dropdown, _Component);

  function Dropdown(props) {
    var _this;

    _this = _Component.call(this, props) || this;
    _this.state = {
      selected: props.selected,
      open: false
    };

    _this.handleClick = function () {
      if (_this.state.open) {
        _this.setOpen(false);
      }
    };

    return _this;
  }

  var _proto = Dropdown.prototype;

  _proto.componentWillUnmount = function () {
    function componentWillUnmount() {
      window.removeEventListener('click', this.handleClick);
    }

    return componentWillUnmount;
  }();

  _proto.setOpen = function () {
    function setOpen(open) {
      var _this2 = this;

      this.setState({
        open: open
      });

      if (open) {
        setTimeout(function () {
          return window.addEventListener('click', _this2.handleClick);
        });
        this.menuRef.focus();
      } else {
        window.removeEventListener('click', this.handleClick);
      }
    }

    return setOpen;
  }();

  _proto.setSelected = function () {
    function setSelected(selected) {
      this.setState({
        selected: selected
      });
      this.setOpen(false);
      this.props.onSelected(selected);
    }

    return setSelected;
  }();

  _proto.buildMenu = function () {
    function buildMenu() {
      var _this3 = this;

      var _this$props$options = this.props.options,
          options = _this$props$options === void 0 ? [] : _this$props$options;
      var ops = options.map(function (option) {
        return (0, _inferno.createComponentVNode)(2, _Box.Box, {
          "className": "Dropdown__menuentry",
          "onClick": function () {
            function onClick() {
              _this3.setSelected(option);
            }

            return onClick;
          }(),
          children: option
        }, option);
      });
      return ops.length ? ops : 'No Options Found';
    }

    return buildMenu;
  }();

  _proto.render = function () {
    function render() {
      var _this4 = this;

      var props = this.props;

      var icon = props.icon,
          iconRotation = props.iconRotation,
          iconSpin = props.iconSpin,
          _props$color = props.color,
          color = _props$color === void 0 ? 'default' : _props$color,
          over = props.over,
          noscroll = props.noscroll,
          nochevron = props.nochevron,
          width = props.width,
          onClick = props.onClick,
          selected = props.selected,
          disabled = props.disabled,
          displayText = props.displayText,
          boxProps = _objectWithoutPropertiesLoose(props, _excluded);

      var className = boxProps.className,
          rest = _objectWithoutPropertiesLoose(boxProps, _excluded2);

      var adjustedOpen = over ? !this.state.open : this.state.open; // idk why we cache it

      if (this.state.selected !== props.selected) {
        this.setState({
          selected: props.selected
        });
      }

      var menu = this.state.open ? (0, _inferno.createVNode)(1, "div", (0, _react.classes)([noscroll && 'Dropdown__menu-noscroll' || 'Dropdown__menu', over && 'Dropdown__over']), this.buildMenu(), 0, {
        "tabIndex": "-1",
        "style": {
          'width': width
        }
      }, null, function (menu) {
        _this4.menuRef = menu;
      }) : null;
      return (0, _inferno.createVNode)(1, "div", "Dropdown", [(0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, _Box.Box, Object.assign({
        "width": width,
        "className": (0, _react.classes)(['Dropdown__control', 'Button', 'Button--color--' + color, disabled && 'Button--disabled', className])
      }, rest, {
        "onClick": function () {
          function onClick() {
            if (disabled && !_this4.state.open) {
              return;
            }

            _this4.setOpen(!_this4.state.open);
          }

          return onClick;
        }(),
        children: [icon && (0, _inferno.createComponentVNode)(2, _Icon.Icon, {
          "name": icon,
          "rotation": iconRotation,
          "spin": iconSpin,
          "mr": 1
        }), (0, _inferno.createVNode)(1, "span", "Dropdown__selected-text", displayText ? displayText : this.state.selected, 0), !!nochevron || (0, _inferno.createVNode)(1, "span", "Dropdown__arrow-button", (0, _inferno.createComponentVNode)(2, _Icon.Icon, {
          "name": adjustedOpen ? 'chevron-up' : 'chevron-down'
        }), 2)]
      }))), menu], 0);
    }

    return render;
  }();

  return Dropdown;
}(_inferno.Component);

exports.Dropdown = Dropdown;

/***/ }),

/***/ 67486:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.computeFlexItemProps = exports.Flex = exports.computeFlexProps = void 0;

var _inferno = __webpack_require__(96323);

var _react = __webpack_require__(1060);

var _Box = __webpack_require__(54425);

var _excluded = ["className", "direction", "wrap", "align", "justify", "inline"],
    _excluded2 = ["className", "style", "grow", "order", "shrink", "basis", "align"];

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

var computeFlexProps = function computeFlexProps(props) {
  var className = props.className,
      direction = props.direction,
      wrap = props.wrap,
      align = props.align,
      justify = props.justify,
      inline = props.inline,
      rest = _objectWithoutPropertiesLoose(props, _excluded);

  return Object.assign({
    className: (0, _react.classes)(['Flex', Byond.IS_LTE_IE10 && (direction === 'column' ? 'Flex--iefix--column' : 'Flex--iefix'), inline && 'Flex--inline', className]),
    style: Object.assign({}, rest.style, {
      'flex-direction': direction,
      'flex-wrap': wrap === true ? 'wrap' : wrap,
      'align-items': align,
      'justify-content': justify
    })
  }, rest);
};

exports.computeFlexProps = computeFlexProps;

var Flex = function Flex(props) {
  return (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, _Box.Box, Object.assign({}, computeFlexProps(props))));
};

exports.Flex = Flex;
Flex.defaultHooks = _react.pureComponentHooks;

var computeFlexItemProps = function computeFlexItemProps(props) {
  var className = props.className,
      style = props.style,
      grow = props.grow,
      order = props.order,
      shrink = props.shrink,
      _props$basis = props.basis,
      basis = _props$basis === void 0 ? props.width : _props$basis,
      align = props.align,
      rest = _objectWithoutPropertiesLoose(props, _excluded2);

  return Object.assign({
    className: (0, _react.classes)(['Flex__item', Byond.IS_LTE_IE10 && 'Flex__item--iefix', Byond.IS_LTE_IE10 && grow > 0 && 'Flex__item--iefix--grow', className]),
    style: Object.assign({}, style, {
      'flex-grow': grow !== undefined && Number(grow),
      'flex-shrink': shrink !== undefined && Number(shrink),
      'flex-basis': (0, _Box.unit)(basis),
      'order': order,
      'align-self': align
    })
  }, rest);
};

exports.computeFlexItemProps = computeFlexItemProps;

var FlexItem = function FlexItem(props) {
  return (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, _Box.Box, Object.assign({}, computeFlexItemProps(props))));
};

FlexItem.defaultHooks = _react.pureComponentHooks;
Flex.Item = FlexItem;

/***/ }),

/***/ 28408:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.GridColumn = exports.Grid = void 0;

var _inferno = __webpack_require__(96323);

var _Table = __webpack_require__(28581);

var _react = __webpack_require__(1060);

var _excluded = ["children"],
    _excluded2 = ["size", "style"];

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

/** @deprecated */
var Grid = function Grid(props) {
  var children = props.children,
      rest = _objectWithoutPropertiesLoose(props, _excluded);

  return (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, _Table.Table, Object.assign({}, rest, {
    children: (0, _inferno.createComponentVNode)(2, _Table.Table.Row, {
      children: children
    })
  })));
};

exports.Grid = Grid;
Grid.defaultHooks = _react.pureComponentHooks;
/** @deprecated */

var GridColumn = function GridColumn(props) {
  var _props$size = props.size,
      size = _props$size === void 0 ? 1 : _props$size,
      style = props.style,
      rest = _objectWithoutPropertiesLoose(props, _excluded2);

  return (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, _Table.Table.Cell, Object.assign({
    "style": Object.assign({
      width: size + '%'
    }, style)
  }, rest)));
};

exports.GridColumn = GridColumn;
Grid.defaultHooks = _react.pureComponentHooks;
Grid.Column = GridColumn;

/***/ }),

/***/ 65743:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.IconStack = exports.Icon = void 0;

var _inferno = __webpack_require__(96323);

var _react = __webpack_require__(1060);

var _Box = __webpack_require__(54425);

var _excluded = ["name", "size", "spin", "className", "style", "rotation", "inverse"],
    _excluded2 = ["className", "style", "children"];

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

var FA_OUTLINE_REGEX = /-o$/;

var Icon = function Icon(props) {
  var name = props.name,
      size = props.size,
      spin = props.spin,
      className = props.className,
      _props$style = props.style,
      style = _props$style === void 0 ? {} : _props$style,
      rotation = props.rotation,
      inverse = props.inverse,
      rest = _objectWithoutPropertiesLoose(props, _excluded);

  if (size) {
    style['font-size'] = size * 100 + '%';
  }

  if (typeof rotation === 'number') {
    style['transform'] = "rotate(" + rotation + "deg)";
  }

  var iconClass = "";

  if (name.startsWith("tg-")) {
    // tgfont icon
    iconClass = name;
  } else {
    // font awesome icon
    var faRegular = FA_OUTLINE_REGEX.test(name);
    var faName = name.replace(FA_OUTLINE_REGEX, '');
    iconClass = (faRegular ? 'far ' : 'fas ') + 'fa-' + faName + (spin ? " fa-spin" : "");
  }

  return (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, _Box.Box, Object.assign({
    "as": "i",
    "className": (0, _react.classes)(['Icon', className, iconClass]),
    "style": style
  }, rest)));
};

exports.Icon = Icon;
Icon.defaultHooks = _react.pureComponentHooks;

var IconStack = function IconStack(props) {
  var className = props.className,
      _props$style2 = props.style,
      style = _props$style2 === void 0 ? {} : _props$style2,
      children = props.children,
      rest = _objectWithoutPropertiesLoose(props, _excluded2);

  return (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, _Box.Box, Object.assign({
    "as": "span",
    "class": (0, _react.classes)(['IconStack', className]),
    "style": style
  }, rest, {
    children: children
  })));
};

exports.IconStack = IconStack;
Icon.Stack = IconStack;

/***/ }),

/***/ 63268:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.InfinitePlane = void 0;

var _inferno = __webpack_require__(96323);

var _Box = __webpack_require__(54425);

var _Stack = __webpack_require__(4033);

var _ProgressBar = __webpack_require__(12194);

var _Button = __webpack_require__(25808);

var _excluded = ["children", "backgroundImage", "imageWidth", "initialLeft", "initialTop"];

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

function _assertThisInitialized(self) { if (self === void 0) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return self; }

function _inheritsLoose(subClass, superClass) { subClass.prototype = Object.create(superClass.prototype); subClass.prototype.constructor = subClass; _setPrototypeOf(subClass, superClass); }

function _setPrototypeOf(o, p) { _setPrototypeOf = Object.setPrototypeOf || function () { function _setPrototypeOf(o, p) { o.__proto__ = p; return o; } return _setPrototypeOf; }(); return _setPrototypeOf(o, p); }

var ZOOM_MIN_VAL = 0.5;
var ZOOM_MAX_VAL = 1.5;
var ZOOM_INCREMENT = 0.1;

var InfinitePlane = /*#__PURE__*/function (_Component) {
  _inheritsLoose(InfinitePlane, _Component);

  function InfinitePlane() {
    var _this;

    _this = _Component.call(this) || this;
    _this.state = {
      mouseDown: false,
      left: 0,
      top: 0,
      lastLeft: 0,
      lastTop: 0,
      zoom: 1
    };
    _this.handleMouseDown = _this.handleMouseDown.bind(_assertThisInitialized(_this));
    _this.handleMouseMove = _this.handleMouseMove.bind(_assertThisInitialized(_this));
    _this.handleZoomIncrease = _this.handleZoomIncrease.bind(_assertThisInitialized(_this));
    _this.handleZoomDecrease = _this.handleZoomDecrease.bind(_assertThisInitialized(_this));
    _this.onMouseUp = _this.onMouseUp.bind(_assertThisInitialized(_this));
    _this.doOffsetMouse = _this.doOffsetMouse.bind(_assertThisInitialized(_this));
    return _this;
  }

  var _proto = InfinitePlane.prototype;

  _proto.componentDidMount = function () {
    function componentDidMount() {
      window.addEventListener("mouseup", this.onMouseUp);
      window.addEventListener("mousedown", this.doOffsetMouse);
      window.addEventListener("mousemove", this.doOffsetMouse);
      window.addEventListener("mouseup", this.doOffsetMouse);
    }

    return componentDidMount;
  }();

  _proto.componentWillUnmount = function () {
    function componentWillUnmount() {
      window.removeEventListener("mouseup", this.onMouseUp);
      window.removeEventListener("mousedown", this.doOffsetMouse);
      window.removeEventListener("mousemove", this.doOffsetMouse);
      window.removeEventListener("mouseup", this.doOffsetMouse);
    }

    return componentWillUnmount;
  }();

  _proto.doOffsetMouse = function () {
    function doOffsetMouse(event) {
      var zoom = this.state.zoom;
      event.screenZoomX = event.screenX * Math.pow(zoom, -1);
      event.screenZoomY = event.screenY * Math.pow(zoom, -1);
    }

    return doOffsetMouse;
  }();

  _proto.handleMouseDown = function () {
    function handleMouseDown(event) {
      this.setState(function (state) {
        return {
          mouseDown: true,
          lastLeft: event.clientX - state.left,
          lastTop: event.clientY - state.top
        };
      });
    }

    return handleMouseDown;
  }();

  _proto.onMouseUp = function () {
    function onMouseUp() {
      this.setState({
        mouseDown: false
      });
    }

    return onMouseUp;
  }();

  _proto.handleZoomIncrease = function () {
    function handleZoomIncrease(event) {
      var onZoomChange = this.props.onZoomChange;
      var zoom = this.state.zoom;
      var newZoomValue = Math.min(zoom + ZOOM_INCREMENT, ZOOM_MAX_VAL);
      this.setState({
        zoom: newZoomValue
      });

      if (onZoomChange) {
        onZoomChange(newZoomValue);
      }
    }

    return handleZoomIncrease;
  }();

  _proto.handleZoomDecrease = function () {
    function handleZoomDecrease(event) {
      var onZoomChange = this.props.onZoomChange;
      var zoom = this.state.zoom;
      var newZoomValue = Math.max(zoom - ZOOM_INCREMENT, ZOOM_MIN_VAL);
      this.setState({
        zoom: newZoomValue
      });

      if (onZoomChange) {
        onZoomChange(newZoomValue);
      }
    }

    return handleZoomDecrease;
  }();

  _proto.handleMouseMove = function () {
    function handleMouseMove(event) {
      var _this$props = this.props,
          onBackgroundMoved = _this$props.onBackgroundMoved,
          _this$props$initialLe = _this$props.initialLeft,
          initialLeft = _this$props$initialLe === void 0 ? 0 : _this$props$initialLe,
          _this$props$initialTo = _this$props.initialTop,
          initialTop = _this$props$initialTo === void 0 ? 0 : _this$props$initialTo;

      if (this.state.mouseDown) {
        var newX, newY;
        this.setState(function (state) {
          newX = event.clientX - state.lastLeft;
          newY = event.clientY - state.lastTop;
          return {
            left: newX,
            top: newY
          };
        });

        if (onBackgroundMoved) {
          onBackgroundMoved(newX + initialLeft, newY + initialTop);
        }
      }
    }

    return handleMouseMove;
  }();

  _proto.render = function () {
    function render() {
      var _this$props2 = this.props,
          children = _this$props2.children,
          backgroundImage = _this$props2.backgroundImage,
          imageWidth = _this$props2.imageWidth,
          _this$props2$initialL = _this$props2.initialLeft,
          initialLeft = _this$props2$initialL === void 0 ? 0 : _this$props2$initialL,
          _this$props2$initialT = _this$props2.initialTop,
          initialTop = _this$props2$initialT === void 0 ? 0 : _this$props2$initialT,
          rest = _objectWithoutPropertiesLoose(_this$props2, _excluded);

      var _this$state = this.state,
          left = _this$state.left,
          top = _this$state.top,
          zoom = _this$state.zoom;
      var finalLeft = initialLeft + left;
      var finalTop = initialTop + top;
      return (0, _inferno.normalizeProps)((0, _inferno.createVNode)(1, "div", null, [(0, _inferno.createVNode)(1, "div", null, null, 1, {
        "onMouseDown": this.handleMouseDown,
        "onMouseMove": this.handleMouseMove,
        "style": {
          "position": "fixed",
          "height": "100%",
          "width": "100%",
          "background-image": "url(\"" + backgroundImage + "\")",
          "background-position": finalLeft + "px " + finalTop + "px",
          "background-repeat": "repeat",
          "background-size": zoom * imageWidth + "px"
        }
      }), (0, _inferno.createVNode)(1, "div", null, children, 0, {
        "onMouseDown": this.handleMouseDown,
        "onMouseMove": this.handleMouseMove,
        "style": {
          "position": "fixed",
          "transform": "translate(" + finalLeft + "px, " + finalTop + "px) scale(" + zoom + ")",
          "transform-origin": "top left",
          "height": "100%",
          "width": "100%"
        }
      }), (0, _inferno.createComponentVNode)(2, _Stack.Stack, {
        "position": "absolute",
        "width": "100%",
        children: [(0, _inferno.createComponentVNode)(2, _Stack.Stack.Item, {
          children: (0, _inferno.createComponentVNode)(2, _Button.Button, {
            "icon": "minus",
            "onClick": this.handleZoomDecrease
          })
        }), (0, _inferno.createComponentVNode)(2, _Stack.Stack.Item, {
          "grow": 1,
          children: (0, _inferno.createComponentVNode)(2, _ProgressBar.ProgressBar, {
            "minValue": ZOOM_MIN_VAL,
            "value": zoom,
            "maxValue": ZOOM_MAX_VAL,
            children: [zoom, "x"]
          })
        }), (0, _inferno.createComponentVNode)(2, _Stack.Stack.Item, {
          children: (0, _inferno.createComponentVNode)(2, _Button.Button, {
            "icon": "plus",
            "onClick": this.handleZoomIncrease
          })
        })]
      })], 4, Object.assign({}, (0, _Box.computeBoxProps)(Object.assign({}, rest, {
        style: Object.assign({}, rest.style, {
          overflow: "hidden",
          position: "relative"
        })
      }))), null, this.ref));
    }

    return render;
  }();

  return InfinitePlane;
}(_inferno.Component);

exports.InfinitePlane = InfinitePlane;

/***/ }),

/***/ 77863:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Input = exports.toInputValue = void 0;

var _inferno = __webpack_require__(96323);

var _react = __webpack_require__(1060);

var _Box = __webpack_require__(54425);

var _keycodes = __webpack_require__(57890);

var _excluded = ["selfClear", "onInput", "onChange", "onEnter", "value", "maxLength", "placeholder"],
    _excluded2 = ["className", "fluid", "monospace"];

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

function _inheritsLoose(subClass, superClass) { subClass.prototype = Object.create(superClass.prototype); subClass.prototype.constructor = subClass; _setPrototypeOf(subClass, superClass); }

function _setPrototypeOf(o, p) { _setPrototypeOf = Object.setPrototypeOf || function () { function _setPrototypeOf(o, p) { o.__proto__ = p; return o; } return _setPrototypeOf; }(); return _setPrototypeOf(o, p); }

var toInputValue = function toInputValue(value) {
  return typeof value !== 'number' && typeof value !== 'string' ? '' : String(value);
};

exports.toInputValue = toInputValue;

var Input = /*#__PURE__*/function (_Component) {
  _inheritsLoose(Input, _Component);

  function Input() {
    var _this;

    _this = _Component.call(this) || this;
    _this.inputRef = (0, _inferno.createRef)();
    _this.state = {
      editing: false
    };

    _this.handleInput = function (e) {
      var editing = _this.state.editing;
      var onInput = _this.props.onInput;

      if (!editing) {
        _this.setEditing(true);
      }

      if (onInput) {
        onInput(e, e.target.value);
      }
    };

    _this.handleFocus = function (e) {
      var editing = _this.state.editing;

      if (!editing) {
        _this.setEditing(true);
      }
    };

    _this.handleBlur = function (e) {
      var editing = _this.state.editing;
      var onChange = _this.props.onChange;

      if (editing) {
        _this.setEditing(false);

        if (onChange) {
          onChange(e, e.target.value);
        }
      }
    };

    _this.handleKeyDown = function (e) {
      var _this$props = _this.props,
          onInput = _this$props.onInput,
          onChange = _this$props.onChange,
          onEnter = _this$props.onEnter;

      if (e.keyCode === _keycodes.KEY_ENTER) {
        _this.setEditing(false);

        if (onChange) {
          onChange(e, e.target.value);
        }

        if (onInput) {
          onInput(e, e.target.value);
        }

        if (onEnter) {
          onEnter(e, e.target.value);
        }

        if (_this.props.selfClear) {
          e.target.value = '';
        } else {
          e.target.blur();
        }

        return;
      }

      if (e.keyCode === _keycodes.KEY_ESCAPE) {
        _this.setEditing(false);

        e.target.value = toInputValue(_this.props.value);
        e.target.blur();
        return;
      }
    };

    return _this;
  }

  var _proto = Input.prototype;

  _proto.componentDidMount = function () {
    function componentDidMount() {
      var nextValue = this.props.value;
      var input = this.inputRef.current;

      if (input) {
        input.value = toInputValue(nextValue);
      }

      if (this.props.autoFocus) {
        setTimeout(function () {
          return input.focus();
        }, 1);
      }
    }

    return componentDidMount;
  }();

  _proto.componentDidUpdate = function () {
    function componentDidUpdate(prevProps, prevState) {
      var editing = this.state.editing;
      var prevValue = prevProps.value;
      var nextValue = this.props.value;
      var input = this.inputRef.current;

      if (input && !editing && prevValue !== nextValue) {
        input.value = toInputValue(nextValue);
      }
    }

    return componentDidUpdate;
  }();

  _proto.setEditing = function () {
    function setEditing(editing) {
      this.setState({
        editing: editing
      });
    }

    return setEditing;
  }();

  _proto.render = function () {
    function render() {
      var props = this.props; // Input only props

      var selfClear = props.selfClear,
          onInput = props.onInput,
          onChange = props.onChange,
          onEnter = props.onEnter,
          value = props.value,
          maxLength = props.maxLength,
          placeholder = props.placeholder,
          boxProps = _objectWithoutPropertiesLoose(props, _excluded); // Box props


      var className = boxProps.className,
          fluid = boxProps.fluid,
          monospace = boxProps.monospace,
          rest = _objectWithoutPropertiesLoose(boxProps, _excluded2);

      return (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, _Box.Box, Object.assign({
        "className": (0, _react.classes)(['Input', fluid && 'Input--fluid', monospace && 'Input--monospace', className])
      }, rest, {
        children: [(0, _inferno.createVNode)(1, "div", "Input__baseline", ".", 16), (0, _inferno.createVNode)(64, "input", "Input__input", null, 1, {
          "placeholder": placeholder,
          "onInput": this.handleInput,
          "onFocus": this.handleFocus,
          "onBlur": this.handleBlur,
          "onKeyDown": this.handleKeyDown,
          "maxLength": maxLength
        }, null, this.inputRef)]
      })));
    }

    return render;
  }();

  return Input;
}(_inferno.Component);

exports.Input = Input;

/***/ }),

/***/ 61515:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.KeyListener = void 0;

var _inferno = __webpack_require__(96323);

var _hotkeys = __webpack_require__(6965);

function _inheritsLoose(subClass, superClass) { subClass.prototype = Object.create(superClass.prototype); subClass.prototype.constructor = subClass; _setPrototypeOf(subClass, superClass); }

function _setPrototypeOf(o, p) { _setPrototypeOf = Object.setPrototypeOf || function () { function _setPrototypeOf(o, p) { o.__proto__ = p; return o; } return _setPrototypeOf; }(); return _setPrototypeOf(o, p); }

var KeyListener = /*#__PURE__*/function (_Component) {
  _inheritsLoose(KeyListener, _Component);

  function KeyListener(props) {
    var _this;

    _this = _Component.call(this, props) || this;
    _this.dispose = void 0;
    _this.dispose = (0, _hotkeys.listenForKeyEvents)(function (key) {
      if (_this.props.onKey) {
        _this.props.onKey(key);
      }

      if (key.isDown() && _this.props.onKeyDown) {
        _this.props.onKeyDown(key);
      }

      if (key.isUp() && _this.props.onKeyUp) {
        _this.props.onKeyUp(key);
      }
    });
    return _this;
  }

  var _proto = KeyListener.prototype;

  _proto.componentWillUnmount = function () {
    function componentWillUnmount() {
      this.dispose();
    }

    return componentWillUnmount;
  }();

  _proto.render = function () {
    function render() {
      return null;
    }

    return render;
  }();

  return KeyListener;
}(_inferno.Component);

exports.KeyListener = KeyListener;

/***/ }),

/***/ 14274:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Knob = void 0;

var _inferno = __webpack_require__(96323);

var _math = __webpack_require__(97622);

var _react = __webpack_require__(1060);

var _Box = __webpack_require__(54425);

var _DraggableControl = __webpack_require__(42833);

var _NumberInput = __webpack_require__(97667);

var _excluded = ["animated", "format", "maxValue", "minValue", "unclamped", "onChange", "onDrag", "step", "stepPixelSize", "suppressFlicker", "unit", "value", "className", "style", "fillValue", "color", "ranges", "size", "bipolar", "children"];

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

var Knob = function Knob(props) {
  // IE8: I don't want to support a yet another component on IE8.
  // IE8: It also can't handle SVG.
  if (Byond.IS_LTE_IE8) {
    return (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, _NumberInput.NumberInput, Object.assign({}, props)));
  }

  var animated = props.animated,
      format = props.format,
      maxValue = props.maxValue,
      minValue = props.minValue,
      unclamped = props.unclamped,
      onChange = props.onChange,
      onDrag = props.onDrag,
      step = props.step,
      stepPixelSize = props.stepPixelSize,
      suppressFlicker = props.suppressFlicker,
      unit = props.unit,
      value = props.value,
      className = props.className,
      style = props.style,
      fillValue = props.fillValue,
      color = props.color,
      _props$ranges = props.ranges,
      ranges = _props$ranges === void 0 ? {} : _props$ranges,
      _props$size = props.size,
      size = _props$size === void 0 ? 1 : _props$size,
      bipolar = props.bipolar,
      children = props.children,
      rest = _objectWithoutPropertiesLoose(props, _excluded);

  return (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, _DraggableControl.DraggableControl, Object.assign({
    "dragMatrix": [0, -1]
  }, {
    animated: animated,
    format: format,
    maxValue: maxValue,
    minValue: minValue,
    unclamped: unclamped,
    onChange: onChange,
    onDrag: onDrag,
    step: step,
    stepPixelSize: stepPixelSize,
    suppressFlicker: suppressFlicker,
    unit: unit,
    value: value
  }, {
    children: function () {
      function children(control) {
        var dragging = control.dragging,
            editing = control.editing,
            value = control.value,
            displayValue = control.displayValue,
            displayElement = control.displayElement,
            inputElement = control.inputElement,
            handleDragStart = control.handleDragStart;
        var scaledFillValue = (0, _math.scale)(fillValue != null ? fillValue : displayValue, minValue, maxValue);
        var scaledDisplayValue = (0, _math.scale)(displayValue, minValue, maxValue);
        var effectiveColor = color || (0, _math.keyOfMatchingRange)(fillValue != null ? fillValue : value, ranges) || 'default';
        var rotation = Math.min((scaledDisplayValue - 0.5) * 270, 225);
        return (0, _inferno.normalizeProps)((0, _inferno.createVNode)(1, "div", (0, _react.classes)(['Knob', 'Knob--color--' + effectiveColor, bipolar && 'Knob--bipolar', className, (0, _Box.computeBoxClassName)(rest)]), [(0, _inferno.createVNode)(1, "div", "Knob__circle", (0, _inferno.createVNode)(1, "div", "Knob__cursorBox", (0, _inferno.createVNode)(1, "div", "Knob__cursor"), 2, {
          "style": {
            transform: "rotate(" + rotation + "deg)"
          }
        }), 2), dragging && (0, _inferno.createVNode)(1, "div", "Knob__popupValue", displayElement, 0), (0, _inferno.createVNode)(32, "svg", "Knob__ring Knob__ringTrackPivot", (0, _inferno.createVNode)(32, "circle", "Knob__ringTrack", null, 1, {
          "cx": "50",
          "cy": "50",
          "r": "50"
        }), 2, {
          "viewBox": "0 0 100 100"
        }), (0, _inferno.createVNode)(32, "svg", "Knob__ring Knob__ringFillPivot", (0, _inferno.createVNode)(32, "circle", "Knob__ringFill", null, 1, {
          "style": {
            'stroke-dashoffset': Math.max(((bipolar ? 2.75 : 2.00) - scaledFillValue * 1.5) * Math.PI * 50, 0)
          },
          "cx": "50",
          "cy": "50",
          "r": "50"
        }), 2, {
          "viewBox": "0 0 100 100"
        }), inputElement], 0, Object.assign({}, (0, _Box.computeBoxProps)(Object.assign({
          style: Object.assign({
            'font-size': size + 'em'
          }, style)
        }, rest)), {
          "onMouseDown": handleDragStart
        })));
      }

      return children;
    }()
  })));
};

exports.Knob = Knob;

/***/ }),

/***/ 95592:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.LabeledControls = void 0;

var _inferno = __webpack_require__(96323);

var _Flex = __webpack_require__(67486);

var _excluded = ["children", "wrap"],
    _excluded2 = ["label", "children", "mx"];

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

var LabeledControls = function LabeledControls(props) {
  var children = props.children,
      wrap = props.wrap,
      rest = _objectWithoutPropertiesLoose(props, _excluded);

  return (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, _Flex.Flex, Object.assign({
    "mx": -0.5,
    "wrap": wrap,
    "align": "stretch",
    "justify": "space-between"
  }, rest, {
    children: children
  })));
};

exports.LabeledControls = LabeledControls;

var LabeledControlsItem = function LabeledControlsItem(props) {
  var label = props.label,
      children = props.children,
      _props$mx = props.mx,
      mx = _props$mx === void 0 ? 1 : _props$mx,
      rest = _objectWithoutPropertiesLoose(props, _excluded2);

  return (0, _inferno.createComponentVNode)(2, _Flex.Flex.Item, {
    "mx": mx,
    children: (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, _Flex.Flex, Object.assign({
      "height": "100%",
      "direction": "column",
      "align": "center",
      "textAlign": "center",
      "justify": "space-between"
    }, rest, {
      children: [(0, _inferno.createComponentVNode)(2, _Flex.Flex.Item), (0, _inferno.createComponentVNode)(2, _Flex.Flex.Item, {
        children: children
      }), (0, _inferno.createComponentVNode)(2, _Flex.Flex.Item, {
        "color": "label",
        children: label
      })]
    })))
  });
};

LabeledControls.Item = LabeledControlsItem;

/***/ }),

/***/ 21173:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.LabeledList = void 0;

var _inferno = __webpack_require__(96323);

var _react = __webpack_require__(1060);

var _Box = __webpack_require__(54425);

var _Divider = __webpack_require__(22755);

/**
 * @file
 * @copyright 2020 Aleksej Komarov
 * @license MIT
 */
var LabeledList = function LabeledList(props) {
  var children = props.children;
  return (0, _inferno.createVNode)(1, "table", "LabeledList", children, 0);
};

exports.LabeledList = LabeledList;
LabeledList.defaultHooks = _react.pureComponentHooks;

var LabeledListItem = function LabeledListItem(props) {
  var className = props.className,
      label = props.label,
      _props$labelColor = props.labelColor,
      labelColor = _props$labelColor === void 0 ? 'label' : _props$labelColor,
      color = props.color,
      textAlign = props.textAlign,
      buttons = props.buttons,
      content = props.content,
      children = props.children;
  return (0, _inferno.createVNode)(1, "tr", (0, _react.classes)(['LabeledList__row', className]), [(0, _inferno.createComponentVNode)(2, _Box.Box, {
    "as": "td",
    "color": labelColor,
    "className": (0, _react.classes)(['LabeledList__cell', 'LabeledList__label']),
    children: label ? label + ':' : null
  }), (0, _inferno.createComponentVNode)(2, _Box.Box, {
    "as": "td",
    "color": color,
    "textAlign": textAlign,
    "className": (0, _react.classes)(['LabeledList__cell', 'LabeledList__content']),
    "colSpan": buttons ? undefined : 2,
    children: [content, children]
  }), buttons && (0, _inferno.createVNode)(1, "td", "LabeledList__cell LabeledList__buttons", buttons, 0)], 0);
};

LabeledListItem.defaultHooks = _react.pureComponentHooks;

var LabeledListDivider = function LabeledListDivider(props) {
  var padding = props.size ? (0, _Box.unit)(Math.max(0, props.size - 1)) : 0;
  return (0, _inferno.createVNode)(1, "tr", "LabeledList__row", (0, _inferno.createVNode)(1, "td", null, (0, _inferno.createComponentVNode)(2, _Divider.Divider), 2, {
    "colSpan": 3,
    "style": {
      'padding-top': padding,
      'padding-bottom': padding
    }
  }), 2);
};

LabeledListDivider.defaultHooks = _react.pureComponentHooks;
LabeledList.Item = LabeledListItem;
LabeledList.Divider = LabeledListDivider;

/***/ }),

/***/ 89416:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Modal = void 0;

var _inferno = __webpack_require__(96323);

var _react = __webpack_require__(1060);

var _Box = __webpack_require__(54425);

var _Dimmer = __webpack_require__(93913);

var _excluded = ["className", "children"];

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

var Modal = function Modal(props) {
  var className = props.className,
      children = props.children,
      rest = _objectWithoutPropertiesLoose(props, _excluded);

  return (0, _inferno.createComponentVNode)(2, _Dimmer.Dimmer, {
    children: (0, _inferno.normalizeProps)((0, _inferno.createVNode)(1, "div", (0, _react.classes)(['Modal', className, (0, _Box.computeBoxClassName)(rest)]), children, 0, Object.assign({}, (0, _Box.computeBoxProps)(rest))))
  });
};

exports.Modal = Modal;

/***/ }),

/***/ 23257:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.NoticeBox = void 0;

var _inferno = __webpack_require__(96323);

var _react = __webpack_require__(1060);

var _Box = __webpack_require__(54425);

var _excluded = ["className", "color", "info", "warning", "success", "danger"];

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

var NoticeBox = function NoticeBox(props) {
  var className = props.className,
      color = props.color,
      info = props.info,
      warning = props.warning,
      success = props.success,
      danger = props.danger,
      rest = _objectWithoutPropertiesLoose(props, _excluded);

  return (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, _Box.Box, Object.assign({
    "className": (0, _react.classes)(['NoticeBox', color && 'NoticeBox--color--' + color, info && 'NoticeBox--type--info', success && 'NoticeBox--type--success', danger && 'NoticeBox--type--danger', className])
  }, rest)));
};

exports.NoticeBox = NoticeBox;
NoticeBox.defaultHooks = _react.pureComponentHooks;

/***/ }),

/***/ 97667:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.NumberInput = void 0;

var _inferno = __webpack_require__(96323);

var _math = __webpack_require__(97622);

var _react = __webpack_require__(1060);

var _AnimatedNumber = __webpack_require__(13251);

var _Box = __webpack_require__(54425);

function _inheritsLoose(subClass, superClass) { subClass.prototype = Object.create(superClass.prototype); subClass.prototype.constructor = subClass; _setPrototypeOf(subClass, superClass); }

function _setPrototypeOf(o, p) { _setPrototypeOf = Object.setPrototypeOf || function () { function _setPrototypeOf(o, p) { o.__proto__ = p; return o; } return _setPrototypeOf; }(); return _setPrototypeOf(o, p); }

var DEFAULT_UPDATE_RATE = 400;

var NumberInput = /*#__PURE__*/function (_Component) {
  _inheritsLoose(NumberInput, _Component);

  function NumberInput(props) {
    var _this;

    _this = _Component.call(this, props) || this;
    var value = props.value;
    _this.inputRef = (0, _inferno.createRef)();
    _this.state = {
      value: value,
      dragging: false,
      editing: false,
      internalValue: null,
      origin: null,
      suppressingFlicker: false
    }; // Suppresses flickering while the value propagates through the backend

    _this.flickerTimer = null;

    _this.suppressFlicker = function () {
      var suppressFlicker = _this.props.suppressFlicker;

      if (suppressFlicker > 0) {
        _this.setState({
          suppressingFlicker: true
        });

        clearTimeout(_this.flickerTimer);
        _this.flickerTimer = setTimeout(function () {
          return _this.setState({
            suppressingFlicker: false
          });
        }, suppressFlicker);
      }
    };

    _this.handleDragStart = function (e) {
      var value = _this.props.value;
      var editing = _this.state.editing;

      if (editing) {
        return;
      }

      document.body.style['pointer-events'] = 'none';
      _this.ref = e.target;

      _this.setState({
        dragging: false,
        origin: e.screenY,
        value: value,
        internalValue: value
      });

      _this.timer = setTimeout(function () {
        _this.setState({
          dragging: true
        });
      }, 250);
      _this.dragInterval = setInterval(function () {
        var _this$state = _this.state,
            dragging = _this$state.dragging,
            value = _this$state.value;
        var onDrag = _this.props.onDrag;

        if (dragging && onDrag) {
          onDrag(e, value);
        }
      }, _this.props.updateRate || DEFAULT_UPDATE_RATE);
      document.addEventListener('mousemove', _this.handleDragMove);
      document.addEventListener('mouseup', _this.handleDragEnd);
    };

    _this.handleDragMove = function (e) {
      var _this$props = _this.props,
          minValue = _this$props.minValue,
          maxValue = _this$props.maxValue,
          step = _this$props.step,
          stepPixelSize = _this$props.stepPixelSize;

      _this.setState(function (prevState) {
        var state = Object.assign({}, prevState);
        var offset = state.origin - e.screenY;

        if (prevState.dragging) {
          var stepOffset = Number.isFinite(minValue) ? minValue % step : 0; // Translate mouse movement to value
          // Give it some headroom (by increasing clamp range by 1 step)

          state.internalValue = (0, _math.clamp)(state.internalValue + offset * step / stepPixelSize, minValue - step, maxValue + step); // Clamp the final value

          state.value = (0, _math.clamp)(state.internalValue - state.internalValue % step + stepOffset, minValue, maxValue);
          state.origin = e.screenY;
        } else if (Math.abs(offset) > 4) {
          state.dragging = true;
        }

        return state;
      });
    };

    _this.handleDragEnd = function (e) {
      var _this$props2 = _this.props,
          onChange = _this$props2.onChange,
          onDrag = _this$props2.onDrag;
      var _this$state2 = _this.state,
          dragging = _this$state2.dragging,
          value = _this$state2.value,
          internalValue = _this$state2.internalValue;
      document.body.style['pointer-events'] = 'auto';
      clearTimeout(_this.timer);
      clearInterval(_this.dragInterval);

      _this.setState({
        dragging: false,
        editing: !dragging,
        origin: null
      });

      document.removeEventListener('mousemove', _this.handleDragMove);
      document.removeEventListener('mouseup', _this.handleDragEnd);

      if (dragging) {
        _this.suppressFlicker();

        if (onChange) {
          onChange(e, value);
        }

        if (onDrag) {
          onDrag(e, value);
        }
      } else if (_this.inputRef) {
        var input = _this.inputRef.current;
        input.value = internalValue; // IE8: Dies when trying to focus a hidden element
        // (Error: Object does not support this action)

        try {
          input.focus();
          input.select();
        } catch (_unused) {}
      }
    };

    return _this;
  }

  var _proto = NumberInput.prototype;

  _proto.render = function () {
    function render() {
      var _this2 = this;

      var _this$state3 = this.state,
          dragging = _this$state3.dragging,
          editing = _this$state3.editing,
          intermediateValue = _this$state3.value,
          suppressingFlicker = _this$state3.suppressingFlicker;
      var _this$props3 = this.props,
          className = _this$props3.className,
          fluid = _this$props3.fluid,
          animated = _this$props3.animated,
          value = _this$props3.value,
          unit = _this$props3.unit,
          minValue = _this$props3.minValue,
          maxValue = _this$props3.maxValue,
          height = _this$props3.height,
          width = _this$props3.width,
          lineHeight = _this$props3.lineHeight,
          fontSize = _this$props3.fontSize,
          format = _this$props3.format,
          onChange = _this$props3.onChange,
          onDrag = _this$props3.onDrag;
      var displayValue = value;

      if (dragging || suppressingFlicker) {
        displayValue = intermediateValue;
      } // IE8: Use an "unselectable" prop because "user-select" doesn't work.


      var renderContentElement = function () {
        function renderContentElement(value) {
          return (0, _inferno.createVNode)(1, "div", "NumberInput__content", value + (unit ? ' ' + unit : ''), 0, {
            "unselectable": Byond.IS_LTE_IE8
          });
        }

        return renderContentElement;
      }();

      var contentElement = animated && !dragging && !suppressingFlicker && (0, _inferno.createComponentVNode)(2, _AnimatedNumber.AnimatedNumber, {
        "value": displayValue,
        "format": format,
        children: renderContentElement
      }) || renderContentElement(format ? format(displayValue) : displayValue);
      return (0, _inferno.createComponentVNode)(2, _Box.Box, {
        "className": (0, _react.classes)(['NumberInput', fluid && 'NumberInput--fluid', className]),
        "minWidth": width,
        "minHeight": height,
        "lineHeight": lineHeight,
        "fontSize": fontSize,
        "onMouseDown": this.handleDragStart,
        children: [(0, _inferno.createVNode)(1, "div", "NumberInput__barContainer", (0, _inferno.createVNode)(1, "div", "NumberInput__bar", null, 1, {
          "style": {
            height: (0, _math.clamp)((displayValue - minValue) / (maxValue - minValue) * 100, 0, 100) + '%'
          }
        }), 2), contentElement, (0, _inferno.createVNode)(64, "input", "NumberInput__input", null, 1, {
          "style": {
            display: !editing ? 'none' : undefined,
            height: height,
            'line-height': lineHeight,
            'font-size': fontSize
          },
          "onBlur": function () {
            function onBlur(e) {
              if (!editing) {
                return;
              }

              var value = (0, _math.clamp)(parseFloat(e.target.value), minValue, maxValue);

              if (Number.isNaN(value)) {
                _this2.setState({
                  editing: false
                });

                return;
              }

              _this2.setState({
                editing: false,
                value: value
              });

              _this2.suppressFlicker();

              if (onChange) {
                onChange(e, value);
              }

              if (onDrag) {
                onDrag(e, value);
              }
            }

            return onBlur;
          }(),
          "onKeyDown": function () {
            function onKeyDown(e) {
              if (e.keyCode === 13) {
                var _value = (0, _math.clamp)(parseFloat(e.target.value), minValue, maxValue);

                if (Number.isNaN(_value)) {
                  _this2.setState({
                    editing: false
                  });

                  return;
                }

                _this2.setState({
                  editing: false,
                  value: _value
                });

                _this2.suppressFlicker();

                if (onChange) {
                  onChange(e, _value);
                }

                if (onDrag) {
                  onDrag(e, _value);
                }

                return;
              }

              if (e.keyCode === 27) {
                _this2.setState({
                  editing: false
                });

                return;
              }
            }

            return onKeyDown;
          }()
        }, null, this.inputRef)]
      });
    }

    return render;
  }();

  return NumberInput;
}(_inferno.Component);

exports.NumberInput = NumberInput;
NumberInput.defaultHooks = _react.pureComponentHooks;
NumberInput.defaultProps = {
  minValue: -Infinity,
  maxValue: +Infinity,
  step: 1,
  stepPixelSize: 1,
  suppressFlicker: 50
};

/***/ }),

/***/ 16513:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Popper = void 0;

var _core = __webpack_require__(34739);

var _inferno = __webpack_require__(96323);

function _inheritsLoose(subClass, superClass) { subClass.prototype = Object.create(superClass.prototype); subClass.prototype.constructor = subClass; _setPrototypeOf(subClass, superClass); }

function _setPrototypeOf(o, p) { _setPrototypeOf = Object.setPrototypeOf || function () { function _setPrototypeOf(o, p) { o.__proto__ = p; return o; } return _setPrototypeOf; }(); return _setPrototypeOf(o, p); }

var Popper = /*#__PURE__*/function (_Component) {
  _inheritsLoose(Popper, _Component);

  function Popper() {
    var _this;

    _this = _Component.call(this) || this;
    _this.renderedContent = void 0;
    _this.popperInstance = void 0;
    Popper.id += 1;
    return _this;
  }

  var _proto = Popper.prototype;

  _proto.componentDidMount = function () {
    function componentDidMount() {
      var _this2 = this;

      var _this$props = this.props,
          additionalStyles = _this$props.additionalStyles,
          options = _this$props.options;
      this.renderedContent = document.createElement("div");

      if (additionalStyles) {
        for (var _i = 0, _Object$entries = Object.entries(additionalStyles); _i < _Object$entries.length; _i++) {
          var _Object$entries$_i = _Object$entries[_i],
              attribute = _Object$entries$_i[0],
              value = _Object$entries$_i[1];
          this.renderedContent.style[attribute] = value;
        }
      }

      this.renderPopperContent(function () {
        document.body.appendChild(_this2.renderedContent);
        _this2.popperInstance = (0, _core.createPopper)( // HACK: We don't want to create a wrapper, as it could break the layout
        // of consumers, so we do the inferno equivalent of `findDOMNode(this)`.
        // This is usually bad as refs are usually better, but refs did
        // not work in this case, as they weren't propagating correctly.
        // A previous attempt was made as a render prop that passed an ID,
        // but this made consuming use too unwieldly.
        // This code is copied from `findDOMNode` in inferno-extras.
        // Because this component is written in TypeScript, we will know
        // immediately if this internal variable is removed.
        (0, _inferno.findDOMfromVNode)(_this2.$LI, true), _this2.renderedContent, options);
      });
    }

    return componentDidMount;
  }();

  _proto.componentDidUpdate = function () {
    function componentDidUpdate() {
      var _this3 = this;

      this.renderPopperContent(function () {
        var _this3$popperInstance;

        return (_this3$popperInstance = _this3.popperInstance) == null ? void 0 : _this3$popperInstance.update();
      });
    }

    return componentDidUpdate;
  }();

  _proto.componentWillUnmount = function () {
    function componentWillUnmount() {
      var _this$popperInstance;

      (_this$popperInstance = this.popperInstance) == null ? void 0 : _this$popperInstance.destroy();
      this.renderedContent.remove();
      this.renderedContent = null;
    }

    return componentWillUnmount;
  }();

  _proto.renderPopperContent = function () {
    function renderPopperContent(callback) {
      (0, _inferno.render)(this.props.popperContent, this.renderedContent, callback);
    }

    return renderPopperContent;
  }();

  _proto.render = function () {
    function render() {
      return this.props.children;
    }

    return render;
  }();

  return Popper;
}(_inferno.Component);

exports.Popper = Popper;
Popper.id = 0;

/***/ }),

/***/ 12194:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.ProgressBar = void 0;

var _inferno = __webpack_require__(96323);

var _math = __webpack_require__(97622);

var _react = __webpack_require__(1060);

var _Box = __webpack_require__(54425);

var _excluded = ["className", "value", "minValue", "maxValue", "color", "ranges", "children"];

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

var ProgressBar = function ProgressBar(props) {
  var className = props.className,
      value = props.value,
      _props$minValue = props.minValue,
      minValue = _props$minValue === void 0 ? 0 : _props$minValue,
      _props$maxValue = props.maxValue,
      maxValue = _props$maxValue === void 0 ? 1 : _props$maxValue,
      color = props.color,
      _props$ranges = props.ranges,
      ranges = _props$ranges === void 0 ? {} : _props$ranges,
      children = props.children,
      rest = _objectWithoutPropertiesLoose(props, _excluded);

  var scaledValue = (0, _math.scale)(value, minValue, maxValue);
  var hasContent = children !== undefined;
  var effectiveColor = color || (0, _math.keyOfMatchingRange)(value, ranges) || 'default';
  return (0, _inferno.normalizeProps)((0, _inferno.createVNode)(1, "div", (0, _react.classes)(['ProgressBar', 'ProgressBar--color--' + effectiveColor, className, (0, _Box.computeBoxClassName)(rest)]), [(0, _inferno.createVNode)(1, "div", "ProgressBar__fill ProgressBar__fill--animated", null, 1, {
    "style": {
      width: (0, _math.clamp01)(scaledValue) * 100 + '%'
    }
  }), (0, _inferno.createVNode)(1, "div", "ProgressBar__content", hasContent ? children : (0, _math.toFixed)(scaledValue * 100) + '%', 0)], 4, Object.assign({}, (0, _Box.computeBoxProps)(rest))));
};

exports.ProgressBar = ProgressBar;
ProgressBar.defaultHooks = _react.pureComponentHooks;

/***/ }),

/***/ 94709:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.RoundGauge = void 0;

var _inferno = __webpack_require__(96323);

var _math = __webpack_require__(97622);

var _react = __webpack_require__(1060);

var _AnimatedNumber = __webpack_require__(13251);

var _Box = __webpack_require__(54425);

var _excluded = ["value", "minValue", "maxValue", "ranges", "alertAfter", "format", "size", "className", "style"];

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

var RoundGauge = function RoundGauge(props) {
  // Support for IE8 is for losers sorry B)
  if (Byond.IS_LTE_IE8) {
    return (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, _AnimatedNumber.AnimatedNumber, Object.assign({}, props)));
  }

  var value = props.value,
      _props$minValue = props.minValue,
      minValue = _props$minValue === void 0 ? 1 : _props$minValue,
      _props$maxValue = props.maxValue,
      maxValue = _props$maxValue === void 0 ? 1 : _props$maxValue,
      ranges = props.ranges,
      alertAfter = props.alertAfter,
      format = props.format,
      _props$size = props.size,
      size = _props$size === void 0 ? 1 : _props$size,
      className = props.className,
      style = props.style,
      rest = _objectWithoutPropertiesLoose(props, _excluded);

  var scaledValue = (0, _math.scale)(value, minValue, maxValue);
  var clampedValue = (0, _math.clamp01)(scaledValue);
  var scaledRanges = ranges ? {} : {
    "primary": [0, 1]
  };

  if (ranges) {
    Object.keys(ranges).forEach(function (x) {
      var range = ranges[x];
      scaledRanges[x] = [(0, _math.scale)(range[0], minValue, maxValue), (0, _math.scale)(range[1], minValue, maxValue)];
    });
  }

  var alertColor = null;

  if (alertAfter < value) {
    alertColor = (0, _math.keyOfMatchingRange)(clampedValue, scaledRanges);
  }

  return (0, _inferno.createComponentVNode)(2, _Box.Box, {
    "inline": true,
    children: [(0, _inferno.normalizeProps)((0, _inferno.createVNode)(1, "div", (0, _react.classes)(['RoundGauge', className, (0, _Box.computeBoxClassName)(rest)]), (0, _inferno.createVNode)(32, "svg", null, [alertAfter && (0, _inferno.createVNode)(32, "g", (0, _react.classes)(['RoundGauge__alert', alertColor ? "active RoundGauge__alert--" + alertColor : '']), (0, _inferno.createVNode)(32, "path", null, null, 1, {
      "d": "M48.211,14.578C48.55,13.9 49.242,13.472 50,13.472C50.758,13.472 51.45,13.9 51.789,14.578C54.793,20.587 60.795,32.589 63.553,38.106C63.863,38.726 63.83,39.462 63.465,40.051C63.101,40.641 62.457,41 61.764,41C55.996,41 44.004,41 38.236,41C37.543,41 36.899,40.641 36.535,40.051C36.17,39.462 36.137,38.726 36.447,38.106C39.205,32.589 45.207,20.587 48.211,14.578ZM50,34.417C51.426,34.417 52.583,35.574 52.583,37C52.583,38.426 51.426,39.583 50,39.583C48.574,39.583 47.417,38.426 47.417,37C47.417,35.574 48.574,34.417 50,34.417ZM50,32.75C50,32.75 53,31.805 53,22.25C53,20.594 51.656,19.25 50,19.25C48.344,19.25 47,20.594 47,22.25C47,31.805 50,32.75 50,32.75Z"
    }), 2), (0, _inferno.createVNode)(32, "g", null, (0, _inferno.createVNode)(32, "circle", "RoundGauge__ringTrack", null, 1, {
      "cx": "50",
      "cy": "50",
      "r": "45"
    }), 2), (0, _inferno.createVNode)(32, "g", null, Object.keys(scaledRanges).map(function (x, i) {
      var col_ranges = scaledRanges[x];
      return (0, _inferno.createVNode)(32, "circle", "RoundGauge__ringFill RoundGauge--color--" + x, null, 1, {
        "style": {
          'stroke-dashoffset': Math.max((2.0 - (col_ranges[1] - col_ranges[0])) * Math.PI * 50, 0)
        },
        "transform": "rotate(" + (180 + 180 * col_ranges[0]) + " 50 50)",
        "cx": "50",
        "cy": "50",
        "r": "45"
      }, i);
    }), 0), (0, _inferno.createVNode)(32, "g", "RoundGauge__needle", [(0, _inferno.createVNode)(32, "polygon", "RoundGauge__needleLine", null, 1, {
      "points": "46,50 50,0 54,50"
    }), (0, _inferno.createVNode)(32, "circle", "RoundGauge__needleMiddle", null, 1, {
      "cx": "50",
      "cy": "50",
      "r": "8"
    })], 4, {
      "transform": "rotate(" + (clampedValue * 180 - 90) + " 50 50)"
    })], 0, {
      "viewBox": "0 0 100 50"
    }), 2, Object.assign({}, (0, _Box.computeBoxProps)(Object.assign({
      style: Object.assign({
        'font-size': size + 'em'
      }, style)
    }, rest))))), (0, _inferno.createComponentVNode)(2, _AnimatedNumber.AnimatedNumber, {
      "value": value,
      "format": format,
      "size": size
    })]
  });
};

exports.RoundGauge = RoundGauge;

/***/ }),

/***/ 77381:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Section = void 0;

var _inferno = __webpack_require__(96323);

var _react = __webpack_require__(1060);

var _events = __webpack_require__(99397);

var _Box = __webpack_require__(54425);

var _excluded = ["className", "title", "buttons", "fill", "fitted", "scrollable", "children"];

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

function _inheritsLoose(subClass, superClass) { subClass.prototype = Object.create(superClass.prototype); subClass.prototype.constructor = subClass; _setPrototypeOf(subClass, superClass); }

function _setPrototypeOf(o, p) { _setPrototypeOf = Object.setPrototypeOf || function () { function _setPrototypeOf(o, p) { o.__proto__ = p; return o; } return _setPrototypeOf; }(); return _setPrototypeOf(o, p); }

var Section = /*#__PURE__*/function (_Component) {
  _inheritsLoose(Section, _Component);

  function Section(props) {
    var _this;

    _this = _Component.call(this, props) || this;
    _this.scrollableRef = void 0;
    _this.scrollable = void 0;
    _this.scrollableRef = (0, _inferno.createRef)();
    _this.scrollable = props.scrollable;
    return _this;
  }

  var _proto = Section.prototype;

  _proto.componentDidMount = function () {
    function componentDidMount() {
      if (this.scrollable) {
        (0, _events.addScrollableNode)(this.scrollableRef.current);
      }
    }

    return componentDidMount;
  }();

  _proto.componentWillUnmount = function () {
    function componentWillUnmount() {
      if (this.scrollable) {
        (0, _events.removeScrollableNode)(this.scrollableRef.current);
      }
    }

    return componentWillUnmount;
  }();

  _proto.render = function () {
    function render() {
      var _this$props = this.props,
          className = _this$props.className,
          title = _this$props.title,
          buttons = _this$props.buttons,
          fill = _this$props.fill,
          fitted = _this$props.fitted,
          scrollable = _this$props.scrollable,
          children = _this$props.children,
          rest = _objectWithoutPropertiesLoose(_this$props, _excluded);

      var hasTitle = (0, _react.canRender)(title) || (0, _react.canRender)(buttons);
      return (0, _inferno.normalizeProps)((0, _inferno.createVNode)(1, "div", (0, _react.classes)(['Section', Byond.IS_LTE_IE8 && 'Section--iefix', fill && 'Section--fill', fitted && 'Section--fitted', scrollable && 'Section--scrollable', className, (0, _Box.computeBoxClassName)(rest)]), [hasTitle && (0, _inferno.createVNode)(1, "div", "Section__title", [(0, _inferno.createVNode)(1, "span", "Section__titleText", title, 0), (0, _inferno.createVNode)(1, "div", "Section__buttons", buttons, 0)], 4), (0, _inferno.createVNode)(1, "div", "Section__rest", (0, _inferno.createVNode)(1, "div", "Section__content", children, 0, null, null, this.scrollableRef), 2)], 0, Object.assign({}, (0, _Box.computeBoxProps)(rest))));
    }

    return render;
  }();

  return Section;
}(_inferno.Component);

exports.Section = Section;

/***/ }),

/***/ 97968:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.SegmentDisplay = void 0;

var _inferno = __webpack_require__(96323);

var _Box = __webpack_require__(54425);

var _excluded = ["display_text", "display_cells_amount", "display_height"];

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

var SegmentDisplay = function SegmentDisplay(props) {
  var display_text = props.display_text,
      display_cells_amount = props.display_cells_amount,
      display_height = props.display_height,
      rest = _objectWithoutPropertiesLoose(props, _excluded);

  var segment_height = display_height;
  var segment_width = display_height * 0.641;
  var segments_count = display_cells_amount;
  var segments_width = segments_count * segment_width;
  var segments = [];

  for (var i = 0; i < segments_count; i++) {
    segments.push("#");
  }

  return (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, _Box.Box, Object.assign({
    "width": segments_width + "px",
    "height": display_height + "px",
    "overflow": "hidden",
    "position": "relative"
  }, rest, {
    children: [(0, _inferno.createComponentVNode)(2, _Box.Box, {
      "width": segments_width + "px",
      "height": segment_height + "px",
      "position": "absolute",
      "top": "0px",
      "textColor": "#261f16",
      "fontSize": display_height + "px",
      "textAlign": "right",
      "fontFamily": "Gys14Segment",
      "bold": 0,
      children: [" ", segments, " "]
    }), (0, _inferno.createComponentVNode)(2, _Box.Box, {
      "width": segments_width + "px",
      "height": segment_height + "px",
      "position": "absolute",
      "top": "0px",
      "textColor": "#D0330f",
      "fontSize": display_height + "px",
      "textAlign": "right",
      "fontFamily": "Gys14Segment",
      "bold": 0,
      children: [" ", display_text, " "]
    })]
  })));
};

exports.SegmentDisplay = SegmentDisplay;

/***/ }),

/***/ 4482:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Slider = void 0;

var _inferno = __webpack_require__(96323);

var _math = __webpack_require__(97622);

var _react = __webpack_require__(1060);

var _Box = __webpack_require__(54425);

var _DraggableControl = __webpack_require__(42833);

var _NumberInput = __webpack_require__(97667);

var _excluded = ["animated", "format", "maxValue", "minValue", "onChange", "onDrag", "step", "stepPixelSize", "suppressFlicker", "unit", "value", "className", "fillValue", "color", "ranges", "children"];

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

var Slider = function Slider(props) {
  // IE8: I don't want to support a yet another component on IE8.
  if (Byond.IS_LTE_IE8) {
    return (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, _NumberInput.NumberInput, Object.assign({}, props)));
  }

  var animated = props.animated,
      format = props.format,
      maxValue = props.maxValue,
      minValue = props.minValue,
      onChange = props.onChange,
      onDrag = props.onDrag,
      step = props.step,
      stepPixelSize = props.stepPixelSize,
      suppressFlicker = props.suppressFlicker,
      unit = props.unit,
      value = props.value,
      className = props.className,
      fillValue = props.fillValue,
      color = props.color,
      _props$ranges = props.ranges,
      ranges = _props$ranges === void 0 ? {} : _props$ranges,
      _children = props.children,
      rest = _objectWithoutPropertiesLoose(props, _excluded);

  var hasContent = _children !== undefined;
  return (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, _DraggableControl.DraggableControl, Object.assign({
    "dragMatrix": [1, 0]
  }, {
    animated: animated,
    format: format,
    maxValue: maxValue,
    minValue: minValue,
    onChange: onChange,
    onDrag: onDrag,
    step: step,
    stepPixelSize: stepPixelSize,
    suppressFlicker: suppressFlicker,
    unit: unit,
    value: value
  }, {
    children: function () {
      function children(control) {
        var dragging = control.dragging,
            editing = control.editing,
            value = control.value,
            displayValue = control.displayValue,
            displayElement = control.displayElement,
            inputElement = control.inputElement,
            handleDragStart = control.handleDragStart;
        var hasFillValue = fillValue !== undefined && fillValue !== null;
        var scaledValue = (0, _math.scale)(value, minValue, maxValue);
        var scaledFillValue = (0, _math.scale)(fillValue != null ? fillValue : displayValue, minValue, maxValue);
        var scaledDisplayValue = (0, _math.scale)(displayValue, minValue, maxValue);
        var effectiveColor = color || (0, _math.keyOfMatchingRange)(fillValue != null ? fillValue : value, ranges) || 'default';
        return (0, _inferno.normalizeProps)((0, _inferno.createVNode)(1, "div", (0, _react.classes)(['Slider', 'ProgressBar', 'ProgressBar--color--' + effectiveColor, className, (0, _Box.computeBoxClassName)(rest)]), [(0, _inferno.createVNode)(1, "div", (0, _react.classes)(['ProgressBar__fill', hasFillValue && 'ProgressBar__fill--animated']), null, 1, {
          "style": {
            width: (0, _math.clamp01)(scaledFillValue) * 100 + '%',
            opacity: 0.4
          }
        }), (0, _inferno.createVNode)(1, "div", "ProgressBar__fill", null, 1, {
          "style": {
            width: (0, _math.clamp01)(Math.min(scaledFillValue, scaledDisplayValue)) * 100 + '%'
          }
        }), (0, _inferno.createVNode)(1, "div", "Slider__cursorOffset", [(0, _inferno.createVNode)(1, "div", "Slider__cursor"), (0, _inferno.createVNode)(1, "div", "Slider__pointer"), dragging && (0, _inferno.createVNode)(1, "div", "Slider__popupValue", displayElement, 0)], 0, {
          "style": {
            width: (0, _math.clamp01)(scaledDisplayValue) * 100 + '%'
          }
        }), (0, _inferno.createVNode)(1, "div", "ProgressBar__content", hasContent ? _children : displayElement, 0), inputElement], 0, Object.assign({}, (0, _Box.computeBoxProps)(rest), {
          "onMouseDown": handleDragStart
        })));
      }

      return children;
    }()
  })));
};

exports.Slider = Slider;

/***/ }),

/***/ 4033:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Stack = void 0;

var _inferno = __webpack_require__(96323);

var _react = __webpack_require__(1060);

var _Flex = __webpack_require__(67486);

var _excluded = ["className", "vertical", "fill"],
    _excluded2 = ["className"],
    _excluded3 = ["className", "hidden"];

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

var Stack = function Stack(props) {
  var className = props.className,
      vertical = props.vertical,
      fill = props.fill,
      rest = _objectWithoutPropertiesLoose(props, _excluded);

  return (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, _Flex.Flex, Object.assign({
    "className": (0, _react.classes)(['Stack', fill && 'Stack--fill', vertical ? 'Stack--vertical' : 'Stack--horizontal', className]),
    "direction": vertical ? 'column' : 'row'
  }, rest)));
};

exports.Stack = Stack;

var StackItem = function StackItem(props) {
  var className = props.className,
      rest = _objectWithoutPropertiesLoose(props, _excluded2);

  return (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, _Flex.Flex.Item, Object.assign({
    "className": (0, _react.classes)(['Stack__item', className])
  }, rest)));
};

Stack.Item = StackItem;

var StackDivider = function StackDivider(props) {
  var className = props.className,
      hidden = props.hidden,
      rest = _objectWithoutPropertiesLoose(props, _excluded3);

  return (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, _Flex.Flex.Item, Object.assign({
    "className": (0, _react.classes)(['Stack__item', 'Stack__divider', hidden && 'Stack__divider--hidden', className])
  }, rest)));
};

Stack.Divider = StackDivider;

/***/ }),

/***/ 28581:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.TableCell = exports.TableRow = exports.Table = void 0;

var _inferno = __webpack_require__(96323);

var _react = __webpack_require__(1060);

var _Box = __webpack_require__(54425);

var _excluded = ["className", "collapsing", "children"],
    _excluded2 = ["className", "header"],
    _excluded3 = ["className", "collapsing", "header"];

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

var Table = function Table(props) {
  var className = props.className,
      collapsing = props.collapsing,
      children = props.children,
      rest = _objectWithoutPropertiesLoose(props, _excluded);

  return (0, _inferno.normalizeProps)((0, _inferno.createVNode)(1, "table", (0, _react.classes)(['Table', collapsing && 'Table--collapsing', className, (0, _Box.computeBoxClassName)(rest)]), (0, _inferno.createVNode)(1, "tbody", null, children, 0), 2, Object.assign({}, (0, _Box.computeBoxProps)(rest))));
};

exports.Table = Table;
Table.defaultHooks = _react.pureComponentHooks;

var TableRow = function TableRow(props) {
  var className = props.className,
      header = props.header,
      rest = _objectWithoutPropertiesLoose(props, _excluded2);

  return (0, _inferno.normalizeProps)((0, _inferno.createVNode)(1, "tr", (0, _react.classes)(['Table__row', header && 'Table__row--header', className, (0, _Box.computeBoxClassName)(props)]), null, 1, Object.assign({}, (0, _Box.computeBoxProps)(rest))));
};

exports.TableRow = TableRow;
TableRow.defaultHooks = _react.pureComponentHooks;

var TableCell = function TableCell(props) {
  var className = props.className,
      collapsing = props.collapsing,
      header = props.header,
      rest = _objectWithoutPropertiesLoose(props, _excluded3);

  return (0, _inferno.normalizeProps)((0, _inferno.createVNode)(1, "td", (0, _react.classes)(['Table__cell', collapsing && 'Table__cell--collapsing', header && 'Table__cell--header', className, (0, _Box.computeBoxClassName)(props)]), null, 1, Object.assign({}, (0, _Box.computeBoxProps)(rest))));
};

exports.TableCell = TableCell;
TableCell.defaultHooks = _react.pureComponentHooks;
Table.Row = TableRow;
Table.Cell = TableCell;

/***/ }),

/***/ 60839:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Tabs = void 0;

var _inferno = __webpack_require__(96323);

var _react = __webpack_require__(1060);

var _Box = __webpack_require__(54425);

var _Icon = __webpack_require__(65743);

var _excluded = ["className", "vertical", "fill", "fluid", "children"],
    _excluded2 = ["className", "selected", "color", "icon", "leftSlot", "rightSlot", "children"];

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

var Tabs = function Tabs(props) {
  var className = props.className,
      vertical = props.vertical,
      fill = props.fill,
      fluid = props.fluid,
      children = props.children,
      rest = _objectWithoutPropertiesLoose(props, _excluded);

  return (0, _inferno.normalizeProps)((0, _inferno.createVNode)(1, "div", (0, _react.classes)(['Tabs', vertical ? 'Tabs--vertical' : 'Tabs--horizontal', fill && 'Tabs--fill', fluid && 'Tabs--fluid', className, (0, _Box.computeBoxClassName)(rest)]), children, 0, Object.assign({}, (0, _Box.computeBoxProps)(rest))));
};

exports.Tabs = Tabs;

var Tab = function Tab(props) {
  var className = props.className,
      selected = props.selected,
      color = props.color,
      icon = props.icon,
      leftSlot = props.leftSlot,
      rightSlot = props.rightSlot,
      children = props.children,
      rest = _objectWithoutPropertiesLoose(props, _excluded2);

  return (0, _inferno.normalizeProps)((0, _inferno.createVNode)(1, "div", (0, _react.classes)(['Tab', 'Tabs__Tab', 'Tab--color--' + color, selected && 'Tab--selected', className].concat((0, _Box.computeBoxClassName)(rest))), [(0, _react.canRender)(leftSlot) && (0, _inferno.createVNode)(1, "div", "Tab__left", leftSlot, 0) || !!icon && (0, _inferno.createVNode)(1, "div", "Tab__left", (0, _inferno.createComponentVNode)(2, _Icon.Icon, {
    "name": icon
  }), 2), (0, _inferno.createVNode)(1, "div", "Tab__text", children, 0), (0, _react.canRender)(rightSlot) && (0, _inferno.createVNode)(1, "div", "Tab__right", rightSlot, 0)], 0, Object.assign({}, (0, _Box.computeBoxProps)(rest))));
};

Tabs.Tab = Tab;

/***/ }),

/***/ 51129:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.TextArea = void 0;

var _inferno = __webpack_require__(96323);

var _react = __webpack_require__(1060);

var _Box = __webpack_require__(54425);

var _Input = __webpack_require__(77863);

var _keycodes = __webpack_require__(57890);

var _excluded = ["onChange", "onKeyDown", "onKeyPress", "onInput", "onFocus", "onBlur", "onEnter", "value", "maxLength", "placeholder"],
    _excluded2 = ["className", "fluid"];

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

function _inheritsLoose(subClass, superClass) { subClass.prototype = Object.create(superClass.prototype); subClass.prototype.constructor = subClass; _setPrototypeOf(subClass, superClass); }

function _setPrototypeOf(o, p) { _setPrototypeOf = Object.setPrototypeOf || function () { function _setPrototypeOf(o, p) { o.__proto__ = p; return o; } return _setPrototypeOf; }(); return _setPrototypeOf(o, p); }

var TextArea = /*#__PURE__*/function (_Component) {
  _inheritsLoose(TextArea, _Component);

  function TextArea(props, context) {
    var _this;

    _this = _Component.call(this, props, context) || this;
    _this.textareaRef = (0, _inferno.createRef)();
    _this.fillerRef = (0, _inferno.createRef)();
    _this.state = {
      editing: false
    };
    var _props$dontUseTabForI = props.dontUseTabForIndent,
        dontUseTabForIndent = _props$dontUseTabForI === void 0 ? false : _props$dontUseTabForI;

    _this.handleOnInput = function (e) {
      var editing = _this.state.editing;
      var onInput = _this.props.onInput;

      if (!editing) {
        _this.setEditing(true);
      }

      if (onInput) {
        onInput(e, e.target.value);
      }
    };

    _this.handleOnChange = function (e) {
      var editing = _this.state.editing;
      var onChange = _this.props.onChange;

      if (editing) {
        _this.setEditing(false);
      }

      if (onChange) {
        onChange(e, e.target.value);
      }
    };

    _this.handleKeyPress = function (e) {
      var editing = _this.state.editing;
      var onKeyPress = _this.props.onKeyPress;

      if (!editing) {
        _this.setEditing(true);
      }

      if (onKeyPress) {
        onKeyPress(e, e.target.value);
      }
    };

    _this.handleKeyDown = function (e) {
      var editing = _this.state.editing;
      var onKeyDown = _this.props.onKeyDown;

      if (e.keyCode === _keycodes.KEY_ESCAPE) {
        _this.setEditing(false);

        e.target.value = (0, _Input.toInputValue)(_this.props.value);
        e.target.blur();
        return;
      }

      if (!editing) {
        _this.setEditing(true);
      }

      if (!dontUseTabForIndent) {
        var keyCode = e.keyCode || e.which;

        if (keyCode === 9) {
          e.preventDefault();
          var _e$target = e.target,
              value = _e$target.value,
              selectionStart = _e$target.selectionStart,
              selectionEnd = _e$target.selectionEnd;
          e.target.value = value.substring(0, selectionStart) + "\t" + value.substring(selectionEnd);
          e.target.selectionEnd = selectionStart + 1;
        }
      }

      if (onKeyDown) {
        onKeyDown(e, e.target.value);
      }
    };

    _this.handleFocus = function (e) {
      var editing = _this.state.editing;

      if (!editing) {
        _this.setEditing(true);
      }
    };

    _this.handleBlur = function (e) {
      var editing = _this.state.editing;
      var onChange = _this.props.onChange;

      if (editing) {
        _this.setEditing(false);

        if (onChange) {
          onChange(e, e.target.value);
        }
      }
    };

    return _this;
  }

  var _proto = TextArea.prototype;

  _proto.componentDidMount = function () {
    function componentDidMount() {
      var nextValue = this.props.value;
      var input = this.textareaRef.current;

      if (input) {
        input.value = (0, _Input.toInputValue)(nextValue);
      }
    }

    return componentDidMount;
  }();

  _proto.componentDidUpdate = function () {
    function componentDidUpdate(prevProps, prevState) {
      var editing = this.state.editing;
      var prevValue = prevProps.value;
      var nextValue = this.props.value;
      var input = this.textareaRef.current;

      if (input && !editing && prevValue !== nextValue) {
        input.value = (0, _Input.toInputValue)(nextValue);
      }
    }

    return componentDidUpdate;
  }();

  _proto.setEditing = function () {
    function setEditing(editing) {
      this.setState({
        editing: editing
      });
    }

    return setEditing;
  }();

  _proto.getValue = function () {
    function getValue() {
      return this.textareaRef.current && this.textareaRef.current.value;
    }

    return getValue;
  }();

  _proto.render = function () {
    function render() {
      // Input only props
      var _this$props = this.props,
          onChange = _this$props.onChange,
          onKeyDown = _this$props.onKeyDown,
          onKeyPress = _this$props.onKeyPress,
          onInput = _this$props.onInput,
          onFocus = _this$props.onFocus,
          onBlur = _this$props.onBlur,
          onEnter = _this$props.onEnter,
          value = _this$props.value,
          maxLength = _this$props.maxLength,
          placeholder = _this$props.placeholder,
          boxProps = _objectWithoutPropertiesLoose(_this$props, _excluded); // Box props


      var className = boxProps.className,
          fluid = boxProps.fluid,
          rest = _objectWithoutPropertiesLoose(boxProps, _excluded2);

      return (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, _Box.Box, Object.assign({
        "className": (0, _react.classes)(['TextArea', fluid && 'TextArea--fluid', className])
      }, rest, {
        children: (0, _inferno.createVNode)(128, "textarea", "TextArea__textarea", null, 1, {
          "placeholder": placeholder,
          "onChange": this.handleOnChange,
          "onKeyDown": this.handleKeyDown,
          "onKeyPress": this.handleKeyPress,
          "onInput": this.handleOnInput,
          "onFocus": this.handleFocus,
          "onBlur": this.handleBlur,
          "maxLength": maxLength
        }, null, this.textareaRef)
      })));
    }

    return render;
  }();

  return TextArea;
}(_inferno.Component);

exports.TextArea = TextArea;

/***/ }),

/***/ 92464:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.TimeDisplay = void 0;

var _format = __webpack_require__(1033);

var _inferno = __webpack_require__(96323);

function _inheritsLoose(subClass, superClass) { subClass.prototype = Object.create(superClass.prototype); subClass.prototype.constructor = subClass; _setPrototypeOf(subClass, superClass); }

function _setPrototypeOf(o, p) { _setPrototypeOf = Object.setPrototypeOf || function () { function _setPrototypeOf(o, p) { o.__proto__ = p; return o; } return _setPrototypeOf; }(); return _setPrototypeOf(o, p); }

// AnimatedNumber Copypaste
var isSafeNumber = function isSafeNumber(value) {
  return typeof value === 'number' && Number.isFinite(value) && !Number.isNaN(value);
};

var TimeDisplay = /*#__PURE__*/function (_Component) {
  _inheritsLoose(TimeDisplay, _Component);

  function TimeDisplay(props) {
    var _this;

    _this = _Component.call(this, props) || this;
    _this.timer = null;
    _this.last_seen_value = undefined;
    _this.state = {
      value: 0
    }; // Set initial state with value provided in props

    if (isSafeNumber(props.value)) {
      _this.state.value = Number(props.value);
      _this.last_seen_value = Number(props.value);
    }

    return _this;
  }

  var _proto = TimeDisplay.prototype;

  _proto.componentDidUpdate = function () {
    function componentDidUpdate() {
      var _this2 = this;

      if (this.props.auto !== undefined) {
        clearInterval(this.timer);
        this.timer = setInterval(function () {
          return _this2.tick();
        }, 1000); // every 1 s
      }
    }

    return componentDidUpdate;
  }();

  _proto.tick = function () {
    function tick() {
      var current = Number(this.state.value);

      if (this.props.value !== this.last_seen_value) {
        this.last_seen_value = this.props.value;
        current = this.props.value;
      }

      var mod = this.props.auto === "up" ? 10 : -10; // Time down by default.

      var value = Math.max(0, current + mod); // one sec tick

      this.setState({
        value: value
      });
    }

    return tick;
  }();

  _proto.componentDidMount = function () {
    function componentDidMount() {
      var _this3 = this;

      if (this.props.auto !== undefined) {
        this.timer = setInterval(function () {
          return _this3.tick();
        }, 1000); // every 1 s
      }
    }

    return componentDidMount;
  }();

  _proto.componentWillUnmount = function () {
    function componentWillUnmount() {
      clearInterval(this.timer);
    }

    return componentWillUnmount;
  }();

  _proto.render = function () {
    function render() {
      var val = this.state.value; // Directly display weird stuff

      if (!isSafeNumber(val)) {
        return this.state.value || null;
      }

      return (0, _format.formatTime)(val);
    }

    return render;
  }();

  return TimeDisplay;
}(_inferno.Component);

exports.TimeDisplay = TimeDisplay;

/***/ }),

/***/ 59463:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Tooltip = void 0;

var _inferno = __webpack_require__(96323);

var _core = __webpack_require__(34739);

function _inheritsLoose(subClass, superClass) { subClass.prototype = Object.create(superClass.prototype); subClass.prototype.constructor = subClass; _setPrototypeOf(subClass, superClass); }

function _setPrototypeOf(o, p) { _setPrototypeOf = Object.setPrototypeOf || function () { function _setPrototypeOf(o, p) { o.__proto__ = p; return o; } return _setPrototypeOf; }(); return _setPrototypeOf(o, p); }

var DEFAULT_OPTIONS = {
  modifiers: [{
    name: "eventListeners",
    enabled: false
  }]
};
var NULL_RECT = {
  width: 0,
  height: 0,
  top: 0,
  right: 0,
  bottom: 0,
  left: 0
};

var Tooltip = /*#__PURE__*/function (_Component) {
  _inheritsLoose(Tooltip, _Component);

  function Tooltip() {
    return _Component.apply(this, arguments) || this;
  }

  var _proto = Tooltip.prototype;

  // Mounting poppers is really laggy because popper.js is very slow.
  // Thus, instead of using the Popper component, Tooltip creates ONE popper
  // and stores every tooltip inside that.
  // This means you can never have two tooltips at once, for instance.
  _proto.getDOMNode = function () {
    function getDOMNode() {
      // HACK: We don't want to create a wrapper, as it could break the layout
      // of consumers, so we do the inferno equivalent of `findDOMNode(this)`.
      // My attempt to avoid this was a render prop that passed in
      // callbacks to onmouseenter and onmouseleave, but this was unwiedly
      // to consumers, specifically buttons.
      // This code is copied from `findDOMNode` in inferno-extras.
      // Because this component is written in TypeScript, we will know
      // immediately if this internal variable is removed.
      return (0, _inferno.findDOMfromVNode)(this.$LI, true);
    }

    return getDOMNode;
  }();

  _proto.componentDidMount = function () {
    function componentDidMount() {
      var _this = this;

      var domNode = this.getDOMNode();

      if (!domNode) {
        return;
      }

      domNode.addEventListener("mouseenter", function () {
        var renderedTooltip = Tooltip.renderedTooltip;

        if (renderedTooltip === undefined) {
          renderedTooltip = document.createElement("div");
          renderedTooltip.className = "Tooltip";
          document.body.appendChild(renderedTooltip);
          Tooltip.renderedTooltip = renderedTooltip;
        }

        Tooltip.currentHoveredElement = domNode;
        renderedTooltip.style.opacity = "1";

        _this.renderPopperContent();
      });
      domNode.addEventListener("mouseleave", function () {
        _this.fadeOut();
      });
    }

    return componentDidMount;
  }();

  _proto.fadeOut = function () {
    function fadeOut() {
      if (Tooltip.currentHoveredElement !== this.getDOMNode()) {
        return;
      }

      Tooltip.currentHoveredElement = undefined;
      Tooltip.renderedTooltip.style.opacity = "0";
    }

    return fadeOut;
  }();

  _proto.renderPopperContent = function () {
    function renderPopperContent() {
      var _this2 = this;

      var renderedTooltip = Tooltip.renderedTooltip;

      if (!renderedTooltip) {
        return;
      }

      (0, _inferno.render)((0, _inferno.createVNode)(1, "span", null, this.props.content, 0), renderedTooltip, function () {
        var singletonPopper = Tooltip.singletonPopper;

        if (singletonPopper === undefined) {
          singletonPopper = (0, _core.createPopper)(Tooltip.virtualElement, renderedTooltip, Object.assign({}, DEFAULT_OPTIONS, {
            placement: _this2.props.position || "auto"
          }));
          Tooltip.singletonPopper = singletonPopper;
        } else {
          singletonPopper.setOptions(Object.assign({}, DEFAULT_OPTIONS, {
            placement: _this2.props.position || "auto"
          }));
          singletonPopper.update();
        }
      }, this.context);
    }

    return renderPopperContent;
  }();

  _proto.componentDidUpdate = function () {
    function componentDidUpdate() {
      if (Tooltip.currentHoveredElement !== this.getDOMNode()) {
        return;
      }

      this.renderPopperContent();
    }

    return componentDidUpdate;
  }();

  _proto.componentWillUnmount = function () {
    function componentWillUnmount() {
      this.fadeOut();
    }

    return componentWillUnmount;
  }();

  _proto.render = function () {
    function render() {
      return this.props.children;
    }

    return render;
  }();

  return Tooltip;
}(_inferno.Component);

exports.Tooltip = Tooltip;
Tooltip.renderedTooltip = void 0;
Tooltip.singletonPopper = void 0;
Tooltip.currentHoveredElement = void 0;
Tooltip.virtualElement = {
  getBoundingClientRect: function () {
    function getBoundingClientRect() {
      var _Tooltip$currentHover, _Tooltip$currentHover2;

      return (_Tooltip$currentHover = (_Tooltip$currentHover2 = Tooltip.currentHoveredElement) == null ? void 0 : _Tooltip$currentHover2.getBoundingClientRect()) != null ? _Tooltip$currentHover : NULL_RECT;
    }

    return getBoundingClientRect;
  }()
};

/***/ }),

/***/ 10580:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.TrackOutsideClicks = void 0;

var _inferno = __webpack_require__(96323);

function _assertThisInitialized(self) { if (self === void 0) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return self; }

function _inheritsLoose(subClass, superClass) { subClass.prototype = Object.create(superClass.prototype); subClass.prototype.constructor = subClass; _setPrototypeOf(subClass, superClass); }

function _setPrototypeOf(o, p) { _setPrototypeOf = Object.setPrototypeOf || function () { function _setPrototypeOf(o, p) { o.__proto__ = p; return o; } return _setPrototypeOf; }(); return _setPrototypeOf(o, p); }

var TrackOutsideClicks = /*#__PURE__*/function (_Component) {
  _inheritsLoose(TrackOutsideClicks, _Component);

  function TrackOutsideClicks() {
    var _this;

    _this = _Component.call(this) || this;
    _this.ref = (0, _inferno.createRef)();
    _this.handleOutsideClick = _this.handleOutsideClick.bind(_assertThisInitialized(_this));
    document.addEventListener('click', _this.handleOutsideClick);
    return _this;
  }

  var _proto = TrackOutsideClicks.prototype;

  _proto.componentWillUnmount = function () {
    function componentWillUnmount() {
      document.removeEventListener('click', this.handleOutsideClick);
    }

    return componentWillUnmount;
  }();

  _proto.handleOutsideClick = function () {
    function handleOutsideClick(event) {
      if (!(event.target instanceof Node)) {
        return;
      }

      if (this.ref.current && !this.ref.current.contains(event.target)) {
        this.props.onOutsideClick();
      }
    }

    return handleOutsideClick;
  }();

  _proto.render = function () {
    function render() {
      return (0, _inferno.createVNode)(1, "div", null, this.props.children, 0, null, null, this.ref);
    }

    return render;
  }();

  return TrackOutsideClicks;
}(_inferno.Component);

exports.TrackOutsideClicks = TrackOutsideClicks;

/***/ }),

/***/ 26949:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.TrackOutsideClicks = exports.Tooltip = exports.TimeDisplay = exports.TextArea = exports.Tabs = exports.Table = exports.Stack = exports.Slider = exports.Section = exports.RoundGauge = exports.Popper = exports.ProgressBar = exports.NumberInput = exports.NoticeBox = exports.Modal = exports.LabeledList = exports.LabeledControls = exports.Knob = exports.KeyListener = exports.Input = exports.InfinitePlane = exports.Icon = exports.Grid = exports.Flex = exports.Dropdown = exports.DraggableControl = exports.Divider = exports.Dimmer = exports.ColorBox = exports.Collapsible = exports.Chart = exports.ByondUi = exports.Button = exports.Box = exports.BlockQuote = exports.Blink = exports.AnimatedNumber = void 0;

var _AnimatedNumber = __webpack_require__(13251);

exports.AnimatedNumber = _AnimatedNumber.AnimatedNumber;

var _Blink = __webpack_require__(90297);

exports.Blink = _Blink.Blink;

var _BlockQuote = __webpack_require__(7149);

exports.BlockQuote = _BlockQuote.BlockQuote;

var _Box = __webpack_require__(54425);

exports.Box = _Box.Box;

var _Button = __webpack_require__(25808);

exports.Button = _Button.Button;

var _ByondUi = __webpack_require__(44051);

exports.ByondUi = _ByondUi.ByondUi;

var _Chart = __webpack_require__(64726);

exports.Chart = _Chart.Chart;

var _Collapsible = __webpack_require__(49751);

exports.Collapsible = _Collapsible.Collapsible;

var _ColorBox = __webpack_require__(47233);

exports.ColorBox = _ColorBox.ColorBox;

var _Dimmer = __webpack_require__(93913);

exports.Dimmer = _Dimmer.Dimmer;

var _Divider = __webpack_require__(22755);

exports.Divider = _Divider.Divider;

var _DraggableControl = __webpack_require__(42833);

exports.DraggableControl = _DraggableControl.DraggableControl;

var _Dropdown = __webpack_require__(12489);

exports.Dropdown = _Dropdown.Dropdown;

var _Flex = __webpack_require__(67486);

exports.Flex = _Flex.Flex;

var _Grid = __webpack_require__(28408);

exports.Grid = _Grid.Grid;

var _Icon = __webpack_require__(65743);

exports.Icon = _Icon.Icon;

var _InfinitePlane = __webpack_require__(63268);

exports.InfinitePlane = _InfinitePlane.InfinitePlane;

var _Input = __webpack_require__(77863);

exports.Input = _Input.Input;

var _KeyListener = __webpack_require__(61515);

exports.KeyListener = _KeyListener.KeyListener;

var _Knob = __webpack_require__(14274);

exports.Knob = _Knob.Knob;

var _LabeledControls = __webpack_require__(95592);

exports.LabeledControls = _LabeledControls.LabeledControls;

var _LabeledList = __webpack_require__(21173);

exports.LabeledList = _LabeledList.LabeledList;

var _Modal = __webpack_require__(89416);

exports.Modal = _Modal.Modal;

var _NoticeBox = __webpack_require__(23257);

exports.NoticeBox = _NoticeBox.NoticeBox;

var _NumberInput = __webpack_require__(97667);

exports.NumberInput = _NumberInput.NumberInput;

var _ProgressBar = __webpack_require__(12194);

exports.ProgressBar = _ProgressBar.ProgressBar;

var _Popper = __webpack_require__(16513);

exports.Popper = _Popper.Popper;

var _RoundGauge = __webpack_require__(94709);

exports.RoundGauge = _RoundGauge.RoundGauge;

var _Section = __webpack_require__(77381);

exports.Section = _Section.Section;

var _Slider = __webpack_require__(4482);

exports.Slider = _Slider.Slider;

var _Stack = __webpack_require__(4033);

exports.Stack = _Stack.Stack;

var _Table = __webpack_require__(28581);

exports.Table = _Table.Table;

var _Tabs = __webpack_require__(60839);

exports.Tabs = _Tabs.Tabs;

var _TextArea = __webpack_require__(51129);

exports.TextArea = _TextArea.TextArea;

var _TimeDisplay = __webpack_require__(92464);

exports.TimeDisplay = _TimeDisplay.TimeDisplay;

var _Tooltip = __webpack_require__(59463);

exports.Tooltip = _Tooltip.Tooltip;

var _TrackOutsideClicks = __webpack_require__(10580);

exports.TrackOutsideClicks = _TrackOutsideClicks.TrackOutsideClicks;

/***/ }),

/***/ 84200:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.getGasColor = exports.getGasLabel = exports.RADIO_CHANNELS = exports.CSS_COLORS = exports.COLORS = exports.UI_CLOSE = exports.UI_DISABLED = exports.UI_UPDATE = exports.UI_INTERACTIVE = void 0;

/**
 * @file
 * @copyright 2020 Aleksej Komarov
 * @license MIT
 */
// UI states, which are mirrored from the BYOND code.
var UI_INTERACTIVE = 2;
exports.UI_INTERACTIVE = UI_INTERACTIVE;
var UI_UPDATE = 1;
exports.UI_UPDATE = UI_UPDATE;
var UI_DISABLED = 0;
exports.UI_DISABLED = UI_DISABLED;
var UI_CLOSE = -1; // All game related colors are stored here

exports.UI_CLOSE = UI_CLOSE;
var COLORS = {
  // Department colors
  department: {
    captain: '#c06616',
    security: '#e74c3c',
    medbay: '#3498db',
    science: '#9b59b6',
    engineering: '#f1c40f',
    cargo: '#f39c12',
    centcom: '#00c100',
    other: '#c38312'
  },
  // Damage type colors
  damageType: {
    oxy: '#3498db',
    toxin: '#2ecc71',
    burn: '#e67e22',
    brute: '#e74c3c'
  },
  // reagent / chemistry related colours
  reagent: {
    acidicbuffer: "#fbc314",
    basicbuffer: "#3853a4"
  }
}; // Colors defined in CSS

exports.COLORS = COLORS;
var CSS_COLORS = ['black', 'white', 'red', 'orange', 'yellow', 'olive', 'green', 'teal', 'blue', 'violet', 'purple', 'pink', 'brown', 'grey', 'good', 'average', 'bad', 'label'];
/* IF YOU CHANGE THIS KEEP IT IN SYNC WITH CHAT CSS */

exports.CSS_COLORS = CSS_COLORS;
var RADIO_CHANNELS = [{
  name: 'Syndicate',
  freq: 1213,
  color: '#8f4a4b'
}, {
  name: 'Red Team',
  freq: 1215,
  color: '#ff4444'
}, {
  name: 'Blue Team',
  freq: 1217,
  color: '#3434fd'
}, {
  name: 'CentCom',
  freq: 1337,
  color: '#2681a5'
}, {
  name: 'Supply',
  freq: 1347,
  color: '#b88646'
}, {
  name: 'Service',
  freq: 1349,
  color: '#6ca729'
}, {
  name: 'Science',
  freq: 1351,
  color: '#c68cfa'
}, {
  name: 'Command',
  freq: 1353,
  color: '#fcdf03'
}, {
  name: 'Medical',
  freq: 1355,
  color: '#57b8f0'
}, {
  name: 'Engineering',
  freq: 1357,
  color: '#f37746'
}, {
  name: 'Security',
  freq: 1359,
  color: '#dd3535'
}, {
  name: 'AI Private',
  freq: 1447,
  color: '#d65d95'
}, {
  name: 'Common',
  freq: 1459,
  color: '#1ecc43'
}];
exports.RADIO_CHANNELS = RADIO_CHANNELS;
var GASES = [{
  'id': 'o2',
  'name': 'Oxygen',
  'label': 'O₂',
  'color': 'blue'
}, {
  'id': 'n2',
  'name': 'Nitrogen',
  'label': 'N₂',
  'color': 'red'
}, {
  'id': 'co2',
  'name': 'Carbon Dioxide',
  'label': 'CO₂',
  'color': 'grey'
}, {
  'id': 'plasma',
  'name': 'Plasma',
  'label': 'Plasma',
  'color': 'pink'
}, {
  'id': 'water_vapor',
  'name': 'Water Vapor',
  'label': 'H₂O',
  'color': 'grey'
}, {
  'id': 'nob',
  'name': 'Hyper-noblium',
  'label': 'Hyper-nob',
  'color': 'teal'
}, {
  'id': 'n2o',
  'name': 'Nitrous Oxide',
  'label': 'N₂O',
  'color': 'red'
}, {
  'id': 'no2',
  'name': 'Nitryl',
  'label': 'NO₂',
  'color': 'brown'
}, {
  'id': 'tritium',
  'name': 'Tritium',
  'label': 'Tritium',
  'color': 'green'
}, {
  'id': 'bz',
  'name': 'BZ',
  'label': 'BZ',
  'color': 'purple'
}, {
  'id': 'stim',
  'name': 'Stimulum',
  'label': 'Stimulum',
  'color': 'purple'
}, {
  'id': 'pluox',
  'name': 'Pluoxium',
  'label': 'Pluoxium',
  'color': 'blue'
}, {
  'id': 'miasma',
  'name': 'Miasma',
  'label': 'Miasma',
  'color': 'olive'
}, {
  'id': 'hydrogen',
  'name': 'Hydrogen',
  'label': 'H₂',
  'color': 'white'
}];

var getGasLabel = function getGasLabel(gasId, fallbackValue) {
  var gasSearchString = String(gasId).toLowerCase();
  var gas = GASES.find(function (gas) {
    return gas.id === gasSearchString || gas.name.toLowerCase() === gasSearchString;
  });
  return gas && gas.label || fallbackValue || gasId;
};

exports.getGasLabel = getGasLabel;

var getGasColor = function getGasColor(gasId) {
  var gasSearchString = String(gasId).toLowerCase();
  var gas = GASES.find(function (gas) {
    return gas.id === gasSearchString || gas.name.toLowerCase() === gasSearchString;
  });
  return gas && gas.color;
};

exports.getGasColor = getGasColor;

/***/ }),

/***/ 71224:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.KitchenSink = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

/**
 * @file
 * @copyright 2020 Aleksej Komarov
 * @license MIT
 */
var r = __webpack_require__(69321);
/**
 * @returns {{
 *   meta: {
 *     title: string,
 *     render: () => any,
 *   },
 * }[]}
 */


var getStories = function getStories() {
  return r.keys().map(function (path) {
    return r(path);
  });
};

var KitchenSink = function KitchenSink(props, context) {
  var panel = props.panel;

  var _useLocalState = (0, _backend.useLocalState)(context, 'kitchenSinkTheme'),
      theme = _useLocalState[0];

  var _useLocalState2 = (0, _backend.useLocalState)(context, 'pageIndex', 0),
      pageIndex = _useLocalState2[0],
      setPageIndex = _useLocalState2[1];

  var stories = getStories();
  var story = stories[pageIndex];
  var Layout = panel ? _layouts.Pane : _layouts.Window;
  return (0, _inferno.createComponentVNode)(2, Layout, {
    "title": "Kitchen Sink",
    "width": 600,
    "height": 500,
    "theme": theme,
    children: (0, _inferno.createComponentVNode)(2, _components.Flex, {
      "height": "100%",
      children: [(0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
        "m": 1,
        "mr": 0,
        children: (0, _inferno.createComponentVNode)(2, _components.Section, {
          "fill": true,
          "fitted": true,
          children: (0, _inferno.createComponentVNode)(2, _components.Tabs, {
            "vertical": true,
            children: stories.map(function (story, i) {
              return (0, _inferno.createComponentVNode)(2, _components.Tabs.Tab, {
                "color": "transparent",
                "selected": i === pageIndex,
                "onClick": function () {
                  function onClick() {
                    return setPageIndex(i);
                  }

                  return onClick;
                }(),
                children: story.meta.title
              }, i);
            })
          })
        })
      }), (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
        "position": "relative",
        "grow": 1,
        children: (0, _inferno.createComponentVNode)(2, Layout.Content, {
          "scrollable": true,
          children: story.meta.render()
        })
      })]
    })
  });
};

exports.KitchenSink = KitchenSink;

/***/ }),

/***/ 98069:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.openExternalBrowser = exports.toggleDebugLayout = exports.toggleKitchenSink = void 0;

var _redux = __webpack_require__(465);

/**
 * @file
 * @copyright 2020 Aleksej Komarov
 * @license MIT
 */
var toggleKitchenSink = (0, _redux.createAction)('debug/toggleKitchenSink');
exports.toggleKitchenSink = toggleKitchenSink;
var toggleDebugLayout = (0, _redux.createAction)('debug/toggleDebugLayout');
exports.toggleDebugLayout = toggleDebugLayout;
var openExternalBrowser = (0, _redux.createAction)('debug/openExternalBrowser');
exports.openExternalBrowser = openExternalBrowser;

/***/ }),

/***/ 35177:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.useDebug = void 0;

var _redux = __webpack_require__(465);

var _selectors = __webpack_require__(12184);

/**
 * @file
 * @copyright 2020 Aleksej Komarov
 * @license MIT
 */
var useDebug = function useDebug(context) {
  return (0, _redux.useSelector)(context, _selectors.selectDebug);
};

exports.useDebug = useDebug;

/***/ }),

/***/ 27157:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.debugReducer = exports.relayMiddleware = exports.debugMiddleware = exports.KitchenSink = exports.useDebug = void 0;

var _hooks = __webpack_require__(35177);

exports.useDebug = _hooks.useDebug;

var _KitchenSink = __webpack_require__(71224);

exports.KitchenSink = _KitchenSink.KitchenSink;

var _middleware = __webpack_require__(46656);

exports.debugMiddleware = _middleware.debugMiddleware;
exports.relayMiddleware = _middleware.relayMiddleware;

var _reducer = __webpack_require__(7124);

exports.debugReducer = _reducer.debugReducer;

/***/ }),

/***/ 46656:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.relayMiddleware = exports.debugMiddleware = void 0;

var _keycodes = __webpack_require__(57890);

var _events = __webpack_require__(99397);

var _hotkeys = __webpack_require__(6965);

var _actions = __webpack_require__(98069);

/**
 * @file
 * @copyright 2020 Aleksej Komarov
 * @license MIT
 */
var relayedTypes = ['backend/update', 'chat/message'];

var debugMiddleware = function debugMiddleware(store) {
  (0, _hotkeys.acquireHotKey)(_keycodes.KEY_F11);
  (0, _hotkeys.acquireHotKey)(_keycodes.KEY_F12);

  _events.globalEvents.on('keydown', function (key) {
    if (key.code === _keycodes.KEY_F11) {
      store.dispatch((0, _actions.toggleDebugLayout)());
    }

    if (key.code === _keycodes.KEY_F12) {
      store.dispatch((0, _actions.toggleKitchenSink)());
    }

    if (key.ctrl && key.alt && key.code === _keycodes.KEY_BACKSPACE) {
      // NOTE: We need to call this in a timeout, because we need a clean
      // stack in order for this to be a fatal error.
      setTimeout(function () {
        throw new Error('OOPSIE WOOPSIE!! UwU We made a fucky wucky!! A wittle' + ' fucko boingo! The code monkeys at our headquarters are' + ' working VEWY HAWD to fix this!');
      });
    }
  });

  return function (next) {
    return function (action) {
      return next(action);
    };
  };
};

exports.debugMiddleware = debugMiddleware;

var relayMiddleware = function relayMiddleware(store) {
  var devServer = __webpack_require__(82252);

  var externalBrowser = location.search === '?external';

  if (externalBrowser) {
    devServer.subscribe(function (msg) {
      var type = msg.type,
          payload = msg.payload;

      if (type === 'relay' && payload.windowId === window.__windowId__) {
        store.dispatch(Object.assign({}, payload.action, {
          relayed: true
        }));
      }
    });
  } else {
    (0, _hotkeys.acquireHotKey)(_keycodes.KEY_F10);

    _events.globalEvents.on('keydown', function (key) {
      if (key === _keycodes.KEY_F10) {
        store.dispatch((0, _actions.openExternalBrowser)());
      }
    });
  }

  return function (next) {
    return function (action) {
      var type = action.type,
          payload = action.payload,
          relayed = action.relayed;

      if (type === _actions.openExternalBrowser.type) {
        window.open(location.href + '?external', '_blank');
        return;
      }

      if (relayedTypes.includes(type) && !relayed && !externalBrowser) {
        devServer.sendMessage({
          type: 'relay',
          payload: {
            windowId: window.__windowId__,
            action: action
          }
        });
      }

      return next(action);
    };
  };
};

exports.relayMiddleware = relayMiddleware;

/***/ }),

/***/ 7124:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.debugReducer = void 0;

/**
 * @file
 * @copyright 2020 Aleksej Komarov
 * @license MIT
 */
var debugReducer = function debugReducer(state, action) {
  if (state === void 0) {
    state = {};
  }

  var type = action.type,
      payload = action.payload;

  if (type === 'debug/toggleKitchenSink') {
    return Object.assign({}, state, {
      kitchenSink: !state.kitchenSink
    });
  }

  if (type === 'debug/toggleDebugLayout') {
    return Object.assign({}, state, {
      debugLayout: !state.debugLayout
    });
  }

  return state;
};

exports.debugReducer = debugReducer;

/***/ }),

/***/ 12184:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.selectDebug = void 0;

/**
 * @file
 * @copyright 2020 Aleksej Komarov
 * @license MIT
 */
var selectDebug = function selectDebug(state) {
  return state.debug;
};

exports.selectDebug = selectDebug;

/***/ }),

/***/ 41410:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.resizeStartHandler = exports.dragStartHandler = exports.setupDrag = exports.recallWindowGeometry = exports.storeWindowGeometry = exports.getScreenSize = exports.getScreenPosition = exports.setWindowSize = exports.setWindowPosition = exports.getWindowSize = exports.getWindowPosition = exports.setWindowKey = void 0;

var _storage = __webpack_require__(7151);

var _vector = __webpack_require__(67288);

var _logging = __webpack_require__(79926);

function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { Promise.resolve(value).then(_next, _throw); } }

function _asyncToGenerator(fn) { return function () { var self = this, args = arguments; return new Promise(function (resolve, reject) { var gen = fn.apply(self, args); function _next(value) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value); } function _throw(err) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err); } _next(undefined); }); }; }

var logger = (0, _logging.createLogger)('drag');
var windowKey = window.__windowId__;
var dragging = false;
var resizing = false;
var screenOffset = [0, 0];
var screenOffsetPromise;
var dragPointOffset;
var resizeMatrix;
var initialSize;
var size;

var setWindowKey = function setWindowKey(key) {
  windowKey = key;
};

exports.setWindowKey = setWindowKey;

var getWindowPosition = function getWindowPosition() {
  return [window.screenLeft, window.screenTop];
};

exports.getWindowPosition = getWindowPosition;

var getWindowSize = function getWindowSize() {
  return [window.innerWidth, window.innerHeight];
};

exports.getWindowSize = getWindowSize;

var setWindowPosition = function setWindowPosition(vec) {
  var byondPos = (0, _vector.vecAdd)(vec, screenOffset);
  return Byond.winset(window.__windowId__, {
    pos: byondPos[0] + ',' + byondPos[1]
  });
};

exports.setWindowPosition = setWindowPosition;

var setWindowSize = function setWindowSize(vec) {
  return Byond.winset(window.__windowId__, {
    size: vec[0] + 'x' + vec[1]
  });
};

exports.setWindowSize = setWindowSize;

var getScreenPosition = function getScreenPosition() {
  return [0 - screenOffset[0], 0 - screenOffset[1]];
};

exports.getScreenPosition = getScreenPosition;

var getScreenSize = function getScreenSize() {
  return [window.screen.availWidth, window.screen.availHeight];
};
/**
 * Moves an item to the top of the recents array, and keeps its length
 * limited to the number in `limit` argument.
 *
 * Uses a strict equality check for comparisons.
 *
 * Returns new recents and an item which was trimmed.
 */


exports.getScreenSize = getScreenSize;

var touchRecents = function touchRecents(recents, touchedItem, limit) {
  if (limit === void 0) {
    limit = 50;
  }

  var nextRecents = [touchedItem];
  var trimmedItem;

  for (var i = 0; i < recents.length; i++) {
    var item = recents[i];

    if (item === touchedItem) {
      continue;
    }

    if (nextRecents.length < limit) {
      nextRecents.push(item);
    } else {
      trimmedItem = item;
    }
  }

  return [nextRecents, trimmedItem];
};

var storeWindowGeometry = /*#__PURE__*/function () {
  var _ref = _asyncToGenerator( /*#__PURE__*/regeneratorRuntime.mark(function () {
    function _callee() {
      var geometry, _touchRecents, geometries, trimmedKey;

      return regeneratorRuntime.wrap(function () {
        function _callee$(_context) {
          while (1) {
            switch (_context.prev = _context.next) {
              case 0:
                logger.log('storing geometry');
                geometry = {
                  pos: getWindowPosition(),
                  size: getWindowSize()
                };

                _storage.storage.set(windowKey, geometry); // Update the list of stored geometries


                _context.t0 = touchRecents;
                _context.next = 6;
                return _storage.storage.get('geometries');

              case 6:
                _context.t1 = _context.sent;

                if (_context.t1) {
                  _context.next = 9;
                  break;
                }

                _context.t1 = [];

              case 9:
                _context.t2 = _context.t1;
                _context.t3 = windowKey;
                _touchRecents = (0, _context.t0)(_context.t2, _context.t3);
                geometries = _touchRecents[0];
                trimmedKey = _touchRecents[1];

                if (trimmedKey) {
                  _storage.storage.remove(trimmedKey);
                }

                _storage.storage.set('geometries', geometries);

              case 16:
              case "end":
                return _context.stop();
            }
          }
        }

        return _callee$;
      }(), _callee);
    }

    return _callee;
  }()));

  return function () {
    function storeWindowGeometry() {
      return _ref.apply(this, arguments);
    }

    return storeWindowGeometry;
  }();
}();

exports.storeWindowGeometry = storeWindowGeometry;

var recallWindowGeometry = /*#__PURE__*/function () {
  var _ref2 = _asyncToGenerator( /*#__PURE__*/regeneratorRuntime.mark(function () {
    function _callee2(options) {
      var geometry, pos, size, areaAvailable;
      return regeneratorRuntime.wrap(function () {
        function _callee2$(_context2) {
          while (1) {
            switch (_context2.prev = _context2.next) {
              case 0:
                if (options === void 0) {
                  options = {};
                }

                _context2.t0 = options.fancy;

                if (!_context2.t0) {
                  _context2.next = 6;
                  break;
                }

                _context2.next = 5;
                return _storage.storage.get(windowKey);

              case 5:
                _context2.t0 = _context2.sent;

              case 6:
                geometry = _context2.t0;

                if (geometry) {
                  logger.log('recalled geometry:', geometry);
                }

                pos = (geometry == null ? void 0 : geometry.pos) || options.pos;
                size = options.size; // Wait until screen offset gets resolved

                _context2.next = 12;
                return screenOffsetPromise;

              case 12:
                areaAvailable = [window.screen.availWidth, window.screen.availHeight]; // Set window size

                if (size) {
                  // Constraint size to not exceed available screen area.
                  size = [Math.min(areaAvailable[0], size[0]), Math.min(areaAvailable[1], size[1])];
                  setWindowSize(size);
                } // Set window position


                if (pos) {
                  // Constraint window position if monitor lock was set in preferences.
                  if (size && options.locked) {
                    pos = constraintPosition(pos, size)[1];
                  }

                  setWindowPosition(pos);
                } // Set window position at the center of the screen.
                else if (size) {
                  pos = (0, _vector.vecAdd)((0, _vector.vecScale)(areaAvailable, 0.5), (0, _vector.vecScale)(size, -0.5), (0, _vector.vecScale)(screenOffset, -1.0));
                  setWindowPosition(pos);
                }

              case 15:
              case "end":
                return _context2.stop();
            }
          }
        }

        return _callee2$;
      }(), _callee2);
    }

    return _callee2;
  }()));

  return function () {
    function recallWindowGeometry(_x) {
      return _ref2.apply(this, arguments);
    }

    return recallWindowGeometry;
  }();
}();

exports.recallWindowGeometry = recallWindowGeometry;

var setupDrag = /*#__PURE__*/function () {
  var _ref3 = _asyncToGenerator( /*#__PURE__*/regeneratorRuntime.mark(function () {
    function _callee3() {
      return regeneratorRuntime.wrap(function () {
        function _callee3$(_context3) {
          while (1) {
            switch (_context3.prev = _context3.next) {
              case 0:
                // Calculate screen offset caused by the windows taskbar
                screenOffsetPromise = Byond.winget(window.__windowId__, 'pos').then(function (pos) {
                  return [pos.x - window.screenLeft, pos.y - window.screenTop];
                });
                _context3.next = 3;
                return screenOffsetPromise;

              case 3:
                screenOffset = _context3.sent;
                logger.debug('screen offset', screenOffset);

              case 5:
              case "end":
                return _context3.stop();
            }
          }
        }

        return _callee3$;
      }(), _callee3);
    }

    return _callee3;
  }()));

  return function () {
    function setupDrag() {
      return _ref3.apply(this, arguments);
    }

    return setupDrag;
  }();
}();
/**
 * Constraints window position to safe screen area, accounting for safe
 * margins which could be a system taskbar.
 */


exports.setupDrag = setupDrag;

var constraintPosition = function constraintPosition(pos, size) {
  var screenPos = getScreenPosition();
  var screenSize = getScreenSize();
  var nextPos = [pos[0], pos[1]];
  var relocated = false;

  for (var i = 0; i < 2; i++) {
    var leftBoundary = screenPos[i];
    var rightBoundary = screenPos[i] + screenSize[i];

    if (pos[i] < leftBoundary) {
      nextPos[i] = leftBoundary;
      relocated = true;
    } else if (pos[i] + size[i] > rightBoundary) {
      nextPos[i] = rightBoundary - size[i];
      relocated = true;
    }
  }

  return [relocated, nextPos];
};

var dragStartHandler = function dragStartHandler(event) {
  var _event$target;

  logger.log('drag start');
  dragging = true;
  dragPointOffset = [window.screenLeft - event.screenX, window.screenTop - event.screenY]; // Focus click target

  (_event$target = event.target) == null ? void 0 : _event$target.focus();
  document.addEventListener('mousemove', dragMoveHandler);
  document.addEventListener('mouseup', dragEndHandler);
  dragMoveHandler(event);
};

exports.dragStartHandler = dragStartHandler;

var dragEndHandler = function dragEndHandler(event) {
  logger.log('drag end');
  dragMoveHandler(event);
  document.removeEventListener('mousemove', dragMoveHandler);
  document.removeEventListener('mouseup', dragEndHandler);
  dragging = false;
  storeWindowGeometry();
};

var dragMoveHandler = function dragMoveHandler(event) {
  if (!dragging) {
    return;
  }

  event.preventDefault();
  setWindowPosition((0, _vector.vecAdd)([event.screenX, event.screenY], dragPointOffset));
};

var resizeStartHandler = function resizeStartHandler(x, y) {
  return function (event) {
    var _event$target2;

    resizeMatrix = [x, y];
    logger.log('resize start', resizeMatrix);
    resizing = true;
    dragPointOffset = [window.screenLeft - event.screenX, window.screenTop - event.screenY];
    initialSize = [window.innerWidth, window.innerHeight]; // Focus click target

    (_event$target2 = event.target) == null ? void 0 : _event$target2.focus();
    document.addEventListener('mousemove', resizeMoveHandler);
    document.addEventListener('mouseup', resizeEndHandler);
    resizeMoveHandler(event);
  };
};

exports.resizeStartHandler = resizeStartHandler;

var resizeEndHandler = function resizeEndHandler(event) {
  logger.log('resize end', size);
  resizeMoveHandler(event);
  document.removeEventListener('mousemove', resizeMoveHandler);
  document.removeEventListener('mouseup', resizeEndHandler);
  resizing = false;
  storeWindowGeometry();
};

var resizeMoveHandler = function resizeMoveHandler(event) {
  if (!resizing) {
    return;
  }

  event.preventDefault();
  size = (0, _vector.vecAdd)(initialSize, (0, _vector.vecMultiply)(resizeMatrix, (0, _vector.vecAdd)([event.screenX, event.screenY], (0, _vector.vecInverse)([window.screenLeft, window.screenTop]), dragPointOffset, [1, 1]))); // Sane window size values

  size[0] = Math.max(size[0], 150);
  size[1] = Math.max(size[1], 50);
  setWindowSize(size);
};

/***/ }),

/***/ 99397:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.KeyEvent = exports.removeScrollableNode = exports.addScrollableNode = exports.canStealFocus = exports.setupGlobalEvents = exports.globalEvents = void 0;

var _events = __webpack_require__(52844);

var _keycodes = __webpack_require__(57890);

/**
 * Normalized browser focus events and BYOND-specific focus helpers.
 *
 * @file
 * @copyright 2020 Aleksej Komarov
 * @license MIT
 */
var globalEvents = new _events.EventEmitter();
exports.globalEvents = globalEvents;
var ignoreWindowFocus = false;

var setupGlobalEvents = function setupGlobalEvents(options) {
  if (options === void 0) {
    options = {};
  }

  ignoreWindowFocus = !!options.ignoreWindowFocus;
}; // Window focus
// --------------------------------------------------------


exports.setupGlobalEvents = setupGlobalEvents;
var windowFocusTimeout;
var windowFocused = true;

var setWindowFocus = function setWindowFocus(value, delayed) {
  // Pretend to always be in focus.
  if (ignoreWindowFocus) {
    windowFocused = true;
    return;
  }

  if (windowFocusTimeout) {
    clearTimeout(windowFocusTimeout);
    windowFocusTimeout = null;
  }

  if (delayed) {
    windowFocusTimeout = setTimeout(function () {
      return setWindowFocus(value);
    });
    return;
  }

  if (windowFocused !== value) {
    windowFocused = value;
    globalEvents.emit(value ? 'window-focus' : 'window-blur');
    globalEvents.emit('window-focus-change', value);
  }
}; // Focus stealing
// --------------------------------------------------------


var focusStolenBy = null;

var canStealFocus = function canStealFocus(node) {
  var tag = String(node.tagName).toLowerCase();
  return tag === 'input' || tag === 'textarea';
};

exports.canStealFocus = canStealFocus;

var stealFocus = function stealFocus(node) {
  releaseStolenFocus();
  focusStolenBy = node;
  focusStolenBy.addEventListener('blur', releaseStolenFocus);
};

var releaseStolenFocus = function releaseStolenFocus() {
  if (focusStolenBy) {
    focusStolenBy.removeEventListener('blur', releaseStolenFocus);
    focusStolenBy = null;
  }
}; // Focus follows the mouse
// --------------------------------------------------------


var focusedNode = null;
var lastVisitedNode = null;
var trackedNodes = [];

var addScrollableNode = function addScrollableNode(node) {
  trackedNodes.push(node);
};

exports.addScrollableNode = addScrollableNode;

var removeScrollableNode = function removeScrollableNode(node) {
  var index = trackedNodes.indexOf(node);

  if (index >= 0) {
    trackedNodes.splice(index, 1);
  }
};

exports.removeScrollableNode = removeScrollableNode;

var focusNearestTrackedParent = function focusNearestTrackedParent(node) {
  if (focusStolenBy || !windowFocused) {
    return;
  }

  var body = document.body;

  while (node && node !== body) {
    if (trackedNodes.includes(node)) {
      // NOTE: Contains is a DOM4 method
      if (node.contains(focusedNode)) {
        return;
      }

      focusedNode = node;
      node.focus();
      return;
    }

    node = node.parentNode;
  }
};

window.addEventListener('mousemove', function (e) {
  var node = e.target;

  if (node !== lastVisitedNode) {
    lastVisitedNode = node;
    focusNearestTrackedParent(node);
  }
}); // Focus event hooks
// --------------------------------------------------------

window.addEventListener('focusin', function (e) {
  lastVisitedNode = null;
  focusedNode = e.target;
  setWindowFocus(true);

  if (canStealFocus(e.target)) {
    stealFocus(e.target);
    return;
  }
});
window.addEventListener('focusout', function (e) {
  lastVisitedNode = null;
  setWindowFocus(false, true);
});
window.addEventListener('blur', function (e) {
  lastVisitedNode = null;
  setWindowFocus(false, true);
});
window.addEventListener('beforeunload', function (e) {
  setWindowFocus(false);
}); // Key events
// --------------------------------------------------------

var keyHeldByCode = {};

var KeyEvent = /*#__PURE__*/function () {
  function KeyEvent(e, type, repeat) {
    this.event = e;
    this.type = type;
    this.code = window.event ? e.which : e.keyCode;
    this.ctrl = e.ctrlKey;
    this.shift = e.shiftKey;
    this.alt = e.altKey;
    this.repeat = !!repeat;
  }

  var _proto = KeyEvent.prototype;

  _proto.hasModifierKeys = function () {
    function hasModifierKeys() {
      return this.ctrl || this.alt || this.shift;
    }

    return hasModifierKeys;
  }();

  _proto.isModifierKey = function () {
    function isModifierKey() {
      return this.code === _keycodes.KEY_CTRL || this.code === _keycodes.KEY_SHIFT || this.code === _keycodes.KEY_ALT;
    }

    return isModifierKey;
  }();

  _proto.isDown = function () {
    function isDown() {
      return this.type === 'keydown';
    }

    return isDown;
  }();

  _proto.isUp = function () {
    function isUp() {
      return this.type === 'keyup';
    }

    return isUp;
  }();

  _proto.toString = function () {
    function toString() {
      if (this._str) {
        return this._str;
      }

      this._str = '';

      if (this.ctrl) {
        this._str += 'Ctrl+';
      }

      if (this.alt) {
        this._str += 'Alt+';
      }

      if (this.shift) {
        this._str += 'Shift+';
      }

      if (this.code >= 48 && this.code <= 90) {
        this._str += String.fromCharCode(this.code);
      } else if (this.code >= _keycodes.KEY_F1 && this.code <= _keycodes.KEY_F12) {
        this._str += 'F' + (this.code - 111);
      } else {
        this._str += '[' + this.code + ']';
      }

      return this._str;
    }

    return toString;
  }();

  return KeyEvent;
}(); // IE8: Keydown event is only available on document.


exports.KeyEvent = KeyEvent;
document.addEventListener('keydown', function (e) {
  if (canStealFocus(e.target)) {
    return;
  }

  var code = e.keyCode;
  var key = new KeyEvent(e, 'keydown', keyHeldByCode[code]);
  globalEvents.emit('keydown', key);
  globalEvents.emit('key', key);
  keyHeldByCode[code] = true;
});
document.addEventListener('keyup', function (e) {
  if (canStealFocus(e.target)) {
    return;
  }

  var code = e.keyCode;
  var key = new KeyEvent(e, 'keyup');
  globalEvents.emit('keyup', key);
  globalEvents.emit('key', key);
  keyHeldByCode[code] = false;
});

/***/ }),

/***/ 40509:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.focusWindow = exports.focusMap = void 0;

/**
 * Various focus helpers.
 *
 * @file
 * @copyright 2020 Aleksej Komarov
 * @license MIT
 */

/**
 * Moves focus to the BYOND map window.
 */
var focusMap = function focusMap() {
  Byond.winset('taumapwindow.map', {
    focus: true
  });
};
/**
 * Moves focus to the browser window.
 */


exports.focusMap = focusMap;

var focusWindow = function focusWindow() {
  Byond.winset(window.__windowId__, {
    focus: true
  });
};

exports.focusWindow = focusWindow;

/***/ }),

/***/ 1033:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.formatTime = exports.formatSiBaseTenUnit = exports.formatDb = exports.formatMoney = exports.formatPower = exports.formatSiUnit = void 0;

var _math = __webpack_require__(97622);

/**
 * @file
 * @copyright 2020 Aleksej Komarov
 * @license MIT
 */
var SI_SYMBOLS = ['f', // femto
'p', // pico
'n', // nano
'μ', // micro
'm', // milli
// NOTE: This is a space for a reason. When we right align si numbers,
// in monospace mode, we want to units and numbers stay in their respective
// columns. If rendering in HTML mode, this space will collapse into
// a single space anyway.
' ', 'k', // kilo
'M', // mega
'G', // giga
'T', // tera
'P', // peta
'E', // exa
'Z', // zetta
'Y', // yotta
'R', // ronna
'Q', // quecca
'F', 'N', 'H'];
var SI_BASE_INDEX = SI_SYMBOLS.indexOf(' ');
/**
 * Formats a number to a human readable form, by reducing it to SI units.
 * TODO: This is quite a shit code and shit math, needs optimization.
 */

var formatSiUnit = function formatSiUnit(value, minBase1000, unit) {
  if (minBase1000 === void 0) {
    minBase1000 = -SI_BASE_INDEX;
  }

  if (unit === void 0) {
    unit = '';
  }

  if (typeof value !== 'number' || !Number.isFinite(value)) {
    return value;
  }

  var realBase10 = Math.floor(Math.log10(value));
  var base10 = Math.floor(Math.max(minBase1000 * 3, realBase10));
  var realBase1000 = Math.floor(realBase10 / 3);
  var base1000 = Math.floor(base10 / 3);
  var symbolIndex = (0, _math.clamp)(SI_BASE_INDEX + base1000, 0, SI_SYMBOLS.length);
  var symbol = SI_SYMBOLS[symbolIndex];
  var scaledNumber = value / Math.pow(1000, base1000);
  var scaledPrecision = realBase1000 > minBase1000 ? 2 + base1000 * 3 - base10 : 0; // TODO: Make numbers bigger than precision value show
  // up to 2 decimal numbers.

  var finalString = (0, _math.toFixed)(scaledNumber, scaledPrecision) + ' ' + symbol + unit;
  return finalString.trim();
};

exports.formatSiUnit = formatSiUnit;

var formatPower = function formatPower(value, minBase1000) {
  if (minBase1000 === void 0) {
    minBase1000 = 0;
  }

  return formatSiUnit(value, minBase1000, 'W');
};

exports.formatPower = formatPower;

var formatMoney = function formatMoney(value, precision) {
  if (precision === void 0) {
    precision = 0;
  }

  if (!Number.isFinite(value)) {
    return value;
  } // Round the number and make it fixed precision


  var fixed = (0, _math.round)(value, precision);

  if (precision > 0) {
    fixed = (0, _math.toFixed)(value, precision);
  }

  fixed = String(fixed); // Place thousand separators

  var length = fixed.length;
  var indexOfPoint = fixed.indexOf('.');

  if (indexOfPoint === -1) {
    indexOfPoint = length;
  }

  var result = '';

  for (var i = 0; i < length; i++) {
    if (i > 0 && i < indexOfPoint && (indexOfPoint - i) % 3 === 0) {
      // Thin space
      result += "\u2009";
    }

    result += fixed.charAt(i);
  }

  return result;
};
/**
 * Formats a floating point number as a number on the decibel scale.
 */


exports.formatMoney = formatMoney;

var formatDb = function formatDb(value) {
  var db = 20 * Math.log(value) / Math.log(10);
  var sign = db >= 0 ? '+' : '–';
  var formatted = Math.abs(db);

  if (formatted === Infinity) {
    formatted = 'Inf';
  } else {
    formatted = (0, _math.toFixed)(formatted, 2);
  }

  return sign + formatted + ' dB';
};

exports.formatDb = formatDb;
var SI_BASE_TEN_UNIT = ['', '· 10³', // kilo
'· 10⁶', // mega
'· 10⁹', // giga
'· 10¹²', // tera
'· 10¹⁵', // peta
'· 10¹⁸', // exa
'· 10²¹', // zetta
'· 10²⁴', // yotta
'· 10²⁷', // ronna
'· 10³⁰', // quecca
'· 10³³', '· 10³⁶', '· 10³⁹'];
var SI_BASE_TEN_INDEX = SI_BASE_TEN_UNIT.indexOf(' ');
/**
 * Formats a number to a human readable form, by reducing it to SI units.
 * TODO: This is quite a shit code and shit math, needs optimization.
 */

var formatSiBaseTenUnit = function formatSiBaseTenUnit(value, minBase1000, unit) {
  if (minBase1000 === void 0) {
    minBase1000 = -SI_BASE_TEN_INDEX;
  }

  if (unit === void 0) {
    unit = '';
  }

  if (typeof value !== 'number' || !Number.isFinite(value)) {
    return value;
  }

  var realBase10 = Math.floor(Math.log10(value));
  var base10 = Math.floor(Math.max(minBase1000 * 3, realBase10));
  var realBase1000 = Math.floor(realBase10 / 3);
  var base1000 = Math.floor(base10 / 3);
  var symbolIndex = (0, _math.clamp)(SI_BASE_TEN_INDEX + base1000, 0, SI_BASE_TEN_UNIT.length);
  var symbol = SI_BASE_TEN_UNIT[symbolIndex];
  var scaledNumber = value / Math.pow(1000, base1000);
  var scaledPrecision = realBase1000 > minBase1000 ? 2 + base1000 * 3 - base10 : 0; // TODO: Make numbers bigger than precision value show
  // up to 2 decimal numbers.

  var finalString = (0, _math.toFixed)(scaledNumber, scaledPrecision) + ' ' + symbol + ' ' + unit;
  return finalString.trim();
};
/**
 * Formats decisecond count into HH::MM::SS display by default
 * "short" format does not pad and adds hms suffixes
 */


exports.formatSiBaseTenUnit = formatSiBaseTenUnit;

var formatTime = function formatTime(val, formatType) {
  // THERE IS AS YET INSUFFICIENT DATA FOR A MEANINGFUL ANSWER
  // HH:MM:SS
  // 00:02:13
  var seconds = (0, _math.toFixed)(Math.floor(val / 10 % 60));
  var minutes = (0, _math.toFixed)(Math.floor(val / (10 * 60) % 60));
  var hours = (0, _math.toFixed)(Math.floor(val / (10 * 60 * 60) % 24));

  switch (formatType) {
    case "short":
      {
        var hours_truncated = hours > 0 ? hours + "h" : "";
        var minutes_truncated = minutes > 0 ? minutes + "m" : "";
        var seconds_truncated = seconds > 0 ? seconds + "s" : "";
        return "" + hours_truncated + minutes_truncated + seconds_truncated;
      }

    default:
      {
        var seconds_padded = seconds.padStart(2, "0");
        var minutes_padded = minutes.padStart(2, "0");
        var hours_padded = hours.padStart(2, "0");
        return hours_padded + ":" + minutes_padded + ":" + seconds_padded;
      }
  }
};

exports.formatTime = formatTime;

/***/ }),

/***/ 6965:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.listenForKeyEvents = exports.setupHotKeys = exports.releaseHeldKeys = exports.releaseHotKey = exports.acquireHotKey = void 0;

var keycodes = _interopRequireWildcard(__webpack_require__(57890));

var _events = __webpack_require__(99397);

var _logging = __webpack_require__(79926);

function _getRequireWildcardCache(nodeInterop) { if (typeof WeakMap !== "function") return null; var cacheBabelInterop = new WeakMap(); var cacheNodeInterop = new WeakMap(); return (_getRequireWildcardCache = function _getRequireWildcardCache(nodeInterop) { return nodeInterop ? cacheNodeInterop : cacheBabelInterop; })(nodeInterop); }

function _interopRequireWildcard(obj, nodeInterop) { if (!nodeInterop && obj && obj.__esModule) { return obj; } if (obj === null || typeof obj !== "object" && typeof obj !== "function") { return { "default": obj }; } var cache = _getRequireWildcardCache(nodeInterop); if (cache && cache.has(obj)) { return cache.get(obj); } var newObj = {}; var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor; for (var key in obj) { if (key !== "default" && Object.prototype.hasOwnProperty.call(obj, key)) { var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null; if (desc && (desc.get || desc.set)) { Object.defineProperty(newObj, key, desc); } else { newObj[key] = obj[key]; } } } newObj["default"] = obj; if (cache) { cache.set(obj, newObj); } return newObj; }

function _createForOfIteratorHelperLoose(o, allowArrayLike) { var it = typeof Symbol !== "undefined" && o[Symbol.iterator] || o["@@iterator"]; if (it) return (it = it.call(o)).next.bind(it); if (Array.isArray(o) || (it = _unsupportedIterableToArray(o)) || allowArrayLike && o && typeof o.length === "number") { if (it) o = it; var i = 0; return function () { if (i >= o.length) return { done: true }; return { done: false, value: o[i++] }; }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }

function _unsupportedIterableToArray(o, minLen) { if (!o) return; if (typeof o === "string") return _arrayLikeToArray(o, minLen); var n = Object.prototype.toString.call(o).slice(8, -1); if (n === "Object" && o.constructor) n = o.constructor.name; if (n === "Map" || n === "Set") return Array.from(o); if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen); }

function _arrayLikeToArray(arr, len) { if (len == null || len > arr.length) len = arr.length; for (var i = 0, arr2 = new Array(len); i < len; i++) { arr2[i] = arr[i]; } return arr2; }

var logger = (0, _logging.createLogger)('hotkeys'); // BYOND macros, in `key: command` format.

var byondMacros = {}; // Default set of acquired keys, which will not be sent to BYOND.

var hotKeysAcquired = [keycodes.KEY_ESCAPE, keycodes.KEY_ENTER, keycodes.KEY_SPACE, keycodes.KEY_TAB, keycodes.KEY_CTRL, keycodes.KEY_SHIFT, keycodes.KEY_UP, keycodes.KEY_DOWN, keycodes.KEY_LEFT, keycodes.KEY_RIGHT, keycodes.KEY_F5]; // State of passed-through keys.

var keyState = {}; // Custom listeners for key events

var keyListeners = [];
/**
 * Converts a browser keycode to BYOND keycode.
 */

var keyCodeToByond = function keyCodeToByond(keyCode) {
  if (keyCode === 16) return 'Shift';
  if (keyCode === 17) return 'Ctrl';
  if (keyCode === 18) return 'Alt';
  if (keyCode === 33) return 'Northeast';
  if (keyCode === 34) return 'Southeast';
  if (keyCode === 35) return 'Southwest';
  if (keyCode === 36) return 'Northwest';
  if (keyCode === 37) return 'West';
  if (keyCode === 38) return 'North';
  if (keyCode === 39) return 'East';
  if (keyCode === 40) return 'South';
  if (keyCode === 45) return 'Insert';
  if (keyCode === 46) return 'Delete';

  if (keyCode >= 48 && keyCode <= 57 || keyCode >= 65 && keyCode <= 90) {
    return String.fromCharCode(keyCode);
  }

  if (keyCode >= 96 && keyCode <= 105) {
    return 'Numpad' + (keyCode - 96);
  }

  if (keyCode >= 112 && keyCode <= 123) {
    return 'F' + (keyCode - 111);
  }

  if (keyCode === 188) return ',';
  if (keyCode === 189) return '-';
  if (keyCode === 190) return '.';
};
/**
 * Keyboard passthrough logic. This allows you to keep doing things
 * in game while the browser window is focused.
 */


var handlePassthrough = function handlePassthrough(key) {
  var keyString = String(key); // In addition to F5, support reloading with Ctrl+R and Ctrl+F5

  if (keyString === 'Ctrl+F5' || keyString === 'Ctrl+R') {
    location.reload();
    return;
  } // Prevent passthrough on Ctrl+F


  if (keyString === 'Ctrl+F') {
    return;
  } // NOTE: Alt modifier is pretty bad and sticky in IE11.


  if (key.event.defaultPrevented || key.isModifierKey() || hotKeysAcquired.includes(key.code)) {
    return;
  }

  var byondKeyCode = keyCodeToByond(key.code);

  if (!byondKeyCode) {
    return;
  } // Macro


  var macro = byondMacros[byondKeyCode];

  if (macro) {
    logger.debug('macro', macro);
    return Byond.command(macro);
  } // KeyDown


  if (key.isDown() && !keyState[byondKeyCode]) {
    keyState[byondKeyCode] = true;
    var command = "KeyDown \"" + byondKeyCode + "\"";
    logger.debug(command);
    return Byond.command(command);
  } // KeyUp


  if (key.isUp() && keyState[byondKeyCode]) {
    keyState[byondKeyCode] = false;

    var _command = "KeyUp \"" + byondKeyCode + "\"";

    logger.debug(_command);
    return Byond.command(_command);
  }
};
/**
 * Acquires a lock on the hotkey, which prevents it from being
 * passed through to BYOND.
 */


var acquireHotKey = function acquireHotKey(keyCode) {
  hotKeysAcquired.push(keyCode);
};
/**
 * Makes the hotkey available to BYOND again.
 */


exports.acquireHotKey = acquireHotKey;

var releaseHotKey = function releaseHotKey(keyCode) {
  var index = hotKeysAcquired.indexOf(keyCode);

  if (index >= 0) {
    hotKeysAcquired.splice(index, 1);
  }
};

exports.releaseHotKey = releaseHotKey;

var releaseHeldKeys = function releaseHeldKeys() {
  for (var _i = 0, _Object$keys = Object.keys(keyState); _i < _Object$keys.length; _i++) {
    var byondKeyCode = _Object$keys[_i];

    if (keyState[byondKeyCode]) {
      keyState[byondKeyCode] = false;
      logger.log("releasing key \"" + byondKeyCode + "\"");
      Byond.command("KeyUp \"" + byondKeyCode + "\"");
    }
  }
};

exports.releaseHeldKeys = releaseHeldKeys;

var setupHotKeys = function setupHotKeys() {
  // Read macros
  Byond.winget('default.*').then(function (data) {
    // Group each macro by ref
    var groupedByRef = {};

    for (var _i2 = 0, _Object$keys2 = Object.keys(data); _i2 < _Object$keys2.length; _i2++) {
      var _key = _Object$keys2[_i2];

      var keyPath = _key.split('.');

      var ref = keyPath[1];
      var prop = keyPath[2];

      if (ref && prop) {
        // This piece of code imperatively adds each property to a
        // ByondSkinMacro object in the order we meet it, which is hard
        // to express safely in typescript.
        if (!groupedByRef[ref]) {
          groupedByRef[ref] = {};
        }

        groupedByRef[ref][prop] = data[_key];
      }
    } // Insert macros


    var escapedQuotRegex = /\\"/g;

    var unescape = function unescape(str) {
      return str.substring(1, str.length - 1).replace(escapedQuotRegex, '"');
    };

    for (var _i3 = 0, _Object$keys3 = Object.keys(groupedByRef); _i3 < _Object$keys3.length; _i3++) {
      var _ref = _Object$keys3[_i3];
      var macro = groupedByRef[_ref];
      var byondKeyName = unescape(macro.name);
      byondMacros[byondKeyName] = unescape(macro.command);
    }

    logger.debug('loaded macros', byondMacros);
  }); // Setup event handlers

  _events.globalEvents.on('window-blur', function () {
    releaseHeldKeys();
  });

  _events.globalEvents.on('key', function (key) {
    for (var _iterator = _createForOfIteratorHelperLoose(keyListeners), _step; !(_step = _iterator()).done;) {
      var keyListener = _step.value;
      keyListener(key);
    }

    handlePassthrough(key);
  });
};
/**
 * Registers for any key events, such as key down or key up.
 * This should be preferred over directly connecting to keydown/keyup
 * as it lets tgui prevent the key from reaching BYOND.
 *
 * If using in a component, prefer KeyListener, which automatically handles
 * stopping listening when unmounting.
 *
 * @param callback The function to call whenever a key event occurs
 * @returns A callback to stop listening
 */


exports.setupHotKeys = setupHotKeys;

var listenForKeyEvents = function listenForKeyEvents(callback) {
  keyListeners.push(callback);
  var removed = false;
  return function () {
    if (removed) {
      return;
    }

    removed = true;
    keyListeners.splice(keyListeners.indexOf(callback), 1);
  };
};

exports.listenForKeyEvents = listenForKeyEvents;

/***/ }),

/***/ 64008:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.AirlockElectronics = exports.AirLockMainSection = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var _AccessList = __webpack_require__(52126);

var AirLockMainSection = function AirLockMainSection(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var _data$accesses = data.accesses,
      accesses = _data$accesses === void 0 ? [] : _data$accesses,
      oneAccess = data.oneAccess,
      passedName = data.passedName,
      _data$regions = data.regions,
      regions = _data$regions === void 0 ? [] : _data$regions,
      unres_direction = data.unres_direction,
      lastOperator = data.lastOperator;
  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    "title": "Main",
    children: [(0, _inferno.createComponentVNode)(2, _components.NoticeBox, {
      "info": true,
      children: ["Last operator: ", lastOperator, "."]
    }), (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
      children: [(0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Access Required",
        children: (0, _inferno.createComponentVNode)(2, _components.Button, {
          "icon": oneAccess ? 'unlock' : 'lock',
          "content": oneAccess ? 'One' : 'All',
          "onClick": function () {
            function onClick() {
              return act('one_access');
            }

            return onClick;
          }()
        })
      }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Unrestricted Access",
        children: [(0, _inferno.createComponentVNode)(2, _components.Button, {
          "icon": unres_direction & 1 ? 'check-square-o' : 'square-o',
          "content": "North",
          "selected": unres_direction & 1,
          "onClick": function () {
            function onClick() {
              return act('direc_set', {
                unres_direction: '1'
              });
            }

            return onClick;
          }()
        }), (0, _inferno.createComponentVNode)(2, _components.Button, {
          "icon": unres_direction & 2 ? 'check-square-o' : 'square-o',
          "content": "South",
          "selected": unres_direction & 2,
          "onClick": function () {
            function onClick() {
              return act('direc_set', {
                unres_direction: '2'
              });
            }

            return onClick;
          }()
        }), (0, _inferno.createComponentVNode)(2, _components.Button, {
          "icon": unres_direction & 4 ? 'check-square-o' : 'square-o',
          "content": "East",
          "selected": unres_direction & 4,
          "onClick": function () {
            function onClick() {
              return act('direc_set', {
                unres_direction: '4'
              });
            }

            return onClick;
          }()
        }), (0, _inferno.createComponentVNode)(2, _components.Button, {
          "icon": unres_direction & 8 ? 'check-square-o' : 'square-o',
          "content": "West",
          "selected": unres_direction & 8,
          "onClick": function () {
            function onClick() {
              return act('direc_set', {
                unres_direction: '8'
              });
            }

            return onClick;
          }()
        })]
      }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Airlock Name",
        children: (0, _inferno.createComponentVNode)(2, _components.Input, {
          "fluid": true,
          "maxLength": 30,
          "value": passedName,
          "onChange": function () {
            function onChange(e, value) {
              return act('passedName', {
                passedName: value
              });
            }

            return onChange;
          }()
        })
      })]
    }), (0, _inferno.createComponentVNode)(2, _AccessList.AccessList, {
      "accesses": data.regions,
      "selectedList": data.accesses,
      "accessMod": function () {
        function accessMod(ref) {
          return act('set', {
            access: ref
          });
        }

        return accessMod;
      }(),
      "grantAll": function () {
        function grantAll() {
          return act('grant_all');
        }

        return grantAll;
      }(),
      "denyAll": function () {
        function denyAll() {
          return act('clear_all');
        }

        return denyAll;
      }(),
      "grantDep": function () {
        function grantDep(ref) {
          return act('grant_region', {
            region: ref
          });
        }

        return grantDep;
      }(),
      "denyDep": function () {
        function denyDep(ref) {
          return act('deny_region', {
            region: ref
          });
        }

        return denyDep;
      }()
    })]
  });
};

exports.AirLockMainSection = AirLockMainSection;

var AirlockElectronics = function AirlockElectronics(props, context) {
  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "width": 420,
    "height": 550,
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      children: (0, _inferno.createComponentVNode)(2, AirLockMainSection)
    })
  });
};

exports.AirlockElectronics = AirlockElectronics;

/***/ }),

/***/ 72272:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Loader = exports.AlertModal = void 0;

var _inferno = __webpack_require__(96323);

var _math = __webpack_require__(97622);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var _keycodes = __webpack_require__(57890);

function _inheritsLoose(subClass, superClass) { subClass.prototype = Object.create(superClass.prototype); subClass.prototype.constructor = subClass; _setPrototypeOf(subClass, superClass); }

function _setPrototypeOf(o, p) { _setPrototypeOf = Object.setPrototypeOf || function () { function _setPrototypeOf(o, p) { o.__proto__ = p; return o; } return _setPrototypeOf; }(); return _setPrototypeOf(o, p); }

var AlertModal = /*#__PURE__*/function (_Component) {
  _inheritsLoose(AlertModal, _Component);

  function AlertModal() {
    var _this;

    _this = _Component.call(this) || this;
    _this.buttonRefs = [(0, _inferno.createRef)()];
    _this.state = {
      current: 0
    };
    return _this;
  }

  var _proto = AlertModal.prototype;

  _proto.componentDidMount = function () {
    function componentDidMount() {
      var _useBackend = (0, _backend.useBackend)(this.context),
          data = _useBackend.data;

      var buttons = data.buttons;
      var current = this.state.current;
      var button = this.buttonRefs[current].current; // Fill ref array with refs for other buttons

      for (var i = 1; i < buttons.length; i++) {
        this.buttonRefs.push((0, _inferno.createRef)());
      }

      setTimeout(function () {
        return button.focus();
      }, 1);
    }

    return componentDidMount;
  }();

  _proto.setCurrent = function () {
    function setCurrent(current, isArrowKey) {
      var _useBackend2 = (0, _backend.useBackend)(this.context),
          data = _useBackend2.data;

      var buttons = data.buttons; // Mimic alert() behavior for tabs and arrow keys

      if (current >= buttons.length) {
        current = isArrowKey ? current - 1 : 0;
      } else if (current < 0) {
        current = isArrowKey ? 0 : buttons.length - 1;
      }

      var button = this.buttonRefs[current].current; // Prevents an error from occurring on close

      if (button) {
        setTimeout(function () {
          return button.focus();
        }, 1);
      }

      this.setState({
        current: current
      });
    }

    return setCurrent;
  }();

  _proto.render = function () {
    function render() {
      var _this2 = this;

      var _useBackend3 = (0, _backend.useBackend)(this.context),
          act = _useBackend3.act,
          data = _useBackend3.data;

      var title = data.title,
          message = data.message,
          buttons = data.buttons,
          timeout = data.timeout;
      var current = this.state.current;

      var focusCurrentButton = function () {
        function focusCurrentButton() {
          return _this2.setCurrent(current, false);
        }

        return focusCurrentButton;
      }();

      return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
        "title": title,
        "width": 350,
        "height": 150,
        "canClose": timeout > 0,
        children: [timeout && (0, _inferno.createComponentVNode)(2, Loader, {
          "value": timeout
        }), (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
          "onFocus": focusCurrentButton,
          "onClick": focusCurrentButton,
          children: (0, _inferno.createComponentVNode)(2, _components.Section, {
            "fill": true,
            children: (0, _inferno.createComponentVNode)(2, _components.Flex, {
              "direction": "column",
              children: [(0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
                "grow": 1,
                children: (0, _inferno.createComponentVNode)(2, _components.Flex, {
                  "direction": "column",
                  "className": "AlertModal__Message",
                  "textAlign": "center",
                  children: (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
                    children: (0, _inferno.createComponentVNode)(2, _components.Box, {
                      "m": 1,
                      children: message
                    })
                  })
                })
              }), (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
                "my": 2,
                children: (0, _inferno.createComponentVNode)(2, _components.Flex, {
                  "className": "AlertModal__Buttons",
                  children: buttons.map(function (button, buttonIndex) {
                    return (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
                      "mx": 1,
                      children: (0, _inferno.createVNode)(1, "div", "Button Button--color--default", button, 0, {
                        "px": 3,
                        "onClick": function () {
                          function onClick() {
                            return act("choose", {
                              choice: button
                            });
                          }

                          return onClick;
                        }(),
                        "onKeyDown": function () {
                          function onKeyDown(e) {
                            var keyCode = window.event ? e.which : e.keyCode;
                            /**
                             * Simulate a click when pressing space or enter,
                             * allow keyboard navigation, override tab behavior
                             */

                            if (keyCode === _keycodes.KEY_SPACE || keyCode === _keycodes.KEY_ENTER) {
                              act("choose", {
                                choice: button
                              });
                            } else if (keyCode === _keycodes.KEY_LEFT || e.shiftKey && keyCode === _keycodes.KEY_TAB) {
                              _this2.setCurrent(current - 1, keyCode === _keycodes.KEY_LEFT);
                            } else if (keyCode === _keycodes.KEY_RIGHT || keyCode === _keycodes.KEY_TAB) {
                              _this2.setCurrent(current + 1, keyCode === _keycodes.KEY_RIGHT);
                            }
                          }

                          return onKeyDown;
                        }()
                      }, null, _this2.buttonRefs[buttonIndex])
                    }, buttonIndex);
                  })
                })
              })]
            })
          })
        })]
      });
    }

    return render;
  }();

  return AlertModal;
}(_inferno.Component);

exports.AlertModal = AlertModal;

var Loader = function Loader(props) {
  var value = props.value;
  return (0, _inferno.createVNode)(1, "div", "AlertModal__Loader", (0, _inferno.createComponentVNode)(2, _components.Box, {
    "className": "AlertModal__LoaderProgress",
    "style": {
      width: (0, _math.clamp01)(value) * 100 + '%'
    }
  }), 2);
};

exports.Loader = Loader;

/***/ }),

/***/ 48349:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Apc = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var _InterfaceLockNoticeBox = __webpack_require__(19447);

var Apc = function Apc(props, context) {
  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "width": 500,
    "height": 445,
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      "scrollable": true,
      children: (0, _inferno.createComponentVNode)(2, ApcContent)
    })
  });
};

exports.Apc = Apc;
var powerStatusMap = {
  2: {
    color: 'good',
    externalPowerText: 'Питание от сети',
    chargingText: 'Заряжен'
  },
  1: {
    color: 'average',
    externalPowerText: 'Низкое напряжение в сети',
    chargingText: 'Идёт зарядка...'
  },
  0: {
    color: 'bad',
    externalPowerText: 'Нет внешнего питания',
    chargingText: 'Не заряжается'
  }
};

var ApcContent = function ApcContent(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var locked = data.locked,
      isOperating = data.isOperating,
      externalPower = data.externalPower,
      powerCellStatus = data.powerCellStatus,
      powerCellCharge = data.powerCellCharge,
      chargeMode = data.chargeMode,
      charging = data.charging,
      totalLoad = data.totalLoad,
      coverLocked = data.coverLocked,
      siliconUser = data.siliconUser,
      malfCanHack = data.malfCanHack,
      nightshiftLights = data.nightshiftLights,
      smartlightMode = data.smartlightMode,
      powerChannels = data.powerChannels;
  var isLocked = locked && !siliconUser;
  var externalPowerStatus = powerStatusMap[externalPower] || powerStatusMap[0];
  var chargingStatus = powerStatusMap[charging] || powerStatusMap[0];
  var channelArray = powerChannels || [];
  return (0, _inferno.createFragment)([(0, _inferno.createComponentVNode)(2, _InterfaceLockNoticeBox.InterfaceLockNoticeBox), (0, _inferno.createComponentVNode)(2, _components.Section, {
    "title": "Сеть",
    children: (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
      children: [(0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Вводный автомат",
        "color": externalPowerStatus.color,
        "buttons": (0, _inferno.createComponentVNode)(2, _components.Button, {
          "icon": isOperating ? 'power-off' : 'times',
          "content": isOperating ? 'Вкл.' : 'Выкл.',
          "selected": isOperating && !isLocked,
          "disabled": isLocked,
          "onClick": function () {
            function onClick() {
              return act('breaker');
            }

            return onClick;
          }()
        }),
        children: ["[ ", externalPowerStatus.externalPowerText, " ]"]
      }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Аккумулятор",
        children: !!powerCellStatus && (0, _inferno.createComponentVNode)(2, _components.ProgressBar, {
          "color": "good",
          "fractionDigits": 1,
          "value": powerCellCharge * 0.01
        }) || (0, _inferno.createComponentVNode)(2, _components.Box, {
          "color": "bad",
          children: "\u0418\u0437\u0432\u043B\u0435\u0447\u0451\u043D"
        })
      }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Режим зарядки",
        "color": chargingStatus.color,
        "buttons": (0, _inferno.createComponentVNode)(2, _components.Button, {
          "icon": chargeMode ? 'sync' : 'times',
          "content": chargeMode ? 'Авт.' : 'Выкл.',
          "disabled": isLocked,
          "onClick": function () {
            function onClick() {
              return act('charge');
            }

            return onClick;
          }()
        }),
        children: ["[ ", chargingStatus.chargingText, " ]"]
      })]
    })
  }), (0, _inferno.createComponentVNode)(2, _components.Section, {
    "title": "Автоматы",
    children: (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
      children: [channelArray.map(function (channel) {
        var topicParams = channel.topicParams;
        return (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
          "label": channel.title,
          "buttons": (0, _inferno.createFragment)([(0, _inferno.createComponentVNode)(2, _components.Box, {
            "inline": true,
            "mx": 2,
            "color": channel.status >= 2 ? 'good' : 'bad',
            children: channel.status >= 2 ? 'Вкл.' : 'Выкл.'
          }), (0, _inferno.createComponentVNode)(2, _components.Button, {
            "icon": "sync",
            "content": "Авт.",
            "selected": !isLocked && (channel.status === 1 || channel.status === 3),
            "disabled": isLocked,
            "onClick": function () {
              function onClick() {
                return act('channel', topicParams.auto);
              }

              return onClick;
            }()
          }), (0, _inferno.createComponentVNode)(2, _components.Button, {
            "icon": "power-off",
            "content": "Вкл.",
            "selected": !isLocked && channel.status === 2,
            "disabled": isLocked,
            "onClick": function () {
              function onClick() {
                return act('channel', topicParams.on);
              }

              return onClick;
            }()
          }), (0, _inferno.createComponentVNode)(2, _components.Button, {
            "icon": "times",
            "content": "Выкл.",
            "selected": !isLocked && channel.status === 0,
            "disabled": isLocked,
            "onClick": function () {
              function onClick() {
                return act('channel', topicParams.off);
              }

              return onClick;
            }()
          })], 4),
          children: channel.powerLoad
        }, channel.title);
      }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Общая нагрузка",
        children: (0, _inferno.createVNode)(1, "b", null, totalLoad, 0)
      })]
    })
  }), (0, _inferno.createComponentVNode)(2, _components.Section, {
    "title": "Разное",
    "buttons": !!siliconUser && (0, _inferno.createFragment)([!!malfCanHack && (0, _inferno.createComponentVNode)(2, _components.Button, {
      "icon": "terminal",
      "content": "Перепрошить",
      "color": "bad",
      "onClick": function () {
        function onClick() {
          return act('hack');
        }

        return onClick;
      }()
    }), (0, _inferno.createComponentVNode)(2, _components.Button, {
      "icon": "lightbulb-o",
      "content": "Перегрузить",
      "onClick": function () {
        function onClick() {
          return act('overload');
        }

        return onClick;
      }()
    })], 0),
    children: (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
      children: [(0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Крышка",
        "buttons": (0, _inferno.createComponentVNode)(2, _components.Button, {
          "icon": coverLocked ? 'lock' : 'unlock',
          "content": coverLocked ? 'Заблокирована' : 'Разблокирована',
          "disabled": isLocked,
          "onClick": function () {
            function onClick() {
              return act('cover');
            }

            return onClick;
          }()
        })
      }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Ночной режим",
        "buttons": (0, _inferno.createComponentVNode)(2, _components.Button, {
          "icon": "lightbulb-o",
          "content": nightshiftLights ? 'Вкл' : 'Выкл',
          "onClick": function () {
            function onClick() {
              return act('toggle_nightshift');
            }

            return onClick;
          }()
        })
      }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Текущий режим освещения",
        "buttons": (0, _inferno.createComponentVNode)(2, _components.Button, {
          "icon": "lightbulb-o",
          "content": smartlightMode,
          "onClick": function () {
            function onClick() {
              return act('change_smartlight');
            }

            return onClick;
          }()
        })
      })]
    })
  })], 4);
};

/***/ }),

/***/ 47533:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Materials = exports.Autolathe = void 0;

var _inferno = __webpack_require__(96323);

var _fp = __webpack_require__(32416);

var _collections = __webpack_require__(1397);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var _string = __webpack_require__(21118);

var _react = __webpack_require__(1060);

var _format = __webpack_require__(1033);

var _math = __webpack_require__(97622);

var Autolathe = function Autolathe(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var recipes = data.recipes,
      busy = data.busy,
      materials = data.materials,
      categories = data.categories,
      coeff = data.coeff;

  var _useSharedState = (0, _backend.useSharedState)(context, "category", 0),
      category = _useSharedState[0],
      setCategory = _useSharedState[1];

  var _useSharedState2 = (0, _backend.useSharedState)(context, "searchText", ""),
      searchText = _useSharedState2[0],
      setSearchText = _useSharedState2[1];

  var testSearch = (0, _string.createSearch)(searchText, function (recipe) {
    return recipe.name;
  });
  var recipesToShow = (0, _fp.flow)([(0, _collections.filter)(function (recipe) {
    return recipe.category === categories[category] || categories[category] === "All";
  }), searchText && (0, _collections.filter)(testSearch), (0, _collections.sortBy)(function (recipe) {
    return recipe.name.toLowerCase();
  })])(recipes);
  var categorieToShow = (0, _fp.flow)([(0, _collections.sortBy)(function (category) {
    return category.toLowerCase();
  })])(categories);
  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "width": 550,
    "height": 700,
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      "scrollable": true,
      children: [(0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": "Materials",
        children: (0, _inferno.createComponentVNode)(2, _components.Flex, {
          "wrap": "wrap",
          children: (0, _inferno.createComponentVNode)(2, Materials)
        })
      }), (0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": "Recipes",
        "buttons": (0, _inferno.createComponentVNode)(2, _components.Dropdown, {
          "width": "190px",
          "options": categorieToShow,
          "selected": categories[category],
          "onSelected": function () {
            function onSelected(val) {
              return setCategory(categories.indexOf(val));
            }

            return onSelected;
          }()
        }),
        children: [(0, _inferno.createComponentVNode)(2, _components.Input, {
          "autoFocus": true,
          "fluid": true,
          "placeholder": "Search for...",
          "onInput": function () {
            function onInput(e, v) {
              return setSearchText(v);
            }

            return onInput;
          }(),
          "mb": 1
        }), recipesToShow.map(function (recipe) {
          return (0, _inferno.createComponentVNode)(2, _components.Flex, {
            "justify": "space-between",
            "align": "center",
            children: [(0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
              "mr": 1,
              children: (0, _inferno.createVNode)(1, "span", (0, _react.classes)(['autolathe32x32', recipe.path]), null, 1, {
                "style": {
                  'vertical-align': 'middle',
                  'horizontal-align': 'middle'
                }
              })
            }), (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
              "grow": 1,
              children: [(0, _inferno.createComponentVNode)(2, _components.Button, {
                "color": recipe.hidden && "red" || null,
                "icon": "hammer",
                "iconSpin": busy === recipe.name,
                "disabled": !canBeMade(recipe, materials),
                "onClick": function () {
                  function onClick() {
                    return act("make", {
                      make: recipe.ref
                    });
                  }

                  return onClick;
                }(),
                children: (0, _string.toTitleCase)(recipe.name)
              }), recipe.max_mult > 1 && (0, _inferno.createComponentVNode)(2, _components.Box, {
                "as": "span",
                children: [5, 10, recipe.max_mult / 2 >> 0, recipe.max_mult].map(function (mult) {
                  return MultButton(recipe, materials, act, mult);
                })
              })]
            }), (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
              "width": "30%",
              children: (0, _inferno.createComponentVNode)(2, _components.Flex, {
                "direction": "row",
                "visibility": "collapse",
                children: recipe.requirements && Object.keys(recipe.requirements).map(function (mat) {
                  return (0, _inferno.createComponentVNode)(2, _components.Flex, {
                    "width": "100%",
                    children: recipe.requirements[mat] > 0 && (0, _inferno.createComponentVNode)(2, MaterialAmount, {
                      "name": mat,
                      "amount": recipe.requirements[mat] / coeff,
                      "formatsi": true,
                      "csspath": materials.find(function (val) {
                        return val.name === mat;
                      }).path,
                      "width": "50%"
                    }) || (0, _inferno.createComponentVNode)(2, _components.Flex, {
                      "width": "50%"
                    })
                  }, mat);
                }) || (0, _inferno.createComponentVNode)(2, _components.Box, {
                  children: "No resources required."
                })
              })
            })]
          }, recipe.ref);
        })]
      })]
    })
  });
};

exports.Autolathe = Autolathe;

var MaterialAmount = function MaterialAmount(props, context) {
  var name = props.name,
      csspath = props.csspath,
      amount = props.amount,
      color = props.color,
      style = props.style,
      direction = props.direction,
      width = props.width,
      formatsi = props.formatsi,
      formatmoney = props.formatmoney;
  var amountDisplay = "0";

  if (amount < 1 && amount > 0) {
    amountDisplay = (0, _math.toFixed)(amount, 2);
  } else if (formatsi) {
    amountDisplay = (0, _format.formatSiUnit)(amount, 0).replace(" ", "");
  } else if (formatmoney) {
    amountDisplay = (0, _format.formatMoney)(amount);
  } else {
    amountDisplay = amount;
  }

  return (0, _inferno.createComponentVNode)(2, _components.Flex, {
    "direction": direction,
    "align": "center",
    "width": width,
    children: [(0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
      children: (0, _inferno.createComponentVNode)(2, _components.Box, {
        "className": (0, _react.classes)(['sheetmaterials32x32', csspath]),
        "position": "relative",
        "style": style,
        children: (0, _inferno.createComponentVNode)(2, _components.Tooltip, {
          "position": "bottom",
          "content": (0, _string.toTitleCase)(name)
        })
      })
    }), (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
      children: (0, _inferno.createComponentVNode)(2, _components.Box, {
        "textColor": color,
        "style": {
          "text-align": "center"
        },
        children: amountDisplay
      })
    })]
  });
};

var canBeMade = function canBeMade(recipe, materials, mult) {
  if (mult === void 0) {
    mult = 1;
  }

  if (recipe.requirements === null) {
    return true;
  }

  var recipeRequiredMaterials = Object.keys(recipe.requirements);

  var _loop = function _loop() {
    var mat_id = _recipeRequiredMateri[_i];
    var material = materials.find(function (val) {
      return val.name === mat_id;
    });

    if (!material) {
      return "continue"; // yes, if we cannot find the material, we just ignore it :V
    }

    if (material.amount < recipe.requirements[mat_id] * mult) {
      return {
        v: false
      };
    }
  };

  for (var _i = 0, _recipeRequiredMateri = recipeRequiredMaterials; _i < _recipeRequiredMateri.length; _i++) {
    var _ret = _loop();

    if (_ret === "continue") continue;
    if (typeof _ret === "object") return _ret.v;
  }

  return true;
};

var MultButton = function MultButton(recipe, materials, act, mult) {
  if (mult <= recipe.max_mult) {
    return (0, _inferno.createComponentVNode)(2, _components.Button, {
      "color": recipe.hidden && "red" || null,
      "disabled": !canBeMade(recipe, materials, mult),
      "onClick": function () {
        function onClick() {
          return act("make", {
            make: recipe.ref,
            multiplier: mult
          });
        }

        return onClick;
      }(),
      children: ["x", mult]
    });
  }
};

var Materials = function Materials(props, context) {
  var _useBackend2 = (0, _backend.useBackend)(context),
      data = _useBackend2.data;

  var displayAllMat = props.displayAllMat;
  var materials = data.materials || [];
  var display_materials = materials.filter(function (mat) {
    return displayAllMat || mat.amount > 0;
  });

  if (display_materials.length === 0) {
    return (0, _inferno.createComponentVNode)(2, _components.Box, {
      "width": "100%",
      "textAlign": "center",
      children: [(0, _inferno.createComponentVNode)(2, _components.Icon, {
        "textAlign": "center",
        "size": 5,
        "name": "inbox"
      }), (0, _inferno.createVNode)(1, "br"), (0, _inferno.createVNode)(1, "b", null, "No Materials Loaded.", 16)]
    });
  }

  return (0, _inferno.createComponentVNode)(2, _components.Flex, {
    "wrap": "wrap",
    children: display_materials.map(function (material) {
      return (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
        "width": "80px",
        children: [(0, _inferno.createComponentVNode)(2, MaterialAmount, {
          "name": material.name,
          "amount": material.amount,
          "csspath": material.path,
          "formatsi": true,
          "direction": "column"
        }), (0, _inferno.createComponentVNode)(2, _components.Box, {
          "mt": 1,
          "style": {
            "text-align": "center"
          }
        })]
      }, material.name) || null;
    })
  });
};

exports.Materials = Materials;

/***/ }),

/***/ 15466:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.BloomEdit = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var BloomEdit = function BloomEdit(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var glow_brightness_base = data.glow_brightness_base,
      glow_brightness_power = data.glow_brightness_power,
      glow_contrast_base = data.glow_contrast_base,
      glow_contrast_power = data.glow_contrast_power,
      exposure_brightness_base = data.exposure_brightness_base,
      exposure_brightness_power = data.exposure_brightness_power,
      exposure_contrast_base = data.exposure_contrast_base,
      exposure_contrast_power = data.exposure_contrast_power;
  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "title": "BloomEdit",
    "width": 500,
    "height": 500,
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      children: (0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": "Bloom Edit",
        children: (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
          children: [(0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Lamp Brightness Base",
            children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
              "inline": true,
              children: "\u0411\u0430\u0437\u043E\u0432\u0430\u044F \u044F\u0440\u043A\u043E\u0441\u0442\u044C \u043B\u0430\u043C\u043F\u043E\u0447\u043A\u0438, \u043D\u0435\u0437\u0430\u0432\u0438\u0441\u0438\u043C\u043E \u043E\u0442 \u0441\u0432\u0435\u0442\u0430"
            }), (0, _inferno.createComponentVNode)(2, _components.NumberInput, {
              "fluid": true,
              "value": glow_brightness_base,
              "minValue": -10,
              "maxValue": 10,
              "step": 0.01,
              "width": "20px",
              "onChange": function () {
                function onChange(e, value) {
                  return act('glow_brightness_base', {
                    value: value
                  });
                }

                return onChange;
              }()
            })]
          }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Lamp Brightness Power",
            children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
              "inline": true,
              children: "\u042F\u0440\u043A\u043E\u0441\u0442\u044C \u043B\u0430\u043C\u043F\u043E\u0447\u043A\u0438 \u043D\u0430 power \u0441\u0432\u0435\u0442\u0430"
            }), (0, _inferno.createComponentVNode)(2, _components.NumberInput, {
              "fluid": true,
              "value": glow_brightness_power,
              "minValue": -10,
              "maxValue": 10,
              "step": 0.01,
              "width": "20px",
              "onChange": function () {
                function onChange(e, value) {
                  return act('glow_brightness_power', {
                    value: value
                  });
                }

                return onChange;
              }()
            })]
          }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Lamp Contrast Base",
            children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
              "inline": true,
              children: "\u0411\u0430\u0437\u043E\u0432\u044B\u0439 \u043A\u043E\u043D\u0442\u0440\u0430\u0441\u0442 \u043B\u0430\u043C\u043F\u043E\u0447\u043A\u0438, \u043D\u0435\u0437\u0430\u0432\u0438\u0441\u0438\u043C\u043E \u043E\u0442 \u0441\u0432\u0435\u0442\u0430"
            }), (0, _inferno.createComponentVNode)(2, _components.NumberInput, {
              "fluid": true,
              "value": glow_contrast_base,
              "minValue": -10,
              "maxValue": 10,
              "step": 0.01,
              "width": "20px",
              "onChange": function () {
                function onChange(e, value) {
                  return act('glow_contrast_base', {
                    value: value
                  });
                }

                return onChange;
              }()
            })]
          }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Lamp Contrast Power",
            children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
              "inline": true,
              children: "\u041A\u043E\u043D\u0442\u0440\u0430\u0441\u0442 \u043B\u0430\u043C\u043F\u043E\u0447\u043A\u0438 \u043D\u0430 power \u0441\u0432\u0435\u0442\u0430"
            }), (0, _inferno.createComponentVNode)(2, _components.NumberInput, {
              "fluid": true,
              "value": glow_contrast_power,
              "minValue": -10,
              "maxValue": 10,
              "step": 0.01,
              "width": "20px",
              "onChange": function () {
                function onChange(e, value) {
                  return act('glow_contrast_power', {
                    value: value
                  });
                }

                return onChange;
              }()
            })]
          }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Exposure Brightness Base",
            children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
              "inline": true,
              children: "\u042F\u0440\u043A\u043E\u0441\u0442\u044C \u0441\u0432\u0435\u0447\u0435\u043D\u0438\u044F \u043A\u043E\u043D\u0443\u0441\u0430"
            }), (0, _inferno.createComponentVNode)(2, _components.NumberInput, {
              "fluid": true,
              "value": exposure_brightness_base,
              "minValue": -10,
              "maxValue": 10,
              "step": 0.01,
              "width": "20px",
              "onChange": function () {
                function onChange(e, value) {
                  return act('exposure_brightness_base', {
                    value: value
                  });
                }

                return onChange;
              }()
            })]
          }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Exposure Brightness Power",
            children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
              "inline": true,
              children: "\u042F\u0440\u043A\u043E\u0441\u0442\u044C \u0441\u0432\u0435\u0447\u0435\u043D\u0438\u044F \u043A\u043E\u043D\u0443\u0441\u0430 \u043D\u0430 power \u0441\u0432\u0435\u0442\u0430"
            }), (0, _inferno.createComponentVNode)(2, _components.NumberInput, {
              "fluid": true,
              "value": exposure_brightness_power,
              "minValue": -10,
              "maxValue": 10,
              "step": 0.01,
              "width": "20px",
              "onChange": function () {
                function onChange(e, value) {
                  return act('exposure_brightness_power', {
                    value: value
                  });
                }

                return onChange;
              }()
            })]
          }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Exposure Contrast Base",
            children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
              "inline": true,
              children: "\u041A\u043E\u043D\u0442\u0440\u0430\u0441\u0442 \u0441\u0432\u0435\u0447\u0435\u043D\u0438\u044F \u043A\u043E\u043D\u0443\u0441\u0430"
            }), (0, _inferno.createComponentVNode)(2, _components.NumberInput, {
              "fluid": true,
              "value": exposure_contrast_base,
              "minValue": -10,
              "maxValue": 10,
              "step": 0.01,
              "width": "20px",
              "onChange": function () {
                function onChange(e, value) {
                  return act('exposure_contrast_base', {
                    value: value
                  });
                }

                return onChange;
              }()
            })]
          }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Exposure Contrast Power",
            children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
              "inline": true,
              children: "\u041A\u043E\u043D\u0442\u0440\u0430\u0441\u0442 \u0441\u0432\u0435\u0447\u0435\u043D\u0438\u044F \u043A\u043E\u043D\u0443\u0441\u0430 \u043D\u0430 power \u0441\u0432\u0435\u0442\u0430"
            }), (0, _inferno.createComponentVNode)(2, _components.NumberInput, {
              "fluid": true,
              "value": exposure_contrast_power,
              "minValue": -10,
              "maxValue": 10,
              "step": 0.01,
              "width": "20px",
              "onChange": function () {
                function onChange(e, value) {
                  return act('exposure_contrast_power', {
                    value: value
                  });
                }

                return onChange;
              }()
            })]
          }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Divider), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            children: [(0, _inferno.createComponentVNode)(2, _components.Button, {
              "content": "Перезагрузить лампы с новыми параметрами",
              "onClick": function () {
                function onClick() {
                  return act('update_lamps');
                }

                return onClick;
              }()
            }), (0, _inferno.createComponentVNode)(2, _components.Button, {
              "content": "Сбросить по умолчанию",
              "onClick": function () {
                function onClick() {
                  return act('default');
                }

                return onClick;
              }()
            })]
          })]
        })
      })
    })
  });
};

exports.BloomEdit = BloomEdit;

/***/ }),

/***/ 33897:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.BodyScanner = void 0;

var _inferno = __webpack_require__(96323);

var _math = __webpack_require__(97622);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var stats = [['good', 'Стабильное'], ['average', 'Критическое'], ['bad', 'Мёртв']];
var abnormalities = [['hasBorer', 'bad', 'В лобной доле злокачественное новообразование.'], ['blind', 'bad', 'Катаракта.'], ['hasVirus', 'average', 'В кровотоке вирусный патоген.'], ['nearsighted', 'average', 'Смещение сетчатки.']];
var damages = [['Асфиксия', 'oxyLoss'], ['Мозговые', 'brainLoss'], ['Интоксикация', 'toxLoss'], ['Облучение', 'radLoss'], ['Механические', 'bruteLoss'], ['Генетические', 'cloneLoss'], ['Термические', 'fireLoss'], ['Опьянение', 'drunkenness']];
var damageRange = {
  average: [0.25, 0.5],
  bad: [0.5, Infinity]
};

var mapTwoByTwo = function mapTwoByTwo(a, c) {
  var result = [];

  for (var i = 0; i < a.length; i += 2) {
    result.push(c(a[i], a[i + 1], i));
  }

  return result;
};

var reduceOrganStatus = function reduceOrganStatus(A) {
  return A.length > 0 ? A.filter(function (s) {
    return !!s;
  }).reduce(function (a, s) {
    return (0, _inferno.createFragment)([a, (0, _inferno.createComponentVNode)(2, _components.Box, {
      children: s
    }, s)], 0);
  }, null) : null;
};

var BodyScanner = function BodyScanner(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      data = _useBackend.data;

  var occupied = data.occupied,
      _data$occupant = data.occupant,
      occupant = _data$occupant === void 0 ? {} : _data$occupant;
  var body = occupied ? (0, _inferno.createComponentVNode)(2, BodyScannerMain, {
    "occupant": occupant
  }) : (0, _inferno.createComponentVNode)(2, BodyScannerEmpty);
  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "resizable": true,
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      "scrollable": true,
      "className": "Layout__content--flexColumn",
      children: body
    })
  });
};

exports.BodyScanner = BodyScanner;

var BodyScannerMain = function BodyScannerMain(props) {
  var occupant = props.occupant;
  return (0, _inferno.createComponentVNode)(2, _components.Box, {
    children: [(0, _inferno.createComponentVNode)(2, BodyScannerMainOccupant, {
      "occupant": occupant
    }), (0, _inferno.createComponentVNode)(2, BodyScannerMainAbnormalities, {
      "occupant": occupant
    }), (0, _inferno.createComponentVNode)(2, BodyScannerMainDamage, {
      "occupant": occupant
    }), (0, _inferno.createComponentVNode)(2, BodyScannerMainOrgansExternal, {
      "organs": occupant.extOrgan
    }), (0, _inferno.createComponentVNode)(2, BodyScannerMainOrgansInternal, {
      "organs": occupant.intOrgan
    })]
  });
};

var BodyScannerMainOccupant = function BodyScannerMainOccupant(props, context) {
  var _useBackend2 = (0, _backend.useBackend)(context),
      act = _useBackend2.act,
      data = _useBackend2.data;

  var occupant = data.occupant;
  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    "title": "Пациент",
    "buttons": (0, _inferno.createFragment)([(0, _inferno.createComponentVNode)(2, _components.Button, {
      "icon": "print",
      "onClick": function () {
        function onClick() {
          return act('print_p');
        }

        return onClick;
      }(),
      children: "\u0420\u0430\u0441\u043F\u0435\u0447\u0430\u0442\u0430\u0442\u044C \u043E\u0442\u0447\u0435\u0442"
    }), (0, _inferno.createComponentVNode)(2, _components.Button, {
      "icon": "user-slash",
      "onClick": function () {
        function onClick() {
          return act('ejectify');
        }

        return onClick;
      }(),
      children: "\u0418\u0437\u0432\u043B\u0435\u0447\u044C"
    })], 4),
    children: (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
      children: [(0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Имя",
        children: occupant.name
      }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Здоровье",
        children: (0, _inferno.createComponentVNode)(2, _components.ProgressBar, {
          "min": "0",
          "max": occupant.maxHealth,
          "value": occupant.health / occupant.maxHealth,
          "ranges": {
            good: [0.5, Infinity],
            average: [0, 0.5],
            bad: [-Infinity, 0]
          }
        })
      }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Кровь",
        children: occupant.blood.hasBlood ? (0, _inferno.createComponentVNode)(2, _components.ProgressBar, {
          "min": "0",
          "max": occupant.blood.bloodNormal,
          "value": occupant.blood.bloodLevel / occupant.blood.bloodNormal,
          "ranges": {
            good: [0.8, Infinity],
            average: [0.6, 0.8],
            bad: [-Infinity, 0.6]
          },
          children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
            "inline": true,
            "style": {
              "float": 'left'
            },
            children: [occupant.blood.pulse, " \u0443\u0434/\u043C\u0438\u043D"]
          }), (0, _inferno.createComponentVNode)(2, _components.Box, {
            "inline": true,
            children: [occupant.blood.percent, "%"]
          })]
        }) : (0, _inferno.createComponentVNode)(2, _components.Box, {
          "color": "average",
          children: "\u041D\u0435 \u043E\u0431\u043D\u0430\u0440\u0443\u0436\u0435\u043D\u0430"
        })
      }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Состояние",
        "color": stats[occupant.stat][0],
        children: stats[occupant.stat][1]
      }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Температура",
        children: [(0, _inferno.createComponentVNode)(2, _components.AnimatedNumber, {
          "value": (0, _math.round)(occupant.bodyTempC, 0)
        }), "\xB0C,\xA0", (0, _inferno.createComponentVNode)(2, _components.AnimatedNumber, {
          "value": (0, _math.round)(occupant.bodyTempF, 0)
        }), "\xB0F"]
      })]
    })
  });
};

var BodyScannerMainAbnormalities = function BodyScannerMainAbnormalities(props) {
  var occupant = props.occupant;

  if (!(occupant.hasBorer || occupant.blind || occupant.nearsighted || occupant.hasVirus)) {
    return (0, _inferno.createComponentVNode)(2, _components.Section, {
      "title": "Отклонения",
      children: (0, _inferno.createComponentVNode)(2, _components.Box, {
        "color": "label",
        children: "\u041D\u0435 \u043E\u0431\u043D\u0430\u0440\u0443\u0436\u0435\u043D\u043E."
      })
    });
  }

  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    "title": "Отклонения",
    children: abnormalities.map(function (a, i) {
      if (occupant[a[0]]) {
        return (0, _inferno.createComponentVNode)(2, _components.Box, {
          "color": a[1],
          "bold": a[1] === 'bad',
          children: a[2]
        }, a[2]);
      }
    })
  });
};

var BodyScannerMainDamage = function BodyScannerMainDamage(props) {
  var occupant = props.occupant;
  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    "title": "Повреждения",
    children: (0, _inferno.createComponentVNode)(2, _components.Table, {
      children: mapTwoByTwo(damages, function (d1, d2, i) {
        return (0, _inferno.createFragment)([(0, _inferno.createComponentVNode)(2, _components.Table.Row, {
          "color": "label",
          children: [(0, _inferno.createComponentVNode)(2, _components.Table.Cell, {
            children: [d1[0], ":"]
          }), (0, _inferno.createComponentVNode)(2, _components.Table.Cell, {
            children: !!d2 && d2[0] + ':'
          })]
        }), (0, _inferno.createComponentVNode)(2, _components.Table.Row, {
          children: [(0, _inferno.createComponentVNode)(2, _components.Table.Cell, {
            children: (0, _inferno.createComponentVNode)(2, BodyScannerMainDamageBar, {
              "value": occupant[d1[1]],
              "marginBottom": i < damages.length - 2
            })
          }), (0, _inferno.createComponentVNode)(2, _components.Table.Cell, {
            children: !!d2 && (0, _inferno.createComponentVNode)(2, BodyScannerMainDamageBar, {
              "value": occupant[d2[1]]
            })
          })]
        })], 4);
      })
    })
  });
};

var BodyScannerMainDamageBar = function BodyScannerMainDamageBar(props) {
  return (0, _inferno.createComponentVNode)(2, _components.ProgressBar, {
    "min": "0",
    "max": "100",
    "value": props.value / 100,
    "mt": "0.5rem",
    "mb": !!props.marginBottom && '0.5rem',
    "ranges": damageRange,
    children: (0, _math.round)(props.value, 0)
  });
};

var BodyScannerMainOrgansExternal = function BodyScannerMainOrgansExternal(props) {
  if (props.organs.length === 0) {
    return (0, _inferno.createComponentVNode)(2, _components.Section, {
      "title": "Внешние органы",
      children: (0, _inferno.createComponentVNode)(2, _components.Box, {
        "color": "label",
        children: "\u041D\u0435\u0442 \u0441\u0432\u0435\u0434\u0435\u043D\u0438\u0439"
      })
    });
  }

  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    "title": "Внешние органы",
    children: (0, _inferno.createComponentVNode)(2, _components.Table, {
      children: [(0, _inferno.createComponentVNode)(2, _components.Table.Row, {
        "header": true,
        children: [(0, _inferno.createComponentVNode)(2, _components.Table.Cell, {
          children: "\u041D\u0430\u0437\u0432\u0430\u043D\u0438\u0435"
        }), (0, _inferno.createComponentVNode)(2, _components.Table.Cell, {
          "textAlign": "center",
          children: "\u041F\u043E\u0432\u0440\u0435\u0436\u0434\u0435\u043D\u0438\u044F"
        }), (0, _inferno.createComponentVNode)(2, _components.Table.Cell, {
          "textAlign": "right",
          children: "\u0414\u043E\u043F\u043E\u043B\u043D\u0438\u0442\u0435\u043B\u044C\u043D\u044B\u0435 \u0441\u0432\u0435\u0434\u0435\u043D\u0438\u044F"
        })]
      }), props.organs.map(function (o, i) {
        var _o$implant;

        return (0, _inferno.createComponentVNode)(2, _components.Table.Row, {
          "textTransform": "capitalize",
          "backgroundColor": i % 2 !== 0 && "rgba(255, 255, 255, 0.05)",
          children: [(0, _inferno.createComponentVNode)(2, _components.Table.Cell, {
            "color": (!!o.status.dead || !!o.internalBleeding || !!o.stump || !!o.missing) && 'bad' || (!!o.lungRuptured || !!o.status.broken || !!o.open || !!o.germ_level || !!o.unknown_implant) && 'average' || !!o.status.robotic && 'label',
            "width": "33%",
            children: o.name
          }), (0, _inferno.createComponentVNode)(2, _components.Table.Cell, {
            "textAlign": "center",
            "q": true,
            children: (0, _inferno.createComponentVNode)(2, _components.ProgressBar, {
              "min": "0",
              "max": o.maxHealth,
              "mt": i > 0 && '0.5rem',
              "value": o.totalLoss / o.maxHealth,
              "ranges": damageRange,
              children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
                "inline": true,
                "style": {
                  "float": 'left'
                },
                children: [!!o.bruteLoss && (0, _inferno.createComponentVNode)(2, _components.Box, {
                  "inline": true,
                  "position": "relative",
                  children: [(0, _inferno.createComponentVNode)(2, _components.Icon, {
                    "name": "bone"
                  }), (0, _math.round)(o.bruteLoss, 0), "\xA0"]
                }), !!o.fireLoss && (0, _inferno.createComponentVNode)(2, _components.Box, {
                  "inline": true,
                  "position": "relative",
                  children: [(0, _inferno.createComponentVNode)(2, _components.Icon, {
                    "name": "fire"
                  }), (0, _math.round)(o.fireLoss, 0)]
                })]
              }), (0, _inferno.createComponentVNode)(2, _components.Box, {
                "inline": true,
                children: (0, _math.round)(o.totalLoss, 0)
              })]
            })
          }), (0, _inferno.createComponentVNode)(2, _components.Table.Cell, {
            "textAlign": "right",
            "verticalAlign": "top",
            "width": "33%",
            "pt": i > 0 && 'calc(0.5rem + 2px)',
            children: (0, _inferno.createComponentVNode)(2, _components.Box, {
              "inline": true,
              children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
                "color": "bad",
                "bold": true,
                children: reduceOrganStatus([!!o.internalBleeding && 'Артериальное кровотечение', !!o.status.dead && 'Отказ', !!o.stump && 'Культя', !!o.missing && 'Отсутствует'])
              }), (0, _inferno.createComponentVNode)(2, _components.Box, {
                "color": "average",
                children: reduceOrganStatus([!!o.lungRuptured && 'Разрыв легкого', !!o.status.broken && o.status.broken, !!o.germ_level && o.germ_level, !!o.open && 'Открытый разрез'])
              }), (_o$implant = o.implant) == null ? void 0 : _o$implant.map(function (s) {
                return s.name ? (0, _inferno.createComponentVNode)(2, _components.Box, {
                  "color": "good",
                  children: s.name
                }) : (0, _inferno.createComponentVNode)(2, _components.Box, {
                  "color": "average",
                  children: "\u0418\u043D\u043E\u0440\u043E\u0434\u043D\u044B\u0439 \u043E\u0431\u044A\u0435\u043A\u0442"
                });
              }), reduceOrganStatus([!!o.status.splinted && (0, _inferno.createComponentVNode)(2, _components.Box, {
                "color": "good",
                children: "\u041D\u0430\u043B\u043E\u0436\u0435\u043D\u0430 \u0448\u0438\u043D\u0430"
              }), !!o.status.robotic && (0, _inferno.createComponentVNode)(2, _components.Box, {
                "color": "label",
                children: "\u041F\u0440\u043E\u0442\u0435\u0437"
              })])]
            })
          })]
        }, i);
      })]
    })
  });
};

var BodyScannerMainOrgansInternal = function BodyScannerMainOrgansInternal(props) {
  if (props.organs.length === 0) {
    return (0, _inferno.createComponentVNode)(2, _components.Section, {
      "title": "Внутренние органы",
      children: (0, _inferno.createComponentVNode)(2, _components.Box, {
        "color": "label",
        children: "\u041D\u0435\u0442 \u0441\u0432\u0435\u0434\u0435\u043D\u0438\u0439"
      })
    });
  }

  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    "title": "Внутренние органы",
    children: (0, _inferno.createComponentVNode)(2, _components.Table, {
      children: [(0, _inferno.createComponentVNode)(2, _components.Table.Row, {
        "header": true,
        children: [(0, _inferno.createComponentVNode)(2, _components.Table.Cell, {
          children: "\u041D\u0430\u0437\u0432\u0430\u043D\u0438\u0435"
        }), (0, _inferno.createComponentVNode)(2, _components.Table.Cell, {
          "textAlign": "center",
          children: "\u041F\u043E\u0432\u0440\u0435\u0436\u0434\u0435\u043D\u0438\u044F"
        }), (0, _inferno.createComponentVNode)(2, _components.Table.Cell, {
          "textAlign": "right",
          children: "\u0414\u043E\u043F\u043E\u043B\u043D\u0438\u0442\u0435\u043B\u044C\u043D\u044B\u0435 \u0441\u0432\u0435\u0434\u0435\u043D\u0438\u044F"
        })]
      }), props.organs.map(function (o, i) {
        return (0, _inferno.createComponentVNode)(2, _components.Table.Row, {
          "textTransform": "capitalize",
          "backgroundColor": i % 2 !== 0 && "rgba(255, 255, 255, 0.05)",
          children: [(0, _inferno.createComponentVNode)(2, _components.Table.Cell, {
            "color": (!!o.dead || !!o.broken) && 'bad' || !!o.robotic && 'label' || (!!o.germ_level || !!o.bruised) && 'average',
            "width": "33%",
            children: o.name
          }), (0, _inferno.createComponentVNode)(2, _components.Table.Cell, {
            "textAlign": "center",
            children: (0, _inferno.createComponentVNode)(2, _components.ProgressBar, {
              "min": "0",
              "max": o.maxHealth,
              "value": o.damage / o.maxHealth,
              "mt": i > 0 && '0.5rem',
              "ranges": damageRange,
              children: (0, _math.round)(o.damage, 0)
            })
          }), (0, _inferno.createComponentVNode)(2, _components.Table.Cell, {
            "textAlign": "right",
            "verticalAlign": "top",
            "width": "33%",
            "pt": i > 0 && 'calc(0.5rem + 2px)',
            children: (0, _inferno.createComponentVNode)(2, _components.Box, {
              "inline": true,
              children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
                "color": "average",
                children: reduceOrganStatus([!!o.germ_level && o.germ_level])
              }), reduceOrganStatus([!!o.robotic && (0, _inferno.createComponentVNode)(2, _components.Box, {
                "color": "label",
                children: "\u041F\u0440\u043E\u0442\u0435\u0437"
              }), !!o.assisted && (0, _inferno.createComponentVNode)(2, _components.Box, {
                "color": "label",
                children: "\u0412\u0441\u043F\u043E\u043C\u043E\u0433\u0430\u0442\u0435\u043B\u044C\u043D\u044B\u0439 \u0438\u043C\u043F\u043B\u0430\u043D\u0442"
              }), !!o.dead && (0, _inferno.createComponentVNode)(2, _components.Box, {
                "color": "bad",
                "bold": true,
                children: "\u041E\u0442\u043A\u0430\u0437"
              })])]
            })
          })]
        }, i);
      })]
    })
  });
};

var BodyScannerEmpty = function BodyScannerEmpty() {
  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    "fill": true,
    "textAlign": "center",
    children: (0, _inferno.createComponentVNode)(2, _components.Flex, {
      "height": "100%",
      children: (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
        "grow": "1",
        "align": "center",
        "color": "label",
        children: [(0, _inferno.createComponentVNode)(2, _components.Icon, {
          "name": "user-slash",
          "mb": "0.5rem",
          "size": "5"
        }), (0, _inferno.createVNode)(1, "br"), "\u041F\u0430\u0446\u0438\u0435\u043D\u0442 \u043D\u0435 \u043E\u0431\u043D\u0430\u0440\u0443\u0436\u0435\u043D."]
      })
    })
  });
};

/***/ }),

/***/ 83548:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.CameraConsoleContent = exports.CameraConsole = exports.selectCameras = exports.prevNextCamera = void 0;

var _inferno = __webpack_require__(96323);

var _collections = __webpack_require__(1397);

var _fp = __webpack_require__(32416);

var _react = __webpack_require__(1060);

var _string = __webpack_require__(21118);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

/**
 * Returns previous and next camera names relative to the currently
 * active camera.
 */
var prevNextCamera = function prevNextCamera(cameras, activeCamera) {
  var _cameras, _cameras2;

  if (!activeCamera) {
    return [];
  }

  var index = cameras.findIndex(function (camera) {
    return camera.name === activeCamera.name;
  });
  return [(_cameras = cameras[index - 1]) == null ? void 0 : _cameras.name, (_cameras2 = cameras[index + 1]) == null ? void 0 : _cameras2.name];
};
/**
 * Camera selector.
 *
 * Filters cameras, applies search terms and sorts the alphabetically.
 */


exports.prevNextCamera = prevNextCamera;

var selectCameras = function selectCameras(cameras, searchText) {
  if (searchText === void 0) {
    searchText = '';
  }

  var testSearch = (0, _string.createSearch)(searchText, function (camera) {
    return camera.name;
  });
  return (0, _fp.flow)([// Null camera filter
  (0, _collections.filter)(function (camera) {
    return camera == null ? void 0 : camera.name;
  }), // Optional search term
  searchText && (0, _collections.filter)(testSearch), // Slightly expensive, but way better than sorting in BYOND
  (0, _collections.sortBy)(function (camera) {
    return camera.name;
  })])(cameras);
};

exports.selectCameras = selectCameras;

var CameraConsole = function CameraConsole(props, context) {
  Byond.winget("taumapwindow.map", "style").then(function (style) {
    Byond.winset(mapRef, "style", style);
  });

  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var mapRef = data.mapRef,
      activeCamera = data.activeCamera;
  var cameras = selectCameras(data.cameras);

  var _prevNextCamera = prevNextCamera(cameras, activeCamera),
      prevCameraName = _prevNextCamera[0],
      nextCameraName = _prevNextCamera[1];

  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "width": 870,
    "height": 708,
    children: [(0, _inferno.createVNode)(1, "div", "CameraConsole__left", (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      children: (0, _inferno.createComponentVNode)(2, CameraConsoleContent)
    }), 2), (0, _inferno.createVNode)(1, "div", "CameraConsole__right", [(0, _inferno.createVNode)(1, "div", "CameraConsole__toolbar", [(0, _inferno.createVNode)(1, "b", null, "Camera: ", 16), activeCamera && activeCamera.name || '—'], 0), (0, _inferno.createVNode)(1, "div", "CameraConsole__toolbarRight", [(0, _inferno.createComponentVNode)(2, _components.Button, {
      "icon": "chevron-left",
      "disabled": !prevCameraName,
      "onClick": function () {
        function onClick() {
          return act('switch_camera', {
            name: prevCameraName
          });
        }

        return onClick;
      }()
    }), (0, _inferno.createComponentVNode)(2, _components.Button, {
      "icon": "chevron-right",
      "disabled": !nextCameraName,
      "onClick": function () {
        function onClick() {
          return act('switch_camera', {
            name: nextCameraName
          });
        }

        return onClick;
      }()
    })], 4), (0, _inferno.createComponentVNode)(2, _components.ByondUi, {
      "className": "CameraConsole__map",
      "params": {
        id: mapRef,
        type: 'map'
      }
    })], 4)]
  });
};

exports.CameraConsole = CameraConsole;

var CameraConsoleContent = function CameraConsoleContent(props, context) {
  var _useBackend2 = (0, _backend.useBackend)(context),
      act = _useBackend2.act,
      data = _useBackend2.data;

  var _useLocalState = (0, _backend.useLocalState)(context, 'searchText', ''),
      searchText = _useLocalState[0],
      setSearchText = _useLocalState[1];

  var activeCamera = data.activeCamera;
  var cameras = selectCameras(data.cameras, searchText);
  return (0, _inferno.createComponentVNode)(2, _components.Flex, {
    "direction": "column",
    "height": "100%",
    children: [(0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
      children: (0, _inferno.createComponentVNode)(2, _components.Input, {
        "autoFocus": true,
        "fluid": true,
        "mt": 1,
        "placeholder": "Search for a camera",
        "onInput": function () {
          function onInput(e, value) {
            return setSearchText(value);
          }

          return onInput;
        }()
      })
    }), (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
      "height": "100%",
      children: (0, _inferno.createComponentVNode)(2, _components.Section, {
        "fill": true,
        "scrollable": true,
        children: cameras.map(function (camera) {
          return (// We're not using the component here because performance
            // would be absolutely abysmal (50+ ms for each re-render).
            (0, _inferno.createVNode)(1, "div", (0, _react.classes)(['Button', 'Button--fluid', 'Button--color--transparent', 'Button--ellipsis', activeCamera && camera.name === activeCamera.name && 'Button--selected']), camera.name, 0, {
              "title": camera.name,
              "onClick": function () {
                function onClick() {
                  return act('switch_camera', {
                    name: camera.name
                  });
                }

                return onClick;
              }()
            }, camera.name)
          );
        })
      })
    })]
  });
};

exports.CameraConsoleContent = CameraConsoleContent;

/***/ }),

/***/ 98566:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Canister = void 0;

var _inferno = __webpack_require__(96323);

var _math = __webpack_require__(97622);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _format = __webpack_require__(1033);

var _layouts = __webpack_require__(2381);

var Canister = function Canister(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var name = data.name,
      canLabel = data.canLabel,
      portConnected = data.portConnected,
      tankPressure = data.tankPressure,
      releasePressure = data.releasePressure,
      defaultReleasePressure = data.defaultReleasePressure,
      minReleasePressure = data.minReleasePressure,
      maxReleasePressure = data.maxReleasePressure,
      valveOpen = data.valveOpen,
      hasHoldingTank = data.hasHoldingTank,
      holdingTank = data.holdingTank;
  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "width": 300,
    "height": 232,
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      children: [(0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": name,
        "buttons": (0, _inferno.createComponentVNode)(2, _components.Button, {
          "icon": "pencil-alt",
          "content": "Relabel",
          "disabled": !canLabel,
          "onClick": function () {
            function onClick() {
              return act('relabel');
            }

            return onClick;
          }()
        }),
        children: (0, _inferno.createComponentVNode)(2, _components.LabeledControls, {
          children: [(0, _inferno.createComponentVNode)(2, _components.LabeledControls.Item, {
            "minWidth": "66px",
            "label": "Pressure",
            children: (0, _inferno.createComponentVNode)(2, _components.AnimatedNumber, {
              "value": tankPressure,
              "format": function () {
                function format(value) {
                  if (value < 10000) {
                    return (0, _math.toFixed)(value) + ' kPa';
                  }

                  return (0, _format.formatSiUnit)(value * 1000, 1, 'Pa');
                }

                return format;
              }()
            })
          }), (0, _inferno.createComponentVNode)(2, _components.LabeledControls.Item, {
            "label": "Regulator",
            children: (0, _inferno.createComponentVNode)(2, _components.Box, {
              "position": "relative",
              "left": "-8px",
              children: [(0, _inferno.createComponentVNode)(2, _components.Knob, {
                "size": 1.25,
                "color": !!valveOpen && 'yellow',
                "value": releasePressure,
                "unit": "kPa",
                "minValue": minReleasePressure,
                "maxValue": maxReleasePressure,
                "step": 5,
                "stepPixelSize": 1,
                "onDrag": function () {
                  function onDrag(e, value) {
                    return act('pressure', {
                      pressure: value
                    });
                  }

                  return onDrag;
                }()
              }), (0, _inferno.createComponentVNode)(2, _components.Button, {
                "fluid": true,
                "position": "absolute",
                "top": "-2px",
                "right": "-20px",
                "color": "transparent",
                "icon": "fast-forward",
                "onClick": function () {
                  function onClick() {
                    return act('pressure', {
                      pressure: maxReleasePressure
                    });
                  }

                  return onClick;
                }()
              }), (0, _inferno.createComponentVNode)(2, _components.Button, {
                "fluid": true,
                "position": "absolute",
                "top": "16px",
                "right": "-20px",
                "color": "transparent",
                "icon": "undo",
                "onClick": function () {
                  function onClick() {
                    return act('pressure', {
                      pressure: defaultReleasePressure
                    });
                  }

                  return onClick;
                }()
              })]
            })
          }), (0, _inferno.createComponentVNode)(2, _components.LabeledControls.Item, {
            "label": "Valve",
            children: (0, _inferno.createComponentVNode)(2, _components.Button, {
              "my": 0.5,
              "width": "50px",
              "lineHeight": 2,
              "fontSize": "11px",
              "color": valveOpen ? hasHoldingTank ? 'caution' : 'danger' : null,
              "content": valveOpen ? 'Open' : 'Closed',
              "onClick": function () {
                function onClick() {
                  return act('valve');
                }

                return onClick;
              }()
            })
          }), (0, _inferno.createComponentVNode)(2, _components.LabeledControls.Item, {
            "mr": 1,
            "label": "Port",
            children: (0, _inferno.createComponentVNode)(2, _components.Box, {
              "position": "relative",
              children: [(0, _inferno.createComponentVNode)(2, _components.Icon, {
                "size": 1.25,
                "name": portConnected ? 'plug' : 'times',
                "color": portConnected ? 'good' : 'bad'
              }), (0, _inferno.createComponentVNode)(2, _components.Tooltip, {
                "content": portConnected ? 'Connected' : 'Disconnected',
                "position": "top"
              })]
            })
          })]
        })
      }), (0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": "Holding Tank",
        "buttons": !!hasHoldingTank && (0, _inferno.createComponentVNode)(2, _components.Button, {
          "icon": "eject",
          "color": valveOpen && 'danger',
          "content": "Eject",
          "onClick": function () {
            function onClick() {
              return act('eject');
            }

            return onClick;
          }()
        }),
        children: [!!hasHoldingTank && (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
          children: [(0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Label",
            children: holdingTank.name
          }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Pressure",
            children: [(0, _inferno.createComponentVNode)(2, _components.AnimatedNumber, {
              "value": holdingTank.tankPressure
            }), " kPa"]
          })]
        }), !hasHoldingTank && (0, _inferno.createComponentVNode)(2, _components.Box, {
          "color": "average",
          children: "No Holding Tank"
        })]
      })]
    })
  });
};

exports.Canister = Canister;

/***/ }),

/***/ 6789:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Canvas = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var _excluded = ["res", "value", "dotsize"];

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

function _inheritsLoose(subClass, superClass) { subClass.prototype = Object.create(superClass.prototype); subClass.prototype.constructor = subClass; _setPrototypeOf(subClass, superClass); }

function _setPrototypeOf(o, p) { _setPrototypeOf = Object.setPrototypeOf || function () { function _setPrototypeOf(o, p) { o.__proto__ = p; return o; } return _setPrototypeOf; }(); return _setPrototypeOf(o, p); }

var PX_PER_UNIT = 24;

var PaintCanvas = /*#__PURE__*/function (_Component) {
  _inheritsLoose(PaintCanvas, _Component);

  function PaintCanvas(props) {
    var _this;

    _this = _Component.call(this, props) || this;
    _this.canvasRef = (0, _inferno.createRef)();
    _this.onCVClick = props.onCanvasClick;
    return _this;
  }

  var _proto = PaintCanvas.prototype;

  _proto.componentDidMount = function () {
    function componentDidMount() {
      this.drawCanvas(this.props);
    }

    return componentDidMount;
  }();

  _proto.componentDidUpdate = function () {
    function componentDidUpdate() {
      this.drawCanvas(this.props);
    }

    return componentDidUpdate;
  }();

  _proto.drawCanvas = function () {
    function drawCanvas(propSource) {
      var ctx = this.canvasRef.current.getContext("2d");
      var grid = propSource.value;
      var x_size = grid.length;

      if (!x_size) {
        return;
      }

      var y_size = grid[0].length;
      var x_scale = Math.round(this.canvasRef.current.width / x_size);
      var y_scale = Math.round(this.canvasRef.current.height / y_size);
      ctx.save();
      ctx.scale(x_scale, y_scale);

      for (var x = 0; x < grid.length; x++) {
        var element = grid[x];

        for (var y = 0; y < element.length; y++) {
          var color = element[y];
          ctx.fillStyle = color;
          ctx.fillRect(x, y, 1, 1);
        }
      }

      ctx.restore();
    }

    return drawCanvas;
  }();

  _proto.clickwrapper = function () {
    function clickwrapper(event) {
      var x_size = this.props.value.length;

      if (!x_size) {
        return;
      }

      var y_size = this.props.value[0].length;
      var x_scale = this.canvasRef.current.width / x_size;
      var y_scale = this.canvasRef.current.height / y_size;
      var x = Math.floor(event.offsetX / x_scale) + 1;
      var y = Math.floor(event.offsetY / y_scale) + 1;
      this.onCVClick(x, y);
    }

    return clickwrapper;
  }();

  _proto.render = function () {
    function render() {
      var _this2 = this;

      var _this$props = this.props,
          _this$props$res = _this$props.res,
          res = _this$props$res === void 0 ? 1 : _this$props$res,
          value = _this$props.value,
          _this$props$dotsize = _this$props.dotsize,
          dotsize = _this$props$dotsize === void 0 ? PX_PER_UNIT : _this$props$dotsize,
          rest = _objectWithoutPropertiesLoose(_this$props, _excluded);

      var _getImageSize = getImageSize(value),
          width = _getImageSize[0],
          height = _getImageSize[1];

      return (0, _inferno.normalizeProps)((0, _inferno.createVNode)(1, "canvas", null, "Canvas failed to render.", 16, Object.assign({
        "width": width * dotsize || 300,
        "height": height * dotsize || 300
      }, rest, {
        "onClick": function () {
          function onClick(e) {
            return _this2.clickwrapper(e);
          }

          return onClick;
        }()
      }), null, this.canvasRef));
    }

    return render;
  }();

  return PaintCanvas;
}(_inferno.Component);

var getImageSize = function getImageSize(value) {
  var width = value.length;
  var height = width !== 0 ? value[0].length : 0;
  return [width, height];
};

var Canvas = function Canvas(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var dotsize = PX_PER_UNIT;

  var _getImageSize2 = getImageSize(data.grid),
      width = _getImageSize2[0],
      height = _getImageSize2[1];

  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "width": Math.min(700, width * dotsize + 72),
    "height": Math.min(700, height * dotsize + 72),
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      children: (0, _inferno.createComponentVNode)(2, _components.Box, {
        "textAlign": "center",
        children: [(0, _inferno.createComponentVNode)(2, PaintCanvas, {
          "value": data.grid,
          "dotsize": dotsize,
          "onCanvasClick": function () {
            function onCanvasClick(x, y) {
              return act("paint", {
                x: x,
                y: y
              });
            }

            return onCanvasClick;
          }()
        }), (0, _inferno.createComponentVNode)(2, _components.Box, {
          children: [!data.finalized && (0, _inferno.createComponentVNode)(2, _components.Button.Confirm, {
            "onClick": function () {
              function onClick() {
                return act("finalize");
              }

              return onClick;
            }(),
            "content": "Finalize"
          }), data.name]
        })]
      })
    })
  });
};

exports.Canvas = Canvas;

/***/ }),

/***/ 60301:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.CardPay = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var _SegmentDisplay = __webpack_require__(97968);

var CardPay = function CardPay(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var numbers = data.numbers,
      reset_numbers = data.reset_numbers,
      mode = data.mode;
  var buttons = [];

  var _loop = function _loop(row) {
    var _loop2 = function _loop2(col) {
      buttons.push((0, _inferno.createComponentVNode)(2, _components.Button, {
        "className": "cardpay_button cardpay_button--normal",
        "content": (0, _inferno.createComponentVNode)(2, _components.Box, {
          "className": "cardpay_button-inside cardpay_button-inside--normal",
          children: row * 3 + col
        }),
        "onClick": function () {
          function onClick() {
            return act("pressnumber", {
              number: row * 3 + col
            });
          }

          return onClick;
        }()
      }));
    };

    for (var col = 1; col < 4; col++) {
      _loop2(col);
    }
  };

  for (var row = 0; row < 3; row++) {
    _loop(row);
  }

  var getText = function getText() {
    var result_text = "";
    var numtext = numbers.toString();

    switch (mode) {
      case 'Mode_EnterPin':
        result_text += "PIN:" + (numbers ? numtext : "-");

        for (var excess = 1; excess <= 4 - numtext.length; excess++) {
          result_text += "-";
        }

        break;

      case 'Mode_Account':
        result_text += "N°" + (numbers ? numtext : "-");

        for (var _excess = 1; _excess <= 6 - numtext.length; _excess++) {
          result_text += "-";
        }

        break;

      case 'Mode_Pay':
        result_text += "N°" + (numbers ? numtext : "-");

        for (var _excess2 = 1; _excess2 <= 6 - numtext.length; _excess2++) {
          result_text += "-";
        }

        break;

      case 'Mode_Idle':
        for (var _excess3 = 1; _excess3 <= 3 - numtext.length; _excess3++) {
          result_text += "-";
        }

        result_text += (numbers ? numtext : "-") + "$";
        break;
    }

    return result_text;
  };

  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "theme": "",
    "width": 220,
    "height": 340,
    "titleClassName": "cardpay_window-titlebar",
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      "className": "cardpay_window-contents",
      children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
        "className": "cardpay_monitor",
        children: (0, _inferno.createComponentVNode)(2, _SegmentDisplay.SegmentDisplay, {
          "display_cells_amount": 8,
          "display_height": 40,
          "display_text": getText()
        })
      }), (0, _inferno.createComponentVNode)(2, _components.Box, {
        "width": "158px",
        "height": "208px",
        "position": "absolute",
        "left": "32px",
        "top": "95px",
        children: [buttons, (0, _inferno.createComponentVNode)(2, _components.Button, {
          "className": "cardpay_button cardpay_button--red",
          "content": (0, _inferno.createComponentVNode)(2, _components.Box, {
            "className": "cardpay_button-inside cardpay_button-inside--red",
            children: "X"
          }),
          "onClick": function () {
            function onClick() {
              return act("clearnumbers");
            }

            return onClick;
          }()
        }), (0, _inferno.createComponentVNode)(2, _components.Button, {
          "className": "cardpay_button cardpay_button--normal",
          "content": (0, _inferno.createComponentVNode)(2, _components.Box, {
            "className": "cardpay_button-inside cardpay_button-inside--normal",
            children: 0
          }),
          "onClick": function () {
            function onClick() {
              return act("pressnumber", {
                number: 0
              });
            }

            return onClick;
          }()
        }), (0, _inferno.createComponentVNode)(2, _components.Button, {
          "className": "cardpay_button cardpay_button--green",
          "content": (0, _inferno.createComponentVNode)(2, _components.Box, {
            "className": "cardpay_button-inside cardpay_button-inside--green",
            children: "O"
          }),
          "onClick": function () {
            function onClick() {
              return act("approveprice");
            }

            return onClick;
          }()
        })]
      }), (0, _inferno.createComponentVNode)(2, _components.Box, {
        "position": "absolute",
        "top": "62px",
        "left": "25px",
        "textColor": "#333344",
        "fontSize": 0.85,
        "bold": 1,
        children: ["\u0441\u0431\u0440\u043E\u0441", (0, _inferno.createComponentVNode)(2, _components.Button, {
          "position": "absolute",
          "top": "15px",
          "left": "10px",
          "className": "cardpay_resethole",
          "onClick": function () {
            function onClick() {
              return act("toggleenteraccount");
            }

            return onClick;
          }()
        })]
      }), (0, _inferno.createComponentVNode)(2, _components.Button, {
        "selected": reset_numbers ? 1 : 0,
        "position": "absolute",
        "top": "60px",
        "left": "140px",
        "className": "cardpay_switch",
        "content": (0, _inferno.createComponentVNode)(2, _components.Box, {
          "className": "cardpay_switch-inside",
          children: "|||"
        }),
        "onClick": function () {
          function onClick() {
            return act("togglereset");
          }

          return onClick;
        }()
      })]
    })
  });
};

exports.CardPay = CardPay;

/***/ }),

/***/ 68078:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.ChemDispenser = void 0;

var _inferno = __webpack_require__(96323);

var _math = __webpack_require__(97622);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var ChemDispenser = function ChemDispenser(_, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var amount = data.amount,
      energy = data.energy,
      maxEnergy = data.maxEnergy,
      isBeakerLoaded = data.isBeakerLoaded,
      glass = data.glass,
      beakerContents = data.beakerContents,
      beakerCurrentVolume = data.beakerCurrentVolume,
      beakerMaxVolume = data.beakerMaxVolume,
      chemicals = data.chemicals;
  var DISPENSE_AMOUNTS = [5, 10, 20, 30, 40];
  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "width": 420,
    "height": 650,
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      children: [(0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": "Status",
        children: (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
          children: (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Energy",
            children: (0, _inferno.createComponentVNode)(2, _components.ProgressBar, {
              "value": energy / maxEnergy,
              children: (0, _math.toFixed)(energy) + ' units'
            })
          })
        })
      }), (0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": "Dispense",
        "buttons": (0, _inferno.createFragment)([DISPENSE_AMOUNTS.map(function (amountSel) {
          return (0, _inferno.createComponentVNode)(2, _components.Button, {
            "icon": "plus",
            "align": "center",
            "content": amountSel,
            "selected": amountSel === amount,
            "onClick": function () {
              function onClick() {
                return act("change_amount", {
                  new_amount: amountSel
                });
              }

              return onClick;
            }()
          }, amountSel);
        }), (0, _inferno.createComponentVNode)(2, _components.NumberInput, {
          "width": "40px",
          "animated": 1,
          "step": 5,
          "minValue": 1,
          "maxValue": 100,
          "stepPixelSize": 3,
          "value": amount,
          "onChange": function () {
            function onChange(_, value) {
              return act("change_amount", {
                new_amount: value
              });
            }

            return onChange;
          }()
        })], 0),
        children: chemicals.map(function (chemical) {
          return (0, _inferno.createComponentVNode)(2, _components.Button, {
            "disabled": !isBeakerLoaded,
            "icon": "tint",
            "width": "130px",
            "lineHeight": 1.75,
            "content": chemical.title,
            "onClick": function () {
              function onClick() {
                return act("dispense", {
                  chemical: chemical.id
                });
              }

              return onClick;
            }()
          }, chemical.id);
        })
      }), (0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": (glass ? "Glass" : "Beaker") + " contents",
        "buttons": (0, _inferno.createComponentVNode)(2, _components.Button, {
          "content": "Eject beaker",
          "icon": "eject",
          "disabled": !isBeakerLoaded,
          "onClick": function () {
            function onClick() {
              return act("eject_beaker");
            }

            return onClick;
          }()
        }),
        children: (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
          children: [(0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": glass ? "Glass" : "Beaker",
            children: isBeakerLoaded && (0, _inferno.createFragment)([(0, _inferno.createComponentVNode)(2, _components.AnimatedNumber, {
              "initial": 0,
              "value": beakerCurrentVolume
            }), (0, _inferno.createTextVNode)("/"), beakerMaxVolume, (0, _inferno.createTextVNode)(" units")], 0) || 'No ' + (glass ? "glass" : "beaker")
          }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Contents",
            children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
              "color": "label",
              children: !isBeakerLoaded && 'N/A' || beakerContents.length === 0 && 'Nothing'
            }), beakerContents.map(function (chemical) {
              return (0, _inferno.createComponentVNode)(2, _components.Box, {
                "color": "label",
                children: [(0, _inferno.createComponentVNode)(2, _components.AnimatedNumber, {
                  "initial": 0,
                  "value": chemical.volume
                }), ' ', "units of ", chemical.name]
              }, chemical.name);
            })]
          })]
        })
      })]
    })
  });
};

exports.ChemDispenser = ChemDispenser;

/***/ }),

/***/ 85028:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.ClientSettings = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _keys = __webpack_require__(94831);

var _events = __webpack_require__(99397);

var _layouts = __webpack_require__(2381);

// byond <-> IE names
// order is important, we use it for replace, so place verbose names up
// p.s. i hate them both endlessly
var byond2jsKeysDictionary = {
  "Northwest": "Home",
  "Northeast": "PageUp",
  "Southwest": "End",
  "Southeast": "PageDown",
  "Space": "Spacebar",
  "Ctrl": "Control",
  "Return": "Enter",
  "Delete": "Del",
  "North": "Up",
  "East": "Right",
  "South": "Down",
  "West": "Left"
};

var ClientSettings = function ClientSettings(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var active_tab = data.active_tab,
      settings = data.settings,
      tabs = data.tabs,
      tabs_tips = data.tabs_tips;

  var _useLocalState = (0, _backend.useLocalState)(context, 'showmodal', null),
      keyBindingState = _useLocalState[0],
      setKeyBindingState = _useLocalState[1];

  var settingsList; // this all is too overcomplicated because of keybinds tab that handled differently

  if (active_tab === "keybinds") {
    var sortedByCategory = settings.reduce(function (acc, setting) {
      if (!acc[setting.category]) {
        acc[setting.category] = [];
      }

      acc[setting.category].push(setting);
      return acc;
    }, {});
    settingsList = Object.keys(sortedByCategory).map(function (categoryName) {
      return (0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": categoryName,
        children: sortedByCategory[categoryName].map(function (setting) {
          return (0, _inferno.createComponentVNode)(2, SettingField, {
            "setting": setting,
            "setKeyBindingState": setKeyBindingState
          }, setting.type);
        })
      }, categoryName);
    });
  } else {
    settingsList = settings.map(function (setting) {
      return (0, _inferno.createComponentVNode)(2, SettingField, {
        "setting": setting
      }, setting.type);
    });
  }

  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "title": "Настройки",
    "width": 500,
    "height": 650,
    children: [keyBindingState && (0, _inferno.createComponentVNode)(2, KeyBindingModal, {
      "settings": settings,
      "keyBindingState": keyBindingState,
      "setKeyBindingState": setKeyBindingState
    }), (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      "scrollable": true,
      children: [(0, _inferno.createComponentVNode)(2, _components.Tabs, {
        children: Object.keys(tabs).map(function (tab) {
          return (0, _inferno.createComponentVNode)(2, _components.Tabs.Tab, {
            "selected": tab === active_tab,
            "onClick": function () {
              function onClick() {
                return act("set_tab", {
                  "tab": tab
                });
              }

              return onClick;
            }(),
            children: tabs[tab]
          }, tab);
        })
      }), tabs_tips[active_tab] && (0, _inferno.createComponentVNode)(2, _components.NoticeBox, {
        "info": true,
        children: tabs_tips[active_tab]
      }), settingsList]
    })]
  });
};

exports.ClientSettings = ClientSettings;

var KeyBindingModal = function KeyBindingModal(props, context) {
  var _useBackend2 = (0, _backend.useBackend)(context),
      act = _useBackend2.act,
      data = _useBackend2.data;

  var settings = props.settings,
      keyBindingState = props.keyBindingState,
      setKeyBindingState = props.setKeyBindingState;

  var handleKeyDown = function handleKeyDown(keyEvent) {
    var event = keyEvent.event;
    event.preventDefault();
  };

  var handleKeyUp = function handleKeyUp(keyEvent) {
    var event = keyEvent.event;
    event.preventDefault();

    if ((0, _keys.isEscape)(event.key)) {
      // empty key (reset)
      act('set_keybind_value', {
        type: keyBindingState.type,
        index: keyBindingState.index + 1,
        key: ""
      });
      setKeyBindingState(null);
      return;
    } // todo: rewrite it all to use event.code with 516
    // also maybe this should be moved to keyEvent.toString()


    var sanitizedKey = event.key; // so shift+numeric keys will be parsed as 1234 instead of !@#$

    if (47 < event.keyCode && event.keyCode < 58) {
      sanitizedKey = String.fromCharCode(event.keyCode);
    } // so shift+letter keys will be parsed as latin and in lower case
    else if (64 < event.keyCode && event.keyCode < 91) {
      sanitizedKey = String.fromCharCode(event.keyCode);
    } // format Numpad0-Numpad9 for byond
    else if (95 < event.keyCode && event.keyCode < 106) {
      sanitizedKey = "Numpad" + sanitizedKey;
    } // replace by dictionary if needed


    sanitizedKey = Object.keys(byond2jsKeysDictionary).find(function (key) {
      return byond2jsKeysDictionary[key] === sanitizedKey;
    }) || sanitizedKey;
    act('set_keybind_value', {
      type: keyBindingState.type,
      index: keyBindingState.index + 1,
      key: sanitizedKey,
      altMod: event.altKey,
      ctrlMod: event.ctrlKey,
      shiftMod: event.shiftKey
    });
    setKeyBindingState(null);
  };

  var preference = settings.find(function (pref) {
    return pref.type === keyBindingState.type;
  });
  return (0, _inferno.createComponentVNode)(2, _components.Modal, {
    "textAlign": "center",
    children: [(0, _inferno.createComponentVNode)(2, _components.KeyListener, {
      "onKeyDown": handleKeyDown,
      "onKeyUp": handleKeyUp
    }), (0, _inferno.createComponentVNode)(2, _components.Section, {
      "title": "\u041D\u0430\u0437\u043D\u0430\u0447\u0438\u0442\u044C \u043A\u043B\u0430\u0432\u0438\u0448\u0443 \u0434\u043B\u044F " + preference.name,
      children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
        "italic": true,
        children: [preference.description, " ", (0, _inferno.createVNode)(1, "br"), (0, _inferno.createVNode)(1, "br")]
      }), (0, _inferno.createComponentVNode)(2, _components.Box, {
        "bold": true,
        children: ["\u041D\u0430\u0436\u043C\u0438\u0442\u0438 \u043B\u044E\u0431\u0443\u044E \u043A\u043B\u0430\u0432\u0438\u0448\u0443.", (0, _inferno.createVNode)(1, "br"), (0, _inferno.createVNode)(1, "br"), "\u041C\u043E\u0436\u043D\u043E \u0438\u0441\u043F\u043E\u043B\u044C\u0437\u043E\u0432\u0430\u0442\u044C \u043A\u043E\u043C\u0431\u0438\u043D\u0430\u0446\u0438\u0438 \u0441 Alt/Ctrl/Shift.", (0, _inferno.createVNode)(1, "br"), (0, _inferno.createVNode)(1, "br"), "\u041D\u0430\u0436\u043C\u0438\u0442\u0435 ESC \u0434\u043B\u044F \u0441\u0431\u0440\u043E\u0441\u0430."]
      })]
    })]
  });
};

var SettingField = function SettingField(props, context) {
  var _useBackend3 = (0, _backend.useBackend)(context),
      act = _useBackend3.act,
      data = _useBackend3.data;

  var setting = props.setting;
  var settingTypes = {
    "boolean": (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, SettingTypeBoolean, Object.assign({}, props))),
    text: (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, SettingTypeText, Object.assign({}, props))),
    range: (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, SettingTypeRange, Object.assign({}, props))),
    select: (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, SettingTypeSelect, Object.assign({}, props))),
    hex: (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, SettingTypeHex, Object.assign({}, props))),
    keybind: (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, SettingTypeKeybind, Object.assign({}, props)))
  };
  var outline = null;
  var access_message = null;

  if (setting.admins_only) {
    outline = "1px solid rgb(0, 255, 255)";
    access_message = "Эта настройка доступна вам, так как вы админ.";
  } else if (setting.supporters_only) {
    outline = "1px solid rgb(255, 200, 0)";
    access_message = "Эта настройка доступна вам, так как вы поддерживаете сервер.";
  }

  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    "style": {
      "outline": outline
    },
    children: (0, _inferno.createComponentVNode)(2, _components.Flex, {
      "wrap": "wrap",
      children: [(0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
        "basis": "30%",
        "grow": "2",
        "color": "label",
        "pr": "1em",
        "bold": true,
        children: setting.name
      }), (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
        "basis": "50%",
        "shrink": true,
        "grow": "2",
        children: settingTypes[setting.v_type] || "Unknown Setting Type"
      }), (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
        "basis": "4em",
        "grow": "1",
        "textAlign": "right",
        children: !setting["default"] && (0, _inferno.createComponentVNode)(2, _components.Button, {
          "tooltip": "Сбросить",
          "color": "neutral",
          "icon": "undo",
          "onClick": function () {
            function onClick() {
              return act("reset_value", {
                type: setting.type
              });
            }

            return onClick;
          }()
        })
      }), (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
        "basis": "100%",
        "pt": "1em",
        "style": {
          "white-space": "pre-wrap"
        },
        children: [setting.description, access_message && (0, _inferno.createFragment)([(0, _inferno.createVNode)(1, "br"), (0, _inferno.createVNode)(1, "br"), (0, _inferno.createVNode)(1, "i", null, access_message, 0)], 4)]
      })]
    })
  });
};

var SettingTypeRange = function SettingTypeRange(props, context) {
  var _useBackend4 = (0, _backend.useBackend)(context),
      act = _useBackend4.act,
      data = _useBackend4.data;

  var setting = props.setting; // approximately for default windows width
  // should be part of the slider option...

  var relativeStepSize = 250 / (setting.v_parameters[1] - setting.v_parameters[0]);

  if (setting.v_parameters[2]) {
    relativeStepSize = relativeStepSize * setting.v_parameters[2];
  }

  relativeStepSize = Math.floor(relativeStepSize) || 1;
  return (0, _inferno.createComponentVNode)(2, _components.Tooltip, {
    "position": "top",
    "content": "Вы можете единожды нажать на слайдер для установки точного значения",
    children: (0, _inferno.createComponentVNode)(2, _components.Slider, {
      "value": setting.value,
      "minValue": setting.v_parameters[0],
      "maxValue": setting.v_parameters[1],
      "step": setting.v_parameters[2] || 1,
      "stepPixelSize": relativeStepSize,
      "unit": setting.v_parameters[3],
      "onChange": function () {
        function onChange(e, value) {
          return act('set_value', {
            type: setting.type,
            value: value
          });
        }

        return onChange;
      }()
    })
  });
};

var SettingTypeHex = function SettingTypeHex(props, context) {
  var _useBackend5 = (0, _backend.useBackend)(context),
      act = _useBackend5.act,
      data = _useBackend5.data;

  var setting = props.setting;
  return (0, _inferno.createComponentVNode)(2, _components.Button, {
    "onClick": function () {
      function onClick() {
        return act('modify_color_value', {
          type: setting.type
        });
      }

      return onClick;
    }(),
    children: [(0, _inferno.createComponentVNode)(2, _components.ColorBox, {
      "color": setting.value,
      "mr": 0.5
    }), setting.value]
  });
};

var SettingTypeText = function SettingTypeText(props, context) {
  var _useBackend6 = (0, _backend.useBackend)(context),
      act = _useBackend6.act,
      data = _useBackend6.data;

  var setting = props.setting;
  return (0, _inferno.createComponentVNode)(2, _components.Input, {
    "fluid": true,
    "value": setting.value,
    "onInput": function () {
      function onInput(e, value) {
        return act('set_value', {
          type: setting.type,
          value: value
        });
      }

      return onInput;
    }()
  });
};

var SettingTypeSelect = function SettingTypeSelect(props, context) {
  var _useBackend7 = (0, _backend.useBackend)(context),
      act = _useBackend7.act,
      data = _useBackend7.data;

  var setting = props.setting; // dropdown is not select, apparently, and we can't do different value/text, so this part is a mess

  if (Array.isArray(setting.v_parameters)) {
    return (0, _inferno.createComponentVNode)(2, _components.Dropdown, {
      "width": "100%",
      "options": setting.v_parameters,
      "selected": setting.value,
      "noscroll": true,
      "onSelected": function () {
        function onSelected(value) {
          return act('set_value', {
            type: setting.type,
            value: value
          });
        }

        return onSelected;
      }()
    });
  } else {
    return (0, _inferno.createComponentVNode)(2, _components.Dropdown, {
      "width": "100%",
      "options": Object.values(setting.v_parameters),
      "selected": setting.v_parameters[setting.value],
      "noscroll": true,
      "onSelected": function () {
        function onSelected(value) {
          return act('set_value', {
            type: setting.type,
            value: Object.keys(setting.v_parameters).find(function (key) {
              return setting.v_parameters[key] === value;
            })
          });
        }

        return onSelected;
      }()
    });
  }
};

var SettingTypeBoolean = function SettingTypeBoolean(props, context) {
  var _useBackend8 = (0, _backend.useBackend)(context),
      act = _useBackend8.act,
      data = _useBackend8.data;

  var setting = props.setting;
  return (0, _inferno.createComponentVNode)(2, _components.Button.Checkbox, {
    "checked": setting.value,
    "selected": setting.value,
    "content": setting.value ? "Включено" : "Выключено",
    "onClick": function () {
      function onClick() {
        return act('set_value', {
          type: setting.type,
          value: !setting.value
        });
      }

      return onClick;
    }()
  });
};

var SettingTypeKeybind = function SettingTypeKeybind(props, context) {
  var _useBackend9 = (0, _backend.useBackend)(context),
      act = _useBackend9.act,
      data = _useBackend9.data;

  var setting = props.setting,
      setKeyBindingState = props.setKeyBindingState;

  var humaniseByondKey = function humaniseByondKey(text) {
    var patterns = Object.keys(byond2jsKeysDictionary).join("|");
    return text.replace(new RegExp(patterns, "g"), function (match) {
      return byond2jsKeysDictionary[match];
    });
  };

  var binds = [];

  if (setting.value) {
    binds = setting.value.split(" ");
  }

  var buttons = [];

  var _loop = function _loop(i) {
    buttons.push((0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
      "basis": "33.3%",
      children: (0, _inferno.createComponentVNode)(2, _components.Button, {
        "fluid": true,
        "textAlign": "center",
        "content": binds[i] ? humaniseByondKey(binds[i]) : "---",
        "color": !binds[i] && "neutral",
        "onClick": function () {
          function onClick() {
            return setKeyBindingState({
              type: setting.type,
              index: i
            });
          }

          return onClick;
        }()
      })
    }));
  };

  for (var i = 0; i < 3; i++) {
    _loop(i);
  }

  return (0, _inferno.createComponentVNode)(2, _components.Stack, {
    children: buttons
  });
};

/***/ }),

/***/ 10996:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Cryo = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var damageTypes = [{
  label: "Асфиксия",
  type: "oxyLoss"
}, {
  label: "Интоксикация",
  type: "toxLoss"
}, {
  label: "Механические",
  type: "bruteLoss"
}, {
  label: "Термические",
  type: "fireLoss"
}, {
  label: "Генетические",
  type: "cloneLoss"
}];
var statNames = [["good", "В сознании"], ["average", "Без сознания"], ["bad", "Мёртв"]];

var Cryo = function Cryo(props, context) {
  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "width": 400,
    "height": 425,
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      "className": "Layout__content--flexColumn",
      children: (0, _inferno.createComponentVNode)(2, CryoContent)
    })
  });
};

exports.Cryo = Cryo;

var CryoContent = function CryoContent(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var isOperating = data.isOperating,
      hasOccupant = data.hasOccupant,
      _data$occupant = data.occupant,
      occupant = _data$occupant === void 0 ? [] : _data$occupant,
      cellTemperature = data.cellTemperature,
      cellTemperatureStatus = data.cellTemperatureStatus,
      isBeakerLoaded = data.isBeakerLoaded,
      isOpen = data.isOpen,
      hasAir = data.hasAir;
  return (0, _inferno.createFragment)([(0, _inferno.createComponentVNode)(2, _components.Section, {
    "title": "Пациент",
    "flexGrow": "1",
    "buttons": (0, _inferno.createComponentVNode)(2, _components.Button, {
      "icon": isOpen ? "toggle-off" : "toggle-on",
      "onClick": function () {
        function onClick() {
          return act(isOpen ? 'close' : 'open');
        }

        return onClick;
      }(),
      "selected": !isOpen,
      children: isOpen ? "Открыто" : "Закрыто"
    }),
    children: hasOccupant ? (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
      children: [(0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Пациент",
        children: occupant.name || "Неизвестно"
      }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Здоровье",
        children: (0, _inferno.createComponentVNode)(2, _components.ProgressBar, {
          "min": occupant.health,
          "max": occupant.maxHealth,
          "value": occupant.health / occupant.maxHealth,
          "color": occupant.health > 0 ? 'good' : 'average',
          children: (0, _inferno.createComponentVNode)(2, _components.AnimatedNumber, {
            "value": Math.round(occupant.health)
          })
        })
      }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Состояние",
        "color": statNames[occupant.stat][0],
        children: statNames[occupant.stat][1]
      }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Температура",
        children: [(0, _inferno.createComponentVNode)(2, _components.AnimatedNumber, {
          "value": Math.round(occupant.bodyTemperature)
        }), ' K']
      }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Divider), damageTypes.map(function (damageType) {
        return (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
          "label": damageType.label,
          children: (0, _inferno.createComponentVNode)(2, _components.ProgressBar, {
            "value": occupant[damageType.type] / 100,
            "ranges": {
              bad: [0.25, Infinity]
            },
            children: (0, _inferno.createComponentVNode)(2, _components.AnimatedNumber, {
              "value": Math.round(occupant[damageType.type])
            })
          })
        }, damageType.id);
      })]
    }) : (0, _inferno.createComponentVNode)(2, _components.Flex, {
      "height": "100%",
      "textAlign": "center",
      children: (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
        "grow": "1",
        "align": "center",
        "color": "label",
        children: [(0, _inferno.createComponentVNode)(2, _components.Icon, {
          "name": "user-slash",
          "mb": "0.5rem",
          "size": "5"
        }), (0, _inferno.createVNode)(1, "br"), "\u041F\u0430\u0446\u0438\u0435\u043D\u0442 \u043D\u0435 \u043E\u0431\u043D\u0430\u0440\u0443\u0436\u0435\u043D."]
      })
    })
  }), (0, _inferno.createComponentVNode)(2, _components.Section, {
    "title": "Капсула",
    "buttons": (0, _inferno.createComponentVNode)(2, _components.Button, {
      "icon": "eject",
      "onClick": function () {
        function onClick() {
          return act('ejectBeaker');
        }

        return onClick;
      }(),
      "disabled": !isBeakerLoaded,
      children: "\u0418\u0437\u0432\u043B\u0435\u0447\u044C \u0441\u043E\u0441\u0443\u0434"
    }),
    children: (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
      children: [(0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Питание",
        children: (0, _inferno.createComponentVNode)(2, _components.Button, {
          "icon": "power-off",
          "onClick": function () {
            function onClick() {
              return act(isOperating ? 'switchOff' : 'switchOn');
            }

            return onClick;
          }(),
          "selected": isOperating,
          "disabled": isOpen || !hasAir,
          children: isOperating ? "Вкл" : "Выкл"
        })
      }), hasAir ? (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Температура воздуха",
        "color": cellTemperatureStatus,
        children: [(0, _inferno.createComponentVNode)(2, _components.AnimatedNumber, {
          "value": cellTemperature
        }), " K"]
      }) : (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Состояние воздуха",
        "color": "bad",
        children: "\u041D\u0435\u0442 \u0432\u043E\u0437\u0434\u0443\u0445\u0430"
      }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Сосуд",
        children: (0, _inferno.createComponentVNode)(2, CryoBeaker)
      })]
    })
  })], 4);
};

var CryoBeaker = function CryoBeaker(props, context) {
  var _useBackend2 = (0, _backend.useBackend)(context),
      act = _useBackend2.act,
      data = _useBackend2.data;

  var isBeakerLoaded = data.isBeakerLoaded,
      beakerVolume = data.beakerVolume;

  if (isBeakerLoaded) {
    return (0, _inferno.createComponentVNode)(2, _components.Box, {
      "color": !beakerVolume && "bad",
      children: beakerVolume ? (0, _inferno.createComponentVNode)(2, _components.AnimatedNumber, {
        "value": beakerVolume,
        "format": function () {
          function format(v) {
            return Math.round(v) + " юнитов осталось";
          }

          return format;
        }()
      }) : "Сосуд пуст"
    });
  } else {
    return (0, _inferno.createComponentVNode)(2, _components.Box, {
      "color": "average",
      children: "\u041E\u0442\u0441\u0443\u0442\u0441\u0442\u0432\u0443\u0435\u0442"
    });
  }
};

/***/ }),

/***/ 26617:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.CustomAnnounce = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _layouts = __webpack_require__(2381);

var _components = __webpack_require__(26949);

var CustomAnnounce = function CustomAnnounce(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "width": 600,
    "height": 500,
    children: (0, _inferno.createComponentVNode)(2, _components.Flex, {
      "height": "100%",
      "direction": "column",
      children: [(0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
        "grow": 1,
        "position": "relative",
        children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
          children: (0, _inferno.createComponentVNode)(2, _components.Section, {
            "title": "Preview",
            "fill": true,
            children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
              "bold": true,
              "fontSize": "24px",
              children: data.title === null ? "<NO TITLE>" : data.title
            }), (0, _inferno.createComponentVNode)(2, _components.Box, {
              "fontSize": "18px",
              children: data.subtitle === null ? "<NO SUBTITLE>" : data.subtitle
            }), (0, _inferno.createComponentVNode)(2, _components.Box, {
              "mt": 2,
              children: data.message === null ? "<NO MESSAGE>" : data.message
            }), (0, _inferno.createComponentVNode)(2, _components.Box, {
              "italic": true,
              children: [data.announcer !== null && "-", " ", data.announcer]
            }), (0, _inferno.createComponentVNode)(2, _components.Box, {
              "mt": 2,
              children: ["Current sound is \"", data.sound, "\""]
            })]
          })
        })
      }), (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
        "mx": 1,
        "mb": 1,
        children: (0, _inferno.createComponentVNode)(2, _components.Section, {
          "title": "Settings",
          children: [(0, _inferno.createComponentVNode)(2, _components.LabeledList, {
            children: [(0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
              "label": "Content",
              children: [(0, _inferno.createComponentVNode)(2, _components.Button, {
                "content": "Title",
                "icon": "heading",
                "onClick": function () {
                  function onClick() {
                    return act('title');
                  }

                  return onClick;
                }()
              }), (0, _inferno.createComponentVNode)(2, _components.Button, {
                "ml": 1,
                "content": "Subtitle",
                "icon": "paragraph",
                "onClick": function () {
                  function onClick() {
                    return act('subtitle');
                  }

                  return onClick;
                }()
              }), (0, _inferno.createComponentVNode)(2, _components.Button, {
                "ml": 1,
                "content": "Message",
                "icon": "pencil-alt",
                "onClick": function () {
                  function onClick() {
                    return act('message');
                  }

                  return onClick;
                }()
              }), (0, _inferno.createComponentVNode)(2, _components.Button, {
                "ml": 1,
                "mr": 2,
                "content": "Announcer",
                "icon": "bullhorn",
                "onClick": function () {
                  function onClick() {
                    return act('announcer');
                  }

                  return onClick;
                }()
              })]
            }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
              "label": "Output",
              children: [(0, _inferno.createComponentVNode)(2, _components.Button, {
                "content": "Chat",
                "icon": "comments",
                "selected": data.flags.text,
                "onClick": function () {
                  function onClick() {
                    return act('flag_text');
                  }

                  return onClick;
                }()
              }), (0, _inferno.createComponentVNode)(2, _components.Button, {
                "mx": 1,
                "content": "Sound",
                "icon": "volume-up",
                "selected": data.flags.sound,
                "onClick": function () {
                  function onClick() {
                    return act('flag_sound');
                  }

                  return onClick;
                }()
              }), (0, _inferno.createComponentVNode)(2, _components.Button, {
                "content": "Consoles",
                "icon": "sticky-note",
                "selected": data.flags.comms,
                "onClick": function () {
                  function onClick() {
                    return act('flag_comms');
                  }

                  return onClick;
                }()
              })]
            }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
              "label": "Sound",
              children: [(0, _inferno.createComponentVNode)(2, _components.Button, {
                "content": "Existing",
                "icon": "list",
                "disabled": !data.rights.funevent,
                "onClick": function () {
                  function onClick() {
                    return act('sound_select');
                  }

                  return onClick;
                }()
              }), (0, _inferno.createComponentVNode)(2, _components.Button, {
                "ml": 1,
                "content": "Upload",
                "icon": "file-upload",
                "disabled": !(data.rights.sound && data.rights.funevent),
                "onClick": function () {
                  function onClick() {
                    return act('sound_upload');
                  }

                  return onClick;
                }()
              })]
            }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
              "label": "Volume",
              children: [(0, _inferno.createComponentVNode)(2, _components.NumberInput, {
                "animated": true,
                "ml": 1,
                "value": parseInt(data.volume, 10),
                "disabled": !(data.rights.sound && data.rights.funevent),
                "width": "60px",
                "step": 1,
                "unit": "%",
                "minValue": 0,
                "maxValue": 200,
                "onChange": function () {
                  function onChange(e, value) {
                    return act('volume', {
                      volume: value
                    });
                  }

                  return onChange;
                }()
              }), (0, _inferno.createComponentVNode)(2, _components.Button, {
                "ml": 1,
                "content": "Test",
                "icon": "user",
                "onClick": function () {
                  function onClick() {
                    return act('test', {
                      source: "admin"
                    });
                  }

                  return onClick;
                }()
              }), (0, _inferno.createComponentVNode)(2, _components.Button, {
                "ml": 1,
                "content": "Sample",
                "icon": "closed-captioning",
                "onClick": function () {
                  function onClick() {
                    return act('test', {
                      source: "sample"
                    });
                  }

                  return onClick;
                }()
              })]
            })]
          }), (0, _inferno.createComponentVNode)(2, _components.Box, {
            "mt": 2,
            "mb": 1,
            children: (0, _inferno.createComponentVNode)(2, _components.Button, {
              "content": "Select preset",
              "icon": "archive",
              "disabled": !data.rights.funevent,
              "onClick": function () {
                function onClick() {
                  return act('preset_select');
                }

                return onClick;
              }()
            })
          }), (0, _inferno.createComponentVNode)(2, _components.Box, {
            children: (0, _inferno.createComponentVNode)(2, _components.Button, {
              "content": "Make an announcement",
              "onClick": function () {
                function onClick() {
                  return act('announce');
                }

                return onClick;
              }()
            })
          })]
        })
      })]
    })
  });
};

exports.CustomAnnounce = CustomAnnounce;

/***/ }),

/***/ 69974:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.DisposalUnit = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var DisposalUnit = function DisposalUnit(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var stateColor;
  var stateText;

  if (data.mode === 2) {
    stateColor = "good";
    stateText = "Ready";
  } else if (data.mode === 0) {
    stateColor = "bad";
    stateText = "Off";
  } else if (data.mode < 0) {
    stateColor = "bad";
    stateText = "N/A";
  } else if (data.mode === 1) {
    stateColor = "average";
    stateText = "Pressurizing";
  } else {
    stateColor = "average";
    stateText = "Idle";
  }

  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "width": 300,
    "height": 180,
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      children: (0, _inferno.createComponentVNode)(2, _components.Section, {
        children: (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
          children: [(0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "State",
            "color": stateColor,
            children: stateText
          }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Pressure",
            children: (0, _inferno.createComponentVNode)(2, _components.ProgressBar, {
              "ranges": {
                bad: [-Infinity, 0],
                average: [0, 99],
                good: [99, Infinity]
              },
              "value": data.pressure,
              "minValue": 0,
              "maxValue": 100
            })
          }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Handle",
            children: [(0, _inferno.createComponentVNode)(2, _components.Button, {
              "icon": "toggle-off",
              "disabled": data.isAI || data.panel_open,
              "content": "Disengaged",
              "selected": data.flushing ? null : "selected",
              "onClick": function () {
                function onClick() {
                  return act("handle-0");
                }

                return onClick;
              }()
            }), (0, _inferno.createComponentVNode)(2, _components.Button, {
              "icon": "toggle-on",
              "disabled": data.isAI || data.panel_open,
              "content": "Engaged",
              "selected": data.flushing ? "selected" : null,
              "onClick": function () {
                function onClick() {
                  return act("handle-1");
                }

                return onClick;
              }()
            })]
          }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Power",
            children: [(0, _inferno.createComponentVNode)(2, _components.Button, {
              "icon": "toggle-off",
              "disabled": data.mode === -1,
              "content": "Off",
              "selected": data.mode ? null : "selected",
              "onClick": function () {
                function onClick() {
                  return act("pump-0");
                }

                return onClick;
              }()
            }), (0, _inferno.createComponentVNode)(2, _components.Button, {
              "icon": "toggle-on",
              "disabled": data.mode === -1,
              "content": "On",
              "selected": data.mode ? "selected" : null,
              "onClick": function () {
                function onClick() {
                  return act("pump-1");
                }

                return onClick;
              }()
            })]
          }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Eject",
            children: (0, _inferno.createComponentVNode)(2, _components.Button, {
              "icon": "sign-out-alt",
              "disabled": data.isAI,
              "content": "Eject Contents",
              "onClick": function () {
                function onClick() {
                  return act("eject");
                }

                return onClick;
              }()
            })
          })]
        })
      })
    })
  });
};

exports.DisposalUnit = DisposalUnit;

/***/ }),

/***/ 10704:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.EmotePanelEditor = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var EmotePanelEditor = function EmotePanelEditor(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var customEmotes = data.customEmotes,
      allHumanEmotes = data.allHumanEmotes;
  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "width": 250,
    "height": 400,
    "resizable": true,
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      children: allHumanEmotes.sort().map(function (emote) {
        return (0, _inferno.createComponentVNode)(2, _components.Box, {
          "fontSize": "20px",
          children: (0, _inferno.createComponentVNode)(2, _components.Button.Checkbox, {
            "checked": customEmotes.includes(emote),
            "onClick": function () {
              function onClick() {
                return act('switchEmote', {
                  emote: emote
                });
              }

              return onClick;
            }(),
            "content": emote
          })
        }, emote);
      })
    })
  });
};

exports.EmotePanelEditor = EmotePanelEditor;

/***/ }),

/***/ 53600:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Filteriffic = void 0;

var _inferno = __webpack_require__(96323);

var _collections = __webpack_require__(1397);

var _math = __webpack_require__(97622);

var _math2 = __webpack_require__(97622);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var _excluded = ["type", "priority"];

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

var FilterIntegerEntry = function FilterIntegerEntry(props, context) {
  var value = props.value,
      name = props.name,
      filterName = props.filterName;

  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act;

  return (0, _inferno.createComponentVNode)(2, _components.NumberInput, {
    "value": value,
    "minValue": -500,
    "maxValue": 500,
    "stepPixelSize": 5,
    "width": "39px",
    "onDrag": function () {
      function onDrag(e, value) {
        var _new_data;

        return act('modify_filter_value', {
          name: filterName,
          new_data: (_new_data = {}, _new_data[name] = value, _new_data)
        });
      }

      return onDrag;
    }()
  });
};

var FilterFloatEntry = function FilterFloatEntry(props, context) {
  var value = props.value,
      name = props.name,
      filterName = props.filterName;

  var _useBackend2 = (0, _backend.useBackend)(context),
      act = _useBackend2.act;

  var _useLocalState = (0, _backend.useLocalState)(context, filterName + "-" + name, 0.01),
      step = _useLocalState[0],
      setStep = _useLocalState[1];

  return (0, _inferno.createFragment)([(0, _inferno.createComponentVNode)(2, _components.NumberInput, {
    "value": value,
    "minValue": -500,
    "maxValue": 500,
    "stepPixelSize": 4,
    "step": step,
    "format": function () {
      function format(value) {
        return (0, _math.toFixed)(value, (0, _math2.numberOfDecimalDigits)(step));
      }

      return format;
    }(),
    "width": "80px",
    "onDrag": function () {
      function onDrag(e, value) {
        var _new_data2;

        return act('transition_filter_value', {
          name: filterName,
          new_data: (_new_data2 = {}, _new_data2[name] = value, _new_data2)
        });
      }

      return onDrag;
    }()
  }), (0, _inferno.createComponentVNode)(2, _components.Box, {
    "inline": true,
    "ml": 2,
    "mr": 1,
    children: "Step:"
  }), (0, _inferno.createComponentVNode)(2, _components.NumberInput, {
    "value": step,
    "step": 0.01,
    "format": function () {
      function format(value) {
        return (0, _math.toFixed)(value, 4);
      }

      return format;
    }(),
    "width": "70px",
    "onChange": function () {
      function onChange(e, value) {
        return setStep(value);
      }

      return onChange;
    }()
  })], 4);
};

var FilterTextEntry = function FilterTextEntry(props, context) {
  var value = props.value,
      name = props.name,
      filterName = props.filterName;

  var _useBackend3 = (0, _backend.useBackend)(context),
      act = _useBackend3.act;

  return (0, _inferno.createComponentVNode)(2, _components.Input, {
    "value": value,
    "width": "250px",
    "onInput": function () {
      function onInput(e, value) {
        var _new_data3;

        return act('modify_filter_value', {
          name: filterName,
          new_data: (_new_data3 = {}, _new_data3[name] = value, _new_data3)
        });
      }

      return onInput;
    }()
  });
};

var FilterColorEntry = function FilterColorEntry(props, context) {
  var value = props.value,
      filterName = props.filterName,
      name = props.name;

  var _useBackend4 = (0, _backend.useBackend)(context),
      act = _useBackend4.act;

  return (0, _inferno.createFragment)([(0, _inferno.createComponentVNode)(2, _components.Button, {
    "icon": "pencil-alt",
    "onClick": function () {
      function onClick() {
        return act('modify_color_value', {
          name: filterName
        });
      }

      return onClick;
    }()
  }), (0, _inferno.createComponentVNode)(2, _components.ColorBox, {
    "color": value,
    "mr": 0.5
  }), (0, _inferno.createComponentVNode)(2, _components.Input, {
    "value": value,
    "width": "90px",
    "onInput": function () {
      function onInput(e, value) {
        var _new_data4;

        return act('transition_filter_value', {
          name: filterName,
          new_data: (_new_data4 = {}, _new_data4[name] = value, _new_data4)
        });
      }

      return onInput;
    }()
  })], 4);
};

var FilterIconEntry = function FilterIconEntry(props, context) {
  var value = props.value,
      filterName = props.filterName;

  var _useBackend5 = (0, _backend.useBackend)(context),
      act = _useBackend5.act;

  return (0, _inferno.createFragment)([(0, _inferno.createComponentVNode)(2, _components.Button, {
    "icon": "pencil-alt",
    "onClick": function () {
      function onClick() {
        return act('modify_icon_value', {
          name: filterName
        });
      }

      return onClick;
    }()
  }), (0, _inferno.createComponentVNode)(2, _components.Box, {
    "inline": true,
    "ml": 1,
    children: value
  })], 4);
};

var FilterFlagsEntry = function FilterFlagsEntry(props, context) {
  var name = props.name,
      value = props.value,
      filterName = props.filterName,
      filterType = props.filterType;

  var _useBackend6 = (0, _backend.useBackend)(context),
      act = _useBackend6.act,
      data = _useBackend6.data;

  var filterInfo = data.filter_info;
  var flags = filterInfo[filterType]['flags'];
  return (0, _collections.map)(function (bitField, flagName) {
    return (0, _inferno.createComponentVNode)(2, _components.Button.Checkbox, {
      "checked": value & bitField,
      "content": flagName,
      "onClick": function () {
        function onClick() {
          var _new_data5;

          return act('modify_filter_value', {
            name: filterName,
            new_data: (_new_data5 = {}, _new_data5[name] = value ^ bitField, _new_data5)
          });
        }

        return onClick;
      }()
    });
  })(flags);
};

var FilterDataEntry = function FilterDataEntry(props, context) {
  var name = props.name,
      value = props.value,
      hasValue = props.hasValue,
      filterName = props.filterName;
  var filterEntryTypes = {
    "int": (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, FilterIntegerEntry, Object.assign({}, props))),
    "float": (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, FilterFloatEntry, Object.assign({}, props))),
    string: (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, FilterTextEntry, Object.assign({}, props))),
    color: (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, FilterColorEntry, Object.assign({}, props))),
    icon: (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, FilterIconEntry, Object.assign({}, props))),
    flags: (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, FilterFlagsEntry, Object.assign({}, props)))
  };
  var filterEntryMap = {
    x: 'float',
    y: 'float',
    icon: 'icon',
    render_source: 'string',
    flags: 'flags',
    size: 'float',
    color: 'color',
    offset: 'float',
    radius: 'float',
    falloff: 'float',
    density: 'int',
    threshold: 'float',
    factor: 'float',
    repeat: 'int',
    blend_mode: 'int'
  };
  return (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
    "label": name,
    children: [filterEntryTypes[filterEntryMap[name]] || "Not Found (This is an error)", ' ', !hasValue && (0, _inferno.createComponentVNode)(2, _components.Box, {
      "inline": true,
      "color": "average",
      children: "(Default)"
    })]
  });
};

var FilterEntry = function FilterEntry(props, context) {
  var _useBackend7 = (0, _backend.useBackend)(context),
      act = _useBackend7.act,
      data = _useBackend7.data;

  var name = props.name,
      filterDataEntry = props.filterDataEntry;

  var type = filterDataEntry.type,
      priority = filterDataEntry.priority,
      restOfProps = _objectWithoutPropertiesLoose(filterDataEntry, _excluded);

  var filterDefaults = data["filter_info"];
  var targetFilterPossibleKeys = Object.keys(filterDefaults[type]['defaults']);
  return (0, _inferno.createComponentVNode)(2, _components.Collapsible, {
    "title": name + " (" + type + ")",
    "buttons": (0, _inferno.createFragment)([(0, _inferno.createComponentVNode)(2, _components.NumberInput, {
      "value": priority,
      "stepPixelSize": 10,
      "width": "60px",
      "onChange": function () {
        function onChange(e, value) {
          return act('change_priority', {
            name: name,
            new_priority: value
          });
        }

        return onChange;
      }()
    }), (0, _inferno.createComponentVNode)(2, _components.Button.Input, {
      "content": "Rename",
      "placeholder": name,
      "onCommit": function () {
        function onCommit(e, new_name) {
          return act('rename_filter', {
            name: name,
            new_name: new_name
          });
        }

        return onCommit;
      }(),
      "width": "90px"
    }), (0, _inferno.createComponentVNode)(2, _components.Button.Confirm, {
      "icon": "minus",
      "onClick": function () {
        function onClick() {
          return act("remove_filter", {
            name: name
          });
        }

        return onClick;
      }()
    })], 4),
    children: (0, _inferno.createComponentVNode)(2, _components.Section, {
      "level": 2,
      children: (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
        children: targetFilterPossibleKeys.map(function (entryName) {
          var defaults = filterDefaults[type]['defaults'];
          var value = restOfProps[entryName] || defaults[entryName];
          var hasValue = value !== defaults[entryName];
          return (0, _inferno.createComponentVNode)(2, FilterDataEntry, {
            "filterName": name,
            "filterType": type,
            "name": entryName,
            "value": value,
            "hasValue": hasValue
          }, entryName);
        })
      })
    })
  });
};

var Filteriffic = function Filteriffic(props, context) {
  var _useBackend8 = (0, _backend.useBackend)(context),
      act = _useBackend8.act,
      data = _useBackend8.data;

  var name = data.target_name || "Unknown Object";
  var filters = data.target_filter_data || {};
  var hasFilters = Object.keys(filters).length !== 0;
  var filterDefaults = data["filter_info"];

  var _useLocalState2 = (0, _backend.useLocalState)(context, 'massApplyPath', ''),
      massApplyPath = _useLocalState2[0],
      setMassApplyPath = _useLocalState2[1];

  var _useLocalState3 = (0, _backend.useLocalState)(context, 'hidden', false),
      hiddenSecret = _useLocalState3[0],
      setHiddenSecret = _useLocalState3[1];

  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "title": "Filteriffic",
    "width": 500,
    "height": 500,
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      "scrollable": true,
      children: [(0, _inferno.createComponentVNode)(2, _components.NoticeBox, {
        "danger": true,
        children: "DO NOT MESS WITH EXISTING FILTERS IF YOU DO NOT KNOW THE CONSEQUENCES. YOU HAVE BEEN WARNED."
      }), (0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": hiddenSecret ? (0, _inferno.createFragment)([(0, _inferno.createComponentVNode)(2, _components.Box, {
          "mr": 0.5,
          "inline": true,
          children: "MASS EDIT:"
        }), (0, _inferno.createComponentVNode)(2, _components.Input, {
          "value": massApplyPath,
          "width": "100px",
          "onInput": function () {
            function onInput(e, value) {
              return setMassApplyPath(value);
            }

            return onInput;
          }()
        }), (0, _inferno.createComponentVNode)(2, _components.Button.Confirm, {
          "content": "Apply",
          "confirmContent": "ARE YOU SURE?",
          "onClick": function () {
            function onClick() {
              return act('mass_apply', {
                path: massApplyPath
              });
            }

            return onClick;
          }()
        })], 4) : (0, _inferno.createComponentVNode)(2, _components.Box, {
          "inline": true,
          "onDblClick": function () {
            function onDblClick() {
              return setHiddenSecret(true);
            }

            return onDblClick;
          }(),
          children: name
        }),
        "buttons": (0, _inferno.createComponentVNode)(2, _components.Dropdown, {
          "icon": "plus",
          "displayText": "Add Filter",
          "nochevron": true,
          "options": Object.keys(filterDefaults),
          "onSelected": function () {
            function onSelected(value) {
              return act('add_filter', {
                name: 'default',
                priority: 10,
                type: value
              });
            }

            return onSelected;
          }()
        }),
        children: !hasFilters ? (0, _inferno.createComponentVNode)(2, _components.Box, {
          children: "No filters"
        }) : (0, _collections.map)(function (entry, key) {
          return (0, _inferno.createComponentVNode)(2, FilterEntry, {
            "filterDataEntry": entry,
            "name": key
          }, key);
        })(filters)
      })]
    })
  });
};

exports.Filteriffic = Filteriffic;

/***/ }),

/***/ 24482:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.GPS = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var vectorText = function vectorText(vector) {
  return vector ? "(" + vector.join(", ") + ")" : "ERROR";
};

var rad2deg = function rad2deg(rad) {
  return rad * (180 / Math.PI);
};

var distanceToPoint = function distanceToPoint(from, to) {
  if (!from || !to) {
    return;
  } // Different Z-level


  if (from[2] !== to[2]) {
    return null;
  }

  var angle = Math.atan2(to[1] - from[1], to[0] - from[0]);
  var dist = Math.sqrt(Math.pow(to[1] - from[1], 2) + Math.pow(to[0] - from[0], 2));
  return {
    angle: rad2deg(angle),
    distance: dist
  };
};

var GPS = function GPS(properties, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      data = _useBackend.data;

  var emped = data.emped,
      active = data.active,
      area = data.area,
      position = data.position,
      saved = data.saved;
  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      children: (0, _inferno.createComponentVNode)(2, _components.Flex, {
        "direction": "column",
        "height": "100%",
        children: emped ? (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
          "grow": "1",
          "basis": "0",
          children: (0, _inferno.createComponentVNode)(2, TurnedOff, {
            "emp": true
          })
        }) : (0, _inferno.createFragment)([(0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
          children: (0, _inferno.createComponentVNode)(2, Settings)
        }), active ? (0, _inferno.createFragment)([(0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
          "mt": "0.5rem",
          children: (0, _inferno.createComponentVNode)(2, Position, {
            "area": area,
            "position": position
          })
        }), saved && (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
          "mt": "0.5rem",
          children: (0, _inferno.createComponentVNode)(2, Position, {
            "title": "Saved Position",
            "position": saved
          })
        }), (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
          "mt": "0.5rem",
          "grow": "1",
          "basis": "0",
          children: (0, _inferno.createComponentVNode)(2, Signals, {
            "height": "100%"
          })
        })], 0) : (0, _inferno.createComponentVNode)(2, TurnedOff)], 0)
      })
    })
  });
};

exports.GPS = GPS;

var TurnedOff = function TurnedOff(_ref, context) {
  var emp = _ref.emp;
  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    "mt": "0.5rem",
    "width": "100%",
    "height": "100%",
    "stretchContents": true,
    children: (0, _inferno.createComponentVNode)(2, _components.Box, {
      "width": "100%",
      "height": "100%",
      "color": "label",
      "textAlign": "center",
      children: (0, _inferno.createComponentVNode)(2, _components.Flex, {
        "height": "100%",
        children: (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
          "grow": "1",
          "align": "center",
          "color": "label",
          children: [(0, _inferno.createComponentVNode)(2, _components.Icon, {
            "name": emp ? "ban" : "power-off",
            "mb": "0.5rem",
            "size": "5"
          }), (0, _inferno.createVNode)(1, "br"), emp ? "ERROR: Device temporarily lost signal." : "Device is disabled."]
        })
      })
    })
  });
};

var Settings = function Settings(properties, context) {
  var _useBackend2 = (0, _backend.useBackend)(context),
      act = _useBackend2.act,
      data = _useBackend2.data;

  var active = data.active,
      tag = data.tag,
      same_z = data.same_z;

  var _useLocalState = (0, _backend.useLocalState)(context, "newTag", tag),
      newTag = _useLocalState[0],
      setNewTag = _useLocalState[1];

  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    "title": "Settings",
    "buttons": (0, _inferno.createComponentVNode)(2, _components.Button, {
      "selected": active,
      "icon": active ? "toggle-on" : "toggle-off",
      "content": active ? "On" : "Off",
      "onClick": function () {
        function onClick() {
          return act('toggle');
        }

        return onClick;
      }()
    }),
    children: (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
      children: [(0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Tag",
        children: [(0, _inferno.createComponentVNode)(2, _components.Input, {
          "width": "5rem",
          "value": tag,
          "onEnter": function () {
            function onEnter() {
              return act('tag', {
                newtag: newTag
              });
            }

            return onEnter;
          }(),
          "onInput": function () {
            function onInput(e, value) {
              return setNewTag(value);
            }

            return onInput;
          }()
        }), (0, _inferno.createComponentVNode)(2, _components.Button, {
          "disabled": tag === newTag,
          "width": "20px",
          "mb": "0",
          "ml": "0.25rem",
          "onClick": function () {
            function onClick() {
              return act('tag', {
                newtag: newTag
              });
            }

            return onClick;
          }(),
          children: (0, _inferno.createComponentVNode)(2, _components.Icon, {
            "name": "pen"
          })
        })]
      }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Range",
        children: (0, _inferno.createComponentVNode)(2, _components.Button, {
          "selected": !same_z,
          "icon": same_z ? "compress" : "expand",
          "content": same_z ? "Local Sector" : "Global",
          "onClick": function () {
            function onClick() {
              return act('same_z');
            }

            return onClick;
          }()
        })
      })]
    })
  });
};

var Position = function Position(_ref2, context) {
  var title = _ref2.title,
      area = _ref2.area,
      position = _ref2.position;
  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    "title": title || "Position",
    children: (0, _inferno.createComponentVNode)(2, _components.Box, {
      "fontSize": "1.5rem",
      children: [area && (0, _inferno.createFragment)([area, (0, _inferno.createVNode)(1, "br")], 0), vectorText(position)]
    })
  });
};

var Signals = function Signals(properties, context) {
  var _useBackend3 = (0, _backend.useBackend)(context),
      data = _useBackend3.data;

  var position = data.position,
      signals = data.signals;
  return (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, _components.Section, Object.assign({
    "title": "Signals",
    "overflow": "auto"
  }, properties, {
    children: (0, _inferno.createComponentVNode)(2, _components.Table, {
      children: signals.map(function (signal) {
        return Object.assign({}, signal, distanceToPoint(position, signal.position));
      }).map(function (signal, i) {
        return (0, _inferno.createComponentVNode)(2, _components.Table.Row, {
          "backgroundColor": i % 2 === 0 && "rgba(255, 255, 255, 0.05)",
          children: [(0, _inferno.createComponentVNode)(2, _components.Table.Cell, {
            "width": "30%",
            "verticalAlign": "middle",
            "color": "label",
            "p": "0.25rem",
            "bold": true,
            children: signal.tag
          }), (0, _inferno.createComponentVNode)(2, _components.Table.Cell, {
            "verticalAlign": "middle",
            "color": "grey",
            children: signal.area
          }), (0, _inferno.createComponentVNode)(2, _components.Table.Cell, {
            "verticalAlign": "middle",
            "collapsing": true,
            children: signal.distance !== undefined && (0, _inferno.createComponentVNode)(2, _components.Box, {
              "opacity": Math.max(1 - Math.min(signal.distance, 100) / 100, 0.5),
              children: [(0, _inferno.createComponentVNode)(2, _components.Icon, {
                "name": signal.distance > 0 ? "arrow-right" : "circle",
                "rotation": -signal.angle
              }), "\xA0", Math.floor(signal.distance) + "m"]
            })
          }), (0, _inferno.createComponentVNode)(2, _components.Table.Cell, {
            "verticalAlign": "middle",
            "pr": "0.25rem",
            "collapsing": true,
            children: vectorText(signal.position)
          })]
        }, i);
      })
    })
  })));
};

/***/ }),

/***/ 19609:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.GuestPass = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var _AccessList = __webpack_require__(52126);

var GuestPass = function GuestPass(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      "scrollable": true,
      children: [(0, _inferno.createComponentVNode)(2, _components.Tabs, {
        children: [(0, _inferno.createComponentVNode)(2, _components.Tabs.Tab, {
          "icon": "id-card",
          "selected": !data.showlogs,
          "onClick": function () {
            function onClick() {
              return act("mode", {
                mode: 0
              });
            }

            return onClick;
          }(),
          children: "Issue Pass"
        }), (0, _inferno.createComponentVNode)(2, _components.Tabs.Tab, {
          "icon": "scroll",
          "selected": data.showlogs,
          "onClick": function () {
            function onClick() {
              return act("mode", {
                mode: 1
              });
            }

            return onClick;
          }(),
          children: ["Records (", data.issue_log.length, ")"]
        })]
      }), (0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": "Authorization",
        children: (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
          children: (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "ID Card",
            children: (0, _inferno.createComponentVNode)(2, _components.Button, {
              "icon": data.scan_name ? 'eject' : 'id-card',
              "selected": data.scan_name,
              "content": data.scan_name ? data.scan_name : '-----',
              "tooltip": data.scan_name ? "Eject ID" : "Insert ID",
              "onClick": function () {
                function onClick() {
                  return act("scan");
                }

                return onClick;
              }()
            })
          })
        })
      }), !data.showlogs && (0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": "Issue Guest Pass",
        children: [(0, _inferno.createComponentVNode)(2, _components.LabeledList, {
          children: [(0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Issue To",
            children: (0, _inferno.createComponentVNode)(2, _components.Button, {
              "icon": "pencil-alt",
              "content": data.giv_name ? data.giv_name : "-----",
              "disabled": !data.scan_name,
              "onClick": function () {
                function onClick() {
                  return act("giv_name");
                }

                return onClick;
              }()
            })
          }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Reason",
            children: (0, _inferno.createComponentVNode)(2, _components.Button, {
              "icon": "pencil-alt",
              "content": data.reason ? data.reason : "-----",
              "disabled": !data.scan_name,
              "onClick": function () {
                function onClick() {
                  return act("reason");
                }

                return onClick;
              }()
            })
          }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Duration",
            children: (0, _inferno.createComponentVNode)(2, _components.Button, {
              "icon": "pencil-alt",
              "content": data.duration ? data.duration : "-----",
              "disabled": !data.scan_name,
              "onClick": function () {
                function onClick() {
                  return act("duration");
                }

                return onClick;
              }()
            })
          })]
        }), !!data.scan_name && (0, _inferno.createFragment)([(0, _inferno.createComponentVNode)(2, _AccessList.AccessList, {
          "grantableList": data.grantableList,
          "accesses": data.regions,
          "selectedList": data.selectedAccess,
          "accessMod": function () {
            function accessMod(ref) {
              return act('access', {
                access: ref
              });
            }

            return accessMod;
          }(),
          "grantAll": function () {
            function grantAll() {
              return act('grant_all');
            }

            return grantAll;
          }(),
          "denyAll": function () {
            function denyAll() {
              return act('clear_all');
            }

            return denyAll;
          }(),
          "grantDep": function () {
            function grantDep(ref) {
              return act('grant_region', {
                region: ref
              });
            }

            return grantDep;
          }(),
          "denyDep": function () {
            function denyDep(ref) {
              return act('deny_region', {
                region: ref
              });
            }

            return denyDep;
          }()
        }), (0, _inferno.createComponentVNode)(2, _components.Button, {
          "icon": "id-card",
          "content": data.printmsg,
          "disabled": !data.canprint,
          "onClick": function () {
            function onClick() {
              return act("issue");
            }

            return onClick;
          }()
        })], 4)]
      }), !!data.showlogs && (0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": "Issuance Log",
        children: !!data.issue_log.length && (0, _inferno.createFragment)([(0, _inferno.createComponentVNode)(2, _components.LabeledList, {
          children: data.issue_log.map(function (a, i) {
            return (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
              children: a
            }, i);
          })
        }), (0, _inferno.createComponentVNode)(2, _components.Button, {
          "icon": "print",
          "content": "Print",
          "disabled": !data.scan_name,
          "onClick": function () {
            function onClick() {
              return act("print");
            }

            return onClick;
          }()
        })], 4) || (0, _inferno.createComponentVNode)(2, _components.Box, {
          children: "None."
        })
      })]
    })
  });
};

exports.GuestPass = GuestPass;

/***/ }),

/***/ 96696:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Loader = exports.ListInput = void 0;

var _inferno = __webpack_require__(96323);

var _string = __webpack_require__(21118);

var _math = __webpack_require__(97622);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var _keycodes = __webpack_require__(57890);

var _hotkeys = __webpack_require__(6965);

/**
 * @file
 * @copyright 2020 watermelon914 (https://github.com/watermelon914)
 * @license MIT
 */
var lastScrollTime = 0;

var ListInput = function ListInput(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var title = data.title,
      message = data.message,
      buttons = data.buttons,
      timeout = data.timeout; // Search

  var _useLocalState = (0, _backend.useLocalState)(context, 'search_bar', false),
      showSearchBar = _useLocalState[0],
      setShowSearchBar = _useLocalState[1];

  var _useLocalState2 = (0, _backend.useLocalState)(context, 'displayed_array', buttons),
      displayedArray = _useLocalState2[0],
      setDisplayedArray = _useLocalState2[1]; // KeyPress


  var _useLocalState3 = (0, _backend.useLocalState)(context, 'search_array', []),
      searchArray = _useLocalState3[0],
      setSearchArray = _useLocalState3[1];

  var _useLocalState4 = (0, _backend.useLocalState)(context, 'search_index', 0),
      searchIndex = _useLocalState4[0],
      setSearchIndex = _useLocalState4[1];

  var _useLocalState5 = (0, _backend.useLocalState)(context, 'last_char_code', null),
      lastCharCode = _useLocalState5[0],
      setLastCharCode = _useLocalState5[1]; // Selected Button


  var _useLocalState6 = (0, _backend.useLocalState)(context, 'selected_button', buttons[0]),
      selectedButton = _useLocalState6[0],
      setSelectedButton = _useLocalState6[1];

  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "title": title,
    "width": 325,
    "height": 325,
    "resizable": true,
    children: [timeout !== undefined && (0, _inferno.createComponentVNode)(2, Loader, {
      "value": timeout
    }), (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      children: (0, _inferno.createComponentVNode)(2, _components.Flex, {
        "direction": "column",
        "height": "100%",
        children: [(0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
          "grow": 1,
          children: (0, _inferno.createComponentVNode)(2, _components.Section, {
            "scrollable": true,
            "className": "ListInput__Section",
            "width": "100%",
            "fill": true,
            "title": message,
            "tabIndex": 0,
            "onKeyDown": function () {
              function onKeyDown(e) {
                e.preventDefault();
                (0, _hotkeys.acquireHotKey)(_keycodes.KEY_DOWN);
                (0, _hotkeys.acquireHotKey)(_keycodes.KEY_UP);

                if (lastScrollTime > performance.now()) {
                  return;
                }

                lastScrollTime = performance.now() + 125;

                if (e.keyCode === _keycodes.KEY_UP || e.keyCode === _keycodes.KEY_DOWN) {
                  var direction = 1;

                  if (e.keyCode === _keycodes.KEY_UP) {
                    direction = -1;
                  }

                  {}
                  var index = 0;

                  for (index; index < buttons.length; index++) {
                    if (buttons[index] === selectedButton) {
                      break;
                    }
                  }

                  {
                    index += direction;
                  }

                  if (index < 0) {
                    index = buttons.length - 1;
                  } else if (index >= buttons.length) {
                    index = 0;
                  }

                  setSelectedButton(buttons[index]);
                  setLastCharCode(null);
                  document.getElementById(buttons[index]).focus();
                  return;
                }

                if (e.keyCode === _keycodes.KEY_SPACE || e.keyCode === _keycodes.KEY_ENTER) {
                  act("choose", {
                    choice: selectedButton
                  });
                  return;
                }

                var charCode = String.fromCharCode(e.keyCode).toLowerCase();

                if (!charCode) {
                  return;
                }

                var foundValue;

                if (charCode === lastCharCode && searchArray.length > 0) {
                  var nextIndex = searchIndex + 1;

                  if (nextIndex < searchArray.length) {
                    foundValue = searchArray[nextIndex];
                    setSearchIndex(nextIndex);
                  } else {
                    foundValue = searchArray[0];
                    setSearchIndex(0);
                  }
                } else {
                  var resultArray = displayedArray.filter(function (value) {
                    return value.substring(0, 1).toLowerCase() === charCode;
                  });

                  if (resultArray.length > 0) {
                    setSearchArray(resultArray);
                    setSearchIndex(0);
                    foundValue = resultArray[0];
                  }
                }

                if (foundValue) {
                  setLastCharCode(charCode);
                  setSelectedButton(foundValue);
                  document.getElementById(foundValue).focus();
                }
              }

              return onKeyDown;
            }(),
            "buttons": (0, _inferno.createComponentVNode)(2, _components.Button, {
              "icon": "search",
              "color": "transparent",
              "selected": showSearchBar,
              "tooltip": "Search Bar",
              "tooltipPosition": "left",
              "onClick": function () {
                function onClick() {
                  setShowSearchBar(!showSearchBar);
                  setDisplayedArray(buttons);
                }

                return onClick;
              }(),
              "compact": true
            }),
            children: (0, _inferno.createComponentVNode)(2, _components.Flex, {
              "wrap": "wrap",
              children: displayedArray.map(function (button) {
                return (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
                  "basis": "100%",
                  children: (0, _inferno.createComponentVNode)(2, _components.Button, {
                    "color": "transparent",
                    "content": button,
                    "id": button,
                    "width": "100%",
                    "selected": selectedButton === button,
                    "onClick": function () {
                      function onClick() {
                        if (selectedButton === button) {
                          act("choose", {
                            choice: button
                          });
                        } else {
                          setSelectedButton(button);
                          var index = 0;

                          for (index; index < buttons.length; index++) {
                            if (buttons[index] === button) {
                              break;
                            }
                          }

                          if (index < 0) {
                            index = buttons.length - 1;
                          } else if (index >= buttons.length) {
                            index = 0;
                          }

                          setLastCharCode(null);
                          document.getElementById(buttons[index]).focus();
                        }

                        setLastCharCode(null);
                      }

                      return onClick;
                    }()
                  }, null, {
                    "onComponentDidMount": function () {
                      function onComponentDidMount(node) {
                        if (selectedButton === button) {
                          node.focus();
                        }
                      }

                      return onComponentDidMount;
                    }()
                  })
                }, button);
              })
            })
          })
        }), showSearchBar && (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
          "mt": 1,
          children: (0, _inferno.createComponentVNode)(2, _components.Input, {
            "fluid": true,
            "onInput": function () {
              function onInput(e, value) {
                return setDisplayedArray(buttons.filter((0, _string.createSearch)(value)));
              }

              return onInput;
            }()
          })
        }), (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
          "mt": 1,
          children: (0, _inferno.createComponentVNode)(2, _components.Flex, {
            "textAlign": "center",
            children: [(0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
              "grow": 1,
              "basis": 0,
              children: (0, _inferno.createComponentVNode)(2, _components.Button, {
                "fluid": true,
                "color": "bad",
                "lineHeight": 2,
                "content": "Cancel",
                "onClick": function () {
                  function onClick() {
                    return act("cancel");
                  }

                  return onClick;
                }()
              })
            }), (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
              "grow": 1,
              "basis": 0,
              "ml": 1,
              children: (0, _inferno.createComponentVNode)(2, _components.Button, {
                "fluid": true,
                "color": "good",
                "lineHeight": 2,
                "content": "Confirm",
                "disabled": selectedButton === null,
                "onClick": function () {
                  function onClick() {
                    return act("choose", {
                      choice: selectedButton
                    });
                  }

                  return onClick;
                }()
              })
            })]
          })
        })]
      })
    })]
  });
};

exports.ListInput = ListInput;

var Loader = function Loader(props) {
  var value = props.value;
  return (0, _inferno.createVNode)(1, "div", "ListInput__Loader", (0, _inferno.createComponentVNode)(2, _components.Box, {
    "className": "ListInput__LoaderProgress",
    "style": {
      width: (0, _math.clamp01)(value) * 100 + '%'
    }
  }), 2);
};

exports.Loader = Loader;

/***/ }),

/***/ 52954:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Lootcrate = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var Lootcrate = function Lootcrate(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var code = data.code,
      buttons_pressed = data.buttons_pressed;
  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "width": 235,
    "height": 140,
    "theme": "ntos",
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      children: (0, _inferno.createComponentVNode)(2, _components.Section, {
        children: (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
          children: [(0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            children: [(0, _inferno.createComponentVNode)(2, _components.Button, {
              "top": "+6px",
              "right": "-9px",
              "content": 1,
              "color": buttons_pressed[0] ? code[0] === 1 || code[1] === 1 || code[2] === 1 ? "good" : "bad" : null,
              "onClick": function () {
                function onClick() {
                  return act('test_for_luck', {
                    number: 1
                  });
                }

                return onClick;
              }()
            }), (0, _inferno.createComponentVNode)(2, _components.Button, {
              "top": "+6px",
              "right": "-14px",
              "content": 2,
              "color": buttons_pressed[1] ? code[0] === 2 || code[1] === 2 || code[2] === 2 ? "good" : "bad" : null,
              "onClick": function () {
                function onClick() {
                  return act('test_for_luck', {
                    number: 2
                  });
                }

                return onClick;
              }()
            }), (0, _inferno.createComponentVNode)(2, _components.Button, {
              "top": "+6px",
              "right": "-19px",
              "content": 3,
              "color": buttons_pressed[2] ? code[0] === 3 || code[1] === 3 || code[2] === 3 ? "good" : "bad" : null,
              "onClick": function () {
                function onClick() {
                  return act('test_for_luck', {
                    number: 3
                  });
                }

                return onClick;
              }()
            })]
          }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            children: [(0, _inferno.createComponentVNode)(2, _components.Button, {
              "top": "+5px",
              "right": "-9px",
              "content": 4,
              "color": buttons_pressed[3] ? code[0] === 4 || code[1] === 4 || code[2] === 4 ? "good" : "bad" : null,
              "onClick": function () {
                function onClick() {
                  return act('test_for_luck', {
                    number: 4
                  });
                }

                return onClick;
              }()
            }), (0, _inferno.createComponentVNode)(2, _components.Button, {
              "top": "+5px",
              "right": "-14px",
              "content": 5,
              "color": buttons_pressed[4] ? code[0] === 5 || code[1] === 5 || code[2] === 5 ? "good" : "bad" : null,
              "onClick": function () {
                function onClick() {
                  return act('test_for_luck', {
                    number: 5
                  });
                }

                return onClick;
              }()
            }), (0, _inferno.createComponentVNode)(2, _components.Button, {
              "top": "+5px",
              "right": "-19px",
              "content": 6,
              "color": buttons_pressed[5] ? code[0] === 6 || code[1] === 6 || code[2] === 6 ? "good" : "bad" : null,
              "onClick": function () {
                function onClick() {
                  return act('test_for_luck', {
                    number: 6
                  });
                }

                return onClick;
              }()
            })]
          }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            children: [(0, _inferno.createComponentVNode)(2, _components.Button, {
              "top": "+4px",
              "right": "-9px",
              "content": 7,
              "color": buttons_pressed[6] ? code[0] === 7 || code[1] === 7 || code[2] === 7 ? "good" : "bad" : null,
              "onClick": function () {
                function onClick() {
                  return act('test_for_luck', {
                    number: 7
                  });
                }

                return onClick;
              }()
            }), (0, _inferno.createComponentVNode)(2, _components.Button, {
              "top": "+4px",
              "right": "-14px",
              "content": 8,
              "color": buttons_pressed[7] ? code[0] === 8 || code[1] === 8 || code[2] === 8 ? "good" : "bad" : null,
              "onClick": function () {
                function onClick() {
                  return act('test_for_luck', {
                    number: 8
                  });
                }

                return onClick;
              }()
            }), (0, _inferno.createComponentVNode)(2, _components.Button, {
              "top": "+4px",
              "right": "-19px",
              "content": 9,
              "color": buttons_pressed[8] ? code[0] === 9 || code[1] === 9 || code[2] === 9 ? "good" : "bad" : null,
              "onClick": function () {
                function onClick() {
                  return act('test_for_luck', {
                    number: 9
                  });
                }

                return onClick;
              }()
            })]
          })]
        })
      })
    })
  });
};

exports.Lootcrate = Lootcrate;

/***/ }),

/***/ 18707:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.MafiaPanel = void 0;

var _inferno = __webpack_require__(96323);

var _react = __webpack_require__(1060);

var _string = __webpack_require__(21118);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var MafiaPanel = function MafiaPanel(props, context) {
  var _roleinfo$action_log;

  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var actions = data.actions,
      phase = data.phase,
      roleinfo = data.roleinfo,
      role_theme = data.role_theme,
      admin_controls = data.admin_controls;
  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "title": "Мафия",
    "theme": role_theme,
    "width": 650,
    "height": 580,
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      children: (0, _inferno.createComponentVNode)(2, _components.Stack, {
        "fill": true,
        "vertical": true,
        children: [!roleinfo && (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
          "grow": true,
          children: (0, _inferno.createComponentVNode)(2, MafiaLobby)
        }), !!roleinfo && (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
          children: (0, _inferno.createComponentVNode)(2, MafiaRole)
        }), actions == null ? void 0 : actions.map(function (action) {
          return (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
            children: (0, _inferno.createComponentVNode)(2, _components.Button, {
              "onClick": function () {
                function onClick() {
                  return act('mf_action', {
                    atype: action
                  });
                }

                return onClick;
              }(),
              children: action
            })
          }, action);
        }), !!roleinfo && (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
          children: (0, _inferno.createComponentVNode)(2, MafiaJudgement)
        }), phase !== "Нет Игры" && (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
          "grow": true,
          children: (0, _inferno.createComponentVNode)(2, _components.Stack, {
            "fill": true,
            children: [(0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
              "grow": 1.34,
              "basis": 0,
              children: (0, _inferno.createComponentVNode)(2, MafiaPlayers)
            }), (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
              "grow": 1,
              "basis": 0,
              children: (0, _inferno.createComponentVNode)(2, _components.Stack, {
                "fill": true,
                "vertical": true,
                children: [(0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
                  "grow": true,
                  children: (0, _inferno.createComponentVNode)(2, MafiaListOfRoles)
                }), !!roleinfo && (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
                  "height": "80px",
                  children: (0, _inferno.createComponentVNode)(2, _components.Section, {
                    "fill": true,
                    "scrollable": true,
                    children: (_roleinfo$action_log = roleinfo.action_log) == null ? void 0 : _roleinfo$action_log.map(function (line) {
                      return (0, _inferno.createComponentVNode)(2, _components.Box, {
                        children: line
                      }, line);
                    })
                  })
                })]
              })
            })]
          })
        }), !!admin_controls && (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
          children: (0, _inferno.createComponentVNode)(2, MafiaAdmin)
        })]
      })
    })
  });
};

exports.MafiaPanel = MafiaPanel;

var MafiaLobby = function MafiaLobby(props, context) {
  var _useBackend2 = (0, _backend.useBackend)(context),
      act = _useBackend2.act,
      data = _useBackend2.data;

  var lobbydata = data.lobbydata,
      phase = data.phase,
      timeleft = data.timeleft;
  var readyGhosts = lobbydata ? lobbydata.filter(function (player) {
    return player.status === "Готов";
  }) : null;
  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    "fill": true,
    "scrollable": true,
    "title": "Lobby",
    "buttons": (0, _inferno.createFragment)([(0, _inferno.createTextVNode)("\u0421\u0442\u0430\u0434\u0438\u044F = "), phase, (0, _inferno.createTextVNode)(' | '), (0, _inferno.createComponentVNode)(2, _components.TimeDisplay, {
      "auto": "down",
      "value": timeleft
    }), (0, _inferno.createTextVNode)(' '), (0, _inferno.createComponentVNode)(2, _components.Button, {
      "icon": "clipboard-check",
      "tooltipPosition": "bottom-start",
      "tooltip": "\u0420\u0435\u0433\u0438\u0441\u0442\u0440\u0430\u0446\u0438\u044F \u0432 \u0438\u0433\u0440\u0443. \u0415\u0441\u043B\u0438 \u043E\u043D\u0430 \u0443\u0436\u0435 \u0438\u0434\u0451\u0442, \u0442\u043E\n\u0412\u044B \u0432\u043E\u0439\u0434\u0451\u0442\u0435 \u0432 \u0441\u043B\u0435\u0434\u0443\u044E\u0449\u0443\u044E.",
      "content": "Войти",
      "onClick": function () {
        function onClick() {
          return act('mf_signup');
        }

        return onClick;
      }()
    }), (0, _inferno.createComponentVNode)(2, _components.Button, {
      "icon": "eye",
      "tooltipPosition": "bottom-start",
      "tooltip": "\u0412\u044B \u0431\u0443\u0434\u0435\u0442\u0435 \u043D\u0430\u0431\u043B\u044E\u0434\u0430\u0442\u0435\u043B\u0435\u043C, \u043F\u043E\u043A\u0430 \u043D\u0435 \u0412\u044B\u043A\u043B\u044E\u0447\u0438\u0442\u0435 \u044D\u0442\u043E.\n\u0410\u0432\u0442\u043E\u043C\u0430\u0442\u0438\u0447\u0435\u0441\u043A\u0438 \u0432\u043A\u043B\u044E\u0447\u0430\u0435\u0442\u0441\u044F, \u043A\u043E\u0433\u0434\u0430 \u0412\u044B \u0443\u043C\u0438\u0440\u0430\u0435\u0442\u0435, \u0447\u0442\u043E\u0431\u044B \u0443\u0432\u0438\u0434\u0435\u0442\u044C \u0440\u0435\u0437\u0443\u043B\u044C\u0442\u0430\u0442 \u0438\u0433\u0440\u044B.\n\u0421\u043E\u043E\u0431\u0449\u0435\u043D\u0438\u044F \u043D\u0435 \u0431\u0443\u0434\u0443\u0442 \u043F\u0440\u0438\u0445\u043E\u0434\u0438\u0442\u044C, \u0435\u0441\u043B\u0438 \u0412\u044B \u0432\u043E\u0439\u0434\u0451\u0442\u0435 \u0432 \u0440\u0430\u0443\u043D\u0434.",
      "content": "Наблюдать",
      "onClick": function () {
        function onClick() {
          return act('mf_spectate');
        }

        return onClick;
      }()
    })], 0),
    children: [(0, _inferno.createComponentVNode)(2, _components.NoticeBox, {
      "info": true,
      children: ["\u0412 \u043B\u043E\u0431\u0431\u0438 ", readyGhosts ? readyGhosts.length : "0", "/12 \u0432\u0430\u043B\u0438\u0434\u043D\u044B\u0445 \u0438\u0433\u0440\u043E\u043A\u043E\u0432."]
    }), lobbydata == null ? void 0 : lobbydata.map(function (lobbyist) {
      return (0, _inferno.createComponentVNode)(2, _components.Stack, {
        "className": "candystripe",
        "p": 1,
        "align": "baseline",
        children: [(0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
          "grow": true,
          children: lobbyist.name
        }), (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
          children: "\u0421\u0442\u0430\u0442\u0443\u0441:"
        }), (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
          "color": lobbyist.status === 'Готов' ? 'green' : 'red',
          children: [lobbyist.spectating, " ", lobbyist.status]
        })]
      }, lobbyist);
    })]
  });
};

var MafiaRole = function MafiaRole(props, context) {
  var _useBackend3 = (0, _backend.useBackend)(context),
      act = _useBackend3.act,
      data = _useBackend3.data;

  var phase = data.phase,
      roleinfo = data.roleinfo,
      timeleft = data.timeleft;
  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    "title": phase,
    "minHeight": "100px",
    "maxHeight": "50px",
    "buttons": (0, _inferno.createComponentVNode)(2, _components.Box, {
      "style": {
        'font-family': 'Consolas, monospace',
        'font-size': '14px',
        'line-height': 1.5,
        'font-weight': 'bold'
      },
      children: (0, _inferno.createComponentVNode)(2, _components.TimeDisplay, {
        "auto": "down",
        "value": timeleft
      })
    }),
    children: (0, _inferno.createComponentVNode)(2, _components.Stack, {
      "align": "center",
      children: [(0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
        "grow": true,
        "textAlign": "center",
        children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
          "bold": true,
          children: ["\u0412\u044B - ", roleinfo.role]
        }), (0, _inferno.createComponentVNode)(2, _components.Box, {
          "italic": true,
          children: roleinfo.desc
        })]
      }), (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
        children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
          "className": (0, _react.classes)(['mafia32x32', roleinfo.revealed_icon]),
          "style": {
            'transform': 'scale(2) translate(0px, 10%)',
            'vertical-align': 'middle'
          }
        }), (0, _inferno.createComponentVNode)(2, _components.Box, {
          "className": (0, _react.classes)(['mafia32x32', roleinfo.hud_icon]),
          "style": {
            'transform': 'scale(2) translate(-5px, -5px)',
            'vertical-align': 'middle'
          }
        })]
      })]
    })
  });
};

var MafiaListOfRoles = function MafiaListOfRoles(props, context) {
  var _useBackend4 = (0, _backend.useBackend)(context),
      act = _useBackend4.act,
      data = _useBackend4.data;

  var all_roles = data.all_roles;
  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    "fill": true,
    "scrollable": true,
    "title": "Роли и Заметки",
    "minHeight": "120px",
    "buttons": (0, _inferno.createFragment)([(0, _inferno.createComponentVNode)(2, _components.Button, {
      "color": "transparent",
      "icon": "address-book",
      "tooltipPosition": "bottom-start",
      "tooltip": "\u0412\u0435\u0440\u0445\u043D\u0438\u0439 \u0440\u0430\u0437\u0434\u0435\u043B - \u044D\u0442\u043E \u0440\u043E\u043B\u0438 \u0432 \u0438\u0433\u0440\u0435. \u0412\u044B \u043C\u043E\u0436\u0435\u0442\u0435 \u043D\u0430\u0436\u0430\u0442\u044C \u043D\u0430 \u0437\u043D\u0430\u043A \u0432\u043E\u043F\u0440\u043E\u0441\u0430,\n\u0447\u0442\u043E\u0431\u044B \u0443\u0437\u043D\u0430\u0442\u044C \u0438\u043D\u0444\u043E\u0440\u043C\u0430\u0446\u0438\u044E \u043E \u0440\u043E\u043B\u0438."
    }), (0, _inferno.createComponentVNode)(2, _components.Button, {
      "color": "transparent",
      "icon": "edit",
      "tooltipPosition": "bottom-start",
      "tooltip": "\u041D\u0438\u0436\u043D\u0438\u0439 \u0440\u0430\u0437\u0434\u0435\u043B - \u0432\u0430\u0448\u0438 \u0442\u0435\u043A\u0443\u0449\u0438\u0435 \u0437\u0430\u043C\u0435\u0442\u043A\u0438. \u041D\u0430 \u043D\u0435\u043A\u043E\u0442\u043E\u0440\u044B\u0445 \u0440\u043E\u043B\u044F\u0445 \u043E\u043D \u0431\u0443\u0434\u0435\u0442 \u043F\u0443\u0441\u0442\u043E\u0439,\n\u043D\u043E \u043D\u0430 \u0434\u0440\u0443\u0433\u0438\u0445 \u0442\u0443\u0434\u0430 \u0431\u0443\u0434\u0443\u0442 \u0437\u0430\u043F\u0438\u0441\u044B\u0432\u0430\u0442\u044C\u0441\u044F \u0432\u0430\u0448\u0438 \u0434\u0435\u0439\u0441\u0442\u0432\u0438\u044F(\u0434\u0435\u0442\u0435\u043A\u0442\u0438\u0432\u043D\u044B\u0435 \u0440\u0430\u0441\u0441\u043B\u0435\u0434\u043E\u0432\u0430\u043D\u0438\u044F)"
    })], 4),
    children: (0, _inferno.createComponentVNode)(2, _components.Flex, {
      "direction": "column",
      children: all_roles == null ? void 0 : all_roles.map(function (r) {
        return (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
          "height": "30px",
          "className": "Section__title candystripe",
          children: (0, _inferno.createComponentVNode)(2, _components.Flex, {
            "height": "18px",
            "align": "center",
            "justify": "space-between",
            children: [(0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
              children: r
            }), (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
              "textAlign": "right",
              children: (0, _inferno.createComponentVNode)(2, _components.Button, {
                "color": "transparent",
                "icon": "question",
                "onClick": function () {
                  function onClick() {
                    return act('mf_lookup', {
                      atype: r.slice(0, -3)
                    });
                  }

                  return onClick;
                }()
              })
            })]
          })
        }, r);
      })
    })
  });
};

var MafiaJudgement = function MafiaJudgement(props, context) {
  var _useBackend5 = (0, _backend.useBackend)(context),
      act = _useBackend5.act,
      data = _useBackend5.data;

  var judgement_phase = data.judgement_phase;
  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    "title": "Суд",
    "buttons": (0, _inferno.createComponentVNode)(2, _components.Button, {
      "color": "transparent",
      "icon": "info",
      "tooltipPosition": "left",
      "tooltip": "\u041A\u043E\u0433\u0434\u0430 \u043A\u043E\u0433\u043E-\u0442\u043E \u0441\u0443\u0434\u044F\u0442, \u0432\u044B \u0440\u0435\u0448\u0430\u0435\u0442\u0435 \u0435\u0433\u043E \u0441\u0443\u0434\u044C\u0431\u0443.\n\u041F\u043E\u0431\u0435\u0434\u0430 \u041D\u0415\u0412\u0418\u041D\u041E\u0412\u0415\u041D \u043E\u0437\u043D\u0430\u0447\u0430\u0435\u0442, \u0447\u0442\u043E \u0447\u0435\u043B\u043E\u0432\u0435\u043A \u0441\u043C\u043E\u0436\u0435\u0442 \u0443\u0432\u0438\u0434\u0435\u0442\u044C \u0435\u0449\u0451 \u043E\u0434\u0438\u043D \u0434\u0435\u043D\u044C,\n\u043D\u043E... \u0432 \u0441\u043B\u0443\u0447\u0430\u0435 \u043F\u043E\u0440\u0430\u0436\u0435\u043D\u0438\u044F \u044D\u0442\u043E\u0433\u043E \u0443\u0436\u0435 \u043D\u0435 \u0441\u043B\u0443\u0447\u0438\u0442\u0441\u044F.\n\u0412\u044B \u043C\u043E\u0436\u0435\u0442\u0435 \u043D\u0435 \u0433\u043E\u043B\u043E\u0441\u043E\u0432\u0430\u0442\u044C, \u043D\u0430\u0436\u0430\u0432 \u043D\u0430 \u0441\u0440\u0435\u0434\u043D\u044E\u044E \u043A\u043D\u043E\u043F\u043A\u0443."
    }),
    children: [(0, _inferno.createComponentVNode)(2, _components.Flex, {
      "justify": "space-around",
      children: [(0, _inferno.createComponentVNode)(2, _components.Button, {
        "icon": "smile-beam",
        "content": "НЕВИНОВЕН!",
        "color": "good",
        "disabled": !judgement_phase,
        "onClick": function () {
          function onClick() {
            return act('vote_innocent');
          }

          return onClick;
        }()
      }), !judgement_phase && (0, _inferno.createComponentVNode)(2, _components.Box, {
        children: "\u0412 \u0434\u0430\u043D\u043D\u044B\u0439 \u043C\u043E\u043C\u0435\u043D\u0442 \u043D\u0438\u043A\u0442\u043E \u043D\u0435 \u0441\u0443\u0434\u0438\u0442\u0441\u044F."
      }), !!judgement_phase && (0, _inferno.createComponentVNode)(2, _components.Box, {
        children: "\u0412\u0440\u0435\u043C\u044F \u0433\u043E\u043B\u043E\u0441\u043E\u0432\u0430\u043D\u0438\u044F. \u041F\u0440\u043E\u0433\u043E\u043B\u043E\u0441\u0443\u0439 \u0438\u043B\u0438 \u0432\u043E\u0437\u0434\u0435\u0440\u0436\u0438\u0441\u044C."
      }), (0, _inferno.createComponentVNode)(2, _components.Button, {
        "icon": "angry",
        "color": "bad",
        "disabled": !judgement_phase,
        "onClick": function () {
          function onClick() {
            return act('vote_guilty');
          }

          return onClick;
        }(),
        children: "\u0412\u0418\u041D\u041E\u0412\u0415\u041D!"
      })]
    }), (0, _inferno.createComponentVNode)(2, _components.Flex, {
      "justify": "center",
      children: (0, _inferno.createComponentVNode)(2, _components.Button, {
        "icon": "meh",
        "color": "white",
        "disabled": !judgement_phase,
        "onClick": function () {
          function onClick() {
            return act('vote_abstain');
          }

          return onClick;
        }(),
        children: "\u0412\u043E\u0437\u0434\u0435\u0440\u0436\u0430\u0442\u044C\u0441\u044F"
      })
    })]
  });
};

var MafiaPlayers = function MafiaPlayers(props, context) {
  var _useBackend6 = (0, _backend.useBackend)(context),
      act = _useBackend6.act,
      data = _useBackend6.data;

  var players = data.players;
  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    "fill": true,
    "scrollable": true,
    "title": "Играцфоки",
    children: (0, _inferno.createComponentVNode)(2, _components.Flex, {
      "direction": "column",
      children: players == null ? void 0 : players.map(function (player) {
        var _player$actions;

        return (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
          "height": "30px",
          "className": "Section__title candystripe",
          children: (0, _inferno.createComponentVNode)(2, _components.Stack, {
            "height": "18px",
            "align": "center",
            children: [(0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
              "grow": true,
              "color": !player.alive && 'red',
              children: [player.name, " ", !player.alive && '(МЁРТВ)']
            }), (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
              "shrink": 0,
              children: player.votes !== undefined && !!player.alive && "\u0413\u043E\u043B\u043E\u0441\u0430: " + player.votes
            }), (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
              "shrink": 0,
              "minWidth": "42px",
              "textAlign": "center",
              children: (_player$actions = player.actions) == null ? void 0 : _player$actions.map(function (action) {
                return (0, _inferno.createComponentVNode)(2, _components.Button, {
                  "onClick": function () {
                    function onClick() {
                      return act('mf_targ_action', {
                        atype: action,
                        target: player.ref
                      });
                    }

                    return onClick;
                  }(),
                  children: action
                }, action);
              })
            })]
          })
        }, player.ref);
      })
    })
  });
};

var MafiaAdmin = function MafiaAdmin(props, context) {
  var _useBackend7 = (0, _backend.useBackend)(context),
      act = _useBackend7.act,
      data = _useBackend7.data;

  return (0, _inferno.createComponentVNode)(2, _components.Collapsible, {
    "title": "АДМИН ПАНЕЛЬ УПРАВЛЕНИЯ",
    "color": "red",
    children: (0, _inferno.createComponentVNode)(2, _components.Section, {
      children: [(0, _inferno.createComponentVNode)(2, _components.Collapsible, {
        "title": "Предупреждение от Кодеров!",
        "color": "transparent",
        children: "\u041F\u043E\u0447\u0442\u0438 \u0432\u0441\u0435 \u044D\u0442\u043E \u0441\u043E\u0437\u0434\u0430\u043D\u043E \u0434\u043B\u044F \u0442\u043E\u0433\u043E, \u0447\u0442\u043E\u0431\u044B \u043F\u043E\u043C\u043E\u0447\u044C \u043C\u043D\u0435 \u043E\u0442\u043B\u0430\u0434\u0438\u0442\u044C \u0438\u0433\u0440\u0443 (\u043E\u0439, \u043E\u0442\u043B\u0430\u0434\u043A\u0430 \u0438\u0433\u0440\u044B \u043D\u0430 12 \u0438\u0433\u0440\u043E\u043A\u043E\u0432!). \u0422\u0430\u043A \u0447\u0442\u043E, \u043E\u043D\u043E \u0432\u0441\u0435 \u0433\u0440\u0443\u0431\u043E\u0432\u0430\u0442\u043E\u0435 \u0438 \u0441\u043A\u043B\u043E\u043D\u043D\u043E \u043B\u043E\u043C\u0430\u0442\u044C\u0441\u044F \u043F\u043E \u043C\u0430\u043B\u0435\u0439\u0448\u0435\u043C\u0443 \u043F\u043E\u0432\u043E\u0434\u0443. \u0423\u0431\u0435\u0434\u0438\u0442\u0435\u0441\u044C, \u0447\u0442\u043E \u0412\u044B \u0437\u043D\u0430\u0435\u0442\u0435 \u0434\u0435\u0439\u0441\u0442\u0432\u0438\u0435 \u043A\u043D\u043E\u043F\u043A\u0438, \u043A\u043E\u0433\u0434\u0430 \u0436\u043C\u0451\u0442\u0435 \u043D\u0430 \u043D\u0435\u0451. \u0422\u0430\u043A \u0436\u0435(\u043E\u0434\u0438\u043D \u0438\u0437 \u0430\u0434\u043C\u0438\u043D\u0438\u0441\u0442\u0440\u0430\u0442\u043E\u0440\u043E\u0432 \u044D\u0442\u043E \u0441\u0434\u0435\u043B\u0430\u043B), \u043D\u0438\u043A\u043E\u0433\u043E \u043D\u0435 \u0433\u0438\u0431\u0430\u0439\u0442\u0435 \u0438 \u043D\u0435 \u0443\u0434\u0430\u043B\u044F\u0439\u0442\u0435 \u043B\u044E\u0431\u044B\u043C\u0438 \u0441\u043F\u043E\u0441\u043E\u0431\u0430\u043C\u0438! \u042D\u0442\u043E \u043F\u0440\u0438\u0432\u0435\u0434\u0451\u0442 \u043A \u0440\u0430\u043D\u0442\u0430\u0439\u043C\u0443, \u043A\u043E\u0442\u043E\u0440\u044B\u0439 \u0441\u043B\u043E\u043C\u0430\u0435\u0442 \u0432\u0441\u044E \u0438\u0433\u0440\u0443, \u043A\u043E\u0442\u043E\u0440\u0430\u044F \u0441\u043B\u043E\u043C\u0430\u0435\u0442 \u0441\u0435\u0440\u0432\u0435\u0440!"
      }), (0, _inferno.createComponentVNode)(2, _components.Button, {
        "icon": "arrow-right",
        "onClick": function () {
          function onClick() {
            return act('next_phase');
          }

          return onClick;
        }(),
        children: "\u0421\u043B\u0435\u0434\u0443\u044E\u0449\u0430\u044F \u0441\u0442\u0430\u0434\u0438\u044F"
      }), (0, _inferno.createComponentVNode)(2, _components.Button, {
        "icon": "home",
        "onClick": function () {
          function onClick() {
            return act('players_home');
          }

          return onClick;
        }(),
        "tooltip": "\u041D\u0430\u0434\u0435\u044E\u0441\u044C, \u0412\u044B \u043D\u0435 \u0431\u0443\u0434\u0435\u0442\u0435 \u043D\u0430\u0436\u0438\u043C\u0430\u0442\u044C \u044D\u0442\u0443 \u043A\u043D\u043E\u043F\u043A\u0443 \u043E\u0447\u0435\u043D\u044C \u0447\u0430\u0441\u0442\u043E,\n\u044D\u0442\u043E \u043D\u0443\u0436\u043D\u043E \u043D\u0430 \u0442\u043E\u0442 \u0441\u043B\u0443\u0447\u0430\u0439, \u0435\u0441\u043B\u0438 \u043A\u0430\u043A\u043E\u0439-\u0442\u043E \u0438\u0433\u0440\u043E\u043A\n\u043A\u0430\u043A\u0438\u043C-\u0442\u043E \u043E\u0431\u0440\u0430\u0437\u043E\u043C \u0441\u0431\u0435\u0433\u0430\u0435\u0442 (nullspace, \u0442\u0435\u043B\u0435\u043F\u043E\u0440\u0442\u0430\u0446\u0438\u0438, \u043E\u0442\u043A\u0440\u044B\u0442\u0430\u044F \u0434\u0432\u0435\u0440\u044C).\n\u0412 \u043B\u044E\u0431\u043E\u043C \u0441\u043B\u0443\u0447\u0430\u0435, \u041E\u0427\u0415\u041D\u042C \u041F\u041B\u041E\u0425\u041E \u0415\u0421\u041B\u0418 \u042D\u0422\u041E \u041F\u0420\u041E\u0418\u0417\u041E\u0419\u0414\u0415\u0422.\n\u0418\u0441\u043F\u043E\u043B\u044C\u0437\u0443\u0439 \u044D\u0442\u043E, \u0447\u0442\u043E\u0431\u044B \u0438\u0433\u0440\u043E\u043A\u043E\u0432 \u0432\u0435\u0440\u043D\u0443\u0442\u044C, \u0430 \u0437\u0430\u0442\u0435\u043C \u0441\u043E\u043E\u0431\u0449\u0438 \u043D\u0430 \u0433\u0438\u0442\u0445\u0430\u0431.",
        children: "\u041E\u0442\u043F\u0440\u0430\u0432\u0438\u0442\u044C \u0412\u0441\u0435\u0445 \u0414\u043E\u043C\u043E\u0439"
      }), (0, _inferno.createComponentVNode)(2, _components.Button, {
        "icon": "sync-alt",
        "onClick": function () {
          function onClick() {
            return act('new_game');
          }

          return onClick;
        }(),
        "tooltip": "\u042D\u0442\u043E \u043D\u0435\u0437\u0430\u043C\u0435\u0434\u043B\u0438\u0442\u0435\u043B\u044C\u043D\u043E \u0437\u0430\u0432\u0435\u0440\u0448\u0430\u0435\u0442 \u0442\u0435\u043A\u0443\u0449\u0443\u044E \u0438\u0433\u0440\u0443 \u0438 \u043F\u043E\u043F\u044B\u0442\u0430\u0435\u0442\u0441\u044F \u043D\u0430\u0447\u0430\u0442\u044C \u043D\u043E\u0432\u0443\u044E",
        children: "\u041D\u043E\u0432\u0430\u044F \u0418\u0433\u0440\u0430"
      }), (0, _inferno.createComponentVNode)(2, _components.Button, {
        "icon": "skull",
        "onClick": function () {
          function onClick() {
            return act('nuke');
          }

          return onClick;
        }(),
        "tooltip": "\u0423\u0434\u0430\u043B\u044F\u0435\u0442 \u0434\u0430\u0442\u0443\u043C\u044B, \u043E\u0447\u0438\u0449\u0430\u0435\u0442 \u0432\u0441\u0435 landmarks, \u0443\u0431\u0438\u0432\u0430\u0435\u0442 \u0432\u0441\u0435\u0445 \u0436\u0438\u0442\u0435\u043B\u0435\u0439 \u0438 \u043C\u0430\u0444\u0438\u044E,\n\u0441\u0442\u0438\u0440\u0430\u0435\u0442 \u043C\u0435\u0441\u0442\u043E \u0438\u0433\u0440\u044B. \u041D\u0430\u0436\u043C\u0438 \u044D\u0442\u043E, \u0435\u0441\u043B\u0438 \u0434\u0435\u0439\u0441\u0442\u0432\u0438\u0442\u0435\u043B\u044C\u043D\u043E \u0432\u0441\u0451 \u043F\u043E\u043B\u043E\u043C\u0430\u043D\u043E.\n\u0422\u044B \u0432\u0435\u0434\u044C \u0443\u0436\u0435 \u0432\u0441\u0451 \u0441\u043B\u043E\u043C\u0430\u043B, \u043D\u0435 \u0442\u0430\u043A \u043B\u0438?",
        children: "Nuke"
      }), (0, _inferno.createVNode)(1, "br"), (0, _inferno.createComponentVNode)(2, _components.Button, {
        "icon": "paint-brush",
        "onClick": function () {
          function onClick() {
            return act('debug_setup');
          }

          return onClick;
        }(),
        "tooltip": "\u042D\u0442\u043E \u043F\u043E\u0437\u0432\u043E\u043B\u0438\u0442 \u0441\u043E\u0437\u0434\u0430\u0442\u044C \u0441\u0432\u043E\u044E \u043D\u0430\u0441\u0442\u0440\u043E\u0439\u043A\u0443 \u0434\u043B\u044F \u0438\u0433\u0440\u044B, \u044D\u0442\u043E \u0442\u0430\u043A... \u043F\u0440\u043E\u0441\u0442\u043E.\n\u0412\u044B \u0434\u043E\u0431\u0430\u0432\u043B\u044F\u0435\u0442\u0435 \u0440\u043E\u043B\u044C \u0434\u043E \u0442\u0435\u0445 \u043F\u043E\u0440, \u043F\u043E\u043A\u0430 \u043D\u0435 \u043D\u0430\u0436\u043C\u0451\u0442\u0435 CANCEL \u0438\u043B\u0438 FINISH.\n\u0421\u0431\u0440\u0430\u0441\u044B\u0432\u0430\u0435\u0442\u0441\u044F \u043F\u043E\u0441\u043B\u0435 \u0437\u0430\u0432\u0435\u0440\u0448\u0435\u043D\u0438\u044F \u0440\u0430\u0443\u043D\u0434\u0430, \u0432\u043E\u0437\u0432\u0440\u0430\u0449\u0430\u044F \u0441\u043B\u0443\u0447\u0430\u0439\u043D\u044B\u0435 \u043D\u0430\u0441\u0442\u0440\u043E\u0439\u043A\u0438.",
        children: "\u0421\u043E\u0437\u0434\u0430\u0442\u044C \u0421\u0432\u043E\u044E \u0418\u0433\u0440\u0443"
      }), (0, _inferno.createComponentVNode)(2, _components.Button, {
        "icon": "paint-roller",
        "onClick": function () {
          function onClick() {
            return act('cancel_setup');
          }

          return onClick;
        }(),
        "tooltip": "\u0415\u0441\u043B\u0438 \u0432\u044B \u0447\u0442\u043E-\u0442\u043E \u043D\u0430\u043F\u0443\u0442\u0430\u043B\u0438, \u0442\u043E \u043C\u043E\u0436\u0435\u0442\u0435 \u0441\u044E\u0434\u0430 \u043D\u0430\u0436\u0430\u0442\u044C, \u0447\u0442\u043E\u0431\u044B \u0441\u0431\u0440\u043E\u0441\u0438\u0442\u044C \u0441\u0432\u043E\u044E \u043D\u0430\u0441\u0442\u0440\u043E\u0439\u043A\u0443.\n\u0418\u0433\u0440\u0430 \u0430\u0432\u0442\u043E\u043C\u0430\u0442\u0438\u0447\u0435\u0441\u043A\u0438 \u0441\u0431\u0440\u0430\u0441\u044B\u0432\u0430\u0435\u0442 \u0435\u0451 \u043F\u043E\u0441\u043B\u0435 \u043A\u0430\u0436\u0434\u043E\u0439 \u0438\u0433\u0440\u044B.",
        children: "\u0421\u0431\u0440\u043E\u0441\u0438\u0442\u044C \u0421\u0432\u043E\u044E \u0418\u0433\u0440\u0443"
      })]
    })
  });
};

/***/ }),

/***/ 55712:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Minesweeper = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var Minesweeper = function Minesweeper(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var width = data.width,
      height = data.height,
      grid = data.grid,
      mines = data.mines;
  var num_to_color = {
    " ": "#ffffff",
    "1": "#0092cc",
    "2": "#779933",
    "3": "#ff3333",
    "4": "#087099",
    "5": "#cc3333",
    "6": "#A6B2EC",
    "7": "#600095",
    "8": "#E5E5E5"
  };
  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "width": width,
    "height": height + 32,
    "title": mines,
    "className": "Minesweeper__Window",
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      "fitted": true,
      "height": height + 32,
      children: grid.map(function (line) {
        return (0, _inferno.createFragment)([line.map(function (butn, index) {
          return (0, _inferno.createComponentVNode)(2, _components.Button, {
            "className": "Minesweeper__Button",
            "disabled": butn.state === 'empty' ? 1 : 0,
            "textColor": num_to_color[butn.nearest],
            "content": (0, _inferno.createComponentVNode)(2, _components.Box, {
              "className": "Minesweeper__Button-Content",
              children: butn.flag ? (0, _inferno.createComponentVNode)(2, _components.Icon, {
                "name": "flag",
                "color": "#e73409"
              }) : butn.nearest
            }),
            "onClick": function () {
              function onClick() {
                return act('button_press', {
                  choice_x: butn.x,
                  choice_y: butn.y
                });
              }

              return onClick;
            }(),
            "onContextMenu": function () {
              function onContextMenu(e) {
                e.preventDefault();
                act('button_flag', {
                  choice_x: butn.x,
                  choice_y: butn.y
                });
              }

              return onContextMenu;
            }()
          }, index);
        }), (0, _inferno.createVNode)(1, "br")], 0);
      })
    })
  });
};

exports.Minesweeper = Minesweeper;

/***/ }),

/***/ 48078:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.MiningOreProcessingConsole = void 0;

var _inferno = __webpack_require__(96323);

var _string = __webpack_require__(21118);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var _Mining = __webpack_require__(11409);

var MiningOreProcessingConsole = function MiningOreProcessingConsole(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var unclaimedPoints = data.unclaimedPoints,
      ores = data.ores,
      showAllOres = data.showAllOres,
      power = data.power,
      speed = data.speed,
      ore_values = data.ore_values;
  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "width": 400,
    "height": 500,
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      "scrollable": true,
      children: [(0, _inferno.createComponentVNode)(2, _Mining.MiningUser, {
        "insertIdText": (0, _inferno.createComponentVNode)(2, _components.Box, {
          children: [(0, _inferno.createComponentVNode)(2, _components.Button, {
            "icon": "arrow-right",
            "mr": 1,
            "onClick": function () {
              function onClick() {
                return act("insert");
              }

              return onClick;
            }(),
            children: "Insert ID"
          }), "in order to claim points."]
        })
      }), (0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": "Status",
        "buttons": (0, _inferno.createComponentVNode)(2, _components.Button, {
          "icon": "power-off",
          "selected": power,
          "onClick": function () {
            function onClick() {
              return act("power");
            }

            return onClick;
          }(),
          children: power ? "Processing" : "Disabled"
        }),
        children: (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
          children: (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Current unclaimed points",
            "buttons": (0, _inferno.createComponentVNode)(2, _components.Button, {
              "disabled": unclaimedPoints < 1,
              "icon": "download",
              "onClick": function () {
                function onClick() {
                  return act("claim");
                }

                return onClick;
              }(),
              children: "Claim"
            }),
            children: (0, _inferno.createComponentVNode)(2, _components.AnimatedNumber, {
              "value": unclaimedPoints
            })
          })
        })
      }), (0, _inferno.createComponentVNode)(2, MOPCOres)]
    })
  });
}; // ORDER IS IMPORTANT HERE.


exports.MiningOreProcessingConsole = MiningOreProcessingConsole;
var processingOptions = ["Not Processing", "Smelting", "Compressing", "Alloying"]; // Higher in the list == closer to top
// This is just kind of an arbitrary list to sort by because
// the machine has no predictable ore order in it's list
// and alphabetizing them doesn't really make sense

var oreOrder = (/* unused pure expression or super */ null && (["glass", "iron", "coal", "steel", "hydrogen", "uranium", "phoron", "phoron glass", "silver", "gold", "platinum", "plasteel", "diamond"]));

var MOPCOres = function MOPCOres(props, context) {
  var _useBackend2 = (0, _backend.useBackend)(context),
      act = _useBackend2.act,
      data = _useBackend2.data;

  var ores = data.ores,
      showAllOres = data.showAllOres,
      ore_values = data.ore_values;
  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    "title": "Ore Processing Controls",
    "buttons": (0, _inferno.createComponentVNode)(2, _components.Button, {
      "icon": showAllOres ? "toggle-on" : "toggle-off",
      "selected": showAllOres,
      "onClick": function () {
        function onClick() {
          return act("showAllOres");
        }

        return onClick;
      }(),
      children: showAllOres ? "All Ores" : "Ores in Machine"
    }),
    children: [(0, _inferno.createComponentVNode)(2, _components.LabeledList, {
      children: ores.length && ores.sort().map(function (ore) {
        return (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
          "label": (0, _string.toTitleCase)(ore.ore),
          "buttons": (0, _inferno.createComponentVNode)(2, _components.Dropdown, {
            "width": "120px",
            "color": ore.processing === 0 && 'red' || ore.processing === 1 && 'green' || ore.processing === 2 && 'blue' || ore.processing === 3 && 'yellow',
            "options": processingOptions,
            "selected": processingOptions[ore.processing],
            "onSelected": function () {
              function onSelected(val) {
                return act("toggleSmelting", {
                  ore: ore.ore,
                  set: processingOptions.indexOf(val)
                });
              }

              return onSelected;
            }()
          }),
          children: (0, _inferno.createComponentVNode)(2, _components.Box, {
            "inline": true,
            children: (0, _inferno.createComponentVNode)(2, _components.AnimatedNumber, {
              "value": ore.amount
            })
          })
        }, ore.ore);
      }) || (0, _inferno.createComponentVNode)(2, _components.Box, {
        "color": "bad",
        "textAlign": "center",
        children: "No ores in machine."
      })
    }), (0, _inferno.createComponentVNode)(2, _components.Section, {
      children: (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
        children: (0, _inferno.createComponentVNode)(2, _components.Collapsible, {
          "title": "Mineral Value List",
          children: ore_values.length && ore_values.sort().map(function (item) {
            return (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
              "label": (0, _string.toTitleCase)(item.name),
              children: [item.amount, " points"]
            }, item.name);
          })
        })
      })
    })]
  });
};

/***/ }),

/***/ 98948:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.MiningStackingConsole = void 0;

var _inferno = __webpack_require__(96323);

var _string = __webpack_require__(21118);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var MiningStackingConsole = function MiningStackingConsole(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var stacktypes = data.stacktypes,
      stackingAmt = data.stackingAmt;
  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "width": 400,
    "height": 500,
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      children: (0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": "Stacker Controls",
        children: (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
          children: [(0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Stacking",
            children: (0, _inferno.createComponentVNode)(2, _components.NumberInput, {
              "fluid": true,
              "value": stackingAmt,
              "minValue": 1,
              "maxValue": 50,
              "stepPixelSize": 5,
              "onChange": function () {
                function onChange(e, val) {
                  return act("change_stack", {
                    amt: val
                  });
                }

                return onChange;
              }()
            })
          }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Divider), stacktypes.length && stacktypes.sort().map(function (stack) {
            return (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
              "label": (0, _string.toTitleCase)(stack.type),
              "buttons": (0, _inferno.createComponentVNode)(2, _components.Button, {
                "icon": "eject",
                "onClick": function () {
                  function onClick() {
                    return act("release_stack", {
                      stack: stack.type
                    });
                  }

                  return onClick;
                }(),
                children: "Eject"
              }),
              children: (0, _inferno.createComponentVNode)(2, _components.AnimatedNumber, {
                "value": stack.amt
              })
            }, stack.type);
          }) || (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Empty",
            "color": "average",
            children: "No stacks in machine."
          })]
        })
      })
    })
  });
};

exports.MiningStackingConsole = MiningStackingConsole;

/***/ }),

/***/ 70496:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.MiningVendor = void 0;

var _inferno = __webpack_require__(96323);

var _string = __webpack_require__(21118);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var _Mining = __webpack_require__(11409);

var _react = __webpack_require__(1060);

var _excluded = ["title", "items"];

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

var sortTypes = {
  'Alphabetical': function () {
    function Alphabetical(a, b) {
      return a - b;
    }

    return Alphabetical;
  }(),
  'By availability': function () {
    function ByAvailability(a, b) {
      return -(a.affordable - b.affordable);
    }

    return ByAvailability;
  }(),
  'By price': function () {
    function ByPrice(a, b) {
      return a.price - b.price;
    }

    return ByPrice;
  }()
};

var MiningVendor = function MiningVendor(props, context) {
  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "width": 410,
    "height": 450,
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      "className": "Layout__content--flexColumn",
      "scrollable": true,
      children: [(0, _inferno.createComponentVNode)(2, _Mining.MiningUser, {
        "insertIdText": "Please insert an ID in order to make purchases."
      }), (0, _inferno.createComponentVNode)(2, MiningVendorSearch), (0, _inferno.createComponentVNode)(2, MiningVendorItems)]
    })
  });
};

exports.MiningVendor = MiningVendor;

var MiningVendorItems = function MiningVendorItems(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var has_id = data.has_id,
      id = data.id,
      items = data.items; // Search thingies

  var _useLocalState = (0, _backend.useLocalState)(context, 'search', ''),
      searchText = _useLocalState[0],
      _setSearchText = _useLocalState[1];

  var _useLocalState2 = (0, _backend.useLocalState)(context, 'sort', 'Alphabetical'),
      sortOrder = _useLocalState2[0],
      _setSortOrder = _useLocalState2[1];

  var _useLocalState3 = (0, _backend.useLocalState)(context, 'descending', false),
      descending = _useLocalState3[0],
      _setDescending = _useLocalState3[1];

  var searcher = (0, _string.createSearch)(searchText, function (item) {
    return item[0];
  });
  var has_contents = false;
  var contents = Object.entries(items).map(function (kv, _i) {
    var items_in_cat = Object.entries(kv[1]).filter(searcher).map(function (kv2) {
      kv2[1].affordable = has_id && id.points >= kv2[1].price;
      return kv2[1];
    }).sort(sortTypes[sortOrder]);

    if (items_in_cat.length === 0) {
      return;
    }

    if (descending) {
      items_in_cat = items_in_cat.reverse();
    }

    has_contents = true;
    return (0, _inferno.createComponentVNode)(2, MiningVendorItemsCategory, {
      "title": kv[0],
      "items": items_in_cat
    }, kv[0]);
  });
  return (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
    "grow": "1",
    children: has_contents ? contents : (0, _inferno.createComponentVNode)(2, _components.Box, {
      "color": "label",
      children: "No items matching your criteria was found!"
    })
  });
};

var MiningVendorSearch = function MiningVendorSearch(props, context) {
  var _useLocalState4 = (0, _backend.useLocalState)(context, 'search', ''),
      _searchText = _useLocalState4[0],
      setSearchText = _useLocalState4[1];

  var _useLocalState5 = (0, _backend.useLocalState)(context, 'sort', ''),
      _sortOrder = _useLocalState5[0],
      setSortOrder = _useLocalState5[1];

  var _useLocalState6 = (0, _backend.useLocalState)(context, 'descending', false),
      descending = _useLocalState6[0],
      setDescending = _useLocalState6[1];

  return (0, _inferno.createComponentVNode)(2, _components.Box, {
    "mb": "0.5rem",
    children: (0, _inferno.createComponentVNode)(2, _components.Flex, {
      "width": "100%",
      children: [(0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
        "grow": "1",
        "mr": "0.5rem",
        children: (0, _inferno.createComponentVNode)(2, _components.Input, {
          "placeholder": "Search by item name..",
          "width": "100%",
          "autoFocus": true,
          "onInput": function () {
            function onInput(_e, value) {
              return setSearchText(value);
            }

            return onInput;
          }()
        })
      }), (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
        "basis": "30%",
        children: (0, _inferno.createComponentVNode)(2, _components.Dropdown, {
          "selected": "Alphabetical",
          "options": Object.keys(sortTypes),
          "width": "100%",
          "lineHeight": "19px",
          "onSelected": function () {
            function onSelected(v) {
              return setSortOrder(v);
            }

            return onSelected;
          }()
        })
      }), (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
        children: (0, _inferno.createComponentVNode)(2, _components.Button, {
          "icon": descending ? "arrow-down" : "arrow-up",
          "height": "19px",
          "tooltip": descending ? "Descending order" : "Ascending order",
          "tooltipPosition": "bottom-start",
          "ml": "0.5rem",
          "onClick": function () {
            function onClick() {
              return setDescending(!descending);
            }

            return onClick;
          }()
        })
      })]
    })
  });
};

var MiningVendorItemsCategory = function MiningVendorItemsCategory(properties, context) {
  var _useBackend2 = (0, _backend.useBackend)(context),
      act = _useBackend2.act,
      data = _useBackend2.data;

  var title = properties.title,
      items = properties.items,
      rest = _objectWithoutPropertiesLoose(properties, _excluded);

  return (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, _components.Collapsible, Object.assign({
    "open": true,
    "title": title
  }, rest, {
    children: items.map(function (item) {
      return (0, _inferno.createComponentVNode)(2, _components.Table, {
        children: [(0, _inferno.createComponentVNode)(2, _components.Table.Cell, {
          "collapsing": true,
          children: (0, _inferno.createVNode)(1, "span", (0, _react.classes)(['equipment_locker32x32', item.path]), null, 1, {
            "style": {
              'vertical-align': 'middle',
              'horizontal-align': 'middle'
            }
          })
        }), (0, _inferno.createComponentVNode)(2, _components.Table.Cell, {
          children: item.name
        }), (0, _inferno.createComponentVNode)(2, _components.Table.Cell, {
          "collapsing": true,
          "textAlign": "center",
          children: (0, _inferno.createComponentVNode)(2, _components.Button, {
            "fluid": true,
            "icon": "certificate",
            "disabled": !data.has_id || data.id.points < item.price,
            "content": item.price.toLocaleString('en-US'),
            "onClick": function () {
              function onClick() {
                return act('purchase', {
                  cat: title,
                  name: item.name
                });
              }

              return onClick;
            }()
          })
        })]
      }, item.name);
    })
  })));
};

/***/ }),

/***/ 72160:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.NoticeBoard = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var NoticeBoard = function NoticeBoard(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var _data$notices = data.notices,
      notices = _data$notices === void 0 ? {} : _data$notices;
  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "width": 425,
    "height": 176,
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      "backgroundColor": "#704D25",
      children: !notices.length ? (0, _inferno.createComponentVNode)(2, _components.Section, {
        children: (0, _inferno.createComponentVNode)(2, _components.Box, {
          "color": "white",
          "align": "center",
          children: "The notice board is empty!"
        })
      }) : notices.map(function (notice) {
        return (0, _inferno.createComponentVNode)(2, _components.Flex, {
          "color": "black",
          "backgroundColor": "white",
          "style": {
            padding: "2px 2px 0 2px"
          },
          "mb": 0.5,
          children: [(0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
            "align": "center",
            "grow": 1,
            children: (0, _inferno.createComponentVNode)(2, _components.Box, {
              "align": "center",
              children: notice.name
            })
          }), (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
            children: [notice.isphoto && (0, _inferno.createComponentVNode)(2, _components.Button, {
              "icon": "image",
              "content": "Look",
              "onClick": function () {
                function onClick() {
                  return act("look", {
                    ref: notice.ref
                  });
                }

                return onClick;
              }()
            }) || notice.ispaper && (0, _inferno.createFragment)([(0, _inferno.createComponentVNode)(2, _components.Button, {
              "icon": "eye",
              "onClick": function () {
                function onClick() {
                  return act("read", {
                    ref: notice.ref
                  });
                }

                return onClick;
              }()
            }), (0, _inferno.createComponentVNode)(2, _components.Button, {
              "icon": "pen",
              "onClick": function () {
                function onClick() {
                  return act("write", {
                    ref: notice.ref
                  });
                }

                return onClick;
              }()
            })], 4) || "Unknown Entity", (0, _inferno.createComponentVNode)(2, _components.Button, {
              "icon": "eject",
              "onClick": function () {
                function onClick() {
                  return act("remove", {
                    ref: notice.ref
                  });
                }

                return onClick;
              }()
            })]
          })]
        }, notice.ref);
      })
    })
  });
};

exports.NoticeBoard = NoticeBoard;

/***/ }),

/***/ 83271:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.NuclearBomb = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var NukeKeypad = function NukeKeypad(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var keypadKeys = [['1', '4', '7', 'R'], ['2', '5', '8', '0'], ['3', '6', '9', 'E']];
  var code = data.code,
      hasDisk = data.hasDisk,
      deployed = data.deployed;
  return (0, _inferno.createComponentVNode)(2, _components.Box, {
    "width": "306px",
    "align": "center",
    children: (0, _inferno.createComponentVNode)(2, _components.Grid, {
      "width": "35%",
      children: keypadKeys.map(function (keyColumn) {
        return (0, _inferno.createComponentVNode)(2, _components.Grid.Column, {
          children: keyColumn.map(function (key) {
            return (0, _inferno.createComponentVNode)(2, _components.Button, {
              "fluid": true,
              "bold": true,
              "mb": "6px",
              "content": key,
              "textAlign": "center",
              "fontSize": "20px",
              "height": "25px",
              "lineHeight": 1.25,
              "disabled": code === "ERROR" && key !== "R" || !hasDisk || !deployed,
              "onClick": function () {
                function onClick() {
                  return act('type', {
                    digit: key
                  });
                }

                return onClick;
              }()
            }, key);
          })
        }, keyColumn[0]);
      })
    })
  });
};

var NuclearBomb = function NuclearBomb(props, context) {
  var _useBackend2 = (0, _backend.useBackend)(context),
      act = _useBackend2.act,
      data = _useBackend2.data;

  var deployed = data.deployed,
      timing = data.timing,
      timeLeft = data.timeLeft,
      safety = data.safety,
      hasDisk = data.hasDisk,
      authorized = data.authorized,
      code = data.code,
      timerMin = data.timerMin,
      timerMax = data.timerMax;
  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "width": 326,
    "height": 400,
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      children: (0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": "Статус",
        "fill": true,
        "buttons": (0, _inferno.createComponentVNode)(2, _components.Box, {
          "textAlign": "right",
          children: (0, _inferno.createComponentVNode)(2, _components.Button, {
            "color": deployed ? "green" : "red",
            "content": deployed ? "Развернута" : "Не развернута",
            "icon": "power-off",
            "disabled": !safety || timing,
            "onClick": function () {
              function onClick() {
                return act('deploy');
              }

              return onClick;
            }()
          })
        }),
        children: [(0, _inferno.createComponentVNode)(2, _components.LabeledList, {
          children: (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Диск аутентификации",
            children: (0, _inferno.createComponentVNode)(2, _components.Box, {
              "textAlign": "right",
              children: (0, _inferno.createComponentVNode)(2, _components.Button, {
                "selected": hasDisk,
                "icon": "eject",
                "content": hasDisk ? "Вставлен" : "Не вставлен",
                "disabled": !deployed,
                "onClick": function () {
                  function onClick() {
                    return act(hasDisk ? 'ejectDisk' : 'insertDisk');
                  }

                  return onClick;
                }()
              })
            })
          })
        }), (0, _inferno.createVNode)(1, "br"), (0, _inferno.createComponentVNode)(2, _components.Box, {
          "fontSize": "25px",
          "textAlign": "center",
          "position": "center",
          children: code && code || (0, _inferno.createComponentVNode)(2, _components.Box, {
            "textColor": authorized ? "green" : "red",
            children: authorized ? "ДОСТУП РАЗРЕШЁН" : hasDisk ? "ВВЕДИТЕ КОД" : deployed ? "ВСТАВЬТЕ ДИСК" : "ДИСК НЕ ВСТАВЛЕН"
          })
        }), (0, _inferno.createComponentVNode)(2, NukeKeypad), (0, _inferno.createVNode)(1, "br"), (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
          children: [(0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Осталось времени",
            children: (0, _inferno.createComponentVNode)(2, _components.ProgressBar, {
              "value": timeLeft / timerMax,
              "ranges": {
                good: [0.65, Infinity],
                average: [0.25, 0.65],
                bad: [-Infinity, 0.25]
              },
              children: [timeLeft, " \u0441\u0435\u043A\u0443\u043D\u0434"]
            })
          }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Регулятор таймера",
            children: [(0, _inferno.createComponentVNode)(2, _components.Button, {
              "icon": "fast-backward",
              "disabled": !authorized || timeLeft <= timerMin,
              "width": "23.5%",
              "onClick": function () {
                function onClick() {
                  return act('adjustTimer', {
                    time: timerMin
                  });
                }

                return onClick;
              }()
            }), (0, _inferno.createComponentVNode)(2, _components.Button, {
              "icon": "backward",
              "disabled": !authorized || timeLeft <= timerMin,
              "width": "23.5%",
              "onClick": function () {
                function onClick() {
                  return act('adjustTimer', {
                    time: timeLeft - 5
                  });
                }

                return onClick;
              }()
            }), (0, _inferno.createComponentVNode)(2, _components.Button, {
              "icon": "forward",
              "disabled": !authorized || timeLeft >= timerMax,
              "width": "23.5%",
              "onClick": function () {
                function onClick() {
                  return act('adjustTimer', {
                    time: timeLeft + 5
                  });
                }

                return onClick;
              }()
            }), (0, _inferno.createComponentVNode)(2, _components.Button, {
              "icon": "fast-forward",
              "disabled": !authorized || timeLeft >= timerMax,
              "width": "23.5%",
              "onClick": function () {
                function onClick() {
                  return act('adjustTimer', {
                    time: timerMax
                  });
                }

                return onClick;
              }()
            })]
          }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Предохранитель",
            children: (0, _inferno.createComponentVNode)(2, _components.Button, {
              "selected": safety,
              "icon": safety ? "toggle-on" : "toggle-off",
              "content": safety ? "Включён" : "Отключён",
              "disabled": !authorized || timing && !safety,
              "onClick": function () {
                function onClick() {
                  return act('toggleSafety');
                }

                return onClick;
              }()
            })
          }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Таймер",
            children: (0, _inferno.createComponentVNode)(2, _components.Button, {
              "selected": timing,
              "icon": "power-off",
              "disabled": !authorized || safety,
              "content": timing ? "Включён" : "Отключён",
              "onClick": function () {
                function onClick() {
                  return act('bombSet');
                }

                return onClick;
              }()
            })
          })]
        })]
      })
    })
  });
};

exports.NuclearBomb = NuclearBomb;

/***/ }),

/***/ 64427:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Orbit = void 0;

var _inferno = __webpack_require__(96323);

var _string = __webpack_require__(21118);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

function _createForOfIteratorHelperLoose(o, allowArrayLike) { var it = typeof Symbol !== "undefined" && o[Symbol.iterator] || o["@@iterator"]; if (it) return (it = it.call(o)).next.bind(it); if (Array.isArray(o) || (it = _unsupportedIterableToArray(o)) || allowArrayLike && o && typeof o.length === "number") { if (it) o = it; var i = 0; return function () { if (i >= o.length) return { done: true }; return { done: false, value: o[i++] }; }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }

function _unsupportedIterableToArray(o, minLen) { if (!o) return; if (typeof o === "string") return _arrayLikeToArray(o, minLen); var n = Object.prototype.toString.call(o).slice(8, -1); if (n === "Object" && o.constructor) n = o.constructor.name; if (n === "Map" || n === "Set") return Array.from(o); if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen); }

function _arrayLikeToArray(arr, len) { if (len == null || len > arr.length) len = arr.length; for (var i = 0, arr2 = new Array(len); i < len; i++) { arr2[i] = arr[i]; } return arr2; }

var PATTERN_NUMBER = / \(([0-9]+)\)$/;

var searchFor = function searchFor(searchText) {
  return (0, _string.createSearch)(searchText, function (thing) {
    return thing.name;
  });
};

var compareString = function compareString(a, b) {
  return a < b ? -1 : a > b;
};

var compareNumberedText = function compareNumberedText(a, b) {
  var aName = a.name;
  var bName = b.name;

  if (!aName || !bName) {
    return 0;
  } // Check if aName and bName are the same except for a number at the end
  // e.g. Medibot (2) and Medibot (3)


  var aNumberMatch = aName.match(PATTERN_NUMBER);
  var bNumberMatch = bName.match(PATTERN_NUMBER);

  if (aNumberMatch && bNumberMatch && aName.replace(PATTERN_NUMBER, "") === bName.replace(PATTERN_NUMBER, "")) {
    var aNumber = parseInt(aNumberMatch[1], 10);
    var bNumber = parseInt(bNumberMatch[1], 10);
    return aNumber - bNumber;
  }

  return compareString(aName, bName);
};

var BasicSection = function BasicSection(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act;

  var searchText = props.searchText,
      source = props.source,
      title = props.title;
  var things = source.filter(searchFor(searchText));
  things.sort(compareNumberedText);
  return source.length > 0 && (0, _inferno.createComponentVNode)(2, _components.Section, {
    "title": title + " - (" + source.length + ")",
    children: things.map(function (thing) {
      return (0, _inferno.createComponentVNode)(2, _components.Button, {
        "content": thing.name,
        "onClick": function () {
          function onClick() {
            return act("orbit", {
              ref: thing.ref
            });
          }

          return onClick;
        }()
      }, thing.name);
    })
  });
};

var OrbitedButton = function OrbitedButton(props, context) {
  var _useBackend2 = (0, _backend.useBackend)(context),
      act = _useBackend2.act;

  var color = props.color,
      thing = props.thing;
  return (0, _inferno.createComponentVNode)(2, _components.Button, {
    "color": color,
    "onClick": function () {
      function onClick() {
        return act("orbit", {
          ref: thing.ref
        });
      }

      return onClick;
    }(),
    children: thing.name
  });
};

var Orbit = function Orbit(props, context) {
  var _useBackend3 = (0, _backend.useBackend)(context),
      act = _useBackend3.act,
      data = _useBackend3.data;

  var alive = data.alive,
      antagonists = data.antagonists,
      dead = data.dead,
      ghosts = data.ghosts,
      misc = data.misc,
      npcs = data.npcs;

  var _useLocalState = (0, _backend.useLocalState)(context, "searchText", ""),
      searchText = _useLocalState[0],
      setSearchText = _useLocalState[1];

  var collatedAntagonists = {};

  for (var _iterator = _createForOfIteratorHelperLoose(antagonists), _step; !(_step = _iterator()).done;) {
    var antagonist = _step.value;

    for (var _iterator2 = _createForOfIteratorHelperLoose(antagonist.antag), _step2; !(_step2 = _iterator2()).done;) {
      var anta = _step2.value;

      if (collatedAntagonists[anta] === undefined) {
        collatedAntagonists[anta] = [];
      }

      collatedAntagonists[anta].push(antagonist);
    }
  }

  var sortedAntagonists = Object.entries(collatedAntagonists);
  sortedAntagonists.sort(function (a, b) {
    return compareString(a[0], b[0]);
  });

  var orbitMostRelevant = function orbitMostRelevant(searchText) {
    for (var _i = 0, _arr = [sortedAntagonists.map(function (_ref) {
      var _ = _ref[0],
          antags = _ref[1];
      return antags;
    }), alive, ghosts, dead, npcs, misc]; _i < _arr.length; _i++) {
      var source = _arr[_i];
      var member = source.filter(searchFor(searchText)).sort(compareNumberedText)[0];

      if (member !== undefined) {
        act("orbit", {
          ref: member.ref
        });
        break;
      }
    }
  };

  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      "scrollable": true,
      children: [(0, _inferno.createComponentVNode)(2, _components.Section, {
        children: (0, _inferno.createComponentVNode)(2, _components.Flex, {
          children: [(0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
            children: (0, _inferno.createComponentVNode)(2, _components.Icon, {
              "name": "search",
              "mr": 1
            })
          }), (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
            "grow": 1,
            children: (0, _inferno.createComponentVNode)(2, _components.Input, {
              "placeholder": "Search...",
              "autoFocus": true,
              "fluid": true,
              "value": searchText,
              "onInput": function () {
                function onInput(_, value) {
                  return setSearchText(value);
                }

                return onInput;
              }(),
              "onEnter": function () {
                function onEnter(_, value) {
                  return orbitMostRelevant(value);
                }

                return onEnter;
              }()
            })
          }), (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
            children: (0, _inferno.createComponentVNode)(2, _components.Divider, {
              "vertical": true
            })
          }), (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
            children: (0, _inferno.createComponentVNode)(2, _components.Button, {
              "inline": true,
              "color": "transparent",
              "tooltip": "Refresh",
              "tooltipPosition": "bottom-start",
              "icon": "sync-alt",
              "onClick": function () {
                function onClick() {
                  return act("refresh");
                }

                return onClick;
              }()
            })
          })]
        })
      }), antagonists.length > 0 && (0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": "Antagonists",
        children: sortedAntagonists.map(function (_ref2) {
          var name = _ref2[0],
              antags = _ref2[1];
          return (0, _inferno.createComponentVNode)(2, _components.Section, {
            "title": name,
            "level": 2,
            children: antags.filter(searchFor(searchText)).sort(compareNumberedText).map(function (antag) {
              return (0, _inferno.createComponentVNode)(2, OrbitedButton, {
                "color": "bad",
                "thing": antag
              }, antag.name);
            })
          }, name);
        })
      }), (0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": "Alive - (" + alive.length + ")",
        children: alive.filter(searchFor(searchText)).sort(compareNumberedText).map(function (thing) {
          return (0, _inferno.createComponentVNode)(2, OrbitedButton, {
            "color": "good",
            "thing": thing
          }, thing.name);
        })
      }), (0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": "Ghosts - (" + ghosts.length + ")",
        children: ghosts.filter(searchFor(searchText)).sort(compareNumberedText).map(function (thing) {
          return (0, _inferno.createComponentVNode)(2, OrbitedButton, {
            "color": "grey",
            "thing": thing
          }, thing.name);
        })
      }), (0, _inferno.createComponentVNode)(2, BasicSection, {
        "title": "Dead",
        "source": dead,
        "searchText": searchText
      }), (0, _inferno.createComponentVNode)(2, BasicSection, {
        "title": "NPCs",
        "source": npcs,
        "searchText": searchText
      }), (0, _inferno.createComponentVNode)(2, BasicSection, {
        "title": "Misc",
        "source": misc,
        "searchText": searchText
      })]
    })
  });
};

exports.Orbit = Orbit;

/***/ }),

/***/ 21293:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Particool = void 0;

var _inferno = __webpack_require__(96323);

var _math = __webpack_require__(97622);

var _math2 = __webpack_require__(97622);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var _logging = __webpack_require__(79926);

var ParticleIntegerEntry = function ParticleIntegerEntry(props, context) {
  var value = props.value,
      name = props.name;

  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act;

  return (0, _inferno.createComponentVNode)(2, _components.NumberInput, {
    "value": value,
    "stepPixelSize": 5,
    "width": "39px",
    "onDrag": function () {
      function onDrag(e, value) {
        return act('modify_particle_value', {
          new_data: {
            name: name,
            value: value,
            type: 'int'
          }
        });
      }

      return onDrag;
    }()
  });
};

var ParticleMatrixEntry = function ParticleMatrixEntry(props, context) {
  var value = props.value,
      name = props.name;

  var _useBackend2 = (0, _backend.useBackend)(context),
      act = _useBackend2.act; // Actual matrix, or matrix of 0


  value = value || [0, 0, 0, 0, 0, 0];
  return (0, _inferno.createComponentVNode)(2, _components.Flex, {
    children: (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
      children: value.map(function (val, i) {
        return (0, _inferno.createComponentVNode)(2, _components.NumberInput, {
          "value": val,
          "onDrag": function () {
            function onDrag(e, v) {
              value[i] = v;
              act('modify_particle_value', {
                new_data: {
                  name: name,
                  value: value,
                  type: 'matrix'
                }
              });
            }

            return onDrag;
          }()
        }, i);
      })
    })
  });
};

var ParticleFloatEntry = function ParticleFloatEntry(props, context) {
  var value = props.value,
      name = props.name;

  var _useBackend3 = (0, _backend.useBackend)(context),
      act = _useBackend3.act;

  var _useLocalState = (0, _backend.useLocalState)(context, 'particleFloatStep', 0.01),
      step = _useLocalState[0],
      setStep = _useLocalState[1];

  return (0, _inferno.createFragment)([(0, _inferno.createComponentVNode)(2, _components.NumberInput, {
    "value": value,
    "stepPixelSize": 4,
    "step": step,
    "format": function () {
      function format(value) {
        return (0, _math.toFixed)(value, (0, _math2.numberOfDecimalDigits)(step));
      }

      return format;
    }(),
    "width": "80px",
    "onDrag": function () {
      function onDrag(e, value) {
        return act('modify_particle_value', {
          new_data: {
            name: name,
            value: value,
            type: 'float'
          }
        });
      }

      return onDrag;
    }()
  }), (0, _inferno.createComponentVNode)(2, _components.Box, {
    "inline": true,
    "ml": 2,
    "mr": 1,
    children: "Step:"
  }), (0, _inferno.createComponentVNode)(2, _components.NumberInput, {
    "value": step,
    "step": 0.001,
    "format": function () {
      function format(value) {
        return (0, _math.toFixed)(value, 4);
      }

      return format;
    }(),
    "width": "70px",
    "onChange": function () {
      function onChange(e, value) {
        return setStep(value);
      }

      return onChange;
    }()
  })], 4);
}; // array for our working varz
// let genWorking = [];


var ParticleGeneratorEntry = function ParticleGeneratorEntry(props, context) {
  var value = props.value,
      name = props.name;

  var _useBackend4 = (0, _backend.useBackend)(context),
      act = _useBackend4.act;

  var generatorTypes = ["num", "vector", "box", "color", "circle", "sphere", "square", "cube"];
  var randTypes = ["UNIFORM_RAND", "NORMAL_RAND", "LINEAR_RAND", "SQUARE_RAND"];
  var tempGenType = '';
  var tempA = '';
  var tempB = '';
  var tempRand = '';

  _logging.logger.log(value); // Value will come through a binobj of the generator, i.e
  // "client generator(box, UNIFORM_RAND, list(-10,-10,-10), list(10,10,10))"
  // So do this hacky garbage to convert it back into values


  if (value) {
    // Get contents of brackets
    var params = value.match(/\((.*)\)/);
    params = params ? params : ["", "", "", ""]; // Split into params

    params = params[1].split(', ');

    if (params.length === 4) {
      tempGenType = params[0];
      tempRand = params[1]; // Try to get contents of list(), just pass value if null

      var aTemp = params[2].match(/\((.*)\)/);
      tempA = aTemp ? aTemp[1] : aTemp; // fermented soy beans

      var bTemp = params[3].match(/\((.*)\)/);
      tempB = bTemp ? bTemp[1] : params[3];
    }
  }

  var _useLocalState2 = (0, _backend.useLocalState)(context, name + 'genType', tempGenType),
      genType = _useLocalState2[0],
      setGenType = _useLocalState2[1];

  var _useLocalState3 = (0, _backend.useLocalState)(context, name + 'a', tempA),
      a = _useLocalState3[0],
      setA = _useLocalState3[1];

  var _useLocalState4 = (0, _backend.useLocalState)(context, name + 'b', tempB),
      b = _useLocalState4[0],
      setB = _useLocalState4[1];

  var _useLocalState5 = (0, _backend.useLocalState)(context, name + 'rand', tempRand),
      rand = _useLocalState5[0],
      setRand = _useLocalState5[1];

  var doAct = function doAct() {
    _logging.logger.log(genType);

    act('modify_particle_value', {
      new_data: {
        name: name,
        value: {
          genType: genType,
          a: a,
          b: b,
          rand: rand
        },
        type: 'generator'
      }
    });
  };

  return (0, _inferno.createComponentVNode)(2, _components.Collapsible, {
    "title": "Generator Settings - Hit Set to save",
    children: (0, _inferno.createComponentVNode)(2, _components.Section, {
      "level": 2,
      children: [(0, _inferno.createComponentVNode)(2, _components.LabeledList, {
        children: [(0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
          "label": genType,
          children: (0, _inferno.createComponentVNode)(2, _components.Tooltip, {
            "position": "bottom",
            "content": "" + generatorTypes.join(", "),
            children: (0, _inferno.createComponentVNode)(2, _components.Input, {
              "value": genType,
              "onInput": function () {
                function onInput(e, val) {
                  return setGenType(val);
                }

                return onInput;
              }()
            })
          })
        }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
          "label": "A",
          children: (0, _inferno.createComponentVNode)(2, _components.Input, {
            "value": a,
            "onInput": function () {
              function onInput(e, val) {
                return setA(val);
              }

              return onInput;
            }()
          })
        }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
          "label": "B",
          children: (0, _inferno.createComponentVNode)(2, _components.Input, {
            "value": b,
            "onInput": function () {
              function onInput(e, val) {
                return setB(val);
              }

              return onInput;
            }()
          })
        }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
          "label": "Rand Type",
          children: (0, _inferno.createComponentVNode)(2, _components.Tooltip, {
            "position": "bottom",
            "content": "" + randTypes.join(", "),
            children: (0, _inferno.createComponentVNode)(2, _components.Input, {
              "value": rand,
              "onInput": function () {
                function onInput(e, val) {
                  return setRand(val);
                }

                return onInput;
              }()
            })
          })
        })]
      }), (0, _inferno.createComponentVNode)(2, _components.Button, {
        "content": "Set",
        "onClick": function () {
          function onClick() {
            return doAct();
          }

          return onClick;
        }()
      })]
    })
  });
};

var ParticleTextEntry = function ParticleTextEntry(props, context) {
  var value = props.value,
      name = props.name;

  var _useBackend5 = (0, _backend.useBackend)(context),
      act = _useBackend5.act;

  return (0, _inferno.createComponentVNode)(2, _components.Input, {
    "value": value,
    "width": "250px",
    "onInput": function () {
      function onInput(e, value) {
        return act('modify_particle_value', {
          new_data: {
            name: name,
            value: value,
            type: 'string'
          }
        });
      }

      return onInput;
    }()
  });
};

var ParticleNumListEntry = function ParticleNumListEntry(props, context) {
  var value = props.value,
      name = props.name;

  var _useBackend6 = (0, _backend.useBackend)(context),
      act = _useBackend6.act;

  var valArr = value ? Object.keys(value).map(function (key) {
    return value[key];
  }) : [];
  return (0, _inferno.createComponentVNode)(2, _components.Input, {
    "value": valArr.join(','),
    "width": "250px",
    "onInput": function () {
      function onInput(e, val) {
        return act('modify_particle_value', {
          new_data: {
            name: name,
            value: val,
            type: 'numList'
          }
        });
      }

      return onInput;
    }()
  });
};

var ParticleColorEntry = function ParticleColorEntry(props, context) {
  var value = props.value,
      name = props.name;

  var _useBackend7 = (0, _backend.useBackend)(context),
      act = _useBackend7.act;

  return (0, _inferno.createFragment)([(0, _inferno.createComponentVNode)(2, _components.Button, {
    "icon": "pencil-alt",
    "onClick": function () {
      function onClick() {
        return act('modify_color_value');
      }

      return onClick;
    }()
  }), (0, _inferno.createComponentVNode)(2, _components.ColorBox, {
    "color": value,
    "mr": 0.5
  }), (0, _inferno.createComponentVNode)(2, _components.Input, {
    "value": value,
    "width": "90px",
    "onInput": function () {
      function onInput(e, value) {
        return act('modify_particle_value', {
          new_data: {
            name: name,
            value: value,
            type: 'color'
          }
        });
      }

      return onInput;
    }()
  })], 4);
};

var ParticleIconEntry = function ParticleIconEntry(props, context) {
  var value = props.value;

  var _useBackend8 = (0, _backend.useBackend)(context),
      act = _useBackend8.act;

  return (0, _inferno.createFragment)([(0, _inferno.createComponentVNode)(2, _components.Button, {
    "icon": "pencil-alt",
    "onClick": function () {
      function onClick() {
        return act('modify_icon_value');
      }

      return onClick;
    }()
  }), (0, _inferno.createComponentVNode)(2, _components.Box, {
    "inline": true,
    "ml": 1,
    children: value
  })], 4);
};

var particleEntryMap = {
  width: 'float',
  height: 'float',
  count: 'int',
  spawning: 'float',
  bound1: 'numlist',
  bound2: 'numlist',
  gravity: 'numlist',
  gradient: 'string',
  transform: 'matrix',
  lifespan: 'float',
  fade: 'float',
  icon: 'icon',
  icon_state: 'string',
  color: 'color',
  color_change: 'float',
  position: 'generator',
  velocity: 'generator',
  scale: 'generator',
  grow: 'generator',
  rotation: 'float',
  spin: 'float',
  friction: 'float',
  drift: 'generator'
};

var ParticleDataEntry = function ParticleDataEntry(props, context) {
  var name = props.name;
  var particleEntryTypes = {
    "int": (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, ParticleIntegerEntry, Object.assign({}, props))),
    "float": (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, ParticleFloatEntry, Object.assign({}, props))),
    string: (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, ParticleTextEntry, Object.assign({}, props))),
    numlist: (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, ParticleNumListEntry, Object.assign({}, props))),
    color: (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, ParticleColorEntry, Object.assign({}, props))),
    icon: (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, ParticleIconEntry, Object.assign({}, props))),
    generator: (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, ParticleGeneratorEntry, Object.assign({}, props))),
    matrix: (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, ParticleMatrixEntry, Object.assign({}, props)))
  };
  return (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
    "label": name,
    children: particleEntryTypes[particleEntryMap[name]] || "Not Found (This is an error)"
  });
};

var ParticleEntry = function ParticleEntry(props, context) {
  var _useBackend9 = (0, _backend.useBackend)(context),
      act = _useBackend9.act,
      data = _useBackend9.data;

  var particle = props.particle;
  return (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
    children: Object.keys(particleEntryMap).map(function (entryName) {
      var value = particle[entryName];
      return (0, _inferno.createComponentVNode)(2, ParticleDataEntry, {
        "name": entryName,
        "value": value
      }, entryName);
    })
  });
};

var GeneratorHelp = function GeneratorHelp() {
  return (0, _inferno.createComponentVNode)(2, _components.Collapsible, {
    "title": "Generator Help",
    children: [(0, _inferno.createComponentVNode)(2, _components.Section, {
      "level": 2
    }), (0, _inferno.createComponentVNode)(2, _components.Section, {
      "level": 2,
      children: (0, _inferno.createVNode)(1, "table", null, (0, _inferno.createVNode)(1, "tbody", null, [(0, _inferno.createVNode)(1, "tr", null, [(0, _inferno.createVNode)(1, "td", null, "Generator type", 16), (0, _inferno.createVNode)(1, "td", null, "Result type", 16), (0, _inferno.createVNode)(1, "td", null, "Description", 16)], 4), (0, _inferno.createVNode)(1, "tr", null, [(0, _inferno.createVNode)(1, "td", null, "num", 16), (0, _inferno.createVNode)(1, "td", null, "num", 16), (0, _inferno.createVNode)(1, "td", null, "A random number between A and B.", 16)], 4), (0, _inferno.createVNode)(1, "tr", null, [(0, _inferno.createVNode)(1, "td", null, "vector", 16), (0, _inferno.createVNode)(1, "td", null, "vector", 16), (0, _inferno.createVNode)(1, "td", null, "A random vector on a line between A and B.", 16)], 4), (0, _inferno.createVNode)(1, "tr", null, [(0, _inferno.createVNode)(1, "td", null, "box", 16), (0, _inferno.createVNode)(1, "td", null, "vector", 16), (0, _inferno.createVNode)(1, "td", null, "A random vector within a box whose corners are at A and B", 16)], 4), (0, _inferno.createVNode)(1, "tr", null, [(0, _inferno.createVNode)(1, "td", null, "color", 16), (0, _inferno.createVNode)(1, "td", null, "color (string) or color matrix", 16), (0, _inferno.createVNode)(1, "td", null, "Result type depends on whether A or B are matrices or not. The result is interpolated between A and B; components are not randomized separately.", 16)], 4), (0, _inferno.createVNode)(1, "tr", null, [(0, _inferno.createVNode)(1, "td", null, "circle", 16), (0, _inferno.createVNode)(1, "td", null, "vector", 16), (0, _inferno.createVNode)(1, "td", null, "A random XY-only vector in a ring between radius A and B, centered at 0,0.", 16)], 4), (0, _inferno.createVNode)(1, "tr", null, [(0, _inferno.createVNode)(1, "td", null, "sphere", 16), (0, _inferno.createVNode)(1, "td", null, "vector", 16), (0, _inferno.createVNode)(1, "td", null, "A random vector in a spherical shell between radius A and B, centered at 0,0,0.", 16)], 4), (0, _inferno.createVNode)(1, "tr", null, [(0, _inferno.createVNode)(1, "td", null, "square", 16), (0, _inferno.createVNode)(1, "td", null, "vector", 16), (0, _inferno.createVNode)(1, "td", null, "A random XY-only vector between squares of sizes A and B. (The length of the square is between A*2 and B*2, centered at 0,0.)", 16)], 4), (0, _inferno.createVNode)(1, "tr", null, [(0, _inferno.createVNode)(1, "td", null, "cube", 16), (0, _inferno.createVNode)(1, "td", null, "vector", 16), (0, _inferno.createVNode)(1, "td", null, "A random vector between cubes of sizes A and B. (The length of the cube is between A*2 and B*2, centered at 0,0,0.)", 16)], 4)], 4), 2)
    })]
  });
};

var Particool = function Particool(props, context) {
  var _useBackend10 = (0, _backend.useBackend)(context),
      act = _useBackend10.act,
      data = _useBackend10.data;

  var particles = data.target_particle || {};
  var hasParticles = particles && Object.keys(particles).length > 0;
  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "title": "Particool",
    "width": 700,
    "height": 500,
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      "scrollable": true,
      children: [(0, _inferno.createComponentVNode)(2, _components.NoticeBox, {
        "danger": true,
        children: [" ", String(Date.now()), " ", (0, _inferno.createVNode)(1, "br"), "Particles? ", hasParticles.toString(), " -", (data.target_particle === null).toString(), " ", (0, _inferno.createVNode)(1, "br"), "dazta Json - ", JSON.stringify(data.target_particle)]
      }), (0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": (0, _inferno.createComponentVNode)(2, _components.Box, {
          "inline": true,
          children: "Particle"
        }),
        "buttons": !hasParticles ? (0, _inferno.createComponentVNode)(2, _components.Button, {
          "icon": "plus",
          "content": "Add Particle",
          "onClick": function () {
            function onClick() {
              return act('add_particle');
            }

            return onClick;
          }()
        }) : (0, _inferno.createComponentVNode)(2, _components.Button.Confirm, {
          "icon": "minus",
          "content": "Remove Particle",
          "onClick": function () {
            function onClick() {
              return act("remove_particle");
            }

            return onClick;
          }()
        }),
        children: [(0, _inferno.createComponentVNode)(2, GeneratorHelp), !hasParticles ? (0, _inferno.createComponentVNode)(2, _components.Box, {
          children: "No particle"
        }) : (0, _inferno.createComponentVNode)(2, ParticleEntry, {
          "particle": particles
        })]
      })]
    })
  });
};

exports.Particool = Particool;

/***/ }),

/***/ 28508:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Photocopier = void 0;

var _inferno = __webpack_require__(96323);

var _components = __webpack_require__(26949);

var _backend = __webpack_require__(31246);

var _layouts = __webpack_require__(2381);

var Photocopier = function Photocopier(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      data = _useBackend.data;

  var isAI = data.isAI,
      has_toner = data.has_toner,
      has_item = data.has_item;
  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "title": "Photocopier",
    "width": 240,
    "height": isAI ? 309 : 234,
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      children: [has_toner ? (0, _inferno.createComponentVNode)(2, Toner) : (0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": "Toner",
        children: (0, _inferno.createComponentVNode)(2, _components.Box, {
          "color": "average",
          children: "No inserted toner cartridge."
        })
      }), has_item ? (0, _inferno.createComponentVNode)(2, Options) : (0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": "Options",
        children: (0, _inferno.createComponentVNode)(2, _components.Box, {
          "color": "average",
          children: "No inserted item."
        })
      }), !!isAI && (0, _inferno.createComponentVNode)(2, AIOptions)]
    })
  });
};

exports.Photocopier = Photocopier;

var Toner = function Toner(props, context) {
  var _useBackend2 = (0, _backend.useBackend)(context),
      act = _useBackend2.act,
      data = _useBackend2.data;

  var max_toner = data.max_toner,
      current_toner = data.current_toner;
  var average_toner = max_toner * 0.66;
  var bad_toner = max_toner * 0.33;
  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    "title": "Toner",
    children: (0, _inferno.createComponentVNode)(2, _components.ProgressBar, {
      "ranges": {
        good: [average_toner, max_toner],
        average: [bad_toner, average_toner],
        bad: [0, bad_toner]
      },
      "value": current_toner,
      "minValue": 0,
      "maxValue": max_toner
    })
  });
};

var Options = function Options(props, context) {
  var _useBackend3 = (0, _backend.useBackend)(context),
      act = _useBackend3.act,
      data = _useBackend3.data;

  var num_copies = data.num_copies,
      has_enough_toner = data.has_enough_toner;
  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    "title": "Options",
    children: [(0, _inferno.createComponentVNode)(2, _components.Flex, {
      children: [(0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
        "mt": 0.4,
        "width": 11,
        "color": "label",
        children: "Make copies:"
      }), (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
        children: (0, _inferno.createComponentVNode)(2, _components.NumberInput, {
          "animate": true,
          "width": 2.6,
          "height": 1.65,
          "step": 1,
          "stepPixelSize": 8,
          "minValue": 1,
          "maxValue": 10,
          "value": num_copies,
          "onDrag": function () {
            function onDrag(e, value) {
              return act('set_copies', {
                num_copies: value
              });
            }

            return onDrag;
          }()
        })
      }), (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
        children: (0, _inferno.createComponentVNode)(2, _components.Button, {
          "ml": 0.2,
          "icon": "copy",
          "textAlign": "center",
          "onClick": function () {
            function onClick() {
              return act('make_copy');
            }

            return onClick;
          }(),
          children: "Copy"
        })
      })]
    }), (0, _inferno.createComponentVNode)(2, _components.Button, {
      "mt": 0.5,
      "textAlign": "center",
      "icon": "reply",
      "fluid": true,
      "onClick": function () {
        function onClick() {
          return act('remove');
        }

        return onClick;
      }(),
      children: "Remove item"
    })]
  });
};

var AIOptions = function AIOptions(props, context) {
  var _useBackend4 = (0, _backend.useBackend)(context),
      act = _useBackend4.act,
      data = _useBackend4.data;

  var can_AI_print = data.can_AI_print;
  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    "title": "AI Options",
    children: (0, _inferno.createComponentVNode)(2, _components.Box, {
      children: (0, _inferno.createComponentVNode)(2, _components.Button, {
        "fluid": true,
        "icon": "images",
        "textAlign": "center",
        "disabled": !can_AI_print,
        "onClick": function () {
          function onClick() {
            return act('ai_photo');
          }

          return onClick;
        }(),
        children: "Print photo from database"
      })
    })
  });
};

/***/ }),

/***/ 13484:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.PortablePump = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var _PortableAtmos = __webpack_require__(41161);

var PortablePump = function PortablePump(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var direction = data.direction,
      target_pressure = data.target_pressure,
      default_pressure = data.default_pressure,
      min_pressure = data.min_pressure,
      max_pressure = data.max_pressure;
  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "width": 300,
    "height": 405,
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      children: [(0, _inferno.createComponentVNode)(2, _PortableAtmos.PortableBasicInfo), (0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": "Pump",
        "buttons": (0, _inferno.createComponentVNode)(2, _components.Button, {
          "icon": direction ? 'sign-in-alt' : 'sign-out-alt',
          "content": direction ? 'In' : 'Out',
          "selected": direction,
          "onClick": function () {
            function onClick() {
              return act('direction');
            }

            return onClick;
          }()
        }),
        children: (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
          children: [(0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Output",
            children: (0, _inferno.createComponentVNode)(2, _components.NumberInput, {
              "value": target_pressure,
              "unit": "kPa",
              "width": "75px",
              "minValue": min_pressure,
              "maxValue": max_pressure,
              "step": 10,
              "onChange": function () {
                function onChange(e, value) {
                  return act('pressure', {
                    pressure: value
                  });
                }

                return onChange;
              }()
            })
          }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Presets",
            children: [(0, _inferno.createComponentVNode)(2, _components.Button, {
              "icon": "minus",
              "disabled": target_pressure === min_pressure,
              "onClick": function () {
                function onClick() {
                  return act('pressure', {
                    pressure: 'min'
                  });
                }

                return onClick;
              }()
            }), (0, _inferno.createComponentVNode)(2, _components.Button, {
              "icon": "sync",
              "disabled": target_pressure === default_pressure,
              "onClick": function () {
                function onClick() {
                  return act('pressure', {
                    pressure: 'reset'
                  });
                }

                return onClick;
              }()
            }), (0, _inferno.createComponentVNode)(2, _components.Button, {
              "icon": "plus",
              "disabled": target_pressure === max_pressure,
              "onClick": function () {
                function onClick() {
                  return act('pressure', {
                    pressure: 'max'
                  });
                }

                return onClick;
              }()
            })]
          })]
        })
      })]
    })
  });
};

exports.PortablePump = PortablePump;

/***/ }),

/***/ 54652:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.PortableScrubber = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var _PortableAtmos = __webpack_require__(41161);

var PortableScrubber = function PortableScrubber(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var filter_types = data.filter_types || [];
  var target_rate = data.target_rate,
      default_rate = data.default_rate,
      min_rate = data.min_rate,
      max_rate = data.max_rate;
  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "width": 300,
    "height": 405,
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      children: [(0, _inferno.createComponentVNode)(2, _PortableAtmos.PortableBasicInfo), (0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": "Scrubber",
        children: (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
          children: [(0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Volume Rate",
            children: (0, _inferno.createComponentVNode)(2, _components.NumberInput, {
              "value": target_rate,
              "unit": "L/s",
              "width": "75px",
              "minValue": min_rate,
              "maxValue": max_rate,
              "step": 10,
              "onChange": function () {
                function onChange(e, value) {
                  return act('rate', {
                    rate: value
                  });
                }

                return onChange;
              }()
            })
          }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Presets",
            children: [(0, _inferno.createComponentVNode)(2, _components.Button, {
              "icon": "minus",
              "disabled": target_rate === min_rate,
              "onClick": function () {
                function onClick() {
                  return act('rate', {
                    rate: 'min'
                  });
                }

                return onClick;
              }()
            }), (0, _inferno.createComponentVNode)(2, _components.Button, {
              "icon": "sync",
              "disabled": target_rate === default_rate,
              "onClick": function () {
                function onClick() {
                  return act('rate', {
                    rate: 'reset'
                  });
                }

                return onClick;
              }()
            }), (0, _inferno.createComponentVNode)(2, _components.Button, {
              "icon": "plus",
              "disabled": target_rate === max_rate,
              "onClick": function () {
                function onClick() {
                  return act('rate', {
                    rate: 'max'
                  });
                }

                return onClick;
              }()
            })]
          })]
        })
      })]
    })
  });
};

exports.PortableScrubber = PortableScrubber;

/***/ }),

/***/ 79051:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.ReligiousTool = void 0;

var _inferno = __webpack_require__(96323);

var _string = __webpack_require__(21118);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var ASPECT2COLOR = [];

var GetTab = function GetTab(tab, sects) {
  if (tab === 3) {
    return (0, _inferno.createComponentVNode)(2, Encyclopedia);
  }

  if (sects) {
    return (0, _inferno.createComponentVNode)(2, SectSelectTab);
  }

  if (tab === 1) {
    return (0, _inferno.createComponentVNode)(2, ReligionTab);
  }

  if (tab === 2) {
    return (0, _inferno.createComponentVNode)(2, RiteTab);
  }
};

var ReligiousTool = function ReligiousTool(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var _useSharedState = (0, _backend.useSharedState)(context, 'tab', 1),
      tab = _useSharedState[0],
      setTab = _useSharedState[1];

  var sects = data.sects,
      encyclopedia = data.encyclopedia;
  encyclopedia.ASPECTS.map(function (aspect) {
    return ASPECT2COLOR[aspect.name] = aspect.color;
  });
  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "width": 1000,
    "height": 700,
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      "fontSize": "14px",
      children: [(0, _inferno.createComponentVNode)(2, _components.Tabs, {
        "textAlign": "center",
        children: (0, _inferno.createComponentVNode)(2, _components.Stack, {
          "direction": "raw",
          "width": "100%",
          children: [(0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
            "grow": true,
            children: (0, _inferno.createComponentVNode)(2, _components.Tabs.Tab, {
              "fluid": true,
              "selected": tab === 1,
              "onClick": function () {
                function onClick() {
                  return setTab(1);
                }

                return onClick;
              }(),
              children: ["Religion ", (0, _inferno.createComponentVNode)(2, _components.Icon, {
                "name": "place-of-worship"
              })]
            })
          }), !sects && (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
            "grow": true,
            children: (0, _inferno.createComponentVNode)(2, _components.Tabs.Tab, {
              "fluid": true,
              "selected": tab === 2,
              "onClick": function () {
                function onClick() {
                  return setTab(2);
                }

                return onClick;
              }(),
              children: ["Rites ", (0, _inferno.createComponentVNode)(2, _components.Icon, {
                "name": "pray"
              })]
            })
          }), (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
            "grow": true,
            children: (0, _inferno.createComponentVNode)(2, _components.Tabs.Tab, {
              "fluid": true,
              "selected": tab === 3,
              "onClick": function () {
                function onClick() {
                  return setTab(3);
                }

                return onClick;
              }(),
              children: ["Encyclopedia ", (0, _inferno.createComponentVNode)(2, _components.Icon, {
                "name": "book-open"
              })]
            })
          })]
        })
      }), (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
        children: GetTab(tab, sects)
      })]
    })
  });
};

exports.ReligiousTool = ReligiousTool;

var GetInfoItem = function GetInfoItem(title, list) {
  var listItems = null;

  if (!list.length) {
    listItems = (0, _inferno.createComponentVNode)(2, _components.Box, {
      "color": "gray",
      children: "Nothing."
    });
  } else {
    listItems = list.map(function (elem) {
      return (0, _inferno.createVNode)(1, "li", null, (0, _inferno.createComponentVNode)(2, _components.Box, {
        children: (0, _string.toTitleCase)(elem)
      }), 2, null, elem);
    });
  }

  return (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
    "width": "100%",
    "height": 22,
    children: (0, _inferno.createComponentVNode)(2, _components.Section, {
      "title": title,
      "fill": 1,
      children: (0, _inferno.createComponentVNode)(2, _components.Box, {
        "textAlign": "left",
        "ml": !list.length ? 0 : 3,
        children: (0, _inferno.createVNode)(1, "ui", null, listItems, 0)
      })
    })
  });
};

var ReligionTab = function ReligionTab(props, context) {
  var _useBackend2 = (0, _backend.useBackend)(context),
      act = _useBackend2.act,
      data = _useBackend2.data;

  var name = data.name,
      deities = data.deities,
      favor = data.favor,
      piety = data.piety,
      max_favor = data.max_favor,
      passive_favor_gain = data.passive_favor_gain,
      aspects = data.aspects,
      techs = data.techs,
      god_spells = data.god_spells,
      holy_reagents = data.holy_reagents,
      faith_reactions = data.faith_reactions;
  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    "title": name,
    "textAlign": "center",
    children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
      children: deities
    }), (0, _inferno.createComponentVNode)(2, _components.Box, {
      children: [(0, _inferno.createComponentVNode)(2, _components.Stack, {
        "mt": 2,
        children: [(0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
          "width": "100%",
          "height": 22,
          children: (0, _inferno.createComponentVNode)(2, _components.Section, {
            "title": "Resources",
            "fill": 1,
            children: (0, _inferno.createComponentVNode)(2, _components.Box, {
              "textAlign": "left",
              "ml": 3,
              children: (0, _inferno.createVNode)(1, "ui", null, [(0, _inferno.createVNode)(1, "li", null, (0, _inferno.createComponentVNode)(2, _components.Box, {
                "color": "yellow",
                children: ["Favor: ", favor, " / ", max_favor, (0, _inferno.createVNode)(1, "span", null, [(0, _inferno.createTextVNode)(" (+"), passive_favor_gain, (0, _inferno.createTextVNode)(")")], 0, {
                  "style": {
                    'color': 'gray',
                    'font-size': '8pt'
                  }
                })]
              }), 2), (0, _inferno.createVNode)(1, "li", null, (0, _inferno.createComponentVNode)(2, _components.Box, {
                "color": "pink",
                children: ["Piety: ", piety]
              }), 2)], 4)
            })
          })
        }), (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
          "width": "100%",
          "height": 22,
          children: (0, _inferno.createComponentVNode)(2, _components.Section, {
            "title": "Aspects",
            "fill": 1,
            children: (0, _inferno.createComponentVNode)(2, _components.Box, {
              "textAlign": "left",
              children: GetAspectBox("", aspects)
            })
          })
        }), GetInfoItem("Techs", techs)]
      }), (0, _inferno.createComponentVNode)(2, _components.Stack, {
        children: [GetInfoItem("God Spells", god_spells), GetInfoItem("Holy Reagents", holy_reagents), GetInfoItem("Faith Reactions", faith_reactions)]
      })]
    })]
  });
};

var GetAspectBox = function GetAspectBox(title, aspects, need_br) {
  if (need_br === void 0) {
    need_br = true;
  }

  if (!aspects) {
    return null;
  }

  return (0, _inferno.createComponentVNode)(2, _components.Box, {
    children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
      "bold": true,
      children: title
    }), (0, _inferno.createComponentVNode)(2, _components.Box, {
      "ml": 3,
      children: [(0, _inferno.createVNode)(1, "ui", null, Object.keys(aspects).map(function (aspect) {
        return (0, _inferno.createVNode)(1, "li", null, (0, _inferno.createComponentVNode)(2, _components.Box, {
          "color": ASPECT2COLOR[aspect],
          children: [aspect, " = ", aspects[aspect]]
        }), 2, null, aspect);
      }), 0), need_br ? (0, _inferno.createVNode)(1, "br") : ""]
    })]
  });
};

var GetCostsBox = function GetCostsBox(favor, piety, need_br) {
  if (need_br === void 0) {
    need_br = true;
  }

  if (!favor && !piety) {
    return null;
  }

  return (0, _inferno.createComponentVNode)(2, _components.Box, {
    children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
      "bold": true,
      children: "Costs:"
    }), (0, _inferno.createComponentVNode)(2, _components.Box, {
      "ml": 3,
      children: (0, _inferno.createVNode)(1, "ui", null, [!!favor && (0, _inferno.createVNode)(1, "li", null, (0, _inferno.createComponentVNode)(2, _components.Box, {
        "color": "yellow",
        children: [favor, " favor"]
      }), 2), !!piety && (0, _inferno.createVNode)(1, "li", null, (0, _inferno.createComponentVNode)(2, _components.Box, {
        "color": "pink",
        children: [piety, " piety"]
      }), 2)], 0)
    }), need_br ? (0, _inferno.createVNode)(1, "br") : ""]
  });
};

var SectSelectTab = function SectSelectTab(props, context) {
  var _useBackend3 = (0, _backend.useBackend)(context),
      act = _useBackend3.act,
      data = _useBackend3.data;

  var sects = data.sects,
      holds_religious_tool = data.holds_religious_tool;
  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    "fill": true,
    "title": "Sect Select",
    children: (0, _inferno.createComponentVNode)(2, _components.Stack, {
      "vertical": true,
      children: sects.map(function (sect) {
        return (0, _inferno.createComponentVNode)(2, _components.Collapsible, {
          "title": (0, _inferno.createVNode)(1, "b", null, sect.name, 0),
          "color": "transparent",
          children: (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
            children: [(0, _inferno.createComponentVNode)(2, _components.BlockQuote, {
              children: (0, _inferno.createComponentVNode)(2, _components.Box, {
                children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
                  "bold": true,
                  children: sect.desc
                }), (0, _inferno.createComponentVNode)(2, _components.Box, {
                  children: [sect.aspect_preset || sect.aspects_count ? (0, _inferno.createVNode)(1, "br") : "", GetAspectBox("Aspects:", sect.aspect_preset), sect.aspects_count && (0, _inferno.createComponentVNode)(2, _components.Box, {
                    children: ["You can choose ", sect.aspects_count, " aspects."]
                  })]
                })]
              })
            }), (0, _inferno.createComponentVNode)(2, _components.Button, {
              "mt": 2,
              "textAlign": "center",
              "icon": "plus",
              "fluid": true,
              "disabled": !holds_religious_tool,
              "onClick": function () {
                function onClick() {
                  return act('sect_select', {
                    path: sect.path
                  });
                }

                return onClick;
              }(),
              children: [sect.aspects_count ? "Create" : "Select", " ", sect.name]
            })]
          }, sect.name)
        }, sect.name);
      })
    })
  });
};

var Encyclopedia = function Encyclopedia(props, context) {
  var _useBackend4 = (0, _backend.useBackend)(context),
      act = _useBackend4.act,
      data = _useBackend4.data;

  var _useSharedState2 = (0, _backend.useSharedState)(context, 'cat', ''),
      cat = _useSharedState2[0],
      setCat = _useSharedState2[1];

  var encyclopedia = data.encyclopedia; // about categories here code\modules\religion\encyclopedia.dm

  return (0, _inferno.createComponentVNode)(2, _components.Stack, {
    children: [(0, _inferno.createComponentVNode)(2, _components.Tabs, {
      "vertical": 1,
      children: Object.keys(encyclopedia).map(function (category) {
        return (0, _inferno.createComponentVNode)(2, _components.Tabs.Tab, {
          "fluid": true,
          "selected": cat === category,
          "onClick": function () {
            function onClick() {
              return setCat(category);
            }

            return onClick;
          }(),
          children: (0, _string.toTitleCase)(category)
        }, category);
      })
    }), (0, _inferno.createComponentVNode)(2, _components.Section, {
      "height": 52,
      "fill": true,
      "scrollable": true,
      "width": "100%",
      children: (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
        children: [cat === "RITES" && (0, _inferno.createComponentVNode)(2, ERitesTab), cat === "SECTS" && (0, _inferno.createComponentVNode)(2, ESectsTab), cat === "ASPECTS" && (0, _inferno.createComponentVNode)(2, EAspectsTab), cat === "GOD SPELLS" && (0, _inferno.createComponentVNode)(2, ESpellsTab), cat === "HOLY REAGENTS" && (0, _inferno.createComponentVNode)(2, EReagentsTab), cat === "FAITH REACTIONS" && (0, _inferno.createComponentVNode)(2, EReactionsTab)]
      })
    })]
  });
};

var ERitesTab = function ERitesTab(props, context) {
  var _useBackend5 = (0, _backend.useBackend)(context),
      act = _useBackend5.act,
      data = _useBackend5.data;

  var encyclopedia = data.encyclopedia;
  var RITES = encyclopedia.RITES;

  var _useSharedState3 = (0, _backend.useSharedState)(context, 'searchText', ''),
      searchText = _useSharedState3[0],
      setSearchText = _useSharedState3[1];

  var testSearch = (0, _string.createSearch)(searchText, function (rite) {
    return rite.name;
  });
  var items = searchText.length > 0 // Flatten all categories and apply search to it
  && RITES.filter(testSearch) // If none of that results in a list, return an empty list
  || RITES;
  return (0, _inferno.createComponentVNode)(2, _components.Box, {
    children: [(0, _inferno.createComponentVNode)(2, _components.Input, {
      "autoFocus": true,
      "fluid": true,
      "placeholder": "Search for...",
      "onInput": function () {
        function onInput(e, v) {
          return setSearchText(v);
        }

        return onInput;
      }(),
      "mb": 1
    }), items.map(function (rite) {
      return (0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": rite.name,
        children: (0, _inferno.createComponentVNode)(2, _components.BlockQuote, {
          children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
            children: rite.desc.replace(/<[/]?i>/g, '')
          }), (0, _inferno.createVNode)(1, "br"), (0, _inferno.createComponentVNode)(2, _components.Box, {
            children: [(0, _inferno.createVNode)(1, "b", null, "Length:", 16), " ", rite.ritual_length / 10, " seconds."]
          }), (0, _inferno.createComponentVNode)(2, _components.Box, {
            "color": rite.can_talismaned ? "green" : "red",
            children: ["Can", rite.can_talismaned ? "" : "'t", " be talismaned."]
          }), !rite.needed_aspects ? "" : (0, _inferno.createVNode)(1, "br"), GetAspectBox("Needed Aspects:", rite.needed_aspects, false), (!!rite.favor_cost || !!rite.piety_cost) && (0, _inferno.createVNode)(1, "br"), GetCostsBox(rite.favor_cost, rite.piety_cost, false), (0, _inferno.createComponentVNode)(2, _components.Box, {
            children: !!rite.tips.length && (0, _inferno.createComponentVNode)(2, _components.Box, {
              children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
                "bold": true,
                children: [" ", (0, _inferno.createVNode)(1, "br"), "Tips:"]
              }), (0, _inferno.createComponentVNode)(2, _components.Box, {
                "ml": 3,
                children: (0, _inferno.createVNode)(1, "ui", null, rite.tips.map(function (tip) {
                  return (0, _inferno.createVNode)(1, "li", null, (0, _inferno.createComponentVNode)(2, _components.Box, {
                    children: tip.replace(/<[/]?i>/g, '')
                  }), 2, null, tip);
                }), 0)
              })]
            })
          })]
        })
      }, rite.name);
    })]
  });
};

var ESectsTab = function ESectsTab(props, context) {
  var _useBackend6 = (0, _backend.useBackend)(context),
      act = _useBackend6.act,
      data = _useBackend6.data;

  var encyclopedia = data.encyclopedia;
  var SECTS = encyclopedia.SECTS;

  var _useSharedState4 = (0, _backend.useSharedState)(context, 'searchText', ''),
      searchText = _useSharedState4[0],
      setSearchText = _useSharedState4[1];

  var testSearch = (0, _string.createSearch)(searchText, function (sect) {
    return sect.name;
  });
  var items = searchText.length > 0 // Flatten all categories and apply search to it
  && SECTS.filter(testSearch) // If none of that results in a list, return an empty list
  || SECTS;
  return (0, _inferno.createComponentVNode)(2, _components.Box, {
    children: [(0, _inferno.createComponentVNode)(2, _components.Input, {
      "autoFocus": true,
      "fluid": true,
      "placeholder": "Search for...",
      "onInput": function () {
        function onInput(e, v) {
          return setSearchText(v);
        }

        return onInput;
      }(),
      "mb": 1
    }), items.map(function (sect) {
      return (0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": sect.name,
        children: (0, _inferno.createComponentVNode)(2, _components.BlockQuote, {
          children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
            children: sect.desc
          }), (0, _inferno.createVNode)(1, "br"), GetAspectBox("Aspect Preset:", sect.aspect_preset, false), sect.aspects_count && (0, _inferno.createComponentVNode)(2, _components.Box, {
            children: ["You can choose ", sect.aspects_count, " aspects."]
          })]
        })
      }, sect.name);
    })]
  });
};

var EAspectsTab = function EAspectsTab(props, context) {
  var _useBackend7 = (0, _backend.useBackend)(context),
      act = _useBackend7.act,
      data = _useBackend7.data;

  var encyclopedia = data.encyclopedia;
  var ASPECTS = encyclopedia.ASPECTS;

  var _useSharedState5 = (0, _backend.useSharedState)(context, 'searchText', ''),
      searchText = _useSharedState5[0],
      setSearchText = _useSharedState5[1];

  var testSearch = (0, _string.createSearch)(searchText, function (aspect) {
    return aspect.name;
  });
  var items = searchText.length > 0 // Flatten all categories and apply search to it
  && ASPECTS.filter(testSearch) // If none of that results in a list, return an empty list
  || ASPECTS;
  return (0, _inferno.createComponentVNode)(2, _components.Box, {
    children: [(0, _inferno.createComponentVNode)(2, _components.Input, {
      "autoFocus": true,
      "fluid": true,
      "placeholder": "Search for...",
      "onInput": function () {
        function onInput(e, v) {
          return setSearchText(v);
        }

        return onInput;
      }(),
      "mb": 1
    }), items.map(function (aspect) {
      return (0, _inferno.createComponentVNode)(2, _components.Section, {
        "color": ASPECT2COLOR[aspect.name],
        "title": aspect.name,
        children: (0, _inferno.createComponentVNode)(2, _components.BlockQuote, {
          children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
            children: aspect.desc
          }), aspect.god_desc && (0, _inferno.createComponentVNode)(2, _components.Box, {
            children: [" ", (0, _inferno.createVNode)(1, "br"), aspect.god_desc]
          })]
        })
      }, aspect.name);
    })]
  });
};

var ESpellsTab = function ESpellsTab(props, context) {
  var _useBackend8 = (0, _backend.useBackend)(context),
      act = _useBackend8.act,
      data = _useBackend8.data;

  var encyclopedia = data.encyclopedia;
  var GOD_SPELLS = encyclopedia["GOD SPELLS"];

  var _useSharedState6 = (0, _backend.useSharedState)(context, 'searchText', ''),
      searchText = _useSharedState6[0],
      setSearchText = _useSharedState6[1];

  var testSearch = (0, _string.createSearch)(searchText, function (spell) {
    return spell.name;
  });
  var items = searchText.length > 0 // Flatten all categories and apply search to it
  && GOD_SPELLS.filter(testSearch) // If none of that results in a list, return an empty list
  || GOD_SPELLS;
  return (0, _inferno.createComponentVNode)(2, _components.Box, {
    children: [(0, _inferno.createComponentVNode)(2, _components.Input, {
      "autoFocus": true,
      "fluid": true,
      "placeholder": "Search for...",
      "onInput": function () {
        function onInput(e, v) {
          return setSearchText(v);
        }

        return onInput;
      }(),
      "mb": 1
    }), items.map(function (spell) {
      return (0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": spell.name,
        children: (0, _inferno.createComponentVNode)(2, _components.BlockQuote, {
          children: [GetAspectBox("Needed Aspects:", spell.needed_aspects), GetCostsBox(spell.favor_cost), (0, _inferno.createComponentVNode)(2, _components.Box, {
            children: ["Cooldown: ", spell.charge_max / 10, " seconds"]
          })]
        })
      }, spell.name);
    })]
  });
};

var EReagentsTab = function EReagentsTab(props, context) {
  var _useBackend9 = (0, _backend.useBackend)(context),
      act = _useBackend9.act,
      data = _useBackend9.data;

  var encyclopedia = data.encyclopedia;
  var HOLY_REAGENTS = encyclopedia["HOLY REAGENTS"];

  var _useSharedState7 = (0, _backend.useSharedState)(context, 'searchText', ''),
      searchText = _useSharedState7[0],
      setSearchText = _useSharedState7[1];

  var testSearch = (0, _string.createSearch)(searchText, function (reagent) {
    return reagent.name;
  });
  var items = searchText.length > 0 // Flatten all categories and apply search to it
  && HOLY_REAGENTS.filter(testSearch) // If none of that results in a list, return an empty list
  || HOLY_REAGENTS;
  return (0, _inferno.createComponentVNode)(2, _components.Box, {
    children: [(0, _inferno.createComponentVNode)(2, _components.Input, {
      "autoFocus": true,
      "fluid": true,
      "placeholder": "Search for...",
      "onInput": function () {
        function onInput(e, v) {
          return setSearchText(v);
        }

        return onInput;
      }(),
      "mb": 1
    }), items.map(function (reagent) {
      return (0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": reagent.name,
        children: (0, _inferno.createComponentVNode)(2, _components.BlockQuote, {
          children: GetAspectBox("Needed Aspects:", reagent.needed_aspects, false)
        })
      }, reagent.name);
    })]
  });
};

var EReactionsTab = function EReactionsTab(props, context) {
  var _useBackend10 = (0, _backend.useBackend)(context),
      act = _useBackend10.act,
      data = _useBackend10.data;

  var encyclopedia = data.encyclopedia;
  var FAITH_REACTIONS = encyclopedia["FAITH REACTIONS"];

  var _useSharedState8 = (0, _backend.useSharedState)(context, 'searchText', ''),
      searchText = _useSharedState8[0],
      setSearchText = _useSharedState8[1];

  var testSearch = (0, _string.createSearch)(searchText, function (reaction) {
    return reaction.convertable_id + " to " + reaction.result_id;
  });
  var items = searchText.length > 0 // Flatten all categories and apply search to it
  && FAITH_REACTIONS.filter(testSearch) // If none of that results in a list, return an empty list
  || FAITH_REACTIONS;
  return (0, _inferno.createComponentVNode)(2, _components.Box, {
    children: [(0, _inferno.createComponentVNode)(2, _components.Input, {
      "autoFocus": true,
      "fluid": true,
      "placeholder": "Search for...",
      "onInput": function () {
        function onInput(e, v) {
          return setSearchText(v);
        }

        return onInput;
      }(),
      "mb": 1
    }), items.map(function (reaction) {
      return (0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": (0, _string.capitalize)(reaction.convertable_id) + " to " + (0, _string.capitalize)(reaction.result_id),
        children: (0, _inferno.createComponentVNode)(2, _components.BlockQuote, {
          children: [GetAspectBox("Needed Aspects:", reaction.needed_aspects, false), reaction.favor_cost ? (0, _inferno.createVNode)(1, "br") : "", GetCostsBox(reaction.favor_cost, 0, false)]
        })
      }, (0, _string.capitalize)(reaction.convertable_id) + " to " + (0, _string.capitalize)(reaction.result_id));
    })]
  });
};

var RiteTab = function RiteTab(props, context) {
  var _useBackend11 = (0, _backend.useBackend)(context),
      act = _useBackend11.act,
      data = _useBackend11.data;

  var rites = data.rites,
      favor = data.favor,
      piety = data.piety,
      can_talismaning = data.can_talismaning,
      holds_religious_tool = data.holds_religious_tool;

  var _useSharedState9 = (0, _backend.useSharedState)(context, 'searchText', ''),
      searchText = _useSharedState9[0],
      setSearchText = _useSharedState9[1];

  var testSearch = (0, _string.createSearch)(searchText, function (rite) {
    return rite.name;
  });
  var items = searchText.length > 0 // Flatten all categories and apply search to it
  && rites.filter(testSearch) // If none of that results in a list, return an empty list
  || rites;
  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    "height": 52,
    "fill": true,
    "scrollable": true,
    "width": "100%",
    children: [(0, _inferno.createComponentVNode)(2, _components.Input, {
      "autoFocus": true,
      "fluid": true,
      "placeholder": "Search for...",
      "onInput": function () {
        function onInput(e, v) {
          return setSearchText(v);
        }

        return onInput;
      }(),
      "mb": 1
    }), (0, _inferno.createComponentVNode)(2, _components.Stack, {
      "vertical": true,
      "mt": 2,
      children: items.map(function (rite) {
        return (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
          children: (0, _inferno.createComponentVNode)(2, _components.Section, {
            "title": rite.name,
            "buttons": (0, _inferno.createFragment)([(0, _inferno.createComponentVNode)(2, _components.Button, {
              "fontColor": "white",
              "disabled": !holds_religious_tool || favor < rite.favor_cost || piety < rite.piety_cost,
              "icon": "arrow-right",
              "onClick": function () {
                function onClick() {
                  return act('perform_rite', {
                    rite_name: rite.name
                  });
                }

                return onClick;
              }(),
              children: "Invoke"
            }), (0, _inferno.createComponentVNode)(2, _components.Button, {
              "fontColor": "white",
              "tooltip": rite.favor_cost * 2 + " favor" + (rite.piety_cost > 0 ? " " + rite.piety_cost + " piety" : ""),
              "disabled": !can_talismaning || !rite.can_talismaned || favor < rite.favor_cost * 2 || piety < rite.piety_cost * 2,
              "icon": "scroll",
              "onClick": function () {
                function onClick() {
                  return act('talismaning_rite', {
                    rite_name: rite.name
                  });
                }

                return onClick;
              }(),
              children: "Talismaning"
            })], 4),
            children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
              "color": favor < rite.favor_cost ? "red" : "yellow",
              "mb": 0.5,
              children: [(0, _inferno.createComponentVNode)(2, _components.Icon, {
                "name": "star"
              }), " Costs: ", rite.favor_cost, " favor", rite.piety_cost > 0 ? " and " + rite.piety_cost + " piety" : "", "."]
            }), (0, _inferno.createComponentVNode)(2, _components.BlockQuote, {
              children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
                children: rite.desc
              }), (0, _inferno.createVNode)(1, "br"), (0, _inferno.createComponentVNode)(2, _components.Box, {
                children: ["Power: ", rite.power]
              }), (0, _inferno.createComponentVNode)(2, _components.Box, {
                children: !!rite.tips.length && (0, _inferno.createComponentVNode)(2, _components.Box, {
                  children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
                    "bold": true,
                    children: [" ", (0, _inferno.createVNode)(1, "br"), "Tips:"]
                  }), (0, _inferno.createComponentVNode)(2, _components.Box, {
                    "ml": 3,
                    children: (0, _inferno.createVNode)(1, "ui", null, rite.tips.map(function (tip) {
                      return (0, _inferno.createVNode)(1, "li", null, (0, _inferno.createComponentVNode)(2, _components.Box, {
                        children: tip.replace(/<[/]?i>/g, '')
                      }), 2, null, tip);
                    }), 0)
                  })]
                })
              })]
            })]
          })
        }, rite.name);
      })
    })]
  });
};

/***/ }),

/***/ 1945:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Safe = void 0;

var _inferno = __webpack_require__(96323);

var _assets = __webpack_require__(39646);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var Safe = function Safe(properties, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var dial = data.dial,
      open = data.open;
  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "width": 625,
    "height": 760,
    "theme": "ntos",
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
        "className": "Safe__engraving",
        children: [(0, _inferno.createComponentVNode)(2, Dialer), (0, _inferno.createComponentVNode)(2, _components.Box, {
          children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
            "className": "Safe__engraving-hinge",
            "top": "25%"
          }), (0, _inferno.createComponentVNode)(2, _components.Box, {
            "className": "Safe__engraving-hinge",
            "top": "75%"
          })]
        }), (0, _inferno.createComponentVNode)(2, _components.Icon, {
          "className": "Safe__engraving-arrow",
          "name": "long-arrow-alt-down",
          "size": "3"
        }), (0, _inferno.createVNode)(1, "br"), open ? (0, _inferno.createComponentVNode)(2, Contents) : (0, _inferno.createComponentVNode)(2, _components.Box, {
          "as": "img",
          "className": "Safe__dial",
          "src": (0, _assets.resolveAsset)('safe_dial.png'),
          "style": {
            "transform": "rotate(-" + 3.6 * dial + "deg)"
          }
        })]
      }), !open && (0, _inferno.createComponentVNode)(2, Help)]
    })
  });
};

exports.Safe = Safe;

var Dialer = function Dialer(properties, context) {
  var _useBackend2 = (0, _backend.useBackend)(context),
      act = _useBackend2.act,
      data = _useBackend2.data;

  var dial = data.dial,
      open = data.open,
      locked = data.locked;

  var dialButton = function dialButton(amount, right) {
    return (0, _inferno.createComponentVNode)(2, _components.Button, {
      "disabled": open || right && !locked,
      "icon": "arrow-" + (right ? "right" : "left"),
      "content": (right ? "Right" : "Left") + " " + amount,
      "iconPosition": right ? "right" : "left",
      "onClick": function () {
        function onClick() {
          return act(!right ? "turnright" : "turnleft", {
            num: amount
          });
        }

        return onClick;
      }()
    });
  };

  return (0, _inferno.createComponentVNode)(2, _components.Box, {
    "className": "Safe__dialer",
    children: [(0, _inferno.createComponentVNode)(2, _components.Button, {
      "disabled": locked,
      "icon": open ? "lock" : "lock-open",
      "content": open ? "Close" : "Open",
      "mb": "0.5rem",
      "onClick": function () {
        function onClick() {
          return act('open');
        }

        return onClick;
      }()
    }), (0, _inferno.createVNode)(1, "br"), (0, _inferno.createComponentVNode)(2, _components.Box, {
      "position": "absolute",
      children: [dialButton(50), dialButton(10), dialButton(1)]
    }), (0, _inferno.createComponentVNode)(2, _components.Box, {
      "className": "Safe__dialer-right",
      "position": "absolute",
      "right": "5px",
      children: [dialButton(1, true), dialButton(10, true), dialButton(50, true)]
    }), (0, _inferno.createComponentVNode)(2, _components.Box, {
      "className": "Safe__dialer-number",
      children: dial
    })]
  });
};

var Contents = function Contents(properties, context) {
  var _useBackend3 = (0, _backend.useBackend)(context),
      act = _useBackend3.act,
      data = _useBackend3.data;

  var contents = data.contents;
  return (0, _inferno.createComponentVNode)(2, _components.Box, {
    "className": "Safe__contents",
    "overflow": "auto",
    children: contents.map(function (item, index) {
      return (0, _inferno.createFragment)([(0, _inferno.createComponentVNode)(2, _components.Button, {
        "mb": "0.5rem",
        "onClick": function () {
          function onClick() {
            return act("retrieve", {
              index: index + 1
            });
          }

          return onClick;
        }(),
        children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
          "as": "img",
          "src": item.sprite + ".png",
          "verticalAlign": "middle",
          "ml": "-6px",
          "mr": "0.5rem"
        }), item.name]
      }), (0, _inferno.createVNode)(1, "br")], 4, item);
    })
  });
};

var Help = function Help(properties, context) {
  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    "className": "Safe__help",
    "title": "Safe opening instructions (because you all keep forgetting)",
    children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
      children: ["1. Turn the dial left to the first number.", (0, _inferno.createVNode)(1, "br"), "2. Turn the dial right to the second number.", (0, _inferno.createVNode)(1, "br"), "3. Continue repeating this process for each number, switching between left and right each time.", (0, _inferno.createVNode)(1, "br"), "4. Open the safe."]
    }), (0, _inferno.createComponentVNode)(2, _components.Box, {
      "bold": true,
      children: "To lock fully, turn the dial to the left after closing the safe."
    })]
  });
};

/***/ }),

/***/ 69905:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.SamplerPipe = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var SamplerPipe = function SamplerPipe(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var gases = data.gases,
      nodeName = data.nodeName,
      locked = data.locked;
  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "title": "Atmospherics Alet System",
    "width": 300,
    "height": 400,
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      children: [(0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": "Node name",
        children: (0, _inferno.createComponentVNode)(2, _components.Input, {
          "disabled": locked,
          "fluid": true,
          "value": nodeName,
          "onChange": function () {
            function onChange(_, n) {
              return act("setName", {
                name: n
              });
            }

            return onChange;
          }()
        })
      }), (0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": "Gases",
        children: gases.map(function (gas) {
          return (0, _inferno.createComponentVNode)(2, _components.Section, {
            "title": gas.name,
            children: [(0, _inferno.createComponentVNode)(2, _components.Stack, {
              children: [(0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
                "grow": true,
                children: "Lower"
              }), (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
                children: (0, _inferno.createComponentVNode)(2, _components.NumberInput, {
                  "disabled": locked,
                  "width": 5,
                  "minValue": 0,
                  "maxValue": 1,
                  "step": 0.01,
                  "value": gas.threshold.min,
                  "onChange": function () {
                    function onChange(_, v) {
                      return act("setBound", {
                        id: gas.id,
                        bound: "min",
                        value: v
                      });
                    }

                    return onChange;
                  }()
                })
              })]
            }), (0, _inferno.createComponentVNode)(2, _components.Stack, {
              children: [(0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
                "grow": true,
                children: "Upper"
              }), (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
                children: (0, _inferno.createComponentVNode)(2, _components.NumberInput, {
                  "disabled": locked,
                  "width": 5,
                  "minValue": 0,
                  "maxValue": 1,
                  "step": 0.01,
                  "value": gas.threshold.max,
                  "onChange": function () {
                    function onChange(_, v) {
                      return act("setBound", {
                        id: gas.id,
                        bound: "max",
                        value: v
                      });
                    }

                    return onChange;
                  }()
                })
              })]
            })]
          }, gas.id);
        })
      })]
    })
  });
};

exports.SamplerPipe = SamplerPipe;

/***/ }),

/***/ 95285:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.SecureSafe = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var NukeKeypad = function NukeKeypad(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var keypadKeys = [['1', '4', '7', 'R'], ['2', '5', '8', '0'], ['3', '6', '9', 'E']];
  var locked = data.locked,
      l_setshort = data.l_setshort,
      code = data.code,
      emagged = data.emagged;
  return (0, _inferno.createComponentVNode)(2, _components.Box, {
    "width": "185px",
    children: (0, _inferno.createComponentVNode)(2, _components.Grid, {
      "width": "1px",
      children: keypadKeys.map(function (keyColumn) {
        return (0, _inferno.createComponentVNode)(2, _components.Grid.Column, {
          children: keyColumn.map(function (key) {
            return (0, _inferno.createComponentVNode)(2, _components.Button, {
              "fluid": true,
              "bold": true,
              "mb": "6px",
              "content": key,
              "textAlign": "center",
              "fontSize": "40px",
              "height": "50px",
              "lineHeight": 1.25,
              "disabled": !!emagged || !!l_setshort && 1 || key !== "R" && !locked || code === "ERROR" && key !== "R" && 1,
              "onClick": function () {
                function onClick() {
                  return act('type', {
                    digit: key
                  });
                }

                return onClick;
              }()
            }, key);
          })
        }, keyColumn[0]);
      })
    })
  });
};

var SecureSafe = function SecureSafe(props, context) {
  var _useBackend2 = (0, _backend.useBackend)(context),
      act = _useBackend2.act,
      data = _useBackend2.data;

  var code = data.code,
      l_setshort = data.l_setshort,
      l_set = data.l_set,
      emagged = data.emagged,
      locked = data.locked;
  var new_code = !(!!l_set || !!l_setshort);
  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "width": 250,
    "height": 380,
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      children: (0, _inferno.createComponentVNode)(2, _components.Box, {
        "m": "6px",
        children: [new_code && (0, _inferno.createComponentVNode)(2, _components.NoticeBox, {
          "textAlign": "center",
          "info": 1,
          children: "ENTER NEW 5-DIGIT PASSCODE."
        }), !!emagged && (0, _inferno.createComponentVNode)(2, _components.NoticeBox, {
          "textAlign": "center",
          "danger": 1,
          children: "LOCKING SYSTEM ERROR - 1701"
        }), !!l_setshort && (0, _inferno.createComponentVNode)(2, _components.NoticeBox, {
          "textAlign": "center",
          "danger": 1,
          children: "ALERT: MEMORY SYSTEM ERROR - 6040 201"
        }), (0, _inferno.createComponentVNode)(2, _components.Section, {
          "height": "60px",
          children: (0, _inferno.createComponentVNode)(2, _components.Box, {
            "textAlign": "center",
            "position": "center",
            "fontSize": "35px",
            children: code && code || (0, _inferno.createComponentVNode)(2, _components.Box, {
              "textColor": locked ? "red" : "green",
              children: locked ? "LOCKED" : "UNLOCKED"
            })
          })
        }), (0, _inferno.createComponentVNode)(2, _components.Flex, {
          "ml": "3px",
          children: [(0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
            children: (0, _inferno.createComponentVNode)(2, NukeKeypad)
          }), (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
            "ml": "6px",
            "width": "129px"
          })]
        })]
      })
    })
  });
};

exports.SecureSafe = SecureSafe;

/***/ }),

/***/ 98115:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.SkillsConsole = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var SkillsConsole = function SkillsConsole(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data; // Extract `health` and `color` variables from the `data` object.


  var skill_list = data.skill_list,
      IQ = data.IQ,
      MDI = data.MDI,
      skill_min_value = data.skill_min_value,
      skill_max_value = data.skill_max_value,
      compatible_species = data.compatible_species,
      inserted_cartridge = data.inserted_cartridge,
      cartridge_name = data.cartridge_name,
      cartridge_unpacked = data.cartridge_unpacked,
      connected_table = data.connected_table,
      cartridge_points = data.cartridge_points,
      connected_patient = data.connected_patient,
      free_points = data.free_points,
      can_inject = data.can_inject,
      power_usage = data.power_usage,
      power_max = data.power_max,
      power_current = data.power_current;
  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "resizable": true,
    "width": 600,
    "height": 675,
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      "scrollable": true,
      children: (0, _inferno.createComponentVNode)(2, _components.Stack, {
        "vertical": true,
        children: [(0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
          children: (0, _inferno.createComponentVNode)(2, _components.Section, {
            "title": "Power info",
            children: (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
              children: [(0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
                "label": "Active power usage",
                children: [power_usage / 1000, " kW"]
              }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
                "label": "Available power in area",
                children: (0, _inferno.createComponentVNode)(2, _components.ProgressBar, {
                  "ranges": {
                    good: [0.5, Infinity],
                    average: [0.25, 0.5],
                    bad: [-Infinity, 0.25]
                  },
                  "value": power_max !== 'No data' ? power_current / power_max : 'No data'
                })
              })]
            })
          })
        }), (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
          children: (0, _inferno.createComponentVNode)(2, _components.Stack, {
            children: [(0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
              "width": "40%",
              "mr": 1,
              children: (0, _inferno.createComponentVNode)(2, _components.Section, {
                "title": "Patient status",
                children: (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
                  children: [!connected_table && (0, _inferno.createComponentVNode)(2, _components.Box, {
                    children: "CMF manipulion table is not connected"
                  }), !connected_patient && (0, _inferno.createComponentVNode)(2, _components.Box, {
                    children: "No patient detected"
                  }), connected_table && connected_patient && (0, _inferno.createFragment)([(0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
                    "label": "IQ",
                    children: IQ
                  }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
                    "label": "MDI",
                    children: MDI
                  })], 4)]
                })
              })
            }), (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
              "width": "60%",
              children: (0, _inferno.createComponentVNode)(2, _components.Section, {
                "title": "Cartridge information",
                children: [!inserted_cartridge && (0, _inferno.createComponentVNode)(2, _components.Box, {
                  children: "No cartridge inserted"
                }), inserted_cartridge === 1 && (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
                  children: [(0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
                    "label": "Installed cartridge",
                    children: [cartridge_name, !cartridge_unpacked && (0, _inferno.createComponentVNode)(2, _components.Box, {
                      "as": "span",
                      "m": 5,
                      children: (0, _inferno.createComponentVNode)(2, _components.Button, {
                        "icon": "eject",
                        "content": "Eject cartridge",
                        "onClick": function () {
                          function onClick() {
                            act('eject');
                          }

                          return onClick;
                        }(),
                        "style": {
                          marginLeft: 20
                        }
                      })
                    })]
                  }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
                    "label": "Compatible species",
                    children: compatible_species.join(', ')
                  }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
                    "label": "Available USP",
                    children: [cartridge_unpacked === 0 && cartridge_points, cartridge_unpacked === 1 && free_points]
                  })]
                })]
              })
            })]
          })
        }), cartridge_unpacked === 0 && inserted_cartridge === 1 && (0, _inferno.createComponentVNode)(2, _components.Box, {
          "textAlign": "center",
          children: (0, _inferno.createComponentVNode)(2, _components.Button, {
            "onClick": function () {
              function onClick() {
                act('unpack');
              }

              return onClick;
            }(),
            "fluid": true,
            "color": "danger",
            "tooltip": "This action will destroy the cartridge and begin the CMF manipulation procedure.",
            children: "Unpack cartridge"
          })
        }), cartridge_unpacked === 1 && inserted_cartridge === 1 && (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
          children: (0, _inferno.createComponentVNode)(2, _components.Section, {
            "title": "CMF manipulation",
            children: [Object.keys(skill_list).map(function (skill) {
              return (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
                "label": skill,
                children: (0, _inferno.createComponentVNode)(2, _components.Flex, {
                  "inline": true,
                  "width": "100%",
                  children: (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
                    "grow": 1,
                    "mx": 1,
                    children: (0, _inferno.createComponentVNode)(2, _components.Slider, {
                      "onChange": function () {
                        function onChange(_e, value) {
                          skill_list[skill] = value;
                          act('change_skill', skill_list);
                        }

                        return onChange;
                      }(),
                      "step": 1,
                      "value": skill_list[skill],
                      "maxValue": skill_max_value,
                      "stepPixelSize": 50,
                      "minValue": skill_min_value
                    })
                  })
                })
              }, skill);
            }), (0, _inferno.createComponentVNode)(2, _components.Box, {
              "textAlign": "center",
              children: [(0, _inferno.createComponentVNode)(2, _components.Button, {
                "onClick": function () {
                  function onClick() {
                    act('inject');
                  }

                  return onClick;
                }(),
                "fluid": true,
                "disabled": can_inject === 0,
                "color": "green",
                children: "Inject implant"
              }), (0, _inferno.createComponentVNode)(2, _components.Button.Confirm, {
                "onClick": function () {
                  function onClick() {
                    act('abort');
                  }

                  return onClick;
                }(),
                "fluid": true,
                "color": "danger",
                "confirmContent": "Confirm ",
                children: "Abort"
              })]
            })]
          })
        })]
      })
    })
  });
};

exports.SkillsConsole = SkillsConsole;

/***/ }),

/***/ 68952:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Smes = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _format = __webpack_require__(1033);

var _layouts = __webpack_require__(2381);

// Common power multiplier
var POWER_MUL = 1e3;

var Smes = function Smes(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var capacityPercent = data.capacityPercent,
      charge = data.charge,
      inputAttempt = data.inputAttempt,
      inputting = data.inputting,
      inputLevel = data.inputLevel,
      inputLevelMax = data.inputLevelMax,
      inputAvailable = data.inputAvailable,
      outputAttempt = data.outputAttempt,
      outputting = data.outputting,
      outputLevel = data.outputLevel,
      outputLevelMax = data.outputLevelMax,
      outputUsed = data.outputUsed;
  var inputState = capacityPercent >= 100 && 'good' || inputting && 'average' || 'bad';
  var outputState = outputting && 'good' || charge > 0 && 'average' || 'bad';
  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "width": 340,
    "height": 350,
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      children: [(0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": "Stored Energy",
        children: (0, _inferno.createComponentVNode)(2, _components.ProgressBar, {
          "fractionDigits": 1,
          "value": capacityPercent * 0.01,
          "ranges": {
            good: [0.5, Infinity],
            average: [0.15, 0.5],
            bad: [-Infinity, 0.15]
          }
        })
      }), (0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": "Input",
        children: (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
          children: [(0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Charge Mode",
            "buttons": (0, _inferno.createComponentVNode)(2, _components.Button, {
              "icon": inputAttempt ? 'sync-alt' : 'times',
              "selected": inputAttempt,
              "onClick": function () {
                function onClick() {
                  return act('tryinput');
                }

                return onClick;
              }(),
              children: inputAttempt ? 'Auto' : 'Off'
            }),
            children: (0, _inferno.createComponentVNode)(2, _components.Box, {
              "color": inputState,
              children: capacityPercent >= 100 && 'Fully Charged' || inputting && 'Charging' || 'Not Charging'
            })
          }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Target Input",
            children: (0, _inferno.createComponentVNode)(2, _components.Flex, {
              "inline": true,
              "width": "100%",
              children: [(0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
                children: [(0, _inferno.createComponentVNode)(2, _components.Button, {
                  "icon": "fast-backward",
                  "disabled": inputLevel === 0,
                  "onClick": function () {
                    function onClick() {
                      return act('input', {
                        target: 'min'
                      });
                    }

                    return onClick;
                  }()
                }), (0, _inferno.createComponentVNode)(2, _components.Button, {
                  "icon": "backward",
                  "disabled": inputLevel === 0,
                  "onClick": function () {
                    function onClick() {
                      return act('input', {
                        adjust: -10000
                      });
                    }

                    return onClick;
                  }()
                })]
              }), (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
                "grow": 1,
                "mx": 1,
                children: (0, _inferno.createComponentVNode)(2, _components.Slider, {
                  "value": inputLevel / POWER_MUL,
                  "fillValue": inputAvailable / POWER_MUL,
                  "minValue": 0,
                  "maxValue": inputLevelMax / POWER_MUL,
                  "step": 5,
                  "stepPixelSize": 4,
                  "format": function () {
                    function format(value) {
                      return (0, _format.formatPower)(value * POWER_MUL, 1);
                    }

                    return format;
                  }(),
                  "onDrag": function () {
                    function onDrag(e, value) {
                      return act('input', {
                        target: value * POWER_MUL
                      });
                    }

                    return onDrag;
                  }()
                })
              }), (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
                children: [(0, _inferno.createComponentVNode)(2, _components.Button, {
                  "icon": "forward",
                  "disabled": inputLevel === inputLevelMax,
                  "onClick": function () {
                    function onClick() {
                      return act('input', {
                        adjust: 10000
                      });
                    }

                    return onClick;
                  }()
                }), (0, _inferno.createComponentVNode)(2, _components.Button, {
                  "icon": "fast-forward",
                  "disabled": inputLevel === inputLevelMax,
                  "onClick": function () {
                    function onClick() {
                      return act('input', {
                        target: 'max'
                      });
                    }

                    return onClick;
                  }()
                })]
              })]
            })
          }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Available",
            children: (0, _format.formatPower)(inputAvailable)
          })]
        })
      }), (0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": "Output",
        children: (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
          children: [(0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Output Mode",
            "buttons": (0, _inferno.createComponentVNode)(2, _components.Button, {
              "icon": outputAttempt ? 'power-off' : 'times',
              "selected": outputAttempt,
              "onClick": function () {
                function onClick() {
                  return act('tryoutput');
                }

                return onClick;
              }(),
              children: outputAttempt ? 'On' : 'Off'
            }),
            children: (0, _inferno.createComponentVNode)(2, _components.Box, {
              "color": outputState,
              children: outputting ? 'Sending' : charge > 0 ? 'Not Sending' : 'No Charge'
            })
          }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Target Output",
            children: (0, _inferno.createComponentVNode)(2, _components.Flex, {
              "inline": true,
              "width": "100%",
              children: [(0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
                children: [(0, _inferno.createComponentVNode)(2, _components.Button, {
                  "icon": "fast-backward",
                  "disabled": outputLevel === 0,
                  "onClick": function () {
                    function onClick() {
                      return act('output', {
                        target: 'min'
                      });
                    }

                    return onClick;
                  }()
                }), (0, _inferno.createComponentVNode)(2, _components.Button, {
                  "icon": "backward",
                  "disabled": outputLevel === 0,
                  "onClick": function () {
                    function onClick() {
                      return act('output', {
                        adjust: -10000
                      });
                    }

                    return onClick;
                  }()
                })]
              }), (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
                "grow": 1,
                "mx": 1,
                children: (0, _inferno.createComponentVNode)(2, _components.Slider, {
                  "value": outputLevel / POWER_MUL,
                  "minValue": 0,
                  "maxValue": outputLevelMax / POWER_MUL,
                  "step": 5,
                  "stepPixelSize": 4,
                  "format": function () {
                    function format(value) {
                      return (0, _format.formatPower)(value * POWER_MUL, 1);
                    }

                    return format;
                  }(),
                  "onDrag": function () {
                    function onDrag(e, value) {
                      return act('output', {
                        target: value * POWER_MUL
                      });
                    }

                    return onDrag;
                  }()
                })
              }), (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
                children: [(0, _inferno.createComponentVNode)(2, _components.Button, {
                  "icon": "forward",
                  "disabled": outputLevel === outputLevelMax,
                  "onClick": function () {
                    function onClick() {
                      return act('output', {
                        adjust: 10000
                      });
                    }

                    return onClick;
                  }()
                }), (0, _inferno.createComponentVNode)(2, _components.Button, {
                  "icon": "fast-forward",
                  "disabled": outputLevel === outputLevelMax,
                  "onClick": function () {
                    function onClick() {
                      return act('output', {
                        target: 'max'
                      });
                    }

                    return onClick;
                  }()
                })]
              })]
            })
          }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Outputting",
            children: (0, _format.formatPower)(outputUsed)
          })]
        })
      })]
    })
  });
};

exports.Smes = Smes;

/***/ }),

/***/ 56747:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.SpawnersMenu = void 0;

var _inferno = __webpack_require__(96323);

var _string = __webpack_require__(21118);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var _format = __webpack_require__(1033);

var SpawnersMenu = function SpawnersMenu(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var spawners = data.spawners;
  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "title": "Меню специальных ролей",
    "width": 700,
    "height": 525,
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      "scrollable": true,
      children: (0, _inferno.createComponentVNode)(2, _components.Flex, {
        "direction": "column",
        children: [!spawners.length && (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
          "fontSize": "14px",
          "textAlign": "center",
          children: (0, _inferno.createComponentVNode)(2, _components.Section, {
            children: "\u0420\u043E\u043B\u0435\u0439 \u043D\u0435\u0442, \u043D\u043E \u043D\u0435 \u043F\u0435\u0447\u0430\u043B\u044C\u0441\u044F, \u043E\u043D\u0438 \u0441\u043A\u043E\u0440\u043E \u0431\u0443\u0434\u0443\u0442!"
          })
        }), spawners.map(function (spawner) {
          return (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
            children: [(0, _inferno.createComponentVNode)(2, _components.Section, {
              "title": (0, _string.toTitleCase)(spawner.name),
              "buttons": (0, _inferno.createFragment)([(0, _inferno.createComponentVNode)(2, _components.Button, {
                "content": "Осмотреться",
                "onClick": function () {
                  function onClick() {
                    return act('jump', {
                      ref: spawner.ref
                    });
                  }

                  return onClick;
                }()
              }), (0, _inferno.createComponentVNode)(2, _components.Button, {
                "content": !spawner.register_only ? "Появиться" : !spawner.checked ? "Заявить" : "Отменить",
                "selected": spawner.checked,
                "blocked": spawner.blocked,
                "onClick": function () {
                  function onClick() {
                    return act('spawn', {
                      ref: spawner.ref
                    });
                  }

                  return onClick;
                }()
              })], 4),
              children: (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
                children: [spawner.time_left && (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
                  "label": spawner.time_type === 1 ? "Регистрация" : "Доступно",
                  "color": spawner.time_type === 1 ? "green" : "red",
                  children: (0, _format.formatTime)(spawner.time_left)
                }), !!spawner.register_only && (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
                  "color": "green",
                  "label": "Кандидатов",
                  children: spawner.registered_candidates
                }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
                  "color": "green",
                  "label": "Позиций свободно",
                  children: spawner.positions
                }), spawner.playing > 0 && (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
                  "color": "green",
                  "label": "Играет",
                  children: spawner.playing
                }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
                  "label": "Описание",
                  children: spawner.short_desc
                }), spawner.important_warning && (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
                  "label": "Важная информация",
                  children: spawner.important_warning
                }), spawner.wiki_ref && (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
                  "label": "Вики",
                  children: (0, _inferno.createVNode)(1, "a", null, spawner.wiki_ref, 0, {
                    "href": "{spawner.wiki_ref}",
                    "target": "_blank"
                  })
                })]
              })
            }), (0, _inferno.createVNode)(1, "br")]
          }, spawner.ref);
        })]
      })
    })
  });
};

exports.SpawnersMenu = SpawnersMenu;

/***/ }),

/***/ 47466:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Stack = void 0;

var _inferno = __webpack_require__(96323);

var _math = __webpack_require__(97622);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var _logging = __webpack_require__(79926);

var Stack = function Stack(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var amount = data.amount,
      recipes = data.recipes;
  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "width": 400,
    "height": 720,
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      "scrollable": true,
      children: (0, _inferno.createComponentVNode)(2, _components.Section, {
        "title": "Amount: " + amount,
        children: (0, _inferno.createComponentVNode)(2, RecipeList, {
          "recipes": recipes
        })
      })
    })
  });
};

exports.Stack = Stack;

var RecipeList = function RecipeList(props, context) {
  var _useBackend2 = (0, _backend.useBackend)(context),
      act = _useBackend2.act,
      data = _useBackend2.data;

  var recipes = props.recipes;
  var sortedKeys = Object.keys(recipes).sort(); // Shunt all categories to the top.
  // We're not using this for now, keeping it here
  // in case someone really hates color coding later.
  // let nonCategories = sortedKeys.filter(item => recipes[item].ref
  // !== undefined);
  // let categories = sortedKeys.filter(item => recipes[item].ref
  // === undefined);
  // categories.unshift("--DIVIDER--");
  // let newSortedKeys = nonCategories.concat(categories);

  return sortedKeys.map(function (title) {
    // if (title === "--DIVIDER--") {
    //   return (
    //     <Box mt={1} mb={1}>
    //       <Divider />
    //     </Box>
    //   );
    // }
    var recipe = recipes[title];

    if (recipe.ref === undefined) {
      return (0, _inferno.createComponentVNode)(2, _components.Collapsible, {
        "ml": 1,
        "mb": -0.7,
        "color": "label",
        "title": title,
        children: (0, _inferno.createComponentVNode)(2, _components.Box, {
          "ml": 1,
          children: (0, _inferno.createComponentVNode)(2, RecipeList, {
            "recipes": recipe
          })
        })
      });
    } else {
      return (0, _inferno.createComponentVNode)(2, Recipe, {
        "title": title,
        "recipe": recipe
      });
    }
  });
};

var buildMultiplier = function buildMultiplier(recipe, amount) {
  if (recipe.req_amount > amount) {
    return 0;
  }

  return Math.floor(amount / recipe.req_amount);
};

var Multipliers = function Multipliers(props, context) {
  var _useBackend3 = (0, _backend.useBackend)(context),
      act = _useBackend3.act,
      data = _useBackend3.data;

  var recipe = props.recipe,
      maxMultiplier = props.maxMultiplier;
  var maxM = Math.min(maxMultiplier, Math.floor(recipe.max_res_amount / recipe.res_amount));
  var multipliers = [5, 10, 25];
  var finalResult = [];

  var _loop = function _loop() {
    var multiplier = _multipliers[_i];

    if (maxM >= multiplier) {
      finalResult.push((0, _inferno.createComponentVNode)(2, _components.Button, {
        "content": multiplier * recipe.res_amount + "x",
        "onClick": function () {
          function onClick() {
            return act("make", {
              ref: recipe.ref,
              multiplier: multiplier
            });
          }

          return onClick;
        }()
      }));
    }
  };

  for (var _i = 0, _multipliers = multipliers; _i < _multipliers.length; _i++) {
    _loop();
  }

  if (multipliers.indexOf(maxM) === -1) {
    finalResult.push((0, _inferno.createComponentVNode)(2, _components.Button, {
      "content": maxM * recipe.res_amount + "x",
      "onClick": function () {
        function onClick() {
          return act("make", {
            ref: recipe.ref,
            multiplier: maxM
          });
        }

        return onClick;
      }()
    }));
  }

  return finalResult;
};

var Recipe = function Recipe(props, context) {
  var _useBackend4 = (0, _backend.useBackend)(context),
      act = _useBackend4.act,
      data = _useBackend4.data;

  var amount = data.amount;
  var recipe = props.recipe,
      title = props.title;
  var res_amount = recipe.res_amount,
      max_res_amount = recipe.max_res_amount,
      req_amount = recipe.req_amount,
      ref = recipe.ref;
  var buttonName = title;
  buttonName += " (";
  buttonName += req_amount + " ";
  buttonName += "sheet" + (req_amount > 1 ? "s" : "");
  buttonName += ")";

  if (res_amount > 1) {
    buttonName = res_amount + "x " + buttonName;
  }

  var maxMultiplier = buildMultiplier(recipe, amount);
  return (0, _inferno.createComponentVNode)(2, _components.Box, {
    children: (0, _inferno.createComponentVNode)(2, _components.Table, {
      children: (0, _inferno.createComponentVNode)(2, _components.Table.Row, {
        children: [(0, _inferno.createComponentVNode)(2, _components.Table.Cell, {
          children: (0, _inferno.createComponentVNode)(2, _components.Button, {
            "fluid": true,
            "disabled": !maxMultiplier,
            "icon": "wrench",
            "content": buttonName,
            "onClick": function () {
              function onClick() {
                return act("make", {
                  ref: recipe.ref,
                  multiplier: 1
                });
              }

              return onClick;
            }()
          })
        }), max_res_amount > 1 && maxMultiplier > 1 && (0, _inferno.createComponentVNode)(2, _components.Table.Cell, {
          "collapsing": true,
          children: (0, _inferno.createComponentVNode)(2, Multipliers, {
            "recipe": recipe,
            "maxMultiplier": maxMultiplier
          })
        })]
      })
    })
  });
};

/***/ }),

/***/ 53759:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Tank = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var Tank = function Tank(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "width": 400,
    "height": 120,
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      children: (0, _inferno.createComponentVNode)(2, _components.Section, {
        children: (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
          children: [(0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Pressure",
            children: (0, _inferno.createComponentVNode)(2, _components.ProgressBar, {
              "value": data.tankPressure / 1013,
              "ranges": {
                good: [0.35, Infinity],
                average: [0.15, 0.35],
                bad: [-Infinity, 0.15]
              },
              children: data.tankPressure + ' kPa'
            })
          }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
            "label": "Pressure Regulator",
            children: [(0, _inferno.createComponentVNode)(2, _components.Button, {
              "icon": "fast-backward",
              "disabled": data.ReleasePressure === data.minReleasePressure,
              "onClick": function () {
                function onClick() {
                  return act('pressure', {
                    pressure: 'min'
                  });
                }

                return onClick;
              }()
            }), (0, _inferno.createComponentVNode)(2, _components.NumberInput, {
              "animated": true,
              "value": parseFloat(data.releasePressure),
              "width": "65px",
              "unit": "kPa",
              "minValue": data.minReleasePressure,
              "maxValue": data.maxReleasePressure,
              "onChange": function () {
                function onChange(e, value) {
                  return act('pressure', {
                    pressure: value
                  });
                }

                return onChange;
              }()
            }), (0, _inferno.createComponentVNode)(2, _components.Button, {
              "icon": "fast-forward",
              "disabled": data.ReleasePressure === data.maxReleasePressure,
              "onClick": function () {
                function onClick() {
                  return act('pressure', {
                    pressure: 'max'
                  });
                }

                return onClick;
              }()
            }), (0, _inferno.createComponentVNode)(2, _components.Button, {
              "icon": "undo",
              "content": "",
              "disabled": data.ReleasePressure === data.defaultReleasePressure,
              "onClick": function () {
                function onClick() {
                  return act('pressure', {
                    pressure: 'reset'
                  });
                }

                return onClick;
              }()
            }), (0, _inferno.createComponentVNode)(2, _components.Button, {
              "icon": data.connected ? "toggle-on" : "toggle-off",
              "content": "",
              "selected": data.connected ? "selected" : null,
              "onClick": function () {
                function onClick() {
                  return act('internal');
                }

                return onClick;
              }()
            })]
          })]
        })
      })
    })
  });
};

exports.Tank = Tank;

/***/ }),

/***/ 83634:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Vote = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _sanitize = __webpack_require__(81890);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var Vote = function Vote(_, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      data = _useBackend.data;

  var isAdmin = data.isAdmin,
      currentPoll = data.currentPoll,
      polls = data.polls;
  var height = Math.min(730, 90 + (!currentPoll || isAdmin ? 45 + 26 * polls.filter(function (poll) {
    return !poll.adminOnly || !!isAdmin;
  }).length : 0) + (currentPoll ? 135 + 22 * currentPoll.choices.length : 23));
  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "width": 450,
    "height": height,
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      children: [(0, _inferno.createComponentVNode)(2, VoteInfoModal), (0, _inferno.createComponentVNode)(2, _components.Stack, {
        "fill": true,
        "vertical": true,
        children: [(0, _inferno.createComponentVNode)(2, Choices), (!currentPoll || !!isAdmin) && (0, _inferno.createComponentVNode)(2, ListPolls), !!currentPoll && (0, _inferno.createComponentVNode)(2, Timer)]
      })]
    })
  });
};

exports.Vote = Vote;

var VoteInfoModal = function VoteInfoModal(_, context) {
  var _useBackend2 = (0, _backend.useBackend)(context),
      data = _useBackend2.data;

  var currentPoll = data.currentPoll;

  var _useLocalState = (0, _backend.useLocalState)(context, 'infoModalOpen', false),
      infoModalOpen = _useLocalState[0],
      setinfoModalOpen = _useLocalState[1];

  if (!infoModalOpen) return null;

  if (!currentPoll) {
    setinfoModalOpen(false);
    return null;
  }

  return (0, _inferno.createComponentVNode)(2, _components.Modal, {
    children: ["\u0412\u044B \u043C\u043E\u0436\u0435\u0442\u0435 \u043F\u0440\u043E\u0433\u043E\u043B\u043E\u0441\u043E\u0432\u0430\u0442\u044C", ' ', (0, _inferno.createComponentVNode)(2, _components.Box, {
      "inline": true,
      "bold": true,
      children: currentPoll.canVoteMultiple ? 'за несколько вариантов' : 'только за один вариант'
    }), (0, _inferno.createVNode)(1, "br"), "\u0412\u044B", ' ', (0, _inferno.createComponentVNode)(2, _components.Box, {
      "inline": true,
      "bold": true,
      children: currentPoll.canRevote ? 'можете изменить' : 'не можете изменить'
    }), ' ', "\u0441\u0432\u043E\u0439 \u0433\u043E\u043B\u043E\u0441", (0, _inferno.createVNode)(1, "br"), "\u0412\u044B", ' ', (0, _inferno.createComponentVNode)(2, _components.Box, {
      "inline": true,
      "bold": true,
      children: currentPoll.canUnvote ? 'можете отменить' : 'не можете отменить'
    }), ' ', "\u0441\u0432\u043E\u0439 \u0433\u043E\u043B\u043E\u0441", (0, _inferno.createVNode)(1, "br"), currentPoll.minimumWinPercentage ? (0, _inferno.createFragment)([(0, _inferno.createTextVNode)("\u041D\u0435\u043E\u0431\u0445\u043E\u0434\u0438\u043C\u043E \u043D\u0430\u0431\u0440\u0430\u0442\u044C \u043C\u0438\u043D\u0438\u043C\u0443\u043C"), (0, _inferno.createTextVNode)(' '), (0, _inferno.createComponentVNode)(2, _components.Box, {
      "inline": true,
      "bold": true,
      children: [currentPoll.minimumWinPercentage * 100, "%"]
    }), (0, _inferno.createTextVNode)(", \u0447\u0442\u043E\u0431\u044B \u0432\u0430\u0440\u0438\u0430\u043D\u0442 \u043F\u043E\u0431\u0435\u0434\u0438\u043B")], 0) : '', currentPoll.description && (0, _inferno.createFragment)([(0, _inferno.createVNode)(1, "hr"), (0, _inferno.createComponentVNode)(2, _components.Box, {
      "dangerouslySetInnerHTML": {
        __html: (0, _sanitize.sanitizeText)(currentPoll.description)
      }
    })], 4), (0, _inferno.createVNode)(1, "hr"), (0, _inferno.createComponentVNode)(2, _components.Button, {
      "fluid": true,
      "align": "center",
      "onClick": function () {
        function onClick() {
          return setinfoModalOpen(false);
        }

        return onClick;
      }(),
      children: "\u0417\u0430\u043A\u0440\u044B\u0442\u044C"
    })]
  });
};

var Choices = function Choices(_, context) {
  var _useBackend3 = (0, _backend.useBackend)(context),
      act = _useBackend3.act,
      data = _useBackend3.data;

  var currentPoll = data.currentPoll;

  var _useLocalState2 = (0, _backend.useLocalState)(context, 'infoModalOpen', false),
      infoModalOpen = _useLocalState2[0],
      setInfoModalOpen = _useLocalState2[1];

  var anyChoiceMade = currentPoll == null ? void 0 : currentPoll.choices.some(function (choice) {
    return choice.selected;
  });
  return (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
    "grow": true,
    children: (0, _inferno.createComponentVNode)(2, _components.Section, {
      "fill": true,
      "scrollable": !!currentPoll && currentPoll.choices.length !== 0,
      "title": currentPoll ? "\u0413\u043E\u043B\u043E\u0441\u043E\u0432\u0430\u043D\u0438\u0435: " + currentPoll.poll.name : 'Голосование',
      "buttons": currentPoll ? (0, _inferno.createComponentVNode)(2, _components.Button, {
        "icon": "info",
        "color": "transparent",
        "disabled": !currentPoll,
        "onClick": function () {
          function onClick() {
            return setInfoModalOpen(true);
          }

          return onClick;
        }()
      }) : undefined,
      children: !!currentPoll && currentPoll.choices.length !== 0 ? (0, _inferno.createFragment)([!currentPoll.showWarning ? '' : (0, _inferno.createComponentVNode)(2, _components.NoticeBox, {
        children: currentPoll.poll.message
      }), !!currentPoll.question && (0, _inferno.createComponentVNode)(2, _components.Box, {
        "italic": true,
        children: currentPoll.question
      }), (0, _inferno.createComponentVNode)(2, _components.Divider), (0, _inferno.createComponentVNode)(2, _components.Stack, {
        "vertical": true,
        children: [(0, _inferno.createComponentVNode)(2, _components.Stack, {
          "fill": true,
          "justify": "space-around",
          children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
            "bold": true,
            children: "\u0412\u0430\u0440\u0438\u0430\u043D\u0442\u044B"
          }), (0, _inferno.createComponentVNode)(2, _components.Box, {
            "bold": true,
            children: "\u0413\u043E\u043B\u043E\u0441\u0430"
          })]
        }), (0, _inferno.createVNode)(1, "br"), currentPoll.choices.map(function (choice) {
          return (0, _inferno.createComponentVNode)(2, _components.Stack, {
            "justify": "space-between",
            children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
              "height": "22px",
              children: [(0, _inferno.createComponentVNode)(2, _components.Button, {
                "maxWidth": "260px",
                "ellipsis": true,
                "disabled": !currentPoll.canRevote && anyChoiceMade,
                "selected": choice.selected,
                "onClick": function () {
                  function onClick() {
                    return act('putVote', {
                      choiceRef: choice.ref
                    });
                  }

                  return onClick;
                }(),
                children: choice.name.replace(/^\w/, function (c) {
                  return c.toUpperCase();
                })
              }), !!choice.selected && (0, _inferno.createComponentVNode)(2, _components.Icon, {
                "name": "vote-yea",
                "color": "green",
                "ml": 1,
                "verticalAlign": "super"
              })]
            }), (0, _inferno.createComponentVNode)(2, _components.Box, {
              "mr": 15,
              children: choice.votes
            })]
          }, choice.ref);
        })]
      })], 0) : (0, _inferno.createComponentVNode)(2, _components.NoticeBox, {
        "info": true,
        "mb": "0",
        children: !currentPoll ? 'Нет активного голосования!' : 'Нет доступных вариантов!'
      })
    })
  });
};

var ListPolls = function ListPolls(_, context) {
  var _useBackend4 = (0, _backend.useBackend)(context),
      act = _useBackend4.act,
      data = _useBackend4.data;

  var isAdmin = data.isAdmin,
      polls = data.polls;
  return (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
    children: (0, _inferno.createComponentVNode)(2, _components.Section, {
      "title": "Начать голосование",
      children: (0, _inferno.createComponentVNode)(2, _components.Stack, {
        "vertical": true,
        "justify": "space-between",
        children: polls ? polls.map(function (poll) {
          return (!poll.adminOnly || !!isAdmin) && (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
            children: (0, _inferno.createComponentVNode)(2, _components.Stack, {
              "horizontal": true,
              children: [!!isAdmin && (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
                children: (0, _inferno.createComponentVNode)(2, _components.Button, {
                  "width": 9.5,
                  "textAlign": "center",
                  "onClick": function () {
                    function onClick() {
                      return act('toggleAdminOnly', {
                        pollRef: poll.type
                      });
                    }

                    return onClick;
                  }(),
                  children: poll.adminOnly ? 'Только админы' : 'Разрешено всем'
                })
              }), (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
                children: (0, _inferno.createComponentVNode)(2, _components.Button, {
                  "disabled": !poll.canStart && !isAdmin || poll.forceBlocked,
                  "color": !isAdmin ? undefined : !poll.canStart ? 'red' : undefined,
                  "tooltip": poll.message,
                  "content": poll.name,
                  "onClick": function () {
                    function onClick() {
                      return act('callVote', {
                        pollRef: poll.type
                      });
                    }

                    return onClick;
                  }()
                })
              })]
            })
          }, poll.name);
        }) : (0, _inferno.createComponentVNode)(2, _components.NoticeBox, {
          "info": true,
          children: "\u041D\u0435\u0442 \u0434\u043E\u0441\u0442\u0443\u043F\u043D\u044B\u0445 \u0433\u043E\u043B\u043E\u0441\u043E\u0432\u0430\u043D\u0438\u0439!"
        })
      })
    })
  });
};

var Timer = function Timer(_, context) {
  var _useBackend5 = (0, _backend.useBackend)(context),
      act = _useBackend5.act,
      data = _useBackend5.data;

  var currentPoll = data.currentPoll,
      isAdmin = data.isAdmin;
  return (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
    children: (0, _inferno.createComponentVNode)(2, _components.Section, {
      children: (0, _inferno.createComponentVNode)(2, _components.Stack, {
        "justify": "space-between",
        children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
          "fontSize": 1.5,
          children: ["\u041E\u0441\u0442\u0430\u043B\u043E\u0441\u044C \u0432\u0440\u0435\u043C\u0435\u043D\u0438: ", (currentPoll == null ? void 0 : currentPoll.timeRemaining) || 0, "\u0441"]
        }), !!isAdmin && !!currentPoll && (0, _inferno.createComponentVNode)(2, _components.Button, {
          "color": "red",
          "disabled": !isAdmin,
          "onClick": function () {
            function onClick() {
              return act('cancelVote');
            }

            return onClick;
          }(),
          children: "\u041E\u0442\u043C\u0435\u043D\u0438\u0442\u044C \u0433\u043E\u043B\u043E\u0441\u043E\u0432\u0430\u043D\u0438\u0435"
        })]
      })
    })
  });
};

/***/ }),

/***/ 35045:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Wires = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _layouts = __webpack_require__(2381);

var Wires = function Wires(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var wires = data.wires || [];
  var statuses = data.status || [];
  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    "width": 350,
    "height": 45 + wires.length * 25 + (statuses.length > 0 ? 35 : 0) + statuses.length * 12,
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      children: [(0, _inferno.createComponentVNode)(2, _components.Section, {
        children: (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
          children: wires.map(function (wire) {
            return (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
              "className": "candystripe",
              "label": wire.label ? wire.label : "Провода",
              "labelColor": wire.color,
              "color": wire.color,
              "buttons": (0, _inferno.createFragment)([(0, _inferno.createComponentVNode)(2, _components.Button, {
                "content": wire.cut ? 'Соединить' : 'Перерезать',
                "onClick": function () {
                  function onClick() {
                    return act('cut', {
                      wire: wire.wire
                    });
                  }

                  return onClick;
                }()
              }), (0, _inferno.createComponentVNode)(2, _components.Button, {
                "content": "Пульс",
                "onClick": function () {
                  function onClick() {
                    return act('pulse', {
                      wire: wire.wire
                    });
                  }

                  return onClick;
                }()
              }), (0, _inferno.createComponentVNode)(2, _components.Button, {
                "content": wire.attached ? 'Отсоединить' : 'Присоединить',
                "onClick": function () {
                  function onClick() {
                    return act('attach', {
                      wire: wire.wire
                    });
                  }

                  return onClick;
                }()
              })], 4)
            }, wire.color);
          })
        })
      }), !!statuses.length && (0, _inferno.createComponentVNode)(2, _components.Section, {
        children: statuses.map(function (status) {
          return typeof status === "string" ? (0, _inferno.createComponentVNode)(2, _components.Box, {
            children: status
          }, status) : (0, _inferno.createComponentVNode)(2, _components.Button, {
            "content": status.label,
            "onClick": function () {
              function onClick() {
                return act(status.act, status.act_params ? status.act_params : undefined);
              }

              return onClick;
            }()
          });
        })
      })]
    })
  });
};

exports.Wires = Wires;

/***/ }),

/***/ 52126:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.AccessList = void 0;

var _inferno = __webpack_require__(96323);

var _collections = __webpack_require__(1397);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

function _createForOfIteratorHelperLoose(o, allowArrayLike) { var it = typeof Symbol !== "undefined" && o[Symbol.iterator] || o["@@iterator"]; if (it) return (it = it.call(o)).next.bind(it); if (Array.isArray(o) || (it = _unsupportedIterableToArray(o)) || allowArrayLike && o && typeof o.length === "number") { if (it) o = it; var i = 0; return function () { if (i >= o.length) return { done: true }; return { done: false, value: o[i++] }; }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }

function _unsupportedIterableToArray(o, minLen) { if (!o) return; if (typeof o === "string") return _arrayLikeToArray(o, minLen); var n = Object.prototype.toString.call(o).slice(8, -1); if (n === "Object" && o.constructor) n = o.constructor.name; if (n === "Map" || n === "Set") return Array.from(o); if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen); }

function _arrayLikeToArray(arr, len) { if (len == null || len > arr.length) len = arr.length; for (var i = 0, arr2 = new Array(len); i < len; i++) { arr2[i] = arr[i]; } return arr2; }

var diffMap = {
  0: {
    icon: 'times-circle',
    color: 'bad'
  },
  1: {
    icon: 'stop-circle',
    color: null
  },
  2: {
    icon: 'check-circle',
    color: 'good'
  }
};

var AccessList = function AccessList(props, context) {
  var _accesses$;

  var _props$sectionButtons = props.sectionButtons,
      sectionButtons = _props$sectionButtons === void 0 ? null : _props$sectionButtons,
      _props$sectionFlexGro = props.sectionFlexGrow,
      sectionFlexGrow = _props$sectionFlexGro === void 0 ? null : _props$sectionFlexGro,
      usedByRcd = props.usedByRcd,
      rcdButtons = props.rcdButtons,
      _props$accesses = props.accesses,
      accesses = _props$accesses === void 0 ? [] : _props$accesses,
      _props$selectedList = props.selectedList,
      selectedList = _props$selectedList === void 0 ? [] : _props$selectedList,
      _props$grantableList = props.grantableList,
      grantableList = _props$grantableList === void 0 ? [] : _props$grantableList,
      accessMod = props.accessMod,
      grantAll = props.grantAll,
      denyAll = props.denyAll,
      grantDep = props.grantDep,
      denyDep = props.denyDep;

  var _useLocalState = (0, _backend.useLocalState)(context, 'accessName', (_accesses$ = accesses[0]) == null ? void 0 : _accesses$.name),
      selectedAccessName = _useLocalState[0],
      setSelectedAccessName = _useLocalState[1];

  var selectedAccess = accesses.find(function (access) {
    return access.name === selectedAccessName;
  });
  var selectedAccessEntries = (0, _collections.sortBy)(function (entry) {
    return entry.desc;
  })((selectedAccess == null ? void 0 : selectedAccess.accesses) || []);

  var checkAccessIcon = function checkAccessIcon(accesses) {
    var oneAccess = false;
    var oneInaccess = false;

    for (var _iterator = _createForOfIteratorHelperLoose(accesses), _step; !(_step = _iterator()).done;) {
      var element = _step.value;

      if (selectedList.includes(element.ref)) {
        oneAccess = true;
      } else {
        oneInaccess = true;
      }
    }

    if (!oneAccess && oneInaccess) {
      return 0;
    } else if (oneAccess && oneInaccess) {
      return 1;
    } else {
      return 2;
    }
  };

  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    "title": "Access",
    "flexGrow": sectionFlexGrow,
    "buttons": (0, _inferno.createFragment)([(0, _inferno.createComponentVNode)(2, _components.Button, {
      "icon": "check-double",
      "content": "Select All",
      "color": "good",
      "onClick": function () {
        function onClick() {
          return grantAll();
        }

        return onClick;
      }()
    }), (0, _inferno.createComponentVNode)(2, _components.Button, {
      "icon": "undo",
      "content": "Deselect All",
      "color": "bad",
      "onClick": function () {
        function onClick() {
          return denyAll();
        }

        return onClick;
      }()
    }), sectionButtons], 0),
    children: (0, _inferno.createComponentVNode)(2, _components.Flex, {
      children: [(0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
        children: (0, _inferno.createComponentVNode)(2, _components.Tabs, {
          "vertical": true,
          children: accesses.map(function (access) {
            var entries = access.accesses || [];
            var icon = diffMap[checkAccessIcon(entries)].icon;
            var color = diffMap[checkAccessIcon(entries)].color;
            return (0, _inferno.createComponentVNode)(2, _components.Tabs.Tab, {
              "altSelection": true,
              "color": color,
              "icon": icon,
              "selected": access.name === selectedAccessName,
              "onClick": function () {
                function onClick() {
                  return setSelectedAccessName(access.name);
                }

                return onClick;
              }(),
              children: access.name
            }, access.name);
          })
        })
      }), (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
        "grow": 1,
        "ml": 1.5,
        children: [(0, _inferno.createComponentVNode)(2, _components.Flex, {
          children: [(0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
            "width": "50%",
            "mr": 0,
            children: (0, _inferno.createComponentVNode)(2, _components.Button, {
              "fluid": true,
              "icon": "check",
              "content": "Select Region",
              "color": "good",
              "onClick": function () {
                function onClick() {
                  return grantDep(selectedAccess.regid);
                }

                return onClick;
              }()
            })
          }), (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
            "width": "50%",
            "ml": 0,
            children: (0, _inferno.createComponentVNode)(2, _components.Button, {
              "fluid": true,
              "icon": "times",
              "content": "Deselect Region",
              "color": "bad",
              "onClick": function () {
                function onClick() {
                  return denyDep(selectedAccess.regid);
                }

                return onClick;
              }()
            })
          })]
        }), !!usedByRcd && (0, _inferno.createComponentVNode)(2, _components.Box, {
          "my": 1.5,
          children: (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
            children: (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
              "label": "Require",
              children: rcdButtons
            })
          })
        }), selectedAccessEntries.map(function (entry) {
          return (0, _inferno.createComponentVNode)(2, _components.Button.Checkbox, {
            "fluid": true,
            "content": entry.desc,
            "disabled": grantableList.length > 0 && !grantableList.includes(entry.ref) && !selectedList.includes(entry.ref),
            "checked": selectedList.includes(entry.ref),
            "onClick": function () {
              function onClick() {
                return accessMod(entry.ref);
              }

              return onClick;
            }()
          }, entry.desc);
        })]
      })]
    })
  });
};

exports.AccessList = AccessList;

/***/ }),

/***/ 19447:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.InterfaceLockNoticeBox = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

/**
 * This component by expects the following fields to be returned
 * from ui_data:
 *
 * - siliconUser: boolean
 * - locked: boolean
 *
 * And expects the following ui_act action to be implemented:
 *
 * - lock - for toggling the lock as a silicon user.
 *
 * All props can be redefined if you want custom behavior, but
 * it's preferred to stick to defaults.
 */
var InterfaceLockNoticeBox = function InterfaceLockNoticeBox(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var _props$siliconUser = props.siliconUser,
      siliconUser = _props$siliconUser === void 0 ? data.siliconUser : _props$siliconUser,
      _props$locked = props.locked,
      locked = _props$locked === void 0 ? data.locked : _props$locked,
      _props$onLockStatusCh = props.onLockStatusChange,
      onLockStatusChange = _props$onLockStatusCh === void 0 ? function () {
    return act('lock');
  } : _props$onLockStatusCh,
      _props$accessText = props.accessText,
      accessText = _props$accessText === void 0 ? 'ID-картой' : _props$accessText; // For silicon users

  if (siliconUser) {
    return (0, _inferno.createComponentVNode)(2, _components.NoticeBox, {
      "color": "grey",
      children: (0, _inferno.createComponentVNode)(2, _components.Flex, {
        "align": "center",
        children: [(0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
          children: "\u0418\u043D\u0442\u0435\u0440\u0444\u0435\u0439\u0441:"
        }), (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
          "grow": 1
        }), (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
          children: (0, _inferno.createComponentVNode)(2, _components.Button, {
            "m": 0,
            "color": locked ? 'red' : 'green',
            "icon": locked ? 'lock' : 'unlock',
            "content": locked ? 'Заблокирован' : 'Разблокирован',
            "onClick": function () {
              function onClick() {
                if (onLockStatusChange) {
                  onLockStatusChange(!locked);
                }
              }

              return onClick;
            }()
          })
        })]
      })
    });
  } // For everyone else


  return (0, _inferno.createComponentVNode)(2, _components.NoticeBox, {
    children: ["\u041F\u0440\u043E\u0432\u0435\u0434\u0438\u0442\u0435 ", accessText, ' ', "\u0434\u043B\u044F ", locked ? 'разблокировки' : 'блокировки', "."]
  });
};

exports.InterfaceLockNoticeBox = InterfaceLockNoticeBox;

/***/ }),

/***/ 11409:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.MiningUser = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var MiningUser = function MiningUser(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var insertIdText = props.insertIdText;
  var has_id = data.has_id,
      id = data.id;
  return (0, _inferno.createComponentVNode)(2, _components.NoticeBox, {
    "success": has_id,
    children: has_id ? (0, _inferno.createFragment)([(0, _inferno.createComponentVNode)(2, _components.Box, {
      "display": "inline-block",
      "verticalAlign": "middle",
      "style": {
        "float": 'left'
      },
      children: ["Logged in as ", id.name, ".", (0, _inferno.createVNode)(1, "br"), "You have ", id.points.toLocaleString('en-US'), " points."]
    }), (0, _inferno.createComponentVNode)(2, _components.Button, {
      "icon": "eject",
      "content": "Eject ID",
      "style": {
        "float": 'right'
      },
      "onClick": function () {
        function onClick() {
          return act('logoff');
        }

        return onClick;
      }()
    }), (0, _inferno.createComponentVNode)(2, _components.Box, {
      "style": {
        clear: "both"
      }
    })], 4) : insertIdText
  });
};

exports.MiningUser = MiningUser;

/***/ }),

/***/ 41161:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.PortableBasicInfo = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var PortableBasicInfo = function PortableBasicInfo(props, context) {
  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var connected = data.connected,
      holding = data.holding,
      on = data.on,
      pressure = data.pressure,
      power_draw = data.power_draw,
      cell_charge = data.cell_charge,
      cell_maxcharge = data.cell_maxcharge;
  var cell_chargepercent = cell_charge / cell_maxcharge;
  return (0, _inferno.createFragment)([(0, _inferno.createComponentVNode)(2, _components.Section, {
    "title": "Status",
    "buttons": (0, _inferno.createComponentVNode)(2, _components.Button, {
      "icon": on ? 'power-off' : 'times',
      "content": on ? 'On' : 'Off',
      "selected": on,
      "onClick": function () {
        function onClick() {
          return act('power');
        }

        return onClick;
      }()
    }),
    children: (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
      children: [(0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Pressure",
        children: [(0, _inferno.createComponentVNode)(2, _components.AnimatedNumber, {
          "value": pressure
        }), ' kPa']
      }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Port",
        "color": connected ? 'good' : 'average',
        children: connected ? 'Connected' : 'Not Connected'
      })]
    })
  }), (0, _inferno.createComponentVNode)(2, _components.Section, {
    "title": "Cell",
    children: (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
      children: [(0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Load",
        children: [(0, _inferno.createComponentVNode)(2, _components.AnimatedNumber, {
          "value": power_draw
        }), ' W']
      }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Charge",
        children: (0, _inferno.createComponentVNode)(2, _components.ProgressBar, {
          "ranges": {
            bad: [-Infinity, .1],
            average: [.1, .6],
            good: [.6, Infinity]
          },
          "value": cell_chargepercent
        })
      })]
    })
  }), (0, _inferno.createComponentVNode)(2, _components.Section, {
    "title": "Holding Tank",
    "minHeight": "82px",
    "buttons": (0, _inferno.createComponentVNode)(2, _components.Button, {
      "icon": "eject",
      "content": "Eject",
      "disabled": !holding,
      "onClick": function () {
        function onClick() {
          return act('eject');
        }

        return onClick;
      }()
    }),
    children: holding ? (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
      children: [(0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Label",
        children: holding.name
      }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Pressure",
        children: [(0, _inferno.createComponentVNode)(2, _components.AnimatedNumber, {
          "value": holding.pressure
        }), ' kPa']
      })]
    }) : (0, _inferno.createComponentVNode)(2, _components.Box, {
      "color": "average",
      children: "No holding tank"
    })
  })], 4);
};

exports.PortableBasicInfo = PortableBasicInfo;

/***/ }),

/***/ 30041:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Layout = void 0;

var _inferno = __webpack_require__(96323);

var _react = __webpack_require__(1060);

var _Box = __webpack_require__(54425);

var _events = __webpack_require__(99397);

var _excluded = ["className", "theme", "children"],
    _excluded2 = ["className", "scrollable", "children"];

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

var Layout = function Layout(props) {
  var className = props.className,
      _props$theme = props.theme,
      theme = _props$theme === void 0 ? 'nanotrasen' : _props$theme,
      children = props.children,
      rest = _objectWithoutPropertiesLoose(props, _excluded);

  return (0, _inferno.createVNode)(1, "div", 'theme-' + theme, (0, _inferno.normalizeProps)((0, _inferno.createVNode)(1, "div", (0, _react.classes)(['Layout', className].concat((0, _Box.computeBoxClassName)(rest))), children, 0, Object.assign({}, (0, _Box.computeBoxProps)(rest)))), 2);
};

exports.Layout = Layout;

var LayoutContent = function LayoutContent(props) {
  var className = props.className,
      scrollable = props.scrollable,
      children = props.children,
      rest = _objectWithoutPropertiesLoose(props, _excluded2);

  return (0, _inferno.normalizeProps)((0, _inferno.createVNode)(1, "div", (0, _react.classes)(['Layout__content', scrollable && 'Layout__content--scrollable', className].concat((0, _Box.computeBoxClassName)(rest))), children, 0, Object.assign({}, (0, _Box.computeBoxProps)(rest))));
};

LayoutContent.defaultHooks = {
  onComponentDidMount: function () {
    function onComponentDidMount(node) {
      return (0, _events.addScrollableNode)(node);
    }

    return onComponentDidMount;
  }(),
  onComponentWillUnmount: function () {
    function onComponentWillUnmount(node) {
      return (0, _events.removeScrollableNode)(node);
    }

    return onComponentWillUnmount;
  }()
};
Layout.Content = LayoutContent;

/***/ }),

/***/ 76738:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.NtosWindow = void 0;

var _inferno = __webpack_require__(96323);

var _assets = __webpack_require__(39646);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _Window = __webpack_require__(34881);

/**
 * @file
 * @copyright 2020 Aleksej Komarov
 * @license MIT
 */
var NtosWindow = function NtosWindow(props, context) {
  var title = props.title,
      _props$width = props.width,
      width = _props$width === void 0 ? 575 : _props$width,
      _props$height = props.height,
      height = _props$height === void 0 ? 700 : _props$height,
      _props$theme = props.theme,
      theme = _props$theme === void 0 ? 'ntos' : _props$theme,
      children = props.children;

  var _useBackend = (0, _backend.useBackend)(context),
      act = _useBackend.act,
      data = _useBackend.data;

  var PC_device_theme = data.PC_device_theme,
      PC_batteryicon = data.PC_batteryicon,
      PC_showbatteryicon = data.PC_showbatteryicon,
      PC_batterypercent = data.PC_batterypercent,
      PC_ntneticon = data.PC_ntneticon,
      PC_apclinkicon = data.PC_apclinkicon,
      PC_stationtime = data.PC_stationtime,
      _data$PC_programheade = data.PC_programheaders,
      PC_programheaders = _data$PC_programheade === void 0 ? [] : _data$PC_programheade,
      PC_showexitprogram = data.PC_showexitprogram;
  return (0, _inferno.createComponentVNode)(2, _Window.Window, {
    "title": title,
    "width": width,
    "height": height,
    "theme": theme,
    children: (0, _inferno.createVNode)(1, "div", "NtosWindow", [(0, _inferno.createVNode)(1, "div", "NtosWindow__header NtosHeader", [(0, _inferno.createVNode)(1, "div", "NtosHeader__left", [(0, _inferno.createComponentVNode)(2, _components.Box, {
      "inline": true,
      "bold": true,
      "mr": 2,
      children: PC_stationtime
    }), (0, _inferno.createComponentVNode)(2, _components.Box, {
      "inline": true,
      "italic": true,
      "mr": 2,
      "opacity": 0.33,
      children: [PC_device_theme === 'ntos' && 'NtOS', PC_device_theme === 'syndicate' && 'Syndix']
    })], 4), (0, _inferno.createVNode)(1, "div", "NtosHeader__right", [PC_programheaders.map(function (header) {
      return (0, _inferno.createComponentVNode)(2, _components.Box, {
        "inline": true,
        "mr": 1,
        children: (0, _inferno.createVNode)(1, "img", "NtosHeader__icon", null, 1, {
          "src": (0, _assets.resolveAsset)(header.icon)
        })
      }, header.icon);
    }), (0, _inferno.createComponentVNode)(2, _components.Box, {
      "inline": true,
      children: PC_ntneticon && (0, _inferno.createVNode)(1, "img", "NtosHeader__icon", null, 1, {
        "src": (0, _assets.resolveAsset)(PC_ntneticon)
      })
    }), !!(PC_showbatteryicon && PC_batteryicon) && (0, _inferno.createComponentVNode)(2, _components.Box, {
      "inline": true,
      "mr": 1,
      children: [(0, _inferno.createVNode)(1, "img", "NtosHeader__icon", null, 1, {
        "src": (0, _assets.resolveAsset)(PC_batteryicon)
      }), PC_batterypercent && PC_batterypercent]
    }), PC_apclinkicon && (0, _inferno.createComponentVNode)(2, _components.Box, {
      "inline": true,
      "mr": 1,
      children: (0, _inferno.createVNode)(1, "img", "NtosHeader__icon", null, 1, {
        "src": (0, _assets.resolveAsset)(PC_apclinkicon)
      })
    }), !!PC_showexitprogram && (0, _inferno.createComponentVNode)(2, _components.Button, {
      "width": "26px",
      "lineHeight": "22px",
      "textAlign": "center",
      "color": "transparent",
      "icon": "window-minimize-o",
      "tooltip": "Minimize",
      "tooltipPosition": "bottom",
      "onClick": function () {
        function onClick() {
          return act('PC_minimize');
        }

        return onClick;
      }()
    }), !!PC_showexitprogram && (0, _inferno.createComponentVNode)(2, _components.Button, {
      "mr": "-3px",
      "width": "26px",
      "lineHeight": "22px",
      "textAlign": "center",
      "color": "transparent",
      "icon": "window-close-o",
      "tooltip": "Close",
      "tooltipPosition": "bottom-start",
      "onClick": function () {
        function onClick() {
          return act('PC_exit');
        }

        return onClick;
      }()
    }), !PC_showexitprogram && (0, _inferno.createComponentVNode)(2, _components.Button, {
      "mr": "-3px",
      "width": "26px",
      "lineHeight": "22px",
      "textAlign": "center",
      "color": "transparent",
      "icon": "power-off",
      "tooltip": "Power off",
      "tooltipPosition": "bottom-start",
      "onClick": function () {
        function onClick() {
          return act('PC_shutdown');
        }

        return onClick;
      }()
    })], 0)], 4), children], 0)
  });
};

exports.NtosWindow = NtosWindow;

var NtosWindowContent = function NtosWindowContent(props) {
  return (0, _inferno.createVNode)(1, "div", "NtosWindow__content", (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, _Window.Window.Content, Object.assign({}, props))), 2);
};

NtosWindow.Content = NtosWindowContent;

/***/ }),

/***/ 52243:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Pane = void 0;

var _inferno = __webpack_require__(96323);

var _react = __webpack_require__(1060);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _debug = __webpack_require__(27157);

var _Layout = __webpack_require__(30041);

var _excluded = ["theme", "children", "className"],
    _excluded2 = ["className", "fitted", "children"];

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

var Pane = function Pane(props, context) {
  var theme = props.theme,
      children = props.children,
      className = props.className,
      rest = _objectWithoutPropertiesLoose(props, _excluded);

  var _useBackend = (0, _backend.useBackend)(context),
      suspended = _useBackend.suspended;

  var _useDebug = (0, _debug.useDebug)(context),
      debugLayout = _useDebug.debugLayout;

  return (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, _Layout.Layout, Object.assign({
    "className": (0, _react.classes)(['Window', className]),
    "theme": theme
  }, rest, {
    children: (0, _inferno.createComponentVNode)(2, _components.Box, {
      "fillPositionedParent": true,
      "className": debugLayout && 'debug-layout',
      children: !suspended && children
    })
  })));
};

exports.Pane = Pane;

var PaneContent = function PaneContent(props) {
  var className = props.className,
      fitted = props.fitted,
      children = props.children,
      rest = _objectWithoutPropertiesLoose(props, _excluded2);

  return (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, _Layout.Layout.Content, Object.assign({
    "className": (0, _react.classes)(['Window__content', className])
  }, rest, {
    children: fitted && children || (0, _inferno.createVNode)(1, "div", "Window__contentPadding", children, 0)
  })));
};

Pane.Content = PaneContent;

/***/ }),

/***/ 34881:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Window = void 0;

var _inferno = __webpack_require__(96323);

var _react = __webpack_require__(1060);

var _redux = __webpack_require__(465);

var _string = __webpack_require__(21118);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _constants = __webpack_require__(84200);

var _debug = __webpack_require__(27157);

var _actions = __webpack_require__(98069);

var _drag = __webpack_require__(41410);

var _logging = __webpack_require__(79926);

var _Layout = __webpack_require__(30041);

var _excluded = ["className", "fitted", "children"];

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

function _inheritsLoose(subClass, superClass) { subClass.prototype = Object.create(superClass.prototype); subClass.prototype.constructor = subClass; _setPrototypeOf(subClass, superClass); }

function _setPrototypeOf(o, p) { _setPrototypeOf = Object.setPrototypeOf || function () { function _setPrototypeOf(o, p) { o.__proto__ = p; return o; } return _setPrototypeOf; }(); return _setPrototypeOf(o, p); }

var logger = (0, _logging.createLogger)('Window');
var DEFAULT_SIZE = [400, 600];

var Window = /*#__PURE__*/function (_Component) {
  _inheritsLoose(Window, _Component);

  function Window() {
    return _Component.apply(this, arguments) || this;
  }

  var _proto = Window.prototype;

  _proto.componentDidMount = function () {
    function componentDidMount() {
      var _useBackend = (0, _backend.useBackend)(this.context),
          suspended = _useBackend.suspended;

      var _this$props$canClose = this.props.canClose,
          canClose = _this$props$canClose === void 0 ? true : _this$props$canClose;

      if (suspended) {
        return;
      }

      Byond.winset(window.__windowId__, {
        'can-close': Boolean(canClose)
      });
      logger.log('mounting');
      this.updateGeometry();
    }

    return componentDidMount;
  }();

  _proto.componentDidUpdate = function () {
    function componentDidUpdate(prevProps) {
      var shouldUpdateGeometry = this.props.width !== prevProps.width || this.props.height !== prevProps.height;

      if (shouldUpdateGeometry) {
        this.updateGeometry();
      }
    }

    return componentDidUpdate;
  }();

  _proto.updateGeometry = function () {
    function updateGeometry() {
      var _config$window;

      var _useBackend2 = (0, _backend.useBackend)(this.context),
          config = _useBackend2.config;

      var options = Object.assign({
        size: DEFAULT_SIZE
      }, config.window);

      if (this.props.width && this.props.height) {
        options.size = [this.props.width, this.props.height];
      }

      if ((_config$window = config.window) != null && _config$window.key) {
        (0, _drag.setWindowKey)(config.window.key);
      }

      (0, _drag.recallWindowGeometry)(options);
    }

    return updateGeometry;
  }();

  _proto.render = function () {
    function render() {
      var _config$window2;

      var _this$props = this.props,
          _this$props$canClose2 = _this$props.canClose,
          canClose = _this$props$canClose2 === void 0 ? true : _this$props$canClose2,
          theme = _this$props.theme,
          title = _this$props.title,
          children = _this$props.children,
          buttons = _this$props.buttons,
          className = _this$props.className,
          titleClassName = _this$props.titleClassName;

      var _useBackend3 = (0, _backend.useBackend)(this.context),
          config = _useBackend3.config,
          suspended = _useBackend3.suspended;

      var _useDebug = (0, _debug.useDebug)(this.context),
          debugLayout = _useDebug.debugLayout;

      var dispatch = (0, _redux.useDispatch)(this.context);
      var fancy = (_config$window2 = config.window) == null ? void 0 : _config$window2.fancy; // Determine when to show dimmer

      var showDimmer = config.user && (config.user.observer ? config.status < _constants.UI_DISABLED : config.status < _constants.UI_INTERACTIVE);
      return (0, _inferno.createComponentVNode)(2, _Layout.Layout, {
        "className": (0, _react.classes)(["Window", className]),
        "theme": theme,
        children: [(0, _inferno.createComponentVNode)(2, TitleBar, {
          "className": (0, _react.classes)(["Window__titleBar", titleClassName]),
          "title": !suspended && (title || (0, _string.decodeHtmlEntities)(config.title)),
          "status": config.status,
          "fancy": fancy,
          "onDragStart": _drag.dragStartHandler,
          "onClose": function () {
            function onClose() {
              logger.log('pressed close');
              dispatch((0, _backend.backendSuspendStart)());
            }

            return onClose;
          }(),
          "canClose": canClose,
          children: buttons
        }), (0, _inferno.createVNode)(1, "div", (0, _react.classes)(['Window__rest', debugLayout && 'debug-layout']), [!suspended && children, showDimmer && (0, _inferno.createVNode)(1, "div", "Window__dimmer")], 0), fancy && (0, _inferno.createFragment)([(0, _inferno.createVNode)(1, "div", "Window__resizeHandle__e", null, 1, {
          "onMousedown": (0, _drag.resizeStartHandler)(1, 0)
        }), (0, _inferno.createVNode)(1, "div", "Window__resizeHandle__s", null, 1, {
          "onMousedown": (0, _drag.resizeStartHandler)(0, 1)
        }), (0, _inferno.createVNode)(1, "div", "Window__resizeHandle__se", null, 1, {
          "onMousedown": (0, _drag.resizeStartHandler)(1, 1)
        })], 4)]
      });
    }

    return render;
  }();

  return Window;
}(_inferno.Component);

exports.Window = Window;

var WindowContent = function WindowContent(props) {
  var className = props.className,
      fitted = props.fitted,
      children = props.children,
      rest = _objectWithoutPropertiesLoose(props, _excluded);

  return (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, _Layout.Layout.Content, Object.assign({
    "className": (0, _react.classes)(['Window__content', className])
  }, rest, {
    children: fitted && children || (0, _inferno.createVNode)(1, "div", "Window__contentPadding", children, 0)
  })));
};

Window.Content = WindowContent;

var statusToColor = function statusToColor(status) {
  switch (status) {
    case _constants.UI_INTERACTIVE:
      return 'good';

    case _constants.UI_UPDATE:
      return 'average';

    case _constants.UI_DISABLED:
    default:
      return 'bad';
  }
};

var TitleBar = function TitleBar(props, context) {
  var className = props.className,
      title = props.title,
      status = props.status,
      canClose = props.canClose,
      fancy = props.fancy,
      onDragStart = props.onDragStart,
      onClose = props.onClose,
      children = props.children;
  var dispatch = (0, _redux.useDispatch)(context);
  return (0, _inferno.createVNode)(1, "div", (0, _react.classes)(['TitleBar', className]), [status === undefined && (0, _inferno.createComponentVNode)(2, _components.Icon, {
    "className": "TitleBar__statusIcon",
    "name": "tools",
    "opacity": 0.5
  }) || (0, _inferno.createComponentVNode)(2, _components.Icon, {
    "className": "TitleBar__statusIcon",
    "color": statusToColor(status),
    "name": "eye"
  }), (0, _inferno.createVNode)(1, "div", "TitleBar__dragZone", null, 1, {
    "onMousedown": function () {
      function onMousedown(e) {
        return fancy && onDragStart(e);
      }

      return onMousedown;
    }()
  }), (0, _inferno.createVNode)(1, "div", "TitleBar__title", [typeof title === 'string' && title === title.toLowerCase() && (0, _string.toTitleCase)(title) || title, !!children && (0, _inferno.createVNode)(1, "div", "TitleBar__buttons", children, 0)], 0),  false && 0, Boolean(fancy && canClose) && (0, _inferno.createVNode)(1, "div", "TitleBar__close TitleBar__clickable", Byond.IS_LTE_IE8 ? 'x' : '×', 0, {
    "onclick": onClose
  })], 0);
};

/***/ }),

/***/ 2381:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Window = exports.Pane = exports.NtosWindow = exports.Layout = void 0;

var _Layout = __webpack_require__(30041);

exports.Layout = _Layout.Layout;

var _NtosWindow = __webpack_require__(76738);

exports.NtosWindow = _NtosWindow.NtosWindow;

var _Pane = __webpack_require__(52243);

exports.Pane = _Pane.Pane;

var _Window = __webpack_require__(34881);

exports.Window = _Window.Window;

/***/ }),

/***/ 13952:
/***/ (function(__unused_webpack_module, exports) {

"use strict";


exports.__esModule = true;
exports.captureExternalLinks = void 0;

/**
 * @file
 * @copyright 2020 Aleksej Komarov
 * @license MIT
 */

/**
 * Prevents baby jailing the user when he clicks an external link.
 */
var captureExternalLinks = function captureExternalLinks() {
  // Subscribe to all document clicks
  document.addEventListener('click', function (e) {
    /** @type {HTMLElement} */
    var target = e.target; // Recurse down the tree to find a valid link

    while (true) {
      // Reached the end, bail.
      if (!target || target === document.body) {
        return;
      }

      var tagName = String(target.tagName).toLowerCase();

      if (tagName === 'a') {
        break;
      }

      target = target.parentElement;
    }

    var hrefAttr = target.getAttribute('href') || ''; // Leave BYOND links alone

    var isByondLink = hrefAttr.charAt(0) === '?' || hrefAttr.startsWith('byond://');

    if (isByondLink) {
      return;
    } // Prevent default action


    e.preventDefault(); // Normalize the URL

    var url = hrefAttr;

    if (url.toLowerCase().startsWith('www')) {
      url = 'https://' + url;
    } // Open the link


    Byond.topic({
      tgui: 1,
      window_id: window.__windowId__,
      type: 'openLink',
      url: url
    });
  });
};

exports.captureExternalLinks = captureExternalLinks;

/***/ }),

/***/ 79926:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.logger = exports.createLogger = void 0;

var _client = __webpack_require__(82252);

/**
 * @file
 * @copyright 2020 Aleksej Komarov
 * @license MIT
 */
var LEVEL_DEBUG = 0;
var LEVEL_LOG = 1;
var LEVEL_INFO = 2;
var LEVEL_WARN = 3;
var LEVEL_ERROR = 4;

var _log = function log(level, ns) {
  for (var _len = arguments.length, args = new Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++) {
    args[_key - 2] = arguments[_key];
  }

  // Send logs to a remote log collector
  if (false) {} // Send important logs to the backend


  if (level >= LEVEL_INFO) {
    var logEntry = [ns].concat(args).map(function (value) {
      if (typeof value === 'string') {
        return value;
      }

      if (value instanceof Error) {
        return value.stack || String(value);
      }

      return JSON.stringify(value);
    }).filter(function (value) {
      return value;
    }).join(' ') + '\nUser Agent: ' + navigator.userAgent;
    Byond.topic({
      tgui: 1,
      window_id: window.__windowId__,
      type: 'log',
      ns: ns,
      message: logEntry
    });
  }
};

var createLogger = function createLogger(ns) {
  return {
    debug: function () {
      function debug() {
        for (var _len2 = arguments.length, args = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
          args[_key2] = arguments[_key2];
        }

        return _log.apply(void 0, [LEVEL_DEBUG, ns].concat(args));
      }

      return debug;
    }(),
    log: function () {
      function log() {
        for (var _len3 = arguments.length, args = new Array(_len3), _key3 = 0; _key3 < _len3; _key3++) {
          args[_key3] = arguments[_key3];
        }

        return _log.apply(void 0, [LEVEL_LOG, ns].concat(args));
      }

      return log;
    }(),
    info: function () {
      function info() {
        for (var _len4 = arguments.length, args = new Array(_len4), _key4 = 0; _key4 < _len4; _key4++) {
          args[_key4] = arguments[_key4];
        }

        return _log.apply(void 0, [LEVEL_INFO, ns].concat(args));
      }

      return info;
    }(),
    warn: function () {
      function warn() {
        for (var _len5 = arguments.length, args = new Array(_len5), _key5 = 0; _key5 < _len5; _key5++) {
          args[_key5] = arguments[_key5];
        }

        return _log.apply(void 0, [LEVEL_WARN, ns].concat(args));
      }

      return warn;
    }(),
    error: function () {
      function error() {
        for (var _len6 = arguments.length, args = new Array(_len6), _key6 = 0; _key6 < _len6; _key6++) {
          args[_key6] = arguments[_key6];
        }

        return _log.apply(void 0, [LEVEL_ERROR, ns].concat(args));
      }

      return error;
    }()
  };
};
/**
 * A generic instance of the logger.
 *
 * Does not have a namespace associated with it.
 */


exports.createLogger = createLogger;
var logger = createLogger();
exports.logger = logger;

/***/ }),

/***/ 77960:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.createRenderer = exports.suspendRenderer = exports.resumeRenderer = void 0;

var _perf = __webpack_require__(28440);

var _inferno = __webpack_require__(96323);

var _logging = __webpack_require__(79926);

var logger = (0, _logging.createLogger)('renderer');
var reactRoot;
var initialRender = true;
var suspended = false; // These functions are used purely for profiling.

var resumeRenderer = function resumeRenderer() {
  initialRender = initialRender || 'resumed';
  suspended = false;
};

exports.resumeRenderer = resumeRenderer;

var suspendRenderer = function suspendRenderer() {
  suspended = true;
};

exports.suspendRenderer = suspendRenderer;

var createRenderer = function createRenderer(getVNode) {
  return function () {
    _perf.perf.mark('render/start'); // Start rendering


    if (!reactRoot) {
      reactRoot = document.getElementById('react-root');
    }

    (0, _inferno.render)(getVNode(), reactRoot);

    _perf.perf.mark('render/finish');

    if (suspended) {
      return;
    } // Report rendering time


    if (false) {}

    if (initialRender) {
      initialRender = false;
    }
  };
};

exports.createRenderer = createRenderer;

/***/ }),

/***/ 91199:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.getRoutedComponent = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _selectors = __webpack_require__(12184);

var _layouts = __webpack_require__(2381);

/**
 * @file
 * @copyright 2020 Aleksej Komarov
 * @license MIT
 */
var requireInterface = __webpack_require__(8156);

var routingError = function routingError(type, name) {
  return function () {
    return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
      children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
        "scrollable": true,
        children: [type === 'notFound' && (0, _inferno.createVNode)(1, "div", null, [(0, _inferno.createTextVNode)("Interface "), (0, _inferno.createVNode)(1, "b", null, name, 0), (0, _inferno.createTextVNode)(" was not found.")], 4), type === 'missingExport' && (0, _inferno.createVNode)(1, "div", null, [(0, _inferno.createTextVNode)("Interface "), (0, _inferno.createVNode)(1, "b", null, name, 0), (0, _inferno.createTextVNode)(" is missing an export.")], 4)]
      })
    });
  };
};

var SuspendedWindow = function SuspendedWindow() {
  return (0, _inferno.createComponentVNode)(2, _layouts.Window, {
    children: (0, _inferno.createComponentVNode)(2, _layouts.Window.Content, {
      "scrollable": true
    })
  });
};

var getRoutedComponent = function getRoutedComponent(store) {
  var state = store.getState();

  var _selectBackend = (0, _backend.selectBackend)(state),
      suspended = _selectBackend.suspended,
      config = _selectBackend.config;

  if (suspended) {
    return SuspendedWindow;
  }

  if (false) { var debug; }

  var name = config == null ? void 0 : config["interface"];
  var interfacePathBuilders = [function (name) {
    return "./" + name + ".tsx";
  }, function (name) {
    return "./" + name + ".js";
  }, function (name) {
    return "./" + name + "/index.tsx";
  }, function (name) {
    return "./" + name + "/index.js";
  }];
  var esModule;

  while (!esModule && interfacePathBuilders.length > 0) {
    var interfacePathBuilder = interfacePathBuilders.shift();
    var interfacePath = interfacePathBuilder(name);

    try {
      esModule = requireInterface(interfacePath);
    } catch (err) {
      if (err.code !== 'MODULE_NOT_FOUND') {
        throw err;
      }
    }
  }

  if (!esModule) {
    return routingError('notFound', name);
  }

  var Component = esModule[name];

  if (!Component) {
    return routingError('missingExport', name);
  }

  return Component;
};

exports.getRoutedComponent = getRoutedComponent;

/***/ }),

/***/ 81890:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.sanitizeText = void 0;

var _dompurify = _interopRequireDefault(__webpack_require__(42950));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

/**
 * Uses DOMPurify to purify/sanitise HTML.
 */
// Default values
var defTag = ['b', 'br', 'center', 'code', 'div', 'font', 'hr', 'i', 'li', 'menu', 'ol', 'p', 'pre', 'span', 'table', 'td', 'th', 'tr', 'u', 'ul'];
var defAttr = ['class', 'style'];
/**
 * Feed it a string and it should spit out a sanitized version.
 *
 * @param {string} input
 * @param {array} tags
 * @param {array} forbidAttr
 */

var sanitizeText = function sanitizeText(input, tags, forbidAttr) {
  if (tags === void 0) {
    tags = defTag;
  }

  if (forbidAttr === void 0) {
    forbidAttr = defAttr;
  }

  // This is VERY important to think first if you NEED
  // the tag you put in here.  We are pushing all this
  // though dangerouslySetInnerHTML and even though
  // the default DOMPurify kills javascript, it dosn't
  // kill href links or such
  return _dompurify["default"].sanitize(input, {
    ALLOWED_TAGS: tags,
    FORBID_ATTR: forbidAttr
  });
};

exports.sanitizeText = sanitizeText;

/***/ }),

/***/ 18749:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.StoreProvider = exports.configureStore = void 0;

var _fp = __webpack_require__(32416);

var _redux = __webpack_require__(465);

var _inferno = __webpack_require__(96323);

var _assets = __webpack_require__(39646);

var _backend = __webpack_require__(31246);

var _debug = __webpack_require__(27157);

var _logging = __webpack_require__(79926);

function _inheritsLoose(subClass, superClass) { subClass.prototype = Object.create(superClass.prototype); subClass.prototype.constructor = subClass; _setPrototypeOf(subClass, superClass); }

function _setPrototypeOf(o, p) { _setPrototypeOf = Object.setPrototypeOf || function () { function _setPrototypeOf(o, p) { o.__proto__ = p; return o; } return _setPrototypeOf; }(); return _setPrototypeOf(o, p); }

var logger = (0, _logging.createLogger)('store');

var configureStore = function configureStore(options) {
  var _options$middleware, _options$middleware2;

  if (options === void 0) {
    options = {};
  }

  var reducer = (0, _fp.flow)([(0, _redux.combineReducers)({
    debug: _debug.debugReducer,
    backend: _backend.backendReducer
  }), options.reducer]);
  var middleware = [].concat(((_options$middleware = options.middleware) == null ? void 0 : _options$middleware.pre) || [], [_assets.assetMiddleware, _backend.backendMiddleware], ((_options$middleware2 = options.middleware) == null ? void 0 : _options$middleware2.post) || []);

  if (false) {}

  var enhancer = _redux.applyMiddleware.apply(void 0, middleware);

  var store = (0, _redux.createStore)(reducer, enhancer); // Globals

  window.__store__ = store;
  window.__augmentStack__ = createStackAugmentor(store);
  return store;
};

exports.configureStore = configureStore;

var loggingMiddleware = function loggingMiddleware(store) {
  return function (next) {
    return function (action) {
      var type = action.type,
          payload = action.payload;

      if (type === 'update' || type === 'backend/update') {
        logger.debug('action', {
          type: type
        });
      } else {
        logger.debug('action', action);
      }

      return next(action);
    };
  };
};
/**
 * Creates a function, which can be assigned to window.__augmentStack__
 * to augment reported stack traces with useful data for debugging.
 */


var createStackAugmentor = function createStackAugmentor(store) {
  return function (stack, error) {
    var _state$backend, _config$client;

    if (!error) {
      error = new Error(stack.split('\n')[0]);
      error.stack = stack;
    } else if (typeof error === 'object' && !error.stack) {
      error.stack = stack;
    }

    logger.log('FatalError:', error);
    var state = store.getState();
    var config = state == null ? void 0 : (_state$backend = state.backend) == null ? void 0 : _state$backend.config;
    var augmentedStack = stack;
    augmentedStack += '\nUser Agent: ' + navigator.userAgent;
    augmentedStack += '\nState: ' + JSON.stringify({
      ckey: config == null ? void 0 : (_config$client = config.client) == null ? void 0 : _config$client.ckey,
      "interface": config == null ? void 0 : config["interface"],
      window: config == null ? void 0 : config.window
    });
    return augmentedStack;
  };
};
/**
 * Store provider for Inferno apps.
 */


var StoreProvider = /*#__PURE__*/function (_Component) {
  _inheritsLoose(StoreProvider, _Component);

  function StoreProvider() {
    return _Component.apply(this, arguments) || this;
  }

  var _proto = StoreProvider.prototype;

  _proto.getChildContext = function () {
    function getChildContext() {
      var store = this.props.store;
      return {
        store: store
      };
    }

    return getChildContext;
  }();

  _proto.render = function () {
    function render() {
      return this.props.children;
    }

    return render;
  }();

  return StoreProvider;
}(_inferno.Component);

exports.StoreProvider = StoreProvider;

/***/ }),

/***/ 88428:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.meta = void 0;

var _inferno = __webpack_require__(96323);

var _components = __webpack_require__(26949);

/**
 * @file
 * @copyright 2021 Aleksej Komarov
 * @license MIT
 */
var meta = {
  title: 'Blink',
  render: function () {
    function render() {
      return (0, _inferno.createComponentVNode)(2, Story);
    }

    return render;
  }()
};
exports.meta = meta;

var Story = function Story(props, context) {
  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    children: (0, _inferno.createComponentVNode)(2, _components.Blink, {
      children: "Blink"
    })
  });
};

/***/ }),

/***/ 9184:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.meta = void 0;

var _inferno = __webpack_require__(96323);

var _components = __webpack_require__(26949);

var _common = __webpack_require__(87758);

/**
 * @file
 * @copyright 2021 Aleksej Komarov
 * @license MIT
 */
var meta = {
  title: 'BlockQuote',
  render: function () {
    function render() {
      return (0, _inferno.createComponentVNode)(2, Story);
    }

    return render;
  }()
};
exports.meta = meta;

var Story = function Story(props, context) {
  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    children: (0, _inferno.createComponentVNode)(2, _components.BlockQuote, {
      children: (0, _inferno.createComponentVNode)(2, _common.BoxWithSampleText)
    })
  });
};

/***/ }),

/***/ 36801:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.meta = void 0;

var _inferno = __webpack_require__(96323);

var _components = __webpack_require__(26949);

/**
 * @file
 * @copyright 2021 Aleksej Komarov
 * @license MIT
 */
var meta = {
  title: 'Box',
  render: function () {
    function render() {
      return (0, _inferno.createComponentVNode)(2, Story);
    }

    return render;
  }()
};
exports.meta = meta;

var Story = function Story(props, context) {
  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
      "bold": true,
      children: "bold"
    }), (0, _inferno.createComponentVNode)(2, _components.Box, {
      "italic": true,
      children: "italic"
    }), (0, _inferno.createComponentVNode)(2, _components.Box, {
      "opacity": 0.5,
      children: "opacity 0.5"
    }), (0, _inferno.createComponentVNode)(2, _components.Box, {
      "opacity": 0.25,
      children: "opacity 0.25"
    }), (0, _inferno.createComponentVNode)(2, _components.Box, {
      "m": 2,
      children: "m: 2"
    }), (0, _inferno.createComponentVNode)(2, _components.Box, {
      "textAlign": "left",
      children: "left"
    }), (0, _inferno.createComponentVNode)(2, _components.Box, {
      "textAlign": "center",
      children: "center"
    }), (0, _inferno.createComponentVNode)(2, _components.Box, {
      "textAlign": "right",
      children: "right"
    })]
  });
};

/***/ }),

/***/ 3750:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.meta = void 0;

var _inferno = __webpack_require__(96323);

var _components = __webpack_require__(26949);

/**
 * @file
 * @copyright 2021 Aleksej Komarov
 * @license MIT
 */
var meta = {
  title: 'Button',
  render: function () {
    function render() {
      return (0, _inferno.createComponentVNode)(2, Story);
    }

    return render;
  }()
};
exports.meta = meta;
var COLORS_SPECTRUM = ['red', 'orange', 'yellow', 'olive', 'green', 'teal', 'blue', 'violet', 'purple', 'pink', 'brown', 'grey'];
var COLORS_STATES = ['good', 'average', 'bad', 'black', 'white'];

var Story = function Story(props, context) {
  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
      "mb": 1,
      children: [(0, _inferno.createComponentVNode)(2, _components.Button, {
        "content": "Simple"
      }), (0, _inferno.createComponentVNode)(2, _components.Button, {
        "selected": true,
        "content": "Selected"
      }), (0, _inferno.createComponentVNode)(2, _components.Button, {
        "altSelected": true,
        "content": "Alt Selected"
      }), (0, _inferno.createComponentVNode)(2, _components.Button, {
        "disabled": true,
        "content": "Disabled"
      }), (0, _inferno.createComponentVNode)(2, _components.Button, {
        "color": "transparent",
        "content": "Transparent"
      }), (0, _inferno.createComponentVNode)(2, _components.Button, {
        "icon": "cog",
        "content": "Icon"
      }), (0, _inferno.createComponentVNode)(2, _components.Button, {
        "icon": "power-off"
      }), (0, _inferno.createComponentVNode)(2, _components.Button, {
        "fluid": true,
        "content": "Fluid"
      }), (0, _inferno.createComponentVNode)(2, _components.Button, {
        "my": 1,
        "lineHeight": 2,
        "minWidth": 15,
        "textAlign": "center",
        "content": "With Box props"
      })]
    }), (0, _inferno.createComponentVNode)(2, _components.Box, {
      "mb": 1,
      children: [COLORS_STATES.map(function (color) {
        return (0, _inferno.createComponentVNode)(2, _components.Button, {
          "color": color,
          "content": color
        }, color);
      }), (0, _inferno.createVNode)(1, "br"), COLORS_SPECTRUM.map(function (color) {
        return (0, _inferno.createComponentVNode)(2, _components.Button, {
          "color": color,
          "content": color
        }, color);
      }), (0, _inferno.createVNode)(1, "br"), COLORS_SPECTRUM.map(function (color) {
        return (0, _inferno.createComponentVNode)(2, _components.Box, {
          "inline": true,
          "mx": "7px",
          "color": color,
          children: color
        }, color);
      })]
    })]
  });
};

/***/ }),

/***/ 68696:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.meta = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

var _logging = __webpack_require__(79926);

/**
 * @file
 * @copyright 2021 Aleksej Komarov
 * @license MIT
 */
var meta = {
  title: 'ByondUi',
  render: function () {
    function render() {
      return (0, _inferno.createComponentVNode)(2, Story);
    }

    return render;
  }()
};
exports.meta = meta;

var Story = function Story(props, context) {
  var _useLocalState = (0, _backend.useLocalState)(context, 'byondUiEvalCode', "Byond.winset('" + window.__windowId__ + "', {\n  'is-visible': true,\n})"),
      code = _useLocalState[0],
      setCode = _useLocalState[1];

  return (0, _inferno.createFragment)([(0, _inferno.createComponentVNode)(2, _components.Section, {
    "title": "Button",
    children: (0, _inferno.createComponentVNode)(2, _components.ByondUi, {
      "params": {
        type: 'button',
        text: 'Button'
      }
    })
  }), (0, _inferno.createComponentVNode)(2, _components.Section, {
    "title": "Make BYOND calls",
    "buttons": (0, _inferno.createComponentVNode)(2, _components.Button, {
      "icon": "chevron-right",
      "onClick": function () {
        function onClick() {
          return setImmediate(function () {
            try {
              var result = new Function('return (' + code + ')')();

              if (result && result.then) {
                _logging.logger.log('Promise');

                result.then(_logging.logger.log);
              } else {
                _logging.logger.log(result);
              }
            } catch (err) {
              _logging.logger.log(err);
            }
          });
        }

        return onClick;
      }(),
      children: "Evaluate"
    }),
    children: (0, _inferno.createComponentVNode)(2, _components.Box, {
      "as": "textarea",
      "width": "100%",
      "height": "10em",
      "onChange": function () {
        function onChange(e) {
          return setCode(e.target.value);
        }

        return onChange;
      }(),
      children: code
    })
  })], 4);
};

/***/ }),

/***/ 88835:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.meta = void 0;

var _inferno = __webpack_require__(96323);

var _components = __webpack_require__(26949);

var _common = __webpack_require__(87758);

/**
 * @file
 * @copyright 2021 Aleksej Komarov
 * @license MIT
 */
var meta = {
  title: 'Collapsible',
  render: function () {
    function render() {
      return (0, _inferno.createComponentVNode)(2, Story);
    }

    return render;
  }()
};
exports.meta = meta;

var Story = function Story(props, context) {
  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    children: (0, _inferno.createComponentVNode)(2, _components.Collapsible, {
      "title": "Collapsible Demo",
      "buttons": (0, _inferno.createComponentVNode)(2, _components.Button, {
        "icon": "cog"
      }),
      children: (0, _inferno.createComponentVNode)(2, _common.BoxWithSampleText)
    })
  });
};

/***/ }),

/***/ 34458:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.meta = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

/**
 * @file
 * @copyright 2021 Aleksej Komarov
 * @license MIT
 */
var meta = {
  title: 'Flex & Sections',
  render: function () {
    function render() {
      return (0, _inferno.createComponentVNode)(2, Story);
    }

    return render;
  }()
};
exports.meta = meta;

var Story = function Story(props, context) {
  var _useLocalState = (0, _backend.useLocalState)(context, 'fs_grow', 1),
      grow = _useLocalState[0],
      setGrow = _useLocalState[1];

  var _useLocalState2 = (0, _backend.useLocalState)(context, 'fs_direction', 'column'),
      direction = _useLocalState2[0],
      setDirection = _useLocalState2[1];

  var _useLocalState3 = (0, _backend.useLocalState)(context, 'fs_fill', true),
      fill = _useLocalState3[0],
      setFill = _useLocalState3[1];

  var _useLocalState4 = (0, _backend.useLocalState)(context, 'fs_title', true),
      hasTitle = _useLocalState4[0],
      setHasTitle = _useLocalState4[1];

  return (0, _inferno.createComponentVNode)(2, _components.Flex, {
    "height": "100%",
    "direction": "column",
    children: [(0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
      "mb": 1,
      children: (0, _inferno.createComponentVNode)(2, _components.Section, {
        children: [(0, _inferno.createComponentVNode)(2, _components.Button, {
          "fluid": true,
          "onClick": function () {
            function onClick() {
              return setDirection(direction === 'column' ? 'row' : 'column');
            }

            return onClick;
          }(),
          children: "Flex direction=\"" + direction + "\""
        }), (0, _inferno.createComponentVNode)(2, _components.Button, {
          "fluid": true,
          "onClick": function () {
            function onClick() {
              return setGrow(Number(!grow));
            }

            return onClick;
          }(),
          children: "Flex.Item grow={" + grow + "}"
        }), (0, _inferno.createComponentVNode)(2, _components.Button, {
          "fluid": true,
          "onClick": function () {
            function onClick() {
              return setFill(!fill);
            }

            return onClick;
          }(),
          children: "Section fill={" + String(fill) + "}"
        }), (0, _inferno.createComponentVNode)(2, _components.Button, {
          "fluid": true,
          "selected": hasTitle,
          "onClick": function () {
            function onClick() {
              return setHasTitle(!hasTitle);
            }

            return onClick;
          }(),
          children: "Section title"
        })]
      })
    }), (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
      "grow": 1,
      children: (0, _inferno.createComponentVNode)(2, _components.Flex, {
        "height": "100%",
        "direction": direction,
        children: [(0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
          "mr": direction === 'row' && 1,
          "mb": direction === 'column' && 1,
          "grow": grow,
          children: (0, _inferno.createComponentVNode)(2, _components.Section, {
            "title": hasTitle && 'Section 1',
            "fill": fill,
            children: "Content"
          })
        }), (0, _inferno.createComponentVNode)(2, _components.Flex.Item, {
          "grow": grow,
          children: (0, _inferno.createComponentVNode)(2, _components.Section, {
            "title": hasTitle && 'Section 2',
            "fill": fill,
            children: "Content"
          })
        })]
      })
    })]
  });
};

/***/ }),

/***/ 23148:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.meta = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

/**
 * @file
 * @copyright 2021 Aleksej Komarov
 * @license MIT
 */
var meta = {
  title: 'Input',
  render: function () {
    function render() {
      return (0, _inferno.createComponentVNode)(2, Story);
    }

    return render;
  }()
};
exports.meta = meta;

var Story = function Story(props, context) {
  var _useLocalState = (0, _backend.useLocalState)(context, 'number', 0),
      number = _useLocalState[0],
      setNumber = _useLocalState[1];

  var _useLocalState2 = (0, _backend.useLocalState)(context, 'text', "Sample text"),
      text = _useLocalState2[0],
      setText = _useLocalState2[1];

  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    children: (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
      children: [(0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Input (onChange)",
        children: (0, _inferno.createComponentVNode)(2, _components.Input, {
          "value": text,
          "onChange": function () {
            function onChange(e, value) {
              return setText(value);
            }

            return onChange;
          }()
        })
      }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Input (onInput)",
        children: (0, _inferno.createComponentVNode)(2, _components.Input, {
          "value": text,
          "onInput": function () {
            function onInput(e, value) {
              return setText(value);
            }

            return onInput;
          }()
        })
      }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "NumberInput (onChange)",
        children: (0, _inferno.createComponentVNode)(2, _components.NumberInput, {
          "animated": true,
          "width": "40px",
          "step": 1,
          "stepPixelSize": 5,
          "value": number,
          "minValue": -100,
          "maxValue": 100,
          "onChange": function () {
            function onChange(e, value) {
              return setNumber(value);
            }

            return onChange;
          }()
        })
      }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "NumberInput (onDrag)",
        children: (0, _inferno.createComponentVNode)(2, _components.NumberInput, {
          "animated": true,
          "width": "40px",
          "step": 1,
          "stepPixelSize": 5,
          "value": number,
          "minValue": -100,
          "maxValue": 100,
          "onDrag": function () {
            function onDrag(e, value) {
              return setNumber(value);
            }

            return onDrag;
          }()
        })
      }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Slider (onDrag)",
        children: (0, _inferno.createComponentVNode)(2, _components.Slider, {
          "step": 1,
          "stepPixelSize": 5,
          "value": number,
          "minValue": -100,
          "maxValue": 100,
          "onDrag": function () {
            function onDrag(e, value) {
              return setNumber(value);
            }

            return onDrag;
          }()
        })
      }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Knob (onDrag)",
        children: [(0, _inferno.createComponentVNode)(2, _components.Knob, {
          "inline": true,
          "size": 1,
          "step": 1,
          "stepPixelSize": 2,
          "value": number,
          "minValue": -100,
          "maxValue": 100,
          "onDrag": function () {
            function onDrag(e, value) {
              return setNumber(value);
            }

            return onDrag;
          }()
        }), (0, _inferno.createComponentVNode)(2, _components.Knob, {
          "ml": 1,
          "inline": true,
          "bipolar": true,
          "size": 1,
          "step": 1,
          "stepPixelSize": 2,
          "value": number,
          "minValue": -100,
          "maxValue": 100,
          "onDrag": function () {
            function onDrag(e, value) {
              return setNumber(value);
            }

            return onDrag;
          }()
        })]
      }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Rotating Icon",
        children: (0, _inferno.createComponentVNode)(2, _components.Box, {
          "inline": true,
          "position": "relative",
          children: (0, _inferno.createComponentVNode)(2, _components.DraggableControl, {
            "value": number,
            "minValue": -100,
            "maxValue": 100,
            "dragMatrix": [0, -1],
            "step": 1,
            "stepPixelSize": 5,
            "onDrag": function () {
              function onDrag(e, value) {
                return setNumber(value);
              }

              return onDrag;
            }(),
            children: function () {
              function children(control) {
                return (0, _inferno.createComponentVNode)(2, _components.Box, {
                  "onMouseDown": control.handleDragStart,
                  children: [(0, _inferno.createComponentVNode)(2, _components.Icon, {
                    "size": 4,
                    "color": "yellow",
                    "name": "times",
                    "rotation": control.displayValue * 4
                  }), control.inputElement]
                });
              }

              return children;
            }()
          })
        })
      })]
    })
  });
};

/***/ }),

/***/ 23613:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.meta = void 0;

var _inferno = __webpack_require__(96323);

var _components = __webpack_require__(26949);

var meta = {
  title: "Popper",
  render: function () {
    function render() {
      return (0, _inferno.createComponentVNode)(2, Story);
    }

    return render;
  }()
};
exports.meta = meta;

var Story = function Story() {
  return (0, _inferno.createFragment)([(0, _inferno.createComponentVNode)(2, _components.Popper, {
    "popperContent": (0, _inferno.createComponentVNode)(2, _components.Box, {
      "style": {
        background: "white",
        border: "2px solid blue"
      },
      children: "Loogatme!"
    }),
    "options": {
      placement: "bottom"
    },
    children: (0, _inferno.createComponentVNode)(2, _components.Box, {
      "style": {
        border: "5px solid white",
        height: "300px",
        width: "200px"
      }
    })
  }), (0, _inferno.createComponentVNode)(2, _components.Popper, {
    "popperContent": (0, _inferno.createComponentVNode)(2, _components.Box, {
      "style": {
        background: "white",
        border: "2px solid blue"
      },
      children: "I am on the right!"
    }),
    "options": {
      placement: "right"
    },
    children: (0, _inferno.createComponentVNode)(2, _components.Box, {
      "style": {
        border: "5px solid white",
        height: "500px",
        width: "100px"
      }
    })
  })], 4);
};

/***/ }),

/***/ 9093:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.meta = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

/**
 * @file
 * @copyright 2021 Aleksej Komarov
 * @license MIT
 */
var meta = {
  title: 'ProgressBar',
  render: function () {
    function render() {
      return (0, _inferno.createComponentVNode)(2, Story);
    }

    return render;
  }()
};
exports.meta = meta;

var Story = function Story(props, context) {
  var _useLocalState = (0, _backend.useLocalState)(context, 'progress', 0.5),
      progress = _useLocalState[0],
      setProgress = _useLocalState[1];

  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    children: [(0, _inferno.createComponentVNode)(2, _components.ProgressBar, {
      "ranges": {
        good: [0.5, Infinity],
        bad: [-Infinity, 0.1],
        average: [0, 0.5]
      },
      "minValue": -1,
      "maxValue": 1,
      "value": progress,
      children: ["Value: ", Number(progress).toFixed(1)]
    }), (0, _inferno.createComponentVNode)(2, _components.Box, {
      "mt": 1,
      children: [(0, _inferno.createComponentVNode)(2, _components.Button, {
        "content": "-0.1",
        "onClick": function () {
          function onClick() {
            return setProgress(progress - 0.1);
          }

          return onClick;
        }()
      }), (0, _inferno.createComponentVNode)(2, _components.Button, {
        "content": "+0.1",
        "onClick": function () {
          function onClick() {
            return setProgress(progress + 0.1);
          }

          return onClick;
        }()
      })]
    })]
  });
};

/***/ }),

/***/ 86482:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.meta = void 0;

var _inferno = __webpack_require__(96323);

var _components = __webpack_require__(26949);

/**
 * @file
 * @copyright 2021 Aleksej Komarov
 * @license MIT
 */
var meta = {
  title: 'Stack',
  render: function () {
    function render() {
      return (0, _inferno.createComponentVNode)(2, Story);
    }

    return render;
  }()
};
exports.meta = meta;

var Filler = function Filler() {
  return (0, _inferno.createComponentVNode)(2, _components.Box, {
    "inline": true,
    "width": 1,
    "height": 1,
    children: "A"
  });
};

var SmallStackItems = function SmallStackItems() {
  return (0, _inferno.createFragment)([(0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
    children: (0, _inferno.createComponentVNode)(2, Filler)
  }), (0, _inferno.createComponentVNode)(2, _components.Stack.Divider), (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
    children: (0, _inferno.createComponentVNode)(2, Filler)
  })], 4);
};

var Story = function Story(props, context) {
  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    "fill": true,
    children: (0, _inferno.createComponentVNode)(2, _components.Stack, {
      "fill": true,
      "className": "debug-layout",
      children: [(0, _inferno.createComponentVNode)(2, SmallStackItems), (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
        "grow": 1,
        children: (0, _inferno.createComponentVNode)(2, _components.Stack, {
          "fill": true,
          "vertical": true,
          children: [(0, _inferno.createComponentVNode)(2, SmallStackItems), (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
            children: (0, _inferno.createComponentVNode)(2, _components.Stack, {
              "fill": true,
              children: [(0, _inferno.createComponentVNode)(2, SmallStackItems), (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
                "grow": 1
              }), (0, _inferno.createComponentVNode)(2, SmallStackItems), (0, _inferno.createComponentVNode)(2, SmallStackItems)]
            })
          }), (0, _inferno.createComponentVNode)(2, _components.Stack.Item, {
            "grow": 1
          }), (0, _inferno.createComponentVNode)(2, SmallStackItems)]
        })
      })]
    })
  });
};

/***/ }),

/***/ 63900:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.meta = void 0;

var _inferno = __webpack_require__(96323);

var _storage = __webpack_require__(7151);

var _components = __webpack_require__(26949);

var _format = __webpack_require__(1033);

/**
 * @file
 * @copyright 2021 Aleksej Komarov
 * @license MIT
 */
var meta = {
  title: 'Storage',
  render: function () {
    function render() {
      return (0, _inferno.createComponentVNode)(2, Story);
    }

    return render;
  }()
};
exports.meta = meta;

var Story = function Story(props, context) {
  if (!window.localStorage) {
    return (0, _inferno.createComponentVNode)(2, _components.NoticeBox, {
      children: "Local storage is not available."
    });
  }

  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    "title": "Local Storage",
    "buttons": (0, _inferno.createComponentVNode)(2, _components.Button, {
      "icon": "recycle",
      "onClick": function () {
        function onClick() {
          localStorage.clear();

          _storage.storage.clear();
        }

        return onClick;
      }(),
      children: "Clear"
    }),
    children: (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
      children: [(0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Keys in use",
        children: localStorage.length
      }), (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Remaining space",
        children: (0, _format.formatSiUnit)(localStorage.remainingSpace, 0, 'B')
      })]
    })
  });
};

/***/ }),

/***/ 88209:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.meta = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

/**
 * @file
 * @copyright 2021 Aleksej Komarov
 * @license MIT
 */
var meta = {
  title: 'Tabs',
  render: function () {
    function render() {
      return (0, _inferno.createComponentVNode)(2, Story);
    }

    return render;
  }()
};
exports.meta = meta;
var TAB_RANGE = ['Tab #1', 'Tab #2', 'Tab #3', 'Tab #4'];

var Story = function Story(props, context) {
  var _useLocalState = (0, _backend.useLocalState)(context, 'tabProps', {}),
      tabProps = _useLocalState[0],
      setTabProps = _useLocalState[1];

  return (0, _inferno.createFragment)([(0, _inferno.createComponentVNode)(2, _components.Section, {
    children: [(0, _inferno.createComponentVNode)(2, _components.Button.Checkbox, {
      "inline": true,
      "content": "vertical",
      "checked": tabProps.vertical,
      "onClick": function () {
        function onClick() {
          return setTabProps(Object.assign({}, tabProps, {
            vertical: !tabProps.vertical
          }));
        }

        return onClick;
      }()
    }), (0, _inferno.createComponentVNode)(2, _components.Button.Checkbox, {
      "inline": true,
      "content": "leftSlot",
      "checked": tabProps.leftSlot,
      "onClick": function () {
        function onClick() {
          return setTabProps(Object.assign({}, tabProps, {
            leftSlot: !tabProps.leftSlot
          }));
        }

        return onClick;
      }()
    }), (0, _inferno.createComponentVNode)(2, _components.Button.Checkbox, {
      "inline": true,
      "content": "rightSlot",
      "checked": tabProps.rightSlot,
      "onClick": function () {
        function onClick() {
          return setTabProps(Object.assign({}, tabProps, {
            rightSlot: !tabProps.rightSlot
          }));
        }

        return onClick;
      }()
    }), (0, _inferno.createComponentVNode)(2, _components.Button.Checkbox, {
      "inline": true,
      "content": "icon",
      "checked": tabProps.icon,
      "onClick": function () {
        function onClick() {
          return setTabProps(Object.assign({}, tabProps, {
            icon: !tabProps.icon
          }));
        }

        return onClick;
      }()
    }), (0, _inferno.createComponentVNode)(2, _components.Button.Checkbox, {
      "inline": true,
      "content": "fluid",
      "checked": tabProps.fluid,
      "onClick": function () {
        function onClick() {
          return setTabProps(Object.assign({}, tabProps, {
            fluid: !tabProps.fluid
          }));
        }

        return onClick;
      }()
    }), (0, _inferno.createComponentVNode)(2, _components.Button.Checkbox, {
      "inline": true,
      "content": "centered",
      "checked": tabProps.centered,
      "onClick": function () {
        function onClick() {
          return setTabProps(Object.assign({}, tabProps, {
            centered: !tabProps.centered
          }));
        }

        return onClick;
      }()
    })]
  }), (0, _inferno.createComponentVNode)(2, _components.Section, {
    "fitted": true,
    children: (0, _inferno.createComponentVNode)(2, TabsPrefab)
  }), (0, _inferno.createComponentVNode)(2, _components.Section, {
    "title": "Normal section",
    children: [(0, _inferno.createComponentVNode)(2, TabsPrefab), "Some text"]
  }), (0, _inferno.createComponentVNode)(2, _components.Section, {
    children: "Section-less tabs appear the same as tabs in a fitted section:"
  }), (0, _inferno.createComponentVNode)(2, TabsPrefab)], 4);
};

var TabsPrefab = function TabsPrefab(props, context) {
  var _useLocalState2 = (0, _backend.useLocalState)(context, 'tabIndex', 0),
      tabIndex = _useLocalState2[0],
      setTabIndex = _useLocalState2[1];

  var _useLocalState3 = (0, _backend.useLocalState)(context, 'tabProps', {}),
      tabProps = _useLocalState3[0];

  return (0, _inferno.createComponentVNode)(2, _components.Tabs, {
    "vertical": tabProps.vertical,
    "fluid": tabProps.fluid,
    "textAlign": tabProps.centered && 'center',
    children: TAB_RANGE.map(function (text, i) {
      return (0, _inferno.createComponentVNode)(2, _components.Tabs.Tab, {
        "selected": i === tabIndex,
        "icon": tabProps.icon && 'info-circle',
        "leftSlot": tabProps.leftSlot && (0, _inferno.createComponentVNode)(2, _components.Button, {
          "circular": true,
          "compact": true,
          "color": "transparent",
          "icon": "times"
        }),
        "rightSlot": tabProps.rightSlot && (0, _inferno.createComponentVNode)(2, _components.Button, {
          "circular": true,
          "compact": true,
          "color": "transparent",
          "icon": "times"
        }),
        "onClick": function () {
          function onClick() {
            return setTabIndex(i);
          }

          return onClick;
        }(),
        children: text
      }, i);
    })
  });
};

/***/ }),

/***/ 38200:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.meta = void 0;

var _inferno = __webpack_require__(96323);

var _backend = __webpack_require__(31246);

var _components = __webpack_require__(26949);

/**
 * @file
 * @copyright 2021 Aleksej Komarov
 * @license MIT
 */
var meta = {
  title: 'Themes',
  render: function () {
    function render() {
      return (0, _inferno.createComponentVNode)(2, Story);
    }

    return render;
  }()
};
exports.meta = meta;

var Story = function Story(props, context) {
  var _useLocalState = (0, _backend.useLocalState)(context, 'kitchenSinkTheme'),
      theme = _useLocalState[0],
      setTheme = _useLocalState[1];

  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    children: (0, _inferno.createComponentVNode)(2, _components.LabeledList, {
      children: (0, _inferno.createComponentVNode)(2, _components.LabeledList.Item, {
        "label": "Use theme",
        children: (0, _inferno.createComponentVNode)(2, _components.Input, {
          "placeholder": "theme_name",
          "value": theme,
          "onInput": function () {
            function onInput(e, value) {
              return setTheme(value);
            }

            return onInput;
          }()
        })
      })
    })
  });
};

/***/ }),

/***/ 48181:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.meta = void 0;

var _inferno = __webpack_require__(96323);

var _core = __webpack_require__(34739);

var _components = __webpack_require__(26949);

/**
 * @file
 * @copyright 2021 Aleksej Komarov
 * @license MIT
 */
var meta = {
  title: 'Tooltip',
  render: function () {
    function render() {
      return (0, _inferno.createComponentVNode)(2, Story);
    }

    return render;
  }()
};
exports.meta = meta;

var Story = function Story() {
  var positions = ['top', 'left', 'right', 'bottom', 'bottom-start', 'bottom-end'];
  return (0, _inferno.createComponentVNode)(2, _components.Section, {
    children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
      children: [(0, _inferno.createComponentVNode)(2, _components.Tooltip, {
        "content": "Tooltip text.",
        children: (0, _inferno.createComponentVNode)(2, _components.Box, {
          "inline": true,
          "position": "relative",
          "mr": 1,
          children: "Box (hover me)."
        })
      }), (0, _inferno.createComponentVNode)(2, _components.Button, {
        "tooltip": "Tooltip text.",
        "content": "Button"
      })]
    }), (0, _inferno.createComponentVNode)(2, _components.Box, {
      "mt": 1,
      children: positions.map(function (position) {
        return (0, _inferno.createComponentVNode)(2, _components.Button, {
          "color": "transparent",
          "tooltip": "Tooltip text.",
          "tooltipPosition": position,
          "content": position
        }, position);
      })
    })]
  });
};

/***/ }),

/***/ 87758:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.BoxWithSampleText = void 0;

var _inferno = __webpack_require__(96323);

var _components = __webpack_require__(26949);

/**
 * @file
 * @copyright 2021 Aleksej Komarov
 * @license MIT
 */
var BoxWithSampleText = function BoxWithSampleText(props) {
  return (0, _inferno.normalizeProps)((0, _inferno.createComponentVNode)(2, _components.Box, Object.assign({}, props, {
    children: [(0, _inferno.createComponentVNode)(2, _components.Box, {
      "italic": true,
      children: "Jackdaws love my big sphinx of quartz."
    }), (0, _inferno.createComponentVNode)(2, _components.Box, {
      "mt": 1,
      "bold": true,
      children: "The wide electrification of the southern provinces will give a powerful impetus to the growth of agriculture."
    })]
  })));
};

exports.BoxWithSampleText = BoxWithSampleText;

/***/ }),

/***/ 33603:
/***/ (function() {

// extracted by mini-css-extract-plugin

/***/ }),

/***/ 57397:
/***/ (function() {

// extracted by mini-css-extract-plugin

/***/ }),

/***/ 62048:
/***/ (function() {

// extracted by mini-css-extract-plugin

/***/ }),

/***/ 39248:
/***/ (function() {

// extracted by mini-css-extract-plugin

/***/ }),

/***/ 36030:
/***/ (function() {

// extracted by mini-css-extract-plugin

/***/ }),

/***/ 32817:
/***/ (function() {

// extracted by mini-css-extract-plugin

/***/ }),

/***/ 44576:
/***/ (function() {

// extracted by mini-css-extract-plugin

/***/ }),

/***/ 21805:
/***/ (function() {

// extracted by mini-css-extract-plugin

/***/ }),

/***/ 39292:
/***/ (function() {

// extracted by mini-css-extract-plugin

/***/ }),

/***/ 74199:
/***/ (function() {

// extracted by mini-css-extract-plugin

/***/ }),

/***/ 65138:
/***/ (function() {

// extracted by mini-css-extract-plugin

/***/ }),

/***/ 8156:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var map = {
	"./AirlockElectronics": 64008,
	"./AirlockElectronics.tsx": 64008,
	"./AlertModal": 72272,
	"./AlertModal.js": 72272,
	"./Apc": 48349,
	"./Apc.js": 48349,
	"./Autolathe": 47533,
	"./Autolathe.js": 47533,
	"./BloomEdit": 15466,
	"./BloomEdit.js": 15466,
	"./BodyScanner": 33897,
	"./BodyScanner.js": 33897,
	"./CameraConsole": 83548,
	"./CameraConsole.js": 83548,
	"./Canister": 98566,
	"./Canister.js": 98566,
	"./Canvas": 6789,
	"./Canvas.js": 6789,
	"./CardPay": 60301,
	"./CardPay.js": 60301,
	"./ChemDispenser": 68078,
	"./ChemDispenser.js": 68078,
	"./ClientSettings": 85028,
	"./ClientSettings.js": 85028,
	"./Cryo": 10996,
	"./Cryo.js": 10996,
	"./CustomAnnounce": 26617,
	"./CustomAnnounce.js": 26617,
	"./DisposalUnit": 69974,
	"./DisposalUnit.js": 69974,
	"./EmotePanelEditor": 10704,
	"./EmotePanelEditor.js": 10704,
	"./Filteriffic": 53600,
	"./Filteriffic.js": 53600,
	"./GPS": 24482,
	"./GPS.js": 24482,
	"./GuestPass": 19609,
	"./GuestPass.js": 19609,
	"./ListInput": 96696,
	"./ListInput.js": 96696,
	"./Lootcrate": 52954,
	"./Lootcrate.js": 52954,
	"./MafiaPanel": 18707,
	"./MafiaPanel.js": 18707,
	"./Minesweeper": 55712,
	"./Minesweeper.js": 55712,
	"./MiningOreProcessingConsole": 48078,
	"./MiningOreProcessingConsole.js": 48078,
	"./MiningStackingConsole": 98948,
	"./MiningStackingConsole.js": 98948,
	"./MiningVendor": 70496,
	"./MiningVendor.js": 70496,
	"./NoticeBoard": 72160,
	"./NoticeBoard.js": 72160,
	"./NuclearBomb": 83271,
	"./NuclearBomb.js": 83271,
	"./Orbit": 64427,
	"./Orbit.js": 64427,
	"./Particool": 21293,
	"./Particool.js": 21293,
	"./Photocopier": 28508,
	"./Photocopier.js": 28508,
	"./PortablePump": 13484,
	"./PortablePump.js": 13484,
	"./PortableScrubber": 54652,
	"./PortableScrubber.js": 54652,
	"./ReligiousTool": 79051,
	"./ReligiousTool.js": 79051,
	"./Safe": 1945,
	"./Safe.js": 1945,
	"./SamplerPipe": 69905,
	"./SamplerPipe.js": 69905,
	"./SecureSafe": 95285,
	"./SecureSafe.js": 95285,
	"./SkillsConsole": 98115,
	"./SkillsConsole.js": 98115,
	"./Smes": 68952,
	"./Smes.js": 68952,
	"./SpawnersMenu": 56747,
	"./SpawnersMenu.js": 56747,
	"./Stack": 47466,
	"./Stack.js": 47466,
	"./Tank": 53759,
	"./Tank.js": 53759,
	"./Vote": 83634,
	"./Vote.tsx": 83634,
	"./Wires": 35045,
	"./Wires.js": 35045,
	"./common/AccessList": 52126,
	"./common/AccessList.js": 52126,
	"./common/InterfaceLockNoticeBox": 19447,
	"./common/InterfaceLockNoticeBox.js": 19447,
	"./common/Mining": 11409,
	"./common/Mining.js": 11409,
	"./common/PortableAtmos": 41161,
	"./common/PortableAtmos.js": 41161
};


function webpackContext(req) {
	var id = webpackContextResolve(req);
	return __webpack_require__(id);
}
function webpackContextResolve(req) {
	if(!__webpack_require__.o(map, req)) {
		var e = new Error("Cannot find module '" + req + "'");
		e.code = 'MODULE_NOT_FOUND';
		throw e;
	}
	return map[req];
}
webpackContext.keys = function webpackContextKeys() {
	return Object.keys(map);
};
webpackContext.resolve = webpackContextResolve;
module.exports = webpackContext;
webpackContext.id = 8156;

/***/ }),

/***/ 69321:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var map = {
	"./Blink.stories.js": 88428,
	"./BlockQuote.stories.js": 9184,
	"./Box.stories.js": 36801,
	"./Button.stories.js": 3750,
	"./ByondUi.stories.js": 68696,
	"./Collapsible.stories.js": 88835,
	"./Flex.stories.js": 34458,
	"./Input.stories.js": 23148,
	"./Popper.stories.js": 23613,
	"./ProgressBar.stories.js": 9093,
	"./Stack.stories.js": 86482,
	"./Storage.stories.js": 63900,
	"./Tabs.stories.js": 88209,
	"./Themes.stories.js": 38200,
	"./Tooltip.stories.js": 48181
};


function webpackContext(req) {
	var id = webpackContextResolve(req);
	return __webpack_require__(id);
}
function webpackContextResolve(req) {
	if(!__webpack_require__.o(map, req)) {
		var e = new Error("Cannot find module '" + req + "'");
		e.code = 'MODULE_NOT_FOUND';
		throw e;
	}
	return map[req];
}
webpackContext.keys = function webpackContextKeys() {
	return Object.keys(map);
};
webpackContext.resolve = webpackContextResolve;
module.exports = webpackContext;
webpackContext.id = 69321;

/***/ }),

/***/ 82252:
/***/ (function(module) {

"use strict";


function _createForOfIteratorHelperLoose(o, allowArrayLike) { var it = typeof Symbol !== "undefined" && o[Symbol.iterator] || o["@@iterator"]; if (it) return (it = it.call(o)).next.bind(it); if (Array.isArray(o) || (it = _unsupportedIterableToArray(o)) || allowArrayLike && o && typeof o.length === "number") { if (it) o = it; var i = 0; return function () { if (i >= o.length) return { done: true }; return { done: false, value: o[i++] }; }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }

function _unsupportedIterableToArray(o, minLen) { if (!o) return; if (typeof o === "string") return _arrayLikeToArray(o, minLen); var n = Object.prototype.toString.call(o).slice(8, -1); if (n === "Object" && o.constructor) n = o.constructor.name; if (n === "Map" || n === "Set") return Array.from(o); if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen); }

function _arrayLikeToArray(arr, len) { if (len == null || len > arr.length) len = arr.length; for (var i = 0, arr2 = new Array(len); i < len; i++) { arr2[i] = arr[i]; } return arr2; }

/**
 * @file
 * @copyright 2020 Aleksej Komarov
 * @license MIT
 */
var socket;
var queue = [];
var subscribers = [];

var ensureConnection = function ensureConnection() {
  if (false) { var DEV_SERVER_IP; }
};

if (false) {}

var subscribe = function subscribe(fn) {
  return subscribers.push(fn);
};
/**
 * A json serializer which handles circular references and other junk.
 */


var serializeObject = function serializeObject(obj) {
  var refs = [];

  var primitiveReviver = function primitiveReviver(value) {
    if (typeof value === 'number' && !Number.isFinite(value)) {
      return {
        __number__: String(value)
      };
    }

    if (typeof value === 'undefined') {
      return {
        __undefined__: true
      };
    }

    return value;
  };

  var objectReviver = function objectReviver(key, value) {
    if (typeof value === 'object') {
      if (value === null) {
        return value;
      } // Circular reference


      if (refs.includes(value)) {
        return '[circular ref]';
      }

      refs.push(value); // Error object

      var isError = value instanceof Error || value.code && value.message && value.message.includes('Error');

      if (isError) {
        return {
          __error__: true,
          string: String(value),
          stack: value.stack
        };
      } // Array


      if (Array.isArray(value)) {
        return value.map(primitiveReviver);
      }

      return value;
    }

    return primitiveReviver(value);
  };

  var json = JSON.stringify(obj, objectReviver);
  refs = null;
  return json;
};

var sendMessage = function sendMessage(msg) {
  if (false) { var req, DEV_SERVER_IP, json; }
};

var sendLogEntry = function sendLogEntry(level, ns) {
  if (false) { var _len, args, _key; }
};

var setupHotReloading = function setupHotReloading() {
  if (false) {}
};

module.exports = {
  subscribe: subscribe,
  sendMessage: sendMessage,
  sendLogEntry: sendLogEntry,
  setupHotReloading: setupHotReloading
};

/***/ })

/******/ 	});
/************************************************************************/
/******/ 	// The module cache
/******/ 	var __webpack_module_cache__ = {};
/******/ 	
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		var cachedModule = __webpack_module_cache__[moduleId];
/******/ 		if (cachedModule !== undefined) {
/******/ 			return cachedModule.exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = __webpack_module_cache__[moduleId] = {
/******/ 			// no module.id needed
/******/ 			// no module.loaded needed
/******/ 			exports: {}
/******/ 		};
/******/ 	
/******/ 		// Execute the module function
/******/ 		__webpack_modules__[moduleId](module, module.exports, __webpack_require__);
/******/ 	
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/ 	
/************************************************************************/
/******/ 	/* webpack/runtime/global */
/******/ 	!function() {
/******/ 		__webpack_require__.g = (function() {
/******/ 			if (typeof globalThis === 'object') return globalThis;
/******/ 			try {
/******/ 				return this || new Function('return this')();
/******/ 			} catch (e) {
/******/ 				if (typeof window === 'object') return window;
/******/ 			}
/******/ 		})();
/******/ 	}();
/******/ 	
/******/ 	/* webpack/runtime/hasOwnProperty shorthand */
/******/ 	!function() {
/******/ 		__webpack_require__.o = function(obj, prop) { return Object.prototype.hasOwnProperty.call(obj, prop); }
/******/ 	}();
/******/ 	
/************************************************************************/
var __webpack_exports__ = {};
// This entry need to be wrapped in an IIFE because it need to be in strict mode.
!function() {
"use strict";


__webpack_require__(65379);

__webpack_require__(43674);

__webpack_require__(69656);

__webpack_require__(72411);

__webpack_require__(24467);

__webpack_require__(37493);

__webpack_require__(87420);

__webpack_require__(9480);

__webpack_require__(66171);

__webpack_require__(53276);

__webpack_require__(71852);

__webpack_require__(59045);

__webpack_require__(70887);

__webpack_require__(72906);

__webpack_require__(24835);

__webpack_require__(14653);

__webpack_require__(58139);

__webpack_require__(34049);

__webpack_require__(38356);

__webpack_require__(72494);

__webpack_require__(31699);

__webpack_require__(3263);

__webpack_require__(81520);

__webpack_require__(80036);

__webpack_require__(60515);

__webpack_require__(94330);

__webpack_require__(96553);

__webpack_require__(99139);

__webpack_require__(98215);

__webpack_require__(32617);

__webpack_require__(80186);

__webpack_require__(31149);

__webpack_require__(71827);

__webpack_require__(79770);

__webpack_require__(32099);

__webpack_require__(72667);

__webpack_require__(94920);

__webpack_require__(64683);

__webpack_require__(26189);

__webpack_require__(41185);

__webpack_require__(51008);

__webpack_require__(47112);

__webpack_require__(86000);

__webpack_require__(15196);

__webpack_require__(95993);

__webpack_require__(25079);

__webpack_require__(87143);

__webpack_require__(72499);

__webpack_require__(73676);

__webpack_require__(76746);

__webpack_require__(19);

__webpack_require__(17038);

__webpack_require__(46360);

__webpack_require__(16835);

__webpack_require__(96683);

__webpack_require__(7756);

__webpack_require__(21842);

__webpack_require__(1613);

__webpack_require__(65115);

__webpack_require__(27115);

__webpack_require__(13280);

__webpack_require__(21019);

__webpack_require__(66619);

__webpack_require__(75469);

__webpack_require__(90967);

__webpack_require__(35022);

__webpack_require__(22604);

__webpack_require__(41843);

__webpack_require__(86810);

__webpack_require__(54670);

__webpack_require__(16580);

__webpack_require__(31304);

__webpack_require__(13078);

__webpack_require__(45128);

__webpack_require__(31938);

__webpack_require__(90958);

__webpack_require__(88885);

__webpack_require__(45017);

__webpack_require__(59743);

__webpack_require__(47560);

__webpack_require__(31569);

__webpack_require__(6134);

__webpack_require__(12622);

__webpack_require__(60852);

__webpack_require__(99085);

__webpack_require__(75180);

__webpack_require__(79437);

__webpack_require__(75370);

__webpack_require__(61022);

__webpack_require__(50580);

__webpack_require__(5301);

__webpack_require__(76438);

__webpack_require__(97307);

__webpack_require__(34016);

__webpack_require__(86944);

__webpack_require__(60687);

__webpack_require__(86957);

__webpack_require__(59163);

__webpack_require__(34128);

__webpack_require__(24267);

__webpack_require__(39816);

__webpack_require__(4239);

__webpack_require__(8381);

__webpack_require__(63669);

__webpack_require__(25779);

__webpack_require__(84580);

__webpack_require__(33198);

__webpack_require__(71895);

__webpack_require__(44910);

__webpack_require__(95962);

__webpack_require__(73769);

__webpack_require__(14962);

__webpack_require__(80345);

__webpack_require__(18438);

__webpack_require__(70552);

__webpack_require__(46356);

__webpack_require__(57128);

__webpack_require__(83588);

__webpack_require__(47057);

__webpack_require__(67092);

__webpack_require__(59478);

__webpack_require__(69203);

__webpack_require__(27879);

__webpack_require__(71507);

__webpack_require__(39021);

__webpack_require__(8641);

__webpack_require__(83064);

__webpack_require__(93115);

__webpack_require__(33362);

__webpack_require__(45090);

__webpack_require__(12654);

__webpack_require__(58870);

__webpack_require__(76581);

__webpack_require__(4567);

__webpack_require__(28638);

__webpack_require__(60314);

__webpack_require__(71595);

__webpack_require__(98954);

__webpack_require__(91332);

__webpack_require__(79445);

__webpack_require__(36493);

__webpack_require__(24476);

__webpack_require__(78192);

__webpack_require__(58660);

__webpack_require__(56592);

__webpack_require__(69957);

__webpack_require__(9747);

__webpack_require__(13578);

__webpack_require__(3684);

__webpack_require__(19209);

__webpack_require__(56737);

__webpack_require__(67586);

__webpack_require__(7743);

__webpack_require__(15890);

__webpack_require__(34651);

__webpack_require__(18037);

__webpack_require__(48415);

__webpack_require__(54950);

__webpack_require__(76102);

__webpack_require__(47816);

__webpack_require__(30037);

__webpack_require__(54705);

__webpack_require__(75831);

__webpack_require__(96958);

__webpack_require__(75344);

__webpack_require__(87853);

__webpack_require__(25865);

__webpack_require__(1150);

__webpack_require__(50444);

__webpack_require__(11220);

__webpack_require__(6636);

__webpack_require__(37650);

__webpack_require__(12424);

__webpack_require__(64346);

__webpack_require__(27553);

__webpack_require__(49007);

__webpack_require__(17621);

__webpack_require__(71078);

__webpack_require__(20500);

__webpack_require__(85923);

__webpack_require__(38956);

__webpack_require__(49264);

__webpack_require__(70448);

__webpack_require__(5775);

__webpack_require__(45164);

__webpack_require__(83762);

__webpack_require__(67265);

__webpack_require__(56058);

__webpack_require__(87001);

__webpack_require__(23535);

__webpack_require__(18617);

__webpack_require__(70595);

__webpack_require__(76820);

__webpack_require__(33357);

__webpack_require__(38215);

__webpack_require__(37281);

__webpack_require__(99584);

__webpack_require__(43582);

__webpack_require__(15378);

__webpack_require__(31213);

__webpack_require__(19275);

__webpack_require__(59);

__webpack_require__(43849);

__webpack_require__(69838);

__webpack_require__(25513);

__webpack_require__(72090);

__webpack_require__(15511);

__webpack_require__(88280);

__webpack_require__(37283);

__webpack_require__(96250);
}();
// This entry need to be wrapped in an IIFE because it need to be in strict mode.
!function() {
"use strict";


var _inferno = __webpack_require__(96323);

__webpack_require__(33603);

__webpack_require__(57397);

__webpack_require__(62048);

__webpack_require__(39248);

__webpack_require__(36030);

__webpack_require__(32817);

__webpack_require__(44576);

__webpack_require__(21805);

__webpack_require__(39292);

__webpack_require__(74199);

__webpack_require__(65138);

var _perf = __webpack_require__(28440);

var _client = __webpack_require__(82252);

var _hotkeys = __webpack_require__(6965);

var _links = __webpack_require__(13952);

var _renderer = __webpack_require__(77960);

var _store = __webpack_require__(18749);

var _events = __webpack_require__(99397);

var _window$performance, _window$performance$t;

_perf.perf.mark('inception', (_window$performance = window.performance) == null ? void 0 : (_window$performance$t = _window$performance.timing) == null ? void 0 : _window$performance$t.navigationStart);

_perf.perf.mark('init');

var store = (0, _store.configureStore)();
var renderApp = (0, _renderer.createRenderer)(function () {
  var _require = __webpack_require__(91199),
      getRoutedComponent = _require.getRoutedComponent;

  var Component = getRoutedComponent(store);
  return (0, _inferno.createComponentVNode)(2, _store.StoreProvider, {
    "store": store,
    children: (0, _inferno.createComponentVNode)(2, Component)
  });
});

var setupApp = function setupApp() {
  // Delay setup
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', setupApp);
    return;
  }

  (0, _events.setupGlobalEvents)();
  (0, _hotkeys.setupHotKeys)();
  (0, _links.captureExternalLinks)(); // Subscribe for state updates

  store.subscribe(renderApp); // Dispatch incoming messages

  window.update = function (msg) {
    return store.dispatch(Byond.parseJson(msg));
  }; // Process the early update queue


  while (true) {
    var msg = window.__updateQueue__.shift();

    if (!msg) {
      break;
    }

    window.update(msg);
  } // Enable hot module reloading


  if (false) {}
};

setupApp();
}();
/******/ })()
;